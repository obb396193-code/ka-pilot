.DS_Store
.env
.env.*
!.env.example
**/node_modules/
**/.vitest/
**/coverage/
**/dist/

# 公司真实数据默认不入库，需要时白名单单独放行

# 内部原始知识资料：允许保留业务敏感内容，但禁止进入 Git

# 本次内部资料下载入口文件；canonical 只保留在上方私有区
/ka-platform-docs-v2.zip
/INDEX.md

# linked worktrees kept inside the repo dir
.worktrees/

# 老板 2026-09-07：这个私有仓就是我们自己的备份，内部资料一律进仓（原始知识源、xlsx/csv 都要）。
# 仍然不进仓的只有：真凭证（.env）、依赖与构建产物、本机工作树。
