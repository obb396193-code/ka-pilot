# 投放 Agent 线 · 工作方法 v0.1

> 建立日期：2026-08-13
> 状态：草案，边做边改。改动记进 `docs/plans/工作台账.md`。

## 0. 这条线是什么

老板（淘宝 App 信息流运营）本职工作方向的产品线。**与 ContentRadar（staradar.xyz）、自媒体/视频线完全独立**：不共用仓库、不共用契约、不互相引用结论。

产出目录唯一：`~/Desktop/投放agent/`。这条线的任何文档、代码、图、数据样本都落这里，不散在桌面。

## 1. 留痕铁律（本线第一条规则）

**每做一步都要留下记录。** 记录不是事后补的总结，是当场写的账。

| 要记的东西 | 落在哪 | 什么时候写 |
|---|---|---|
| 需求、目标、愿景 | `docs/00-目标与愿景.md` | 老板口述后立刻整理进去 |
| 每次聊定的事、每个动作 | `docs/plans/工作台账.md` | 聊定一条记一条，不攒 |
| 有取舍的决定（选了A没选B） | `docs/decisions/YYYY-MM-DD-<主题>.md` | 拍板当场，含"没选的方案和为什么" |
| 跨 agent 派活与回执 | `docs/relay/inbox-<角色>.md` | 派活当场 |

原则：
- **台账是唯一事实源**。会话被 compact 或重开，先读台账再干活，杜绝重复做已完成项。
- 文档写现状与依据，不写画大饼的蓝图；"目标"和"现状"必须分栏标清。
- 记忆/文档都是**快照不是现状**，报状态前对实际文件/线上核实。

## 2. 沟通与决策

- 高密度、结论先行，不铺垫、不写总结段。
- 术语保留原词 + 括号一句大白话。
- 重大决策老板拍板：给方案、列对比、说利弊，不替他选。选择题想问就问，多问不扣分。
- 需求表达不清时先一两句复述重组（"你要的是：…"）再推进，不卡着等确认。
- 老板说"收一下"→ 立刻停。

## 3. 多 Agent 预留

现在是单会话（我 + 老板）。将来会加实现方/审查方等角色，机制先立好，不等到乱了再补：

- 跨角色只走 `docs/relay/` 信箱，规则见 `docs/relay/README.md`。不生成"发给 XX 的话术"让老板手动搬运。
- 每个角色**只动自己领域的文件**，边界写在信箱 README 里。
- 派子 agent 干活**不默认相信其汇报**：机械大改自己动手或逐点核实；子 agent 只准返回文本，不准动共享文件。
- 收到交付（SHA/报告）后逐条审计，✅/❌ 列清。漏项是最大风险。

## 4. 红线（本线特有）

- **公司数据脱敏**：真实账户 ID、账户昵称、消耗金额、考核价、内部人名，不进任何将来可能公开或外发的文件。需要样本时用脱敏假数据，并在文件头标明。
- **写操作必确认**：任何对媒体投放平台的写动作（改预算/改出价/下线/改时段）一律先出预览，老板确认后才执行。诊断阶段只读。
- **不发明数据**：账户 ID、考核价、后台转化、Gap 一律不猜。缺源就标"待补数据源"。
- **不越界到别的线**：不因为这条线的需要去改 ContentRadar 或视频线的任何文件。

## 5. Git 用法

本仓独立、无并行 CLI，可正常 `git add` + `commit`。**不 push**（暂无远端，且是公司相关内容，推之前必须老板拍板）。

- commit 信息中文、说清动了什么。
- 提交后 `git show --stat HEAD` 自查。
- `.gitignore` 至少挡 `.DS_Store`、`.env`、真实数据表（`*.xlsx` 默认忽略，需要时白名单）。

## 6. 开工令（新会话第一件事）

1. 读本文件（工作方法/红线）。
2. 读 `docs/00-目标与愿景.md`（我们要做什么）。
3. 读 `docs/plans/工作台账.md`（进行到哪了，别重做）。
4. 有信箱条目待处理就按编号处理并更新状态。
