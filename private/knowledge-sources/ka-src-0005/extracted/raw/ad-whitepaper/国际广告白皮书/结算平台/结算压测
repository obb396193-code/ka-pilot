## 点击服务器压测

### 涉及的应用
* 点击服务器（直接相关，看机器容量）
* 风控系统-MTEE（直接相关，需要同时观测机器指标）
* DA（评估压测数据是否会有影响）
* 反作弊（评估压测数据是否会有影响）

### 压测方案
* 请求的ip使用内网ip，保证风控不能通过
* 请求的点击地址可以是同一个
* 请求的cookie最好为空

### 观测指标
* 机器性能指标
* 风控超时率
* 哨兵限流指标（如果上述两个指标正常，可以扩大限流量）
* 风控通过的点击指标

### 应急方案
* 风控通过的点击指标开始下降，马上停止压测


## 实时结算压测

### 涉及到的应用
* 结算系统

### 压测方案
* 结算改造应用，升级中间件，兼容测试
* 结算构造影子表，用来存储压测的点击
* 测试端大量构造压测的点击，并写入metaq
* 结算获取metaq中的点击，解析入库，观测系统指标

### 观测指标
* 机器指标
* 数据库更新性能指标
* 数据库异常

### 应急方案
数据库更新失败量增多后立即停止压测

## 结算实时余额接口性能压测

### 涉及到的应用
* 账户

### 压测方案
* 大量调用实时余额的HSF接口
* 避开整点的全量上下线

### 观测指标
* 机器指标
* 实时余额接口的性能
* 账户余额接口的性能
* 结算数据库的连接
* 账户数据库的连接
* 超时率

### 应急方案
查询实时余额超时严重时停止压测
