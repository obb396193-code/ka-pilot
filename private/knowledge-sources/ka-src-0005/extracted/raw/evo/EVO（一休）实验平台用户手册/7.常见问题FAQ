# 1.实验不生效，用户为什么无法命中实验？
可能的原因如下：

| 序号 | **原因** | 详细解释 | 提示 |
| :--- | --- | --- | --- |
| 1 | 实验状态为下线（归档） | 该实验已关闭，任何用户都无法命中实验。 | 服务端实验会提示"experiment not exists" |
| 2 | 实验发布后，生效有一定的延时 | 服务端实验生效在2分钟之内<br/>客户端实验生效在8分钟之内 | 服务端实验会提示"experiment not exists" |
| 3 | 是否初始化分流sdk，配置中的appcode是否填写正确 | 没初始化或者初始化配置的appcode跟该实验场景中的选择应用是否一致 | 服务端实验会提示"experiment not exists" |
| 4 | 不在实验生效时间 | 实验时间未到或者过期，任何用户都无法命中实验。 | 服务端实验会提示"hit experiment failure" |
| 5 | 不属于流量区间 | 该用户分流结果不属于该实验的流量区间，当前实验分配总流量为<实验赋予流量百分比>，该用户属于剩余流量空间。 | 服务端实验会提示"hit experiment failure" |
| 6 | 实验条件不符合 | 该用户不满足受众过滤条件，无法命中。 | 服务端实验会提示"hit experiment failure" |




# 2.实验发布了，没有实验数据
可能的原因如下：

  1.埋点测试验证一下，相应页面上的ut埋点有没有带上ab标，具体检查args中的utparam-cnt中是否有自己的实验分桶标，埋点测试平台：[埋点治理平台](https://usertrack.alibaba-inc.com/validate/qrcode)

![](https://intranetproxy.alipay.com/skylark/lark/0/2019/png/18513/1565700499462-27bf87f6-a54f-404e-9365-567c4194c5e4.png)

2.如果相应页面上的ut埋点没有带上ab标，确认下控制台的实验配置中的pagename是否已经填写，如果未填写可能是分流有效，埋点无效。

 3.如果确认这个实验配置中的pagename已经填写，埋点测试还没数据，那么接着往下走，如果这个实验是服务端分流实验，需要一休提供报表能力，对应的app端需要调用接口激活服务端实验方案，[接入文档](https://yuque.antfin-inc.com/ut/inxe77/quewru#x6ulwp)

4.确认第三步都完成，还没解决，查看是否开启UT的ut-plugin开关，主要联系破风或芮奇确认。



# 3.实验分流不均
如果是服务端分流实验，可能的原因如下：



        1.确认下一休控制台中相应实验详情中的实验数据页面有没有数据。如果有数据，看下这里的数据分流是否均匀。一般有数据的情况下，分桶都是均匀的。除非遇到实验的前置过滤干扰（对实验某几个桶的过滤）。

举个lazada首页的例子：<font style="color:#111F2C;">由于新人卡业务逻辑对不同分桶人群的差异处理（已经命中 5 桶的用户会持续调用分桶接口并记录日志，已经命中 1-4 桶的用户在满足特定业务条件后，不会调用分桶接口即不会有日志记录），拦截了 1-4 桶用户，导致区域时间内的 5 桶的 uv 会明显比 1-4 桶多，而因为 1-4 桶多拦截方式相同，因此 1-4 桶 uv 均匀。</font>



        2.如果是自己埋服务端实验分流数据的，统计这些分流发现分流不均。解决方案：先看下统计数据逻辑中有没有把实验的发布id也算上，因为遇到实验流量重新调整发布的情况，用户可能会串桶，此时如果单按分桶来算就会有统计分流不均的问题。另外仔细排查下，分流接口调用后到自己埋点的代码逻辑中是否有漏洞。





       3.确认下服务端分流接口中用的分流id（bid）跟你们统计最后实验数据的用户标识是否相同，比如说分流id用了utdid，实验数据统计用的是userid，这样统计后的数据分流肯定不均匀。



      4.确认下服务端分流是否是用户id分流的，并且未登录情况下uid是否传了0或者传了utdid，这样我们的数据结果会有分流不均匀问题。



<font style="color:#F5222D;">建议：业务方做实验，</font><font style="color:#F5222D;">强烈接上我们的埋点能力，</font><font style="color:#F5222D;">服务端分流sdk现在提供了好几套埋点链路来支持业务方，以便业务方方便查看</font><font style="color:#F5222D;">实验</font><font style="color:#F5222D;">数据及定位一些数据问题。</font>

<font style="color:#F5222D;"></font>

# 4.白名单不生效
1. 请确认实验是否已发布或beta发布，如果是beta发布，只能对beta发布时选择的测试账号起作用。
2. 白名单变更后，服务端分流实验有1分钟延迟，客户端分流实验一般两三分钟，依赖客户端冷热启动及orange。
3. 客户端实验可扫描白名单右侧“开始测试”二维码，查看日志自行排查原因。



# 5.beta发布后，实验不生效
1. 只会对beta发布时选择的测试账号去走分流命中逻辑，至于是否能命中，还要看实验流量及条件。
2. 另外排查见[第一点](#uy6bmv)



# 6.实验配置流量修改对分桶结果的影响？
平台采用一套科学的流量分配，最大力度保障改动流量对用户的影响最小。

举例：

<font style="color:#191F25;">假设一个普通实验有两个桶（A桶流量5%，B桶流量5%），简单的讲这个实验总共流量100，分流计算时，［0,4]给A桶，［5,9］给B桶。</font>

### <font style="color:#191F25;">情况1:缩小流量，调整分桶流量（A桶流量4%，B桶流量4%）</font>
<font style="color:#191F25;">分流计算时，［0,3]给A桶，［5,8］给B桶，这个时候A桶</font><font style="color:#191F25;">B桶</font><font style="color:#191F25;">部分老用户都会有影响，但是会尽量保障流量的分配科学；</font>

### <font style="color:#191F25;">情况2:放大流量，调整分桶流量（A桶流量10%，B桶流量10%）</font>
<font style="color:#191F25;">分流计算时，［0,4][10,14]给A桶，［5,9］[15,19]给B桶，这个时候A桶老用户不会变，B桶老用户也不会变；</font>



# 7.配置了关联实验，发现两个实验结果不一致？
| 序号 | **原因** | 详细解释 |
| :--- | --- | --- |
| 1 | 实验是否都用了abV3接口 | 老接口不支持关联实验功能 |
| 2 | 实验配置要修改，是否对这些关联实验都重新发布了？ | 目前实验配置修改，都需要重新发布，不然新的关联实验配置不会生效。 |




# 8.业务应用初始化分流sdk时，用了其他应用名，怎么订正？
（1）先找到目前该应用下在调用的在线实验有哪些，然后在这些实验所属的场景中添加上真实应用名。

（2）然后重新发布下实验。



