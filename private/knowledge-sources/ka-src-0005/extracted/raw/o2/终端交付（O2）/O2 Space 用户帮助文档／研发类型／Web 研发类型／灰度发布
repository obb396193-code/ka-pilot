灰度发布功能提供的是一种放量发布机制。执行线上发布时，先让内网及少量外网用户访问新页面。后续可以观察业务数据并调整页面灰度的比例，确认无问题后再全量部署。

> 如遇到项目没有灰度流程且需要开启灰度 请联系 [研发小蜜](https://links.alipay.com/app/room/5fbf53f6cd068c04d342e682/start-conversation-form/)

灰度发布有标准灰度（默认，比例为 5%/25%/50%）和细粒度灰度（比例为 0.1%/0.5%/1%/5%/25%/50%）两种模式。

## 如何使用

迭代线上发布后，进入迭代灰度阶段（迭代未完成）。在这里会展示灰度中页面列表和操作控制，完成/取消灰度后，迭代完成，可以在已完成的迭代里看到当时灰度的页面。

(https://o2-img.alistatic.com/i/i09blZ/ee22yvpRnlB8KvARN99Sh-d4c344e464.png)


### 灰度比例说明

在所使用的域名支持灰度及对应模式的前提下，灰度比例与访问能力对应为：

| 比例 | 内部用户 | 外部用户 | 强制灰度 |
|-|-|-|-|
| 内网灰度 | 全量 | ❌ | ❌ |
| 0% | 全量 | ❌ | ✅ |
| 0.1%仅限细粒度灰度 | 全量 | 0.1% | ✅ |
| 0.5%仅限细粒度灰度 | 全量 | 0.5% | ✅ |
| 1%仅限细粒度灰度 | 全量 | 1% | ✅ |
| 5% | 全量 | 5% | ✅ |
| 25% | 全量 | 25% | ✅ |
| 50% | 全量 | 50% | ✅ |

- 灰度比例逐档提升放量，整体放量时间建议 >30min；（紧急发布，可自行控制灰度时长，根据O2提供的监控数据进行放量节奏控制）
- 若页面回归没有问题，可点击 正式发布，新版页面比例将调整为 100%
- 若页面有问题，不能发布，点击 取消灰度，线上页面将 100% 回到灰度前的版本
- 正式发布 和 取消灰度 都表示灰度已完成，不能再调整为按比例发布。
> 注意所有的灰度操作都是处于迭代完成后的阶段，灰度操作只影响页面展示比例，对流程或代码本身无影响。无论页面取消灰度还是全量发布，代码都已经在灰度操作前合并到了 master

### 细粒度灰度

对于超大流量业务，我们提供了细粒度灰度能力，在标准灰度比例的基础上增加了 1‰、5 ‰、1% 阶段：

(https://o2-img.alistatic.com/i/i09blZ/zJLbXlJOFEb3XDNnKgQQU-6e361bd266.png)


为保证灰度有效性、灰度阶段中有足够的用户请求命中灰度，细粒度灰度能力仅对以下应用开放：

1. 跨端应用（Weex/WebApp 应用不支持细粒度灰度）
1. 平均日 PV 百万以上；
1. 历史迭代中，用户可按照预期比例命中灰度（如下图）(https://o2-img.alistatic.com/i/i09blZ/YFZ6l2svDgWjKBBdYsm6z-61cac73274.png)

如您的应用可以满足以上要求，可以通过答疑申请开启细粒度灰度。注意事项如下：

1. 细粒度灰度需要域名配置支持，请您提前与应用所属站点负责人确认是否已执行过升级，升级需要约 1 个工作日，大促等封网期间时间顺延；
1. 申请开启细粒度灰度期间，请确保应用未处于且不会进入灰度状态；在灰度过程中切换灰度类型可能导致页面异常；
1. 应用开启细粒度灰度后，只能在支持细粒度灰度的域名上进行灰度，因此：
	1. 如果站点下存在多个域名且部分域名未支持细粒度灰度，则在这些域名上所有用户都无法命中灰度；
	1. 开启细粒度灰度的应用不建议切换站点，切换后可能因为新站点的域名不支持细粒度灰度导致无法命中灰度；
## 支持内网灰度的域名

(https://o2-img.alistatic.com/i/i09blZ/RQgXQ3FUxBbL4EHzZOTYQ-535c091f50.png)


如果灰度阶段中存在内网灰度阶段，则表明当前应用支持内网灰度。由于目前方案与线上存在性能差异，内网灰度申请审批接入，需求方可联系研发小蜜咨询。一部分手机客户端使用的是 AMDC 方式解析域名，这种场景下，内灰功能覆盖的域名有：

```
market.m.taobao.com
pages.tmall.com
huodong.taobao.com
1111.taobao.com
1111.tmall.com
1212.taobao.com
1212.tmall.com

```
如须确认其它域名是否已经在 AMDC 场景下支持灰度，请与我们联系 。

如在其他域名上有相关验证需求，建议将比例放至 0% 后参考下方强制命中灰度进行验证。

## 如何强制 命中/不命中 灰度页面

- 下列方法仅用于外网验证，请勿投放至终端用户！！！
- 下列方法仅用于外网验证，请勿投放至终端用户！！！
- 下列方法仅用于外网验证，请勿投放至终端用户！！！
### 通过 Cookie 设置灰度

预发及内网劫持环境提供了灰度打标页面，地址如下：

```
https://{domain}/.well-known/grey-flag

```
灰度打标对整个二级域名的所有子域名生效（当域名倒数第二部分为 com/org/net/ac/co/edu 时，对三级域名的所有子域名生效）。例如 taobao.com 的子域名，可以使用以下地址：

[https://market.wapa.taobao.com/.well-known/grey-flag](https://market.wapa.taobao.com/.well-known/grey-flag)

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ma8rez/img2uRUfhkGx2.png)


### 通过 Header 强制灰度

允许强制灰度时，在外网环境下设置如下 Header 可强制命中灰度页面

```
x-air-grey: true

```
设置如下 Header 可强制不命中灰度页面

```
x-air-grey: false

```
#### 浏览器测试方法

可以在浏览器中安装修改 Header 的插件实现强制命中灰度。以 Chrome 下的 [ModHeader](https://chrome.google.com/webstore/detail/modheader/idgpnmonknjnojddfkpgkljpfnnfcklj) 为例，参照如下设置：
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ma8rez/imgp6GeDoo1UF.png)


#### 客户端测试方法

对于集成了 Air 灰度功能的客户端，可以通过启用相应开关的方式强制命中灰度。以手淘 Debug 包为例：
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ma8rez/imgzWutbOT2pq.png)


## 灰度命中策略

灰度基于请求中提供的设备 ID、账号 ID、Aplus 脚本埋点值来计算灰度分桶值，具有如下特征：

1. 灰度命中用户策略是固定的，灰度期间：命中的用户会一直命中，反之亦然；
1. 在低比例中命中灰度的用户，也会命中高比例灰度；
1. 灰度比例设置值仅用于分桶计算使用的，不代表线上实际灰度比例。实际灰度比例请参考根据 CDN 数据计算的实时灰度比例。
## 注意事项

- 首次上线的页面不会进入灰度流程而直接正式发布
- 页面处于灰度状态时，内网用户会强制命中灰度。如需回滚内网版本，必须取消灰度，重新切分支发布。
- 应用有页面处于灰度发布时，其他迭代都不允许执行线上发布操作，若需发布需要先将灰度完成。
- 灰度发布专注的是发布过程提供按比例发布的功能，使页面在全量发布前提供一个回归的渠道，所以整个灰度过程不应持续太久。不要将灰度发布当成 A/B test 使用。
## FAQ

### 为什么实际灰度比例与设定不一致

在现有客户端实现下，灰度分桶大部分基于账号 ID 计算。如果页面本身访问量较少、访问用户固定，可能出现灰度比例不符合预期的情况。实际灰度比例请以实时数据显示为准。

### 我设置的是内灰 / 0%，为什么在 4G / 5G / Guest 网络访问到了灰度内容

内网灰度通过劫持办公网 DNS 解析到 SPE 接入层实现，因此：

- Guest 网络目前使用的是办公网 DNS，同样纳入内网灰度范围；
- 浏览器 / 端侧解析存在缓存，因此如果设备接入过办公网 / Guest 网络，设备可能留有解析缓存并继续访问 SPE 接入层。请尝试开关飞行模式刷新缓存后再试。
