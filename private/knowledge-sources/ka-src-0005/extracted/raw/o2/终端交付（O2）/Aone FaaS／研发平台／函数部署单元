部署单元

和传统应用一样，函数也是分机房部署，目前弹内仅支持 张北、南通、深圳云机房部署，海外近期计划支持新加坡

# 确认部署单元

- 如果是内网应用，一般只需要部署中心单元（中心即张北）
- 如果有对外流量，则需要根据上下游服务确认函数的的部署单元（比如你的上游服务部署了中心和上海，则你的函数服务最好也部署中心和南通，如果只部署了中心则会出现跨单元调用情况影响服务调用时长），并且部门之间一般有比较固定的部署单元要求，可以跟部门的 PE 或租户管理员确认。（比如淘系C端业务一般需要部署中心和南通、高德C端业务一般需要部署中心、南通、深圳）
# 设置部署单元

设置 > 函数配置 > 应用部署配置

每个租户的管理员都会提前根据租户部署单元需求，提供几套可选的部署模板给租户下的用户使用，用户 只需在此界面选择对应的部署模板即可，选好后更新配置，下次线上发布时，函数就会被部署到模版指定的单元。

# 









