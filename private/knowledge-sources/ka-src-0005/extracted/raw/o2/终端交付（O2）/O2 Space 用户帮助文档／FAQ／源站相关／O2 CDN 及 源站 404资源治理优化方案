# 一、各层面影响分析

## 1.1 CDN层面

- 核心影响：
	- 404请求与200请求消耗同等CDN计算资源
	- 前端重试逻辑会导致请求量倍增（如：1次访问→2-3次重试）
	- 大规模404请求会：▸ 挤占边缘节点带宽资源▸ 延迟正常请求处理▸ 影响实时监控数据上报时效性（延迟可达5-10分钟）
- 针对性措施：
	- 边缘节点实现高频率404请求强长时长缓存
	- 对404请求特征header识别并边缘拦截
	- 对可识别攻击源站及域名聚合的404请求进行IP封禁
## 1.2 源站层面

- 核心影响：
	- 当前CDN缓存策略：404响应，Web类域名30s，Assets类域名1s
	- 两种攻击场景：
| 场景类型 | 特征 | 源站压力 | 典型案例 |
|-|-|-|-|
| 固定URL | 如`/dead-link.html` | 低（N*节点数） | 旧书签访问；业务误投；部分无效兜底请求 |
| 动态URL | 含`?rand=xxxx`或 乱码 | 极高（QPS直接透传） | 恶意扫描攻击 |

- 高危攻击场景：同一时刻动态URL高流量攻击可导致源站限流，所承担回源流量的AServer限流，甚至直接击溃源站服务，击溃回源流量所在AServer
- 针对性措施：
	- 实施参数规范化，源站及域名CDN侧进行限制
	- 对动态URL 404请求启用10分钟长缓存、302跳转或发布兜底资源。（能锁定业务方的需业务评估后执行，应急场景除外，应急等级划分见下文）
	- 部署源站熔断机制（当404回源请求量>应急阈值时返回兜底静态页）
# 1.3 文件存储（OSS层面）

- 核心影响：
	- 当前CDN缓存策略：Assets 404响应仅缓存1s
	- 高风险场景：▸ OSS故障时：`g.alicdn.com`回源全面不可用（因缓存几乎不生效）▸ 攻击场景：20万QPS攻击 → OSS直接承受≈ 15万QPS
			- 若攻击流量为combo场景，则20万QPS则成倍转换为OSS访问，远超15万QPS
			- 核心OSS 15万QPS，非核心超1万 QPS，接近上述量级开始出现限流或5xx错误
1. 针对性措施：
	- 将Assets 404强行增加缓存时间（仅应急场景处置，平台侧进行公告，并同步GOC进行各方通知）
	- 实施OSS或源站切换（当某区域OSS故障时）
# 二、404治理措施

## 2.1 应急等级（2025.03版）

- 可识别归属方404请求
	- 四级治理阈值，根据量级逐级上升。
		- 一级（预警）：CDN 404 > 50万/小时 或 源站 404 QPS > 50 或 OSS 404 QPS > 50 
			- 平台侧开始预警，由业务方跟进，7天内完成治理即可，平台侧不强制处置，需业务确认。
		- 二级（告警）：CDN 404 > 200万/小时 或 源站 404 QPS > 200 或 OSS 404 QPS > 200
			- 平台侧提开始告警，由业务方跟进，3天内完成治理即可，平台侧不强制处置，需业务确认。
		- 三级（紧急）：CDN 404 > 500万/小时 或 源站 404 QPS > 500 或 OSS 404 QPS > 500
			- 平台侧提开始紧急告警，平台方与业务方应急方拉群跟进，需要立即进行问题定位与修复，平台侧不强制处置，需业务确认。
		- 四级（应急）：CDN 404 > 1000万/小时 或 源站 404 QPS > 1000 或 OSS 404 QPS > 1000
			- 平台侧进入故障应急，若评估存在源站故障风险或已经故障场景，平台侧则进行应急处置，优先规避故障。若评估无故障风险，则按三级（紧急）原则进行治理。平台侧存在强制处置可能，视紧急情况而定，但保障应急后通知到业务方接口人。
- 无法识别归属方404请求
	- 无归属404请求定义：（域名无归属人或公共域名）且 （无O2应用）且 （无Code仓库）且 （refer非集团域名，以是否注册在IDNS为准）
	- 无主404请求处置：
		- O2平台侧公告处置资源明细、变更时间，变更操作，ChangeFree等信息，置顶7天以上
		- 处置流程
			- 优先进行预发域名处置，如dev.g.alicdn.com，维持1天
			- 线上分批404资源进行灰度。
		- 变更信息同步：
## 2.2 平台侧提供能力

### 2.2.1 监控告警

可提供业务方定制监控、分析、告警能力，可参看：[https://help.o2.alibaba-inc.com/docs/206](https://help.o2.alibaba-inc.com/docs/206)；相关部分见下文摘要：

进入域名监控页面时，默认会选中【域名】维度与【边缘请求数】指标。您可以根据情况删除或添加其他维度与指标。

部分维度（如：状态码）为专有维度，具有特定的分类规则和指标关联。专有维度不支持与其他维度组合，也不支持选择指标。选择专有维度时，会自动取消其他维度和指标的选择。

(https://o2-img.alistatic.com/i/i09blZ/JRcdWrut7dEQIkl4QxNqD-e294b17ee3.png)


#### 数据下钻

部分指标支持下钻能力。当指标出现异常上涨、下跌时，可以通过不同维度分析变化原因。支持下钻的图表会显示如下图中的提示。此时，可以在图表中找到出现变化的点，当指针变为手型时点击：

(https://o2-img.alistatic.com/i/i09blZ/PRIs55KUjf25eczXX7WoQ-7c3f23ba3e.png)


在弹出的浮层中，选择【对照时间】和【下钻方向】后，点击【开始分析】：

(https://o2-img.alistatic.com/i/i09blZ/FUhOJewskJPzJvygLqKyf-51972fd51c.png)


系统会根据参数查询相关数据并展示，其中“当前值”代表分析时间（即点击的图表值对应的时间）某一维度的数据值；如果选择了对照时间，相应时间的数据值展示为“对比值”，同时会根据当前维度的变化趋势和整体变化趋势计算“贡献度”。贡献度越高，则当前项目越有可能导致整体指标的变化。

(https://o2-img.alistatic.com/i/i09blZ/49T8VsbFWcwRuRJa00rgv-96516a409b.png)


### 2.2.2 异常请求ODPS离线数据表

平台侧可提供业务方根据404或异常请求特征进行数据详情离线表，若需要定制离线表，可走研发小蜜或联系冠夫进行咨询。

(https://o2-img.alistatic.com/i/i09blZ/xxaH0NADGYuc8SyUQkDzh-cc48177154.png)


### 2.2.3 可定制策略

仅针对业务自定义接入域名，可对404请求进行定制处置

- 重定向方案
	- 业务方需提供重定向地址，平台侧支持进行映射
- 自定义404页面
	- 业务方可在站点平台进行操作
	- (https://o2-img.alistatic.com/i/i09blZ/iZzACOcqm9spJ3KgIWSkn-e074bded39.png)

## 三、最佳实践

淘天侧404治理，大致流程如下，支持非淘天方告警订阅。

### 3.1 治理措施

(https://o2-img.alistatic.com/i/i09blZ/x9GFoCiPzCr7C5WlZczLl-d8c02753b2.png)


### 3.2 整体数据

(https://o2-img.alistatic.com/i/i09blZ/z2mfLed8aabr3SX0PQilw-4bde7b48bb.png)


### 3.2 告警列表

(https://o2-img.alistatic.com/i/i09blZ/oMwQQ7KWOkr9buN0TDE3A-8cff0b948e.png)


### 3.3 告警订阅

(https://o2-img.alistatic.com/i/i09blZ/dhfHs1vPtEDpJOwyJ6WpF-fc5a4a448e.png)


# 其他

[O2研发平台CDN相关问题的应急排查SOP](https://help.o2.alibaba-inc.com/docs/445)



