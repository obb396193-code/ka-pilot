## 更换站点

如果应用有更换站点的需求，可以通过“应用-设置-发布设置-域名设置-更换站点“来进行操作。

注意：

- 更换站点相当于重新接入的流程，同样遵循应用接入站点的审批规则（站点负责人审批，站点成员免审批），提交接入申请后请及时跟进审批单，避免影响发布。
- 更换站点需要走重新接入流程，更换前需要确保应用没有在灰度中、进行中的
- 站点更换后需要重新进行一次发布，新域名上的内容才会生效，已有域名下的内容不受影响，仍然可以访问，但是不会再进行更新。
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/sskdn8028vxxka58/imgg9e5Hu3YmN.png)


## 更新域名

如果站点下接入了新域名，但是应用的部署结果中还是展示旧域名，可以通过“应用详情-设置-发布设置-域名设置”来进行更新。

注意：

- 应用的域名更新后需要重新进行发布，部署结果中才会使用新设置的域名。已经发布过的结果仍然可以访问，不受影响。
- 此处设置的域名是“主投放域名”（发布结果展示时主要使用的域名），站点下如果接入了多个域名，则站点下的域名都是可以访问到应用发布的资源的（只是在发布结果中不展示其他域名）。
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/sskdn8028vxxka58/imgYhP1n6EvWd.png)


