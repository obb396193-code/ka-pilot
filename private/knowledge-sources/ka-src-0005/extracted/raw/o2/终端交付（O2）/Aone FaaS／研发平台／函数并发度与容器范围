### 为什么要关注并发与弹性？

相比较于传统应用，函数容器的价值之一在于按需使用资源和自动弹性伸缩：无需申请固定机器，且能够在流量突增时自动扩容。但这两个特性如同双刃剑——容器实例过多会导致空闲资源产生额外费用、实例不足则会引发请求排队甚至大量超时。下文将从概念入手，让您配置正确的容器参数。

(https://o2-img.alistatic.com/i/i09blZ/vUqx6rxWJH6fcnbabb7Ov-8853c5bbfe.png)


### 基础概念：

QPS（每秒请求量）：每秒处理的请求次数。例如：1秒内处理50次请求，也就是 QPS=50

RT（响应时间）：单个请求从发起到返回结果的时间。例如：请求平均耗时0.5秒，也就是 RT=0.5秒

并发数：系统能同时处理的请求总数，并发数 = QPS × RT，例如：QPS=100，RT=0.5秒 ，也就是并发数=50 

### 单实例并发数：

允许单个函数实例同时处理的请求数量。

函数容器按实例占用时长计费。假设访问数据库需要10秒，那么当并发的3个请求分别在3个实例内被处理后，3个实例总的执行时长是30秒。如果能让这3个请求在同一个实例内并发处理，这样实例的占用时间为10秒。为了帮助您节省实例资源费用，AoneFaaS 支持单实例多并发功能，在保证机器水位正常的前提下，您可以适当增加并发数，以增大应用整体吞吐量。

(https://o2-img.alistatic.com/i/i09blZ/VER4LJuZ99rPCAO136iSj-ed47120741.png)


### 容器弹性范围：

弹性范围决定了预留实例数和最大实例数。

- 预留实例：可以类比于传统应用，启动一定数量的机器，不管有没有流量都会一直运行并等待请求进入
- 弹性实例：预留实例可以应对日常流量需求，但是假设业务忽然导了一波流量进来，目前的容器数量已经无法处理这批额外的请求，那服务则会出现大量的请求失败，这时候就要使用我们的弹性容器了。弹性容器顾名思义，会根据一定规则弹起或缩容，目前 AoneFaaS  支持的规则就是并发。当请求某一时间内出现增加，势必会导致函数各容器的并发增加，当函数网关检测到并发达到了用户设置的单实例并发数，就会弹起弹性容器来进行分流，一直到单容器并发降低到预设最大并发值，才会停止新的弹性容器弹起。当这波流量高峰过去并发下降到预设最大并发值后，这部分弹性容器就会自动缩容。


