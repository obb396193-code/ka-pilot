# Assets 开发测试环境域名迁移 FAQ

## Assets 开发测试环境的域名有什么变化？

1. 使用多年的 g-assets.daily.taobao.net 与 g.assets.daily.taobao.net 不再提倡使用，推荐使用 dev.g.alicdn.com 替换掉上述两个域名。旧域名仍然会保持兼容，新资源仍然在旧域名上可以访问到。
1. AONE 上的「多套环境」域名g-assets.daily[N].taobao.net 与 g-assets.prepub[N].taobao.net 暂时维持不变，预计 2023-03-31 下线。
## 为什么要这么变化？

g-assets.daily.taobao.net  与 g.assets.daily.taobao.net 是天生有两方面的大问题的：

1. 这两个域名均非常规运维的域名，没有在公网做解析，无法通过 HTTPS 证书供应商的 CAA 校验，因此也就无法使用正规的 HTTPS 证书。目前这两个域名使用的 HTTPS 证书使用的都是阿里郎 CA 签发的自签证书，这造成了两个问题：
1. 对于没有安装阿里郎的测试用途的移动设备来说，要通过 HTTPS 访问这两个域名非常繁琐，需要手工信任证书或者安装阿里郎 CA
1.  Lazada 的阿里郎附带的 CA 与集团 CA 也不一样， 造成了 Lazada 用户无法正常通过 HTTPS 方式访问这两个域名，也必须需要手工信任证书或者安装集团阿里郎的 CA
1. 这两个域名所在的托管服务器是一台单独的生产测试环境的虚拟机，这也带来了两个问题
1. 该机器配置年久失修，一旦宕机，很难恢复，是个风险非常大的单点
1. 所在的网段是生产测试网段，预发环境的服务器无法访问到这台机器
现在 dev.g.alicdn.com 域名通过统一接入层指到了一个正规的 AONE 应用，整体天生有较高的 SLA，也支持从预发环境访问，并且有正规的 HTTPS 证书加持，彻底与自签证书说再见。

### 以前所使用的绑定 hosts 方式怎么办？

首先申明： 不推荐使用绑定 hosts 的方式来进行开发测试，请尽量使用环境判断来让页面输出正确的域名，即：生产环境使用 g.alicdn.com，开发测试环境使用 dev.g.alicdn.com ，预发环境视业务需要自行选择用哪个域名。



由于机房因不可抗力已经裁撤，当前已不再提供绑定host方式，建议使用代理插件将g.alicdn.com代理至dev.g.alicdn.com上使用。



之前绑定使用的 10.101.73.189 与 10.218.249.33 所在机房因不可抗力已经裁撤，从 2019-11-15 开始不再提供服务。

之前绑定使用的 106.11.210.201，106.11.210.44 所在机房因不可抗力已经裁撤，从 2023-01-17 开始不再提供服务。

### 还有别的问题或者需求？

请移步 研发小蜜 进行沟通

