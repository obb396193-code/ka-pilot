跨端应用默认都会开启灰度放量功能。该功能开启后，执行线上发布时，默认只有特定的用户可以访问到新页面。可以在发布系统调整页面灰度的比例并观察业务数据，确认无问题后再全量部署让线上生效。

## 如何进入灰度

灰度是一个放量发布机制。迭代的线上发布成功后，代码会被 merge 到主干，但是迭代并没有完结，而是处在灰度阶段。

如果此次发布的页面都是首次发布（新增页面），则无需进行灰度放量，线上内容直接生效，迭代完结。

如果此次发布的页面有不是首次发布的页面，则会进入灰度流程（注意：灰度的是已经发布过的页面，新增的页面会直接进行线上发布并生效）。可以在灰度阶段逐步放量到全量发布，或是取消灰度（放弃此次发布）。无论是全量发布还是取消灰度，迭代均会完结，不能再操作发布和放量。

## 放量过程

进入灰度后，默认停留在 INIT 阶段。如果域名支持内网灰度，则通过内网可以直接访问到新内容。如果域名不支持内网灰度，则需要先切换到 0%，通过[强制灰度](https://alidocs.dingtalk.com/i/nodes/6LeBq413JA9BOdm2i0Ad0wD7JDOnGvpb?utm_scene=team_space&iframeQuery=anchorId%3Duu_lln77a8xn9qmg9dsurh)的方式来访问新内容。

可以通过点击小齿轮来进行灰度比例的调节，灰度比例必须逐挡提升放量，直到正式发布。

可以通过页面列表来查看本次灰度的页面信息（访问地址、灰度比例，CDN 实时数据）。

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ugsskvdgi2p3ybz0/imgLsEPxNb2NR.png)


### 灰度比例说明

| 比例 | 内部用户 | 外部用户 | 强制灰度 |
|-|-|-|-|
| INIT/内网灰度 | 若域名支持内网灰度则全量 | ❌ | ❌ |
| 0% | 若域名支持内网灰度则全量 | ❌ | ✅ |
| 5% | 若域名支持内网灰度则全量 | 5% | ✅ |
| 25% | 若域名支持内网灰度则全量 | 25% | ✅ |
| 50% | 若域名支持内网灰度则全量 | 50% | ✅ |

- 灰度比例必须逐档提升放量，每个梯度建议停留 10min，观察相关数据，整体放量时间必须 >30min，否则需要填写紧急发布备注；
- 若页面回归没有问题，可点击 正式发布，发布后，新版页面比例将调整为 100%；
- 若页面有问题，不能发布，点击 取消灰度，放弃此次发布，线上页面将全部回到灰度前的版本；
- 正式发布 和 取消灰度 都表示灰度已完成，不能再进行灰度操作；
### 灰度命中策略

灰度梯度中的比例是基于用户百分比的分桶，也就是有 N% 的用户会命中灰度。我们会根据请求中提供的设备 ID、账号 ID、Aplus 脚本埋点值来计算灰度分桶值，具有如下特征：

1. 灰度命中用户策略是固定的，灰度期间：命中的用户会一直命中，反之亦然；
1. 在低比例中命中灰度的用户，也会命中高比例灰度，也即逐步放大命中比例；
1. 灰度比例设置值仅用于分桶计算使用的，不代表线上实际灰度比例。实际灰度比例可以参考页面列表中的“实时线上灰度比例”表格列。
### 内网灰度

跨端应用的域名由于 spe 环境能力限制，暂时不能提供内网灰度，如果你有非常强的诉求，可以通过[研发小蜜](https://links.alipay.com/app/room/5fbf53f6cd068c04d342e682/start-conversation-form/)进行咨询。

### 注意事项

- 首次上线的页面不会进入灰度流程，而是直接正式发布。
- 应用有页面处于灰度发布时，其他迭代都不允许执行线上发布操作，若需发布需要先将灰度完成（正式发布或是取消灰度）。
- 灰度发布专注的是发布过程提供按比例发布的功能，使页面在全量发布前提供一个回归的渠道，所以整个灰度过程不应持续太久。不要将灰度发布当成 A/B test 使用。
- 灰度操作只影响页面展示比例，对代码本身无影响。无论页面取消灰度还是全量发布，代码都已经在灰度操作前合并到了主干（master）。
## 强制灰度

注意：下列方法仅用于外网验证，请勿投放至终端用户！！！

### 通过 Cookie 设置灰度

预发域名提供了灰度打标页面，地址如下（将 domain 替换为自己业务的预发域名）：

```
https://{domain}/.well-known/grey-flag

```
灰度打标对整个二级域名的所有子域名生效（当域名倒数第二部分为 com/org/net/ac/co/edu 时，对三级域名的所有子域名生效）。例如 taobao.com 的子域名，可以使用以下地址：

[https://market.wapa.taobao.com/.well-known/grey-flag](https://market.wapa.taobao.com/.well-known/grey-flag)

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ugsskvdgi2p3ybz0/imgqtdPDGJ5ms.png)


### 通过 Header 强制灰度

设置如下 Header 可强制命中灰度页面：

```
x-air-grey: true

```
#### 浏览器测试方法

可以在浏览器中安装修改 Header 的插件实现强制命中灰度。以 Chrome 下的 [ModHeader](https://chrome.google.com/webstore/detail/modheader/idgpnmonknjnojddfkpgkljpfnnfcklj) 为例，参照如下设置：
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ugsskvdgi2p3ybz0/imgdhrM8H01nI.png)


#### 客户端测试方法

对于集成了 Air 灰度功能的客户端，可以通过启用相应开关的方式强制命中灰度。以手淘 Debug 包为例：
(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/ugsskvdgi2p3ybz0/imgpyH4SIx5XV.png)


## 常见问题

### 为什么我强制灰度不生效，访问不到灰度内容

- 确认是否已将灰度比例切到了 0%，只有在 0% 以后强灰标才生效。
- 确认是不是正确的带上了强灰标：
- Header 方法：请求头中的 x-air-grey: true
- Cookie 方法：cookie 中的 air_grey: true
### 为什么实际灰度比例与设定不一致

在现有客户端实现下，灰度分桶基于账号 ID （unb）或  注入的变量（cna）  计算。如果页面本身访问量较少、访问用户固定，可能出现灰度比例不符合预期的情况。实际灰度比例请以实时数据显示为准。

### 我设置的是内灰 / 0%，为什么在 4G / 5G / Guest 网络访问到了灰度内容

内网灰度通过劫持办公网 DNS 解析到 SPE 接入层实现，因此：

- Guest 网络目前使用的是办公网 DNS，同样纳入内网灰度范围。
- 浏览器 / 端侧解析存在缓存，因此如果设备接入过办公网 / Guest 网络，设备可能留有解析缓存并继续访问 SPE 接入层。请尝试开关飞行模式刷新缓存后再试。
