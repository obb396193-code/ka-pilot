## 迭代概述

迭代是在 O2 Space 上进行发布活动的单元。O2 Space 研发平台针对一定周期内的需求及所对应的在不同发布环境下的发布、集成区的设置与流程，以迭代概念进行组织，每次迭代代表一个应用发布版本。

## 迭代的作用

以常见的 Assets 为例，前端发布是将资源发布到 CDN，并通过版本来消费资源，需要在同一批完成验证、上线的需求（在平台上表现为变更），应当打包在一起，并用同一个资源版本完成发布。

迭代的作用就是这个，作为平台上承载发布批次的场景，迭代以版本号为划分，每个版本号对应一个资源产物版本，每次发布时，会将添加至该迭代的所有变更合并到集成分支（平台提供），并将该集成分支的构建产物使用迭代对应的版本发布到 CDN 上。

## 应用场景

在平台进行日常 / 线上发布时，均需要使用迭代来完成。

