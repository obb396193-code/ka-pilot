云构建平台于 2024 年 9 月 9 日起将为构建后有持久化缓存生成的项目在日常发布环境默认开启编译缓存。

## 为什么平台要默认开启编译缓存？

不了解什么是编译缓存？您可以通过阅读[《云构建编译缓存：理解，使用，享受》](https://ata.atatech.org/articles/11020296431)搞清编译缓存的优势与核心机制。

我们在回放线上任务时发现在回放周期内，约 55% 左右的项目在构建结束后会生成持久化缓存（我们将其称为「隐式缓存」），在未开启编译缓存时，这些缓存会被直接丢弃，但若复用该类缓存，应用平均可减少约 27.61% 构建耗时，并且构建时长越长，开启缓存的性能收益越明显。因此，我们决定为该类应用在日常构建时默认开启编译缓存功能，让构建更快一步（下图显示了应用在使用隐式缓存后构建时长的缩短情况）！。

(https://o2-img.alistatic.com/i/i09blZ/05i8kJ7GgUIWsnFjB6SuU-3962f9f7c8.png)


目前我们仅检测应用构建结束后，`node_moduels/.cache`目录下是否生成缓存文件。

## 如何判断应用默认开启了编译缓存？

您可以在 O2 Space 平台和云构建详情页感知应用是否默认开启编译缓存：

### O2 Space

当首次命中默认开启编译缓存功能时，您将在构建节点基础信息面板看到如下提示信息：

(https://o2-img.alistatic.com/i/i09blZ/WkcWkbAewSMWmjcMTH3ar-73cea95997.png)


之后，当编译缓存显示「平台开启」时说明应用已命中默认开启编译缓存功能：

(https://o2-img.alistatic.com/i/i09blZ/p1b70Sm6ObQ0Qkespgfub-125bbed8d7.png)


### 云构建详情页

您也可以在云构建日志「编译缓存检查」阶段查看缓存相关日志，当显示「【平台】开启编译缓存」时，说明应用命中默认开启编译缓存功能：

(https://o2-img.alistatic.com/i/i09blZ/v904Beep94alYQMKU5ANV-4e677f98e5.png)


## 如何关闭默认开启编译缓存功能？

在极少数情况下，应用会因为加载了有问题的缓存而导致构建失败，若此类问题偶尔出现，请联系云构建团队重置缓存。

若默认加载的缓存持续影响应用构建，您可以通过如下方式主动关闭平台默认开启编译缓存功能：

1. 在构建节点基础信息面板，hover 编译缓存右侧图标，点击链接跳转至「构建设置」页面：
(https://o2-img.alistatic.com/i/i09blZ/gmzkGVk7MZ4lMDCQUkIad-9d5d525c63.png)


1. 在构建设置页面，如图所示区域，关闭「默认开启编译缓存」功能：
(https://o2-img.alistatic.com/i/i09blZ/N1VNbjCQHtdzCaXDxHRTi-67adf4b47b.png)


## 如何感知默认开启编译缓存收益？

我们将在近期上线应用开启/未开启编译缓存下，构建时长对比报表，敬请期待！

## 默认开启编译缓存的局限性

虽然默认开启编译缓存功能降低了开启编译缓存的成本，但是您应该意识到该功能还存在如下局限：

1. 为了保障构建稳定性，该功能仅在日常发布环境开启；
1. 该功能仅针对`node_modules/.cache`目录下的文件进行缓存，并且无法使用平台提供的「缓存预检功能」进一步优化构建耗时；
因此，我们希望您能主动开启编译缓存，详情见 。



