线上构建是指发布线上的过程执行代码构建

assets 发布，默认线上是不执行构建过程的，而是直接获取对应 commitId 最新日常发布的构建结果。这样做主要是为了遵循 所测即所发 的原则，避免在日常测试好的代码线上发布时重新构建引入 bug，特别是基于 npm 依赖的项目机制，如果依赖出现问题，有一定概率会导致线上事故。

## 开启注意事项

- assets 发布默认不开启线上构建，如果有特殊需求，可按照下面文档开启
## 如何开启

项目如果知道类似风险，有其他机制保障安全，且拥有日常和线上发布代码不一致的需求，可开启线上构建，开启方式如下：

进入应用设置页，点击 发布设置：

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/cr594p/imgyfzyoWOFsb.png)


打开开关即可。

## 开启线上构建对流程的影响

1. 线上发布时会重新执行代码构建，不再取日常构建结果。
1. 默认线上发布是不执行门神检查任务的，只要对应 commitId 的日常发布门神检查通过即可。但开启线上构建后发布会重新执行门神检查任务。
## 构建脚本如何判断当前发布环境

当开启线上构建时，为了保证构建脚本知道当前的构建环境，build_argv 环境变量新增构建环境参数

```
const buildArgv = require('yargs-parser')(process.env.BUILD_ARGV_STR);

```
| 参数 | 值 | 说明 |
|-|-|-|
| buildArgv['def_publish_type'] | 发布类型 | 标明当前发布类型，已有 |
| buildArgv['def_publish_env'] | daily 或 prod | 标明当前发布环境，daily 为日常(预发)，prod 为线上, |

> 如果应用没有开启线上构建， def_publish_env 默认不传入，如果你的构建脚本或构建器需要强制要求用户开启线上构建，可通过该值判断。

请严格按本文档方式判断环境，不要使用字符串直接判断，O2 Space 不保障因为判断方式错误导致的问题

对于构建脚本，可以执行如下命令模拟传入环境变量测试

```
# -- 后面表示传入云构建的环境变量
def build -- --def_publish_type=assets(覆盖为 assets_o) --def_publish_env=prod

```
对于构建器，构建器本地 link 后执行如下命令测试

```
# -- 后面表示传入云构建的环境变量, -l 表示调用本地构建器，调试过程需要先在构建器目录下执行 def dev 链接
def build -l -- --def_publish_type=assets(覆盖为 assets_o) --def_publish_env=prod

```
## 注意事项

除了 assets 发布，其他发布类型由于历史兼容原因以及流程本身特性，暂时不开放是否可更改线上构建流程，默认如下：

1. webapp 类型日常和线上都会开启构建
1. weex 发布类型线上无构建
具体发布类型的环境判断可进一步参考对应发布类型的文档。

