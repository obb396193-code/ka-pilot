用户真实链接：https://links.alipay.com/support/conversation/6807062025b69004b004f5ea  
作者：庞舒蕾  
评审人：叶子旭



问题描述：

构建任务失败并提示：构建产物为空



问题原因：  
构建过程中，通常会因为构建逻辑、构建配置等因素使构建产物未生成或未被正确移动到指定目录。  
  
解决方案：  
1. 首先，检查构建日志，确保构建过程工没有错误影响构建产物的生成。  
2. 如果使用构建脚本，确保在`abc.json`中配置了正确的`mv`命令，将构建产物移动到`$BUILD_DEST`目录。  
3. 如果使用构建器，检查`abc.json`中的`outputDir`字段是否正确指向构建产物目录。



注意事项：

1. 在TNPM研发中如果使用了构建器，构建器会检查NPM包的version字段是否为可发布版本，如果所有包的version字段都是线上已存在的版本，则不会发布任何包并结束构建，此时构建日志中不会有任何报错，但产物仍为空。

2. 云构建默认的产物输出目录`$BUILD_DEST`为.package目录，在未修改的情况下，请确保构建结束时产物都在此目录下。  
  


