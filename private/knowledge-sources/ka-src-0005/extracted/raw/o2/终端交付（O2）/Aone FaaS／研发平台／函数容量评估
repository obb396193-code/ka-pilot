# 容器类型

- 预留容器：预留容器比较好理解可以类比于传统应用，启动一定数量的机器一直放在那里处理请求，函数预留容器与之类似，就是启动多个一定规格的容器（单容器规格一般是 1C2G ）不管有没有流量容器一直存在。
- 弹性容器：预留容器可以应对日常流量需求，但是假设业务忽然导了一波流量进来，目前的容器数量已经无法处理这批额外的请求，那服务岂不是会出现大量的请求失败呢，这时候就要使用我们的弹性容器了，弹性容器顾名思义，会根据一定规则弹起或缩容，目前 FC 支持的规则就是并发，函数上线前用户设置函数单实例并发度值，当请求某一时间内出现增加，势必会导致函数各容器的并发增加（tips: 并发 = qps * rt，例如 qps = 100，rt = 500ms，并发 = 100*0.5 ），FC 检测到并发达到了用户设置的单实例并发度值，就会弹起弹性容器来进行分流，一直到单容器并发降低到预设最大并发值，才会停止新的弹性容器弹起。当这波流量高峰过去并发下降到预设最大并发值后，这部分弹性容器就会自动缩容。
实际业务中推荐预留 + 弹性容器组合使用。

# 单实例并发度配置

# 

设置 > 函数配置

即配置一个函数容器最大可以承载的并发量，如果实际业务并发数超出当前函数所有容器单个并发数之和，则会触发函数扩容，反之则触发缩容。注意（当线上单容器并发超过所配置并发值时，超出的请求将直被快速拒绝）

计算公式：并发数 = QPS * RT 

实际场景举例：假设我发布了一个函数，线上只配置了一个容器，并对他进行梯度压测（如何梯度压测见函数压测课程）

- 施压机给到 QPS 50 时，容器机器水位正常，并且 RT 也在预期内（假设为 200ms）
- 施压机给到 QPS 60 时，容器机器水位出现异常或 RT 不符合预期
- 则 50 是这个容器最大可承受的 QPS，这个实例最大并发度为 10（50 * 0.2）
# 函数容器弹性范围

# 

函数需要配置一个容器弹性的范围即一个函数流量大的时候最多弹起多少容器，流量小的时候会有多少容器一直在线上保底运行。

接着上文举例：

- 假设产品或运营同学预估这个函数接口日常 QPS 为100，每日峰值最高可以达到 300
- 则我们可以配置函数实例数范围为 2（100/50）- 6(300/50)
# 注意

C 端业务或者大流量业务上线之前请务必进行单容器压测得到函数的合理并发值，并根据此值计算函数容器的弹性范围。

