内网劫持用于将全量办公网用户纳入灰度范围，以便在外部放量前对办公网用户进行灰度发布验证。

- 内网劫持接入流程包含众多人工节点，整体接入较为复杂，如无特殊需求不建议申请接入。
- 如果需要开启如手淘 APP 通过 AMDC 内网劫持，联系 O2 研发小蜜进行协助处理。
- 建议：接入 SPE 需满足业务量级较大（外网 QPS > 1000）且业务重要性高（如大促相关域名），其他业务不建议进行 SPE 接入。如仅需要对发布资源进行验证，请参考灰度文档的“如何强制 命中/不命中 灰度页面”
- SPE 接入专用沟通群：35373839
## 需求沟通及协议签署（重要）

SPE 环境为小流量验证环境，Air 方不保证在 SPE 环境提供与线上环境一致的可用性（受限于各BU提供的统一接入集群性能与稳定性）。同时，由于 SPE 环境没有前置 CDN 资源，所能承载的流量有限，需要业务方提前对内网流量进行评估。同时，需要业务方单独提交[需求](https://aone.alibaba-inc.com/v2/project/2049752/req#viewIdentifier=d7f112f9d023e2108fa1b0d8)并注明：

1. 服务场景及预期内网峰值 QPS/流量；
1. 确认内网劫持接入仅用于内部测试或针对办公网用户的小范围灰度，业务方不得以任何方式将外部（非办公网）流量引入内网劫持环境；
1. 确认接受“内网劫持不保证可用性等同或高于外网服务。出现问题时，能够提供修复处理方案和处理即可。”
在[https://project.aone.alibaba-inc.com/projects/1081606](https://project.aone.alibaba-inc.com/projects/1081606)下新建需求，需求样例：
1.本次需求说明（需求简述，此外可以附详细需求aone、lark）
xxx.m.taobao.com 已申请开启全功能 PHA 配置能力，请求重新接入内网灰度环境；并且业务可签署相关协议，保证该灰度环境的使用场景。

服务场景：淘票票C端消费侧页面
QPS（仅内部办公网）：预估最高峰 5 QPS

诉求：在灰度过程中，xxx.m.taobao.com 域名下的页面，不论何种发布类型，在办公网则默认访问灰度资源。且仅影响办公网。
诉求来源，希望在 O2 部署的跨端应用也能支持内网环境灰度，减少扫码强制灰度失效等原因导致阻塞灰度验证的风险。

2.风险影响（可灰度、可监控、可应急三板斧能力说明，可能导致的业务风险）
可以接受“灰度办公网不保证100%可用”，因为仅用于走查和测试，但要求影响不到外网部分流量，即不会影响外部用户，出现问题时，能够提供修复处理方案和处理即可。

3.使用者（内部小二/商家/C端用户等）
内部小二（业务开发、测试、业务产品运营等）

## 提交接入流程

请先在站点控制台提交接入流程，并跟进接入任务。流程中会将域名注册到统一接入，请注意：

不要执行统一接入工单中的 CNAME 变更，自动变更域名 CNAME 确认请点击拒绝

不要执行统一接入工单中的 CNAME 变更，自动变更域名 CNAME 确认请点击拒绝

不要执行统一接入工单中的 CNAME 变更，自动变更域名 CNAME 确认请点击拒绝

统一接入工单入口及 CNAME 变更节点位置如下：

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/4jKqmpyWVJpDnw19/img/044f48ae-80bf-4d25-b265-a2f938ebf69f.png)


(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/4jKqmpyWVJpDnw19/img/cdbfa289-5ea6-45f4-b518-bb12b4d9abc6.png)


记得先完成SPE路由发布，再走O2平台流程改解析

记得先完成SPE路由发布，再走O2平台流程改解析

记得先完成SPE路由发布，再走O2平台流程改解析

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/res/4jKqmpyWVJpDnw19/img/ff943a1a-f6dd-47f3-9b17-5b25efaed341.png)


点击“SPE 接入”审批单联系对应审批人处理。操作完成后，在办公网请求解析测试。如果解析结果返回 `CNAME work.tengine.ingress.alibabacorp.com` 或 `~~CNAME default.wagbridge.work.alibabacorp.com~~`，则接入流程结束。如果仍返回到 CDN 的 CNAME 记录，需要执行后续步骤。

## 办公网 DNS 劫持

注：此部分变更需要网络团队操作，不受 Air 团队控制。建议预留一周时间。

变更需要发送邮件至 [@嶙峋](https://aliyuque.antfin.com/luke.ly)，并由申请人所在团队安全生产接口人回复确认，内容参考如下：

域名：xxx.com

目标：IDNS - xxx.com.rpz.office.zone

原因用途：通过安全生产环境接入内网验证

此变更一般需要晚间操作，需要业务方在变更时间到办公区进行测试验证。

