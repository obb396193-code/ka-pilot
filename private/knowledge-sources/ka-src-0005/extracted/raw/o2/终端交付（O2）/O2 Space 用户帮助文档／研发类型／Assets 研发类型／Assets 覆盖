提醒：因不符合安全生产要求，该研发类型已不支持新仓库接入（存量仓库仍旧可以，但建议迁移），有覆盖发布场景需求的用户请使用 “Assets 灰度覆盖” 研发类型

## 能力介绍



Assets 覆盖式发布 基本与 Assets非覆盖 一致，唯一区别是发布的路径没有前置版本号。且同名文件允许覆盖发布。

- 请先阅读非覆盖的发布文档
[Assets 非覆盖](https://docs.dingtalk.com/i/nodes/20eMKjyp810mMdK4HgOqGOa5JxAZB1Gv)

- 请尽量避免使用下列场景二，后续不排除因为安全管控而下线流程的可能
注意的是无论是否有版本号，默认的缓存时间都是比较大的。如果使用场景2，请请注意设置缓存时间

### 使用场景一：通过添加文件名 hash 实现非覆盖式发布

仓库本身的构建脚本可以在文件名添加 hash，该 hash 由文件内容本身唯一确定，例如下面两个迭代，由于 0.0.2 的 b 文件未修改，所以构建结果中 b 的文件名是不变的。a 文件修改，会生成一个新的版本。

```
# 迭代 0.0.1
/repo/a_0xy12.js
/repo/b_1abcd.js

# 迭代 0.0.2
/repo/a_9wc18.js
/repo/b_1abcd.js # b 文件未修改，所以生成的路径是一致的

```
该方案的优势是对于未修改文件，文件路径是不变的，既实现了非覆盖，也充分使用了缓存。

### 使用场景二：未添加 hash，同名文件直接覆盖

该场景在发布 assets 时会对线上版本直接生效，无论安全性和性能都远比不上非覆盖式，发布系统仍保留这个功能的原因只是因为历史兼容。

1. 请尽量避免使用该模式，因为这种模式的 assets 发布线上会直接生效，可能直接影响引入资源的页面或应用。注意流程不保证这种模式使用的灰度功能和 SLA。
1. 注意该模式需要添加配置文件修改缓存响应头，否则新版发布会由于浏览器和 CDN 缓存不生效。配置文档：[《Assets 自定义 Header 》](https://docs.dingtalk.com/i/nodes/jb9Y4gmKWrx9eo4dCNQKjaGwJGXn6lpz)
