## 能力介绍

#### 基础能力

Assets 灰度覆盖式发布 与 Assets 覆盖发布 基本一致，唯一区别是 灰度覆盖式发布 支持利用 ip 进行用户分桶（可识别 ipv4 和 ipv6 ）灰度放量发布，灰度比例支持 0%, 5%, 25%, 50%。



注意，使用 Assets 灰度覆盖式发布 请注意如下几点

- 请先确认是否需要灰度覆盖式发布，如无必要请使用非覆盖模式进行发布。
- 注意灰度覆盖式 Assets 域名使用的是 o.alicdn.com，日常域名 dev.o.alicdn.com
- 请注意，如果删除文件，发布 dev.o.alicdn.com 的支持删除，但已发布到线上 o.alicdn.com 的资源仅支持覆盖，不支持删除。
- 请先确认发布资源类型，当前灰度覆盖式发布资源类型仅支持.js，.json，.css类型。
- 注意单次发布灰度覆盖式文件个数 少于500个。
- 注意灰度用户访问 ip 变化时，灰度会重新计算用户分桶，判定是否命中灰度。
- 注意path路径不超过 50个字符。
- 注意 灰度 Assets 覆盖式 发布 不支持 combo 逻辑。
- 注意灰度覆盖式 Assets 单文件 不能超过 5mb，资源过大会影响资源的 CDN 命中率，加速效果不佳。
- 注意，由于灰度assets支持按迭代回滚能力，所以要求每次发布的资源必须包含已发布的线上资源，这样才能保证按迭代回滚的完整性。
- 注意灰度覆盖式 Assets 文件发布时，若线上不存在可覆盖资源，则该资源直接发布上线，不再灰度覆盖范围内。


#### 提供的发布环境

- 日常环境
- 线上环境


## 应用创建

选择Assets研发类型，建立Assets灰度类型，若无法见到Assets灰度类型或无创建应用权限，请联系平台值班进行处理。

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/dwcr79/img3yyaE638ag.png)


应用概览-应用信息展示如下。

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/dwcr79/imguJMMRn1Q1x.png)


## 关于灰度调节

线上发布完成后迭代会自动进入灰度阶段，待灰度完成（正式发布或取消灰度）后迭代才会完成。灰度包含 0%, 5%, 25%, 50% 四个比例，放量是需要从低到高逐级增加。

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/dwcr79/imgFakeGFQklw.png)


## 支持按迭代回滚

(https://alidocs.oss-cn-zhangjiakou.aliyuncs.com/lark/https://yuque.antfin.com/def-work/doc_v3/dwcr79/imgu2j6fH3JPy.png)


## 关于 Header

### 一、支持自定义 Header

O2 Space 发布支持 assets 的响应头设置，你只要在仓库根目录添加一个 .assetsmetafile 文件即可。



#### 1.1 支持类型

Content-Type，Content-Encoding，Cache-Control，Content-Disposition，Content-Language，Expires



#### 1.2 格式

.assetsmetafile 文件格式如下，如果有多 header 自定义，换行隔开

```
cache-control:max-age=600,s-maxage=600

```
该配置可以设置浏览器缓存 和 CDN 缓存时间为 600 s，默认值为：max-age=300,s-maxage=600



#### 1.3 注意事项

- 响应头设置依赖于 OSS 支持的 Header 设置（ali-oss），列表请查看文档：[https://github.com/ali-sdk/ali-oss#putname-file-options](https://github.com/ali-sdk/ali-oss#putname-file-options)
### 二、支持功能型 Header（cookie模式）

#### 2.1 支持 mock 灰度

提供header（cookie模式）请求中增加header（cookie） x-assets-pt-source-mock: 127.0.0.1  强制用户使用127.0.0.1进行灰度分桶。



#### 2.2 支持强制灰度

提供 header（cookie模式）请求中增加header（cookie） x-assets-force-grey: true 强制用户进行灰度。

响应 response header 中 x-assets-grey：true 时代表命中灰度。



### 三、支持CDN缓存刷新

当前联系管理员处理 @冠夫。

