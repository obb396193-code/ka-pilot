

评测任务状态变为“已完成”后，可以对评测任务使用的评测数据集进行打标，打标完成后将创建一个新的评测数据集。

​  


1. 点击“打标”
2. 填写打标后评测数据集信息

1. 评测集名称
2. 打标逻辑，可选择采纳/不采纳。选择不采纳数据的时候，可进一步选择不采纳的原因。

1. 注意：这里的打标逻辑是取并集。比如一条数据集被模型/人员a评判为采纳，人员b评判为不采纳，那么在打标的时候，打标逻辑选择采纳或者不采纳，这条数据都会被打上对应的标记。

3. 点击“确定”，生成打标后的数据集

1. 打标后的评测数据集，会在末尾加一列“评分人”，表示打标的来源，值为“model”或“human”或“model，human”，意思为这一条数据被打标是因为model/human评判的
2. 因为应用需要输入的变量是评测数据集列的子集即可成功提交评测任务，所以打标后的评测数据集可以直接用于相同的应用进行评测，帮助应用提升质量

  


  


