
## 1. 背景

在过去的一年多里，我们都看到了AI Agent的巨大潜力，但真正使用过才能体会到AI Agent的局限性，完全依赖大模型去执行复杂任务在当前阶段很难符合人们的预期。

因此，我们上线了Flow流程编排能力来弥补AI Agent的不足，我们希望能将复杂的任务**分解成较小的步骤**，以降低系统复杂度，减少对提示词工程和模型推理能力的依赖，整体上提高AI应用面向复杂任务的能力，提升系统的可解释性、稳定性和容错性。

在产品能力上，我们提供了**可视化搭建**的页面，通过节点拖拽+连线的方式实现流程的编排，没有开发经验的同学也能轻松胜任。同时我们也提供了**丰富的流程节点**，帮助大家尽情发挥想象力，搭建出专业、稳定、可靠的AI业务流程

## **2. 如何开始**

在[ideaLAB工作台](https://aistudio.alibaba-inc.com/)创建一个流程编排类型的IDEAs，会自动生成一个最简单的Flow，包含了开始、LLM、回复 3个节点

 

下面的动图展示了如何实现了一个简单的知识库问答Flow，包括节点编排、调试运行等操作：

## **3. 详细说明**

### **3.1. 基本概念**

#### **3.1.1. 节点**

节点是Flow的重要组成部分，一个节点就是Flow的一个执行步骤，通过连接不同功能的节点可以搭建出复杂而有序流程~

目前支持以下节点：

* **开始节点**：是整个流程的起点，用于设置初始变量，有且仅有一个，不可删除
* **LLM节点**：调用大语言模型回答问题，同时支持工具调用和记忆，可以理解为是一个**Agent**
* **回复节点**：用于生成输出内容，可以有多个
* **知识库**：从知识库中检索与用户问题相关的文本内容，可作为下游 LLM 节点的上下文。
* **代码执行**：执行自定义代码脚本，目前支持Python和JavaScript
* **工具节点**：调用平台工具或者自定义外部服务、API
* **IDEAs**：调用空间内的其他IDEAs
* **用户干预**：对上一个节点的内容进行**人工干预**或者对后续链路进行**信息补充**
* **意图识别**：通过LLM对用户问题进行识别分类，Flow执行到对应的分支
* **变量赋值**：对全局变量进行赋值操作
* **变量聚合**：将多路分支的变量聚合为一个变量，以实现下游节点统一配置
* **条件判断**：根据if else的逻辑判断，拆分出不同的分支
* **循环节点**：循环节点中可以创建**一个**子流程，子流程循环执行，并且支持并行循环
* **并行节点**：并行节点中可以创建**多个**子流程，每个子流程**相互独立并行执行**，所有子流程完成后再执行下游节点

 

#### **3.1.2. 变量**

变量用于传递前后节点的输入与输出，实现流程中的复杂处理逻辑

1. **变量绑定**

在节点的输入框里可以通过**斜杠 / 或者****${x}**这个按钮唤起变量列表，然后选择需要的变量进行绑定。Flow运行时，变量位置会被替换成实际的值。

**注意**：变量列表只会展示当前节点连线上**所有上游节点**的输出变量，选不到其他分支和下游节点的变量

**2. 系统变量**

系统变量是内置的，下游所有节点都可以绑定

* system.question：用户输入的问题
* system.sessionId：当前会话的sessionId，一个会话窗口一个sessionId
* system.messageId: 当前会话消息的messageId，一个sessionId下可以有多个messageId，表示多轮对话
* system.empXXX：当前登录用户的工号、姓名、花名等信息

**3. 全局变量**

全局变量是会话session级别的变量，允许多轮对话内都能引用该变量，适用于临时储存信息和上下文。

比如，在首轮对话中，调用了某个工具，把工具结果设置到全局变量中，下一次对话可以直接获取全局变量的内容，就不需要再次调用工具了。

全局变量需要结合**变量赋值**节点使用

 

**4. 输出变量的格式**

一个节点就像一个函数一样，节点输出变量可能是复杂对象，因此我们支持了对节点**输出变量的复杂格式定义**，让节点的输出结果和变量绑定更灵活。

当变量类型是object时，可以添加多层子变量。出于可读性考虑，**最多支持5层**

例如：某个工具的返回结果是：

对应节点的输出可以配置成：

那么在下游节点中，就可以绑定**任意层级**的变量了：

除了object的复杂类型，还**支持简单类型和数组**，具体格式和示例：

| **名称** | **类型** | **示例** |
| --- | --- | --- |
| 字符串 | string | abc |
| 数字 | number | 1、 1.1（支持整数和小数点） |
| 布尔 | boolean | true、false |
| 对象 | object | {"key": "value"} |
| 字符串数组 | array_string | ["a","b","c"] |
| 数字数组 | array_number | [1,2,3] |
| 布尔数组 | array_boolean | [true,false] |
| 对象数组 | array | [{"key": "value"}] |

### **3.2. 节点介绍**

#### **3.2.1. 开始节点**

在开始节点中，可以设置自定义变量作为流程的输入，下游节点中，可以绑定这些自定义变量，目前支持文本、下拉选择、图片、视频等类型

自定义变量在运行前，需要预先输入

#### **3.2.2. LLM节点**

LLM节点提供了**调用大模型**的能力，可以按需选择模型，调整模型参数，支持多角色的提示词，在user角色中还可以直接绑定图片、视频等多模态参数（需要大模型支持多模态）

同时还支持了**调用工具**（模型支持**FunctionCall**走FucntionCall，不支持就走**ReAct**）和**记忆模式**，实际上LLM节点可以看成一个Agent，在Flow中可以添加多个LLM节点，实现**多Agent的编排**！

  

#### **3.2.3. 回复节点**

回复节点可以将绑定的变量或者静态文案输出到页面上展示，它不是终态节点，可以用于过程信息的展示

在复杂的流程编排中，适当的增加回复节点，可以**减少首字RT**，让用户更快的看到第一个字，提升用户体验

#### **3.2.4. 知识库**

知识库节点用于检索在知识库中的信息和数据，支持**向量检索**和**关键词检索**，一般和LLM节点一起使用，实现答疑机器人的效果

 

节点只提供检索功能，知识库的管理功能可以参考文档：[知识库](https://aliyuque.antfin.com/aiplus/aistudio/cyev4ggl9vd0z0yg)

#### **3.2.5. 代码执行**

代码节点可以执行自定义的代码脚本，最大程度的提供灵活扩展的能力，支持**Python**和**JavaScript**语言, 适用于文本处理、数据计算、脚本执行等场景

**注意**：由于底层使用的是阿里云的函数计算，和内网的网络不通（例如不能执行odps等脚本），和内网打通的方案正在实施中，敬请期待~

***Python运行环境内置了常用的库（json、requests、pandas、numpy等等），如果遇到找不到库的情况，可以联系我添加~***

##### **使用方法**

1. 定义入参，绑定变量

1. 主main函数写法

##### **Python代码示例**

1. 使用 Pandas 进行数据处理

1. 复杂的业务逻辑处理

1. 调用外部api

#### **3.2.6. 工具节点**

工具节点用于调用平台工具或外部工具、服务或API，为Flow提供了强大的第三方能力支持

工具节点只接入了调用能力，工具的注册和管理请参考：[工具管理](https://aliyuque.antfin.com/aiplus/aistudio/qs1xwgqezgvv4wnp)

平台提供了丰富的工具供大家使用，还支持复杂的工具配置项实现定制的能力，比如值班排班工具：

#### **3.2.7. IDEAs**

IDEAs节点用于将同空间下已经发布的IDEAs作为流程的一个节点使用，让**IDEAs可以被复用**，减少重复工作。

**注意**：为避免IDEAs的循环引用，在运行是进行校验拦截，但是尽量在搭建Flow阶段避免循环引用

#### **3.2.8. 用户干预**

用户干预节点能够让用户介入流程的运行，修改节点输出结果或者追加信息，解决了模型输出不符合预期的问题。

Flow运行到干预节点时，会暂停**等待人工介入**，用户执行干预操作后流程恢复运行。

目前提供了2种干预模式：

* **修改上个节点内容**：可以对上个节点的输出结果进行编辑，下游节点绑定的变量均以编辑后的结果为准

* **追加信息**：定义需要补充的变量，让用户干预时填入，下游节点可以使用此变量

#### **3.2.9. 意图识别**

通过LLM对用户的问题进行分类，执行不同分支的流程，可以理解为是 LLM节点 + 条件分支节点的结合。

非常适合于客服问答场景，将意图识别节点作为前置步骤，用户输入问题意图进行分类处理，分类后导向下游不同的节点执行相关动作，以精确回复用户的问题。

#### **3.2.10. 条件判断**

用于实现流程的分支和决策，基于条件判断结果决定执行路径

截图对应的伪代码：

#### **3.2.11. 变量赋值**

通过变量赋值节点，可以将Flow内的变量赋值到全局变量中用于临时存储，并可以在后续对话中**共享引用**。

支持覆盖、追加、初始化3种更新方式：

* **覆盖**：替换掉之前的值
* **追加**：全局变量如果是array系列，会把值追加到array里
* **初始化**：清空全局变量，如果有默认值，重新设置为默认值

#### **3.2.12. 变量聚合**

将多路分支的变量**聚合为一个变量**，以实现下游节点统一配置。

注意：

* 相同类型的变量才可以聚合
* 开启聚合分组后，支持生成多个聚合变量

#### **3.2.13. 循环节点**

对数组类型的变量，循环执行子流程

* 输入变量必须是array系列，出于性能考虑，array的大小不能超过50
* 输出变量绑定的是子流程节点的输出变量，最后汇总为数组类型
* **可以开启并行**循环，子流程会并发执行

在循环中，通过**index**绑定当前循环的顺序号，通过 **item** 绑定当前循环的内容，以上截图翻译成伪代码如下：

#### **3.2.14. 并行节点**

并行节点支持设置多条子流程（最多10个），每个子流程是隔离的不存在依赖关系，能够并行执行，更好的提升Flow的执行效率，当所有子流程都执行完之后，才继续执行下个节点。

### **3.3. 其他功能**

Flow还提供了以下功能：

* **欢迎语**：用户打开IDEAs默认输出的欢迎文本内容，开场白可以引导用户提问，交代应用背景，降低对话提问的使用门槛。
* **提示性问题**：默认带出相关的提示性使用问题，用户可点击直接发送提问
* **预置QA**：预置的问题缓存能力，当用户问题命中缓存时，直接返回缓存结果

#### 

### **3.4. 流程调试**

#### **3.4.1. 运行日志**

调试运行后，可以看到每个节点的执行日志和整个流程的完整日志，记录了出入参、运行状态等信息，方便排查问题

#### **3.4.2. 单节点运行**

知识库、LLM节点、代码节点、工具节点、IDEAs节点、意图识别等节点支持单节点运行，在单节点运行中可以重复测试当前节点的执行是否符合预期。

单节点运行后可以查看运行状态、输入/输出等日志信息

 

### **3.5. 流程发布**

点击右上角的发布按钮，可以将流程发布上线

ideaLAB提供了5种渠道，用于访问发布后的Flow，具体使用和接入方法参考：[IDEAs发布管理](https://aliyuque.antfin.com/aiplus/aistudio/zzndys976agfga40)

* **API**：通过HTTP接口提供服务
* **WEB**：通过独立WEB网页提供服务
* **ideaTALK**：公开发布到ideaLAB平台上
* **嵌入网站**：通过JS插件嵌入到对应的网站
* **阿里钉机器人**：将机器人绑定Flow，通过群聊、单聊的方式提供服务

#### **3.5.1. API调用**

api调用示例，对于自定义多模态参数，参考下面**logo**参数的格式，详细文档参考：[IDEAs发布API使用说明](https://yuque.alibaba-inc.com/aiplus/aistudio/miqnulit7w0xmvll)

#### **3.5.2. SDK调用**

对于Java开发同学，我们提供了**JavaSDK**，可以快速接入，详见：[JavaSDK使用文档](https://yuque.alibaba-inc.com/aiplus/aistudio/betskstlycy2vgsy#LqMVh)

## **4. Flow应用案例**

下面是业务使用的一些典型案例，可以看到Flow拥有非常丰富的节点，能支撑非常复杂的业务流程

#### **业务信息查询小蜜：****问题路由 + 多Agent编排**

* 业务场景：先分析用户意图，再根据用户意图执行不同的Agent

#### **多领域知识库问答：****多场景知识库 + 结果聚合**

* 业务场景：基于用户问题，选择不同类型的知识库进行召回，进行结果聚合

#### **业务测试数据复杂构造流程：****复杂流程编排 + 中间过程输出**

* 业务场景：流程步骤较多且复杂，需要输出中间思考和执行过程

#### **测试团队生成测试用例智能助手：****并行节点加速流程运行**

* 业务场景：多条独立链路并行执行，缩短整体响应时间

#### **商品查询助手：****全局变量实现会话级记忆共享**

* 业务场景：首次对话查询到商品信息，设置到全局变量，后续针对此信息进行多轮问答

## **5. 最后**

Flow编排能力上线2个月以来，越来越多的人开始使用Flow编排自己的业务流程，目前已经创建了**2000+**的Flow。业务反馈也非常积极，提出了各种各样结合业务场景的诉求，这些声音都推动着平台能力不断升级，有些业务搭建的流程非常有想象力，甚至可以用壮观来形容，这也让我们坚定了把Flow这个产品做好的决心。

未来我们会提供更好用的模型、节点、工具、模板和更丝滑的用户体验，用更低的门槛将AI能力应用到大家的工作和生活中~

欢迎大家吐槽建议，有任何问题可以扫码进群交流（25765049689）

## **6. 相关文档链接**

* [AI Studio 控制台](https://aistudio.alibaba-inc.com/)
* [Flow产品使用指南](https://aliyuque.antfin.com/aiplus/aistudio/ppvswv86whfgbaz3)
* [JavaSDK使用文档](https://yuque.alibaba-inc.com/aiplus/aistudio/betskstlycy2vgsy#LqMVh)
