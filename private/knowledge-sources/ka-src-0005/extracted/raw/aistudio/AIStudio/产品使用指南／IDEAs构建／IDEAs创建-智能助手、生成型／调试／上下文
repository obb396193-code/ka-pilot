
## dui概述

如果IDEAs希望利用私有知识库数据来影响模型的对话内容，可以通过使用上下文功能来实现。您可以点击上下文区域中的“添加”按钮，以关联你预先准备好的知识库。

## 上下文管理

添加知识库后，可针对上下文进行召回方式的管理，默认配置如下，可根据需求调整相应配置。

| **​** | **召回方式** | **召回类型** | **召回数量** | **最大距离阈值** | **标签过滤** | **重排模型** |
| --- | --- | --- | --- | --- | --- | --- |
| **默认配置** | 分段召回 | 向量召回 | 20 | 0.8 | - | - |

1. 召回方式：

1. 分段召回：召回分段后的文本内容
2. 全文召回：召回分段文本的全文内容

2. 召回类型：

1. 向量召回

1. 最大召回数量：50
2. 向量匹配阈值：用于衡量查询向量与召回的结果向量之间的相似度。越接近1，表示两个向量相似度差异越大，召回的分段数量越多

2. 关键词召回
3. 最大召回数量：50
4. 关键词匹配阈值：用于衡量关键词与召回的结果词之间的相似度。越接近1，表示两个关键词相似度差异越大，召回的分段数量越多。

3. 重排模型：将多种召回方式的标准统一化，提高排序精度

1. gte：中英文数据均适合

4. 标签：选择标签后，仅带标签的文档会被召回

## 变量绑定

### 智能助手

只需要用户指定绑定变量，查询变量系统已经预置为用户input

绑定变量：指定知识库中召回的文档分段在提示词中填入的位置

### 生成型

都需要需要用户指定

查询变量：指定用哪个变量的内容进行上下文检索

绑定变量：指定知识库中召回的文档分段在提示词中填入的位置

​  


  


