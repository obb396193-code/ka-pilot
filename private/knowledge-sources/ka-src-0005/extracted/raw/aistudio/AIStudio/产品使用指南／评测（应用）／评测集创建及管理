
# 创建评测集

1. 点击新增评测集按钮

2. 输入评测集名称（注意评测集不能重名），并可在此时选择添加文件上传到评测集里（也可以选择不上传文件，即创建一个空评测集）。点击“文件示例”可以下载评测集模版，同时页面上也有评测集列规则的说明。

1. **评测集列规则说明：**

1. **如果需要评测对话型IDEAs，则选择的评测集中必须包括“Q"列（即用户输入的问题）**
2. **如果评测方法需要选择智能评测，评测集必须包括"A"列（即标准答案）**
3. **参与评测的IDEAs中自定义变量需要和评测集中的自定义列名对应，否则将无法匹配输入**

3. 点击“确定”，完成评测集创建

​  


# 添加数据

1. 在评测数据集列表里点击“查看”，进入评测数据集详情页
2. 点击“添加”，页面上支持“文件上传”和“手动上传”两种方式（额外支持通过api上传）

3. 选择“文件上传”：注意如果数据集不为空，则上传文件的列结构需要和已有数据集的列结构一致，否则将上传失败（“评测集列规则说明”在“发起评测任务”说明下有举例说明）

1. 上传文件示例如下。纯文本可直接输入，若需要和图片、视频混合排列，需要按照指定格式处理数据集

4. 选择“手动上传”：每次上传即为一行数据

1. 如果数据集为空，首次通过手动上传方式添加数据，可以编辑数据集列结构，可以增加列，删除列，编辑列名称

2. 如果数据集不为空，通过手动上传方式添加数据，不可以更改数据集列结构，也不可以修改列名称

3. 手动上传数据，支持添加图片、视频、文字，并支持混合排列

5. 对于评测集内已有数据，支持编辑，删除，下载

6. 注意：当评测数据集列数＞3时，页面只展示前三列内容

  


  


## 删除评测数据集的影响

1. 评测数据集内的数据支持删除

1. 影响：如果删除了评测数据集内某条数据，使用了该评测数据集的评测任务里对应数据会消失，但可以对剩下的数据进行人工评测，查看剩下的数据应用运行生成的结果及相关评分结果。如果删除了评测数据集内全部数据，那该评测任务无法进行人工评测，评分详情和评分明细都将没有数据。

2. 评测数据集支持删除

1. 影响：如果删除了整个评测数据集，使用了该评测数据集的评测任务将无法查看，报错“评测数据集不存在”

  


  


  


  


