

* 目标：模型输出的内容符合后续流程节点的入参要求
* 问题：怎么提取参数、怎么让模型输出我想要的格式

  


> **以LLM节点为例**
> 
> * **模型设置，回复格式选择json**
> 
> * **提示词（可根据自己的场景定义，这里我只给出参数提取的单一目标示例，红色字直接复制放在system起始位置）**
> 
> * **System：****用json格式输出。**你是一个参数提取专家，根据用户输入的内容，进行参数提取，分别需要提取name和age的值，并按照以下格式输出： {name:"", age:""}
> * **User：**用户问题
> 
>  
> 
> * 输出格式设置
> 
> * 将默认的string格式修改成你的目标格式，提示词中的输出格式需要和这里修改的格式保持一致，示例中使用object
> 
>   
> 
> 
> * 输出变量效果：后续节点可引用上述变量

​  


​  


