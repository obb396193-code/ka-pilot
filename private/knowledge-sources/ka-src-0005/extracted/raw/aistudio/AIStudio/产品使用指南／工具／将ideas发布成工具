

平台支持将ideas一键转为工具，添加到其他ideas后，实现类似multi-agent的效果。

# 创建

1. Ideas发布后，点击图上红框处按钮，即跳转到工具创建页。

2. 选中一个工具箱后，跳转到以下页面。

 工具路径，请求方法，参数默认填充。

只需填入工具名、工具描述、参数描述、默认值。即可成功注册工具。

question参数：对话型ideas所具有参数，相当于对话框输入文本。

变量参数：

知识库绑定变量默认填充，无需作为工具参数

其余变量会自动解析到工具参数中，是否必填来源于ideas中对该参数的配置。

如图中language即变量。

# 使用

发布好后，将它添加到另一个ideas中即可使用。

由于工具调用时每个参数所传的值是模型给出的，建议使用有语义的参数名称，或在参数描述中详细给出参数含义，可选值等。

如上图中的工具有两个参数

question表示需要翻译的文本

language表示需要翻译成的语言

使用效果

工具调用日志

