
# 最基础的 RAG 知识，以方便使用知识库产品

这里以一个简单的 RAG 为例：

（图片来源：<https://www.datalearner.com/blog/1051699425605719>）

RAG：检索增强生成。通过检索的方式增强模型的生成能力。

​  


## 知识抽取（蓝线）

1. 数据预处理: 将外部数据进行切分，并转换成文本块。
2. 文本向量化: 将文本块转换成向量表示，以便机器理解。
3. 向量存储: 将文本块对应的向量保存起来，以便检索使用。

  


## 知识检索（红线+绿线）

1. 用户输入: 用户输入一段文字。
2. 向量化: 将用户输入转换成向量表示。
3. 检索: 将用户输入的向量与向量数据库中的向量进行匹配，找出最相似的向量。
4. 上下文生成: 将问题、匹配到的向量对应的文本内容作为检索结果输入大模型。
5. 结果输出：大模型输出问题的结果。
