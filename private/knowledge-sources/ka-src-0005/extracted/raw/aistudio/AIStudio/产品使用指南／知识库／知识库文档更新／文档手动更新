

目前平台针对语雀及钉钉文档提供了手动更新功能，方便大家进行在线文档的管理

限制：只针对0419以后上传的在线文档生效。

## 更新入口

  


* 方式1：文档列表，用户选中已经上传的语雀或钉钉文档，点击”...“选择**更新文档**
* 方式2：分段管理，点击‘设置’，选择**文档更新**

  


## 文档更新策略

钉钉文档当前仅支持通过文档链接上传，因此，只能更新单个文档。而语雀文档的更新机制则更为灵活，具体取决于文档的上传方式：

* 若文档通过文档库上传，用户可选择仅更新当前文档或是更新整个文档库中的所有文档。
* 对于通过单独链接上传，或在2024年4月19日之前上传的语雀文档，仅能更新当前文档。

  


## 备注

* **文档更新维持现有结构**：

* 在对文档进行更新时，确保不改变文档的分段结构和处理方式，以保持文档的一致性。

* **更新前进行MD5校验**：

* 在实施文档更新之前，对目标文档进行MD5校验检查。通过对文档内容生成MD5散列值，并与存储在系统中相应文档的散列值进行比较，来判定文档内容是否发生更改。
* 如果通过MD5校验发现文档内容未发生任何更改，则不执行更新操作。
* 如果文档内容有所变动，则继续执行更新流程。
