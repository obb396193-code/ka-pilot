
## 文档上传

1. **开始添加：** 打开指定的知识库界面，点击屏幕右上角的「添加」按钮进入添加文档流程。

  


2. **选择文档类型：** 在弹出的选项中选择您要上传的文档类型，**“表格类”**。

* **支持的数据源：**

* **本地文件：** 上传支持的文件格式包括XLSX、XLS、CSV。
* **在线内容导入：** 目前可以导入语雀文档（仅支持单个表格文档），钉钉文档（仅支持单个钉钉表格）
* **ODPS导入：**支持导入odps表格
* **API上传：**支持通过API导入以上支持的表格文档

  


3. **上传文档：** 在选择好文档类型后，点击「下一步」进入上传环节。

* **本地文档：** 单次最多上传1个文档。行数≤5000，文件大小≤2M。

* **语雀表格：** 支持导入单个语雀表格文档链接，导入时需要提供accesstoken（[获取方法](https://aliyuque.antfin.com/lark/openapi/dh8zp4)）。

* **钉钉表格：**参考。支持导入单个钉钉表格
* **ODPS：**参考，行数≤5000

  


4. **选择文档处理方式：**

* **表格类（配置表结构&清洗）：**表格类文档按单元格进行切分，支持配置表结构

* 支持选择不同的sheet、设置表头、设置数据起始行、设置索引列（索引列内容将参与召回，非索引列内容将在索引列被召回命中后带出。索引列过多会给召回带来噪音，最多支持设置2个索引列）、支持关闭部分列（关闭后将不再参与后续处理，支持将关闭的列开启）

5. **内容预览与索引构建：** 设置完成后，点击「预览」以查看表格的表结构效果。满意后点击「下一步」开始执行内容分段和索引构建，待处理完毕即可。

  


## 查看文档信息

在表格详情页可以查看文档信息，包括：  


  


## 查看表格行信息

在表格文档详情页，可以查看每行的token数，召回次数

  


## 表管理

在单个表格文档的详情页里，展现了表的详细信息，这包括了每个单元格的内容。

进一步的操作可以通过点击右上角“管理”来执行，具体功能如下：

* **配置表结构**：用户可以通过点击右上角的“管理”按钮并选择“配置表结构”，进而进入配置表结构&清洗流程。这对于优化表格结构和提高信息检索效率非常有帮助。
* **新增一行**：此功能允许用户新增一行数据。这样，用户可以根据需要灵活管理分段信息，以便更好地匹配问答系统的效果。

至于对已有分段的管理，包括以下操作：

* **删除行**：当需要移除某个不再需要的行时，可以使用删除功能。
* **编辑行**：为了进一步优化表格的内容，用户可以对行内容。这种灵活的管理操作有利于提升问答系统的适应性和响应速度。

​  


## ​

