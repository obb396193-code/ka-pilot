
> ​[快来AI Studio接入自己的模型吧~](https://ata.atatech.org/articles/11020268403)

# **1 背景**

* 仅靠ideaLAB团队接入市面上的流行模型，难以满足集团内使用模型的需求，体现在：丰富性不够、时效性不够。

* 而且，平台模型（例如，gpt-4o-0513、gemini-1.5-pro）的算力是共享的。在请求高峰期，平台模型会出现限流的情况。

* 现在，[Workspace](https://idealab.alibaba-inc.com/ideaTalk#/aistudio/manage/personalResource)支持用户接入自己的模型（称为：私有模型），并且和平台模型一样，私有模型也可以使用提示词优化、工具、知识库等丰富功能。
* 因此，快来AI Studio接入自己的模型吧~

# **2 怎么接入？**

## **2.1 添加模型**

### **2.1.1 基础信息**

### **2.1.2 http配置**

### **2.1.3 模型配置**

#### **2.1.3.1 元信息配置**

| **名称** | **描述** | **赋值** |
| --- | --- | --- |
| supportFunctionCall | 是否支持function call | 支持填true；不支持填false  （后续，不支持function call的模型，也能通过ReAct模式使用工具了~） |
| supportStreamInvoke | 是否支持流式调用 | 支持填true；不支持填false |
| supportBlockInvoke | 是否支持块式调用 | 支持填true；不支持填false |
| maxTokens | 模型支持的上下文窗口 | 例如，gpt-4o-0513的上下文窗口为128k，那么填128000。 |
| maxPromptTokens | 最大输入token数 | 有些模型会对用户输入进行限制，例如，一次请求，最多输入4096个token，那么填4096。如果你的模型没有这个限制，但为了和平台保持一致，填上下文窗口的3/4（例如：96000）。 |
| maxCompletionTokens | 最大输出token数 | 大多模型会对模型的输出进行限制，例如，最多输出4096个token，那么填4096。如果你的模型没有这个限制，但为了和平台保持一致，填上下文窗口的1/4（例如：32000）。 |
| vectorDimension | 向量维度 | 暂不支持接入向量模型，后续支持后，填向量模型的输出维度。例如，text-embedding-ada-002生成的向量维度为1536，那么填1536。 |
| supportedMimeTypes | 支持的多模态类型 | 例如，gpt-4-0409支持用户传入图片。那么填：["image"]；gemini-1.5-pro还支持用户传入视频，那么填：["image", "video"] |
| promptPrice | 用户输入消耗的token数，每1000token数花费多少人民币 | 暂不支持私有模型收费，毕竟用户创建模型主要给自己用。 |
| completionPrice | 模型输出消耗的token数，每1000token数花费多少人民币 | 暂不支持私有模型收费，毕竟用户创建模型主要给自己用。 |

* ideaLAB团队，没有在workspace接入gpt-4-0409，我以私有模型的方式接入，元信息配置如下：

#### **2.1.3.2 参数配置**

* 目前，workspace最多支持上述参数，因此，私有模型暂时保持一致，后续ideaLAB团队会开放更多参数。
* 上述参数的说明，详见：[openai格式的入参](https://aliyuque.antfin.com/aiplus/aistudio/gurqfferzgx2w9k9#bzqWz)

### **2.1.4 配置好后，点击确定，等待私有模型探活，探活成功后顺利创建私有模型**

### **2.1.5 [重点] 遵循ideaLAB定义的输入和输出规范，实现私有模型的rest api**

#### **2.1.5.1 输入规范（遵循openai格式）**

* 和openai的参数完全一致，详见：[openai格式的入参](https://aliyuque.antfin.com/aiplus/aistudio/gurqfferzgx2w9k9#bzqWz)

#### **2.1.5.2 块式调用的输出格式 （遵循openai的块式输出格式，只需要choices、usage）**

#### 2.1.5.3 流式调用的输出格式 （遵循openai的流式输出格式）

* 与openai不同的是，在倒数第二个“data: {}”中，补充了usage。

#### **2.1.5.4 示例代码**

* 这里面复用了我设计的：**统一llm的调用接口和设计模型接入的框架**

## **2.2 编辑**

* 只有创建者才拥有编辑和删除权限，点击编辑，按需修改（允许修改模型名称）。

## **2.3 删除**

# **3 怎么使用？**

## **3.1 选择自己创建的私有模型**

## **3.2 使用上和平台模型完全一致**

* 多轮对话、使用画图工具，样样齐全

* 咋输入图片？

* 点击右上角的“问答设置”：

# **4 叠甲**

* 如果大家使用过程中遇到了任何问题，先给大家道个歉，然后我们会第一时间处理。
* 感谢大家对ideaLAB团队的支持，希望ideaLAB的产品让大家的效率更上一层楼，ღ( ´･ᴗ･` )比心。

​  


