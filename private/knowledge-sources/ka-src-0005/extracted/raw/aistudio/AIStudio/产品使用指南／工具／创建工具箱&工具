
# 操作流程

构建自定义工具使您能够将个人API接入IDEAs。空间创建的工具仅支持该空间下使用。

1. 进入工具箱管理TAB页面，点击“添加工具箱”。
2. 添加工具的方式主要分为两种 schema添加及表单添加：

* ~~schema添加方式（已下线）~~

| ~~配置项~~ | ~~说明~~ |
| --- | --- |
| ~~工具箱名称~~ | ~~可上传本地图片作为工具箱图标~~ |
| ~~schema（后续将增加表单形式的添加）~~ | ~~参考openapi规范~~[~~https://swagger.io/specification/~~](https://swagger.io/specification/) |

~~schema中单个工具主要包括以下信息：~~

| ~~字段~~ | ~~说明~~ |
| --- | --- |
| ~~工具名称~~ | ~~定义易于理解的名称。~~ |
| ~~工具描述~~ | ~~描述该工具的功能，精准的描述更有利于工具的准确使用~~ |
| ~~工具路径~~ | ~~​~~ |
| ~~请求方法~~ | ~~​~~ |

* 表单添加方式（支持HTTP和HSF）

**http工具注册**

根据表单内容，填写工具接口内容，为方便大家注册，平台提供了Curl解析的方式自动识别表单内容

  


**hsf工具注册**

根据表单内容，填写工具接口内容，会自动解析得到请求参数。

​  


  


3. 添加后，点击测试，保障工具可被正常调用

  


4. 点击确定，即添加工具箱/工具成功

​  


注：请确认工具api通过curl命令能够调用成功

常见问题：需要登陆态的api无法访问

  


# 附：schema字段详解

### 典型的工具箱schema

### 字段解释

工具schema格式参考openapi，以下是使用过程中常用字段在平台中的解释。完整openapi请参考：<https://swagger.io/specification/>

1. openapi：目前仅支持3.0.0版本，不可更改
2. info：工具箱元信息。

title：工具箱名称

description：工具箱描述

version：版本号

3. servers：服务器url信息，列表形式，但推荐使用单地址
4. 🌟paths：工具表示字段，描述工具的有效路径和操作

第一级每一项为一个请求路径，如"/queryOrder"、 "/queryItem/{id}"

第二级为请求方式，如get、post。推荐使用get，原因是目前post仅支持requestbody，大模型无法解析。

operationId：工具名称

description：工具描述

parameters：name：参数名；

description：参数释义；

required：是否必需；

type：参数类型；

in字段参考下表，目前支持path和query。

| **参数位置** | **描述** | **示例** |
| --- | --- | --- |
| path | 参数的值是 URL 操作路径的一部分 | /items/{itemId}，路径参数是{itemId} |
| query | 追加在 URL 地址之后的参数 | /items?id=###，查询参数是 id |
| header | 请求中使用的自定义请求头 | ​ |
| cookie | 用于传递特定的 cookie 值 | ​ |

​  


