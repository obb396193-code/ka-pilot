
> AI Studio技术对接运维群：25765049689

# 文档中没有回答我的问题怎么办？

## 报错问题

如果是报错，则需要定位问题。

首先了解下开发同学定位一个问题需要哪些内容？

* TraceId。调用链路的 id，开发同学可以通过 traceId 在 bsp 运维平台快速查看 sls 日志，通过日志定位到问题原因。[TraceId 怎么获取？](#K5oIS)
* 知识库id。在无法提供 traceid 的情况下，可以通过知识库 id 查看相关的 sls 日志，从而查询出错误信息。[知识库 id 怎么获取？](https://aliyuque.antfin.com/aiplus/aistudio/sfo1xmar7kv5hbb8#ECWdI)
* 请求的入参以及出参。前端报错的接口名称以及 request 请求参数、response 响应参数。通过 request 参数，开发同学可以复现请求，在预发机器上 debug 快速定位原因。

因此，在咨询问题的时候，尽量给出上述的相关内容。方便快速定位原因。

## 功能问题

如果是功能相关问题，可以进入AI Studio技术对接运维群（25765049689），群组里有产品和技术同学每日值班答疑。在答疑结束后，在该文档中及时补充 QA。

# TraceId 怎么获取？有TraceId 后用户可以做哪些事情？

一个用户的请求，可能涉及到多个微服务的调用。通过在 header 中设置 traceId，可以跟踪请求在整个系统中的交互，帮助定位请求链的各个部分。

比如，在 ideaTalk 页面，查看请求的接口，会有一个 traceId：

在代码中，请求某个接口的时候，在 response 的 header 中也会显示 traceId。用户可以打印出header 信息，从 header 中获取 traceId。关于怎么打印出 header 信息，可以将代码上下文粘贴给 GPT，GPT 帮你修改代码。

​  


在获取了 traceId 之后，用户可以自己做哪些事情呢？

在答疑群里，我们已经接入了日志查询工具。通过 traceId 可以查询到相关报错日志，大模型会根据报错日志分析出错误内容和结果。从而最快获取报错的原因和解决方法。

# 知识库收费吗，怎么收费？

知识库的切分是免费的，向量化是收费的。知识库涉及到调用向量化服务的 API 会收费。

知识库没有绑定 AK，向量化模型资源使用公共的资源池，不会收费。

知识库绑定 AK，向量化模型使用 AK 绑定的资源池，会收费。

# IDEAs应用绑定了知识库，如何让知识库召回的内容能够展示图片链接呢？

IDEAs 绑定了知识库，要保证知识库中的文档的链接是完整的。

方式一：召回方式选择原文召回。这样保证原文内容的链接是完整的。

方式二：召回方式选择文档块召回，则需要保证文档块中的链接是完整的。要保证文档块完整，则需要在文档切分的时候，选择自定义切分方式，并且不要清除文档链接。

# 知识库的 AK 怎么申请？

查看文档：

# 表格类型的文档处理时，可以每一行分成一段吗

对于 Excel、语雀表格文档，目前线上不支持按行分段。

可以通过以下方式间接满足要求：

方式一：将表格转换为 ODPS 表，将 ODPS 表授权给 ideaLab 公共账号（[ODPS 表授权](https://aliyuque.antfin.com/aiplus/aistudio/hoexgi3g3nqq43gs)），在分段时选择自定义切分方式，输入的文档块大小尽量大于文本长度。

方式二：使用[知识库 SDK](https://aliyuque.antfin.com/aiplus/aistudio/yx5fmgr4p63gfns7)或者 [知识库分段管理 API](https://aliyuque.antfin.com/aiplus/aistudio/mc76h7gen4iniyoo#JjE2t)，手动将每一行作为一个分段上传到文档。

# 知识库有大小限制吗，知识库的文档数量或者容量有上限吗？

目前没有。

# 文档转换一直显示处理中，能否帮忙处理下状态？

正常现象，文件还在等待调度

# odps 表批量调用api

curl --location '<https://idealab.alibaba-inc.com/aigc/v1/submitTextToTextBatchTask>' \  
--header 'X-AK:xxx' \  
--form 'model="gpt-3.5-turbo"' \  
--form 'ownerEmpId="xxx"' \  
--form 'outId="xxx"' \  
--form 'runMode="INSTANT_RUN"' \  
--form 'odpsProjectName="xxx"' \  
--form 'odpsTableName="xxx"' \  
--form 'dataSupplyMethod="2"'

# pdf文档或者语雀里如果有图片，是否参与召回？将来有没有可能在答案中返回图片，实现图文混排？

PDF图片、语雀图片不会参与召回。

如果知识库开启了多模态识别（联系 加白名单），则会识别文档中的链接，如果链接为图片，则会将图片转换为文本向量参与召回。

但是模型本身不会输出图片，其只可以输出图片链接，这个图片链接来源于知识库文本。

# 知识库文档的超链接是否会处理，是否参与召回？

知识库文档的超链接，如果开启了多模态识别（联系 加白名单），则会识别文档中的链接，如果链接为图片，则会将图片转换为文本向量参与召回。

如果是非图片的超链接，不会做处理，也不会参与召回。

# 切分的文本中为什么会有 code_0、image_0等占位符？

如果使用了智能切分算法，则会使用 opensearch 的 ops-text-embedding-001 进行切分。该算法会将图片、代码等使用占位符替换。

模型切分是一个黑盒子，无法解释其内部的切分结果。目前无法干预。

  


​  


