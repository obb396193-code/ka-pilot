
# 发起评测任务

1. 点击“发起评测”

2. 填写任务信息

1. 任务名称，
2. 需要评测的应用及版本（自动展示应用需要输入的变量及是否为必填），
3. 评测数据集（自动展示评测数据集的列结果）
4. 评测方式（智能评测和人工评测目前仅支持对应用生成的结果评判为采纳/不采纳，其中人工评测额外支持选择不采纳的原因）

1. 选择智能评测时，支持选择用于智能评测的模型及参数
2. 选择人工评测时，支持填写不采纳原因（也可不填），填写之后，在人工评判应用生成的结果时支持对不采纳的结果额外选择原因

3. 点击“保存”，任务状态变为“草稿”
4. 点击“提交”，提交失败，任务状态变为“草稿”，提交成功，任务状态变为“已提交”

1. 提交时会校验待评测应用和所选评测数据集列是否匹配。需要注意，待评测应用的必填输入变量必须是所选评测数据集列的子集，例子如下：

1. eg. 一个对话型应用，自定义变量为“B”且为必填，那么评测数据集至少要有Q列（用户的问题输入）和B列；如果该自定义变量“B”是非必填变量，那么评测数据集可以没有B列，有B列也允许为空。如果该应用需要用智能评测，那么评测数据集除Q列和B列外还需要有A列（标准答案列），因为在智能评测时模型需要知道标准答案采才能给出评判结论
2. eg.一个生成型应用，自定义变量为“B”且为必填，那么评测数据集至少要有B列，Q列不是必要有。其余规则同对话型应用

​  


# 任务状态说明

| 任务状态 | | 不同评测方法，任务状态流转说明 | | | 备注 |
| --- | --- | --- | --- | --- | --- |
| **主状态** | **子状态** | **智能评测** | **人工评测** | **智能评测+人工评测** | **​** |
| 草稿 | ​ | 1. 创建任务，点击“保存” 2. 点击“提交”，提交任务失败 3. “已提交”状态下，点击“撤回” | | |  |
| 已提交 | ​ | 点击“提交”，成功提交任务 | | | 已提交状态下，平台会在闲时运行评测任务（应用跑批任务or智能评测跑批任务） |
| 运行中 | ideas运行中 | 应用运行中，正在评测数据集上执行跑批任务 | | |  |
| 智能评测中 | 应用运行完成，智能评测进行中 | - | - |  |
| 人工评测中 | - | 应用运行完成，人工评测进行中 | - |  |
| 智能评测中，人工评测中 | - | - | 应用运行完成，智能评测和人工评测进行中 |  |
| 智能评测已完成，人工评测中 | - | - | 智能评测已完成，人工评测中 |  |
| 智能评测中，人工评测已完成 | - | - | 智能评测中，人工评测已完成（点击“结束人工”） |  |
| 已完成 |  | 智能评测已完成 | 人工评测已完成（点击“结束人工”） | 智能评测和人工评测均已完成 |  |

# 人工评分

1. 选择了“人工评测”评测方式的任务在应用运行完成之后会收到钉钉通知：

1. 谁会收到通知：任务创建者，参与编辑过任务信息的人

2. 点击“人工评分”，进入人工评分页面。可以把页面链接分享给非空间成员，支持非空间成员点开链接对该评测任务进行人工评分。

或者

3. 在人工评分页面，可以查看每一条评测数据集的信息，并对应用生成的结果进行“采纳”/“不采纳”打标。如果在任务发起的时候输入了不采纳原因，那么在选择“不采纳”的时候可以额外选择不采纳的原因。每次点击“采纳”/“不采纳”平台都会自动提交保存

  


  


# 评测结果

评测任务在智能评测完成/人工评测完成后会在评测结果页显示应用评分，以及其他任务运行信息

1. 评测结果AVG

1. 评测任务既有智能评测也有人工评测时：评测结果AVG = (智能评测AVG+人工评测AVG)/2；
2. 评测任务只有智能评测或人工评测时：评测结果AVG = 智能评测AVG或人工评测AVG；

2. 智能评测AVG

1. 由模型对应用生成结果进行评判（仅评判应用运行成功的结果），采纳=100，不采纳=0
2. 智能评测AVG = (智能评测为采纳的数据量/智能评测运行成功数据量)*100

3. 人工评测AVG

1. 由人工对应用生成结果进行评判（仅评判应用运行成功的结果），采纳=100，不采纳=0
2. 每一条数据人工评分 = (评判为采纳的人数/参与此条数据评分的人数)*100

1. 无人参与评分的数据分数为“-”，不参与计算人工评测AVG

3. 人工评测AVG = 每一条数据人工评分之和/应用运行成功且有人参与评分的数据量

4. 应用运行成功率=应用运行成功数/评测集数据量总数
5. 智能评测成功率=智能评测成功数/应用运行成功数 （应用运行失败的数据不会参与智能评测）

​  


​  


# 评分详情

1. 点击评测结果页的“评分详情”按钮，进入“评分详情”页面

2. “评分详情”页面会展示评分明细，包括参与评分的人（不包含智能评测模型），评测模型的分数（评测结果AVG）。下面展示具体每条数据的评测结果，包括应用需要的输入变量列，标准答案列（评测数据集有就展示），各个应用生成的答案，每个应用每条数据的智能评分、人工评分

3. 点击列表里的“评价明细”，可查看具体模型、人的评分明细

