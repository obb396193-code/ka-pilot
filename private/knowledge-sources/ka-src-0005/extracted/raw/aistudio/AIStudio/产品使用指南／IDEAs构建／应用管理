
## 概述

应用管理旨在提供一个方便易用的平台，集中管理应用的基本信息、数据看板、日志和预置QA问答对等关键模块，以便用户能够更高效地执行日常的应用迭代与维护工作。

## 管理入口

应用列表，选择某个应用，点击应用右上角“...”，选择“管理”入口进入或在调试界面点击IDEAs名称后的下拉图标

## 基本信息

应用的基本信息TAB下，目前支持大家修改应用的logo、名称、描述

这部分信息主要用于该应用在Workspace的信息展示及默认的各终端信息，应用的基本信息和终端信息独立存储，可放心修改

​  


## 数据看板

目前AI Studio针对每个应用提供了基本的数据看板，可根据渠道、时间进行数据筛选，目前开放了**活跃用户、新增用户、平均回复时长、问答数**四个维度数据

​  


​  


## 日志管理&标注

IDEAs各端（钉钉机器人、web、ideaTALK、API、页面嵌入等所有终端）问答数据可通过日志TAB进行查看及管理，并可查看每一轮对话的详细记录及节点日志

  


### 标注&标注回复

目前对话型、Flow型应用支持进行日志标注及标注回复，用于给特定日志进行打标及修改部分模型回复内容用于后续相似问题的回复。

#### 标注

在调试界面或日志界面找到对应的会话记录，并点击“标签”按钮，为当前问答打上自定义的标签。通过标签筛选，可下载对应标签的对话记录。

* 支持新增标签
* 支持删除标签
* 支持标签复用

#### 标注回复

在调试界面或日志界面找到对应的会话记录，并点击“标注回复”按钮，修改当前会话的IDEAs回复内容，修改后的问答对将存储在“预置QA”内，IDEAs开启预置QA开关后，将优先匹配预置QA内容。

* 支持添加标注回复
* 支持修改标注回复
* 支持删除标注回复
* 添加相同问题的标注回复将被覆盖

​  


  


## 预置QA干预

通过配置固定的QA问答对以达到对IDEAs问题回复的干预，一般对于IDEAs无法正确回答的内容或用户常见问题可进行QA问答对配置，从而提升回复准确率及回复效率。

