
# 节点说明

## 开始节点

开始节点是整个流程的起点，标志着流程的启动。触发流程的执行，初始化相关变量，使流程进入下一步操作。

| **开始节点预置变量（variableType=string）** | **开始节点支持的参数类型枚举** |
| --- | --- |
| * system.question：用户输入的问题 * system.sessionId：当前sessionId * system.messageId: 当前messageId * system.empId：工号 * system.empStageName：花名 * system.empRealName：姓名 | * ext : 文本 variableType=string * select：下拉 variableType=string * image：图片 variableType=array * audio：音频 variableType=array * video：视频 variableType=array |

* 如整个流程只需要用户输入文本类型的问题，开始节点无需自定义添加变量，系统已经预置了system.question用户输入的问题变量
* 当需要获取用户信息用于后续节点运行时，开始节点预置了system.empId：工号、system.empStageName：花名、system.empRealName：姓名变量、可根据自身流程需要进行引用
* 当添加图片或视频变量时，请确保流程中有相关配套节点进行图片/视频的处理。
* 每个流程有且只能有一个开始节点

> **使用示例：**
> 
> **场景：支持用户输入图片+文本**
> 
> **操作：只需添加一个图片变量，文本变量默认使用预置变量**system.question

  


## 知识库节点

知识库节点用于访问和处理存储在知识库中的信息或数据。知识库是一种结构化的存储机制，用于保存和组织各类信息（例如文档、图片、诊断数据等），使其易于查找和使用。进行信息检索，从知识库中查找和提取相关信息，例如回答用户的查询、查找文档或数据记录。

| **功能** | **说明** |
| --- | --- |
| 查询变量绑定 | 绑定变量用于知识库检索 |
| 添加知识库 | 选择空间内的知识库 |
| 设置 | 进行知识库召回配置 |
| 输出变量 | text : string |

* 每个流程可添加多个知识库节点。

  


> **使用示例：**
> 
> **场景：客服机器人，需要添加一个知识库**
> 
> **操作：**
> 
> * **在知识库节点添加空间下准备好的知识库**
> * **设置查询变量，该变量主要用于知识库内容的召回，可以设置预置变量**system.question ，通过用户的问题进行知识库的召回，可以根据实际场景添加其他变量
> * 设置知识库召回设置，具体设置参考

## 回复节点

回复节点用于产生输出，通常用于向用户或外部系统发送消息或结果。生成和发送响应，如过程中输出、最终结果输出、状态更新或通知等，可以将处理结果反馈给调用者或其它系统。

| **功能** | **说明** |
| --- | --- |
| 设置回复内容 | 自定义文本+变量混排 |

* 每个流程可添加多个回复节点，且必须添加一个回复节点。

* 中间节点的输出结果
* 最终的输出结果

  


> **使用示例：**
> 
> **场景：设置回复内容格式**
> 
> **操作：**
> 
> * **在回复节点设置固定+动态的回复内容，动态内容指具体的流程输出变量，例如：**

  


## LLM节点

LLM节点（通常代表大语言模型，例如ChatGPT）用于进行自然语言处理和生成任务。利用大语言模型进行文本生成、问答、对话，推理或者其他与自然语言相关的任务。

| **功能** | **说明** |
| --- | --- |
| 模型选择 | 新增支持回复格式及stop参数设置 |
| 提示词添加 | 支持**system、User、****Assistant**  system：定义整体框架角色  Assisent：定义模型的功能、限制等  User：用户提出问题  支持输入文本、变量混排 |
| 工具添加 | 同原有IDEAs，同时开放部分工具配置参数 |
| 记忆模式 | 同现有功能，每个LLM节点记忆模式独立设置 |
| 输出变量 | 支持自定义输出变量 |

* 开启记忆后，建议最后一个提示词以user结束。
* LLM节点中添加工具，工具的调用完全依赖模型的推理能力，添加工具后，LLM节点即一个Agent节点，针对流程可靠性要求较高的流程，建议可单独添加工具节点，实现工具节点的稳定输出。
* 每个LLM节点都有自己的记忆，这个记忆存在message的detail里，从detail字段查询过滤
* 每个流程可添加多个LLM节点。

## IDEAs节点

IDEAs节点用于将已经构建的IDEAs作为流程的一部分来引用和使用。复用已构建的流程或模块，确保代码或逻辑的一致性，减少重复工作，提高效率。

| **功能** | **说明** |
| --- | --- |
| IDEAs选择 | 选择空间下已经发布的IDEAs |
| 变量绑定 | 将IDEAs请求变量同工作流中的变量进行绑定 |
| 输出变量 | 支持自定义输出变量 |

* 每个流程可添加多个IDEAs节点,系统会进行基本的循环调用验证，请确保引用的IDEAs和本流程不存在循环调用。
* 添加的IDEAs使用添加时的IDEAs版本，IDEAs版本更新后，流程中的IDEAs版本不默认更新，需要用户手动切换版本重新发布。

  


> **使用示例：**
> 
> **场景：流程中需要引用一个已经发布完成的其他IDEAs**
> 
> **操作：**
> 
> * **在IDEAs节点在空间下已经发布的IDEAs列表中选择一个需要引用的IDEAs**
> * **被引用的IDEAs相关的入参变量会展示在配置页面，通过变量的绑定实现节点流程的连接**
> 
> * **图片、question为被引用IDEAs的变量，需要和现有Flow流程绑定，实现连接**

## 工具节点

工具节点用于调用外部工具、服务或API。执行特定功能，如数据处理、调用外部服务或接口、文件操作等，使得流程能够集成多种功能和服务。

* 使用方法同IDEAs节点
* 每个流程可添加多个工具节点

## 代码节点

代码节点用于在流程中执行自定义代码或脚本。处理复杂逻辑或特定任务，通过编程方式实现灵活的功能扩展，例如数据计算、业务规则判断等。

* codeLanguage 枚举：python3，javascript
* 输出节点类型

* boolean
* string
* number
* object
* array

* 代码执行使用阿里云函数计算( <https://help.aliyun.com/zh/functioncompute/runtime-overview-2?spm=a2c4g.11186623.0.0.7a742510zQdofc> )，根据id字段和函数关联，id生成符合一定的规则：appCode_时间戳
* 输出变量：可自定义
* 每个流程可添加多个代码节点

## 变量聚合节点

变量聚合节点用于汇集和处理多个变量或数据点。整合数据源，进行数据汇总、计算或转换，便于后续步骤处理和使用。

| **功能** | **说明** |
| --- | --- |
| 变量选择 | 选择需要聚合的变量 |
| 聚合分组 | 支持不同类型变量 |
| 输出变量 | string... |

* 输出变量：Group1.output
* outputType: string, boolean, array, object, number
* 每个流程可添加多个变量聚合节点

## 条件判断节点

**说明**：条件判断节点用于根据特定条件或逻辑进行分支选择。

**作用**：实现流程的分支和决策，基于条件评估决定执行路径，例如根据业务规则选择不同的操作步骤或流程。

| **功能** | **说明** |
| --- | --- |
| 判断设置 | if、else |
| 变量设置 | 绑定变量 |
| 设置条件 | 包含、不包含、等于、不等于... |

* 每个流程可添加多个条件判断节点
* 支持多个case
* 第一个case为 if逻辑，其他的为 else if
* logicalOperator：支持 and 和 or
* comparisonOperator 根据类型不同，支持多种

​  


## 意图识别节点

**说明**：对输入内容进行预设意图的匹配

**作用**：用户意图的识别，实现根据不同用户意图执行后续流程节点。

| **功能** | **说明** |
| --- | --- |
| 模型选择 | 选择执行意图识别的具体模型 |
| 输入变量 | 指定需要进行意图识别的query |
| 意图匹配 | 自定义用户意图，默认有个兜底-其他意图 |
| 高级配置 | 通过针对自定义的意图进行补充添加提示词，辅助模型更好的进行意图识别，补充的提示词只需要针对每个分类进行补充说明，不需要重新定义他的角色、功能和限制 |

* 每个流程可添加多个意图识别节点

## 循环节点

**说明**：对数组执行多次步骤输出所有结果

**作用**：提供局部流程对前置流程数组类型变量执行循环步骤直至输出所有结果。

| **功能** | **说明** |
| --- | --- |
| 输入 | 选择数组变量 |
| 是否并行 | 确定循环节点是否并行执行，选择并行后，循环节点内添加回复即失效 |
| 输出变量 | 绑定循环节点输出变量 |

* 每个流程可添加多个循环节点
* 循环节点输入变量必须是数组类型

## 并行节点

**说明**：触发流程中并行部分，优化流程运行效率

**作用**：可将并行执行的独立流程放入并行节点，同时执行。

| **功能** | **说明** |
| --- | --- |
| 设置并行流程 | ​ |
| 添加并行流程 | 并行流程中不同的分支流程之间不可存在交集 |

* 每个流程可添加多个并行节点
* 并行节点不存在输入输出变量

## 用户干预节点

**说明**：用于在流程执行过程中用户指令输入

**作用**：支持在流程执行过程中暂停，实现用户确认或用户信息补充输入

| **功能** | **说明** |
| --- | --- |
| 模式选择 | * 用户确认上一节点输出内容 * 用户信息补充 |
| 确认上一节点输出 | 支持用户修改上一节点输出，用户确认后可继续运行后续流程 |
| 用户补充信息 | 用户需补充节点内添加的变量信息 |

* 每个流程可添加多个干预节点
* 目前该节点暂不支持钉钉端使用

​  


## 变量赋值节点

**说明**：用于向可写入变量进行变量赋值，目前仅支持和全局变量组合使用

**作用**：通过**变量赋值**节点，你可以将工作流内的变量赋值到**全局变量**中用于临时存储，并可以在后续对话中持续引用。

| **功能** | **说明** |
| --- | --- |
| 赋值变量 | 在全局变量中选择一个全局变量 |
| 写入模式 | * 覆盖：每次运行，被赋值的变量内容将被更新覆盖 * 追加：当赋值变量为数组类型时，支持追加写入的方式 * 初始化：进行赋值变量清空操作 |
| 设置变量 | 选择流程中需要绑定的变量 |

* 每个流程可添加多个变量赋值

> **使用示例：**
> 
> **场景：IDEAs全局记忆**
> 
> * **初始化全局变量**
> 
> 创建一个 `memories` 数组（类型为 `array[object]`），用于存储用户的事实、偏好和历史记录。
> 
> * **更新记忆**
> 
> 在流程中，使用变量赋值节点，将需要存储的信息追加到 `memories` 数组中。
> 
> * **读取全局记忆**
> 
> 在流程中，将 `memories` 作为上下文，引入流程中。

  


## HTTP请求节点

**说明**：允许通过 HTTP 协议发送服务器请求。

**作用**：适用于多种场景，包括获取外部数据、处理 webhooks、生成图片以及下载文件。它使您能够向指定的网络地址发送定制化的 HTTP 请求，实现与各种外部服务的无缝集成。

| **功能** | **说明** |
| --- | --- |
| API | 支持get/post请求，内部接口需要是免登接口 |
| headers | 添加HTTP请求的头部信息。每个头部包含一个键和值。 |
| params | 常用于GET请求的查询参数。每个参数也包含一个键和值。 |
| body | 输入请求体的内容，通常用于POST请求。 |
| 超时设置 | 请求超时的设置选项，超出设定时间将终止请求。   * 连接超时  * 读取超时 |
| 输出变量 | * 配置请求的输出，包括响应体（body）、状态码（status）、响应头（headers）。 * 输出的格式可以进行设置，比如将响应体设置为字符串格式。 |

* 每个流程可添加多个HTTP请求

​  


## While循环节点

**说明**：按照设定的跳出条件进行循环执行

**作用**：通过while循环，获得满足预设条件的输出内容，执行后续节点。

| **功能** | **说明** |
| --- | --- |
| 判断条件 | 设定While中某个、多个变量的预设条件，根据该条件监控while循环跳出 |
| 最大循环轮数 | 设定while循环的最大循环次数，控制无限死循环的情况 |

* 每个流程可添加多个循环节点

# 功能设置

流程编排全局设置，通过页面右上角“功能”按钮进行相关功能设置

> 流程编排默认使用流式输出，批量运行为块式输出，API可自定义输出方式

* 欢迎语，用户打开IDEAs默认输出的欢迎文本内容
* 提示性问题，默认带出相关的提示性使用问题，用户可点击直接发送提问
* 预置QA，同原有功能，添加IDEAs相关的确定性QA

* 全局变量

全局变量为应用开发者提供了一种机制，可以在同一个会话中临时存储特定信息。这些信息在当前工作流的多轮对话中可被随时引用，如上下文、工具输出、用户偏好等。可以看作是为 LLM 提供的一个“备忘录”，避免因 LLM 记忆偏差而产生错误信息。

例如，您可以将用户在首次对话中输入的语言偏好存储到会话变量中，LLM 会参照这些变量，在后续对话中使用指定语言回复用户。

  


## ​

​  


​  


​  


