
> **文本类**和**表格类**均支持上传odps表
> 
> * 文本类上传：odps表的一行对应aistudio知识库内的一个文档
> * 表格类上传：一个odps表对应aistudio知识库内的一个文档

# 表地址填写说明

文本类：

表格类：

1. **项目名：**填写odps表所在项目的项目名称
2. **表名：**odps表名称
3. **分区：**

1. 非分区表：不需要填写分区
2. 最新分区：不需要填写分区，自动获取最新分区
3. 指定分区：需要填写分区，格式为【ds=xxxxxxxx】，获取方式见下图

4. **AccessKeyID、AccessSecret：**

1. 个人账号：填写自己的AK/SK，可以从ODPS个人信息页获取

1. 建议使用应用账号上传，避免人员离职带来数据风险（人员离职后个人账号的ak/sk将失效，导致无法更新）

2. 用户提供的AK/SK需要有上传的ODPS表的Download权限
3. 用户提供的AK/SK需要有表所在空间的createInstance权限

1. 申请地址：<https://guard.alibaba-inc.com/mark/mark.htm#/projectApply/projectAuthApply>

> 权限申请可以咨询DataWorks技术服务群：11704961

5. **endpoint：**数据中心网络接口地址，需要用户自行提供endpoint，可咨询部门数据管理员

1. 淘天集团下大部分为<http://service-corp.odps.aliyun-inc.com/api>
2. 可参考文档<https://aliyuque.antfin.com/computing-platform-doc/mc/guqfmg>

​  


## 示例

​  


# 文本类上传odps

## 表结构说明

需要在页面上和odps表结构做映射

| 列名 | 字段类型 | 含义 | 例子 | 是否必填 |
| --- | --- | --- | --- | --- |
| ID列 | string | 文档唯一id | 12345 | 是 |
| 标题列 | string | 文档标题 | 如何上传odps表 | 是 |
| 内容列 | string | 文档内容，只有文档内容参与召回 | 第一步是，第二步是... | 是 |
| 更新时间 | datetime | 更新时间（每次创建新分区的时候需要更新该字段到当前时间，否则不会更新这一行） | 20240401 11:30:00 | 是 |
| **状态列** | **string** | **文档操作(insert/modified/deleted)**  **请注意，只能用以上值或空，其他值上传会报错** | **insert** | **是** |
| 链接列 | string | 文档原链接 | www.baidu.com | 否 |
| 自定义元信息 | string | 自定义文档元信息 | 文档的作者是小二 | 否 |

## 表结构约束

1. id是唯一键，同样的id平台将当作同一篇文档
2. 每次读取的分区内不能有重复的id，否则读取任务无法提交，会报错
3. id长度不能超过32，不能为空，否则读取任务无法提交，会报错
4. 平台通过status字段来判断该文档将执行什么任务：更新/写入/删除
5. gmt_modified需要datetime格式，后续支持每日更新后会需要用到，建议填写为数据的修改时间，每日更新的时候，平台会记录上次更新的时间，只有gmt_modified大于上次更新时间的数据平台才会读取更新

## 状态列与文档操作信息说明

|  | insert | modified | deleted |
| --- | --- | --- | --- |
| 文档在知识库已存在 | 不会重复插入，非首次上传的文档不会被处理 | 成功更新文档 | 成功删除文档 |
| 文档在知识库不存在 | 成功插入文档 | 文档更新失败 | 文档删除失败 |

* 文档是否存在用id去判断
* 以上文档状态配置错误均不会导致上传任务失败或者阻塞，只会导致脏数据被丢弃

## 示例

## 预览

支持预览前10行odps表数据

  


# 表格类上传odps

* **数量限制：**行数≤5000
* **文档更新：**表格类文档上传odps表无需通过status字段控制每行数据的增删改更新操作，表格类文档上传时使用全覆盖机制

## 配置表结构

数据表：odps表的项目名+表名，不可修改

表头、数据起始行：无法修改

支持预览前10条数据

​  


# 分段&清洗，数据处理

参考文档

