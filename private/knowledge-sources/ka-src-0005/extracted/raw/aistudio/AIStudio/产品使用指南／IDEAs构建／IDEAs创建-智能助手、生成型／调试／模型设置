
### 概述

AI Studio现已集成多种主流AI模型供应商，包括但不限于阿里云的Qwen和星辰系列、Azure OpenAI的GPT系列、Anthropic的Claude系列，以及广泛的开源模型集合。由于不同供应商的模型展现出各异的性能特点和参数配置，您可以根据特定场景的需求来挑选合适的模型。目前，AI Studio通过个人账户和业务访问密钥(AccessKey）来管理模型权限。

### 模型类型

1. **大型语言模型（LLM）**：这类模型在创建IDEAs时被应用，包括多模态模型，[LLM、多模态等模型列表](https://aliyuque.antfin.com/aiplus/aistudio/gy7m97kce97gga33#iA7X)
2. **嵌入式（Embedding）模型**：这些模型在知识库中用于文档分段的 Embedding，在使用知识库的应用中，同样用于处理用户查询的Embedding。

随着市场趋势和用户需求的演变，我们计划逐步增加更多模型的支持。

  


### 模型参数配置

| **参数配置** | **参数说明（以GPT4为例）** | **模型差异** |
| --- | --- | --- |
| 惊喜值（temperature） | 取值范围为[0.0, 2.0]的浮点数。较高的值（如 0.8）将使输出更随机，而较小的值（如 0.0）将使输出更集中，更具确定性。 | **不同模型支持的参数范围、参数取值存在差异** |
| 最大输出token数（max_tokens） | 取值范围为[1, 4096]的整数，表示最大输出token数。 |
| 存在惩罚参数（presence_penalty） | 取值范围为[-2.0, 2.0]的浮点数，越大越禁止模型生成输入中包含的重复内容，用于主题发散，一般为0。 |
| 频率惩罚参数（frequency_penalty） | 取值范围为[-2.0, 2.0]的浮点数，越大越禁止模型生成整体高频词，用于避免重复生成，一般为0。 |

