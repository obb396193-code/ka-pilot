# ☁️ 基础设施与云服务概览

PICPLOT 系统的生产力很大程度上取决于底层云服务的稳定性。

## 1. 存储服务 (OSS)
- **Bucket**: 存放分镜图、配音 MP3、生成视频片段。
- **规范**: 采用 `/{biz}/{date}/{collection_id}/{file_type}/` 的目录结构。
- **生命周期**: 生产素材长期保留，临时调试图片定期清理。

## 2. 视频处理 (ICE)
- **核心组件**: 阿里云智能媒体服务。
- **逻辑**: 后端组装包含层叠逻辑（视频+音频+字幕）的 Timeline JSON，提交给 ICE 进行高性能离线渲染。

## 3. 认证体系 (BUC SSO)
- **场景**: 后台管理系统。
- **逻辑**: 集成集团 BUC 登录插件，实现工号级别的权限隔离。

## 4. 异步队列 (Redis/Kafka)
- **限流**: 使用 Redis 实现多模型调用频率限制。
- **状态同步**: 关键状态流转可通过 Kafka 消息驱动。