# ADR：素材 Brief 采用单变量变体

> 日期：2026-08-21
> 状态：采用

每个复刻变体只允许声明一个改变维度，同时列出需要保持的维度；交付必须绑定 B16 复刻谱系，回测就绪必须消费同一策略版本下的 B17 样本状态。

这样做是为了让“改了什么”和“效果变化”可追溯。多变量创作仍可拆成多个 brief 或标记 `visual_style` 的综合视觉改造，但不能把无法解释的混改冒充单变量测试。
