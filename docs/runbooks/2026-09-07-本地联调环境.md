# 本地联调环境（arch 自用；与 be/be2/fe 完全隔离）

> 目的：不等后端全做完，**先把已暴露的接口一条条打通**，把基础假设（登录态、空间、缺数三态、口径）一次性验穿；同时这套步骤就是 OS 在沙箱里要跑的步骤，等于提前替他排雷。

## 隔离方式（所以不影响别人干活）

| 资源 | arch 联调用 | 别人用 |
|---|---|---|
| 工作树 | `/Users/aik/ka-arch-gates`（detached main） | be `.worktrees/be-r010`、be2 `~/ka-be2-r014`、fe `~/ka-fe-f006` |
| 数据库 | `ka_pilot_local`（产品库，非测试库） | 各自 `ka_be_*_test` / `ka_be2_*_test` |
| 端口 | data-api **3111** | fe dev 3401；默认 3101/3102 留给部署 |
| 门禁库 | `ka_be_win_test` 等，用完即删 | — |

共用的只有 Docker 那个 PG 容器和磁盘。

## 起环境（五步）

```bash
# 1 建库 + 迁移
psql postgres://ka:ka@127.0.0.1:55432/postgres -c "CREATE DATABASE ka_pilot_local"
cd /Users/aik/ka-arch-gates/packages/db
DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_pilot_local npm run migrate      # 12 个迁移

# 2 初始化身份/空间/成员（用契约 fixture 的合成数据）
DATABASE_URL=... npm run seed:bootstrap -- "$(python3 -c "import json;d=json.load(open('../contract/fixtures/ops/bootstrap-seed.json'));d.pop('_note',None);print(json.dumps(d,ensure_ascii=False))")"

# 3 灌四渠道系数
DATABASE_URL=... npm run seed:coefficients -- '{"workspace_id":"00000000-0000-4000-8000-000000000091","effective_date":"2026-09-01"}'

# 4 生成内测账密材料（本地专用，密码 local-integration-test-only）→ /Users/aik/ka-arch-tools/local-cred.json
#   命令见 runbook「生成 scrypt 材料」，注意 scrypt 参数 N=16384,r=8,p=1,32 字节

# 5 起 data-api
source /Users/aik/ka-arch-tools/local-dataapi.sh
npm --prefix /Users/aik/ka-arch-gates/apps/worker run start:data-api
```

## 第一次联调结果（2026-09-07，main @ 1c622d0）

| 接口 | 结果 | 说明 |
|---|---|---|
| `GET /healthz` | ✅ 200 `{"ok":true}` | 只返进程存活，不漏配置 |
| `POST /auth/login` | ✅ 200 | 返身份 + 当前空间 + 两个空间；**团队空间 readOnly=true** 与 AUTH-001 一致 |
| `GET /auth/session` | ✅ 200 | 同上结构 |
| `GET /auth/workspaces` | ✅ 200 | |
| `GET /accounts` | ✅ 200 | `items:[]`、`dataState:"partial"`、`coverage.complete:false`、`dataAsOf:null`、`selectedSource:"qihang"` |
| `GET /tasks` | ✅ 200 | 同上 |
| `GET /work-items` | ✅ 200 | `dataState:"empty"`、`coverage.complete:true`（本来就没有，不是缺数） |

**三条关键行为验穿**：
1. **内部接口必须带服务令牌**（`Authorization: Bearer $DATA_API_INTERNAL_TOKEN`），不带一律 401——包括登录本身。浏览器不直连，走 BFF 加这个头。
2. **没同步过数据 ≠ 空**：账户/任务返回 `partial` + `coverage.complete:false` + `dataAsOf:null`，不是假装 `empty`。工作项才是真 `empty`。这是「缺数不补 0」在真接口上的第一次实证。
3. **空间读写权限**从 seed 的 membership 正确推出（个人可写、团队只读）。

## 下一步

- 起 web 指向 3111（不用 mock），验登录页 → 工作台 → 账户池三页的真数据链路。
- 灌一批合成 canonical 指标，验数据分析页的三态与色标。
- 后端每暴露一批端点，我就在这套环境上验一批，问题直接写信箱。

## 第二次联调：接上前端走完整链路（2026-09-07，main @ 1c622d0）

起前端：`KA_DATA_BACKEND_ORIGIN=http://127.0.0.1:3111`、`KA_DATA_SERVICE_TOKEN=<同 data-api>`、`NEXT_PUBLIC_KA_DATA_PROVIDER=internal_api`，`next dev -p 3411`。

| 链路 | 结果 |
|---|---|
| 浏览器 → `/` 未登录 | ✅ 307 跳 `/login` |
| `/login` 页面 | ✅ 200（修完字体后） |
| `POST /api/internal/auth/login`（浏览器路径，**不带**服务令牌） | ✅ 200，下发 `ka_session` cookie |
| `GET /api/internal/auth/session`（带 cookie） | ✅ 200 |
| `POST /api/internal/auth/workspace` 切团队空间 | ✅ 200，`readOnly:true` |
| `GET /api/internal/tasks` | ✅ 200，`partial` |
| `GET /api/internal/accounts` | ❌ **404（BFF 无此路由）** |
| `GET /api/internal/work-items` | ❌ **404（BFF 只有 `[id]` 详情，无列表）** |

### 发现（编号 I-xxx，进信箱）

| # | 级别 | 内容 | 派给 |
|---|---|---|---|
| **I-001** | P1 | BFF 缺 `app/api/internal/accounts/route.ts` —— 后端 `GET /api/v1/accounts` 已暴露且能用，浏览器路径拿不到，账户池页联调不了 | be2（账户域已临时移交） |
| **I-002** | P1 | BFF 缺 `app/api/internal/work-items/route.ts` 列表路由（只有 `[id]/route.ts`）——工作台今日队列联调不了 | Codex（R-010a2） |
| **I-003** | **P0（部署会炸）** | `apps/web/app/fonts/misans/misans.css` 由 `scripts/fonts/prepare-misans.mjs`（postinstall）生成且不入仓。**没生成 → `app/layout.tsx` 编译失败 → 全站 500**。若沙箱用 `npm ci --ignore-scripts` 或 postinstall 静默失败，部署直接白屏 | arch → runbook + 告 OS |

### 验穿的正确行为（不用改）

1. **两层鉴权**：业务列表接口必须**同时**有服务令牌与会话 cookie；只带令牌 401。浏览器只走 BFF，BFF 加令牌，设计正确。
2. **空间切换**：切到团队空间后 `readOnly:true` 正确从成员关系推出。
3. **缺数不补 0**：未同步过的账户/任务返回 `partial` + `coverage.complete:false` + `dataAsOf:null`，不是假 `empty`。
4. **未登录拦截**：中间件 307 到 `/login`，不泄露业务页。
