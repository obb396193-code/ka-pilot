# 给腾讯渠道同事：数据从哪来、怎么抓

> 出稿：fe（KA Pilot 前端会话），2026-09-08。
> 用途：你要做腾讯渠道的自动化取数 + BI 看板。这份文档把我们在快手渠道踩通的数据通路、字段口径、坑和管道设计全部交给你，你不用重新调研。
> 配套文档：`docs/handoff/腾讯渠道同事-前端规范与组件复用.md`（前端怎么做得和我们一样）。
> **可信度声明**：本文标「已实证」的部分来自我们对快手渠道的真实请求；标「待你实证」的部分我们没跑过腾讯，必须你先探针确认。别把我们的快手结论当腾讯的既成事实。

---

## 1. 最重要的一件事：数据不在腾讯，在奇航

我们最初以为要对接快手官方 MAPI，实读代码后发现**根本不是**。所有连接器指向同一个奇航平台的 HTTP GET 接口：

```
GET https://qh.alibaba-inc.com/qihang/api/rta_auto/tmp/get_data
  ?resource=account|account_offline|account_realtime|ad_realtime
  &userId=<从环境变量读>
  &media=KUAISHOU
  &...
```

对你的意义：**`media` 是个查询参数**。同一个接口、同一套连接器，把 `media` 换成腾讯对应的值就是腾讯的数据。你不需要另找一套接口。

这条请求链里没有 DataWorks、没有 FBI、没有 Multica。就是奇航的一个 GET。

---

## 2. 为什么你的号能拉、我们的号拉不到

这是关键，也是你能独立开工的原因。

鉴权**没有 token、没有签名、没有 API Key**。代码原文（`ks_common.py`）：

```python
ENV_JULANG_OS_USER_IDENTITY = "JULANG_OS_USER_IDENTITY"
ENV_MOZI_USER_ID = "MOZI_USER_ID"
# 无 API Key / Token / 签名机制，纯粹把身份字符串当 query 参数传给服务端
```

就是把身份字符串当 query 参数裸传。服务端**按 userId 校验数据范围**。

所以：
- 我们的 userId 授权范围里没有腾讯户，所以我们拉不到。
- 你的 userId 有腾讯户，所以你能拉。
- 换 userId 就是换可见数据范围。工程上 `resolve_user_identity()` 还支持隐藏 CLI 参数 `--user-identity` / `-u`。

**你要做的就是用你自己的身份跑同一套 skill。** 不需要找我们要任何凭证，也不需要我们授权。

顺带一句红线：任何 token / 密码 / 身份串**不要通过聊天或中转回传**。我们这边作废过一枚内部 token 就是因为回传过。你自己在环境变量里设好就行,回一句「已设置」。

---

## 3. 用哪个 skill

你 OS 里装的取数 skill 有几个变体，别用错：

| Skill | 版本 | 说明 | 你该用哪个 |
|---|---|---|---|
| `rta-data-queryer` | 1.0.4 | **通用实现**，四类 connector + `get_data` | ✅ **用这个** |
| `rta-data-queryer-daemon` | 1.0.4 | 只是守护/安装入口，业务技能是上面那个 | 装它会带上面那个 |
| `ks-data-queryer` | 1.0.2 | **快手定制**：默认 `media=KUAISHOU`，多了 502/503/504 重试和 `insights.py` | ❌ 别用，默认媒体是快手 |
| `qihang-monitor` → `ad-hourly-monitor` | 1.0.2 | 小时监控守护，依赖 `rta-data-queryer`，不是新数据源 | 做盯盘时再看 |

我们实读和真实执行用的是 `ks-data-queryer` 1.0.2。它和通用包**共享同一个 endpoint 和四类 connector**，差异只在默认媒体、重试策略和分析脚本。所以接口协议结论对你通用，但重试等实现行为要按版本分别确认。

---

## 4. 四类资源和字段字典

`resource` 四个取值，对应四张数据：

| resource | 口径 | 新鲜度 | 用来干什么 |
|---|---|---|---|
| `account` | 账户列表 | — | 有哪些户 |
| `account_realtime` | 当日实时 | **已实证分钟级**，带 `last_sync_time` | 盯盘、当日消耗、诊断 |
| `account_offline` | 离线结算（T+1 权威） | 批量分区，产出时间会波动 | 结算、财务口径 |
| `ad_realtime` | 广告级小时累计 | 分钟级，`hh` 参数 | 小时 pacing、归因下钻 |

字段照抄接口原名，不要自己改名：

**账户实时（`account_realtime`）**
| 字段 | 含义 |
|---|---|
| `account_cost` | 账面消耗（当日实时） |
| `account_exposure` / `account_click` | 曝光 / 点击 |
| `account_conversion` | OCPX 回传转化（账面口径） |
| `account_real_conversion` | **真实转化**，我们这边已核与 BI 数同源 |
| `account_cpa` | 真实转化成本，只有有真实转化才返回 |
| `account_budget` / `account_budget_usage_rate` | 预算 / 使用率 |
| `account_deduction_rate` | 扣量比例 |
| `account_main_ad_cost` / `_proportion` | 主力广告消耗 / 占比 |
| `assessment_cost` | 考核价快照 |

**离线结算（`account_offline`）**
| 字段 | 含义 |
|---|---|
| `cost_api` / `cash` / `income` / `rebate` | 结算口径金额 |
| `wake_uv` | 唤端 UV |
| `aac_ptt_uv` | 潜客 UV |
| `newaac_uv_attrib_install` | 归因新装。**只在 `biz_name=CVR` 时含义明确 = 转化 UV**，其他业务线语义待定 |

**广告实时（`ad_realtime`）**
`ad_*_h` 系列是小时累计，含 `ad_real_conversion_h` / `ad_real_cpa_h`。

**维度边界**：只有账户级和广告级（`ad_id`）。**没有计划 / 单元 / 创意的独立连接器**。账户结构要另外经 agent 同步。

---

## 5. 已实证的坑（这段最值钱，直接照做）

这些是我们真实跑出来的，不是推测。你的管道设计必须先满足这些约束，否则数据会静默错。

**① `ad_realtime` 有 2000 行静默截断**
同一范围不加过滤查询返回 2000 行；按 5 账户分片后去重并集是 2017 行。**`pageNum` / `pageSize` 被服务端忽略**。
→ 生产的 `ad_realtime` **必须带 accountIds 或 adIds，默认按 5 账户小批查**。单账户或分片结果恰好等于 2000 行时，必须阻断归因结论,不能当完整数据用。

**② 离线分区不能硬编码 T-1 / T-2**
我们观测到：`account_offline` 当次 D-1 为空、D-2 命中。但**同一天下午 D-1 又由空转非空**。
→ 说明离线重查必须贯穿日内。一次查空只能记「未观测到」,不能记「无数据」。平台**没有分区完成标记**,所以你的产品不能显示「奇航已完整产出」,只能展示最后观测时间、行数或指纹变化。
→ 动态向前探测最新已产出分区,并把**实际结算数据日**显示出来。

**③ `hh` 参数语义**
`ad_realtime` 的 `hh` 是 0..hh 的**累计快照**,不是那一小时的增量。客户端允许查 0..24,但小时桶落库仍是 0..23。`hh=25` 被服务端静默钳制。较早时点会整行省略。
→ 算小时增量要自己做相邻快照差分。

**④ 重试和频控**
只对 502 / 503 / 504 重试 3 次,2 秒起指数退避。**鉴权失败直接抛出不重试**（这是对的,别改成重试）。接口**无主动限速**,但你自己要控。

**⑤ 分批上限**
账户 50 一批,广告 80 一批,可改,自动翻页。

**⑥ 回溯上限未知**
代码里没限制,服务端实际上限未知,要实跑试探。历史 `ds` 查 realtime **已实证可以成功返回**,用于诊断回溯,但不能替代离线结算口径。

**⑦ 素材 URL 每次重新签发**
如果你要拉素材:URL 必须在同一执行链里获取并立即下载,失败就重新获取。**不要把完整 URL 放进持久队列或 checkpoint**,会过期。

---

## 6. 腾讯渠道特有的、你必须先确认的事

这几条我们**没有实证过**,但资料里有,直接影响你的看板设计。开工前先验。

**① 现金折算系数**
我们的口径里,现金消耗要按渠道折算,系数按渠道 + 生效日期版本化,**绝不硬编码**：

| 渠道 | 运算 | 系数 |
|---|---|---|
| 快手 | 乘 | 0.7812 |
| **腾讯** | **除** | **1.045** |
| 头条 | 除 | 1.09 |
| 百度 | 除 | 1.51 |

注意腾讯是**除**不是乘。运算方向按资料原样存,不要自己存倒数。

**② 一个大问题：腾讯的赔付≈消耗**
资料原话：**百度和腾讯的赔付约等于消耗,所以现金贡献≈0**。
如果这条对腾讯成立,那么以现金口径做的考核、达标、成本空间在腾讯渠道会全部趋近于零,你的看板照搬我们的现金口径可能整版没意义。
→ **这是你开工前第一个要和业务确认的事**,不是技术问题。

**③ 腾讯户能不能从这个 endpoint 出数,我们没验过**
契约里一期媒体枚举**只开放了快手**（账户列表接口的 `media` 参数一期仅接受 `KUAISHOU`,其他值会 400）。数据库层 `media` 是 TEXT 不设限,系数表里也已经有腾讯行,说明设计上是留了位的。
→ 但「设计留位」不等于「跑得通」。你的**第一个动作应该是一次最小探针**：用你的 userId、`media=<腾讯值>`、`resource=account`,看能不能返回账户列表。返回了再往下做,返回空或报错就先解决这个。
→ 顺带确认腾讯的字段名是否和快手一致。我们的字段字典是照快手接口抄的,腾讯可能有差异。

**④ `media` 的确切枚举值**
我们契约里写的是 `TENCENT`。奇航接口实际接受什么字符串,你探针时确认。

---

## 7. 建议的管道设计（照我们的架构,已验证可用）

不要直接从 skill 取数喂给前端。中间必须落库,分三层：

```
奇航 get_data
    │
    ▼
① 原样落库 metrics_raw          ← 接口原始行,一个字都不改
    │   · 按 ds 分区
    │   · 存 request_params（hh / 日期范围等）→ 保证可重放
    │   · resource（account|account_offline|account_realtime|ad_realtime）
    │   · source（realtime|offline）—— 和 resource 正交
    ▼
② 算成 canonical account_metrics_daily   ← 一户一日一行
    │   · cost / exposure / click / conversion / real_conversion
    │   · cash_cost / cash_cpa / cost_space / gap
    │   · field_sources JSONB ← 每个字段是 offline 还是 realtime 还是补的
    │   · data_anomaly 标记
    ▼
③ 每日对平自检 data_quality_checks
        · 存 sample / passed / delta
        · 你的「数据健康」页读这张表
```

配套两张运维表：
- `etl_runs`：每次跑的记录,状态 / 入库行数 / 失败在哪一步 / 错误摘要。你的运行监控页直接列它。
- `backfill_jobs`：回填任务。状态机是 `running → raw_done → canonical_done → done`,任一阶段失败置 `failed` 并留 `failed_stage`。**`done` 只在三个阶段对全部日期都终态后才置**,别提前置。

**为什么要原样落库**：奇航是唯一数据源且没有完成标记,一旦你只存算好的结果,数据对不上时你无法复现。存了 `request_params` 就能精确重放当时那次请求。

**`field_sources` 为什么重要**：同一天的同一个户,消耗可能来自实时、也可能来自离线,两者口径不同。不记来源,后面就说不清这个数为什么和财务对不上。

---

## 8. 指标口径（照抄,别自己推）

**公式冻结,前端和 agent 永不自算**,一律后端算好返回：

```
ctr        = click / exposure                 分母 0 → null
cvr        = conversion / click
real_cpa   = cost / real_conversion           账面 CPA,只展示,不用于考核
cash_cost  = (账面消耗 − compensation) ⊕ coefficient      ⊕ = 乘或除,按渠道系数表
cash_cpa   = cash_cost / real_conversion
cost_space = assessment_price × real_conversion − cash_cost
gap        = conversion / real_conversion − 1
潜客率      = aac_ptt_uv / wake_uv
BI 转化率   = real_conversion / aac_ptt_uv
消耗速度    = 近 1h 消耗 / 1h
断量倒计时  = balance / 消耗速度      单位小时
```

**考核全部是现金口径**。考核价、达标、成本空间都用 `cash_cost`,不是账面 `cost`。账面 CPA 只做展示。我们的做法是账面消耗和现金消耗两张卡并排放,让优化师看得到差。

**窗口聚合要先聚合再相除**,不是把每日比率求平均：

```
cash_cpa(窗口) = Σ cash_cost / Σ real_conversion
```

达标判定按生效版本逐日取考核价,价格改动期内各日各用各的。

**三态语义必须分开,这是硬门禁不是审美**：
- `0` = 确认有数据且值为零
- `null` 或 `−` = 没有有效值或不适用
- 「暂无数据」= 当前筛选没有记录
- 「数据更新中」/「部分数据」/「已过期」= 值可能变
- 「无权限」= 不能用空态或 0 伪装

我们用 `MetricValue = {value, availability: available|missing|error}` 承载,比率用 `RatioValue = {value, state: finite|infinite|undefined}`。**只有 `availability=available` 的 0 才是真 0。**

---

## 9. 接口层怎么设计（安全相关,建议照做）

**浏览器绝不直连数据源。** 我们的做法：

```
浏览器 → Next BFF（同源）→ Data API → 奇航
         ↑ 只发 {queryId, params}    ↑ 服务端 token
```

要点：
- 浏览器只发 `{queryId, params}` 两个字段。**禁止提交 SQL、表名、列名、自由表达式、workspaceId、userId**。未知字段直接 400。
- 查询走**白名单 Registry**,每项冻结参数 schema、支持视图、最长日期范围、最大行数、账户范围、模板版本。
- `workspaceId` / `userId` / 可见账户**只能从服务端认证上下文注入**,不接受浏览器传。
- 服务端 token 和身份串**不得进入浏览器 bundle、页面日志或响应正文**。
- 每个数据响应必带来源标识：来源 / 指标版本 / 数据日期 / 时区 / 日切。前端页头常显,让用户一眼看出数字变化是换源不是算错。

如果你就一个人用、内网跑,这套可以简化,但**「浏览器不直连、不接受浏览器传身份」这两条别省**。

---

## 10. 你的第一周建议这么走

1. **探针**：用你的 userId + 腾讯 media + `resource=account`,确认能返回账户列表。同时核对字段名和我们的字典有没有差异。**这一步不通,后面全白做。**
2. **确认赔付口径**：腾讯赔付是否≈消耗。这决定你的看板要不要做现金口径。业务问题,先问人。
3. **落 raw**：先只做第一层,原样落库 + 存 request_params。别急着算指标。
4. **对一天数据**：拿一个成熟日,手工核对 raw 里的数和你在奇航页面上看到的数。对上了再往下。
5. **算 canonical**：加 `field_sources`,把每个字段来源记清楚。
6. **再做前端**：这时候才动看板,照配套的前端规范文档做。

顺序反了最容易返工的是第 4 步。我们的教训是 **mock 测通不等于真接口通**,放行标准必须是真 payload 打真端点。

---

## 11. 我们还没解决的,你别指望现成答案

- 奇航回溯上限服务端实际是多少,未知。
- `newaac_uv_attrib_install` 在非 CVR 业务线的语义,文档原话是「待定」。
- 平台无分区完成标记,所以「数据是否完整」永远只能是本产品的稳定性判断,不是平台保证。
- `account_real_conversion` 与 BI 是否严格同源:我们这边老板已核认为同源,但源码只能证明字段由奇航返回,**不能证明上游业务血缘**。进正式结算前必须逐字段、逐数据日对平。你也一样。
- 素材 / 文案池 ID 没有发现接口,必须人工配置或由上游任务资产提供。
- 没有平台字幕接口。
