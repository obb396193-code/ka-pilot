# F-007 前端全铺开提示词（老板复制给 Claude Code 前端会话；2026-09-06 arch）

> 接续 F-006。老板 9-5 拍板：**契约已全部冻结，前端把所有页面一次铺开，老板逐页精修，后端并行做，最后按 fixtures 联调**。视觉规范以你和老板已定的为准（放 `docs/frontend/design-spec.md`，没有就先建）；本提示词只管功能与纪律。

```
你是 KA Pilot 的前端（Claude Code 会话）。现在的任务：按 docs/relay/F-007-全站页面清单.md 把全站页面一次铺开，连续做，不等审。

先读（顺序）：
1. docs/relay/F-007-全站页面清单.md —— 每页做什么、用母版哪块、主 DTO 在 api.md 哪节、fixture 在哪、八态怎么显、验收句
2. docs/relay/F-006-页面规划.md —— 母版六块解剖与工作台/账户池/任务三页的区块级规划（已按原型重写）
3. packages/contract/api.md + metrics.md —— 契约；packages/contract/fixtures/ —— 样例 JSON（fixture 即契约，mock 层直接读）
4. docs/prototypes/v2/P01~P16.png —— 功能参考（布局/信息组织参考它，视觉不参考它，视觉按 sidebar.html + 设计规范）
5. apps/web/AGENTS.md + docs/frontend/ui-assets/agent-workflow.md —— 组件先查库再取，不自写
6. docs/decisions/2026-09-05-全量偏差审计.md —— 知道哪些地方之前偏了、现在怎么定的

四条硬边界（不变）：
- 组件先查 docs/frontend/ui-assets/（17 库 7,056 条目录 + showroom + source-cache），从官方源码复制，不自写
- 视觉起点 apps/ui-layout-demo/sidebar.html，是起点不是天花板；具体视觉听老板
- 前端永不算数：所有指标/比率/达标/色标来自 DTO，缺数显 −，不用 0 代
- 只动 apps/web（除 app/api 与 lib/data/ 数据契约层，那两处归后端）；契约缺口写 docs/relay/inbox-arch.md「F-007 契约缺口」，不自造字段

做法：
- 顺序：登录 → 数据分析 → 账户池（+详情/基建/开户测试）→ 工作台（+负责人视图/协作 tab）→ 投放任务（+详情八页签）→ 自动化（+画布/运行详情）→ 报告 → 集成与通知 → 知识库 → 商品素材 → 设置 → 治理后台 → 全局 Agent 抽屉 + ⌘K
- 每页定义"做完"：壳 + 八态 + 接 fixture 的 mock + 页内交互（筛选/分组/tab/抽屉/表单校验）+ 空态示例角标 + 状态文件打勾 + 一次路径限定 commit（[fe] 前缀，git commit -- apps/web/...；不 push）
- 视图收敛：子功能做页内 tab（/data?tab=pivot），不加侧栏项、不开一级路由；详情页才二级路由
- 示例态：数据源未接或 P2 的区块显「示例」角标 + 降饱和 + 一句解锁条件；绝不造真实感假数据；"AI 提效"用词是"操作后观察结果"
- fixture 缺的：按 api.md 自造最小 mock，状态文件写 TODO-fixture:<路径>，继续做，arch 会补
- 每页页头常显：空间 · 来源 · 数据日期 · 更新时间 · 口径 ⓘ（DATA-ROUTE-001）
- 画布用 React Flow，节点模型按 api.md「workflow-graph/v1」；知识库复制 ContentRadar 代码（/Users/aik/cr-r128 只读复制，不改原仓）；Agent 抽屉用 Vercel AI Elements
- 不做：Storybook/Playwright 视觉回归/字体（老板单独拍）；不改导航九项

节奏：
- 一页做完立刻下一页，不问"要不要继续"；老板会按页来精修，精修意见直接改
- 每天结束把 docs/plans/F007-状态.md 更新到当前页；卡住超过 30 分钟的问题写进状态文件"阻塞"，跳过做下一页
- 五页（登录/数据分析/账户池/工作台/任务）做完发一次 SHA 给 arch 验功能；其余页做完再发一次

本地起法（mock）：
cd apps/web
NEXT_PUBLIC_KA_DATA_PROVIDER=mock KA_DATA_DEV_AUTH_CONTEXT_ENABLED=true \
KA_DATA_DEV_WORKSPACE_ID=00000000-0000-4000-8000-000000000024 KA_DATA_DEV_USER_ID=demo-user \
KA_DATA_DEV_ACCOUNT_SCOPE_JSON='[{"media":"KUAISHOU","accountId":"demo-account-07"}]' npx next dev -p 3400

纪律：[fe] 前缀、路径限定 commit、不 push、不动 app/api 与 lib/data；改风格不动导航项/路由/数据形状；状态文件 docs/plans/F007-状态.md 逐页更新。
```

## 老板看每页时的判据（arch 给老板，非提示词）

| 看什么 | 怎么判 |
|---|---|
| 功能对不对 | 对照 F-007 清单的"验收句"和原型图 P0x；差的直接口头改 |
| 数据对不对 | 现在全是 fixture，只看字段有没有、缺数是不是 −、色标是不是三色 |
| 空态 | 有没有「示例」角标和解锁条件；有没有假数据 |
| 视觉 | 你们俩的设计规范 |
| 不用看 | 数字本身、性能、联调 |
