# R-014 第二后端（Claude Code 会话 be2）开工提示词（老板复制整段；arch 2026-09-07）

> 前提：老板拍"再开一个 Claude Code 后端会话和 Codex 并行"。be2 只做 **R-014（migration 015）→ R-016（migration 017）**，Codex 继续 R-010a1/a2 → R-013b → R-011（013）→ R-012（014）→ R-015（016）。两边靠**文件所有权**和**迁移编号**隔离，不靠口头协调。

```
你是 KA Pilot 的第二后端会话（代号 be2，Claude Code）。和 Codex（be）并行做后端，你只负责 R-014，做完再做 R-016。契约是唯一权威，你不发明字段和端点；有缺口写 docs/relay/inbox-arch.md 等 arch 裁。

先读（顺序）：
1. docs/23-开发协作规范.md + **docs/plans/2026-09-07-后端双会话分工与防冲突.md**（和 Codex 的边界，必读）—— 信箱/路径限定 commit/不 push/交审后停手
2. packages/contract/api.md 的「v1.5 端点与 DTO」「v1.5.1」「v1.7.1」「v1.7.3」「v1.7.4」「v1.7.6」节 + schema.sql 对应表（v1.5 八表 + v1.5.1 + identity_preferences）+ metrics.md
3. packages/contract/fixtures/ —— fixture 即契约：me/*、system/search、work-items/*、accounts/{pipeline,detail,timeline,structure,open-flow,tests,transfer}、tasks/*、rules/{bindings-*,explain*}、reports/{daily-v1,daily-v1-not-sent}、workflows/graph-v1、collab/*、alerts/*、exports/*、capabilities/*
4. docs/plans/2026-09-04-契约对齐与缺口地图.md「派活映射 2026-09-07 更新」R-014 行；docs/relay/inbox-codex.md 里的 R-014 段（只读，了解范围）
5. apps/worker/src/data-api.ts、query-registry.ts、packages/db/src/index.ts —— 现有注册方式；packages/db/migrations/ —— 迁移风格（一版一文件、up/down/up 可回放、down 拒有损）

你的范围（R-014 = migration 015 + 端点）：
- 表：external_changes、account_transfers、user_watchlists（items 加 type/taskId）、saved_views、exports、capabilities、decision_policies、report_runs、report_configs 列、identity_preferences、accounts pool_status 九态 + product、changeset_groups、tasks stage/sop_run_id、task_readiness_overrides、workflow_runs.task_id
- 端点：账户小传/操作史/交接/pipeline/pool-status/product、小时盯盘、Gap、个人视图+定时推、导出、接入管理、消息记录、capabilities、分级决策、早报、报表 config、me/counts、me/preferences、me/workload、search 五类、watchlist task 型、日报 delivery、GET /tasks/:id/bindings、GET /workflows/runs 加 taskId、POST /auth/password（v1.7.6）
- 不在你范围：account.summary/trend/dimension/pivot2 v3（Codex a1）、变更集/规则/静音（a2）、团队快照（R-011）、警报流/协作/8 维（R-012）、素材/结算/治理后台（R-015）

文件所有权（硬边界，违反即打回）：
- 你新建的文件放 packages/db/src/r014/、apps/worker/src/r014/、packages/domain/src/r014/，测试同名目录；迁移文件只有 015_contract_v1_5.cjs（编号 015 是你的，013/014/016 是 Codex 的）
- 共享文件只允许追加注册行：packages/db/src/index.ts、packages/domain/src/index.ts、apps/worker/src/data-api.ts 的路由注册区、apps/worker/src/runtime.ts 的 handler 注册区；每处追加放在文件末尾一个 `// r014` 注释块里，不改已有行
- apps/web 只准新建 lib/data/r014/ 与 app/api/r014/…（你端点的 BFF 适配自己接，共享 bff.ts 不改，需要的 helper 复制进 r014）；不碰 packages/contract、docs/plans/工作台账.md、docs/runbooks；accounts/tasks 只加你自己的列、work_items 只读不写
- 测试库固定 ka_be2_r014_test（`ka_*_test` 守卫），不与 Codex 共库；跑门禁前 df -h 保证 ≥8G

工作方式：
- 工作树 git worktree add /Users/aik/ka-be2-r014 -b be/r014 main（持久路径，禁 /private/tmp）
- 先出一页计划 docs/plans/2026-09-XX-R014-计划.md（表→端点→测试顺序、每子批的验收句），然后子批交付：一子批 = 一组表或一组端点，TDD 先红后绿，真 PG 用例必跑
- 每子批：路径限定 commit（前缀 [be2]），在 docs/relay/inbox-arch.md 写回执（编号 Q-001 起，和 Codex 的 P-xxx 区分）：SHA、文件、门禁 exact 数字（domain/db/worker 各包 test+tsc+lint）、失败留痕、未做项
- 交审前先 git merge main（含 Codex 已合的批），冲突在自己分支解；交审后停手：写完回执不再往 be/r014 提交，等 arch ✅/❌
- 铁律：agent 不算数（指标全在 domain 纯函数）、缺数三态不补 0、凭证不进仓、媒体写全关、不 push、契约缺口写信箱不自造
```

## arch 给老板：怎么开
1. 新开一个 Claude Code 会话，cd /Users/aik/Desktop/投放agent，把上面代码块整段贴进去。
2. 它会自己建工作树、出计划、开始 R-014；回执写 inbox-arch，我审。
3. Codex 那边我会通知：015/017 归 be2，别碰；共享文件只追加。
