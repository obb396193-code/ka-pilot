# 落地路径与鉴权（关键突破）

> 建立：2026-08-13，来源：OS agent 三轮问答（含实测 `a1` 二进制）
> 这份文档回答的是全项目最卡的问题：**产品跑在哪、数据怎么合法拿到、用户怎么登录。**

## 1. 先确认一件事：OS 拒绝借 token 是对的

OS 明确拒绝导出 MULTICA_TOKEN / 业务 CLI 凭证给外部产品，理由是这些是绑定沙箱会话 + agent 身份 + 具体任务的短期凭证，转借会绕开按身份审计的设计、泄露风险不可控、大概率违反使用条款。

**我认同，而且我们不该要。** 建立在借来的凭证上的产品：随时因 token 过期或轮换而全线瘫；一旦被发现就是"业务侧绕权限"，博弈直接输掉，比慢几周严重得多。

**结论：架构必须建立在「我们自己的合法服务身份」上。** 这不是妥协，是唯一能拿去谈的形态。

## 2. 四条路径与判断

OS 给出的可行路径：

| 路径 | 机制 | 我的判断 |
|---|---|---|
| **A. a1 faas 部署内部应用** | `a1 faas`（阿里内部统一研发 CLI，实测 v0.2.46）建应用 → build → deploy，拿 `ACCESS_URL`；正式链路走 AoneFaaS 函数组 + 代码仓库 + `a1 faas publish <env>`；配套 config vars / logs / health / metrics | **★主路径。** 应用有自己的身份、自己申请数据源凭证、边界清楚、在公司体系内合规、天然可给多人用 |
| B. Multica issue/评论 | 外部产品建 issue / @agent，agent 用自己凭证查，结果回写评论/附件，外部再读 | 只适合偶尔查、能接受异步；不能做产品主链路 |
| C. autopilot webhook | `multica autopilot trigger-add --kind webhook` 生成 `/api/webhooks/autopilots/{token}`，POST 触发一次 agent run | **是触发不是请求-响应**，异步、无同步返回值；且 OS 不确定 POST body 能否传参进 run 上下文。适合事件驱动，不适合查数据 |
| D. port-mapping | 沙箱内服务映射临时公网 URL | 只在会话存活期有效，仅开发期调试可用 |

**采用 A 为主路径，D 做开发期验证，C 留给未来的事件驱动场景，B 不用。**

## 3. 为什么 A 一举解决三个难题

**① 凭证问题** → 应用以自己的服务身份申请快手 MAPI / FBI 数据权限，不借任何人的 token。
**② 合规问题** → 之前担心"产品跑在沙箱外＝在 mitmproxy 审计体系之外处理公司数据"。跑成内部 FaaS 应用，数据不出公司基建，这个担忧基本消解。
**③ 多用户问题** → 内部应用可以接公司统一登录（BUC/域账号 SSO）。**我们不该自己造账号体系**——自建密码库不仅是重复劳动，还是合规风险。用域账号登录，还自带组织架构（谁是谁的上级），正好喂给 04 文档的分发功能和数据可见范围。

## 4. 硬约束（OS 明确说的）

- `a1 faas deploy` 目前**固定发布到日常环境（daily）**，不是生产入口。生产要走 AoneFaaS 函数组 + `a1 faas publish <env>`，需确认权限与流程。
- `a1 faas register` 当前被临时禁用。
- `create aone-platform` 有跨系统副作用（新建代码仓库 + 新建应用），OS 会强制二次确认应用名、Code group、project、脚手架。
- **这一步会留下组织足迹**：建了仓库和应用，项目就从"私下捣鼓"变成"公司里有登记的东西"。博弈上是好事（成果可见、可汇报），但意味着不再隐身。**需要老板拍板时机**。

## 5. 修订前面的结论

| 原结论 | 修订 |
|---|---|
| 02 文档：卡点＝kuaishou-cli 能否拷到沙箱外用 | **不再是主路径。** 正解是 FaaS 应用申请自己的数据源权限，而不是搬别人的 CLI 二进制 |
| 02 文档：产品跑沙箱外有审计合规隐忧 | 走 FaaS 后基本消解 |
| 03 文档：要做多用户鉴权登录系统 | **改为接公司 SSO（BUC/域账号），不自建账号体系。** 需求本质仍是数据可见范围，不是登录本身 |
| 01 文档：先做出来再博弈 | 次序不变，但"做出来"的形态更清晰：先在 daily 环境跑起来给自己和身边人用，拿实效去谈生产环境和数据权限 |

## 7. 架构定稿（2026-08-13 依可行性报告修订）

**新实证（`docs/09-可行性评估报告.md`）推翻两处旧结论：**

| 旧结论 | 新事实 |
|---|---|
| §4 判断"A FaaS 是主路径、B issue 不用" | **取数统一走 webhook/issue 异步模型**——报告 D 推荐架构与决策记录 `2026-08-13-异步ETL落自有库.md` 完全同构 |
| §3 的"kuaishou-cli 可能可移植"（02 文档遗留） | **彻底排除**：CLI 不落地真实 token，真 token 由沙箱 MITM 代理 + 启航服务端动态注入，公网不可达。**所有取数必须经 agent，无第二种可能** |

**终局链路**：

```
外部 Web 应用(我们的产品)
  ├─ 只读/ETL: POST autopilot webhook → Multica 派发 agent
  │             → agent 跑 kuaishou-cli(沙箱内, 凭证由沙箱注入)
  │             → 结果写 issue comment → 我方用 mcn_ PAT 轮询读取 → 落自有库
  └─ 写操作:   两次 webhook(预览 dry-run → 用户确认 → 执行),保留确认门
```

**四条安全红线（OS 原文，直接进 AGENTS.md）**：
1. 快手真实 token 提不出沙箱——设计上就不落盘，我们也不需要。
2. **禁止绕过确认门下发写操作**——那是主动破坏安全设计，不是能力缺失。
3. 沙箱出网流量绕不开 MITM 审计。
4. 公网直连 cloud-cli 端口不可行；port-mapping 仅临时。

**两条工程注意**：
- kuaishou-cli **无请求级幂等键，创建广告重复调用会重复创建**——ETL/执行层必须自己管幂等。
- CLI 能力以代码为准：README 的 raw 透传子命令实际没实现；**新命令必须先实测再采用，不照文档**。

**demo 预计**：有 workspace 的 `mcn_` PAT + 配置好 autopilot 的前提下，**1-2 天跑通只读 demo**（webhook 触发 + runs 轮询取结果 + trigger_payload 传参）。这是从"文档阶段"到"手里有真东西"的最短路径。

自建钉钉网关的架构文档把其中一半问题**实证回答了**：

| # | 问题 | 状态 |
|---|---|---|
| 1 | FaaS 能不能拿快手数据读权限 | **部分已答**：现有网关已通过 `ks-data-queryer` skill 拿到数据（经 agent 中转）。未答：能否绕开 agent 直连底层服务 |
| 2 | BI 真实转化能不能拿到 | **★已答：能。** 数据源里 `conv_real` 与 `conv_report` 是两个独立字段，skill 直接返回。业务价值地基不再悬空 |
| 3 | FaaS 能否托管前端 | **已答：能，且能跑无头 Chromium 服务端渲染。** 常驻 Koa 服务 + `custom.debian10` 运行时实测可行 |
| 4 | 有没有数据库可申请 | 仍未答。现有网关是内存态（`conversationSessions` Map），无持久化——**这正是它只能当问答机器人的原因** |
| 5 | 出网策略 | 部分已答：能连钉钉开放平台与 Google Fonts CDN（构建期）。快手公网 API 未验证 |
| 6 | 能否复用 Multica issue 做任务底座 | **已答：外部服务能用 `mcn_` 令牌 + CLI 建 issue／读评论。** 但一问一答 + 8 秒轮询的架构限制，不适合当产品级任务系统 |

**新增关键答案**：`mcn_`（Cloud Node）令牌是官方为**外部服务**签发的令牌类型，`mul_` 绑个人、`mat_` 绑单次任务且外部不可用。**这就是"合法调 OS 而不借任何人凭证"的正门**，与本文档 §1 原则完全兼容。

**2026-08-13 第三次修订**：可行性报告（09 文档）进一步确认 `mcn_` 为**长期机器凭证**、webhook 触发端点已存在（两种模式）、`--dry-run` 可用于预览。同时确认 `trigger_payload` 传参实测为空、Multica 无公开 OpenAPI——这些作为待申请/待确认项，见 §7。
