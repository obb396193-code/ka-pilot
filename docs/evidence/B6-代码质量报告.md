# B6 分析与报表内核 · 代码质量报告

> 日期：2026-08-19
>
> 功能审查基线：`be/b5` / `53ea264`
>
> B6 功能审查 SHA：`6dc7ed1`
>
> 检查范围：B6 新增 domain/db/worker 代码及 B1a-B5 全量回归。所有数据均为脱敏测试数据；真实 Provider、奇航、Multica/OS、钉钉推送不在本报告验证范围。

## 1. 结论

- Critical：0
- High：0
- Medium：0 个阻断项
- Low：1 个既有依赖提示（npm 安装提示间接依赖 `glob` 旧版本弃用，但四包 `npm audit --audit-level=high` 均为 0 vulnerabilities；B6 未新增该运行时依赖）
- 质量门禁：通过

## 2. 变更范围

B6 功能代码：

- `packages/domain/src/report-plan.ts`
- `packages/domain/src/report-dataset.ts`
- `packages/domain/src/gap-reconciliation.ts`
- `packages/domain/src/strategy-analysis.ts`
- `packages/db/src/report-facts-source.ts`
- `apps/worker/src/reports/types.ts`
- `apps/worker/src/reports/report-generation-handler.ts`
- `apps/worker/src/reports/index.ts`

新增生产代码共 1,038 行；最长文件 `report-dataset.ts` 263 行，低于本项目 300 行关注线。B6 未修改 `packages/contract/` 和 `packages/db/migrations/`。

## 3. 测试与覆盖率

| 包 | 默认测试 | 行覆盖率 | 结果 |
|---|---:|---:|---|
| `@ka/domain` | 110 | 94.72% | 16 files passed |
| `@ka/db` | 64 | 92.60% | 15 files passed；真实 PostgreSQL 16 |
| `@ka/worker` | 111 | 90.54% | 30 files passed；1 opt-in skipped |
| `@ka/dingtalk-gateway` | 19 | 86.62% | 5 files passed |
| 合计 | 304 | 各包均 >80% | 通过 |

B6 新模块覆盖率：

- `report-plan.ts`：95.86%
- `report-dataset.ts`：97.51%
- `gap-reconciliation.ts`：100%
- `strategy-analysis.ts`：98.00%
- `report-facts-source.ts`：92.79%
- Worker `src/reports`：93.23%（handler 94.65%；纯 re-export `index.ts` 不计业务覆盖判断）

额外 opt-in 回归：

- `KA_RUN_AGENT_SDK_SMOKE=1 npm test -- --run test/agent/sdk-gateway-smoke.test.ts`
- 结果：1 passed；真实 Claude Agent SDK 子进程 → localhost gateway → fake OpenAI SSE，耗时约 9.4 秒。
- 该结果仍不代表真实 IdeaLab/Anthropic/Multica 生产通路通过。

## 4. 数据与迁移

- `SemanticReportFactsSource` 专项 PostgreSQL 测试：3 passed。
- DB 全量：64 passed。
- migration test 包含 drop/replay、重复执行、down 3/up 3，已通过。
- `git diff --exit-code be/b5..HEAD -- packages/contract packages/db/migrations`：无差异。
- 无新表、字段、索引、job_type 或公开 API。

## 5. 类型、规范与复杂度

- 四包 `npm run typecheck`：全部通过。
- 四包 `npm run lint`：全部通过。
- B6 八个生产文件附加 ESLint `complexity <= 10`：0 warning。
- `git diff --check be/b5..HEAD`：通过。
- 生产文件长度：2-263 行，无文件超过 300 行。

## 6. 安全检查

- 四包 `npm audit --audit-level=high`：全部 0 vulnerabilities。
- B6 生产文件扫描：无硬编码 API key、PAT、密码、私钥、Bearer Token、`mul_`/`mcn_` 凭证。
- 无 `eval`、`new Function`、Shell、任意子进程、动态 SQL 或字符串插值 SQL。
- Agent/未知输入只能进入严格 `b6-internal-v1` Zod Schema；SQL、公式、脚本、URL、预计算 value/rows、schedule、sharing、layout 均拒绝。
- Plan/Facts 双重 workspace 检查；跨租户 fail-closed。
- Worker 失败日志只写稳定错误码和固定安全消息，不回显 Agent 草稿、Provider key 或存储错误详情。
- Artifact 以 workspace/report/plan fingerprint/asOf 生成 SHA-256 幂等键；相同重试返回 duplicate。

## 7. 正确性与性能检查

- 比率沿用 B1c 聚合结果，未平均行级 CTR/CPA/Gap。
- `finite|infinite|undefined|missing` 四态保留，缺失不改写为 0。
- 相同 dimension 在单次报表内只查询一次，避免重复查询；summary/trend 未使用时不调用。
- 组件局部 missing/empty 不污染其他组件。
- 策略账户数用 Set 去重；CPA 从总消耗/总真实转化重算。
- 策略 winner 只来自 ≥3 户、消耗 ≥100 且 CPA finite 的 cell；并列稳定排序。
- 报表生成未注册进生产 Worker runtime，不会在契约未冻结时被误触发。

## 8. 尚未完成和审查重点

以下是明确边界，不是本报告遗漏：

- 公开 report config/API DTO、布局、共享和官方资产权限未冻结；
- report artifact/snapshot 最终存储未决定；
- schedule 时区、补偿、订阅、导出和真实钉钉推送未实现；
- 版位、出价方式、负责人等策略维度没有可靠落库字段，未接生产查询；
- B6 Worker 未注册 job_type，也未接 Web/Agent 公共工具；
- 真实 Provider、Secret、奇航、Multica/OS 未联调。

Claude/arch 应按 `inbox-arch.md` P-010 逐项审查和裁决，再进入公开集成。
