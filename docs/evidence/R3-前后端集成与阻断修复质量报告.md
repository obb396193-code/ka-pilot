# R3 前后端集成与阻断修复质量报告

日期：2026-08-25  
状态：`integrated / local_verified / codex_prechecked`  
分支：`codex/integration-control`

## 已集成

- 后端终批：`codex/dual-data-backend@2983987`（代码终态 `391a5a2`）。
- Neutral 前端终批：`codex/fe-vertical-slice@776e48b`。
- 知识资产终批：`codex/shared-source-library@5abead4`；合流时补修中文路径下测试 URL 解码问题。
- Claude 仅保留后续复审席位，不作为实现、内网联调或部署前置门。

## root 终审修复

1. PostgreSQL present-invalid/non-finite numeric 不再转成 `null`；Repository 抛契约错误，Platform 统一转 `UPSTREAM_INVALID_RESPONSE`。
2. KA trend 必须所有返回日都覆盖请求账户才可声明 complete；任一天缺户即 partial，并收回 whole total/returnedObjects 结论。
3. workspaceId 在 HTTP、Service、Repository、Canonical Row、详情 DTO 和前端统一为 UUID。
4. Work-item/change-set 服务与 BFF 双层复核 path ID、workspace 及 `(media,accountId)`。
5. 删除 `KA_DATA_DATASET_VERSION` / `PLATFORM_DATASET_VERSION` lineage fallback；版本只能来自上游或持久化查询元数据。
6. Platform summary 与 lineage account count 不一致时 fail closed，不能误判 `source_missing`。
7. 007 单独执行自带 5 秒 lock timeout、5 分钟 statement timeout，并补停写运行手册。
8. Data BFF 对所有 canonical daily rows 同时校验 workspace 与批准 media-account tuple。
9. 前端补齐后端 reconcile semantic refine、requestId 1–128 日志安全字符规则和真实详情 response wrappers。

## 最终门禁

- Domain：34 files / 418 tests passed。
- DB：19 files / 98 tests passed；真实 PostgreSQL 覆盖 migration、复合外键、跨媒体同号和 `numeric NaN` 反例。
- Worker：61 files / 448 tests passed；2 项真实外部凭证 opt-in skipped。
- Web：55 tests passed。
- Domain/DB/Worker/Web typecheck 与 lint 全部通过。
- Web `next build --turbopack` production build 通过，14 个静态/动态页面生成成功。
- 本机脱敏 Mock：九个一级导航 HTTP 200；`ka_data/platform/reconcile`、账户详情、诊断和预览确认门逐页通过；浏览器 console error 为 0。
- 知识资产：18/18 tests 与全库 validator 通过；canonical 私有资料 hash/凭证形态验证通过，`private/` Git 跟踪文件数为 0。

## 未宣称完成

- 未接正式登录/session resolver；production BFF 目前安全 fail closed。
- 未配置真实 KA Data reader token、内网 origin 与业务账户 scope，未做真实内网数据 E2E。
- reconcile 计算引擎仍 pending；当前仅双源并列与 comparability 门。
- Runtime/Multica/媒体写执行仍关闭；页面仅预览，第二步不发送媒体。
- 未部署 daily/FaaS，未达到 `deployed_internal` 或 `runtime_verified`。
- `changesets.work_item_id` 的同租户/同 tuple 数据库级复合关联约束为 P2，可在只读试用后补。
