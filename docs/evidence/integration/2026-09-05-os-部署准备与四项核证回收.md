# OS 回收：部署准备 6 问 + 四项只读核证（2026-09-05）

> 来源：内网 OS agent 按 `docs/plans/发给内网agent-2026-09-05-部署准备与联调门.md` 编号回传，老板转交。证据等级为 OS 自标。
> 脱敏：`DATA_API_INTERNAL_TOKEN` 值在回传中明文出现，**本文不保留且该值作废**（已经过外网中转，视为泄露，部署前由 OS 重新生成）。identityId 为非秘密 UUID，保留。

## 一、环境准备

| # | OS 结论 | 等级 | arch 裁决 |
|---|---|---|---|
| 1 PG16 | CLI 无 create；需 RDS/PolarDB **Web 控制台**建库或资源工单；连接串 `pgm-xxxx.pg.rds.aliyuncs.com:5432/db`；RDS PG / PolarDB-PG 支持 pgcrypto+btree_gist，**ADB-PG 不支持任意扩展**；配额/审批时长未证 | 源码实读/平台文档 | **选 RDS PostgreSQL 16 最小规格（或 PolarDB-PG），不选 ADB-PG**；由老板提资源工单/控制台建；建好只给连接串形态+库名，密码直接进 config vars |
| 2 Node/依赖 | build.sh 构建期 `npm/tnpm install` 走 `registry.anpm.alibaba-inc.com`，自动装 node 20；zip ≤2GB；`file:` 多包未实测 | 源码实读 | 依赖走构建期安装，不打 node_modules；**R-013b 出根级 package.json + install 脚本**保证 5 包按序装；Codex 在干净克隆验一次 |
| 3 拓扑 | 单 app 多进程需手改 bootstrap，无 Procfile；FaaS 请求触发/无状态，后台常驻 worker 会被回收；推荐**方案 B**：web、data-api 各一 app（data-api 听 0.0.0.0 + token），worker 改定时触发单轮 | 源码实读/平台文档 | **采纳 B**。data-api 用现有 `DATA_API_HOST=0.0.0.0`（不改代码）；worker 新增 `worker:once`（tick + 消费到空 + 退出，最长运行时间可配），由 a1 定时函数/autopilot cron 每 N 分钟触发；jobs 表 lease 已能承受容器回收 |
| 4 FFmpeg | 镜像无；`pip install imageio-ffmpeg` 可带静态 ffmpeg（无 root），ffprobe 不含 | 真实执行(沙箱) | 一期不带；素材链路上线前 R-0xx 解决 ffprobe（静态二进制打包） |
| 5 Secret | 已生成 token（两侧同值）与 identityId `5e5ea046-e7eb-4fed-a07a-fb7631bc498e`；账密/PG/奇航 userId 等老板 | — | **token 因经外网回传作废，OS 重生成且不再回传值**；identityId 进 R-013 seed JSON |
| 6 出网 | qh/idealab 常驻模式同通；ka-data 地址随沙箱变；素材 CDN `hwmov.a.kwimgs.com`（视频）`tx2.a.yximgs.com`（封面），HEAD 200/Range 206 | 真实执行 | `MATERIAL_SOURCE_ALLOWED_HOSTS=hwmov.a.kwimgs.com,tx2.a.yximgs.com` 进 runbook |

## 二、四项只读核证

| # | OS 结论 | 等级 | arch 裁决 |
|---|---|---|---|
| 1 8 维字段 | 资源位=`resource_position`（ka-data adgroup 级，枚举 INVENTORY_UNIVERSAL/KUAI_SHOU_YOU_XUAN/KUAI_SHOU_LIAN_MENG/OPEN_SCREEN/ENCOURAGE_VIDEO…）或 MAPI unit `scene_id`；出价工具：ka-data `bid_tool` **整列空**，真实信号在 MAPI unit `bid_type`/`ocpx_action_type`/`unit_type` + account `custom_tags["动态出价"]`；代理/自投：仅账户级 `custom_tags["代投/自投"]`，稀疏多"无匹配"；**UBP 无数据源**；扣量 `deduction_ratio/deduction_rate` 分桶 | 真实执行 | 契约：`ad_entities.resource_position` 直取；`ad_entities.bid_tool` 改为**派生枚举**（由 bid_type+ocpx_action_type 映射，映射表 R-012 从 ka-src-0007 MAPI 枚举提案）；`accounts.agent_type` 取 custom_tags，"无匹配"→null，维度按账户级；**`ubp` 维度永久 `DIMENSION_UNSUPPORTED` 直到有源**，前端空态 |
| 2 ka-data | 在线，数据到 20260904；owner 车程；沙箱地址无预发/生产；token issue/--revoke/--list，TTL 未证；请求形态确认；**一枚 reader 看全团队 704 快手户（+头条 4398/腾讯 2670/百度 250）**；对平：消耗差 0.07%、曝光 0.03%、点击/转化相等、奇航 income 该日 NULL | 真实执行 | 团队空间单 token 成立；**ka-data 与奇航离线同源一致（<0.1%）**，可作团队空间主源；income 缺值按三态 missing；沙箱地址/TTL 记正式前硬门 |
| 3 Excel 对平 | **作废（老板 9-6）**：老板没有这张表——"Excel 手工透视"是 docs/00 里优化师的画像，不是老板本人；需求靠老板口述 | — | 对平改以奇航页面同日同户数字为准（ka-data 对平已 <0.1%），不再要老板给任何表 |
| 4 UNKNOWN | CLI 无本地错误码表，透传 MAPI `code/message/request_id`；读超时是**裸 `TimeoutError` 未被包裹**（rc=1），连接超时才是 KuaishouApiError；超时后服务端是否执行 UNKNOWN；**无客户端幂等键**，request_id 只在成功响应；写后读回时延未证（禁真写）；带 unit 的测试广告主存在（89059600 等） | 源码实读/真实执行 | 契约：写链路执行器把任何非成功（含裸异常）一律 → `unknown`；**UNKNOWN 必须回读（structure/unit list）确认后才能决定重试**，因无幂等键，回读显示未生效才允许重试一次；`api.md` 业务码映射 = MAPI 透传 `{code,message,request_id}`，含义表由 R-012 从 ka-src-0007 提案；后续真写测试用 89059600 |

## 三、部署卡点（外部依赖）

1. RDS PG16 实例——老板 9-6：资源工单预计过不了；候选 ① OS 沙箱跑 PG16（内测，零审批，待 OS 证持久盘）② 个人阿里云 RDS 公网+白名单（待 OS 实测 FaaS 出网 5432 与出口 IP）；老板待定
2. 内测用户名/密码（老板 → OS 生成 scrypt）
3. 奇航 userId——OS 自己查并直接填配置（老板 9-6：userId 不是秘密，可回传，不用脱敏）
4. worker 定时单轮（arch 已裁 B，R-013b 出 `worker:once`）

## 四、红线
OS 声明全程只读，未跑 confirm/dry-run 写。
