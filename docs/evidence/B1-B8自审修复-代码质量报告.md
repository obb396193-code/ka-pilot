# B1-B8 自审修复代码质量报告

> 日期：2026-08-20
>
> 分支：`be/b8a`
>
> 原始审查：`1919a8e`
>
> 修复提交：`50ffff1`、`a82643f`、`fc42fc0`、`c9cdb66`、`cfa83a9`、`c84a26f`、`60fc2ec`、`1ba7c64`
>
> 性质：内部修复与复验证据；不表示待契约项、公开 API 或真实媒体接缝已经完成

## 1. 结果

本批修复了 **6/14 个 P0** 和 **7/17 个 P1**，并额外收紧了 Agent 无证据调整建议。其余问题不是忘记处理，而是需要业务归属规则、公开状态机、权威账户映射或新增持久化模型；继续按 P0/P1 保留，不用局部补丁伪装解决。

### 已修 P0

| ID | 修复结果 | 主要证据 |
|---|---|---|
| P0-01 | full/incr raw 全部成功后，以确定性 ID 派发 canonical；canonical 继续派发 quality | `c9cdb66`；失败不派下游、重试不重复的 Worker tests |
| P0-06 | Job 每次领取生成 lease token；运行、心跳、完成、失败、鉴权阻断全部 CAS token | `a82643f`；A 过期、B 重领后 A 的四类写入均被拒 |
| P0-08 | 确认按 epoch 比较；Workflow 真正执行前和 Changeset 行锁内再次校验 TTL | `50ffff1`、`fc42fc0` |
| P0-09 | 媒体执行错误与本地完成/T+1 错误分阶段；终态重入可幂等补调 T+1 | `fc42fc0`；覆盖 complete 后调度失败与崩溃重入 |
| P0-10 | citation 绑定任一无权业务对象时整段丢弃，不再保留明文片段 | `50ffff1` |
| P0-14 | Workflow 输出凭证扫描覆盖嵌入式 Bearer/token/api key/sk/ak | `50ffff1` |

### 已修 P1

| ID | 修复结果 | 主要证据 |
|---|---|---|
| P1-01 | Backfill 默认 reportDate 按 Asia/Shanghai 业务日生成，不再使用 UTC 日 | `1ba7c64`；覆盖 UTC 23:00 已跨上海自然日的反例 |
| P1-04 | `unknown`/非法生命周期返回 `insufficient_data`，只对明确生命周期应用阈值 | `cfa83a9` |
| P1-10 | 启航身份解析要求 workspace 内 active 用户 | `50ffff1` |
| P1-12 | Changeset 结果必须长度、集合和每项恰好一次一致；execution run 更新必须命中一行 | `50ffff1` |
| P1-13 | 所有规则候选均失败时整项 job 失败可见，局部候选失败仍隔离 | `cfa83a9` |
| P1-15 | 账户分页非空却缺 `totalNum` 时 fail-closed | `50ffff1` |
| P1-17 | 启动及每 24 小时在 advisory lock 下预建未来 6 个月指标分区 | `cfa83a9` |

### 额外安全补强

1. Agent 诊断不是 `ok`、置信度为 0，或调整动作没有证据引用时，结构化输出校验失败并进入既有 fallback。
2. Job active lease CHECK 的非活跃分支使用 `COALESCE(status, '')`，避免 SQL NULL 三值逻辑绕过约束。
3. Agent runtime timeout 被限制在 5 分钟凭证信封最大有效期内。

## 2. 仍为阻断或待裁决

| ID | 当前状态 | 继续修复所缺真相 |
|---|---|---|
| P0-02、P0-11 | 未修，继续 fail-closed | 启航发现账户到 workspace 的权威映射、未知账户待认领/隔离规则 |
| P0-03 | 未修 | 回填批次以 raw/canonical/quality 哪个阶段作为公开完成真相 |
| P0-04 | 未修 | `finite/missing/provisional/error` 公开指标与报表 DTO |
| P0-05 | 未修 | 一账户日唯一任务、主归属或显式分摊规则 |
| P0-07 | 未修 | Workflow 生产运行入口、Run executor lease 和 effect outbox 模型 |
| P0-12 | 未修 | 钉钉 durable inbox/ACK/outbox 状态机及失败可见性 |
| P0-13 | 未修 | Changeset 对 account/campaign/unit/creative 的权威租户关系与操作权限矩阵 |

仍未修的 P1：今日补偿暂估、实时/离线质量对账、mute 语义、健康度有效分母、策略评价模型、Simulation/Changeset preview 统一、租户内唯一约束、Agent session 并发、启航响应资源上限、canonical 批量性能。Agent 的“低置信度阈值”及 `currentValue` 服务端事实绑定也继续待业务/能力口径。

## 3. 全量验证

| 门禁 | 当前结果 |
|---|---|
| 默认测试 | Domain 193 + DB 74 + Worker 136 + DingTalk Gateway 19 = **422 passed**；真 SDK 烟测 1 个按设计 opt-in skipped |
| 真 SDK 烟测 | `KA_RUN_AGENT_SDK_SMOKE=1` 单独复验：**1 passed**；真实 Claude Agent SDK 子进程 → 本地模型网关 → fake upstream 流式返回 |
| TypeScript | 四包 `tsc --noEmit` 全绿 |
| ESLint | 四包全量 lint 全绿 |
| PostgreSQL | 4 个迁移 up/down/up 回放通过；Job fencing 真实并发反例通过 |
| 依赖漏洞 | 四包 `npm audit --omit=dev --audit-level=moderate` 均为 0；修复批次未改 package manifest/lockfile |
| Diff 检查 | `git diff --check`、动态执行/私钥/常见明文凭证扫描无发现 |
| 复杂度 | 变更生产文件 complexity≤10、单函数≤100 行严格门禁 0 发现 |

## 4. 覆盖率

| 包 | Statements | Branches | Functions | Lines |
|---|---:|---:|---:|---:|
| Domain | 95.43% | 85.13% | 99.10% | 95.43% |
| DB | 93.62% | 77.68% | 97.32% | 93.62% |
| Worker | 90.66% | 75.72% | 94.02% | 90.66% |
| DingTalk Gateway | 86.62% | 75.92% | 94.11% | 86.62% |

关键修复模块：`job-repository.ts` 94.53%、`partition-maintenance.ts` 83.87%、Changeset execution handler 93.63%、ETL full 100%、ETL incr 96.05%、rule scan 98.23%、Agent diagnosis 92.50%。

## 5. 审查结论

本批已经把能够由现有内部契约确定的局部安全不变量、Job 单活、Changeset 崩溃恢复、日常 ETL 阶段派发和运行维护补齐。它仍不是可直接接真实媒体写权限上线的完整产品后端；上表待裁决项必须在 Claude/arch 冻结契约后继续实现，并重新做并发、崩溃、跨租户和缺数验收。
