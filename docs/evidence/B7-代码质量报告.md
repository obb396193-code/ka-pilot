# B7 工作流可靠执行内核 · 代码质量报告

> 日期：2026-08-20
>
> 功能审查基线：`be/b6` / `57c5773`
>
> B7 功能实现 SHA：`b0024e1`
>
> 检查范围：B7 新增 Domain/DB/Worker 代码及 B1a-B6 全量回归。真实 Multica/OS、媒体 MAPI、Provider AK 和生产数据不在本报告验证范围。

## 1. 结论

- Critical：0
- High：0
- Medium：0 个阻断项
- Low：1 个可维护性提示：B7 的 graph/runtime/repository/runner 为状态密集文件，超过 300 行关注线；但已保证单函数不超 100 行、圈复杂度不超 10，公开契约冻结后可再按职责拆文件，不阻断内核审查。
- 质量门禁：通过

## 2. 变更范围

B7 生产代码主要包含：

- `packages/domain/src/capability-registry.ts`
- `packages/domain/src/workflow-graph.ts`
- `packages/domain/src/workflow-runtime.ts`
- `packages/db/src/workflow-repository.ts`
- `apps/worker/src/workflows/simulation-runner.ts`
- `apps/worker/src/workflows/run-handler.ts`
- `apps/worker/src/workflows/types.ts`
- B5 Agent operation port 的公共 capability metadata 校验复用。

相对 B6 生产代码新增约 3,287 行，未修改 `packages/contract/` 或 `packages/db/migrations/`。

## 3. 测试与覆盖率

| 包 | 默认测试 | 行覆盖率 | 结果 |
|---|---:|---:|---|
| `@ka/domain` | 150 | 94.42% | 19 files passed |
| `@ka/db` | 70 | 92.97% | 16 files passed；真实 PostgreSQL 16 |
| `@ka/worker` | 128 | 90.43% | 32 files passed；1 opt-in skipped |
| `@ka/dingtalk-gateway` | 19 | 86.62% | 5 files passed |
| 合计 | 367 | 四包均 >80% | 通过 |

B7 新模块覆盖率：

- `capability-registry.ts`：94.59%
- `workflow-graph.ts`：94.81%
- `workflow-runtime.ts`：92.87%
- `workflow-repository.ts`：95.22%
- Worker `src/workflows`：89.78%
- `run-handler.ts`：87.56%
- `simulation-runner.ts`：95.85%

额外 opt-in 回归：

- `KA_RUN_AGENT_SDK_SMOKE=1 npm test -- --run test/agent/sdk-gateway-smoke.test.ts`
- 结果：1 passed；真实 Claude Agent SDK 子进程 → localhost gateway → fake OpenAI SSE，耗时约 11.2 秒。
- 该结果仍不代表真实 IdeaLab/Anthropic/Multica 生产通路通过。

## 4. 数据库、迁移与契约

- Workflow Repository 6 个专项 PostgreSQL tests 通过。
- DB 全量 70 tests 通过。
- migration test 完成 drop/replay、重复执行和 down/up 回归。
- `git diff --exit-code be/b6 -- packages/contract packages/db/migrations`：无差异。
- 未新增表、字段、索引、job_type 或公开 API。

## 5. 类型、规范与复杂度

- 四包 `npm run typecheck`：全部通过。
- 四包 `npm run lint`：全部通过。
- B7 生产文件附加 ESLint `complexity <= 10`、`max-lines-per-function <= 100`：0 warning。
- 初检发现 8 处复杂度告警，已拆分编译校验、事件分派、推进决策、节点准备、输入解引和模拟预检，复验 0 warning。
- Worker Zod 由浮动 `^4.1.5` 收紧为 `4.1.5`，与 Domain 及 SDK 实际安装版本统一；`npm ls zod --depth=1` 无分裂。
- `git diff --check be/b6 --`：通过。

## 6. 依赖与安全

- 四包 `npm audit --audit-level=high`：全部 `found 0 vulnerabilities`。
- B7 生产文件扫描：无硬编码 API key、PAT、密码、私钥、Bearer Token、`mul_`/`mcn_` 凭证。
- 无 `eval`、`new Function`、Shell、子进程、任意 SQL 或用户可控动态执行。
- graph/params/events 都有严格版本 Schema；未知字段、非 JSON 值、非有限数、循环和非 plain object fail-closed。
- Runner 冻结 workspace/initiator/credential owner；每节点重新检查精确 capability 版本和权限。
- 事件只写稳定错误码和受限 ref；凭证形态错误码、字段或输出会被拒绝。
- execute 不可进入 generic invoker；只能经 Changeset preview、hash 确认和 confirmed execute。
- confirmed execute 超时/歧义、或写后输出无法安全持久化，统一进 UNKNOWN 而非失败/重试。

## 7. 正确性与可恢复性

- 固定 published version 和稳定 plan fingerprint，画布 UI 坐标不进入执行指纹。
- 只支持严格 DAG；环、自环、孤岛、重复依赖、未知端点和非上游引用均拒绝。
- 事件 sequence 严格连续，幂等 append 同键同内容复用、异内容冲突。
- 崩溃重入跳过已成功节点；running 读/preview 用原 attempt + 原幂等键恢复。
- 有界重试、步数/墙钟让出、暂停/恢复/取消、fan-in、确认过期/错 hash 和 UNKNOWN 均有测试。
- Simulation 永远把 execute 降为 preview，不能发生真实写操作。

## 8. 已知边界和 Claude/arch 审查重点

- B7 未注册生产 job_type，也未接入公开 API/Worker runtime，不会在契约未裁决时被误触发。
- 公开 React Flow graph DTO、节点配置、发布/模拟/执行/确认/暂停 API 尚未冻结。
- 真实 Multica/OS/CLI 参数与回执协议未编造，`ChangesetActionPort` 和 capability invoker 仍是内部端口。
- 凭证代理、服务身份、同账户冲突锁与 UNKNOWN 生产对账仍需对接 B3 和真实平台。
- 等待确认目前按单 Run 串行；并行分支、条件节点、循环、子流程和补偿流不在 B7a。
- 定时/事件/钉钉触发器及视觉化运行日志等公开产品能力，待 Claude/arch 契约审查后接入。

Claude/arch 应按 `docs/relay/inbox-arch.md` P-011 逐项审查与裁决，再进入公开集成。
