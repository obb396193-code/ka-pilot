# B20 状态

> 分支：`codex/b15-material-teardown-semantics`  
> 基线：B19 最终交接 `acd046c`  
> 当前状态：公共资产治理纯 Domain 内核完成；未合并、未部署、未接 DB/API/权限/前端。

## 已完成

- 报表、工作流、策略、对象组、知识五类资产统一不可变版本。
- owner/维护人、来源、适用范围、依赖、版本和变更说明完整建模。
- `draft→shared→verified→official→deprecated` 严格前进状态机。
- 当前版本验证事实、显式成功率/业务效果、证据引用和定义指纹绑定。
- 使用事实按事件幂等、用户去重，输出使用人数、频次和最近使用时间。
- 废弃原因和替代版本；完整统一治理摘要。
- 稳定排序、深冻结、SHA-256 指纹及嵌套语义完整性校验。

## 关键边界

- UI 起步仍可只暴露“我的/共享”，底层完整保留五段状态。
- Domain 不决定操作者权限；API/Service 必须在调用前校验 workspace 和角色。
- 不内置“成功率多少才能验证”的阈值；passed/failed 来自真实验证流程。
- 不执行工作流/策略/报表，不承载凭证，不修改公共资产定义。
- 公共版本修改必须创建新 draft 版本，不原地覆盖。

## 提交

- 设计与实施计划：`174a2b8`
- 领域内核与自审：`d22a5d8`
- 质量与交接：待回填

## 验证

- B20 定向：29 passed。
- Domain：30 files / 381 passed；97.30% 行、87.73% 分支、99.56% 函数。
- 新模块：99.69% 行、85.00% 分支、100% 函数。
- DB：19 files / 92 passed；真实 PostgreSQL 与 migration replay 通过。
- Worker：48 files / 301 passed，1 opt-in skipped；PG、gateway、FFmpeg 通过。
- DingTalk Gateway：5 files / 19 passed。
- Claude Agent SDK opt-in：1 passed。
- 四包 typecheck/lint/audit、复杂度、安全与冻结目录门禁通过。

## 等待 Claude/arch

1. 统一 assets 表/API 与现有 report config、workflow version、strategy、object group、knowledge document 的关联方式。
2. 五段晋级/废弃的角色权限、复核人与 workspace 边界。
3. 各资产类型的验证证据来源和 passed 判定流程。
4. usage fact 何时记一次、试运行/预览是否计入、唯一事件 ID 由谁产生。
5. 替代版本存在性、同租户/同类型兼容性和依赖图检查。

