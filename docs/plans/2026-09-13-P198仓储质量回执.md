# P198 实际仓储交审回执

日期：2026-09-13。代码 `d66f5dbc`，基线同步 main@95425dc7（merge `739bbdb4`）。状态：候选实现已验证、等待arch审查；不代表P198完整可发送，不代表合流/部署。

## 本笔实现

- Domain私有claim/maintenance/schema；DB实际claim、sent-only去重、deduplicated审计、fenced finish、expired sending按未知结果恢复；Worker使用真实仓储与既有advisory lock。
- 依赖030迁移（已提交`9a037978`）。旧queued合法行补算业务键，旧sent历史只读比较不改历史；job/run日期同workspace读取、缺失才用入队业务日，非法值不回退。
- 领取/结果短事务，发送不占事务；lease token/status/有效期在最终UPDATE再次验证。重复行sent_at=null，dedupe_of指真实首发。
- 每次claim最多100行维护，显式maintenanceLimit→batch_limit；证据最多1000，1001拒绝；SQL预算计payload、target、kind及固定余量，命中16MiB拒绝，避免驱动无界传输。
- apps/web、视觉文件、package/lock零diff；没有真实网络发送、ETL消费或媒体写。

## 真实门禁

| 命令 | 终态证据 |
|---|---|
| `npm --prefix packages/domain test -- run` | 106文件1699通过 |
| `TEST_DATABASE_URL=<新DB合成库> npm --prefix packages/db test -- run` | 164文件1888通过；含14项新仓储PG及8项030迁移 |
| `TEST_DATABASE_URL=<另一个新Worker合成库> npm --prefix apps/worker test -- run` | 212文件2494通过，2项外部opt-in跳过（Idealab/SDK gateway），不是失败或假执行 |
| 三包`run typecheck`、`run lint` | 全部exit0 |
| `npm --prefix apps/web run typecheck` | exit0，A46跨包ES2017兼容；本笔不改Web |
| DB仓储focused `--coverage --coverage.include=src/outbound-delivery-repository.ts` | 14/14；行/语句95.93%，分支86.11%，函数100% |
| Worker `npm audit --omit=dev --offline --json` | 缓存公告扫描0漏洞；不是当日联网审计，不新增依赖 |
| `git diff --check`、限定路径静态检查 | 通过；无console/日志/ENV读取/真实fetch新增于本笔 |

原始日志：`output/p198-store-domain-full.log`、`output/p198-store-db-full.log`、`output/p198-store-worker-full.log`。串行门禁句柄4756终态exit0。新库`ka_be_r010_p198_store_db_test`与`ka_be_r010_p198_store_worker_test`分开，旧库/失败日志未清除。

## 必须保留的失败轮

1. 前笔复用Worker跑过的PG做DB全量：系数审计数据触发保护性down拒绝，1871通过/3失败。不是本次绿；见旧`p198-v1948-db-postmerge.log`。改用不同新合成库，不删审计、不放宽迁移。
2. 最终写租约反例：初始owned验证后租约失效，原finish仍成功（红）。新增最终UPDATE fencing后绿。
3. 近16MiB证据：payload单独低于阈值、加元数据已超，原返回sent证据（红）；SQL合计预算后绿。
4. 维护100行原会误报drained，测试先红；显式batch_limit后绿。另补Domain claim16项及Worker实际锁+PG4项，真实证明跨空间/inbox不被消费。

## 剩余项

- Task3实际Transport、群/DM服务端解析、CLI注册、diagnose、日报deduplicated映射尚未完成；随后按v1.9.49顺序继续，不停等旧裁决。
- 当前消息成功仅指远端明确ack，不能声称用户已读；未知结果仍最多一次重试，连续两次终止，不承诺exactly-once。
- 因新模块仅私有契约，无冻结HTTP DTO变更。请arch按exact SHA审查；新源码全部已跟踪，不靠工作区文件才能运行。
