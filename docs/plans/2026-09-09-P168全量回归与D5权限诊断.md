# P168 全量回归与 D5 权限诊断

日期：2026-09-09。受测生产基线 `cbeb920`（本人 P166/P167 + main `10c26cf`）；诊断脚本独立提交 **83f5a27**。仅本人 worktree `be/r010`，没有修 be2 生产文件、没有 push 或部署。

## 门禁实测

磁盘前检9.0GiB、后检9.1GiB，达到≥8GiB。Node22.22.2。PG仅使用本人既有本机55432隔离合成库，DB与Worker分库，无新建库/业务源/媒体调用。

| 门禁 | 实际结果 |
|---|---|
| Domain全量 | 85文件，**1258通过** |
| DB全量（含真实PG迁移/仓储） | 110文件，**1218通过**；数字含unit，不把1218全称PG |
| Worker全量（含真实PG/HTTP） | **1667通过、1失败、2外部opt-in跳过**；150通过文件/1失败文件/2跳过文件 |
| Domain / DB / Worker typecheck、lint | 三包全部0退出；Worker包含新诊断脚本 |
| DB / Worker production audit | offline缓存均0漏洞，不是联网实时审计 |
| 新诊断脚本 | 真实session+HTTP+PG复现权限违例，**退出2为发现漏洞，不是测试通过** |

命令：Domain目录 `npx --no-install vitest run --maxWorkers=1`；DB/Worker目录分别显式
`TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_p158_20260909_test` /
`.../ka_be_r010_20260907_test`，`npx --no-install vitest run --maxWorkers=1 --hookTimeout=30000`。
三包 `npm run typecheck && npm run lint`，DB/Worker `npm audit --omit=dev --offline --json`。
这批不是Web/Gateway全量门禁，不借arch旧数字补齐。未改生产代码，不重新冒称全生产覆盖率。

## P1：新 D5 任务详情绕过个人 tuple 范围

契约 `packages/contract/api.md:13`：personal只见本人显式获授账户及派生对象。

- `packages/db/src/r014/task-detail-repository.ts:54–68`：主任务只查workspace/taskId，未验证有效task_accounts与个人scope相交。
- `:74–80`：各读函数只收到workspace/taskId，丢失ApprovedWorkspaceAuthContext的scope。
- `:113–190` / `:228`：readiness、累计/近期转化、异常数和blockers均按整个任务读；不能只在主任务补EXISTS，混合任务也会串数/泄露工作项。
- 当前D5 PG用例 `apps/worker/test/r014/task-detail-routes.test.ts:14–17` 仅构造team；不能证明personal范围正确。

复验（不依赖真实凭证；脚本不打印token/ID/业务行）：

```sh
# apps/worker，依赖已迁移的本人测试库
TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_p158_20260909_test node --import tsx scripts/audit-task-detail-scope.ts
```

实际输出：

```json
{"hiddenTaskStatus":200,"hiddenMetadataExposed":true,"mixedTaskStatus":200,"mixedAchieved":8,"authorizedAchieved":1,"unauthorizedBlockerExposed":true,"emptyScopeStatus":200,"missingCookieStatus":401}
```

含义：

1. 原Session从真实grant解析出一个KUAISHOU tuple，未授权TENCENT任务仍200并返回名称。
2. 同workspace、同accountId跨KUAISHOU/TENCENT的混合任务返回转化8，而获授快手部分只有1；腾讯工作项标题进入blockers。
3. 原grant软撤销后，同一token经Session复验scope确为空，但详情仍200。说明不是P166漏过滤，而是D5忽略已批准的空范围。
4. 无Cookie401，证明并非整个HTTP鉴权被省略。没有调用真实登录密码Provider；session由真实仓储issue，后续每请求真实resolve。

诊断脚本不做migration/reset；结束只清随机本例workspace/identity关联行。首两次脚本未完成：先夹具同账户重叠任务时段触发23P01，再误用旧allowedAccounts字段；修为不同hidden账户与判别式scope.accounts后才产生上述有效证据。没有把两次脚本自身错误当产品漏洞。

建议由arch派be2：personal主任务可见性、每条派生查询均保留批准tuple和有效日期；空scope不可读；team只读保持，但个人无账户工作项也不得从team task详情旁路进入。最后一条当前仅代码风险，未在本诊断实测。补RR/RO一致快照，避免七次pool.query读取不同时间点。原SHA不可据现有team-only测试称全授权验收。

## P2：D5 合入后的全量红灯

`apps/worker/test/r014/task-detail-routes.test.ts:140` 期望 `d5-a2 无 unit`，真实返回 `d5-a2 无单元`。
既有v1.9.1要求中文，当前是新测试未同步，不应回退生产中文文案。be2文件，交arch派修；本分支未改期望。

## 给 D5b-2 的现成源指针（纠正“尚未落地”的依赖认知）

Q018称四个cost字段等PlatformWindowQuery落地，但个人源已存在并在Data API使用：

- `apps/worker/src/data/platform-window-query.ts:28–31` 支持workspaceId +批准tuple accounts +window +taskId。
- `:76–81` summary将taskId与tuple同时下推，不用裸taskId代表权限。
- `:136` 导出 `createPlatformWindowQuery(pool)`，同一RR/RO快照读取Semantic与逐日考核。
- `apps/worker/src/data-api.ts:67` 已注入该factory；本轮真实PG `platform-window-query-pg.integration.test.ts` **8/8**。

可直接复用这份已有源继续个人窗口接线；不能把R010a1所有剩余项都做完作为前置。D5整体窗口、costStatus映射仍按arch冻结语义核对，不能擅用null冒充有源，也不能不顾缺数强行填finite。预算三项仍依赖014，team来源另论；这里只给既有内核，不替be2修改其DTO。

## 状态

P166/P167自身相关测试本轮仍通过，但合流组合 **Worker有红、D5权限P1实锤**。未部署/媒体写/跨owner修复/生图。其余hourly、Gap、013/014/016、详情decision、工作流/SSE等仍按P160/P163请求等待arch最小契约/来源裁决；全信箱目标未完成。
