# Gap公开契约与缺源准入计划（P140）

按已冻结api§3.6、gap/account-task-biz fixtures与P138请求定义继续，不增加规则或源优先级。

1. TDD：三种groupBy进入严格Query Registry；公开Envelope含groupBy、account.gap/v1、meta.ruleSetVersion；时间必须源相关、组唯一、缺数不可判正常。Domain/Web永久parity。
2. Service继承Session+批准tuple交集，合法请求无已版本化规则reader时503；非法范围先403、非法参数400。不得借旧table或临时常数生成high/normal；浏览器不能传ruleSetVersion/阈值。
3. BFF接canonical ID及strict shape，source-off无mock成功；完整Envelope规范仅为未来reader边界，不声明已有可用data。真实启动PG源关闭回归。
4. 记录尚缺的真实规则版本Provider/聚合成员证据、preDeduction归因量与fixture时钟冲突。后续真实成功adapter需从可信账户×日明细证明tuple和覆盖，不能只核group label即放行。当前不提供绕过上述证明的可注入gap success口。
5. 定向/Domain全量/Web全node、types/lint/audit、报告及独立SHA；磁盘不足继续明确全DB/Worker/Nextbuild未过，不修改Contract、be2、视觉或媒体写。
