# 全信箱剩余交付依赖复核（P160）

实读时间：2026-09-09；后端 `be/r010@038ec54`，权威 `main@7dd47e6`。本轮是交付范围审计，**没有新功能实现或新测试通过声明**。上轮P159为实际代码进展；不能以其三动作内核替代R010a2用户闭环。

## 归属和验收口径

- 原信箱 R001–006 标记已完成；R007–009 的历史交付不重做，当前迁移链001–012/015仍以实际main和arch审查为准。
- 本会话有效范围仍为 R010a1/a2、R013b、R011、R012、R015、R010b。R014/R016/R017由be2负责，不重复实现其能力；其中明确回移的hourly/gap、decision、workflow taskId仍在be。
- 生图全部以老板最新取消为准，保留候选目录；不按信箱旧头像提醒重开。
- `inbox-codex.md:166–169`规定：不能兑现用户验收句的项只能算基础建设。以下不能因为Domain/Repository绿灯而勾选完成。

## 当前证据与缺口

| 派活 / 用户验收 | 当前代码证据 | 未达到的部分及阻断 |
|---|---|---|
| R010a1真实字段/维度/下钻 | `data-api.ts:63`已真实注入Window/Dimension/Pivot；旧“等arch注入pivot”快照已失效 | `account.hourly`在query-service.ts:438依赖未注入真实port；`account.gap`:432明确拒绝未配置版本源；不能拿日数据/不完整广告数据冒充分时或阈值 |
| R010a1 system/health | `platform-health-repository.ts`仅观测canonical覆盖，不证明connector/executor/agent健康或源新鲜度 | 主HTTP无system/health；fixture.agent为instances，api.md:1014要求total；完整未知健康形态和global授权仍需统一，不能填0/ok |
| R010a1 system/etl-runs | `etl-run-repository.ts`已保持BIGSERIAL文本、真实attempt快照 | 主HTTP无列表；P141三问仍有效：incr只Raw、canonical独立job；旧attempt缺失；quality/coordinator多日期。fixture不允许对应未知语义，不能借当前job attempt或复制计数 |
| R010a2有证据队列/详情/处理 | 列表、旧详情、重复次数与静音落库已有；P159仅三动作授权内核 | 新详情fixture与旧read-detail结构不同，actions返回详情又依赖该结构；decision三门事实未定义缺失输出；P158已给精确清单 |
| R010a2动态规则/explain/缺数抑制 | 受限AST解释器与rule-evidence同RR日证据读取已实现 | 生产扫描Provider、源freshness/initial-full/冷启动/静音/覆盖计数全接线未完成；无公开explain。不能把computed_at当数据新鲜度，不能用静态测试ready代替生产证据 |
| R010a2变更集全流程/T+1 | dry-run HTTP/BFF三值已接，source-off真实503；hash/attempt/追踪内核在库 | confirm/execute/retry/rollback尚未公开；只读preflight按arch明确不接，实际媒体写禁止，不能为演示造成功；反向明细还被013类型冲突挡住 |
| R011团队完整快照/只读 | 方案及staging原则已冻结，不复活旧root Task6 | migrations目录无013；schema.sql:227 changeset_items.id为BIGSERIAL，:1100/:1104引用为UUID。不能自创映射，也不私拆冻结013留下不兼容表；需arch先改类型/授权拆批 |
| R012警报/协作/知识库/卡片/六页签/八维 | 已有B级内核不等于v1.4 API | 无014；accounts六列跨角色归属待arch；ad_entities仅:583–584两个派生列，六raw列未齐。不能声称bid_tool source verified；agent_type昵称来源还依赖be2 R017 |
| R015素材拆片/结算/治理 | 016草稿已按冻结DDL验证，独立DB草稿49PG历史证据；部分治理GET已真实接线 | 无正式016；materials等七基础表仍依赖014。下载/抽帧/云ASR现有内核不等于材料页可用；不切MP4，不引入本地ASR，仍保持原需求 |
| R010b工作流运行与任务关系 | workflow_runs.task_id已由015加列；已有运行内核 | api.md:991要求taskId UUID，而:510任务是opaque ID且DB存TEXT；公开运行列表/详情未接。须明确统一类型，不丢非UUID任务、不伪转UUID |
| R010b Agent消息/SSE/上下文 | 模型清单完整只读、会话归属修复已落地 | api.md:595七种type与fixtures/agent/sse-frames.json的event/context/token/diagnosis及seq不一致；上下文fixture含accounts/window/anomaly而命令只列account/task/work_item等。未形成公开消息链，不能声称聊天可用 |
| R013b首次部署 | install、worker once、HTTP触发、source-off启动已有本机/合成PG证据 | 无本轮内网安装/真实凭证/部署证据。按已给OS拓扑，不再重复索要已给f.yml；正式联调/真实数据仍由授权内网环境执行 |

R010原派活还包含任务创建/编辑/考核价重算与通知、日报12模块、账户结构/标签/转移等；它们并未因上表合并归组而删掉：任务/账户交界和R014移交文件待be2交回，其余依R012的014表/通知与公共DTO逐条交付。Registry/导出/搜索已移交be2的部分不算本会话自交付。最终仍须按原信箱#1–11逐项验收，不用本表代替完整功能验收。

## 给 arch 的最小解阻顺序

1. **先解013/014**：013明细引用统一BIGSERIAL，或书面授权拆开team/SLA与反向明细迁移；明确014中accounts六列由be落DDL且不碰be2源码，补ad_entities六raw列。如此可推进R011→R012→R015整条链，而不是继续新增不能接入口的内核。
2. **收口详情/动作与ETL未知态**：以哪份详情成功结构为准，个人无账户/缺关联对象的null形状；decision缺证据怎么表达；ETL每阶段nullable计数、历史attempt、多日业务日期。对应P141/P158/P159，不需重做产品设计。
3. **统一SSE和taskId**：把权威wire样例/规范改成唯一版本；be才能做严格HTTP/BFF parity，不自己维护第二份契约。
4. 仍属源码归属问题的S6服务字段请交be2修，或显式交回be；不跨文件改对方代码。

## 本轮边界

当前记录是对最新候选头逐项复核后的依赖升级；目标保持active，不标complete。本轮未运行测试、未改契约/真实媒体开关/图片。剩余范围全部保留；收到任何一项新裁决后从对应纵切片继续。
