# D6 公开 HTTP 观测结果接线质量报告

代码：`de63169`。已包含 main `7dd47e6`；候选已提交，未审合、未 push、未部署。

## 范围

- `apps/worker/src/r010/changeset-dry-run-route.ts` 改接 `preview`，拒绝旧 abbreviated run 结果；strict 校验三值、计数、TTL、path changesetId 和观测时间。
- meta.requestId 沿同次请求；dataAsOf 只取已验证的源观测值，未知保持 null；businessDate 按 checkedAt 的上海 03:00 业务日计算。
- 成功与失败响应保留 no-store/requestId；成功 exact16MiB（或更小配置）fail closed，不发送部分结果。
- `data/http-server.ts` 仅调整注入 port 类型；`data-api.ts` 仍没有真实 Provider，内测 source-off503 不变。没有 confirm/execute/retry/rollback 路由，不创建媒体执行 job。
- 本子批不改 Domain/DB/Contract、前端或 be2 文件。BFF 成功态接线仍是下一项，不能称浏览器成功路径已通。

## 真实执行证据

工作目录为 be-r010/apps/worker，测试中 session/provider/账户全为合成数据；PG 为独立本机 ka_be_r010_20260907_test。未接真实凭证。

| 命令/检查 | 结果 |
|---|---|
| `npx vitest run test/changeset-dry-run-http.test.ts --maxWorkers=1`，改实现前 | 10 fail / 23 pass；证明旧 route 不会消费 preview |
| `npx vitest run test/changeset-dry-run-http.test.ts test/changeset-dry-run-service.test.ts --maxWorkers=1` | 87/87 |
| 显式 TEST_DATABASE_URL + `npx vitest run test/changeset-dry-run-service-pg.integration.test.ts --maxWorkers=1` | 10/10，真实 PG+HTTP；ok/changed/unknown 与落库快照逐项一致；同号异媒体/异 workspace 拒绝，零 jobs |
| HTTP suite + `--coverage --coverage.include=src/r010/changeset-dry-run-route.ts --coverage.reporter=text` | 33/33；98.38% 行、90% 分支、100% 函数 |
| `npm run lint` | Worker 通过 |
| `npm run typecheck` | 失败，仍为四个旧 account/task fixture 缺 be2 新 required 字段；本子批无新增类型错误 |
| `git diff --check` / `git show --check` | 通过 |
| `git merge main` | Already up to date（首次非提权因 ORIG_HEAD.lock 权限失败，获准后成功检查） |

97 项独立定向测试通过；覆盖率 rerun 不重复计数。原始结果在本会话工具输出，未虚构磁盘日志。无新增依赖，未跑新的联网 dependency audit。磁盘7.7GiB，小于约定8GiB全量门槛，未跑全量或 Next build，未清缓存。

## 安全与遗留

- HTTP→Service 仍只消费服务端批准 personal context，团队在 repository 前拒绝；伪造 x-ka-* 不改变授权。原401/403/400/405/413/409/503仍覆盖。
- 合法数据但 observed 缺失、summary不一致、path对象不符、future dataAsOf、非法 checkedAt 或附带 token 字段均502，不透出上游内容。
- 两端真实媒体通道尚不存在；合成 Provider 仅证明软件通路，不代表 production-verified。
- Worker typecheck旧四项：account-list-{http,service}.test.ts:37/32、task-list-{http,service}.test.ts:40/31，继续请 arch 协调交界 fixture；不放宽 be2 Contract。
- 下一项：BFF 使用同一 strict Domain DTO，透传合法三值结果并保持 requestId/16MiB/CSRF/源未接入错误；不改页面视觉。
