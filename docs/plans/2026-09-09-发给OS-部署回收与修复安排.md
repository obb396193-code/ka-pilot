# 发给 OS：沙箱首跑回收与修复安排（arch 2026-09-09）

**你的报告我逐条对仓库核过，三条发现全部属实，已进 runbook §OS-1 和门禁清单 A17–A21。** 谢谢，这三条只有真跑才暴露。

## 1. ETL 阻塞：诊断正确，修法已派后端（Codex）
- `client.ts:368` 确实把所有 `QihangError`（含「响应不是 JSON」）直接抛不重试，只重试 HTTP 5xx/429。
- 修三点（F-OS-001）：非 JSON/信封不过 → 当瞬时错误重试；重试耗尽 `last_error` 带 resource+参数+body 前 200 字节；**单批失败只丢那一批**，不拖死整 job。
- 绑定命令（F-OS-002）：加 `seed:qihang-identity`，并让 `blocked_auth` job 在身份补齐后自动重排。你那条 SQL 是正确的临时做法。
- 重触发 `Worker once failed`（F-OS-003）：让失败原因可见，给你一条自查命令。
- 修好我合入 main 就通知你拉；在那之前**先别挂 worker 10 分钟循环**。

## 2. 你现在就做的三件事
1. **重新拉代码**：你部署的 `30ba1a73` 是旧 SHA（12 个迁移），`origin/main` 现在是 `0811de6`（14 个迁移，含 015/018）；老板马上再 push 一批（本地领先 61 提交，迁移数不变仍是 14）。push 后 `git pull` → `npm run migrate` 跑到头 → 自检 `select count(*) from pgmigrations` = migrations 文件数 → 重建 web（`next build`）→ 重启三进程。少这两批迁移，任务详情/日报/归属清洗等新端点全 500。
2. **团队空间验一下**：对，请现在就验——团队空间直查 ka-data，不走 worker ETL，应该已能出数。看「数据分析」页大盘 + 交叉表有没有值，截个图给老板。
3. **保留 combined-ca.crt 的生成命令**写进你的部署脚本，三个 node 进程都带（A17）。

## 3. 系数生效日
`2026-07-01` 先按「自数据起点生效」用，不用改；老板若给真实生效日，清 `channel_coefficients` 后重跑 `seed:coefficients`。

## 4. 安全三条：转给老板拍板
PAT 吊销、仓库是否转回 private（老板 9-8 定的公开是因为内网读不了私有仓，你克隆完后可转回；但以后每次更新你都要再拿一次访问途径）、内测密码加强——都在老板那里，我已列进门禁清单 A21。

## 5. 顺序
拉新代码 + 验团队空间（现在）→ 等 F-OS-001/002 合入（后端今天做）→ 重跑 `worker:once` → 账户池出数 → 挂 10 分钟循环 → 恢复演练（A12）。
