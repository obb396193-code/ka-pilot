# P141 ETL BIGSERIAL 全链字符串质量报告

代码：4523bb3；分支 be/r010，包含 main@c3451db。candidate，未合流/部署；无 push、媒体写或视觉变化。

## 修复范围

- startRun 不再 Number(id)。规范正十进制字符串贯穿 DB、EtlRunStore、Full/Incr、Canonical/Quality/Backfill推导类型、runtime和benchmark；正常行数/金额仍是原类型。
- DB在发送更新SQL前拒绝非字符串、0/负数/前导零/小数/指数/超PG int64输入；错误不回显非法值。现有参数化SQL、running状态门和observation字段白名单保留。
- 两个相邻超安全整数ID及PG int64上限保留原值；重试前scope.execution不随新attempt变化，observation不能覆盖execution或加入token。
- 流水线PG测试移除默认共享ka连接，强制localhost:55432的ka_*_test；仅合成源，未执行真实媒体。

## 实测

日志位于本人worktree output/qa/p141/（不纳入代码仓）。

| 命令/范围 | 结果 |
|---|---|
| DB vitest etl-run-repository.unit + etl-run-bigint-pg.integration，maxWorkers=1 | 19 unit + 3真实PG =22通过 |
| Worker vitest etl-handlers/backfill-day/backfill-coordinator/canonical-handler/data-quality-check/etl-attempt-scope/benchmark-data-pipeline | 7文件65通过 |
| Worker vitest data-pipeline-pg.integration，显式本人TEST_DATABASE_URL | 1真实PG通过：合成奇航→Raw→Canonical→Quality→Semantic→Rules→WorkItem |
| DB/Worker npm run typecheck、lint | 全通过 |
| npm audit --omit=dev --offline | 缓存审计0漏洞，不宣称在线最新公告已核验 |
| etl-run-repository.ts定向V8覆盖率 | statement/branch/function/line均100% |
| git diff --check / production差异人工核查 | 通过；无新增依赖、动态SQL或外部接口 |

大ID PG使用真实schema的事务内TEMP clone及TEMP sequence，ROLLBACK回收，不推进public sequence；不把它等同迁移/FK测试。另1项流水线才使用本人合成库真实主表和production handlers。RED首次15失败/4通过；Worker首次10个旧数字断言失败；PG首次一条字符串否定断言误匹配ID，均修复重跑，旧日志保留。

## 未完成与风险

- 磁盘4.7GiB，未跑本轮全DB/全Worker/Next build；不借用旧批全量数字。
- 仅编号和已存attempt生命周期；没有新公开GET /system/etl-runs。旧scope无execution不能伪造attempt=1，rows_ingested不能同时充当raw/canonical两个计数。
- 当前API/DB计数、历史attempt未知与公开错误摘要语义继续审计；相关缺口会另报arch，不用不可信jobs当前attempts回填历史。

按代码质量技能核验输入/参数化SQL/错误泄露/性能/覆盖率；新增校验常数时间长度上界，无新查询和N+1。未发现本批新增P0/P1；公开列表功能不在本批完成范围。
