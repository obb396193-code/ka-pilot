# R010a2 详情与 S6 交界实读缺口

核验日：2026-09-09；候选分支 be/r010，基线 main@7dd47e6。这是代码现状，不是已完成声明。

## 1. 工作项详情需要 arch 收口契约

| 来源 | 当前事实 |
|---|---|
| `packages/contract/api.md:273` | 旧纵切片允许 personal 的本人无账户工作项；返回持久化证据、诊断、T+1、状态与时间 |
| `packages/contract/fixtures/work-items/detail.json` | 新响应为 `data.workItemId/account/task/rule/assignee/decision/actions` 与成功 meta；account/task/assignee 是对象 |
| `packages/domain/src/read-detail-contract.ts` | 当前 strict 响应是 `data.kind/workItem`，assignee 为 nullable UUID，account tuple 可双 null；尚未采用新 fixture |
| `apps/worker/src/data/read-detail-service.ts:132` | 当前仍输出旧 strict 结构；不能把已有详情 GET 视为新详情页完成 |
| `packages/domain/src/r014/decision-tier-contract.ts:30` | recentManualOps 为非空整数，reversible/withinCap 为非空布尔；不能表达未知事实 |
| `packages/contract/api.md:787` | 要求详情 decision；策略给阈值，未明确历史成功率样本/取数和缺失门限事实的输出 |

请求 arch 明确以下最小问题，然后 be 接线，不修改 Contract 自行裁决：

1. 确认新版 fixture 替换旧成功 envelope，并补 personal 无账户、未分配 assignee、缺 task/rule 的合法 null 形状；不能丢掉已冻结的本人无账户可见性。
2. decision 的历史成功率、近期人工操作、额度判定分别来自哪些持久化事实，历史样本与时间窗如何定义？仅阈值配置不等于实际证据。
3. 上述事实缺失时，decision 是否可 null/带 availability，还是接口明确 unavailable？当前 strict gates 不支持 unknown；不允许补 `recentManualOps=0` 或 `withinCap=true` 冒充查证。
4. actions 的可用性以实际已开放能力为准；真实媒体写仍关闭，不因 fixture 出现按钮名就开放执行。

已存在的 `evaluateDecisionTier()` 可复用，但输入证据必须真实。不存在用默认门限事实先给自动决策的实现。此项记阻塞，继续其他有效派活，不停等。

## 2. S6 不是补齐测试就完成

| 代码位置 | 缺口 | 归属 |
|---|---|---|
| `apps/worker/src/accounts/account-list-service.ts:79` | itemFor 未返回 poolStatus/product/lastAction/nextSuggestion 等 Repository 已新增字段 | 服务已临时移交 be2，不在本批改 |
| `apps/worker/src/tasks/task-list-service.ts:113` | itemFor 未组装 stage/readiness/nextMilestone；Repository 已返回 stage 和 readinessFacts/Overrides | 服务已临时移交 be2，不在本批改 |
| 四个 `apps/worker/test/{account,task}-list-{http,service}.test.ts` | 旧合成样本漏 Repository 新必填字段，导致 Worker typecheck 4 个 TS2740 | be 自有测试，本批修复 |

本批测试只补 null 元数据和显式合成的一个未充值/未基建账户事实；没有修改生产默认值、放宽类型或宣称 S6 已经端到端完成。请 arch 同步 be2 补服务投影和公开 fixture parity。
