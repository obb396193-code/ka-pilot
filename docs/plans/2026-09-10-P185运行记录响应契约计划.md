# P185 运行记录响应契约 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** F-P179 的第一步：按 arch v1.7.5/v1.9.8 及既有 ETL fixture 实现严格响应 schema，不伪造运行次数、计数和新鲜度。

**Architecture:** Domain 负责 runId 字符串、attempt、时间、计数、warning 的一致性边界。后续 Repository 从固定execution快照读，不取可变jobs.attempts冒充历史；HTTP接现有Session/admin闸与16MB守卫。

**Tech Stack:** TypeScript、Zod、Vitest、现有etLBatchFailureWarningSchema与冻结JSON fixture。

---

## Task 1：已冻结响应的TDD（本批可执行）

文件：新`packages/domain/src/etl-run-list.ts`、`packages/domain/test/etl-run-list.test.ts`；修改`packages/domain/src/index.ts`。

1. 先写测试读原`packages/contract/fixtures/system/etl-runs.json`，只删除fixture文档专用meta._note；不改该fixture。
2. runId保留正int64字符串；attempt正安全整数；jobType覆盖withEtlAttempt的六类；状态按实际etl_runs running/done/failed。不发明新状态。
3. rows保持冻结的`{raw,canonical}|null`；计数只接受非负安全整数，不把null变0。warning复用BATCH_FAILED；旧字符串仅接受fixture已冻结的BLOCKED_AUTH，不透出任意error_summary。
4. 时间真实日历校验、finished>=started、running无finished、terminal有finished；strict拒多余凭证/上游字段。不同runId允许同jobId不同attempt，拒重复runId。
5. 测试先红（未实现导出），实现后通过；验证barrel导出实际可import。Domain typecheck/lint、缓存audit；路径限定提交。

## Task 2：仓储前必须明确的历史语义（本批向arch报，不私裁）

- 旧scope无execution时attempt无法证明，不能从当前jobs.attempts推断；展示null还是整个拒绝/显式跳过需契约裁。
- rows_ingested是当前run自身阶段计数，canonical是另一个job/attempt。不能把不同run合成同一个rows对象；是否允许raw/canonical各自null需裁。
- list排序/分页未冻结；当前schema只固定防御性最多10000条，不宣称数据库查询分页已实现。
- rerun是原job重试还是新job，owner绑定、并发/已完成/blocked_auth允许状态未冻；先不新增公开写API。

## Task 3：后续（不冒称本批完成）

arch裁完后另批实现DB RR/RO、SQL限流/16MB、workspace/admin防线、真实PG，再接GET+授权rerun。
P-178工作项仍等be2 Q-027 helper合main；本契约子批不替代该授权工作。
executing-plans不可用，本人按持续执行授权直做；不改前端、不push、不生图、不执行媒体写。
