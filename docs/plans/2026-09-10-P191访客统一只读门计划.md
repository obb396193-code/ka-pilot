# P191 Viewer 统一只读门 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 已登录 viewer 的业务写请求在路由统一层、读取body和Service/Repository前被拒绝为403 READ_ONLY_ROLE。

**Architecture:** Data API 壳完成 internal bearer + Session 后统一阻断 POST/PUT/PATCH/DELETE；三个确为只读的查询POST出口（query/data-query/admin-reconcile）不当成写操作，仍走各自诊断授权。Session登录/切空间/logout不属于业务写门，保持会话退出能力。R014原有门保留，壳门也在其前。

**Tech Stack:** Node HTTP、TypeScript、Vitest、P190 AST路径库存。

---

从 arch@1d93a975 合流后实施，user已批准持续推进；executing-plans技能不可用，沿用writing-plans/code-quality-checker步骤本会话自测。前端0修改、不改be2持有auth文件。

1. 新建 `apps/worker/test/r010/viewer-readonly-http.test.ts`，从P190库存动态枚举所有已注册本人路径，展开POST/PUT/PATCH/DELETE；Session排除、三个query的POST明确只读例外；每条真实HTTP断言403 READ_ONLY_ROLE+requestId、服务调用计数0。新路由加入自动落入测试。
2. 先红（现handler返回FORBIDDEN/405等而非READ_ONLY_ROLE），再改 `apps/worker/src/data/http-server.ts` 统一分支。业务读取GET、数据POST、Session DELETE、非viewer路径和无认证顺序保持。
3. Domain/response不扩自造字段，body稳定 `{ok:false,error:{code,message,retryable:false,requestId}}`；前端冻结文案以其自身为准。
4. 当前仍未实现的rerun/reset/create等不是已注册路由；此批不能声称实现它们，后续注册即被中央门+动态库存测试覆盖。
5. targeted viewer+data-api+session+ETL+旧r014 viewer回归、Worker typecheck/lint、安全自查、cached audit。P190现存两红独立登记，不通过删除/跳过掩盖。
6. 独立代码SHA+质量/信箱；未push、未部署、不执行真实任务。
