# B11 奇航时效、完整性与小时监控状态

> 分支：`be/b11`
>
> 基线：`878126f`
>
> 当前状态：代码与测试已完成，等待 Claude/arch 按 P-018 审查；本机 PostgreSQL 真实测试因系统 Data 卷仅余约 3.8GB、Docker VM 报 `no space left on device` 无法启动而保留为合并/部署前门禁。
>
> 质量与交接 SHA：`67bdfda`

## 已完成

1. 固化 OS 第二轮只读事实：`hh` 累计语义、行级省略、跨日含首尾、D-1 日内由空转有、无 complete marker、无过滤广告疑似固定行数截断。
2. `ad_realtime` 禁止无 accountIds/adIds 查询；恰好命中默认 2000 行时按疑似截断 fail-closed。
3. 每次成功查询生成不含 userId/业务 ID/原始行的安全观测，并追加到 `etl_runs.scope.observations`。
4. `etl_incr` 默认逐日重查 D-1 offline，可配置 0..3；空结果可见，非空 Raw 可修订 D-1 到当日 Canonical。
5. 纯 Domain 相邻累计快照差分：新增行以前值 0，当前缺行不造数，负差分截 0 并报告数据修正。
6. 复用现有 `ad_metrics_hourly`，按 workspace+ad+ds+hh 幂等 Upsert；小时写入失败不派发 Canonical。
7. 建立第三轮 OS 最小只读探针，继续验证历史 realtime、hh 边界、疑似截断和分页。

## 代码提交

- `a3b479b`：固化奇航第二轮只读证据。
- `e60e719`：广告查询防截断与安全观测摘要。
- `c7c6289`：D-1 动态重查与查询观测持久化。
- `c500771`：累计快照小时差分。
- `b88f6ea`：小时表仓储与增量 ETL 接线。
- `5595836`：失败边界、白名单与覆盖率补强。

## 没有做

- 没有修改公开 Contract、Migration、API 或前端。
- 没有把非空/多次稳定推断成 offline 分区 complete。
- 第三轮实证已确认无过滤查询在 2000 行静默截断；当前仍不把“单个过滤分片恰好 2000 行”视为完整，继续 fail-closed。
- 没有放宽 `hh` 到 24；当前生产 payload 继续 0..23，等待第三轮真实执行。
- 没有调用任何媒体写操作，也没有接触或持久化凭证。
- 没有宣称本项目 Worker/FaaS 已连通真实奇航。

## 验证结论

详见 `docs/evidence/B11-代码质量报告.md`。变更模块覆盖率 95.86%~100%，四包 typecheck/lint、依赖审计、复杂度和安全扫描通过；Domain 207、Worker 非 PG 170、DingTalk 19、B11 DB 单元 12 tests 通过，真 Claude Agent SDK opt-in smoke 1 通过。

真实 PG 5 个相关用例未执行：连接 `127.0.0.1:55432` 返回 `EPERM`；老板打开 Docker Desktop 后复测，Docker CLI 仍返回 `unable to start`，VM 日志明确报 `no space left on device`，当时系统 Data 卷 99%、仅余约 3.8GB。只读存储扫描已识别约 12.7GB 可再生缓存与安装残留候选，但尚未获得删除授权，未做任何清理。该项必须在释放空间后的可用 PG16 环境补跑。

## 下一步

1. Claude/arch 审 P-018，重点裁决 D-1 重查频率、小时观测公开方式与保留周期。
2. OS 执行 `docs/plans/B11-OS第三轮只读探针.md`；回传后先解释影响，再决定是否调整 hh/分页/截断策略。
3. 可用 PG16 环境补跑两份 Repository 真测试、全 DB 回归与 migration replay。
4. 有产品 Worker/FaaS 合法身份后，跑 Qihang→Raw→Hourly/Canonical→质量的同链 trace。
