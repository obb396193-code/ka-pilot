# B11 奇航时效、完整性与小时监控状态

> 分支：`be/b11`
>
> 基线：`878126f`
>
> 当前状态：第二、三轮 OS 实证适配、代码、真实 PostgreSQL 和质量门禁均已完成；等待 Claude/arch 按 P-018 审查。产品 Worker/FaaS 携合法身份直连奇航仍是部署联调门禁。
>
> 第三轮代码终态：`1c87e2e`
>
> 最终质量与交接 SHA：待本次交接提交后回填

## 已完成

1. 固化 OS 第二、三轮只读事实：历史 realtime 可查、`hh` 累计及 0/23/24/25 边界、跨日含首尾、D-1 日内由空转有、无 complete marker、无过滤广告 2000 行静默截断和分页参数无效。
2. `ad_realtime` 禁止无 accountIds/adIds 查询；恰好命中默认 2000 行时按疑似截断 fail-closed。
3. 每次成功查询生成不含 userId/业务 ID/原始行的安全观测，并追加到 `etl_runs.scope.observations`。
4. `etl_incr` 默认逐日重查 D-1 offline，可配置 0..3；空结果可见，非空 Raw 可修订 D-1 到当日 Canonical。
5. 纯 Domain 相邻累计快照差分：新增行以前值 0，当前缺行不造数，负差分截 0 并报告数据修正。
6. 复用现有 `ad_metrics_hourly`，按 workspace+ad+ds+hh 幂等 Upsert；小时写入失败不派发 Canonical。
7. Qihang 原子 Client 自校验 `hh` 0..24；ETL payload、小时差分和小时表继续限定 0..23，24 只作全天累计查询。
8. 广告实时默认按 5 账户、80 广告 ID 确定性分片，最多 200 批；逐批 Raw/观测，全部成功后合并去重。冲突重复、空过滤、批次数超限或任一批命中 2000 行均 fail-closed。
9. 分片合并恢复最大 `last_sync_time`；Worker PG 测试关闭文件并行，消除两个迁移测试偶发争锁。

## 代码提交

- `a3b479b`：固化奇航第二轮只读证据。
- `e60e719`：广告查询防截断与安全观测摘要。
- `c7c6289`：D-1 动态重查与查询观测持久化。
- `c500771`：累计快照小时差分。
- `b88f6ea`：小时表仓储与增量 ETL 接线。
- `5595836`：失败边界、白名单与覆盖率补强。
- `9775b87`：固化第三轮历史 realtime、hh、截断与分页实证。
- `ace828f`：Qihang 原子查询 `hh` 0..24 校验。
- `8f46745`：广告实时账户/广告 ID 分片、合并去重和资源门禁。
- `f178731`：补齐分片快照最大同步时间。
- `1c87e2e`：稳定 Worker PG migration 测试并补空标识反例。

## 没有做

- 没有修改公开 Contract、Migration、API 或前端。
- 没有把非空/多次稳定推断成 offline 分区 complete。
- 第三轮实证已确认无过滤查询在 2000 行静默截断；当前仍不把“单个过滤分片恰好 2000 行”视为完整，继续 fail-closed。
- 没有把 `hh=24` 写入小时表；仅原子 Client 接受已实测的 24 全天累计，生产小时 payload 继续 0..23。
- 没有调用任何媒体写操作，也没有接触或持久化凭证。
- 没有宣称本项目 Worker/FaaS 已连通真实奇航。

## 验证结论

详见 `docs/evidence/B11-代码质量报告.md`。Domain 207、DB 92、Worker 200、DingTalk 19 个默认测试全部通过，真 Claude Agent SDK opt-in smoke 1 通过。Worker 全仓 coverage 92.19%，新增分片模块/增量 Handler 行覆盖均 100%；四包 typecheck/lint/audit、复杂度、安全扫描和冻结目录检查通过。

释放第三方可再生缓存后，Docker 29.5.2 与 PostgreSQL 16 恢复。此前缺失的 B11 Repository 真 PG 5/5、DB 全量 92/92、Worker PG 4/4 和 migration replay 均通过。PG 环境门禁已关闭。

## 下一步

1. Claude/arch 审 P-018，重点裁决 D-1 重查频率、分片频控、小时观测公开方式与保留周期。
2. 有产品 Worker/FaaS 合法身份后，跑 Qihang→Raw→Hourly/Canonical→质量的同链 trace。
3. 向奇航确认单账户命中 2000 时的权威 adIds 发现/拆分来源；确认前继续 fail-closed。
