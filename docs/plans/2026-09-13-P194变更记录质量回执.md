# P194② 变更记录质量回执（候选，非完整页面交付）

## 范围与当前结论

- 当前实现位于独立`be/r010`；迁移 **6fdbce49**、接口 **79e331ca**，均路径限定提交、未push。已按arch的`9a564eb8`逐字采用信箱冲突解法，形成`da16da5c`；再用只包含自己两处改动的持久补丁安全同步`main@1e304507`，合并点`90f1b466`。自己的三行data-api注入已恢复，主线新增sourceTimezone参数原样保留。交付前再次merge main返回Already up to date。
- 本批仅028/029迁移、change-log Domain/DB/Service/GET/BFF及测试；未改数据指标/ETL/页面视觉、未调用真实媒体写、未push。
- **接口候选可读取有真实修改时间的三类历史**；老系数缺真实修改时间时默认三源查询仍明确503，尚待arch冻结`at:null`展示形。不能称整个P194或全部真实页面完成。
- 实施沿用writing-plans；提交前按code-quality-checker检查权限、SQL、错误泄露、数值/日期校验、分页、迁移和覆盖。

## 已关闭问题

1. 预算表在schema但未部署：028真实迁移创建`daily_cap`、workspace/task联合FK与版本索引；同日多版本按v1.9.44允许追加。原同日UNIQUE反例先红，再修。schema.sql旧列名/唯一约束仍由arch同步。
2. 系数审计字段缺失：029先加nullable列，再SET DEFAULT；历史行保留unknown，避免将迁移时刻伪装为原修改时刻。两个down都对已有数据/审计元数据拒绝有损回退，真实up/down/up已测。
3. 新GET只在Worker有、浏览器无法到达：BFF已接`GET /api/internal/settings/change-log`，只转服务端bearer及session cookie；过滤参数与后端复用同一Domain parser。
4. 三源版本含revoke、null actor：输出严格id/op；作废newValue=null；旧值按effective_date/created_at/id排序计算，展示按真实at排序。缺actor可null，不扔掉整行。
5. task过滤原来误拒合法媒体系数：Service不再要求系数伪装成task行，由RR查询证明task/media关系，同时新增personal媒体输出守卫。
6. 分页：真实PG同一时间戳60行跨三源翻页，50+10，无丢行/重复；同日20笔预算oldValue链逐项核对。51st sentinel也严格校验。
7. 新BFF对成功/错误的requestId、状态码、非法数值、请求过滤一致性、额外字段、未知query、重复cookie、自报scope、exact16MiB、超时和私密错误文本做fail-closed验证。

## 验证账（截至恢复后，最终以postmerge结果补齐）

| 范围 | 实测证据 |
|---|---|
| Domain同步前 | 103文件1608、typecheck/lint绿 |
| DB同步前 | 161文件1832、真实PG、typecheck/lint绿；`output/p194-history-db-final.log` |
| Worker同步前 | 207文件2418通过+2外部opt-in跳过，typecheck/lint绿；同期完成主线同步，因此不以此轮替代最终postmerge门禁 |
| BFF定向 | 10项通过、限定文件eslint绿；`output/p194-history-bff-final.log`；Web全量按v1.9.46由arch跑 |
| DB覆盖 | Repository unit38项：语句/行/函数100%，分支98.57%，不是全系统覆盖率 |
| 真实HTTP/PG | 新GET suite4项；任务三源/越权/同号跨媒体/切空间/旧token/退出/时间缺失/非法数值 |
| 真实生产入口+BFF | `r010-production-composition-pg.integration.test.ts`启动实际data-api子进程，再由真实BFF模块发HTTP，验证价历史、team切换、旧token401、logout401；连同loader共4项通过，`output/p194-history-production-smoke.log` |
| 依赖 | Worker `npm audit --omit=dev --offline --json` 0 vulnerabilities；仅缓存库，不声称最新联网审计；本批package/lock无改动 |
| 合并后最终Domain | 103文件1613项通过，typecheck/lint绿；`output/p194-history-domain-postmerge.log` |
| 合并后最终DB | 161文件1832项通过，真实PG，typecheck/lint绿；`output/p194-history-db-postmerge.log` |
| 合并后最终Worker | 208文件2443项通过、2外部opt-in跳过，typecheck/lint绿；`output/p194-history-worker-postmerge.log` |

三个全量进程均已收exit0（69876/24027/30119），不是日志尾部推断。复验命令：各包`npm test -- --run`、`npm run typecheck`、`npm run lint`；DB显式使用本机55432的`ka_be_r010_p194_final_test`，Worker使用不同库`ka_be_r010_p194_worker_test`以免迁移回放相互干扰。BFF为`node --test lib/data/settings-change-log-bff.test.ts`及四个新增文件限定eslint。A44检查`git ls-files --others --exclude-standard apps packages`输出为空；既有output与独立review不属于本批，未删除/夹带。

失败轮保留：初次Worker2415过/1失败/2跳过，原因新GET缺BFF；迁移同日UNIQUE反例；Service task过滤/越界media两红；BFF请求过滤与响应冲突一红。均未通过豁免修绿。新增unit空数组case误用it.each导致参数展开为undefined，修为对象载荷；Worker新增scope回调引起TS narrowing错误，改为提前计算授权media集合后复跑。最终证据不复用失败轮。

## A40 / 交接与未完项

- 新响应参照`packages/contract/fixtures/settings/change-log-v1944.json`来自合成PG→真实HTTP原始body，保留实际DB生成id和微秒timestamp；不是生产数据。BFF永久parity直接消费，未把fixture塞进生产服务。
- 老`settings/change-log.json`缺id/op、带过期recomputedDays，未擅改arch唯一参照。请arch确认替换/废弃策略。
- **前端页面尚需fe接线**：`apps/web/components/business/tasks/assessment-price-history.tsx`仍写BFF未接、真实路径暂读timeline；`task-detail-page.tsx`和`settings/settings-page.tsx`仍引老fixture，`lib/fixtures/tasks.ts`仍定义旧ChangeLogItem，changedBy按必有对象用。不要直接把新版null actor塞给旧UI造成崩溃；先按strict DTO适配。此次未改这些视觉/页面文件。
- 老系数at未知：按现strict schema返回503；价格/预算单独kinds可用。需要arch裁定展示未知时间，不能把这个外部契约缺口藏在“全部完成”里。
- 维护窗口执行迁移，无在线低锁承诺；无新增预算/系数写接口。P193全局治理/P198出站/P199及其他信箱仍是独立后续批次。
