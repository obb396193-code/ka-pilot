# P173 Task 1：启航身份绑定命令

- 代码：`faa2045`；状态 implemented / 定向本机 verified，未合流、未部署。F-OS-002 自动恢复尚未实现，不报整条完成。
- `npm run seed:qihang-identity -- '<JSON>' [--force]`，在 packages/db 目录运行。JSON 严格接受 workspace_id、user_id 或 identity_id 二选一、qihang_user_id；DATABASE_URL 由部署环境注入。仅绑定已有 active personal 身份链；同值 unchanged，异值无 force 拒绝。输出只有 workspaceId/userId/status，不含启航身份。
- 事务锁定 actor/workspace/membership/identity，唯一匹配后只改 users.qihang_user_id；5s 锁超时 / 15s 语句超时，错误固定码。无浏览器路由，无 session/role/grant/job 变更，无新依赖。
- CLI 输入含私人身份，部署时不要将实值贴入群/日志/版本库；按现冻结 JSON argv 形式，操作系统命令行仍可能对本机同权限进程可见。本批没有声称消除这层主机边界。

## 实测

- Domain `vitest run test/qihang-identity-seed.test.ts`：13/13。
- DB `vitest run test/qihang-identity-seed.test.ts test/qihang-identity-seed-command.unit.test.ts test/qihang-identity-seed-repository.unit.test.ts --hookTimeout=30000 --coverage --coverage.include=src/qihang-identity-seed-repository.ts --coverage.include=src/seed-qihang-identity.ts`：32/32，其中10项真实PG。TEST_DATABASE_URL 指向本人55432合成隔离库。
- 实际子进程 CLI：首次绑定成功、异值失败、force成功，stdout/stderr不含qid；另一用例验证默认 pool 装配/关闭。
- 跨空间/missing/inactive/team拒绝；并发不同值仅一次成功；ambiguous mapper拒绝；DB错误/回滚失败不漏源错误；更新一列/角色不变/会话0。
- Domain/DB/Worker typecheck + lint全部通过。DB `npm audit --omit=dev --offline` 0（缓存审计，不冒称在线刷新）。
- 两生产模块行90.9%/分支97.01%；Repository行100%，CLI行80.55%。CLI入口34–40不在当前进程覆盖计数内，但真实子进程已执行成功/失败。
- TDD：缺实现时失败；首次实现后测试it.each误传数组参数、PG断言误用auth_sessions.workspace_id分别失败，已改为正确数组夹具和active_workspace_id再复验。
- 本机空闲约7.8GiB，低于8GiB全量门禁要求，只跑定向/类型/lint，不借用上一批全量结果。

## 质量与下一步

- 输入严格有界、固定参数化SQL、无网络媒体写、资源释放明确；未发现本批未修Critical/High问题。长身份链行锁是显式部署操作，限定超时，不用于请求热路径。
- Task2继续：仅原owner身份补齐、live grant通过后的身份缺失blocked job可恢复；不删job，不复活撤权/越权/失败耗尽的job，保留审计。
- 新F-OS-004/v1.9.5已收到，等be2密码仓储合main再接；不碰其login-provider。生图仍取消。
