# D6 三值契约质量报告（P154）

代码 **cef4e90**；已合main@185d34e。仅Domain新文件、测试及index末尾be导出。状态：内部契约基础implemented/定向verified，未合流/部署；**不是D6成功态HTTP全部完成**。

## 已完成

- 按最新arch `changesets/dry-run-ok.json` 严格匹配三值、verdict、summary、hash、TTL、meta。fixture仅移除文档`meta._note`，不修改契约原件；运行时不允许此额外字段。
- BIGSERIAL id以正数字串保留，最大到有符号64位，无Number转换；拒0/前导0/越界/非数字。items上限10000，重复ID或目标字段拒绝。
- `ok`必须现值与fromValue语义相等；`changed`必须不同；`unknown`必须observed=null，缺失字段不是null。非ok必须有原因；summary逐项校验，changed/unknown/blocked均不得confirmAllowed=true。全ok允许额外业务确认门阻断，不把一致性当执行权限。
- 纯判定复用sameChangeValue，JSON键序不影响相等，number与string不隐式转换。无取数、状态变更、媒体调用。
- 初版实测发现Zod可继续执行refinement，非法子值使safeParse抛异常；加入先验检查避免此问题，保留原始非法值失败，不吞成unknown。

## TDD / 门禁

先新增测试，模块缺失红；初版28例中非法observed两例红；修后28绿，补非法from和10k边界后最终30新例。

在packages/domain执行：

```text
npm test -- --run test/changeset-preflight-presentation.test.ts test/changesets.test.ts test/changeset-typed-flow.test.ts test/changeset-values.test.ts --maxWorkers=1 --coverage --coverage.include=src/changeset-preflight-presentation.ts
4 files passed, 82 passed (30 new + 52 existing)
new module lines/statements/functions/branches: 100%
npm run typecheck: exit 0
npm run lint: exit 0
git diff --check: exit 0
```

原始输出留本会话工具回执（测试sessions10774/83978为红，82789/22492为绿）；未伪造磁盘日志。无依赖变更，本轮未新跑联网audit。代码质检：输入边界严格、固定上限、线性计数及Set去重，不接受任意扩展字段；无SQL、Secret或网络通道。媒体权限仍在Worker/Repository，不由此schema授权。

## 仍未完成 / 下一步

- **旧Worker preflightProof只有status/failReason，不含observed**。不能把旧run返回`{executionRunId,hash,status}`硬转三值成功；下一子批扩充受控只读证据、绑定hash/TTL/scope、持久化和public mapper，再接HTTP/BFF。
- 生产source-off仍503，无真实preflight Provider，confirm/execute不开放。本次测试是合成样例/纯逻辑，不是媒体实测/PG证明。
- 磁盘本轮2.8GiB→573MiB，低于8GiB；完整五包/全量PG没跑。P153报告的Worker4处be2新字段fixture类型错误仍待协调，不拿本包绿代替整体绿。
- 013/014/016前置、规则/工作项公开动作及Agent剩余契约仍按状态保留；总目标active，没有缩为只做DTO。
