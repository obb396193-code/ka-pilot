# R-014 状态（be2；compact / 重开会话先读这个）

工作树 `/Users/aik/ka-be2-r014` · 分支 `be/r014`（基于 `main@550e738`）· 测试库 `ka_be2_r014_test`
计划 `docs/plans/2026-09-07-R014-计划.md` · 回执写 `docs/relay/inbox-arch.md`（Q-xxx）

## 子批进度

| 子批 | 内容 | 状态 |
|---|---|---|
| S1 | migration 015（11 新表 + 5 表加列） | ✅ `83b822b` |
| S2a | identity_preferences / user_watchlists / saved_views | ✅ `bcedc87` |
| S2b | decision_policies + exports（含 evaluateDecisionTier） | ✅ `9aebb18` |
| S2c | capabilities + task_readiness_overrides | ✅ `ba82569` |
| S2d | report_runs + external_changes | ✅ `44b986c` |
| S3a | 账户池九态管道 + 人工置态 | ✅ `6f7585e` |
| S3b | 通知流投影 + me/counts + me/workload | ✅ `8bfd16a` |
| S3c | 全局搜索 G6 | ✅ `381e875` |
| S3d | `GET /tasks/:id/bindings` | ✅ `bd7f4a9`（v1.9 ⑨ 冻结 scope 结构后解阻；boundAt 恒 null 待 018） |
| S4a | me/* 八条端点上缝 | ✅ `4dda65a` |
| S4b | 账户池 / capabilities / decision-policy / export / readiness 端点 | ⏳ 下一个 |
| P-123 | task-list-sql 活动态补 dispatched（Codex 经 arch 转来） | ✅ 已修，改用 domain 的 ACTIVE_WORK_ITEM_STATUSES |
| I-001 | BFF `GET /api/internal/accounts`（arch 联调 404） | ✅ 已补，arch 可重跑账户池页联调 |
| S5 | BFF（`apps/web/lib/data/r014` + `app/api/r014`） | ❌ |
| S6 | 交界字段（account-list / task-list 六文件已临时移交我） | ❌ |
| A7 | `POST /accounts/transfer`（4.10 交接） | ⏳ 排 018 之后（v1.9 ① 裁：018 补 revoked_at + revoked_by，015 不回改） |

**回执**：Q-001 开工/边界、Q-002 迁移窗口、Q-003 S1–S2d、Q-004 S3a–S3c+S4a。

## arch 已裁（main@e5fee10，已合入本分支）

Q-001/Q-002 全裁：新规矩「**共享文件结构性改造由 arch 开缝，功能性改造靠所有权临时移交**」。
①② arch 开缝（`extraRoutes` 入参 + handler spread，并在 main 建空的 `r014/routes.ts`、`r014/handlers.ts`，我只填）——**尚未推**，S4 等它；
③④ `account-list-*` 与 `task-list-*` 六文件**临时移交 be2**（R-014 完交回）→ S6 已解锁一半；
⑤ hourly/gap 移给 Codex；⑥⑦ 按我的建议；⑧ 列归我端点归 Codex；⑨ 全量门禁 arch 统一跑，我只跑增量。
Q-002 由 arch 修，采纳我的 `migration-window.ts` 移到 `packages/db/test/`。

## 本批新发现、待写进下个回执的契约缺口

1. **`account_access_grants` 没有 `revoked_at` 列**（schema.sql:101），但 api.md 4.10 交接语义写「原 grant 置 revoked_at，新建 grant」→ A7 按字面写不出来。
2. **`recentManualOps` 门限未定义**：策略只给了 `recentManualOpsWindowHours` 窗口。我按 `>0 即不过` 实现（保守）。
3. **`overriddenBy:"history"` 触发条件未定义**，且 fixture `work-items/detail.json` 里 `recentManualOps=1` 却 `overriddenBy=null` → 排除了"人工操作触发"这一直觉解。我恒返回 null。
4. **decision-policy 写权限未写**：这行决定系统敢不敢自动改价改预算，我取 `lead|admin` 保守值。
5. **`/me/views` 的 `is_shared` 没有读路径**：fixture 项无 owner 字段，无法区分归属，我只返回本人视图，共享读等公共资产（v1.5.1 ⑤）定义。

## S1 已完成事实（复核用）

- `packages/db/migrations/015_contract_v1_5.cjs`：DDL 全部**逐句取自 `packages/contract/schema.sql`** 三段切片（v1.5 / v1.5.1 / v1.7.1 identity_preferences），未自行设计；`alert_rules` 两列属 012，未重复。
- down 守卫顺序：链接/覆盖类（更具体的诊断）→ 表行数类；全部在任何 DDL 之前，无 CASCADE，无 DELETE/TRUNCATE/UPDATE。
- 测试 `packages/db/test/r014/`：`migration-015-bundle.unit.test.ts`（8 条，逐句对 schema.sql + 不许出现别批的表 + 不碰 work_items/account_metrics_daily）、`contract-v1-5-migration.test.ts`（12 条真 PG：建表/默认值回填/九态七态六维 CHECK/四类外键/七种拒绝降级/down-up 回放）。
- 本地：`test/r014` 20/20 ✅；db 包 `tsc --noEmit` 0、`eslint .` 0。

## 铁律（每批自查）

新文件只在 `*/r014/`；共享文件只在末尾 `// be2-r014` 块追加；只有 015 一个迁移；`work_items` 只读；agent 不算数；缺数三态不补 0；凭证不进仓；不 push；路径限定 commit + `git show --stat HEAD` 自查。
