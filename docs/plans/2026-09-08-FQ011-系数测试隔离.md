# Coefficient test isolation Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 处理F-Q011-1；保留跨workspace及并发反例，不靠清库掩盖失败。

**Architecture:** 当前文件已随机workspace和定向清理，专用非空库原样7/7，尚不能确认arch全量失败原因。先验证Promise.all提前reject导致兄弟PG操作未结束就允许afterEach清理的风险；改为allSettled后统一断言，不吞错、不重试。跨空间例加入无关坏历史，证明不依赖干净库。

**Tech Stack:** Vitest / pg / 合成隔离库。

---

1. 本文件增加受控延迟的混合成功/失败反例，先红；finally等待受控请求结束，避免复现本身污染库。
2. 将并发helper改成allSettled后报告失败；保持两个并发测试并发，不降低语义。
3. 专用本机ka_*_test守卫；现有workspace单次fixture清理保持。
4. 定向PG/unit/随机用例顺序复跑、DB type/lint，磁盘不足不跑全量。报告明确修的是已实证harness风险，arch原全量红灯仍需exact复验。

老板已取消R-FE-IMG-003生图，不再生成/压缩/提交头像；已出的图仅保留在工具候选目录，不写public。计划与取消事项写信箱同步arch。
