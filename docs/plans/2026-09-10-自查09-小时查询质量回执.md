# 自查09：账户小时查询正式接线

代码 `3905461d`；随后 `0ccf793d` 合入 arch main `41466e55` 热修。状态：本地 candidate + 合成PG/HTTP verified，未arch合流/部署/push。

## 实现

- `data-api.ts` 正式注入 `createPlatformHourlyQuery(pool)`，不再仅留下可挂载port。只读025 `account_metrics_hourly`，前驱同快照读取，系数用同日生效版本；无表typed unavailable，绝不借daily/ad/Raw补出小时数据。
- shared `projectAccountHourly` 计算累计/差分/现金/成本/预算使用率；缺行保留missing、不补0，hh24不冒充hh23，缺前驱不造差分，未完成小时不造velocity。
- reader和adapter两道三键+窗口守卫；只有经Service缩小后的personal scope，无team/未授权数据通路。source错误安全映射，SQL/上游body/凭证不透出。
- dataAsOf取当前显示的实际样本最老last_sync_time，不用前驱、sampled_at或当前响应时刻。datasetVersion/timezone/dayCut未知为null；来源metadata是partial/unknown，不冒称known。complete按样本覆盖与累计字段齐全，partial总数遵守已有Contract设missing，returnedRowCount仍真实。
- 10000投影格、源16MiB、响应exact16MiB守卫；只允许进程内参数收紧字节门，浏览器不控制阈值。

## 门禁

在 `apps/worker` 执行；PG只用本机 `ka_be_r010_p194_test` 随机合成workspace，不接OS凭证、不启常驻worker。

| 命令 | 结果 |
|---|---|
| `TEST_DATABASE_URL=… npm test -- --run test/platform-hourly-pg.integration.test.ts test/platform-hourly-query.test.ts test/hourly-public-query.test.ts test/data-api-http.test.ts test/data-api-startup.test.ts --coverage --coverage.include=src/data/platform-hourly-query.ts --coverage.reporter=text` | 5 files / 120 passed；新模块行100%、分支97.64% |
| 合main后 `TEST_DATABASE_URL=… npm test -- --run test/platform-hourly-query.test.ts test/platform-hourly-pg.integration.test.ts` | 28/28（上行子集） |
| Worker `npm run typecheck`、`npm run lint` | 通过（合main后额外复跑typecheck） |
| Worker `npm audit --omit=dev --offline` | 缓存0 vulnerabilities |
| 合main后 `apps/web npm test` | 244/244，运行时源码加载通过；未改任何web文件 |

PG用真实025表，三键组合含另workspace、同号跨media、未授权账户；真实HTTP带伪造x-ka-*不改变scope，缺Session401、越权指定账户403。team在Service层unavailable。**SessionAuth是批准上下文测试桩，不冒充完整登录轮换PG链；启动smoke则真实运行data-api.ts验证KA缺凭证默认关闭仍可启动。**

红测：先缺实现红；初版测试误用了Registry/Service参数顺序，改为实读签名；partial信封错误携带available wholeResultTotal被既有strict schema真实拒绝，修为missing且保留returnedRowCount；测试exactOptionalPropertyTypes拒绝显式undefined配置，已改为省略。均未放宽生产Contract。

## 风险/未完成

1. **小时源业务日时区未持久化**：sourceUtcOffset在Raw/payload中而不在reader快照，不得由ISO的Z推断UTC业务日。因此elapsedDayFraction未知，projectedDayCost=missing + `HOURLY_DAY_TIMEZONE_UNKNOWN`。请arch裁定新增真实源元数据/快照字段的落点；不采用环境默认值冒充lineage。
2. 025表与手动合成写入可查询不等于自动有真数：小时采样job、调度与真实内网运行仍未闭环；部分窗口本就应显示missing。
3. P211维度/筛选/级联接口与三BI公开挂载仍继续，BI内核 `7f407c7e` 已交。view与queryId公开入参差异待arch核，不自造第二Registry。
4. 磁盘约3.6GiB，未五包/build；没有缓存清理、真实媒体写或前端变化。使用writing-plans限定范围，code-quality-checker执行strict、安全、复杂度与覆盖检查。
