# P205 账户小时采样边界 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将已冻结account_realtime小时响应整理为021表所需的可信累计记录；缺行不补零、跨scope/非法值拒绝。

**Architecture:** 纯Worker适配函数，不发请求不落库。受信上下文固定workspace/media/accounts/ds/hh/sourceRunId/sampledAt；sourceUtcOffset必须显式传入，禁止以机器时区猜测无offset源时间。当前格式对应v1.9.4字段与小时末+5min complete规则。raw字段只取已实证账户累计字段，保留null，拒绝present-invalid；忽略上游自报workspace，不能覆盖受信上下文。原Raw完整审计由后续store负责。

**Tech Stack:** TypeScript、zod、Vitest；复用Domain calendarDateSchema。

---

1. 新`apps/worker/test/account-hourly-sample.test.ts`，先测试不存在函数红：字段映射、缺失/零、重复/越权/同号跨media、calendar/时间、数值精度、午夜与+5min边界、hh24不可落小时表。
2. 新`apps/worker/src/etl/account-hourly-sample.ts`：有界严格上下文、显式时区解析、白名单规范行、missingAccountIds；稳定安全错误不含原始body/身份。
3. 运行`vitest run test/account-hourly-sample.test.ts`→绿；覆盖完整分支，Worker已有客户端/Full/Incr/广告差分回归；type/lint/audit。
4. 代码与质量回执独立SHA，更新本人状态与审查信箱；不碰Contract/前端/迁移，不声称生产采样或hourly端到端完成。

技能：writing-plans与code-quality-checker。已冻结设计不重新要求老板拍板；sourceUtcOffset只是内部受信来源元数据输入，不是浏览器新参数/默认时区。执行子技能不可用，本人按持续授权实施。
