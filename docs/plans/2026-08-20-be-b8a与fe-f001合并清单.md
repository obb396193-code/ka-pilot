# `be/b8a` 与 `fe/f001` 合并清单

> 生成日期：2026-08-20
>
> 性质：合并前机械事实与操作清单；不是已执行合并记录
>
> 数据脱敏：只含 Git SHA/路径，不含业务数据或凭证

## 1. 当前事实

| 项目 | 值 |
|---|---|
| 共同基线 | `93351506128b77e48e6806e477514503c33a69ac` |
| 前端分支快照 | `fe/f001` / `1de9256`（2026-08-20 最终复核时已从 `4b00b819` 前进） |
| 后端分支快照 | `be/b8a` / `8255692`（本轮质检提交；勘误提交见最新 HEAD） |
| 基线后前端 commits | 57 |
| 基线后后端 commits | 124（勘误提交前） |
| 前端 committed 变更文件 | 410 |
| 后端 committed 变更文件 | 281 |
| Claude 前端未提交路径 | 46 |
| 当前是否可直接 merge | **否**：`fe/f001` 工作树有大量未提交/未跟踪文件 |

以上数字用下列命令重算，合并当天必须重新执行，不能把本文件快照当现状：

```bash
base=$(git merge-base fe/f001 be/b8a)
git rev-list --count "$base"..fe/f001
git rev-list --count "$base"..be/b8a
git diff --name-only "$base"..fe/f001
git diff --name-only "$base"..be/b8a
git -C /Users/aik/Desktop/投放agent status --short
```

## 2. 已提交文件交集

双方 committed 变更共涉及 5 个同路径文件：

| 文件 | `git merge-tree` 结果 | 处理原则 |
|---|---|---|
| `docs/plans/工作台账.md` | 内容冲突 | 按事件编号/时间保留双方全部记录，不使用整文件 ours/theirs；去重后检查编号和 Markdown 表格 |
| `docs/relay/inbox-arch.md` | 内容冲突 | append-only 合并；保留 Claude 已裁决回复和后端 P-001~P-015，不覆盖任一方历史 |
| `docs/relay/inbox-codex.md` | 内容冲突 | append-only 合并；同一 R 条目只保留最新状态，保留派活原文和后端回执证据 |
| `packages/contract/api.md` | 同路径变更、当前 merge-tree 未报文本冲突 | 仍需逐段 diff；契约以 Claude/arch 最新冻结版本为主，后端新增事实若未裁决只能回信箱，不能静默并入公开 DTO |
| `packages/contract/schema.sql` | 内容冲突 | **禁止整文件选边**。先核对 migration 已实现列与 Claude 最新冻结契约，再逐表合并；随后执行 migration up/down/up 和 schema 审计 |

`git merge-tree` 当前明确报告 4 个文本冲突：工作台账、两个信箱、`schema.sql`。`api.md` 虽可自动合并，也必须人工契约审查。

## 3. Claude 当前未提交现场交集

Claude 当前 46 个未提交/未跟踪路径中，与后端 committed 变更有：

- `docs/plans/工作台账.md`
- `docs/relay/inbox-arch.md`
- `docs/relay/inbox-codex.md`

业务代码目录仍没有未提交路径交集，但台账也已进入双重冲突面；这不代表可以在脏工作树直接 merge，Git 仍可能因未跟踪目标、重命名或生成物覆盖而中断。

## 4. 推荐合并顺序

1. **Claude 先收口自己的现场**：在 `fe/f001` 提交可保留代码；临时抓取缓存、Playwright 目录和生成物按其用途清理或忽略。Codex 不代替 Claude stash/删除。
2. **确认两个工作树都干净**：`git status --short` 必须无输出；记录双方 HEAD。
3. **从最新 `fe/f001` 创建集成分支**：建议 `integration/fe-f001-be-b8a`，不直接在前端工作分支 merge。
4. **先做 no-commit merge**：`git merge --no-commit --no-ff be/b8a`，不要 push。
5. **先合契约，再合协作文档**：顺序为 `schema.sql` → `api.md` → 两信箱 → 工作台账。契约错误会影响所有后端 tests，应最先暴露。
6. **检查非冲突代码也没有语义漂移**：尤其 `apps/web` API mock 是否仍符合 contract、Worker job 类型、credential owner、导航路由。
7. **跑全量门禁后再提交 merge**：失败时保留日志；无法在当天解决则 `git merge --abort`，不做半合并提交。

## 5. 合并后必跑门禁

```bash
cd packages/domain && npm test -- --run && npm run typecheck && npm run lint
cd ../db && npm test -- --run --maxWorkers=1 && npm run typecheck && npm run lint
cd ../../apps/worker && npm test -- --run && npm run typecheck && npm run lint
cd ../dingtalk-gateway && npm test -- --run && npm run typecheck && npm run lint
cd ../web && npm test && npm run typecheck && npm run lint
```

另外必须执行：

- PostgreSQL migration up/down/up；
- Worker 真 Claude Agent SDK → localhost gateway → fake upstream opt-in smoke；
- 前端关键路由 E2E；
- `git diff --check`；
- package audit；
- 明文凭证/私钥/动态执行扫描；
- `packages/contract/` 与 migration 实际结构逐项对账。

## 6. 明确禁止

- 不在 Claude 当前脏工作树直接 merge。
- 不用 `git checkout --theirs packages/contract/schema.sql` 或 `--ours` 整文件覆盖。
- 不删除另一方台账/信箱历史来消冲突。
- 不把 fake upstream 测试写成真实启航、Multica/OS 或模型通路已联通。
- 不 push；公司相关代码远端发布必须老板另行拍板。
