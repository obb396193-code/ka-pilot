# 自查-20260910-03：Full/Incr批次隔离执行

代码 **7879b97f**，7文件318+/21-。仅本人handler和测试，无前端/公开Contract/他人仓储改动；无push/真实凭证/媒体写。候选待arch，未合流或部署。

## 完成什么

- Full/Incr增加可选typed failure recorder；显式接入时startRun固化授权batchScope和既有attempt证据。缺scope/lease/focus越界在run/query之前拒绝。
- 日账户查询按50户分片，广告沿用5户/80广告；catch仅包upstream query，只有typed retry耗尽可写失败台账后继续。鉴权、业务错误、截断、越权返回行、Raw落库、台账持久化失败均不得吞。
- 失败回传null为内部判别，不伪造成功空响应/成功observation；历史离线失败仍保留失败事实，不视作“源证实该日无数据”。后续日期探测可以继续。
- 小时广告前/后任一快照失败的账户都不派生本小时值，其他批正常；派生观测标observed_unverified。账户小时采样尚无独立coverage ledger，仍fail-stop，不把hh抹为全日失败。
- Runtime **未注入**recorder，现有线上方式仍fail-stop。不是偷偷开放新开关；需要readiness按日期expected范围接齐后才能启用。后续只剩真实依赖注入+consumer/HTTP+OS双证，不能把本报告叫整条F-OS-001③完成。

## 红绿与门禁

首次新handler10例实际6红/4绿：full未固化batchScope、incr失败直接抛、无ledger行为、非法scope在source后才报错。接线后28例绿；期间新假广告样本漏bid/budget导致既有严格parser拒绝，补合成必填字段（未放松生产validator）。类型发现exactOptional及ES2022不支持findLast，修为条件展开/at(-1)。

最终命令（cwd apps/worker，显式TEST_DATABASE_URL指向本机隔离`ka_be_r010_p194_test`）：

`npm test -- --run test/etl-partial-handlers.test.ts test/partial-query.test.ts test/etl-handlers.test.ts test/etl-partial-pg.integration.test.ts test/etl-batch-failure.test.ts test/qihang-client.test.ts --coverage --coverage.include=src/etl/partial-query.ts --coverage.include=src/etl/full-handler.ts --coverage.include=src/etl/incr-handler.ts`

结果 **6文件97/97**（新handler10、边界14、新PG1、旧handler18/映射16/client38）；Worker typecheck/lint通过、production缓存audit0。覆盖行100%、函数100%、分支93.92%；full90.9/incr95.16/helper95.31。diff-check绿。磁盘3.7GiB，按arch规则未跑五包全量/build，定向不是全量。

## 真实PG+假fetch串联证据

不是mock仓储：真实QihangClient（注入fake fetch）、Full/Incr handler、EtlRunRepository、EtlBatchFailureRepository、RawMetricsRepository、MetricsRepository、Canonical handler；未启动consumer或触及现有队列。

1. 51账户分页metadata同步，新账户主表再Raw；前50户当天接口200坏JSON重试4次耗尽，最后1户正常。
2. full done但scope保留50户BATCH_FAILED；canonical真实执行只入成功户。已存在失败户旧canonical不能读取；另空间同号账户999仍可读、不受影响。
3. 假源恢复后真实incr补采，旧canonical仍false；再跑真实canonical重算，51户全部readable/cost=10。
4. 原失败历史仍1条，没有删warning掩盖恢复。所有样本随机workspace、固定synthetic字段，本suite清理只覆盖自己的两个workspace。

## 待完成项

- 自查02的readiness日期范围与三个be2调用点接线待arch；未据最近run/响应当前时间猜日期，未默认full done就ready。
- Runtime failure recorder注入、完整consumer出队/真实Session公开summary/dimension验证尚未完成。
- OS真凭证etl_full后的etl_runs/个人看板真数尚待实证；本机fake-fetch不替代它。
- 下一可并行项为025小时表；024编号与原逻辑保持，不能为了顺序改编号或虚设占位迁移。生图永久停、旧输出保留。
