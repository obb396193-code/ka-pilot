# P172 重触发在tick前误报冲突：质量回执

代码 **5550c0b**（3文件130新增/5删除，生产只9行diff）。只修F-OS-003已实证的一处根因：`enqueueScheduled` 幂等SELECT错误比较mutable status/last_error，第二次tick未到lease即抛异常。

## 红绿与门禁

- 第一次PG：6红/2过，其中2个active lease夹具漏token/lease_until。修夹具后原生产代码仍**6红/2过**，均为deterministic conflict（`/tmp/ka-p172-red.log`）。没有把夹具错当生产错。
- 改生产SELECT后新PG **8/8**；加原JobRepository **17/17**。模块覆盖 **94.28%行 / 80.51%分支**。
- Worker **29/29**（含workspace-sync PG4、旧once PG/真实进程3、新once完整runtime PG1、其余unit/进程supervisor）；新once使用原tick和原credential resolution：第一次协议失败queued/attempt1，第二次tick正常进入lease→同job done/attempt2；owner/payload/授权快照不变，qid只在runtime不落job。
- DB/Worker typecheck、lint全过，DB offline production audit0；diff --check0。磁盘约7.8GiB<8GiB，不启动全包门禁，不拿P171的1698或P170的1262冒称本批全量。

## 自审（code-quality-checker）

- 仍比较workspace/type/payload/priority/credentialOwner/maxAttempts；6类不可变字段变化都拒绝。
- 仅SELECT，原job字段完整before/after对等；不刷新退避、不复活done/failed/blocked_auth、不改租约fencing、不重置attempts。8路并发只有1次insert。
- 测试新数据全是random workspace内合成job，按workspace清理；旧JobRepository有全jobs清理，运行明确限定専用`ka_be_*_test`，不碰业务库。
- 本机假上游+真实PG/runtime，不是OS父子CLI端到端证据；supervisor安全diagnostic、F-OS-002的身份补齐恢复仍要做。
- F-OS-001③批次warning/missing闭环未实现，计划已向arch解释不应简单吞错伪完成。无视觉/Contract/媒体写/push。
