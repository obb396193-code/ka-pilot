# P188 独立空库质量回执

代码：`9f976e0e`；基线 `main@5682a509`；状态 implemented + 本机 synthetic PG verified，待 arch 验收；未 push、未部署。

## 范围与根因

arch 已在 main 修复 etl-batch-failure-repository，本批不覆盖那两行。继续自查发现 6 个本人新增套件同类缺陷：没有 runMigrations，默认别的测试已经准备好表。包括 BIGINT 测试的 `CREATE TEMP TABLE ... LIKE public.etl_runs`，不是自包含 DDL。

红证据：独占 `ka_be_r010_p188_empty_test` public tables=0，bigint 单文件 **3/3 失败**，错误 relation public.etl_runs does not exist。该失败不是数据库不可达，也不是业务回归。

## 修复与验证

beforeAll 在本机端口/测试库名守卫后调用既有 runMigrations，再连接/插入；hook 30s；不改变业务断言。六个不同的新建库都在执行前查询确认 public tables=0，不用同一个已迁移库假装独立验证：

| 独占数据库（前缀 ka_be_r010_p188_，后缀 _test） | 单文件 | 结果 |
|---|---|---|
| bigint | db/test/etl-run-bigint-pg.integration.test.ts | 3/3 |
| semantic | db/test/semantic-authority-regression.test.ts | 3/3 |
| changeset | db/test/changeset-read-authority.test.ts | 9/9 |
| readability | db/test/etl-batch-readability.test.ts | 7/7 |
| list | db/test/etl-run-list-repository.test.ts | 20/20 |
| http | worker/test/etl-runs-http-pg.integration.test.ts | 7/7 |

合计 **49/49**，每条独立 npm test -- run 对应文件，六次 exit=0。Worker 用真实 data-api 子进程验证登录/GET/switch/logout，合成环境白名单、无 KA env，不启动执行队列，结束关闭自己创建的进程。六库保留用于复验，未删除已有库。

DB/Worker typecheck、lint、git diff --check 均通过。Worker `npm audit --omit=dev --offline` 为 0，仅代表缓存审计。未修改 package/lock/运行时代码/迁移内容；本批仅测试 setup，不新增业务覆盖率口径。

## code-quality-checker 自审

- 安全：数据库目标校验在迁移前；fixture 随机 workspace 清理；不接真实凭证，不改媒体写边界。
- 性能：每文件执行迁移状态检查属于独立运行必要成本；六套单跑时长约 1.35–5.15s。不是业务性能证明。
- 正确性：arch 命名套件已修，本文补同类，不重复声称 arch 的 24 项为本次测试。
- 范围：作者代码 diff 六个测试；前端/合同/迁移 0 diff。基线 fast-forward 包含其他人的已验收代码，不归本批作者。
- 门禁限制：当前磁盘约 6.1GiB，低于 8GiB 全包门；未跑五包全量，不声称全量通过。临时测试驱动首试 pg import 根目录解析失败，修正 createRequire 到 packages/db 后才执行上述实际证明。

## 后续

继续 F-P179 rerun；P190 路由 BFF 绊线；Q027 helper 已随最新 main 到位，P178 不再因 helper 缺失等待。P189 clientIp 另做小提交。P176 Full/Incr 部分失败运行时、021+索引、F-OS-004 等仍按 mailbox 对账；不将 GET 已合流等同全部完成。生图取消，保留既有文件。
