# P134 透视任务筛选质量报告

## 现状与范围

- 代码 `55c37da`；交审前合 main@1d5052a，merge `0f428fc`。7个Worker文件，无Contract、DB、前端视觉、be2受保护文件、共享composition改动；未push、未执行媒体写。
- pivot2 的可选 `taskIds` 由Registry严格解析，经Adapter传至实际窗口查询；使用真实每日有效任务归属筛选。空数组/省略不筛选，未知任务返回空格子，非法/重复ID拒绝。允许opaque中文ID，不假设UUID。
- 先校验完整授权账户日快照，再筛选并复用Domain函数重算现金、转化、CPA及加权考核空间。即使被筛掉的成员非法/越权仍fail closed，不能通过过滤隐藏损坏来源。
- **source observation/lineage仍证明完整授权读取窗口，不是选中任务的对象数**。cellCoverage、rows和wholeResultTotal才描述筛选结果；无匹配可以rows为空且来源returnedObjects非零。被排除任务缺源时保守保留source partial，不伪称选择集已具备完整来源。

## 测试与证据

日志在本worktree `output/qa/p134/`（本地测试产物，不提交真实数据）。

| 命令/范围 | 结果 |
|---|---|
| `npm --prefix apps/worker test -- --run test/platform-pivot-query.test.ts test/pivot-query-registry.test.ts` | 56/56；green2.log |
| `TEST_DATABASE_URL=<本人55432隔离测试库> npm --prefix apps/worker test -- --run test/pivot-public-query.test.ts test/pivot-query-pg.integration.test.ts --maxWorkers=1 --testTimeout=30000 --hookTimeout=30000` | 18/18，其中真实PG3；http-pg.log |
| 最终 `test/pivot-public-query.test.ts` 重跑 | 15/15；含实际BFF→HTTP与taskIds不匹配空结果，bff-final.log |
| Worker `typecheck`、`lint` | exit0；typecheck-final.log/lint.log |
| 两定向文件覆盖 | platform-pivot-query行/语句/函数100%，分支98.88%；coverage.log |
| `npm --prefix apps/worker audit --omit=dev --offline` | 0；只代表缓存公告，不是在线最新漏洞保证 |
| `git diff --check` | 通过 |

真实PG使用本机55432合成隔离库，包含同账户跨两天换任务、跨媒体同号、跨workspace、未知任务，无生产取数。公开HTTP测试分清synthetic reader与真实PG，不把前者当后者。

## 红绿过程与质量判断

- 首轮新增测试5红/51绿，证明原实现拒绝任务筛选。随后1红是新增it.each参数组织错误，修为对象case后56全过；日志不删除。
- 初次typecheck发现exactOptionalPropertyTypes不接受显式undefined，改为省略未给字段，最终通过。
- 无新增SQL字符串拼接，沿用参数化RR/RO reader；无额外网络请求/N+1，筛选使用Set和原有有界快照，不扩大10k/16MiB防线。未知filters仍拒绝，不自创语法。
- 本批定向未发现未关闭的P0/P1；这不是全项目安全/性能证明。磁盘曾降至2.6GiB，交接时恢复6.8GiB，仍低于全量门禁8GiB阈值，未跑全DB/Worker；不引用P133或arch其他SHA全量数字冒充本批结果。

## 仍未完成

- 启动composition仍待arch将 `createPlatformPivotQuery(pool)` 注入PlatformDataSource；本批只交已可测试factory通路，未宣称运行服务已提供筛选。
- 任意filters语法、额外轴、hourly/gap真实来源，以及P129/P132/P133各接线依赖继续保留。
- 本批为local tested候选，未声称merged/deployed/production-verified。总信箱目标继续active。
