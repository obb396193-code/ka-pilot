# P163：F-P153-1 / F-P153-2 实际接线依赖

日期2026-09-09；实读后端e9fb132与main13923a9。此条是源码核查与解阻请求，不是新成功源实现。生图已取消；P161/P162代码已交审。

## F-P153-1：不是只缺装配

| 实际文件 | 核查事实 | 产品影响 |
|---|---|---|
| `apps/worker/src/data/hourly-public-source.ts:5` | 只有`HourlyQueryPort`接口及`validateHourlySource`，没有可实例化的数据库/上游读取器 | 不能直接在data-api import一个现成factory |
| `apps/worker/src/data-api.ts:55` | QueryService未注入hourly | 当前503属实，不能称小时页可用 |
| `apps/worker/src/etl/incr-handler.ts:119` | 分时只处理focusAccountIds，且支持额外adIds过滤；读取ad_realtime的hh及前一hh做差分 | 数据是重点广告子集，不证明完整账户 |
| `apps/worker/src/etl/incr-handler.ts:154` | 落库差分cost/conversion/realConversion等；未保留累计值或lastSyncTime | sum广告小时不能还原完整账户累计或同步时间 |
| `packages/db/src/ad-hourly-metrics-repository.ts:5` | record类型显式Omit lastSyncTime/dataCorrectionFields；`upsertHourly`仅写广告差分 | 缺数和数据修正也无法从该表恢复 |
| `packages/contract/schema.sql:158` | ad_metrics_hourly无last_sync_time、完整性、源版本、采样范围；主键不含media | 不能拿now/computed_at伪造lineage，亦不能证明跨媒体同广告ID共存 |
| `apps/worker/src/qihang/client.ts:43` | account_realtime只有ds，hh仅对ad_realtime声明并组URL | 接账户小时需要扩client与ETL，不是仅接QueryService |
| `packages/domain/src/hourly-projection.ts:9` | 现有纯函数需要账户累计、budget、lastSyncAt、completeHour与elapsedDayFraction | 可以复用投影；这些输入必须真实、缺则missing，而非默认完整小时 |

Raw路径并非不存在：`metrics_raw.request_params`可保留ad查询hh/payload，但当前仍是广告子集。不能仅有一行ad Raw就认定该账户×小时已完整采样，也不能把某次ad抓取当账户级时间。若按广告加总，必须另有明确全量枚举/覆盖快照且现金系数按日期生效；现有ad_metrics_hourly不是这个证据。

### 最小解阻请求

1. 推荐复用账户`account_realtime`累计快照：请arch确认账户hh可用证据，必要时请OS对同一合法账户做hh=0/前一完整小时/当前小时3次只读，回行级`last_sync_time`、累计单调关系、空行与hh24含义；不需回凭证或真实ID。
2. 冻结账户采样证据存储（允许用现有Raw+request_params，但要冻结run完成/采样范围/截断与hh、时间语义；或加账户hourly表DDL）。随后be可做client→ETL→批准tuple reader→projectAccountHourly→data-api装配。不能擅改arch专属schema.sql。
3. 缺完整小时证据时velocity/projectedDayCost按既有DTO保留missing，不猜；源部分完整不能声明ready。

R011团队快照和R012广告六维**本身都不提供上述个人账户分时事实**。因此不能简单把这个问题归咎于013/014未部署。

## F-P153-2：Gap按源和规则拆开

- `packages/contract/api.md:764`要求`meta.ruleSetVersion`及当前Gap规则阈值；`schema.sql:245`的alert_rules有scope/enabled/metric/operator/threshold，`:446`的condition_tree.version是树格式描述，未冻结整套生效规则集版本、多个scope命中/多阈值合并顺序。
- `packages/contract/metrics.md:53–54`明确Gap=conversion/real_conversion−1；扣量前Gap=attribution_volume/real_conversion−1（可得时）。`account_metrics_daily`已有conversion/real_conversion，个人基础Gap不必等R011/R012；**attribution_volume不在该表**，不能拿回传数反推。可按已有MV/RV返回missing，但窗口扣量率如何加权仍需唯一口径，不能简单平均。
- 账户×日授权、task关联有效日期、biz分组可复用已有semantic/window repository；必须在同一RR快照里保留成员tuple和覆盖再聚合，不以group标签代替授权。
- **R011/013只阻挡team版本化snapshot路径**；**R012/014不是个人Gap的直接必需表**。真正的当前阻断是规则集快照/版本及多规则选择语义，不是“等表建完自然有”。
- `apps/worker/src/data/query-service.ts:432`当前仍503，占位事实保留。最小裁决：请arch冻结ruleSetVersion生成/存储和生效选择（例如授权scope内已发布规则集合内容hash是否允许，以及多个Gap阈值命中何者优先）；无规则/缺阈值可否整组missing。确认后be实现reader+严格source/meta+HTTP，不借固定阈值或当前时间造normal/high。

## 验证和剩余边界

本条没有新增测试通过数字。最新P162已验证hourly21项及gap5项Registry/权限/契约/缺源路径，**不证明真实数据已接入**。现有503保持；未改契约、造采样、访问真实账户或调用媒体写。全目标未完成，等待以上最小裁决时继续其他已冻结任务；其他阻断以P160逐项清单为准。
