# P138 分时 / Gap 参数边界质量报告

日期：2026-09-08。代码 `f5e560e`；权威 main@0d358d8 已包含。

本批只有 `operational-query-request.ts`、测试、本人 index 导出；不改 Contract、数据库、Web 或其他 Agent 源码。复用 approvedAccountAccessSchema 的 media/accountId 和真实 calendarDateSchema。

## 已验证

- 两个固定 queryId，各自 strict 参数；小时0..24，范围不反转/不钳制，Gap按account/task/biz分组；必填媒体与日期；账户最多1000、非空/不重复；未知SQL/scope/workspace/dataView拒绝。
- 与现Registry日期语法一致：ISO/紧凑八位输入统一ISO；非法日历日拒绝。不补默认hh、窗口或账户，不查询系统时钟。
- 45新增 + 33投影 + 39既有rows = **117/117**；Domain全量 **74文件1080/1080**；Domain typecheck/lint 与 Worker typecheck通过。
- 核心行/语句/分支/函数四项 **100%**；依赖未变，Domain离线缓存production audit0。diff检查通过，无网络/IO/secret、无动态执行、无副作用。集合去重O(n)，既有账户规模上限1000。
- TDD RED缺模块后实现转绿，日志 `output/qa/p138/`。测试数组表改为单参数tuple，防Vitest展开数组导致测试未覆盖真实数组；未将构造错误当产品缺陷。

## 未完成边界

这是参数语法层，**不是 Registry/HTTP 已接通、不是数据源可用、也不是授权证明**。现公开Query ID枚举/Envelope/BFF尚未接这两个查询，不能宣传小时页面已可查。

下一独立批继续Registry/Service/BFF，必须保留tuple授权、rows版本和Gap规则版本；真实账户hh源、preDeduction与阈值当前版本证据仍待落实。不能拿小时广告子集或日canonical冒充完整账户小时快照。

未跑PG（无DB代码/迁移变化）；未复跑Worker全量/Nextbuild（磁盘5.6GiB低于8GiB窗口）；未部署、不push、不接媒体写。状态 candidate + Domain verified，待arch审查，长期目标 active。
