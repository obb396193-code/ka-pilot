# P194 Change Log Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 该技能当前不可用；按老板已授权的当前会话连续执行TDD，不开新会话。

**Goal:** 按arch v1.9.44把考核价、日预算卡、渠道系数三种版本记录接成真实只读HTTP/BFF，不拿fixture替代生产数据。

**Architecture:** 复用SettingsChangeLogRepository的RR只读事务、live授权和cursor分页。两笔新迁移先补齐真实表；统一旧值按effective_date/created_at/id顺序计算，外层按修改时间倒序分页。仅注册与service注入触及data-api边界，不碰be2聚合。

**Tech Stack:** PostgreSQL、node-pg-migrate、TypeScript/Zod、Vitest、Next BFF。

## 冻结依据与风险

- 现main@94e9dc25，P194①已交ea694595；本批是②，旧9ed8ec5c只完成内核，未注册。
- 最新api.md v1.9.44⑤用daily_cap覆盖schema.sql旧daily_budget_cap列名；schema.sql由arch维护，回信点名同步，不静默修改架构方契约。
- 当前迁移最高027；下一空号028/029，不复用历史预留编号014。交付前再次merge main核查碰撞。
- 系数created_at先加nullable列，再SET DEFAULT now()，旧行保持NULL，新写入才记真实时间；直接ADD DEFAULT会把迁移时间伪装为历史修改时间。旧行无法证明at时仍明确SOURCE_UNAVAILABLE，不造时间。请arch裁定是否新增at:null的展示形。
- 新预算表沿schema的workspace/task FK；按api v1.9.44“同生效日多版本”不继承旧schema的同日唯一约束，使用effective_date/created_at/id稳定版本索引。只追加版本，不新增预算写路由。撤销行只考核价已有op来源支持，不发明预算/系数撤销上游。
- 无损降级：预算表非空不得DROP；系数非空新增metadata不得DROP。迁移为停写维护窗口，短lock timeout，拒绝时完整回滚；不会为门禁清理真实数据。

## Task 1 — 两笔部署迁移（独立SHA）

Files:
- Create packages/db/migrations/028_task_budget_history.cjs
- Create packages/db/migrations/029_channel_coefficient_audit.cjs
- Create packages/db/test/settings-change-log-migrations.test.ts

1. 先写真PG红例，检查两迁移实际被runner加载（不是测试CREATE假表）。合成隔离库显式TEST_DATABASE_URL。
2. 028建表id/workspace_id/task_id/daily_cap/effective_date/changed_by/evidence_url/created_at与联合FK；数值拒绝负数、NaN和Infinity。
3. 029加created_at/evidence_url；旧行NULL、新行默认真实now，原op乘除含义不改。
4. 测up/down/up、重复up、同任务ID跨空间、跨空间FK失败、预算非空/系数metadata非空downgrade拒绝且原数据完整；旧系数无metadata允许down并保留原值。
5. Run `TEST_DATABASE_URL=<isolated-local-test-db> npm test -- --run test/settings-change-log-migrations.test.ts test/migrations.test.ts` (packages/db)。预期全绿；typecheck/lint；先交独立数据层SHA，HTTP未完成如实记。

## Task 2 — strict wire与三源查询

Files: packages/domain/src/settings-change-log.ts、对应test；packages/db/src/settings-change-log-repository.ts、对应unit/PG tests；apps/worker/src/admin/settings-change-log-service.ts与test。

1. 新行id/op必填，changedBy可null；revoke newValue=null，set按种类为scalar/coefficient。拒绝present-invalid，不能把坏数变missing。
2. 先测回溯写价/未来生效、同日多版本、revoke、缺actor、三源同时间游标、跨workspace/media/撤权；旧值LAG按effective_date/created_at/id，显示按at倒序。
3. 系数独立scope.media，personal必须批准并仍有live grant；team仅当前workspace；task过滤只查相关媒体，不泄漏其他任务/空间。
4. LIMIT51读取50条+sentinel；微秒游标不截成毫秒，无重复/漏行。RR/RO读取、稳定错误、超时、缺表显式不可用。

## Task 3 — HTTP/BFF接线与真实fixture

Files: 新settings-change-log路由/测试、apps/worker/src/data/http-server.ts注册hunk、src/data-api.ts注入；Web对应只读BFF/route/测试（不改视觉）。

1. 仅GET，重复/未知query与自报scope拒绝，服务端session context；保留requestId/exact16MiB。
2. 真PG→真实HTTP产出新版fixture，BFF回放原始响应，不能靠手抄转换过关。
3. 三包全量test/typecheck/lint；本机Web仅定向BFF测试/lint，v1.9.46后Web全量由arch门禁树执行。code-quality-checker检查SQL/事务/覆盖与依赖。
4. merge main，路径限定提交，inbox-arch明确A40/未完项；不push、不调用真实投放写或外网。
