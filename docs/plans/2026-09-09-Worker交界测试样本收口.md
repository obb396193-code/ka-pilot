# Worker Boundary Fixtures Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复Worker四个旧测试样本缺少已合流Repository必填字段造成的类型失败，不改be2服务/仓储/Contract。

**Architecture:** 只给合成fixture补齐真实接口定义要求的可空经营字段及显式合成readiness事实。不把字段变optional、不加cast绕类型，不宣称新字段已经由业务Service投影。

**Tech Stack:** TypeScript / Vitest / existing AccountListRepositoryResult and TaskListRepositoryResult.

---

1. 先跑Worker `npm run typecheck`，确认仍只有4处旧fixture错误；此为红灯依据。
2. `apps/worker/test/account-list-{http,service}.test.ts` 各补 `poolStatus/poolStatusSource/productName/productRef/lastAction/nextSuggestion: null`。
3. `apps/worker/test/task-list-{http,service}.test.ts` 各补 `stage/stageSource/stageChangedAt/sopRunId: null`、`readinessOverrides: []`，readinessFacts按该合成fixture的1个授权账户明确未充值/未基建。只表示测试输入，不是业务默认值。
4. 四文件Vitest真实localhost HTTP回归、Worker typecheck/lint、git diff --check；磁盘<8GiB不跑整包test。独立代码SHA+回执，告知arch/be2只修改be自有测试，生产文件0 diff。

执行技能当前未提供，沿用本会话TDD；不创建Coze项目/不上传源码。
