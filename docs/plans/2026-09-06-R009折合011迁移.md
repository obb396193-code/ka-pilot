# R009 折合 011 迁移 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 arch P039/P044 裁决把回灌状态补丁折入唯一 011，不占用 012，也不丢已有 P0 修复。

**Architecture:** 仅变更尚未整批部署的迁移包；011 在同一维护窗口事务完成字段、约束及旧 done 降级重验。保持 runner 的 checkOrder/singleTransaction，不自动改 pgmigrations；旧双011测试数据库不能直接套用新回退计数。

**Tech Stack:** node-pg-migrate / PostgreSQL / Vitest / TypeScript。

---

执行方式沿用老板已选的当前 worktree 连续实施；executing-plans 技能当前不可用，按本计划逐步执行，不新开会话。

## Task 1 — 先固定迁移包结构（纯逻辑红绿测试）

- 新建 `packages/db/test/migration-011-bundle.unit.test.ts`：加载实际 cjs up/down callback，捕获 SQL；检查 011 文件唯一、两个 CHECK 已并入、列在 CHECK 之前、down 先移除 CHECK 后删列、旧 done 重置仍存在、停写窗口 timeout 与原有 P0 约束均保留。
- 命令：`cd packages/db && npm test -- --run test/migration-011-bundle.unit.test.ts`。预期折合前缺约束/重复编号等真实失败；这只是包结构测试，不替代 PG 执行 SQL。

## Task 2 — 最小折合

- 修改 `packages/db/migrations/011_contract_v1_2_p0.cjs`：在 `failed_stage/finished_at` 建列后原样并入两个 CHECK 与 `UPDATE backfill_jobs SET status='running', finished_at=NULL, failed_stage=NULL WHERE status='done'`；down 先 DROP 两约束再删两列。legacy status 预检显式包含 NULL。
- 删除 `packages/db/migrations/011_r009_backfill_state.cjs`：内容已折入主011，Git 历史可恢复，不删除任何库或业务数据。
- 重跑 Task1，预期通过。

## Task 3 — 真实 PG 回放反例随包校正

- `test/contract-v1-2-migration.test.ts`：上下两011变一011，count2→1。
- `test/auth-migration.test.ts`：回退008及以后 count5→4。
- `test/workspace-sync-migration.test.ts`：回退009及以后 count4→3。
- `test/workspace-kind-migration.test.ts`：回退010及以后 count3→2。
- `test/migrations.test.ts`：只向前运行一个011，最终回放12→11；退至005前从7次减为6次，仍实测跨媒体同号阻止005回退。
- `test/backfill-state-migration.test.ts`：down现在会删 finished_at/failed_stage，legacy插入只用旧表字段；验证done→running、null/非法状态与failed_stage拒绝、非法legacy预检失败后修正重放。
- 命令：专用合成测试库 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_r009_test npm test -- --run test/backfill-state-migration.test.ts test/contract-v1-2-migration.test.ts test/auth-migration.test.ts test/workspace-sync-migration.test.ts test/workspace-kind-migration.test.ts`。
- PG不可用时记录原始连接失败，**不得跳过后声称PG通过**。全回放测试会清空schema，只允许核验过的专用合成测试库，不对共享/内网业务库运行。

## 旧双011数据库的维护边界（尚未执行）

1. 先只读核对目标库和 pgmigrations；旧测试库若包含 `011_r009_backfill_state`，禁止直接删除该记录或关闭 checkOrder。
2. 仅对已确认可回退的合成测试库：用折合前 Git revision 的迁移包先回退两个011，再用折合后包 up。旧011含 workflow_effects，回退会删效果账本等列/数据，因此不能把这个步骤用于有业务数据的库。
3. 若目标已有真实业务数据或不能证明可回退，停留旧包并交arch制定显式向前迁移，不擅自做历史记录手术。尚未部署的首次空库直接用唯一011即可。
4. 当前不重启共享 Docker、不新建/删除数据库、不改 pgmigrations。PG恢复后才核验并执行允许的测试路径。

## Task 4 — 门禁与交接

- DB 纯逻辑 `npm test -- --run test/*.unit.test.ts`、typecheck/lint；migration callback 100%可调用不等于SQL已执行。真实PG可用后全部DB+WorkerPG，最终五包门禁归P045。
- 独立代码 SHA + 留痕 SHA，inbox-arch/P044登记；然后在clean分支 merge main，保留 arch 最新 Contract，012仍不在此批实现。未push/未合流/未部署/真实写关闭。

## 执行回执

- 代码 `85ea1bf`：唯一011+九个代码/测试文件；补丁文件已折入删除，版本历史仍能恢复。最初包测试6例5红1绿，折合后6绿；DB纯逻辑共37绿、type/lint绿、offline audit0。
- up/down callback覆盖100%只证明JS生成SQL语句；**真实PG未验证**。01:53只读连接probe仍ECONNREFUSED，未对旧双011库执行迁移，也未改变pgmigrations。
- 下一步clean合main，待PG恢复后按上述维护边界获取真正up/down/up及五包门禁；未完成整批交付。
