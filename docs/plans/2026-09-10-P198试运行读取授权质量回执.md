# P198 试运行读取授权质量回执

## 交付

代码 **94f3e0e4**，5个本人DB/Worker文件；implemented + targeted_verified，未arch合流/内网部署/push。原HTTP错误/DTO保持，不开媒体provider/执行。

## 真问题与修复

P178沿调用链发现：`ChangeSetDryRunService.perform`初读调用`store.find(ws,id)`，落到后台无Session分支，虽然Service最终403，但已把不在授权账户内的正文/items读入进程；prepare阶段也只按ws/id读后再鉴权。此前详情已改三参，**不代表试运行已接**。

本批Service两次都传私有解析后的approved auth；port的函数类型要求必传auth，防再次漏参。Repository prepare第二参显式存在时必须personal、同workspace，再用既有accountScopeParams/accountScopeClause限定锁定SELECT；无命中在assemble前抛授权错误。显式undefined不回退后台；旧单参内部用法保留。不修改共享helper/契约、不复制权限矩阵。

## TDD与实证

- 新PG红：同accountId的TENCENT无权ChangeSet仍查了items；新prepare授权用例错误地成功。Service新增精确三参/二参断言也红。合计3个预期红，修后绿。
- PG阻断无权正文；prepare直接拒绝跨media/跨workspace/team/undefined，旧内部单参和合法授权均保持；初读后实际事务把parent/items转到TENCENT，第二次prepare拒绝，无provider调用、无execution_run。
- HTTP旧测试有一条仍期望二参调用，更新为携带真实auth，不降低返回断言；同号/跨workspace、503源未接、requestId、exact cap、viewer等旧路由继续通过。

| 定向命令（对应包目录） | 实测 |
|---|---|
| Worker `TEST_DATABASE_URL=<本机合成库> npm test -- --run test/changeset-dry-run-service-pg.integration.test.ts test/changeset-dry-run-service.test.ts test/changeset-dry-run-http.test.ts test/r010/viewer-readonly-http.test.ts` | 169/169，含真PG13 |
| DB `TEST_DATABASE_URL=<本机合成库> npm test -- --run test/changeset-read-authority.test.ts test/changeset-repository.test.ts test/changeset-dry-run.unit.test.ts test/changeset-follow-up.test.ts test/changeset-execution-queue.unit.test.ts test/data-quality-repository.test.ts` | 125/125 |
| DB/Worker typecheck + lint | 通过 |
| DB/Worker `npm audit --omit=dev --offline` | 各0，只是缓存审计 |

共 **294项不同定向**；覆盖率复跑不另加：Service行100%/分支94.49%，ChangeSetRepository行97.01%/分支87.8%。DB覆盖报表来自88项原仓储测试，新prepare授权PG分支另在Worker13项真PG中验证，不能把DB报表当所有新分支都已测的唯一证据。

使用code-quality-checker审查：SQL仅代码生成谓词、值参数化；前台强制auth，后台兼容通道显式区分；短事务不跨preflight网络；错误不返回正文/SQL；不增加依赖。仅合成库和mock预检，没跑消费者/真实取数/媒体写。

## P178审计新增证据与未完成边界

行号基于94f3e0e4：

| 文件/调用 | 本轮确认 | 未证明/后续 |
|---|---|---|
| `changeset-follow-up.ts:24` | 只后台scheduleT1；锁同ws source、item逐行三键、原owner不换；真实PG6项含跨ws/跨media/回滚 | 不是页面授权通道，不套当前点击者scope；执行重验仍属consumer |
| `changeset-execution-queue.ts:12/44/61` | source parent已锁；execution_run按changeset+ws、job payload/原owner核绑定；unit18过 | 没启动真实执行，不据此宣称媒体写已上线 |
| `changeset-reconciliation.ts:10/20/55` | 按ws+source+attempt定位，人工项写同源ws/tuple并回读核对 | 后台恢复流程，当前未做新真实执行实证 |
| `quality/check-handler.ts:36` → `data-quality-repository.ts:45/71/98` | job.workspaceId与payload一致才进入；后台workspace维护，SQL各按ws、metric/account联接带media；原库PG13过 | 非成员页面，queryHealth ETL/quality聚合未公开；不冒称所有跨空间恶意job情形已穷尽 |
| `ad-hourly-metrics-repository.ts:14` | 只有upsert无读出口；冲突media/account不符不覆盖 | 小时存储/021另批，不据此解决一期小时功能 |
| `dry-run-service.ts:85/89` | 本次已补两个Session读取接线点，真实PG证明正文之前拒绝 | 不代表预检provider已接内网 |

P178**全域仍未完成**：pivot/rule定义、account-mute/work-item-command等共享化规范和其调用边界仍要逐项收尾，不能以本批294绿覆盖所有读路径。014/021/F-OS-004/sop-run/P176排队不删。

P196重跑BFF红仍已上报F-P196-BFF，P197解析修复已交c6be1f94；本批不新增临时豁免。磁盘4.3GiB，未跑五包/build/清缓存。生图已停止、历史产物保留。
