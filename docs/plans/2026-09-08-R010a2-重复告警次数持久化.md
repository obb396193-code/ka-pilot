# 重复告警次数持久化 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复实际 createOrMergeAlert 未写 occurrence_count/last_triggered_at 的缺陷。

**Architecture:** 复用已存在的 012 列和当前 advisory lock + 行锁事务。首次插入明确记录一次；同级/低级重复在锁内原子加一，last_triggered_at 表示本平台处理触发时间，不冒充源数据刷新时间。仅持久化，不修改旧详情 DTO 或声称完整处理按钮可用。

**Tech Stack:** TypeScript、pg、Vitest、专用本机 PostgreSQL。

---

## Task 1：真实 PG RED

- Create `packages/db/test/work-item-occurrences.test.ts`。
- 覆盖首次计数/时间、同级/低级重触发、dispatched 并发十次、跨媒体/空间、非法 null/零/负数/溢出计数回滚、终态后新建计数重置。
- 运行 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test npm test -- --run test/work-item-occurrences.test.ts --maxWorkers=1`，确认旧代码失败，保留原始日志。

## Task 2：最小实现

- Modify `packages/db/src/work-item-repository.ts`。
- INSERT 加 `occurrence_count=1,last_triggered_at=now()`。
- UPDATE 加 `occurrence_count=occurrence_count+1,last_triggered_at=clock_timestamp()`；使用拿锁后处理时间，避免等待锁的旧事务启动时间覆盖较新触发时间。WHERE 约束 count 在 [1,2147483646]，非法历史值不伪造恢复，返回零行导致事务回滚。补最大可表示值边界。
- 不改 WorkItemRecord/public schema；013 superseded_by 缺失时，旧原地严重度升级仍明确未达新契约，不把本修复包装为升级闭环。

## Task 3：验证与交接

- 定向新 PG + 原 work-item Repository；DB/Worker 全量串行、typecheck/lint、覆盖率、diff/秘密静态检查。
- main 合流检查；限定文件提交，不 push；回执 P-148 和 R010 状态注明 code/留痕 SHA、测试、未完成边界。
- executing-plans 未安装，按用户连续实施授权在当前独立 worktree 自行 TDD，不另开会话/子 Agent。
