# P151 成员与授权档案只读质量报告

代码 **5d9f9d0**，be/r010。交审前合入 main@a15369a，merge **6ce9242** 只对信箱做union保留双方内容，前端视觉均为arch合入的fe原提交，本人功能提交无视觉改动。候选**未合流、未部署、未生产验证**，无push。

## 实际接通

- `GET /api/v1/admin/members` 与 `GET /api/v1/admin/members/:identityId/grants`，及对应两条 `/api/internal/admin/...` BFF。
- 真实membership/identity/user/account_access_grants/session → RR/RO → Service双重scope校验 →主HTTP生产composition，BFF直接共享Domain schema。
- 必须服务令牌+Session，当前membership admin；只看当前workspace，不把personal admin当全局平台超级管理员。外空间identity与不存在目标均404，不泄露是否全局存在。JOIN users带workspace。**未引入跨个人空间全员目录**；若未来要求全局用户管理，应另冻服务端entitlement，不从浏览器role推定。
- team查询SQL不引用account_access_grants，grantsCount=0，授权档案items=[]；真实1001条遗留team grant不影响读取。personal按media/account二元组保留不同媒体同号授权。
- 成员最多1000，SQL1001哨兵；授权档案同样有1001哨兵，超限拒绝整次，不静默裁切。字段字节数SQL限幅，未知/非法字段不当null；公共只回白名单字段，没有provider_subject、token_hash、BUC/奇航ID/Secret。
- meta dataAsOf=null；joinedAt是membership.created_at上海日期格式，lastSeenAt只取**当前绑定本workspace**的session.last_seen_at最大值，不等同全局历史登录；isActive原样描述membership配置，不声称它代表identity/user完整登录可用性。以上保守映射请arch审。
- GETonly，其他方法405；未知query与非法identity path400；稳定错误/requestId/no-store、exact16MiB均保留。没有身份创建、改角色/授权、停用、转交或媒体写。

## 证据与门禁

| 门禁 | 真实结果 |
|---|---|
| Domain新schema/fixture | 17/17，typecheck/lint通过 |
| DB新仓储unit | 16/16，typecheck/lint通过 |
| Worker新HTTP | 18/18，最后测试清理后再跑18/18 |
| 新真实PG+HTTP | 9/9；含同identity两空间/同号跨media/外空间404/team1001grant/成员1000和1001/角色和成员撤销 |
| 旧路由回归 | 10文件201/201，含上一批calendar及Session/query/tasks/accounts/work-items/details/dry-run/mute/model |
| Worker | typecheck/lint通过 |
| Web新BFF | 5/5；核心BFF+test定向tsc，5个新文件eslint通过 |
| 核心覆盖 | Domain100%行/分支；DB100%行、98.14%分支；Worker Service+Route100%行、96.42%分支 |
| 安全/依赖 | 无package/lock改动，Worker offline cached audit0；diff --check/限定提交show --stat过 |

新定向合计65，另旧回归201。实际PG只用本机55432合成库 `ka_be_r010_20260907_test`，清理仅限测试新建3个workspace及其identity，不接真实凭证、不启动任务队列。

实际定向命令分别为 Domain `npm test -- --run test/admin-members.test.ts`、DB `npm test -- --run test/admin-members-repository.unit.test.ts`、Worker `npm test -- --run test/admin-members-http.test.ts test/admin-members-http-pg.integration.test.ts --maxWorkers=1`。Domain/DB覆盖命令另加 `--coverage --coverage.include=src/admin-members.ts` / `src/admin-members-repository.ts`；Worker覆盖 `src/admin/members-service.ts` 和 `src/r010/admin-members-routes.ts`。原始覆盖与回归日志 `output/qa/p151/`，type/lint/Web结果在本轮工具输出。

Web实际：`node --test lib/data/admin-members-bff.test.ts`；`npx tsc --noEmit --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --strict lib/data/admin-members-bff.ts lib/data/admin-members-bff.test.ts`。未跑Next build/完整Web类型/浏览器。

**磁盘4.1GiB<8门槛，本轮没有三包全量测试**，不借用旧绿灯。TDD从Domain缺模块RED开始；本批无新增依赖。质量技能检查SQL/权限/输出/分页/覆盖；列表单SQL含有界相关count/MAX，不是网络N+1，但大workspace查询成本需关注（SQL15s/锁5s超时）。本次真实1000成员完整读通过，不能外推生产性能SLA。

## 下一步和边界

- 成员创建/停用/权限修改、日历POST/DELETE、灰度开关及整个R015仍未实现，不把本双GET当全治理完成。
- 新main把两个list-service临时移交be2，之后不改；R-FE-IMG-003头像12张已收到，D6成功fixture已修正，排入后续；本批先收口不混资源/其它功能。
- 013/014/016和公开Agent wire原待裁决事项继续留账。全目标active，交审后按有效队列继续。
