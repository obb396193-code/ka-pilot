# 自查07：正式取数 Runtime 批次隔离

代码 `07436afc`，依赖自查06 `febe2af0`，基线包含 arch main `d2ab7ab9`。状态：本地 candidate + synthetic PG/HTTP verified；未合流、未部署、未 push。

## 改了什么

- 正式 `createWorkerConsumer` 对 full/incr 注入已有 fenced `EtlBatchFailureRepository`。不是另造队列，也未改变 backfill、媒体写或真实凭证通路。
- 只有已分类、耗尽重试的数据批次故障，在失败账本写入成功后跳过该批并继续成功批。`done` 可以伴随批次警告，不等于数据完整。
- 账户发现、身份、越权/非法行、DB/lease/失败账本错误继续终止。未采到的格保持缺失；已有格被共享可读性守卫屏蔽，直到同范围新 Raw 与 canonical 重算都完成。

## 门禁与证据边界

测试在 `apps/worker`，PG 指向本机 `ka_be_r010_p194_test`，只用随机合成 workspace；Qihang 使用 fake-fetch，HTTP 使用真实 Data API + PlatformDataSource + PG，SessionAuth 为批准上下文测试桩，**不声称真实 OS 或真实登录验证**。没有常驻 worker。

| 命令 | 结果 |
|---|---|
| `npm test -- --run test/etl-partial-runtime-pg.integration.test.ts test/etl-partial-pg.integration.test.ts test/etl-partial-handlers.test.ts test/partial-query.test.ts test/etl-batch-failure.test.ts test/qihang-protocol-pg.integration.test.ts test/qihang-protocol-retry.test.ts`（带 TEST_DATABASE_URL） | 7 files / 55 passed |
| `npm test -- --run test/job-identity.test.ts test/job-consumer.test.ts test/data-api-http.test.ts` | 3 files / 76 passed |
| runtime PG + partial-query 定向 coverage | 18 passed（与55重复）；行99.24%，分支90.32%；runtime行98.86/分支71.42，partial-query行100/分支92.72 |
| `npm run typecheck` / `npm run lint` | 通过 |
| `npm audit --omit=dev --offline` | 0 vulnerabilities；缓存审计，不冒充在线新公告检查 |

真实链路分别覆盖 full/incr：51账户拆50+1，50故障→ledger→canonical→readiness=false→HTTP partial；恢复取数后只有Raw仍false，再消费canonical→readiness=true→HTTP完整51行；另两例发现失败与越权元数据无批次账本、无Raw、无canonical。requestId从实际HTTP响应头验证，私有上游body/qid未进入警告。

红测记录：最初测试令牌不足32位触发配置检查，先修测试数据；随后 full/incr 实际 runtime 均重排queued而非done，证明缺注入。接线后测试误从lineage取requestId，改为既有响应头，不改Contract。最终全部绿。

磁盘不足8GiB，遵循arch限制未跑五包全量/build。未动前端、公共Contract、其他角色Service。既有未跟踪输出原样保留。

## 剩余

真实内网 full→页面真数仍需 OS/部署实证。本批不是数据全功能完成；按老板最新优先级继续 P211 看板维度、筛选、BI指标和 filters 接口，以及账户小时公开装配/采样闭环。非数据功能与生图不推进。
