# 小时查询正式源 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将025真实账户小时reader接到已有account.hourly公开查询，保持无表/缺采样明确而非借日表伪造。

**Architecture:** 新Worker PlatformHourlyQuery实现现有HourlyQueryPort；批准personal tuple传AccountHourlyReadRepository，同RR读小时/系数；复用projectAccountHourly生成累计/差分。缺前驱、缺系数、缺小时均保持missing。正式data-api.ts只增加factory注入，不改鉴权/Registry/公共信封。

**Tech Stack:** TypeScript、Zod、pg、Vitest、既有小时投影与025表。

---

按writing-plans逐项TDD，code-quality-checker核边界；执行技能不可用，由本人继续，不派Agent。P211公开形状在arch信箱待核，先推进另一条已冻结数据接口。

## Task 1

新 `apps/worker/test/platform-hourly-query.test.ts`：真实Registry解析后注入假reader，累计/前驱差分/乘除系数/缺源，跨三键/重复/坏行拒绝，team在reader前拒绝，10k/16MiB守卫，来源时间不造新鲜度。先运行缺模块红测。

## Task 2

新 `apps/worker/src/data/platform-hourly-query.ts`，复用accountHourlyStoredRowSchema（只按reader允许sourceRunId=null），validateHourlySource最终守卫。已知dataAsOf取当前显示样本的最老last_sync_time；未保存dataset timezone/dayCut/version则null，不用部署默认值。**sourceUtcOffset当前只在Raw/payload，不在reader快照：elapsedDayFraction暂unknown，projectedDayCost=missing并告警；不能从ISO的Z假定业务日UTC。** 该缺口报arch，后续用真实源元数据补齐。

## Task 3

新增专用PG+HTTP用例，随机workspace，用已落025表而非临时假表，只有授权tuple可见；查询actualHTTP带requestId，空数据partial，team503/无Session401。data-api.ts注入factory；PG/HTTP、既有hourly/启动回归、type/lint/audit、覆盖率。无真实采样job启动、无媒体写、无前端变化；代码与QA独立SHA。
