# P201 派发迁移预检回执：未启用014

## 状态

探针代码 **e8c999b9**，仅`packages/db/scripts/probe-dispatch-migration.ts`。这是非生产诊断，不是migration implemented。没有增加生产迁移、没有切checkOrder、没有改Contract、没有创建持久派发数据。待arch裁三项后实现014；不将等待裁决当全信箱停工。

## 实测（本机专用合成库）

```sh
# 在 packages/db 运行；禁止换成共享/生产库。
TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test ./node_modules/.bin/tsx scripts/probe-dispatch-migration.ts
```

实际输出：

```json
{"sameWorkspaceAccepted":true,"crossWorkspaceAccepted":true,"rolledBack":true,"orderRejected":true,"installedMigrationCount":18,"migrationHistoryUnchanged":true,"productionMigrationAdded":false}
```

1. **外键**：原样提取schema.sql的dispatches CREATE，在随机私有schema内运行。同空间派发可入；**A空间派发引用B空间work_item_id同样可入**，因为冻结DDL只FK work_items(id)，两个user FK只能限制from/to在A，限制不了工作项的workspace。不是猜测，也没有在真实业务数据上验证。
2. **顺序**：专用库已有18个迁移、015及以后已安装。复制当前迁移目录，在临时目录补014 sentinel后调用同款node-pg-migrate（checkOrder=true/singleTransaction=true），报`Not run migration 014_dispatches_probe is preceding already run migration 015_...`。sentinel没有执行DDL，前后pgmigrations全行完全一致。不能通过假改run记录或静默关顺序闸解决。
3. **下游接源（静态）**：be2 `task-timeline-repository.ts:73-126`仍只有四源UNION；`:145`却仅靠tableExists(dispatches)就清掉unavailableKinds。只建表会变成“声称dispatch源可用、实际永远不查”。表落地必须同时派be2接第五源/共享权限/分页，不是只加to_regclass检测。

## 清理与验证

事务始终ROLLBACK；同workspace合成行及随机schema的to_regclass均校验不存在。迁移副本只删除本次mkdtemp生成目录，未删除仓库/数据/既有产物；数据库与迁移记录未变。探针仅输出布尔和迁移数量，没有ID、URL、凭证或业务正文。

DB typecheck/lint、缓存production audit0；脚本不在包tsconfig include，额外显式执行strict NodeNext `tsc --noEmit ... scripts/probe-dispatch-migration.ts`与eslint，均通过，不将包tsc冒充覆盖脚本。当前同步main **c309fdb6** 后路由库存/解析/pending **19/19**；这是P197门复验，不是014门。低磁盘未五包/构建。

## 提请arch冻结（建议不代裁）

- **迁移物理编号/已部署库升级**：建议给派发表分配新的顺序追加编号（逻辑上仍属于R012/原014），保留checkOrder。另一方案仅允许未越过012的全新部署按014升；对已升后续版本的库不能要求有损回滚。若坚持保留014，需给受控补迁移机制/部署窗口，不全局关闭checkOrder。021同样落在已安装022/023之前，需要一并明确。
- **跨空间约束**：建议work_items加`UNIQUE(workspace_id,id)`，dispatches用`FOREIGN KEY(workspace_id,work_item_id)`引用它（默认RESTRICT），保留已有from/to联合FK。涉及冻结schema，须arch改；本人不自行加字段或改键。
- **联动范围**：请派be2接timeline实际dispatch源（读work_item三类矩阵、ws隔离、分页、missing语义），或建表期间继续明确source unavailable直到reader真接通；也检查其me/counts等依赖，不能以表存在替代接口完成。

采取brainstorming的设计冲突检查与writing-plans记录，不重启产品选择；code-quality-checker核验探针风险/回滚/安全输出。当前只暂缓014/021启用，不擅自实施上述选择；准备继续无其依赖的F-OS-004（020仓储已合main，实际位于`src/identity-password-repository.ts`而非旧信箱中的r014路径）。
