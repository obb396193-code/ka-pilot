# R010 剩余闭环与复合规则待裁

核验点：后端 `be/r010@62aa431`，权威 `main@b8d2b0b`。本次是现场依赖审计与待裁提案，**不是功能交付或完成验收**。上一轮297f736/62aa431为实际进展；本轮没有重复运行旧测试冒充新代码证据。

## 1. 复合规则需要定下来的判断语义

已冻结：`schema.sql:446`、arch P005#1 的 version/all/any/not 结构与 metric/operator/threshold/window_hours；`metrics.md:93-98` 要求任一引用指标缺失就整条不判，不触发、不消触、不递增。当前代码搜索没有 condition_tree 编译器或正式数据provider；`rule-readiness.ts`仅接受调用方提供的requiredMetrics，不能据此证明树引用已完整。

建议A：解释执行受限数据AST（抽象语法树），不生成JS/SQL，不用eval。备选B：编译到受限SQL，查询/权限/三态/窗口都要同步证明，当前复杂度和审计面更大。不推荐让Agent自由判断同一条件树，否则同一证据不能稳定复现。

请arch一次冻结以下精确语义后，be才能接完整规则解释器和provider：

1. **布尔结构**：建议同节点多个组按AND组合；all全部真、any至少一真；not数组采用“每项都不成立”即NOT(OR(...))，不是NOT(AND(...))。例如A=true/B=false，前者不触发、后者触发，不能当无关实现细节。请同时明确嵌套组是否允许、空all/any/not是否拒绝；建议无任何叶子拒绝，空组省略。
2. **窗口定义**：`rules/list.json:6-24` 名称“持续2日”但条件只有cash_cpa>assessment_price、window_hours=48。48小时累计CPA超标不等于两个日窗均超标。建议明确window_hours只是该叶子取数窗；持续两日须显式窗口/连续条件，不从名称反推。canonical只有日累计时不能冒充任意48小时滚动数据。
3. **动态阈值**：threshold="assessment_price"是否与metric同窗？跨考核价版本时不能拿末日价乘整个窗。建议取逐日加权现金考核，但这是新增树取数语义，需arch定。字面字符串不解释成表达式，更不能eval。
4. **指标口径**：`rules/list.json`规则7仍以账面cost>500做自动暂停条件，而`metrics.md:85`账面只展示、策略用现金。请明确该fixture改cash_cost还是列为特定“账面花费上限”例外；be不擅改Contract样例，也不在适配层将cost偷偷重命名现金。
5. **可解释DTO冲突**：api.md:578是snake_case rule_id/tree/passed+小写reason，api.md:800及fixture为ruleId/leaves/pass/notTriggeredReason。建议以后发fixture结构为权威并删旧描述；RatioValue无限CPA以及动态阈值缺失时value/threshold字段怎么表达需补一组样例，禁止把无限变0。
6. **SLA暂停证据**：缺数时要求SLA暂停，但work_items只有sla_due，无pause_started_at/累计暂停或评估历史持久化。建议arch定义暂停区间/恢复事务所需字段和迁移；不能拿本次扫描时间猜上次缺数起点，也不能把进程内Map作为可恢复的唯一证据。

内部资源界限（建议，不是产品能力承诺）：最多128叶子、深度8、只允许六种比较符、symbolic threshold只允许注册指标名。异常树拒绝而非截掉后半树；任意missing/error在all/any/not中都强制undeterminable，不能被短路掩盖。阈值引用也进入requiredMetrics，按(metric,window)识别，不用单metric key覆盖不同窗口。

## 2. 现有真实入口与运行链

- `apps/worker/src/data/http-server.ts:370-385` 只挂三类列表、两类详情、data/query、semantic query、admin reconcile，另有auth Session和/healthz。`/healthz`是进程健康，**不是**产品`/system/health`。
- `apps/worker/src/data-api.ts:33-67`组合这些Service。沒有工作项动作、任务写、变更集写、规则explain的composition。
- `apps/web/app/api/internal/`当前为auth四路、tasks、work-items/:id、changesets/:id、data-query、query。不能把已做一个BFF误报成R010#10“全部BFF”。
- `apps/worker/src/runtime.ts`现有数据同步/回补/质量/通知失败/session清理组合，没有挂RuleScanHandler、changeset_execute或t1_recycle；已存在的执行类不等于生产job已消费。
- migrations只有001至012；013及后续014–017不存在，不能称后续领域已落库。

## 3. 未闭环清单（不是缩小原总目标）

| 信箱原项 | 当前可证明状态 | 还缺什么 / 当前依赖 |
|---|---|---|
| R009/R013 bootstrap与账户发现、系数seed | 已交并有arch历史验收；当前范围未重做 | 最终全量仍需当前版本PG，不沿用旧环境数字 |
| R010#1 / 012 | 迁移文件存在、arch代码审已过 | PG恢复后up/down/up及当前全量硬门 |
| R010a1 summary/trend/table/compare | 公开入口与两源窗口已候选；个人task窗口已接 | P073旧fixture；team task窗口未支持；columns/owner等尚未做 |
| R010#6 dimension/health/tier/pivot2/hourly/gap/export | 部分内部查询/三键已候选 | P077公开三键/priceSource/lineage，后续专门查询与导出仍未接 |
| R010#9 system health/etl-runs | 没挂产品入口 | P077未知健康指标表示、etl run UUID/BIGSERIAL与计数归属 |
| R010#5 账户详情/structure/timeline/star/tags/transfer | 普通账户列表在；详情族没挂 | P077小传版本/未知值；pool/product/transfer需015，不前移表 |
| R010#2 工作项动作/去重 | reject内核、旧mute封锁、account_mutes存取候选 | P083 dispatched/活动态/关旧建新字段、动作参数；P096mute日期/作用域/atomic ignore；新详情DTO与timeline |
| R010#2 规则/coverage/SLA/explain | 缺数门、现金三规则内核候选 | 本提案六问、正式provider、SLA持久化、public explain；现在不能叫每天能巡检 |
| R010#3 变更集全流程 | typed/hash/dry-run/retry/一次核查/原子queue/T1调度候选 | P092重放状态、P093反向关联与逐attempt结果；生产消费者、Provider、HTTP/BFF、T1回收还未接；不提前开写 |
| R010#4 任务详情/CRUD/改价/重算通知 | list/pacing/关系与价格Repository存在 | 写请求DTO与幂等边界，重算完成前response的recomputed_days语义，实际通知/ack记录；预算表依赖014 |
| R010#7 日报、#8 Agent SSE、#9 rerun/search、#11 Registry | B级内核不是新版HTTP交付 | 信箱R010b排R012之后；媒体方法冲突和能力资料尚需实际复核/适配 |
| R011#1–5 team snapshot | P082提案已交，无013 | arch先冻结存储/快照身份/分页完整证明与readiness；再实现staging/publish/真PG，不复活旧Task6 |
| R012#1–6 + settings/8维追加 | 只存在旧内核/文档，不等于HTTP | 顺序等R011；014未落；素材/结算新定义以后发R015为准；bid_tool映射提案可先做只读调研 |
| R013b install/worker-once/session清理 | 入口/单轮/清理已候选并历史验收 | 三份真实f.yml、clean-clone内网安装、打包/部署实证等OS；不得在本机自行造FaaS模板 |
| R014 v1.5 / v1.5.1追加 | 015不存在，不能当实现 | 排R012之后；账户池/批量/阶段/工作流/资产/报表/集成/me等全部保留 |
| R015 v1.6 | 016不存在；B12–19源码不等于新契约已接 | 排R014之后；素材/结算/admin/pivot2/测试/复制均保留 |
| R016 v1.7 | 017不存在 | 排R015之后；策略/归因/竞情/shadow/AI影响/周报/复盘/负责人知悉/月报均保留 |
| R-FE-IMG-001 | 四PNG候选保留在output，未正式提交 | 老板选型→正式尺寸/预算→交fe；不自行批准视觉 |

## 4. 给arch的推进请求

请优先关闭P073/P077（恢复数据读闭环）、P083（工作项动作/活动态）、P082（解除R011→R012→R014后续依赖）；本规则六问可并行裁。若希望先做014或R010b独立切片，请在信箱明确改顺序，be不自行越过已写明的依赖。

本轮新增的设计疑问按brainstorming技能先提案，不在未批准前实现会影响投放判断的布尔/窗口/SLA语义。没有代码/迁移改动、没有新测试/部署/PG通过声明，不改总目标；依赖等待不是已完成。
