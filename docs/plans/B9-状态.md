# B9 后端纵向闭环与批量性能状态

> 分支：`be/b8a`
>
> 基线：`3d90bed`
>
> 当前状态：方案 A 已完成；真实 PostgreSQL 纵向闭环、Canonical 批量化和性能证据已收口，等待 Claude/arch 按 P-016 审查。未接真实奇航/Multica/OS，未新增公开契约或生产 Job。

## 已完成

1. CanonicalStore 从逐账户 settings/history/upsert 改为默认 250、最大 1000 的有界批量端口。
2. 复合 workspace/account/date 键的缺失、重复和越界返回 fail-closed；全部批次完成后才派生质量 Job。
3. MetricsRepository 使用参数化 JSON recordset 批量读取有效配置、历史非零消耗和 bulk Upsert。
4. 真实 PostgreSQL + 假奇航纵向链路已打通：Full ETL→Raw→Canonical→质量→语义查询→规则→工作项→报告事实。
5. 规则候选只在集成测试中从真实语义结果生成，不注册未冻结的生产规则/报告 Job。
6. Full/Incr ETL Run 补齐 workspace_id，运行记录不再丢租户归属。
7. 修复质量对平只看 offline Raw 的假异常：按 Canonical cost 来源选择 latest offline/realtime Raw。
8. 真实 PG benchmark 只允许本机测试库，覆盖 100/1000/5000 行、自动清理和代表性 EXPLAIN。

## 关键提交

| 提交 | 内容 |
|---|---|
| `2240510` | B9 设计 |
| `e3c7a39` | B9 逐文件实施计划 |
| `83d7e4f` | Canonical 批量 Handler/Repository/Runtime/合成基准 |
| `8c6d5e1` | 真实 PostgreSQL 纵向闭环与 ETL workspace 修复 |
| `42efc5c` | 真实 PG benchmark 与查询计划证据 |
| `c85beb8` | benchmark 自动覆盖、复杂度和批次中断测试补强 |
| `83cf855` | realtime/offline 数据质量对平修复 |
| `df07deb` | 最终质量报告、总账和 P-016 审查入口 |

## 验证结果

- 默认测试：Domain 193 + DB 77 + Worker 156 + DingTalk Gateway 19 = 445 passed；Worker opt-in SDK smoke 默认 1 skipped。
- 真 SDK smoke：1 passed，真实 Claude Agent SDK 子进程→localhost gateway→fake upstream。
- Coverage：Domain 95.43%、DB 93.61%、Worker 91.69%、DingTalk Gateway 86.62%。
- B9 关键模块：PG benchmark 98.34%、合成 benchmark 98.21%、Canonical Handler 93.81%。
- 四包 typecheck/lint/audit 全绿；audit 0 vulnerabilities。
- 变更生产代码 complexity≤10、单函数≤100 行；迁移 up/down/up 通过。
- 相对 `3d90bed`：`packages/contract`、`packages/db/migrations`、前端零差异。
- 本地测试库 `pg-benchmark-%` 合成 workspace 残留实查为 0。

## 性能结果

默认 chunk 250，真实 PostgreSQL 三次运行中位数：

| 账户行 | 端口调用 | Canonical 中位耗时 | 最终行数 |
|---:|---:|---:|---:|
| 100 | 7 | 16.818 ms | 100 |
| 1,000 | 16 | 71.545 ms | 1,000 |
| 5,000 | 64 | 394.974 ms | 5,000 |

调用量相对原 `3N+4` 分别减少 97.70%、99.47%、99.57%。这些是热缓存本机测试结果，不是生产 SLA。

## 明确未做

- 不接真实奇航、Multica/OS、Secret、Provider、钉钉 Stream 或 FaaS；
- 不新增生产 rule scan/report job、触发器、公开 API/DTO/Schema；
- 不补知识库 DB/API/前端，不补工作流 React Flow 前端；
- 不处理 Raw append-only 重试去重；当前 latest-row 读取和 Canonical Upsert 会收敛，但 Raw 审计表可保留重复抓取；
- 不解决奇航 1000 IDs 以上的正式分片和 ad_realtime 极宽响应→Raw INSERT 参数上限；
- 不验证并发 workspace、冷缓存、锁竞争、WAL、FaaS CPU/RAM 和 p95/p99。

## 下一步

1. Claude/arch 审 P-016，重点看批量 SQL、部分批次语义、Raw 重试语义和质量 source 选择。
2. 合并前重算 `fe/f001` 与 `be/b8a` 冲突，不在 Claude 脏工作树直接 merge。
3. 拿到真实通路后按 Gate A-E 联调；写操作继续坚持 preview/确认门。
