# F-P139-1 日历数字排序边界计划

目标：复现并修复10000条合法结果502，不放宽排序、数量或安全整数契约。

现状：SELECT id::text AS id 同时 ORDER BY id DESC，疑似使用输出文本别名。不同位数ID会按字典序排序，严格Domain要求数值降序而拒绝。先真实PG跨位数反例，不凭猜测修生产。

1. 现有HTTP/Session/PG测试中新增两条跨位数合成ID，断言较大数字在前；限定清理本人fixture，不改全局sequence。
2. 红后将ORDER BY指定原表BIGINT列，不改输出字符串/安全整数守卫。补SQL纯逻辑断言。
3. 定向Domain/DB/HTTP/真实PG、type/lint，先完成独立代码SHA再质量报告和P153回执。磁盘不足8GiB不跑全量，不清缓存。
