# 小时采样原子落库 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 account_realtime(hh) 的原始证据与025小时快照原子保存，供随后接入已授权ETL使用。

**Architecture:** Domain内部存储边界 + DB仓储；不增加公开DTO、媒体写、后台队列。复用现有etl_incr作业的真实lease/attempt/owner，SQL同时核对run及job冻结范围；每批至多50账户，在短事务里写Raw+小时行，丢失租约回滚。旧采样不得覆盖新采样，Raw仍追加作为历史证据。

**Tech Stack:** Zod / PostgreSQL / Vitest；不引入依赖。

---

## 边界

### 进行中收到新裁决（main@d2ab7ab9）

arch v1.9.24已明确页面业务日readiness并授权三调用点，v1.9.25允许025单发、后续迁移只增不补号。此批已经实现并通过70条PG/33条Domain，收口独立提交后立刻转自查02接线→P176 Runtime→P211；不继续扩小时job。025注释同步新裁决；旧质量报告的发布阻断作为历史已被覆盖。

起点ad5e9838，main仍950b45e9无新裁决。本批先做小时链路必要持久化部分，不称完整小时job/定时器/页面真数据已完成。writing-plans与code-quality-checker用于红绿测试及质量留痕；executing-plans不可用，用户已批准本会话连续本地实施，不转Coze、不另派Agent。

## Task1：内部存储边界

Files: `packages/domain/src/account-hourly-storage.ts`、`packages/domain/test/account-hourly-storage.test.ts`、`packages/domain/src/index.ts`。

1. 先写红：三键/日期/小时与context严格相同、重复/越权账户/非法数值/时间/ID拒绝。
2. 冻结输入为jobId/leaseToken/runId/workspaceId/media/accountIds/ds/hh/sourceUtcOffset/sampledAt/rows/rawRows。normalized rows沿用已有采样函数字段，不发明上游字段。
3. rawRows与rows账户集合一一相同，允许请求账户无返回但不补0；source timestamp仅取既有normalize结果。complete按已冻结sampledAt≥小时末+5min复核。
4. 50账户一批、16MiB exact拒绝；Domain纯逻辑回归。

## Task2：真实事务仓储

Files: `packages/db/src/account-hourly-write-repository.ts`、`packages/db/src/index.ts`、`packages/db/test/account-hourly-write-repository.test.ts`。

1. 先用真实PG测试缺仓储红；套件自己迁移，随机workspace，禁止共享/ka。
2. BEGIN/超时/锁jobs+etl_runs，核对workspace、owner、run=incr、job=etl_incr、attempt、job payload及run scope的ds/accountIds；拒绝team workspace，缺credential owner不降级。
3. 两次参数化批量SQL：Raw仅account_realtime/realtime，request_params保留hh等业务参数不含userId；小时upsert按五键，只有sampledAt较新、lastSyncTime不倒退才覆盖。
4. 提交前再次查租约时间，过期则整笔回滚；错误统一清理不回传SQL/body/身份。
5. 真PG验证三键隔离、旧/并发/重复采样、Raw后失败原子回滚、租约/attempt/owner/run越界拒绝、源缺失不造0、snapshot读回。

## Task3：Worker类型复用与质量

Files: `apps/worker/src/etl/account-hourly-sample.ts` 保留兼容重导出，将既有纯映射机械搬至 `packages/domain/src/account-hourly-sample.ts`，Worker与DB共用；保持现有纯函数行为。自查新增反例证实「normalized cost 与 Raw cost 矛盾」原先可通过，存储边界现在用同一映射复核，避免两份数值/时间证据互相矛盾。

跑Domain定向/类型/lint、DB真实PG+小时reader/类型/lint/缓存audit、Worker采样定向/类型。检查unsafe JSON/SQL/日志，覆盖核心新增路径>80%。独立代码SHA与质量/信箱SHA，不push。不在未有readiness日期裁决前启用P176 partial Runtime。
