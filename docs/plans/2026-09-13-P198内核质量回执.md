# P198 去重/重试与单轮消费者内核 · 质量回执

状态：内核已交审，整体P198未完成；不是实际投递器交付，不称合流/部署/群验证。计划与接收人待定项提交 `787a5bb5`，代码 SHA `0aa677fc`（5文件/435新增行）。

## 范围与证据

- Domain `outbound-delivery.ts`：五段业务身份 tuple 编码后 SHA256，24h sent 证据判定；只有明确 acknowledged 才 sent；连续两次 unknown 终止；已知失败打断 unknown 连续计数但不重置总尝试；最多5次；60s起指数退避是最早重试时间，不是调度承诺。
- Worker `notifications/outbound-once.ts`：独立、有限 batch 的单轮编排，配置缺失不碰锁/库；领取后严格验证 workspace/channel/fencing/sentAt/attempts；同轮重复 id 拒绝；sent 证据校验 workspace+key；锁丢失后不再发；发送成功但结果落库失败停止本轮，由后续持久层恢复不确定结果。
- 内部 ports 的 lock/claim/finish 仍待实际 PG 接线，不能用 mock 通过宣称单飞/原子领取已由真实库证明。`markDeduplicated` 是待接线动作，不是自行新增已冻结的公共状态。
- 不输出原始 error/payload/URL/token；未知 transport envelope 视为 unknown，不谎报 sent。未新增依赖、没有网络请求实现。

## TDD 与质量检查

1. Domain 首轮：缺模块，suite失败；实现后37/37。初次 lint 命中控制字符正则规则，改为显式字符码检查后通过。
2. Worker 首轮：缺模块，suite失败；先27/27，补锁异常/中断边界后32/32。初次 lint 命中 unsafe-finally 与测试未使用参数，修正后 typecheck/lint 通过。
3. 定向覆盖：Domain statements/branches/functions/lines均100%；Worker statements/functions/lines100%、branches98.11%。仅覆盖两份新实现，不代表全部系统。
4. code-quality-checker 自查：异常净化、bounded batch、相同ID重复返回、无凭证不消费、跨workspace/不同dedupe证据、future sentAt、先claim后send、未知两次、5次封顶均有测试。尚未实现的持久化/真实transport/SSRF/凭证解析不计已完成。
5. `npm audit --omit=dev --offline --json`：Worker缓存审计0漏洞。未联网刷新 advisory；package/lock零改动。

## 全量门禁（本机合成库）

| 包 | 命令 | 结果 |
|---|---|---|
| Domain | npm test -- run && npm run typecheck && npm run lint | 104文件/1650项通过；typecheck/lint通过 |
| DB | TEST_DATABASE_URL=独立合成库 npm test -- run && npm run typecheck && npm run lint | 161文件/1832项通过；typecheck/lint通过 |
| Worker | TEST_DATABASE_URL=另一独立合成库 npm test -- run && npm run typecheck && npm run lint | 209文件/2475项通过；另2外部opt-in跳过；typecheck/lint通过 |

原始日志：`output/p198-domain-full.log`、`output/p198-db-full.log`、`output/p198-worker-full.log`。Docker/55432实读正常；DB使用既有独立合成库 `ka_be_r010_p194_final_test`，Worker使用 `ka_be_r010_p194_worker_test`，未读取生产凭证或真实账户。

最后三包命令进程均退出0；本批没有后台发送器。提交前 `git merge main` 为 Already up to date（main@1e304507），`git diff --check` 与提交后 `git show --check` 通过，`git ls-files --others --exclude-standard apps packages` 为空。未动既有未跟踪output/审查文件，不push。

## A40 / 剩余工作

- 未改变任何公开HTTP/BFF/页面响应；新增的是内部Domain导出和未注册Worker函数。be2数据目录、DB生产代码、Web/视觉文件均零diff。
- P198尚缺：实际outbound迁移/仓储、去重命中行终态、崩溃恢复、Group/DM目标解析与模板、签名Transport、单次CLI/HTTP与diagnose接线、实际PG领取/并发/故障验证。不能把旧DB回归当成这些新能力的证据。
- `daily-report-repository.ts:293-294` 目前未知状态映射queued；如引入deduplicated应同步通知状态口径，否则又会显示永久排队，已向arch提问。
- 等arch的接收人/模板定形期间，可以继续已冻结的P193 v1.9.46，不能继续使用旧workspace-local候选。P194历史未知at/前端弹层接线仍未完成。
