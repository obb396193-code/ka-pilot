# P143 规则定义读取质量报告

代码候选：`05fb7484199daac8ada7c20f5216b57f440aa6b4`。权威 main 为 c3451db；未合流、未部署、未 push，未开放媒体写。

## 实现与安全边界

- Domain strict 内部定义/目标，BIGSERIAL 保留 int64 字符串；复用冻结 scope 三类 union，不造上游字段。
- Repository 在 RR/RO 内读规则及账户当日有效任务/业务绑定。personal 先显式 tuple grant，team 限当前 workspace；参数绑定，无账户与越权安全错误。
- 数据库先测量显式字段投影，exact 16MiB 不向应用传 payload；非法 scope/tree/日期/字段拒绝。读取后复制不透明 AST，防后续 await 被适配器改写。
- 真实 PG 验证并发修改下定义/绑定一致快照；内部结果不得作为完整规则公开给浏览器。legacy null tree 原样保留，未冒充已支持的 AST。

## 精确版本门禁

证据位于本人 worktree `output/qa/p143/`，仅合成数据，无真实公司账户或凭证。

| 门禁 | 结果 | 日志 |
|---|---|---|
| Domain 全量 | 76 files，1115/1115 | domain-full.log |
| Domain 规则新旧定向 | 70/70 | domain-cover.log |
| DB 最终全量（unit + 真实 PG） | 90 files，969/969 | db-full-submit.log |
| DB 新仓储 unit | 22/22 | db-cover-submit.log |
| DB 新仓储真实 PG | 11/11 | pg.log |
| Worker 最终全量 | 142 files pass，1541 pass，2 外部 opt-in skip | worker-full-submit.log |
| Web node 回归 | 196/196 | web-test.log |
| Domain/DB/Worker typecheck + lint | 全通过 | 各包 type/lint 日志 |
| 生产依赖离线 audit | 0 | audit.log |

DB 最终日志包含所有新增单测，不使用早先964/968快照充当最终结果。Worker 最终重跑针对提交的相同源代码，退出0。真实 PG 只用 localhost:55432/ka_be_r010_20260907_test，串行运行；无共享 ka fallback。曾有一次审批超时，工具明确进程未创建，重试后成功，不算测试失败或仍受阻。

覆盖率：新 Domain 定义100%行；AST100%行/98.43%分支；Repository100%行/93.18%分支。无新增依赖。diff check、保护路径审查通过。质量技能促成超大投影在PG截断、私有AST复制及安全错误反例。

## 未完成与下一步

本批仅规则定义/适用性读取，不是公开 explain、规则页或生产触发已完成。真实指标/initialFull/readiness/freshness、mute/dedupe、动态扫描 provider、HTTP/BFF仍待接。下一组合必须将规则与指标置于同一 RR 快照；不能把两个独立 read 声称同一快照。内部大ID与公开 numeric fixture接缝不得舍入。

旧013/schema及ETL公开字段问题保留，未以新底层批掩盖。当前磁盘恢复允许补全DB/Worker门禁；未做Next build，全UI类型旧依赖问题未声称修复。整个信箱目标仍 active。
