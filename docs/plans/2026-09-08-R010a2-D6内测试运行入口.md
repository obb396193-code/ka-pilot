# D6 内测试运行入口 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 子技能不可用，按用户连续执行授权本地TDD。

**Goal:** 按arch@0d358d8明确授权，将D6 POST真实挂到主HTTP服务；内测无媒体Provider，合法本人草稿明确503，而非404或虚构成功。

**Architecture:** 原有Bearer+Session解析后调专用R010路由及P129服务；runtime只注入Repository，不注入preflight，故先核对草稿/权限/TTL再SOURCE_UNAVAILABLE，不能prepare/record/enqueue。保留请求限制、requestId、team拒绝和只读旧路由。

**Tech Stack:** TypeScript、Zod、Vitest、PostgreSQL。

### Task 1：失败测试

- 新增 `apps/worker/test/changeset-dry-run-http.test.ts`，真实HTTP主壳验证401/403/400/405/413/404/409/503，team/伪造header/非法body不能运行命令；有效请求从原服务得到503。
- 定向先跑RED，记录旧路由404和无入口的失败。

### Task 2：最小接线

- 新建 `src/r010/changeset-dry-run-route.ts`：只POST、UUID路径、空对象请求、严格auth、稳定错误映射；这是明确disabled-preflight内测模式，意外成功不透传旧内部record DTO而502。
- `src/data/http-server.ts`：本人if链加入路由选项及相同Bearer+Session门，不动be2数组。
- `src/data-api.ts`：构造无Provider的ChangeSetDryRunService并注入；不启动媒体SDK/Job。

### Task 3：证据与交审

- 定向HTTP+现有Service/Session/读路由回归，Worker类型/lint/覆盖/离线audit；真实PG定向遵守本人隔离库、空间门。
- 新成功fixture与当前内部模型仍有类型差异：fixture itemId UUID / hash16位，而DB BIGSERIAL / SHA256；当前proof没有observed。不得猜ID映射或用from填observed。本批source-off可联调，成功态真实映射待源接入/arch统一引用类型，不伪称完整D6成功链完成。
- 独立SHA、P135回执，不push、不改视觉、总目标active。
