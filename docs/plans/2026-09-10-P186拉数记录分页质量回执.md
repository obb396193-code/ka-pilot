# P186 拉数记录分页质量回执

日期：2026-09-10；本人 worktree `be-r010` / 分支 `be/r010`。

## 交付与状态

- `c67e7050`：合入 arch `eb4fd37b`，只同步已冻结 v1.9.12 与 main。merge 中 web/be2 文件是他人已审提交带入，不是本人新写。
- **`d40b14ca`**：正式分页 Domain request/response、旧 attempt/null 与 LEGACY_NO_ATTEMPT、阶段计数独立 null，按授权升级 `system/etl-runs.json`。新 `etl-runs-page.json` 原样对拍，未改内容。
- **`dcf97510`**：新增管理员只读 `EtlRunListRepository`、真实 public barrel 导出、PG/单测。仅内部读仓储，**尚未挂 GET、未开放 rerun、未部署、未 push**。
- 本轮主干实读已至 `8e5fdcfb`：arch 确认 P180–185 合 `248e19fc`；Q027 矩阵版 helper 仍未交。P178 全量授权审计未完成，不能以本批替代。

## 逐项证据

| 项 | 实现/验证 |
|---|---|
| 列表 | page=1/pageSize=50 默认，上限200；安全整数offset；严格items/page/pageSize/total；空超末页有效，非末页静默少行拒绝 |
| snapshot | count/max(finished_at)/页行同一 RR/RO，15s statement /5s lock/15s idle；真实并发插入不污染既有snapshot |
| 排序 | started_at DESC,id BIGINT DESC；真PG相邻超过2^53的run ID不舍入、不按字符串错排 |
| 管理员范围 | 服务器 ApprovedWorkspaceAuthContext，role必须admin；SQL绑定workspace；回读再次检查workspace；普通角色/非法上下文在connect前拒绝 |
| 旧run | 缺execution才 attempt=null + LEGACY_NO_ATTEMPT；execution存在但null/非法/跨workspace拒绝，不读jobs.attempts补历史 |
| 本run计数 | done full/incr/backfill只raw；done canonical只canonical；quality写的是检查条数，两个字段均未知；running/failed初始0不是成功行数，rows=null |
| 日期 | 从真实run scope取：full.asOfDate、incr.ds、canonical.reportDate、backfill/quality.dateTo；不拿服务器今天兜底。缺失/非法返回受控UPSTREAM_INVALID_RESPONSE，不丢记录 |
| warnings | 只投影BATCH_FAILED公开字段；不回传scope全文、private media/failedAt/filters、error_summary；LEGACY消息可省略或固定文本 |
| 时间 | dataAsOf是真实当前workspace max(finished_at)，空空间null；只是运行观测时间。HTTP接线时须加固定说明，不冒充canonical数据新鲜度 |
| 大小 | SQL对单页scope+stage总存储量做保守估算（每行额外512字节），命中16MiB先屏蔽大字段并报SOURCE_TRUNCATED；公开投影序列化后也检查>=16MiB。此处是仓储上限，不代替HTTP完整envelope上限 |
| 安全错误 | 参数、权限、坏数据、截断、不可用、超时分型；不返回driver SQL/URL/body；未知故障固定SOURCE_UNAVAILABLE |

## 测试与门禁（全部本机合成数据）

真实PG唯一库：`ka_ci_be_r010_p178_test`，localhost/127.0.0.1:55432 + `ka_*_test`守卫；用随机workspace隔离，仅清本人seed。

1. TDD：升级fixture后旧Domain **12红/28绿**；DB仓储尚不存在时import红。补实现后通过。
2. `npm --prefix packages/domain test -- run test/etl-run-list.test.ts test/etl-batch-failure.test.ts`：**60/60**（41+19）；新模块单独coverage **100% statements/branches/functions/lines**。
3. 设置上述 `TEST_DATABASE_URL`，`npm --prefix packages/db test -- run test/etl-run-list-repository.test.ts test/etl-run-list-repository.unit.test.ts test/etl-run-repository.unit.test.ts test/etl-run-bigint-pg.integration.test.ts --coverage --coverage.include=src/etl-run-list-repository.ts`：**62/62**（PG19+PG3、纯逻辑21+19）；新仓储 **100% statements/lines/functions、98.68% branches**。未命中分支为序列化扩张后的第二道16MiB守卫；SQL前置exact估算上限已真PG覆盖。
4. `npm --prefix apps/worker test -- run test/etl-attempt-scope.test.ts test/etl-batch-failure.test.ts test/etl-handlers.test.ts test/canonical-handler.test.ts test/backfill-day.test.ts`：**71/71**。
5. Domain/DB/Worker `run typecheck` 与 `run lint` 均通过；三个包 `audit --omit=dev --offline`：**0 vulnerabilities**（缓存审计，不宣称在线最新漏洞库）。`git diff --check`通过。
6. 中途DB unit首跑一红：Vitest.each把嵌套空数组拆为无参数，误触helper默认值。已改对象包装，所有summary反例按真实数组传入；保留该失败说明，不降低断言。

## 质量评估与后续

- 代码质量技能已用于检查类型/规范、私密字段泄漏、SQL参数化、权限/截断/缺数边界及覆盖率；无新依赖。生产模块104行，列表每次两条SQL，无按行N+1。
- 性能边界：当前etl_runs未找到workspace+started_at分页索引；精确count与排序随历史量增长，15s超时受控，但本轮未作生产规模性能承诺。索引需按arch迁移编号协调，后续可在EXPLAIN证据后申请。
- `BLOCKED_AUTH` fixture仍支持，但既有errorSummary只保存Error.message，没有可信历史类型字段；本次不从任意错误文本/当前job状态反推某一次旧attempt原因。后续GET和新run安全类型记录要收口；历史未知保持未知。
- 缺scope日期的旧行尚无nullable businessDate裁决，目前fail closed而不是拼当前日期。真实handler六类均有上述日期来源，未使用公司真实库验证旧数据覆盖。
- **尚未完成：Service/GET（Session/管理员/requestId/完整16MiB）、POST rerun新job/原owner/幂等/timeline、P176 Full/Incr容错/readiness、F-OS-004、021、BI等总信箱队列。**
- 磁盘约7.4GiB，低于arch8GiB整包前置条件，未跑完整五包全量门禁；不能写“全量通过”。不清缓存、不启动Worker/Beat/真实队列。
