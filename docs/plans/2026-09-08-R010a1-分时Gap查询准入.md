# R010a1 分时 / Gap 查询准入实施计划

权威：main@0d358d8；api.md §3.5/§3.6。按写计划技能执行，先测试再实现，不改架构契约。

## 阶段与依赖

1. **独立 Domain 参数契约**：两个固定 queryId、strict params、真实日历日期、media、可选明确账户集、hour范围0..24/非逆序、Gap分组枚举与日期非逆序。复用现日期/账户格式；不接受浏览器workspace/dataView/SQL；不在语法层编造源/规则阈值/当前时间。先提供可复用导出与永久测试。
2. **Registry/Service/BFF 接入**：复用阶段1；保留唯一ApprovedWorkspaceAuthContext与账户tuple，不降级权限；小时源和Gap规则版本没有证明时明确source unavailable，不拿日累计/旧from值或阈值常量充数。独立代码批次，必须补全公开Envelope和两端parity后才允许ready。
3. **真实数据接入**：账户级hh接口仍待OS证据；Gap preDeduction和生效阈值来源仍待可验证适配。复用既有P130投影，不把未入Registry的计算模块说成页面已可查询。

## 当前阶段1修改范围

- `packages/domain/src/operational-query-request.ts`；本人的index追加块；`test/operational-query-request.test.ts`。
- 日期接受现Registry相同的YYYY-MM-DD/YYYYMMDD并规范为ISO；不默认窗口、媒体或账户；可选小时保留可选，不猜调用者默认。时区/数据新鲜度由真实源给，不从入参生成。
- 仅日期排序，不擅自承诺最大Gap天数；执行预算由Registry既有配置控制。accountIds沿用最多1000且显式数组不可空、不可重复。
- 新测试、相关回归、Domain type/lint/覆盖率与状态回执。没有DB/HTTP/前端/媒体调用，不需PG模拟测试冒充接口准入。
