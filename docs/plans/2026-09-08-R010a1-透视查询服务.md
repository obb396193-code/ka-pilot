# R010a1 透视查询服务计划

承接70d50f3真实DB reader与f287252聚合，不切换到无关功能。先完成Worker窗口组合，再独立接Envelope/Registry/HTTP。

1. TDD：新`apps/worker/src/data/platform-pivot-query.ts`与`test/platform-pivot-query.test.ts`。输入服务端ApprovedWorkspaceAuthContext、window、dimA/B；当前真实可分组account/task/biz，其余维度明确unsupported。team待发布源，不借personal grants。
2. 读仓储输出当作边界重新strict校验：三键/期望日期/重复/缺成员、窗口不变、所有observation数量及真实时间范围与成员一致。字节/成员上界先检查，非法source固定错误，不把secret/SQL放异常。
3. 用真实标签把账户日分区成a/b cell；account轴media:accountId；task/biz保留null。每账户日只归一个cell，不join聚合表、不平均CPA。直接复用aggregatePivotWindow；cellCoverage按真实成员有数据及考核是否可判计算，不生成全源ready。
4. 工厂复用PlatformPivotRepository；不启数据库/网络服务。追加factory PG例必须等隔离PG恢复；定向Worker+Domain/DB回归、类型/lint、独立代码及P126留痕。
5. 后续独立批接source result schema/Registry/Adapter/HTTP，保留taskIds/filters未接的事实，当前不冒充完整pivot2端点或八维已齐。
