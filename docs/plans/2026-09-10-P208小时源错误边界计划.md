# Hourly Source Error Boundary Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修正小时源 port 抛出的已知安全错误被统一吞为503，保证P207后续接线不丢失scope/truncation/contract错误语义。

**Architecture:** 保持现有冻结 public error envelope与HTTP状态映射，不新增DTO。仅可信 HourlySourceError 原样交现有 mapError；未知Error/普通伪造code对象仍统一SOURCE_UNAVAILABLE且固定文本。增加SOURCE_UNAVAILABLE/UPSTREAM_TIMEOUT私有端口code，超时按现有Query规则503，不擅改成其他路由的504。

**Tech Stack:** TypeScript / Vitest / 现有真实 HTTP composition。

---

已冻结的授权/稳定错误设计直接执行，不重开产品设计。writing-plans用于步骤留痕、code-quality-checker用于门禁；executing-plans不可用，沿用户连续授权由本会话执行，无子agent。

## Task 1: 红测试

Modify `apps/worker/test/hourly-public-query.test.ts`：hourly port抛FORBIDDEN、SOURCE_TRUNCATED、UPSTREAM_INVALID_RESPONSE，createDataQueryHttpHandler期待403/502/502+对应code，requestId保留，无日/KA fallback。先跑三项确认真实红。

## Task 2: 修复并强化

Modify `apps/worker/src/data/query-service.ts`：hourly catch识别HourlySourceError后throw，其他异常走原安全降级。

Modify `apps/worker/src/data/hourly-public-source.ts`：私有错误类code可表达源不可用/超时；mapError选择固定文案，timeout retryable=true，其余false。测试普通伪造code/含秘密的message/cause不被带出，真实HTTP下认证/未授权范围仍先拒绝且port未执行。

## Task 3: 门禁/交付

小时公开边界+相关Query/数据API回归；Worker type/lint/cacheaudit，覆盖新错误分支；不改HTTP成功shape/前端/Contract/数据库。不将fixture source当作真实小时源、不启用factory或采样job。独立代码SHA与R010/arch回执SHA。
