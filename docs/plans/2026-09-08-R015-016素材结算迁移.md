# R015 migration 016 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按冻结 v1.6 落齐素材、结算、灰度与开户测试/复制的数据库结构，解除后续 API 存储依赖。

**Architecture:** 从 schema.sql 的 v1.6→v1.7 标记直接切片生成单一016，不手写第二份DDL。up只加列/建表；down拿相关表锁后检查新增内容，任何业务数据存在均拒绝有损降级，无CASCADE。只供首次部署前/停写维护窗口使用，不宣称在线低锁。

**Tech Stack:** node-pg-migrate、PostgreSQL、Vitest。

**现场更正（真实PG后）:** 下面是最初实施步骤，不是交付完成声明。初次安装实报 `relation materials does not exist`，014基础表未落；又实读防冲突§6确认其账户列须arch协调。因此016暂放 `packages/db/migration-drafts/016_contract_v1_6.cjs`，不在runner目录。真实PG仅用事务级schema + 原样冻结的7张前置表DDL验证草稿SQL，**不是完整迁移链up/down/up通过**。恢复至runner的条件是014授权/完整落地并重新跑完整链；不会挪借014基表到016。

---

## Task 1：DDL反向对账 RED

- Create `packages/db/test/migration-016-bundle.unit.test.ts`：直接读权威schema切片，up除超时设置外逐句严格全等；检查唯一编号、完整反转、降级保护及无跨批DDL。
- Run `npm test -- --run test/migration-016-bundle.unit.test.ts`，缺016应RED。

## Task 2：生成冻结迁移

- Create `packages/db/migrations/016_contract_v1_6.cjs`，通过读取schema生成apply_patch新增文件，禁止改Contract。
- down为6个新表、7张旧表的全部新列保护；NULL/默认值不伪造业务状态；拒绝删除已有新内容。
- 不进入013/014/017、不修改be2账户/任务文件。

## Task 3：真实PG

- Create `packages/db/test/migration-016.test.ts`：up/down/up、旧数据保留/unknown默认、三键账户FK和跨空间/媒体共存、谱系唯一、每个新增表/列的有损down拒绝。
- 只使用显式localhost55432 `ka_*_test`，禁止共享ka；仅合成数据，测试自建行事务回滚/限定清理。
- 先定向，后串行DB全量与Worker全量；typecheck/lint、diff/security检查。当前015已落，不以013待裁阻塞本批；编号顺序保持，未来013接入由arch统一合流验收。

## Task 4：交付

- 独立代码SHA/留痕SHA，P149写inbox-arch；完整未完成API范围不缩减。
- writing-plans和code-quality-checker；executing-plans未安装，按本计划自行TDD。Coze外部托管不属于用户本仓范围，不创建/上传外部项目。

## 实际执行与剩余工作

- 完成46句DDL全等对账（6新表/40新列）；down保护全部46项并先锁后检查，无CASCADE。
- 完成草稿SQL49项真实PG+5项静态对账，所有临时schema/合成数据事务回滚。原始失败、输入缺started_at的测试修正均留日志。
- 仍未完成：014实际安装、016 runner注册、完整迁移链与全量门禁（磁盘5.6GiB<协作规定8GiB）、后续素材/结算/治理API。
