# ETL 尝试快照质量报告

## 交付

- 独立代码 `9e14169`；交审前合 main 显示 Already up to date（main@4222d4e）。
- 六入口 full/incr/canonical/backfill_day/backfill_coordinator/quality，startRun scope 新增 `execution:{version:'etl-attempt/v1',jobId,workspaceId,jobType,attempt}`。来自已领取 JobRecord，不来自 payload，也不在列表读取时查可变 attempts。
- 同现有 scope 一次插入；recordObservation 现有 SQL 只改 observations。版本化快照不含 leaseToken、奇航 userId 或原始 payload；不改变 runKind 与重试策略，不绕队列 lease/fencing。
- 新纯函数28行；先验证安全正整数/claimed状态/UUID/workspace一致性再持久化，错误仅固定文本。旧记录不反填；这不是公开 ETL 列表交付。

## 实测门禁

| 位置/命令 | 结果 |
|---|---|
| Worker `npm test -- --run test/etl-attempt-scope.test.ts`（实现前） | RED：模块缺失 |
| Worker 六文件定向（helper + etl-handlers/backfill-day/backfill-coordinator/canonical-handler/data-quality-check） | 初轮56过；补六真实handler入口后 **62/62** |
| 同六文件加 `--coverage --coverage.include=src/etl/attempt-scope.ts --coverage.reporter=text` | 核心helper行/语句/函数/分支均 **100%**；不是六handler全覆盖声明 |
| Worker `npm run typecheck` / `npm run lint` | 最终分别exit0；初轮测试访问推断类型中不存在的ds导致TS2339，已改用完整对象匹配断言后复验 |
| DB `npm test -- --run test/etl-run-repository.unit.test.ts` | **4/4**，既有scope观察更新回归 |
| Worker `npm audit --omit=dev --offline` | 0 vulnerabilities（缓存结果） |
| `git diff --check`、逐路径安全检查 | 过；无Contract/runtime/队列/数据库迁移/前端/凭证改动 |

六入口测试通过模拟 startRun 停点，确认进入真实handler后在媒体查询/canonical读取前传正确attempt=2；payload attempt=99无效。独立测试覆盖旧快照不随job重试变化、六jobType、无授权workspace、非法次数/终态拒绝。没有调真实媒体。

## 尚未完成

- PG55432当前拒连（P120有真实失败证据）；本批没再重复启动相同失败用例，也未借ContentRadar5432。落盘与重试历史隔离须恢复后真PG验证。
- 全量门禁未跑：本批空间5.7GiB，低于8GiB规则。已有定向不能替代全量。
- 公开system/etl-runs还需查询/分页/Session/DTO/HTTP/BFF；旧scope.execution缺失如何表达不能用1或当前jobs.attempts凑数。
- BIGSERIAL须全链改string；runtime.ts:58仍显式number，属于共享文件，向arch请求只改该类型接缝，未自改。raw/canonical分阶段计数不可由一个rows_ingested重复填两个字段。
- 不push、不部署、不开放媒体写；P119/P120/P121均是独立交审，未声称main已合入。
