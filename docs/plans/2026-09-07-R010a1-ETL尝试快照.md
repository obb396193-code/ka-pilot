# R010a1 ETL 尝试快照 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 system/etl-runs 固化真实每次 attempt 的证据，禁止读当前 jobs.attempts 冒充历史。

**Architecture:** 复用 etl_runs.scope JSONB，不增 DDL；六个 ETL handler 从已 claim 的 JobRecord 构造 versioned execution 快照，随 startRun 一次插入。观察更新只修改 observations，不覆盖 execution。不是公开列表完成，不反填旧记录。

**Tech Stack:** TypeScript、既有 Zod/JobRecord、Vitest。

---

executing-plans 未安装，当前会话按已批准连续队列直接 TDD。

### Task 1: 快照纯函数

- 新建 `apps/worker/src/etl/attempt-scope.ts`、`apps/worker/test/etl-attempt-scope.test.ts`。
- 先测试缺模块 RED，再实现 `withEtlAttempt(job, scope)`；scope.workspaceId 必须和 job.workspaceId 相同，attempt 为安全正整数且不超过 maxAttempts；job 只允许 leased/running；id/workspace UUID、jobType 为六类现存 ETL 类型。
- 只从 JobRecord 显式投影 version/jobId/jobType/attempt/workspaceId，不写 token、leaseToken、原始 payload；scope 中冒充的 execution 必须被可信值覆盖；不改调用方对象。
- 包内 `npm test -- --run test/etl-attempt-scope.test.ts`；非法身份/attempt/状态、重试后旧快照不变和秘密不落盘必须测。

### Task 2: 六 handler 接线

- 修改 `src/etl/{full-handler,incr-handler,canonical-handler}.ts`、`src/backfill/{day-handler,coordinator-handler}.ts`、`src/quality/check-handler.ts`，仅包裹 startRun scope。
- 原有 `test/{etl-handlers,canonical-handler,backfill-day,backfill-coordinator,data-quality-check}.test.ts` 加运行路径断言；不改 Job lease/fencing/队列，不开放入口或媒体写。
- 跑上述六文件和 Worker typecheck/lint，DB etl-run observation 回归；PG 当前55432拒连，禁止用纯逻辑代替。

### 后续仍需完成

- 列表公开DTO/授权/RR/分页/HTTP/BFF仍未落，旧 scope.execution 缺失只能待补证，不能推测 attempt。
- BIGSERIAL→Number问题需要整体把store runId改string；runtime.ts:58类型适配属于共享文件，已报arch后按允许边界处理，不在这里顺改。
- 参数表不把attempt快照当授权、有效lease证明；执行安全仍由已有队列fencing承担。
