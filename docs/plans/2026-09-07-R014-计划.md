# R-014 实施计划（be2 = Claude Code 第二后端；2026-09-07 开工）

> 依据：`docs/relay/R-014-CC后端开工提示词.md`、`docs/plans/2026-09-07-后端双会话分工与防冲突.md`、契约 v1.5 / v1.5.1 / v1.7.1 / v1.7.3 / v1.7.4 / v1.7.6 + `packages/contract/schema.sql` 已冻 DDL + `fixtures/`（fixture 即契约）。
> 一切以契约为准，不发明字段与端点；缺口写 `docs/relay/inbox-arch.md`（编号 Q-xxx）等 arch 裁，不自造。

## 0. 前置状态（已核实，非照抄文档）

| 项 | 实况 |
|---|---|
| 工作树 | `/Users/aik/ka-be2-r014`（持久路径），分支 `be/r014`，基于 `main@550e738` |
| 测试库 | `ka_be2_r014_test`（55432 容器，已建）；baseline 迁移到 `012_contract_v1_3` 通过，exit 0 |
| 依赖 | `packages/domain` / `packages/db` / `apps/worker` 三包 `npm ci` 完成（apps/web 暂不装，等 S5） |
| 迁移号 | 现有到 012；**015 未被占用**，013/014/016 归 Codex |
| DDL | `schema.sql` 752–882 行（v1.5+v1.5.1）与 1038–1043 行（v1.7.1）已冻，015 = 照此落库，不自行设计 |
| ⚠️ 磁盘 | 装包后剩 **4.2G**，低于「跑全量门禁前 ≥8G」要求 → 见 Q-001 ⑨ |

## 1. 范围（表 → 端点）

### 1.1 migration 015 DDL（全部已在 schema.sql 冻结）

新表 11：`external_changes`、`account_transfers`、`user_watchlists`、`saved_views`、`exports`、`capabilities`、`decision_policies`、`report_runs`、`changeset_groups`、`task_readiness_overrides`、`identity_preferences`

加列 5 表：`report_configs`(+is_shared/version/updated_at)、`accounts`(+pool_status/pool_status_source/pool_status_overridden_by/pool_status_changed_at/product_name/product_ref)、`changesets`(+group_id)、`tasks`(+stage/stage_source/stage_changed_at/sop_run_id)、`workflow_runs`(+task_id)

> 注：`alert_rules` 的 availability_policy/data_freshness_max_hours 虽在同段，schema.sql 标「进 migration 012」且 012 已落，**不重复**。

### 1.2 端点清单（按数据归属分三类）

**A 类 — 只读写我自己的新表，无归属争议（可直接做）**

| # | 端点 | 契约 | fixture |
|---|---|---|---|
| A1 | `GET/PATCH /me/preferences` | v1.7.1 | `me/preferences.json` |
| A2 | `GET/PUT /me/watchlist`（items 升 `{type:"account"\|"task"}`，G2） | v1.5 3.5 / v1.7.4 G2 | `me/watchlist.json` |
| A3 | `GET/POST /me/views`、`PATCH/DELETE /me/views/:id` | v1.5 3.10 | `me/views.json` |
| A4 | `POST /export`、`GET /exports/:id` | v1.5 7.4 | `exports/{queued,done}.json` |
| A5 | `GET /capabilities`、`POST /capabilities/:key/invoke` | v1.5 5.7 | `capabilities/list.json` |
| A6 | `GET/PUT /settings/decision-policy` | v1.5 10.11 | `settings/decision-policy.json` |
| A7 | `POST /accounts/transfer`、`POST /users/:id/transfer-all` | v1.5 4.10 | `accounts/transfer.json` |
| A8 | `GET /accounts/:media/:id/timeline`、`.../timeline/overlay` | v1.5 4.3 | `accounts/timeline*.json`、`overlay.json` |
| A9 | `GET /accounts/:media/:id`（小传） | v1.5 4.2 / v1.7.5 P-077 | `accounts/detail*.json` |
| A10 | `GET /accounts/pipeline`、`PATCH/DELETE /accounts/:media/:id/pool-status`、`PATCH .../product` | v1.5.1 ① | `accounts/pipeline.json` |
| A11 | `PATCH /tasks/:id/stage`、`PUT /tasks/:id/readiness/:dimension`、`POST /tasks/:id/sop-run` | v1.5.1 ② | `tasks/list-v151.json` |
| A12 | `GET /tasks/:id/bindings` | v1.7.3 | `rules/bindings-fixture-task-ready.json` |
| A13 | `GET /reports/daily-brief`（含 `delivery` G8） | v1.5 1.8 / v1.7.4 G8 | `reports/daily-brief*.json`、`daily-v1{,-not-sent}.json` |
| A14 | `POST /reports/render` + report_configs 三列 | v1.5 3.8 | `reports/{config-v1,render}.json` |
| A15 | `GET /integrations/connections`、`POST /:id/check`、`GET/POST /integrations/identity-mappings`、`GET /integrations/messages`、`POST /messages/:id/retry` | v1.5 9.1/9.6 | `integrations/*.json` |
| A16 | `GET /me/counts` | v1.7.1 | `me/counts.json` |
| A17 | `GET /me/workload` | v1.7.4 G9 | `me/workload.json` |
| A18 | `GET /search?q=&type=`（五类，无 LLM） | v1.7.4 G6 | `system/search.json` |
| A19 | `POST /auth/password` | v1.7.6 | 无（204） |

**B 类 — 端点归我、但读写 be 已有文件/已有域，须 arch 裁归属（Q-001 ①–⑧）**：`GET /accounts` 列表加 poolStatus/product/groupBy 与新 item 字段；`GET /tasks` 列表 + `GET /tasks/:id` 加 stage/readiness/sopProgress/blockers；`account.hourly`/`account.gap` 进 Registry；`POST /changesets/batch` + `/changesets/groups/:id/dry-run|confirm`；工作项详情 `decision` 块；`GET /workflows/runs` 加 `taskId`。

**C 类 — 明确不在 be2 范围**：account.summary/trend/dimension/pivot2 v3（R-010a1）、规则解释器/静音（a2）、团队快照（R-011）、警报流/协作/8 维（R-012）、素材/结算/治理后台（R-015）、v1.7.6 的错误页与移动端条（fe）。

## 2. 子批切分（一子批 = 一组表或一组端点；TDD 先红后绿；真 PG 用例必跑）

| 子批 | 内容 | 测试 | 验收句 |
|---|---|---|---|
| **S1** | migration 015（11 新表 + 5 表加列） | `packages/db/test/r014/`：①bundle 静态断言（表/列/约束/索引逐条对 schema.sql）②真 PG `up/down/up` 可回放 ③down 拒有损（有 saved_views/exports/transfers 行、有 `pool_status_source='manual'` 覆盖、有 `changesets.group_id` 引用时必须 RAISE） | **基础建设**（无用户可见功能） |
| **S2** | 我独占 11 表的仓储（`packages/db/src/r014/`）+ DTO/zod（`packages/domain/src/r014/`，按 fixture 逐字段） | 每仓储真 PG CRUD + 空间隔离（跨 workspace 读不到）+ domain 纯函数单测；fixture 逐份 parse 断言 | **基础建设** |
| **S3** | 只读聚合：A16 counts、A17 workload、A18 search、A10 pipeline、A12 bindings、A13 daily-brief delivery | domain 纯函数（指标/计数全在 domain，agent 不算数）+ 真 PG 查询用例；缺数三态断言（不补 0、loadScore 必 `not_configured`） | 合入后，运营在**侧栏/⌘K/账户池页**能用真实数据看到待办计数、搜到账户/任务/工作项、看到九态分布——以前只有 mock |
| **S4** | HTTP 层：A1–A19 全部挂路由（**阻塞于 Q-001 ①②**） | 每端点 HTTP 用例：鉴权（未授权 401/跨空间 403）、入参校验、错误 envelope、响应对 fixture | 合入后，前端 F-007 相关页面可把 mock 换成真端点 |
| **S5** | BFF：`apps/web/lib/data/r014/` + `app/api/r014/…`（helper 自复制，不碰 `bff.ts`） | BFF 转发用例（Session cookie + bearer）、契约 parity | 合入后，浏览器只打同源 `/api/internal/*` 拿到真数据 |
| **S6** | B 类交界字段（**阻塞于 Q-001 ③–⑧ 的归属裁决**） | 同 S4 | 合入后，账户池/任务页显示真实阶段与就绪度 |

顺序：S1 → S2 → S3 →（Q-001 有裁决后）S4 → S5 → S6。S1–S3 不依赖任何裁决，先做。

## 3. 边界与铁律自查（每子批交付前逐条过）

- [ ] 新文件只在 `packages/db/src/r014/`、`packages/domain/src/r014/`、`apps/worker/src/r014/`、`apps/web/lib/data/r014/`、`apps/web/app/api/r014/`
- [ ] 共享文件只在末尾 `// be2-r014` 注释块内追加，不改块外任何行；不碰 `packages/contract/`、`docs/plans/工作台账.md`、`docs/runbooks/`
- [ ] 迁移只有 `015_contract_v1_5.cjs`；`accounts`/`tasks` 只加我自己的列并只写我自己的列；`work_items` 只读不写
- [ ] agent 不算数（指标全在 domain 纯函数）；缺数三态不补 0；凭证不进仓；媒体写全关；不 push
- [ ] 路径限定 commit，前缀 `[be2]`；提交后 `git show --stat HEAD` 自查
- [ ] 交审前 `git merge main`（含 Codex 已合批），冲突在本分支解；交审后停手等 ✅/❌

## 4. 回执与状态

- 状态文件：`docs/plans/R014-状态.md`（逐条更新，compact/重开会话先读它）
- 回执写 `docs/relay/inbox-arch.md`，编号 **Q-001 起**：SHA、文件、门禁 exact 数字（domain/db/worker 各包 test+tsc+lint）、失败留痕、未做项
