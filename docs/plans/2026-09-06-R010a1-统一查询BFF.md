# 统一查询 BFF Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 接通 R010#6/#10 已冻结的浏览器 `/api/internal/query`，转后端 `/api/v1/query`，保留canonical与query_type语法。

**Architecture:** 复用现有Data Query BFF的Session预检、固定来源、bounded JSON、错误/requestId校验，路径只能由两个服务端函数固定选择。前端只做语法转接与响应关联，不复制Registry能力/参数/SQL执行器；严格旧字段映射和Worker用同一测试向量锁定。

**Tech Stack:** Next route、TypeScript、zod、node:test、Vitest。

---

已获R010 HTTP/BFF授权，不改变产品设计；superpowers:executing-plans不可用，主agent按TDD直接执行。

## Task 1: BFF红测试

- Create `apps/web/lib/data/semantic-query-bff.test.ts`：personal/team，summary/trend/table，task/date/compare/page字段不丢，canonical不变；cookie+内部Bearer同requestId，伪头/source/SQL/reconcile不能绕过。
- 新调用 `handleSemanticQueryRequest` 应因函数未存在而失败；运行 `node --test lib/data/semantic-query-bff.test.ts`（cwd apps/web）。

## Task 2: 固定路由与共用内核

- Modify `apps/web/lib/data/bff.ts`；Create `apps/web/lib/data/semantic-query-request.ts` 与 `apps/web/app/api/internal/query/route.ts`。
- `handleDataQueryRequest`、`handleSemanticQueryRequest` 调一个私有handleQueryRequest，分别固定schema与路径，禁止浏览器指定upstream路径。
- 旧语法只按api.md:250映射成canonical，尚未实现的queryId拒绝，不假造功能；未知参数不得被静默剔除，交唯一后端Registry拒绝。
- 新路由所有方法进同一handler：POST只读取数据，其他405稳定envelope。与旧路由Session/token/source/预算完全共用。

## Task 3: 协议parity与负向

- 新测试向量 `packages/domain/test/fixtures/semantic-query-syntax.json`（非权威Contract，仅合成回归输入/期望），Worker/Web分别读取同一向量精确比对转换。
- 新BFF覆盖错误queryId、错误mode/元数据、exact16MB声明/流读取、错误requestId、session失败、超时、未配置、未知query/url/body、所有错误码；默认关闭源仍不fallback。
- 不新增UI/mock业务字段；不从前端计算CPA。

## Task 4: 门禁/交接

- Web定向/全量/typecheck/lint；Worker全量/typecheck/lint（新增parity）；不更动DB，无本批新增PG操作。
- 既有Domain/Web权威fixture失败继续单列，不能改Contract绕过。质量技能检查无新token泄漏/路径注入/隐私日志；依赖不变。
- 路径限定代码与留痕commit，不push；报告仅BFF候选，未跑Next真实服务器或内网就不称部署/端到端真实联通。

## 实际交付与质量报告（2026-09-06 15:00）

- 代码 `0b4f345`，8文件。新增固定`/api/internal/query`→`/api/v1/query`，与旧data-query共用一个私有BFF执行函数；旧query_type只映射语法、canonical原样，所有筛选保留交唯一Registry处理。summary/trend/table可用范围仍由后端决定，team任务窗口422不fallback、不伪造支持。
- TDD先缺handleSemanticQueryRequest失败；新增语法、Session/Query响应、source/身份/queryId/requestId/status/16MB、methods/注入/超时等测试通过。16组共用测试向量分别在Web与Worker精确比对；测试向量是合成回归数据，不是新权威Contract。
- 质量扫描实证旧readBoundedRequestJson全量arrayBuffer后才限1MB，且异常stream会抛出：两条先红，修为按chunk计数>1MB立即cancel，保留原exact1MB可接受语义，读取失败返回可控400；没有提高预算。与16MB响应exact即拒绝是两个不同方向的既有边界。
- 真正跨进程本机HTTP：子进程执行实际Web BFF→fetch→Worker真实HTTP composition→Session/Registry/QueryService→两包schema，summary/trend/table均200且requestId一致；**身份与数据Adapter是合成端口，不是PG、KA或真实登录凭证联调**。HTTP套件50通过，`/tmp/ka-semantic-bff-http.log`。
- 最终Worker非PG102文件1072通过+2外部opt-in skip，`/tmp/ka-semantic-bff-worker-final.log`；Web全量135通过/2失败，仍是P073 ready-lineage/unknown-lineage旧权威fixture，`/tmp/ka-semantic-bff-web-full.log`。Worker/Web typecheck/lint通过。Domain/DB生产代码0diff，本批不重跑其全量或PG，不挪用旧数字；PG最近拒连事实不被本机HTTP通过替代。
- node coverage定向53测试全过：BFF100%行/96.55%分支、新语法100%/100%、共用internal-api-bff86.08%行，`/tmp/ka-semantic-bff-coverage.log`。Web offline production audit0，仅本机锁/缓存已知情报，不是实时审计。
- 没有新依赖/配置、浏览器source权限入口、日志/秘密值、媒体写。唯一新增process调用在Worker测试里：只传合成cookie/token与本机临时端口，production不spawn。非视觉route/lib修改已在R010#10授权，Contract/React页面/brand图片0diff。
- candidate_non_pg_verified/contract_fixture_pending；未合流、未启动Next实际页面/生产服务、未部署、未push。R010a1系统/维度/账户详情等仍有未完成项，P077/R011/P083待arch裁决；本批只完成统一入口BFF，不称数据中心所有功能齐全。
