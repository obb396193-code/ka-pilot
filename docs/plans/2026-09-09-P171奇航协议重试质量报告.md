# P171 F-OS-001协议重试子批质量报告

## 代码与范围

**49ade79**，5文件171新增/7删除。200非JSON、非法信封、非法resource data shape统一typed `QihangProtocolError`，沿用maxRetries与指数退避；401/403、显式业务失败、字节/行数/疑似截断仍按原fail closed策略，不为通过而跳过。

耗尽错误顶层message带resource、白名单日期/hour/page、账户/广告计数、requestFingerprint、响应bytes/kind/SHA256；原始URL、userId、账户ID、关键词、响应HTML/JSON不进入诊断/cause。**与arch要求body前200字节不同：未知body可能第1字节起就包含凭证，本批显式withheld，不冒称原文预览已做。**

这是F-OS-001①和②的安全子集；③批次隔离尚未做。F-OS-002/003、021、Gap、BI、公开dimension source仍未完成，不能写成“OS三个问题全修”。

## 验证

| 检查 | 本轮结果 |
|---|---|
| TDD初次 | 7例中2红：非JSON直接throw而非重试；坏信封和data-shape原本已被generic catch重试（纠正并非三者都不重试） |
| 新旧Client/ETL/Consumer定向 | 6文件 **79 passed**，含1项真实PG贯通 |
| 真实PG | 原Client→原FullHandler→原EtlRunRepository→原JobConsumer/JobRepository；4次协议失败写queued/attempts1 + ETLfailed，same job再次lease→done/attempts2；原失败ETL历史保留；上游仅假响应 |
| 覆盖率 | 三生产模块 **96.2%行 / 92.44%分支**；diagnostic/errors100%/100%，client95.42%/90.07% |
| Worker全量 | **155 files passed + 2 opt-in skipped；1698 passed + 2 skipped**，95.14s |
| Worker typecheck/lint | 最终两项exit0。中途测试callback无用参数lint2红、PGstore观察接口TS2322均已修，非隐瞒 |
| Worker production audit | offline缓存 **0 vulnerabilities**，未在线拉最新公告 |

定向命令：`TEST_DATABASE_URL=<dedicated local ka_be test database> npx --no-install vitest run test/qihang-protocol-retry.test.ts test/qihang-protocol-pg.integration.test.ts test/qihang-client.test.ts test/qihang-observation.test.ts test/etl-handlers.test.ts test/job-consumer.test.ts --hookTimeout=30000 --coverage --coverage.include=src/qihang/protocol-diagnostic.ts --coverage.include=src/qihang/client.ts --coverage.include=src/qihang/errors.ts`。

全量同显式隔离库：`npx --no-install vitest run --maxWorkers=1 --hookTimeout=30000`，原始日志 `/tmp/ka-p171-worker-full.log`。两个skip为真实ASR与SDK网关opt-in，不算生产验证。测试只用本机55432 `ka_be_*_test` 合成数据，清理随机workspace；无真实内网调用/凭证/写媒体。

## 自审与后续

- code-quality-checker：新错误分类不吞auth/resource limit；参数取白名单且错误短于2000，日志无注入原文；重试复用原预算，不另造无限循环；一查询不引入N+1；依赖0diff。
- 原始代码实读：full-handler不跑广告分批，150+广告请求在incr-handler；不要据OS口头把该循环改错文件。
- ③不能只catch并finishRun：`workspace-sync-readiness.ts:36–44`和`workspace-sync-repository.ts:164–174`都把full run done当首次成功，旧canonical可能还在。需要failed-batch(resource/date/tuple)覆盖记录与readiness同时处理，防止残留旧数伪ready；下个子批先收口该路径。
- F-OS-003本轮PG证明普通consumer的同job再次lease可行；**不等于supervisor/IPC已修**，下一步仍需父子诊断与OS复测。
- 基线432a291，候选待arch审查/合流；未部署/push。P170此前Worker typecheck/lint也已补过，P170公开source尚未接。
