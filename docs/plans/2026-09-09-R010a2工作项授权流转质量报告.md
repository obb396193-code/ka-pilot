# P159 工作项授权流转内核质量报告

日期：2026-09-09；代码 **9de07d9**，基于 main@7dd47e6。候选待 arch 审，未合流、未部署、未 push。

## 实际交付

- 新 Domain 内部严格 command：`start_processing / ignore / reject`；拒绝空拒绝理由、客户端自报身份/tuple、额外键、mute/dispatch/escalate/execute 等其他动作。
- 新 `WorkItemCommandRepository.apply` 从数据库锁内工作项读取三键，不接受请求提供工作项归属。先判 personal + 显式 scope，再以 FOR SHARE 锁定活跃 workspace/user/membership/identity/grant/account 复核实时授权。
- 复用已有状态机，状态更新与 `audit_log` 同事务；审计只记录操作者、对象、前后状态，不复制证据/自由文本原因/凭证。拒绝原因保存在工作项已有字段。
- 3 秒锁等待 / 单语句 10 秒上限；并发处理只有一次成功，状态无效不重复写审计。不存在 HTTP 超时重试导致擅自重开事务的逻辑。
- 返回只包含内部动作回执。没有改旧 `WorkItemRepository.transition` 的消费者，没有添加 HTTP、媒体调用、jobs 或静音副作用。

## TDD / 门禁

| 检查 | 结果 |
|---|---|
| Domain 新测试先跑 | 缺新模块，红；实现后16/16 |
| PG 新测试先跑 | 缺新模块，红；初次seed UUID/TEXT混参类型错，修为显式cast后12/12 |
| PG 授权/并发/回滚覆盖 | 删除grant、成员/身份/用户/空间停用、跨media同ID、跨workspace、本人无账户不外推写权限、team/空scope拒绝、并发单次审计、审计失败真实状态回滚 |
| 新DB模块覆盖 | 100%行、86.95%分支 |
| Domain 全量 | 83文件1229项全部过，typecheck/lint过 |
| DB 全量 | 104文件1155项全部过，typecheck/lint过 |
| Worker 全量 | 148文件通过、2外部opt-in跳过；1643过+2跳过，typecheck/lint过 |
| DB离线生产依赖审计 | 缓存0 vulnerabilities，不声称最新联网漏洞结果 |

命令：

```sh
# packages/domain
npx vitest run --maxWorkers=1 --reporter=dot
npm run typecheck
npm run lint
# packages/db：显式设置专用 TEST_DATABASE_URL 后
npx vitest run test/work-item-command-repository.test.ts --maxWorkers=1 --coverage --coverage.include=src/work-item-command-repository.ts
npx vitest run --maxWorkers=1 --reporter=dot
npm run typecheck
npm run lint
npm audit --omit=dev --offline --json
# apps/worker：显式专用TEST_DATABASE_URL，关闭外部探针
KA_RUN_AGENT_SDK_SMOKE=0 IDEALAB_ASR_SMOKE=false npx vitest run --maxWorkers=1 --reporter=dot
npm run typecheck
npm run lint
```

DB 使用本机55432 `ka_be_p158_20260909_test`，测试清理仅本文件随机生成的 workspace/identity；未清业务库。Worker全量使用另一个既有专用库、两包PG不同时运行；外部ASR/SDK探针保持关闭。

## 质控与未完成

- 无新增依赖/SQL拼接外部标识符/外部请求；生产SQL参数化、错误固定，不返回原始数据库异常。新增代码6文件223行，已有文件仅两个be export块各加2行，be2/Contract/视觉0 diff。
- `revoked_at` 读取用行JSON，兼容当前删除型撤销及未来018软撤销；**本轮只实测当前删除型撤销，018真实软撤销PG验收未做**，不宣称跨未来迁移已验证。
- personal 双null工作项既有只读权限保持；本批不外推该类可写，需后续契约收口。team始终拒绝。
- 开始处理/纯忽略/拒绝的公开成功DTO尚未明确，现有HTTP不因本提交开放；派发/升级/SLA/通知/自动T+1/详情新版decision仍未完成。不能把此内核称为工作项全流程交付。
- P158的S6服务字段投影、详情决策缺证据口径仍待arch；本批推进不依赖这些裁决的授权内核，总目标保留。
