# 派发态去重 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复已派发工作项再次收到同规则同tuple信号时，被遗漏为无活动项而重复创建。

**Architecture:** 沿用现有事务/advisory lock，查找活动项直接绑定Domain的ACTIVE_WORK_ITEM_STATUSES；不另写四态清单，不改已冻schema或未实现的升级留档协议。

**Tech Stack:** 现有Domain、pg、Vitest。executing-plans不可用，继续本地TDD；不上传代码/派子agent。

1. 在 `packages/db/test/work-item-repository.test.ts` 加真实PG：先创建→直接设合成dispatched→同严重度并发两次merge，断言仍同ID、同状态、数据库一行。再同号跨媒体新建应隔离；终态旧行可新建。
2. 将该文件shared ka fallback改为显式本地55432 ka_*_test守卫，记录本测试建立的workspace并在afterAll限定清理。
3. 先运行失败用例确认目前重复创建；改 `packages/db/src/work-item-repository.ts` 的findActiveAlert为`status=ANY($5::text[])`，参数来自共用常量。
4. 原仓储PG+Worker规则流水线、DB/Worker type/lint与相关回归；根据源改动小范围校验后交独立SHA和P145报告。无媒体写/路由/前端/Contract修改。

**不冒称完成：** occurrence_count持久化、superseded_by关旧建新/013、派发通知和HTTP动作仍在总清单。此修复仅活动态查找范围，不能把现有原地upgrade当P083最终实现。
