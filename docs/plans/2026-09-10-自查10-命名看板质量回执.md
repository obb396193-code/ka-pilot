# 自查10：P211 个人命名维度纵切片

代码 **f612282a**（20 文件，1385+/7-，其中三份 HTTP 合成响应 720 行）；合入 arch 本机 main 909ba2ca 的同步提交 **3bd1d10b**。当前为本地 candidate + synthetic PG/HTTP verified；**未 arch 合流、未部署、未 OS 真实数据验证、未 push**。

## 本批交付

- `optimizer / goal / placement` 严格行 Schema，保留 v3 与既有 canonical Query ID/信封。三类行加 `source/sources`，account/task/biz 不加；未解析为 null key + 未标注。旧代理维度 fixture 不抢改。
- 复用 be2 `resolveAccountDimensions` 优先级。持久化 override 胜 nickname；改名后的旧昵称不复用、人工决定保留；pending 段不进维度。同维度歧义与 present-invalid 拒绝，不静默 last-win。
- 新历史规则 reader 只读 parse 指向的 rule_version，三键限权；不用最新规则重解旧数据。规则缺失显式 null，已有映射可解释、无法确认的人工补段不猜。
- 命名事实、历史规则、指标、日考核价、lineage 在同一 RR/RO 快照。复用现有 account 计算，不另造 CPA/考核算法。总共固定批量读取，无账户 N+1。来源计数按账户、不按账户日；同值混合来源仍一行。
- 已挂既有 `createPlatformDimensionQuery` → `PlatformDataSource` → Registry → `/api/v1/query`，data-api.ts 原 factory 调用自动获得新方法，不是仅留孤立函数。普通个人源固定 canonical，不向 KA fallback。
- DB invalid evidence → 顶层 502 `UPSTREAM_INVALID_RESPONSE`，截断 → `SOURCE_TRUNCATED`，连接故障 → typed unavailable；不返回 SQL/上游 body/凭证。既有 HTTP requestId、16MiB 闸不改。

## 验证记录

所有 PG/HTTP 只在本机 `ka_be_r010_p194_test`，随机合成 workspace、授权上下文；没有内网凭证。下列工作目录除注明外分别为 Domain/DB/Worker 包。

| 命令/范围 | 结果 |
|---|---|
| Domain `npm test -- --run test/named-dimension.test.ts test/dimension-window-rows.test.ts --coverage --coverage.include=src/named-dimension.ts --coverage.reporter=text` | 68/68；新模块行100%、分支94.91% |
| DB `TEST_DATABASE_URL=… npm test -- --run test/account-dimension-rule-repository.test.ts test/account-dimension-rule-unit.test.ts test/account-dimension-evidence-repository.test.ts` | 34/34（真实 PG 13 + unit 21）；包括单规则16MiB、两条合计超限在 SQL 端截断，未把大 JSON 整批传到 Node |
| DB 新 reader coverage（同文件+unit，后追加 PG 字节反例） | 行/函数/分支100% |
| Worker 定向 `platform-named-dimension-pg.integration / platform-named-dimension / platform-data-source / platform-window-query-pg.integration / data-api-http / data-api-startup / dimension-registry` | 首次115/115；随后补错误分类1项，两个单元文件23/23（与115有重叠） |
| 合 main 后 Worker `TEST_DATABASE_URL=… npm test -- --run test/platform-named-dimension-pg.integration.test.ts test/platform-named-dimension.test.ts test/platform-dimension-query.test.ts test/platform-group-dimension.test.ts --coverage --coverage.include=src/data/platform-dimension-query.ts --coverage.reporter=text` | 38/38（含真实PG/HTTP7）；聚合文件行100%、分支91.30% |
| 三包 `npm run typecheck`、`npm run lint` | 通过 |
| 三包 `npm audit --omit=dev --offline` | 均0 vulnerabilities；是本机缓存审计，不冒充在线最新公告审计 |
| `apps/web npm test` | 244/244，保持0前端文件改动 |
| `git diff 947e8121..HEAD --check` / 前端 diff | 通过 / 空 |

不同 Worker 用例共138项；不将各次重跑累计成更多覆盖。PG 证明同 workspace 跨媒体同号、跨 workspace 同号、空 scope、缺日、人工/昵称混合、pending、并发修改后 RR 不混快照。HTTP 证明三类实际输出、伪造 x-ka-* 不扩大 scope、无 Session 401、非法规则 502；SessionAuth 是批准上下文测试桩，**不冒称完整真实登录/OS链路**。旧 data-api-startup 真进程验证 KA 默认关闭仍启动。

红测留痕：新三维Schema先3红；named方法缺失先8红；DB直接import zod 首次运行红，改用Domain导出Schema（DB不新加依赖）后过；类型检查发现可选source计数undefined，改为拒绝而非静默补零。未放宽生产边界换绿。

## 三份候选与边界

真实 HTTP 合成响应在 `docs/plans/fixtures/selfcheck10/dimension-{optimizer,goal,placement}.json`；生成方法见同目录 README。请 arch 审后搬进正式 Contract fixtures；本批未改 `api.md/schema.sql` 或别人的实现。

仍待完成：P211 多值筛选、级联 `GET /data/filters`、三BI公开挂载、团队 KA 同形来源；P170 其余解析维度及 resource_position 兼容入口；小时自动采样调度和真实源时区；OS full→真实页面数据双证。本批只实现个人命名维度，不称整个P211/所有数据功能完成。

性能与风险：窗口最多31天、授权≤1000、日证据≤10000、源/响应16MiB；规则SQL有1001与51哨兵及合计字节闸。未知lineage保持null。缺规则无法证明新手工段的映射，返回未标注并警告，绝不取最新规则冒补。磁盘3.3GiB，依arch要求未跑五包全量/build，不清缓存。

使用 writing-plans 拆分纵切片与 code-quality-checker 检查安全、性能、类型、测试覆盖；无新High/Critical遗留。本项目限定本机既有仓库协作，未引入Coze远程工程/上传。
