# R010a1 分时 / Gap 严格数据边界 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 接受新版unknown代理标签，并为刚移交的account.hourly/account.gap建立严格、可复用的版本化输出校验，作为后续真实Adapter/Registry接线的前置。

**Architecture:** 直接读arch主线fixtures，不复制另一套样例；复用canonical MetricValue/RatioValue而不由前端计算。此次先实现Domain边界，Query ID公开准入仍等待真实reader与scope/lineage完备，不把“有Schema”宣称接口已实现。昵称解析来源由be2 R017提供，不抢实现。

**Tech Stack:** TypeScript / Zod / Vitest，现有Domain；无新依赖、无DDL。

---

执行方式：老板已授权本会话连续实施；executing-plans技能本机未列出，主会话按本计划TDD，不另派Agent。

### Task 1：unknown代理标签 parity

Files: `packages/domain/src/dimension-window-rows.ts`、`packages/domain/test/dimension-window-rows.test.ts`。
1. 新增unknown样例存在且原样保留、不能推断成self的断言；跑`npm --prefix packages/domain test -- --run test/dimension-window-rows.test.ts`确认RED。
2. `z.enum(["agency", "self", "unknown"])`，仍拒绝任意新值/缺字段/额外字段；不推断agency_name。
3. 跑原维度、canonical契约回归；独立代码提交。

### Task 2：分时 / Gap 行契约

Files: 新建`packages/domain/src/operational-query-rows.ts`、`packages/domain/test/operational-query-rows.test.ts`，仅在domain index本人出口块追加export。
1. 直接加载`packages/contract/fixtures/data-query/hourly.json`与`gap.json`，只取版本化rows投影；新增缺字段、present-invalid、负/越界hh、双null/重复组/同号跨media、缺小时、三态ratio测试。
2. RED后实现：`hourly={media,accountId,hh:0..24,cumulative,delta,ratios,velocity,projectedDayCost,budgetUsage,lastSyncAt}`；四个累积/差分MV均必填。`gap={group,conversion,realConversion,gap,preDeductionGap,deductionRate,gapStatus}`；保持现有MV/RV严格状态，不默认0。
3. 行数组≤10000、复合(media,accountId,hh)去重；Gap按group.key去重。相邻小时若前一行已知缺采样，不允许后一行伪造delta；未包含前一行的窗口不擅自判定它缺采样。缺基础数不允许normal/high或有限gap。只检查已知依赖，不硬编码阈值/重算舍入样例。
4. 版本化投影严格区分account.hourly/v1、account.gap/v1（后者带groupBy），不在本批开放Registry/完整Response envelope。
5. 定向Domain测试与覆盖、typecheck/lint；独立提交，记录完整缺口。

### Task 3：后续真实接线（仍在总目标内，本批不冒充完成）

- Registry、Platform/Team reader、服务端tuple guard、完整envelope与HTTP/BFF接线，需在公开准入前同时具备。
- hourly不能把广告差分表当账户快照：lastSyncAt/现金系数/账户预算/缺采样证据需从真实数据取得；Gap high来自真实规则版本，不能用mismatch替代high。
- 扣量多日桶归属、源单位换算仍需arch细口径确认；etl-runs历史attempt/BIGSERIAL字符串需按上一轮只读预检修。不要修改be2临时接管的账户/任务六文件。
- 交审前merge main；全量前磁盘≥8G（当前4.8GiB，仅定向）；不push/不部署/不接真实媒体写。**执行中收到arch 1c622d0新铁律，覆盖旧冻结规则：交审后立刻继续下一批，有单条依赖就记录并跳过，不等整批。**
- 新增实读依据：`apps/worker/src/qihang/client.ts`的account_realtime类型和URL拼接均不含hh；`etl/incr-handler.ts`只有ad_realtime的相邻小时采样，账户仅当前全天。不能直接声称已有账户历史hh快照；下一步先核实OS account_realtime hh是否生效，或选择有完整性证据的广告汇总，未实证不开放假分时。
