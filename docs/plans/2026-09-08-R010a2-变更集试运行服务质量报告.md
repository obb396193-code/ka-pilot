# R010a2 D6 试运行服务质量报告（2026-09-08）

## 本批范围与状态

**最新合流门禁不是全绿**：合main@01403c8的v1.9后，Domain补验974通过/1失败。唯一失败为be2 `test/r014/search-contract.test.ts:15`，新search fixture含meta、不含subtitle，旧schema相反。已原样通知arch；本批不修改be2源码、不删除fixture断言。下表975全绿属于v1.9合入前，不可外推为最新主线全绿。

- 代码 `12874a4`，6 文件 +438/-2；并发 TTL 错误分类补修 `40d2eb7`（3 文件 +20/-4）。合 main@01b4fbd 的同步提交 `a149b67`。
- 只接服务端授权 → 仓储 prepare → 只读 Provider → 指纹/结果范围校验 → record。没有公开 HTTP/真实 Provider、没有 confirm/execute/媒体写/任务入队。**D6 未完整交付、未部署、未 push**。
- Domain 既有 typed value/聚合与数据库微秒 TTL hash 复用，不另造状态机。内部 PreflightPort schema 不宣称是冻结的公开成功 DTO。
- 没改 Contract、视觉、be2/r014、共享 runtime/http-server/data-api；从 main 合入它们的既有代码不是本批新改动。

## 已核验防线

1. auth 必须唯一 strict ApprovedWorkspaceAuthContext；personal + 对应 media/account 的 preview/execute；read/team/空 scope 不能进入 preflight。个人 initiator/credential owner 必须当前 actor，不借别人凭证。
2. find/prepare 两次核 workspace/id/tuple/actor/status/TTL，Provider 只拿私有草稿的深拷贝，不拿 session/token/grants；无 Provider 明确 SOURCE_UNAVAILABLE。
3. 结果严格三键/凭证归属/draftHash；一项一结果不多不少不重复，内部服务数组上界10000（mock验证），10001拒绝；整体exact16MiB拒绝。真实草稿hash另有既有bounded-JSON节点门，不承诺10000个复杂item都能生成草稿。typed非法值、unsafe id、已执行item拒绝。
4. failReason 只能受控枚举；原始 Provider/SQL/异常正文不进入结果。unknown 保留，成功仍 draft，不生成媒体 jobs。
5. 10s 默认读取超时（部署参数1–60000ms），取消 signal；迟到完成不再 record。record 不是网络超时 Promise 的旁支，不会后台悄悄写入。
6. 修复本批自审发现的 TOCTOU：hash 不含身份，recordDryRun 同一行锁内必须校验服务端 expectedScope，避免预检期间更换 owner 后复用原成功证据。旧可信内部调用兼容可选参数，新服务固定传入。
7. 真 PG 追加反例证实：并发将 TTL 改为已到期，旧仓储通用 Error 被服务映射 INTERNAL_ERROR。`40d2eb7` 将 prepare/record 的到期统一成 INVALID_STATE；不保存成功 run。保留 `expiry-red.log` 失败证据，修后6/6 PG通过。

## 实际测试

| 测试 | 结果 |
|---|---|
| TDD 初次 | 失败：服务文件不存在；随后补实现，不删失败用例 |
| Worker service | 41/41；含10k/10001、exact16MiB、恶意 proof、超时迟到、缺源/错误清洗 |
| Worker 实际 PG 联合 | 最终6/6；重复 attempt、unknown 清 hash、同号跨媒体/空间、并发改值/改 owner、到期/并发缩短TTL |
| DB dry-run unit | 最终31/31（新增4个锁内范围反例 + 到期分类） |
| 新服务 V8 coverage | 行/语句98.54%、分支90.24%、函数90%；两文件均过80%门槛 |
| Domain 合 main 全量 | 70文件975通过 + typecheck/lint exit0 |
| DB 合 main 首轮全量真实PG | 84文件880通过 + typecheck/lint exit0；在TTL补修前，不冒充最终SHA门禁 |
| Worker 合 main 首轮全量 | 129文件1369通过 +2外部opt-in skip + typecheck/lint exit0；在TTL补修前 |
| Worker 缓存 production audit | `npm audit --omit=dev --offline` 0 vulnerabilities；不是在线最新公告证明 |

测试数据库显式为本人 `127.0.0.1:55432/ka_be_r010_20260907_test`，没有共享 `/ka` fallback。新增实际PG文件拒绝非本机/非55432/非 `ka_[a-z0-9_]*_test` 库。Domain 与DB可并行，DB/Worker PG串行；全量前磁盘17GiB。Provider 为合成只读端口，没有使用真实凭证/媒体数据。

中间 typecheck 通过、lint 2 条 unused 报错，已修后重跑全绿。全量日志保留本工作树 `output/qa/p129/{domain,db,worker,audit}.log`，不进仓。

最终代码 SHA 全量重跑使用 `db-final.log`、`worker-final.log`（严格串行）；不以首轮数字替代。

- `40d2eb7` DB最终全量84文件881通过，typecheck/lint exit0；包含本批新增与main的015迁移，真实PG已验。
- `40d2eb7` Worker最终129文件1370通过，2个外部凭证opt-in skipped，typecheck/lint exit0；测试日志时钟出现跳变（DB duration 23156.30s），**不把耗时用于性能基准**。退出码与测试数量独立记录，日志未篡改。
- 最后合main@01403c8，仅新增v1.9 Contract/fixture/relay，runtime无变更；补跑Domain全量与015 bundle/dry-run unit。v1.9的recentManualOps门限、history回退、scope语法和缺源政策已读；不自行改018或be2源码。
- v1.9后：Domain974通过/1搜索fixture失败（日志`domain-post-contract.log`）；独立再跑Domain typecheck/lint exit0；DB 015 bundle + dry-run unit 39/39。未把上游契约变化造成的新失败掩盖为绿。

## 交给 arch 的接线事项（不中断其它队列）

- D6 dry-run POST 成功响应：api.md 只写 item 级预检，`fixtures/changesets/detail.json` 是 GET DTO 且与当前只读 Domain 有差异；请冻结 POST fixture/稳定错误 shape，不能让 BFF 各抄一套。服务已返回现仓储内部 `{executionRunId,hash,status}`，不会擅自作为公开 DTO。
- 真实 preflight Provider：必须读取真实 target/field/permission/current from 并验证 to；没有该能力时不可用，不能拿数据库 fromValue 自己比自己后写 success。测试 Provider 不进生产 composition。
- create 无 work_item_id 时，unit/campaign/creative 如何可信解析到单账户（三键）需要明确来源；不能让客户端自报 tuple 绕过归属。先把既有草稿 dry-run 服务交付，不把 create 标完成。
- 请在你所有的 data-api/http-server 结构里开 R010 路由缝，不能占 be2 的 r014 block。本批仍保持旧五类只读路由与 session 回归，不开启确认执行。
- 凭证/授权会在真正 confirm/execute 再从服务端复核；本批只是保存 preview 证据，不代表获得永久执行权限。
