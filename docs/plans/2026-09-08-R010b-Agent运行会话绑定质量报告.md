# P147 Agent Run 会话绑定质量报告

代码 **52fe688**；`be/r010`，交审前 main c3451db 已 up-to-date。候选未合流/部署，无 push，无真实模型调用或媒体写。

## 问题与红绿证据

旧 `AgentRepository.startRunForUserMessage` 创建 Run 未写012已有的 session_id；completeRun只核对 workspace/user/run，不核对调用指定的session。真实PG建立同用户A/B会话，A的Run可对B完成。旧null-session Run也被任意本人会话接收。

- `output/qa/p147/pg-red.log`：**3失败/3通过**。缺session绑定、跨session错误完成、null-session错误完成三项均被实际旧代码复现，不是mock推测。
- 修复仅生产 **8行增加/6行删除**：新Run插入session_id与用户消息同事务；completeRun传expectedSessionId进入原FOR UPDATE查询，先核对再写助手消息与终态。
- 旧null绑定不猜、不回填，不能完成到任意会话；只允许原failRun按workspace/user/run安全置失败，不写助手消息。
- 新6项真实PG全部通过：原会话绑定、跨会话无副作用、null不被认领、并发只留一条助手消息、跨用户/空间拒绝、绑定与legacy失败链兼容。新测试显式隔离local55432 ka_*_test；仅清理本文件创建的workspace及其Agent行。

## 验证

| 检查 | 本批结果 |
|---|---|
| 定向实际PG | 13/13（新6+旧Agent7），db-focus.log |
| DB全量 | 95文件，**1018/1018**（含unit与实际PG，非1018项PG），db-full.log |
| Worker全量 | 144文件通过，**1560通过+2外部opt-in跳过**，worker-full.log |
| DB/Worker typecheck、lint | 全通过，各自type/lint日志 |
| 核心覆盖 | 整个既有AgentRepository **98.06%行/76.62%分支**；本批绑定SQL三种路径均实际PG覆盖；旧memory/输入异常/rollback异常等分支仍不完整，未称100% |
| 安全/性能 | 参数化SQL、沿用事务行锁、无新N+1、无新外部调用或动态SQL；未放宽身份；diff --check通过 |
| 依赖 | 无新增/改锁；本批Worker offline production audit0，不是在线新公告扫描 |

Domain/Web本批0 diff，最近同源证据为P146的1143/201，本批**未重跑**。没有Next build或生产验收。所有日志在output/qa/p147；门禁未并行重置/使用共享库。

## 尚未完成的真实边界

1. 这是现有Agent内部仓储的一处必要修复，不是R010b公开会话/消息/SSE完成。
2. 原内部success状态、无client_message_id幂等/seq持久化、AgentContext account尚无media判别、用户对象访问控制与公开ApprovedWorkspaceAuthContext接线仍需后续；不能把旧内部接口直接暴露给浏览器。
3. 历史null session的Run不能自动恢复到会话；若需修复历史，必须有额外可证明的关联证据。当前未修改历史数据。
4. **新增契约矛盾需arch裁**：api.md:595 P008七帧形状(type/run_id/seq/ts/data)与agent/sse-frames.json的event/data及context/token/diagnosis不一致；done.seq=6却有7帧；run-events.json使用run_started/tool_call等另一枚举。结构化结果在done才输出的规则也与fixture先发diagnosis冲突。建议明确哪些是UI派生展示、哪些是wire，并给统一续传帧样例。本批不修改arch Contract。

整个信箱目标仍active，013与其他已登记源/契约依赖保持，不等待本批审查才继续可做项。
