# P135 D6 内测试运行主HTTP质量报告

## 交付事实

- 代码 **40fc474**，6文件；已合main@0d358d8（D6裁决+fixture）。按最新责任澄清直接修改本人http-server/data-api，不用be2 r014数组，不触碰其文件。
- `POST /api/v1/changesets/:id/dry-run` 已进入真实启动composition；internal bearer+服务端Session→personal preview/execute grant→现有Service检查实际草稿三键、initiator、credential owner、TTL→无媒体Provider报503。请求为严格空JSON对象`{}`，不接受浏览器传items/现值/scope。
- 与 `dry-run-source-unavailable.json` 完全同形（替换requestId），固定中文message、retryable:true。未知内部异常不输出SQL/凭证/原文；错误请求400、team/越权403、缺对象404、过期409、方法405+Allow、过大请求413且不reset。
- runtime明确只注入Repository，**无preflight**；不会prepare/record/enqueue。confirm/execute/retry/rollback路由继续404。若误注入旧成功结果，不把内部缩略record当公开D6数据，502 fail closed。

## 门禁（本批）

日志在`output/qa/p134/d6-*.log`，沿用本次工作目录而非/tmp。

| 检查 | 结果 |
|---|---|
| 新HTTP TDD RED | 20失败/2通过（旧路由404），d6-red.log |
| 首轮HTTP+原Service | 63/63；d6-green.log |
| 最终6文件联合回归 | **141/141**：新HTTP23、原Service41、真实PG7、原DataAPI56、Session11、实际子进程startup3 |
| Worker typecheck/lint | 最终exit0；初次test closure address未保持缩窄已修，旧红日志保留 |
| 新route覆盖 | 行/语句97.82%、分支90%、函数100% |
| Worker生产依赖offline audit | 0（缓存公告，非实时联网审计） |
| diff/security静态 | diff --check通过；无新凭证、原始错误透传、SQL字符串输入、新媒体调用或队列操作 |

联合命令：`TEST_DATABASE_URL=<本人55432隔离库> npm --prefix apps/worker test -- --run test/changeset-dry-run-http.test.ts test/changeset-dry-run-service.test.ts test/changeset-dry-run-service-pg.integration.test.ts test/data-api-http.test.ts test/data-api-startup.test.ts test/session-http.test.ts --maxWorkers=1 --testTimeout=30000 --hookTimeout=30000`。

真实PG新增一项实际HTTP→Service→Repository验证：本人草稿503、同号其他media403、其他workspace404；execution_runs空、dry_run_hash/confirm_hash仍null、jobs=0。Session使用合成批准上下文，本批不是生产BUC联调。startup确实运行src/data-api.ts子进程，无KA env可起且D6无Session401，不是404。

共享壳层Response上限继续原回归；新D6出口只有固定有限错误，精确命中可配置响应上限也502。新请求使用已测R010 drain式读取，避免超限时socket破坏。没有性能基准结论。磁盘6.8→3.8GiB，未全DB/Worker（门≥8GiB）；Domain/DB源码未改。

## 未完成/需arch判定

- 本批完成**源关闭的内测试运行路径**，不是成功预检/确认/媒体写闭环。D6新成功fixture现无法从旧内部record真实映射：itemId UUID vs实际BIGSERIAL，hash样例16位 vs实际SHA256，内部proof不含observed。没有补假UUID、用from冒充observed或截断hash。
- 请arch将上述与013引用类型冲突统一判定。等真实只读Provider进入时，需扩证据Contract、持久化observed并输出逐项ok/changed/unknown；现意外success502是明确安全边界，不宣称成功fixture已实现。
- 没有BFF、没有部署；请arch合流后用本人有效未过期draft + `{}` 联调503，不能用历史过期fixture期待503（应409）。本批代码已本机测试，未声称merged/deployed。
