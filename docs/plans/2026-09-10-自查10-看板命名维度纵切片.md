# 看板命名维度 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 P211 optimizer/goal/placement 从账户命名事实接到严格维度查询，先完成个人canonical纵切片，再接团队与筛选。

**Architecture:** 按 v1.9.8、v1.9.22：共享resolveAccountDimensions，人工覆盖优先；读取parse所绑定的历史rule_version，不能拿今天最新规范重新解释旧段。缺名归未标注，pending不进维度。账户日/考核价/解析/规则同RR快照，先受信tuple限权，再聚合；不添加另一套Query ID或Envelope。

**Tech Stack:** TypeScript、Zod、pg、Vitest；现有PlatformDimensionQuery、AccountDimensionEvidenceRepository和窗口计算。

---

writing-plans拆批、code-quality-checker门禁；执行技能当前未提供，本人依老板持续授权TDD，不派Agent。

## Task 1：Domain 来源和三新维度

创建 `packages/domain/src/named-dimension.ts` 与 `test/named-dimension.test.ts`。失败测试：manual胜nickname、同维度重复映射不静默last-win、历史规则补原未解析段、pending忽略、失效昵称不复用、来源计数/混合/空值。复用共享resolveAccountDimensions，不碰be2解析器。`dimension-window-rows.ts` 添加三新strict来源行，只增新分支，旧account/task/biz形状不改。

## Task 2：真实快照与查询

创建 `packages/db/src/account-dimension-rule-repository.ts` + unit/PG tests，单条SQL按受信workspace+tuple JOIN parse.rule_version→naming_rules；只投影key/mapsTo/pending，10k/16MiB守卫，旧版本不可换最新版本。Worker同RR调用现有账户指标/考核窗口 + 两份命名事实，固定批量查询，无按账户N+1。新增独立named查询方法/模块，既有账户聚合逻辑复用。

## Task 3：Registry/Adapter/HTTP

三新维度只在真正reader实现后准入。`platform-data-source.ts`调用同一query，沿旧scope/output/lineage守卫，`data-api.ts`注入factory。真实PG/HTTP验证三键隔离、混合来源计数、空名、cash/BI成本与累计窗口不漂移，导出脱敏真实响应候选给arch审fixture。

## Task 4：后续

P211多值筛选与filters接口、团队KA数据源是同目标下的后续独立批，不能以个人或纯内核成功声称两源已完。公开queryId入口不擅自改为文档view，待arch确认别名。缺源显式unavailable，不使用假数据。代码与质量回执独立SHA，磁盘不足8GiB不跑五包build。
