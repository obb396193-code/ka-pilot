# R010a1 透视公开接线质量报告

实现候选 `eaa1994`；补真实 PG/HTTP 与 Web 版本断言 `5413193`。已同步 arch main@bebfb31，交付前分支 HEAD c6f03fa。未 push、未部署、未将合成测试称真实投放验证。

## 本批交付

- account.pivot2/v1 纳入 Domain canonical Query ID/row/source；dimA/dimB、真实窗口、价格来源、单元格唯一性严格校验。响应 meta.cellCoverage 必须存在且 cells/undeterminable 与 rows 对齐；withData 由真实成员观察生成，不从金额是否为0猜。
- Registry 接冻结 window_from/window_to/media/dimA/dimB，31天/10k预算，当前 source=platform、account/task/biz 三维可组合。浏览器不能选择来源/SQL/授权；不支持的轴返回422 DIMENSION_UNSUPPORTED。
- 专用 pivot port 传完整 ApprovedWorkspaceAuthContext，仅收窄媒体 tuple，不编造 role/丢 accessLevel，不借 team 源。真实 RR reader→逐日任务/价格→聚合→SourceQueryResult→Service守卫→既有 HTTP/BFF 已在测试贯通。
- dataAsOf 来自 canonical computed_at 最早值；缺时钟整体 null+unknown/partial。datasetVersion/timezone/dayCut 没源则null。缺账户日是 partial、不是 truncated。完整空scope可有0个cells，缺授权日不宣称完整总数。
- 错误/恶意账户轴/对象计数/维度/窗口/coverage 均 fail closed，坏契约顶层502；16MiB和10k边界复用已验证窗口内核与HTTP出口。无真实写路线。
- Web BFF wire schema 同步；双包实际解析同一批返回和恶意变体，不靠静态字符串或单端 mock 自证 parity。未改任何视觉组件。

## 已执行门禁

隔离数据库 `ka_be_r010_20260907_test`，本机55432。此前拒连，01:35后 SELECT1 恢复；磁盘也从5.4增至9.7GiB，才补以下全量。未清缓存、未借ContentRadar库。

| 范围 | 原始结果 |
|---|---|
| Domain npm test -- --run | 62文件，893通过 |
| DB TEST_DATABASE_URL=本人隔离库 npm test -- --run | 74文件，794通过（含unit与真实PG） |
| Worker 同隔离库 npm test -- --run | 127通过文件+2 opt-in skipped；1323通过+2 skipped |
| Web npm test | 初次159/160；修版本断言后160/160 |
| Domain/DB/Worker typecheck + lint | 最终全部 exit0 |
| 新Web BFF/contracts/rows/pivot tests定向tsc及lint | exit0 |
| Web全typecheck | exit2：shiki/ai/streamdown/xyflow/BlockNote等既有组件缺依赖及派生类型；无本批data/API文件诊断 |
| Worker offline production audit | 缓存审计0 vulnerabilities，不等同在线新漏洞库 |
| git diff --check / 路径检查 | 通过 |

原始日志：`/private/tmp/ka-p128-{domain,db,worker}-full.log`、`ka-p128-web-full.log`（失败保留）、`ka-p128-web-full-retry.log`、`ka-p128-web-typecheck.log`。没有把不同批次重复通过数相加充全量。

新增证据：Domain source/meta7、Worker Registry15+公开链14、Web3；先前透视DB7真PG此次补验全部通过。新增 Worker factory→实际HTTP→PG 两项通过：同workspace跨media/跨workspace同ID排除、缺cookie401、缺canonical日partial/null。Session授权在这两项里是合成approved port，不声称真实登录PG联调；真实Session全链由现有full suite另测。

## 失败与修复记录

1. TDD最初 Query ID/Registry/meta 均RED。接入row表后暴露 window-assessment→data-query-rows barrel 循环，改为依赖实际base-rows，复测窗口价格与source通过。
2. 新测试一度误用 createDataQueryHandler、session.handle，导致函数缺失/500；改为实际 createDataQueryHttpHandler/session.current。类型检查补齐必需非目标路由port并保持调用即失败。
3. 异常cellCoverage原本会落通用500，现先safeParse并归类OutputContractError→502；完整coverage却0个returnedObjects亦拒绝。
4. main 0eb3156 随后被 arch amend 成206f27d；两条历史merge保留了旧非法/重复import，使全Worker typecheck失败。`d153d3d`/`49b2e6f`仅删除旧历史残留，runtime.ts与http-server.ts最终对main **0 diff**。请勿将这两笔merge纠正当新功能单独cherry-pick。
5. Web全测试旧七Query版本断言未加入pivot v1，修正为八Query精确断言，永久专门pivot tests仍在，不跳过新查询验收。

## 尚未完成 / 请 arch 接线

- **实际data-api启动factory未注入**：`apps/worker/src/data-api.ts` 当前 new PlatformDataSource 第4参为createPlatformDimensionQuery(pool)。请arch添加 import createPlatformPivotQuery from ./data/platform-pivot-query.js，并在第5参传createPlatformPivotQuery(pool)。这是你持有的共享composition；本分支未改它。没注入会固定 SOURCE_UNAVAILABLE，不造假ready。
- 非空 taskIds/filters 仍400，不忽略；下一段实现任务有效日筛选。api.md:897只给filters名称，尚无字段/操作符/AND-OR语法；请补明确filter形状或指向已冻结位置，我不自造另一套。其他轴/team发布源仍依赖后续，不声称预设三张都能用。
- fixture meta 还有 `_note`/业务日等通用示例字段；本批实现精确cellCoverage，不把这些说明字段复制成来源证明。当前完整v3公共metadata收口仍需继续。
- I002现已随本次Worker全量复验，但实际Next页面/内网投放源仍需arch合流部署验收。
- 013 item UUID/BIGSERIAL冲突、ETL运行ID runtime第59行number仍按P121/P123待arch处理；本批没有绕过或改DDL。

质控技能落实为类型/静态/全量/真PG/双包parity与失败留痕，未声称未经执行的覆盖率。按main新演示就绪目标，接下来优先R010a2 D6变更集试运行（停在确认前），其余A1/长期队列继续保留，不等审、不宣称总目标完成。
