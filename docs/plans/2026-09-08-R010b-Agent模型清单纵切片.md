# R010b Agent 模型清单纵切片

> 2026-09-08；be/r010；起点 4811b84，main c3451db。仅计划，完成证据另记。

## 范围与依据

- api.md v1.7.1、fixtures/agent/models.json、schema.sql provider_model_capabilities。
- GET /api/v1/agent/models + 同源 BFF；服务令牌及 Session 双鉴权；团队可读同一全局非业务模型清单。禁止模型探测、凭证读取、媒体写、视觉改动。
- R010a1/a2 剩余真实源及 013 DDL 冲突继续保留，不用本批替代前序功能完成。
- 实表没有 label/default：label 使用真实 model ID，default=false（未配置默认）；不拿 fixture 的示例默认模型当生产值。failed 映射 disabled；verified 缺实测时间/版本时降为 documented_unverified。该清单不等于用户凭证可用或 Agent 可运行，运行仍须原 Router 的检查。
- 不修改 arch Contract；将以上映射与默认配置缺口回执 arch。

## 实施

1. Domain agent-model-catalog.ts：严格 DTO、最大 1000 条、(provider,id) 唯一、最多一个 verified 默认；canonical fixture parity、非法值反例。
2. DB agent-model-catalog-repository.ts：已批准上下文，RR/RO，显式列（绝不读 secret/error_summary），确定性 LIMIT 1001 哨兵；类型与数量 fail closed，真实 PG 测试仅操作本批合成行。
3. Worker agent/model-catalog-service.ts + r010/agent-model-routes.ts：类型化安全错误；挂自己的 http-server/data-api；未知 query/写方法拒绝；requestId 与 exact16MiB 边界。
4. BFF 新增 agent-models 文件和 GET route：仅服务端凭证+Session，拒浏览器 scope/source 参数；复用 canonical schema，不手写第二套 DTO；校验 status/requestId/大小。
5. 红绿测试、DB 实际 PG、Domain/DB/Worker 全量 test/typecheck/lint、Web node 测试、定向覆盖及安全静态审查；限定提交，不 push。留痕交 arch 后继续有效队列。

## 非本批

模型配置写入/探测、用户 AK 绑定、消息 SSE、会话上下文、run 续传、默认模型路由配置接线均未由此实现。不得宣传为 Agent 已可对话。
