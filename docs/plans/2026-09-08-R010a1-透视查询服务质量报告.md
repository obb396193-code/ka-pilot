# R010a1 透视查询服务质量报告

代码 **8376333**，交审前已合main **f4205ce**。P125仓储之后继续实际窗口服务，尚未开放pivot2公开Query ID。

- account/task/biz以真实成员分组；跨日换任务产生不同cell，现金CPA按总现金/总转化重算；成本空间逐日price×conversion−cash。不join单维聚合表、不平均CPA。
- reader输出再过strict schema、三键/日期完整集合、现金转化双证据和全部observation对账。缺数保留，withData和undeterminable分开。真实时间只作为observation，未编造完整lineage或ready。
- 已批准输入在传reader前复制，防reader通过修改自己的入参改变返回验证基准。固定typed错误不含原响应/SQL/secret；连接错误保留供上层稳定映射。16MiB精确边界、10k成员预检、未知日期/字段/标签冲突/串scope全拒绝。
- factory实际组合PlatformPivotRepository，同RR/RO，不另起服务/队列。team和未接来源维度在取数前明确不可用；无个人credential或grant fallback。

## 门禁

- TDD missing-module RED → 2 positive → 最终35新增；加既有维度8，共Worker43/43。核心行/语句/分支/函数100%。
- Worker typecheck过；第一次lint因链式it.each换行no-unexpected-multiline失败，已同句修复，最终lint exit0；离线缓存production audit0。
- 前批同读侧Domain84/DB45证据仍归70d50f3，未冒称本SHA全量。新工厂unit走真实仓储但mock PG连接，不是PG证明。
- PG前批SELECT1拒连；本批未重复无意义迁移。磁盘1.6GiB低于8GiB全量门，未全量/上线/真实源联调。

## 未完成

source result schema、Registry、Adapter/HTTP/BFF、taskIds/filters和额外维度仍要接；cellCoverage是服务内部的事实投影，未替代全源readiness。

main新增I-002工作项列表BFF断路优先于常规队列；先修这一联调阻断，再回pivot2公开接线。候选已实现/非PG定向验证、待arch审，未merged/deployed/push。
