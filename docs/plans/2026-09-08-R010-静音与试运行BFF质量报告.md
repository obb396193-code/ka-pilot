# P137 静音与试运行同源 BFF 质量报告

日期：2026-09-08。代码：`29b3bbe`；基于本人 `be/r010`，包含权威 main@0d358d8。

## 交付及边界

- 新增三个同源 POST：`/api/internal/accounts/:media/:id/mute`、`/api/internal/work-items/:id/ignore`、`/api/internal/changesets/:id/dry-run`。各自固定转发至已有 `/api/v1` 路由，没有任意 URL/方法代理。
- 服务端配置注入 internal token，仅提取唯一合法 ka_session；浏览器 bearer、x-ka-*、其他 cookie 不转发。Origin 必须与 Request URL 的 origin 严格一致，Fetch Metadata 若存在必须 same-origin，且要求 JSON。反向代理部署需保留公开请求 origin；不能靠信任客户端 forwarded headers 修复 origin 不匹配。
- 严格路径、输入和响应。请求按真实 UTF8 字节限制 1MiB；响应命中 16MiB 即拒绝。JSON null 不混为超限；requestId 请求/响应头/body 一致；异常消息重新映射为固定安全文案。意外 Set-Cookie、HTTP/envelope 冲突、未知字段、假成功均拒绝。
- 非 POST 路由函数显式返回稳定 405/Allow:POST，不能操作媒体；没有 confirm/execute/retry/rollback BFF。超时/断网只调用一次，不把 HTTP 失败表述成数据库回滚或“恰好一次”。
- 静音及 ignore+mute 成功消费 Domain 同构 DTO。纯 ignore 仍为 503；dry-run 本批按已批准 source-off 方案返回 503，中文 message/retryable 与 arch fixture 完全相同。没有凭空给旧缩略 run record 补 observed 成功态。

## 证据

本轮使用自己的 `ka_be_r010_20260907_test`，仅本机 55432 + 合成数据。没有媒体 API、真实凭证、队列消费或 arch 服务重启。

| 门禁 | 本轮结果 |
|---|---|
| RED | 缺 r010-command-bff 模块，测试先失败；原日志保留于 p134/bff-red.log |
| Web 全部 node:test | **190/190**；新增 14 组，包含多个边界子场景 |
| Worker HTTP/启动/Session/原查询/Domain parity | **150/150**，9 文件；不是 Worker 全量 |
| BFF → 实际 src/data-api.ts 子进程 → DB Session → PG | **1/1** 联合场景；非 fake HTTP composition |
| Worker 全包 typecheck/lint | 通过 |
| 新增 Web 文件及依赖的定向类型检查/lint | 通过；专用 tsconfig 在 output/qa/p137，不改变项目配置 |
| 新 BFF 覆盖率 | 行 **100%**、函数 **100%**、分支 **93.68%** |
| Worker 离线 production audit | 缓存结果 0 vulnerabilities；不是联网最新公告审计 |
| 静态 diff/安全检查 | diff --check 通过；无日志/token/SQL/上游原文回显，无任意地址代理，无跨仓/视觉/Contract 修改 |

真实 PG 联合场景：批准个人 KUAISHOU 静音及 ignore+mute 落库；同号 TENCENT 403；重复 ignore 409；合法未过期草稿 dry-run503且 draft/hash/run 不变；切 team 后旧 token401、当前团队三命令403；logout 后三 BFF401。伪造 x-ka-* 不能扩 scope。严格 Schema 与 Domain 对比，source-off 错误与冻结 fixture 对比均为永久测试。

## 未通过与未执行项

- Web **全量 typecheck 未通过**：本 worktree 既有组件缺 shiki/ai/BlockNote/ogl 等已声明依赖及其派生类型错误；最终日志无新 r010-command 诊断。未改组件、未执行大 npm 安装，不把定向通过说成全 Web 类型绿。
- 初次定向 tsconfig 位于 output，未显式 typeRoots 导致找不到 Node 类型；修正测试配置后通过。初次新代码 lint prefer-const 已修；日志保留。首次回归命令两个文件名未命中，实际仅59测试；已用正确文件名重跑150，不用首轮命令夸大覆盖范围。
- 磁盘约3.6GiB，低于8GiB全门禁窗口；未跑全 DB/Worker、Next build 或真实 Next 页面。本批测的是实际 BFF函数 → 真实后端进程 → PG，**不是启动 Next 后浏览器点击截图**。请 arch 合流后在其部署环境复验三条 Next routes。
- 生产 RuleScan 仍未消费静音；本批只保证配置/事务/HTTP通路，不宣称实际 P1/P2 通知已被抑制。纯ignore成功DTO、D6真实 preflight/成功映射仍分别待后续；媒体执行全部关闭。

日志目录：`output/qa/p137/`。状态：candidate + local targeted verified，待 arch 审查/合流/部署。总信箱目标继续 active，交审后继续其他可做批次。
