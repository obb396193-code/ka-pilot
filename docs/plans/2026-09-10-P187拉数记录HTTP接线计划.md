# P187 拉数记录 HTTP Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 该子技能未提供；沿老板持续目标在本人独立 worktree 直接执行，不派子 Agent。

**Goal:** 将 P186 只读仓储挂入真实可启动的 GET /api/v1/system/etl-runs，供治理后台读取。

**Architecture:** 复用 Data API internal bearer + Session 认证壳；新 EtlRunListService 校验唯一 approved admin 与仓储返回的 workspace/page/数据；独立 R010 route 负责严格 URL query、状态码、requestId 和 exact16MB。data-api.ts 注入真实PG仓储，KA关闭不阻断。POST rerun 留后续独立提交，不启动队列。

**Tech Stack:** TypeScript、Zod、Node HTTP、PostgreSQL、Vitest。

### Task 1：HTTP红例

新增 `apps/worker/test/etl-runs-http.test.ts`：注册真实server，测试200/400/401/403/405/500/502/503/504，默认及非默认分页、重复/未知参数、非法数值、跨workspace返回、分页漂移、时间未知、固定时间口径说明、缺服务、不泄露错误正文、exact上限。

Run: `npm --prefix apps/worker test -- run test/etl-runs-http.test.ts`；期望未接线时404红。然后实现下一步。

### Task 2：Service与composition

- 新建 `apps/worker/src/admin/etl-run-list-service.ts`、`apps/worker/src/r010/etl-run-list-route.ts`。
- 修改 `apps/worker/src/data/http-server.ts`（only option/route/auth分支）、`apps/worker/src/data-api.ts`（真实依赖注入）。
- 修改 `packages/domain/src/etl-run-list.ts`：meta可带固定 `_note`（运行观测时间，不是canonical数据新鲜度），不允许任意私密文本。
- server Session不可由x-ka-*覆盖，team只读admin可看本workspace记录。Service用上海03业务日表示请求业务日，不据此捏造run日期/数据新鲜度。
- type/lint + focused HTTP/既有读路由回归。

### Task 3：真PG联合测试

新建 `apps/worker/test/etl-runs-http-pg.integration.test.ts`：本人本机独占测试库，随机workspace数据；真实session发放/切换/注销，GET读真实仓储，旧cookie/降权/撤权拒绝，旧run次数空保留、跨workspace无泄漏。补真实data-api启动（无KA env）探针，不能仅factory模拟。

执行test/typecheck/lint、质量技能安全/coverage/audit检查。独立代码SHA+质量回执+状态/审查信箱；不改arch台账、不push/不改前端。磁盘6.2GiB不做五包全量门禁，不能冒充已部署。
