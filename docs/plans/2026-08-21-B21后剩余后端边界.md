# B21 后剩余后端边界盘点

> 结论：当前不依赖 Claude 契约、前端和外部权限的高价值纯后端工作已推进到安全边界。继续写 DB/API/Runtime 会开始猜契约，不应抢跑。

## 已有可审底座

- 数据/ETL/质量/语义查询、规则/工作项、Changeset、任务 pacing/日报；
- Claude Agent SDK 单运行时、多模型协议网关、上下文/诊断/Provider；
- 工作流 Registry、DAG、Simulation、固定版本、恢复/确认门；
- 知识正文/双链/权限化 citation；
- 素材整段云 ASR 端口、语义拆解、逐镜头帧墙、相似/谱系、实验矩阵、Brief；
- 月度结算纯内核、公共资产治理、工作流未触发诊断。

## 下一步必须等契约或外部事实

1. **统一接线**：assets、settlement、trigger diagnostic、素材与知识的 DB/API/权限/页面 DTO 需 Claude/arch 审查冻结。
2. **真实 OS/奇航**：产品身份 trace、关户端点、程序化广告完整枚举、商品/承接页、素材 URL 租约、IdeaLab ASR 正式协议。
3. **生产部署**：FaaS/网络 PG/Secret 服务/BUC/钉钉 Stream 的真实环境和回滚方案。
4. **前端验收**：九导航、数据总表/报表设计器、工作流画布、账户池、素材帧墙、结算单、资产列表、运行中心。

## 不应继续盲写的候选

- 加/关账户和开户测试状态机：主数据 ID、关户能力和清理对象契约未冻结。
- 公共资产/结算/诊断 Repository：会修改 migration 和公开 DTO，属于 arch 审查范围。
- 知识库和工作流 UI/API：Claude 负责前端，revision/ETag 与公开 graph 契约仍待裁决。
- 自动策略执行：必须先完成真实写链路、权限和效果回收，不能仅靠纯 Domain 假闭环。

