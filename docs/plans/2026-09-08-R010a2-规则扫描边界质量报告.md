# P132 规则扫描边界质量报告

## 当前交付

- 独立代码 `37fc59d`，main `1d0666d` 通过 `36659a8` 合入，再同步 `39f50b5`（`7092a89`）。两处冲突仅信箱追加和Domain导出，双方内容保留；结构性composition对main零diff。
- Provider批量上限10,000、候选基础结构校验、运行私有快照及评估器独立副本；strict评估/工作项sink/通知sink结果。非法评估先拒绝，不写工作项；sink结果非法则不通知，已经发生的sink写入不能假装回滚。
- 原始Provider异常变固定source错误，逐候选失败只记安全固定消息；workspace mismatch不含两边真实ID。部分失败继续健康候选，全部失败安全抛错。
- 不是新的授权系统。生产Provider与持久化Sink必须独立校验批准tuple；本批不把TypeScript类型或workspace比较冒充账户授权。

## 实测

日志均为本worktree `output/qa/p132/`，合成数据。

| 门禁 | 结果 |
|---|---|
| TDD首轮（red.log） | 16失败/12过，复现原始异常泄露、候选被改、非法输出直通 |
| Worker四文件定向+coverage | 57/57；20新增边界、14扫描回归、21静音、2失败通知 |
| 两核心文件覆盖 | 行98.94%、分支97.18%、函数100%；新边界模块全部100% |
| 合main后Domain规则/就绪/搜索 | 34/34；旧search.meta漂移已由be2修复 |
| 合main后Worker四文件 | 57/57 |
| 合main的R014两路由文件真实PG | 20/20，本人55432 `ka_be_r010_20260907_test`，单worker、30秒hook/test |
| Worker全typecheck/lint | 最终均exit0 |
| Worker production audit --offline | 0；缓存审计，不代表实时公告源 |
| diff --check/源码安全扫描 | 通过；无新增凭证、eval、Shell、原始错误日志 |

保留失败：第一次typecheck exactOptionalPropertyTypes不兼容，改为省略undefined；后续测试mock零参类型报错，补明确sink函数类型。首次lint控制字符正则被拒，改字符码检查。最终均重跑通过。

合main回归意外包含新R014 PG文件，其默认指向be2库；沙箱EPERM在连接阶段拒绝，57过/20skip、2suite失败，不称通过且没有数据库写入。随即显式覆盖为本人隔离库并用升级权限单worker重跑20/20；今后新增回归glob需先复核PG默认连接。未修改be2文件。

## 未完成与风险

- 磁盘约4.5GiB低于8GiB全量阈值，未跑三包全量/全PG，不拿P130旧结果冒充。
- `runtime.ts`没有规则扫描注册；当前只有测试Provider/工作项sink/通知sink，不能宣称已每日运行。尚未把accountMuteIsActive接入扫描，也未持久化suppressedByMute与账户检查coverage。
- 已有`work_items.occurrence_count`不是“被静音而未创建工作项”的持久化载体；不擅自塞入任意JSON或造表。请arch明确流水承载和生产Provider接线。
- 当前只3个既有内建规则；P122复合AST尚未成为生产扫描/公开explain。无Contract/视觉/媒体写/外部部署/push，本批状态candidate + targeted verified。
