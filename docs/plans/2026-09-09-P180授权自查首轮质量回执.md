# P180 授权自查首轮质量回执

## 状态与交付

- 对应arch新P-178授权自查，**不是**已合流的本人旧P178库名守卫。
- 同步main b0351577→**95e46310**；唯一冲突是台账尾部追加，保留双方。同步带来的FE/be2差异均来自main，本批本人没有编写这些文件。
- 代码 **d124cfce**：2处重复tuple谓词改用`r014/workspace-authority.accountScopeClause`；3文件71增14删，公开Contract不变，未新增角色或放宽team。
- 本轮在已验证查询中**没有复现原实现向公开响应泄漏未授权数值**；有2处实现重复已收敛、1组工作项口径冲突待arch裁。不能包装成“修了2个已证越权”。**全域自查尚未完成**。

## 已追踪的查询和边界

以下行号对应本批代码；“有边界”不等于已满足新版所有仓储必须引用共享组件的规范。

| 文件/入口 | 实读发现 | 状态 |
|---|---|---|
| semantic-query-support.ts:82/120 | buildMetricFilter含workspace/date+approved tuple；本批改共享accountScopeClause，空数组仍false | 已收敛/PG负对照 |
| semantic-query-metrics.ts:109/131 | summary/trend经上述filter；expected LEFT JOIN保留缺数 | 已核/PG |
| semantic-query-repository.ts:93/173 | table count/page、lineage物理查询都用filter；task expected同scope | 已核/PG |
| semantic-query-dimension.ts:64/93/139 | overlap/group查询同filter；账户维度输出再次核tuple；摘SQL后输出guard拒绝 | 已核/PG |
| semantic-query-health.ts:119/161 | canonical coverage、Raw分组经metric filter；accounts分母另有重复谓词，本批统一 | 已收敛/PG |
| semantic-query-health.ts:186/207 | etl状态/quality计数仅workspace/date，不是账户指标；查当前Worker未发现queryHealth公开调用 | 内部用途，公开接入前须重审，不算授权完成 |
| window-assessment-repository.ts:88/120/152 | daily/by-account/count均从expected与buildMetricFilter收口 | 已核/PG |
| platform-pivot-sql.ts:3；platform-pivot-repository.ts:66/84 | expected只来自受信tuple数组；库后复核每行ws/pair/day；team该reader不可用 | 有双层边界；仍待按新铁律收敛组件，不冒充完成 |
| platform-health-repository.ts:22/68 | personal visible_accounts受信数组；team限定workspace；strict auth+RR/RO | 有范围边界；仍待统一组件/负对照 |
| work-item-list-sql.ts:10；read-detail-service.ts:47/53/117 | 账户型tuple；双null凭本人assignee/creator，未区分taskId | 新旧口径冲突，已发arch P180问题 |
| work-item-repository.ts:331；changeset-repository.ts:372 | find只按workspace/id；业务ReadDetailService在返回前再核身份/tuple，team changeset提前拒绝 | 仓储无SQLscope不等于响应泄漏；新铁律要求仍需收敛，不标误报后跳过 |
| changeset-repository.ts:160/255/278 | items回读逐行比header tuple；dry-run Service先find authorize，prepare后再authorize，写结果绑定expectedScope | 已读入口，执行/补偿各条仍待完整审计 |
| work-item-command-repository.ts:32/43；account-mute-repository.ts:29/50 | 先取work item再比session tuple；锁live membership/grant再写，包含revoked_at过滤 | 有边界，未改写流程；共享化仍待收口 |
| rule-definition-repository.ts:48/62；rule-evidence-repository.ts:24 | strict approved auth；target先命中tuple，后规则适用范围；evidence缩到单户走pivot | 不是全量账户规则查询；后续核公开包装/统一组件 |
| metrics-repository.ts:126/197；data-quality-repository.ts:45/98 | Canonical settings/history按明确tuple输入；质量任务是workspace后台维护，不接session | 待维护任务授权快照设计核对，不能伪装成成员scope |
| ad-hourly-metrics-repository.ts:17 | 仅upsert无SELECT读出口；冲突要求media/account一致 | 读取检查不适用，写范围另由ETL掌握 |
| r014/external-change-repository.ts:50/119 | 归be2；assertAccountVisible live grant，SQL再限定ws/media/account | 只读核查，未跨域改 |

检索发现changeset-follow-up/reconciliation/execution-queue/dry-run的内部SELECT，已看header/claim限定；尚未把每条持久化job和runtime调用授权闭合证明，因此**不报全域通过**。账户/任务列表、日报、搜索、通知属于be2，不擅改；各SQL共享化要按归属交接。

## 实证与门禁

- 新3项PG先2红1过：共享组件断言未满足、摘除共享谓词找不到目标（原独立谓词仍保护）。改后3过。
- 同空间同account_id下KUAISHOU=7、TENCENT=70，外空间同号=700。正常scope汇总7；测试query wrapper只摘掉共同tuple谓词，汇总/趋势/window变77、table/health计数2；account维度输出层拒绝越权。这是**测试负对照**，生产SQL没有去掉权限。
- 空grant下一请求summary缺数、table/trend/window空；跨workspace读取自己空间700，未混入原空间。Session真实PG回归另外覆盖旧token/切空间/伪造头/logout，不用上述scope替换冒称会话撤权全链证明。
- DB4文件35/35（含P179失效屏蔽7、旧Semantic20、Window5、新3）。Worker6文件83/83（Session PG4、Window PG9、其他70）。**合计118定向，不重复累加覆盖率重跑23项。**
- 三包typecheck/lint/offline production audit全exit0，缓存audit0。两处受改模块v8覆盖94.27%行/88.11%分支，新授权分支已命中；没有依赖升级。
- 磁盘7.6GiB低于8GiB，未跑全包门禁、未清缓存。无媒体请求/写操作、不push。

```sh
# 各PG命令先设置TEST_DATABASE_URL为本人独占ka_ci_be_r010_p178_test（本机55432）
# packages/db
npm test -- --run test/semantic-authority-regression.test.ts test/semantic-query-repository.test.ts test/window-assessment-repository.test.ts test/etl-batch-readability.test.ts
npm test -- --run test/semantic-authority-regression.test.ts test/semantic-query-repository.test.ts --coverage --coverage.include=src/semantic-query-support.ts --coverage.include=src/semantic-query-health.ts --coverage.reporter=text
# apps/worker
npm test -- --run test/platform-window-query-pg.integration.test.ts test/business-read-session-pg.integration.test.ts test/read-detail-service.test.ts test/work-item-list-service.test.ts test/platform-dimension-query.test.ts test/platform-pivot-query.test.ts
# Domain/DB/Worker分别
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

## 质量自查

SQL依旧参数化；共享helper只接代码固定expression与占位符，没有浏览器拼SQL。新增测试没有真实凭证/金额，随机workspace自身清理。无新增IO/锁/查询次数；计划形状仍EXISTS，仅合并相同代码。旧team不携带账户数组的路径不被个人谓词误限。数据库读只认上层已批准scope，不能拿这个helper作鉴权替代。

## 接续

继续其余reader统一化、工作项矩阵待裁和维护job授权边界；之后F-P179（system/etl-runs）→P176 Task3→F-OS-004→021。P179读取屏蔽已交但不能开启Full/Incr半成功。整条信箱目标active，本批没有宣称已合流/部署/完成全部授权自查。
