# P193 Coverage Pending Gate Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 arch v1.9.19 仅暂缓两项明确派给 F8-15 的覆盖缺口，到期自动转红。

**Architecture:** PENDING 是精确路径/方向/负责人/绝对到期时间的测试登记，不是 BACKEND_ONLY 永久豁免。生产路由不改变；已解决登记必须删除。未获准的 reset-password 反向缺口保持失败。

**Tech Stack:** TypeScript, Vitest, existing route inventory.

### Task 1: 先红测试
- Create `apps/worker/test/r010/coverage-pending.test.ts`，验证到期前、恰好到期、到期后、错误方向、非法时间、重复/已完成登记。
- 在 apps/worker：`npm test -- --run test/r010/coverage-pending.test.ts`，模块不存在应红。

### Task 2: 精确登记和实际绊线
- Create `apps/worker/test/r010/coverage-pending.ts`，只登记 `/api/v1/system/etl-runs` 与 `/api/v1/admin/data/reconcile`，owner=F8-15。
- `expiresAt=2026-09-12T00:00:00+08:00`，按部署业务时区到期日开始即恢复红闸；时间不能读取 ENV 绕过。
- Modify `apps/worker/test/r010/bff-coverage.test.ts`：缺路由时只暂缓精确匹配、未到期项；解决后残留登记单测报错要求清理；数量/sentinel/反向检查保持。

### Task 3: 收口
- 三文件定向 test、Worker typecheck/lint、git diff --check，分别报告已批准方向通过与仍未实现的反向失败。
- 不把 reset-password 擅列临时豁免；已发 arch 待裁条目。路径限定独立代码SHA、质量回执/SHA；不改前端/合同/台账、不push。

执行 skill 未安装，按持续目标当前会话逐步实施，不开子 Agent。
