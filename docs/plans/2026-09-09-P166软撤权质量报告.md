# P166 软撤权质量报告

日期：2026-09-09。分支 `be/r010`，代码 **16c7c2517f9080c592d9a59c7e917d0ce31cb5fc**，随后 **6e1e973** 合 main（含 `1602d4b` 对 P164/P165 合流）。新代码未合流、未部署、未 push。生图取消；媒体写仍关闭。

## 发现与范围

018 已在主线，`account_access_grants.revoked_at` 非空代表撤销，保留历史行。原 Session/sync/mute/admin 查询未过滤它：撤销账户会继续作为有效权限、进入同步候选或允许修改静音。

本次修改四个本人生产文件，统一在聚合或锁前加入 `(to_jsonb(grant_alias)->>'revoked_at') IS NULL`：

| 文件 | 修复 |
|---|---|
| packages/db/src/auth-repository.ts | personal Session grants 不含撤销行，撤销历史不占1000有效权限限额；team 仍不解析 grants |
| packages/db/src/workspace-sync-repository.ts | tick 候选仅携带当前有效媒体账户；全部撤销即空范围，沿用原 ACCOUNT_SCOPE_MISSING 门 |
| packages/db/src/account-mute-repository.ts | live authority 的锁查询拒绝已撤销行，旧已批准上下文也不能修改/读取该户静音 |
| packages/db/src/admin-members-repository.ts | 有效权限明细与 grantsCount 同时过滤历史撤销行 |

018 之前没有 JSON key 时为 NULL，这是兼容既有 migration 窗口的查询写法；不改变 DDL、不恢复已撤销授权、不删除历史。该兼容写法已由 work-item-command-repository 使用，P166 没有额外执行生产降级。

## TDD 实证

新增 `packages/db/test/grant-revocation-pg.test.ts`，初跑 **5失败/1通过**，不是推测：

1. 撤 KUAISHOU 后 Session 仍返回它与同号 TENCENT 两项。
2. 1001 撤销历史 + 2 有效行被视作权限超限，会话拒绝。
3. 同步候选仍含撤销户。
4. 成员 grantsCount 仍计两项。
5. 旧授权上下文修改静音成功，本应拒绝。

team readonly 对照原已通过。修复后上述六项全过，新增已撤销后的 find 静音反例也过；未产生真实媒体动作。

HTTP 测试在 `business-read-session-pg.integration.test.ts`：KA on/off 两种配置均真实登录取 Cookie → 先看见1账户 → 直接在合成PG软撤权 → **同一Cookie**下一次 accounts/tasks 返回 total=0/items=[]，伪造 x-ka-account-scope 无效；requestId 保留。测试完成仅恢复本 suite 创建的合成 grant。原双空间、跨媒体/跨workspace、旧token、退出后拒绝测试全部保留。

初次新增HTTP测试有2个500：fixture只提供两个固定token，第三次登录 fallback 复用team token碰唯一键。改为固定两条用尽后生成randomUUID；只改测试工厂，不改生产token。修后测试通过，保留这次失败原因，不掩盖。

## 合 main 后最终门禁

| 范围 | 结果 |
|---|---|
| DB 五文件 | 62/62，其中真实 PG46，unit16 |
| Worker 五文件 | 60/60，其中真实 PG+HTTP4，其他56 |
| DB/Worker typecheck、lint | 全过 |
| DB离线production audit | 缓存报告0 vulnerabilities；不是实时联网安全结论 |
| 4个生产模块覆盖 | 行/语句89.4%，分支78.97%，函数97.72% |
| diff --check | 通过 |

覆盖为本批同代码、合main前采集；分文件行：mute92.48%、admin100%、auth87.45%、sync85.56%。本次新增5条SQL过滤均真实执行。分支尚未全覆盖，不宣称全部故障注入完成。没有新增依赖、动态SQL值或密钥/日志。

复跑命令：

```sh
# packages/db，本机隔离合成库；没有使用共享ka库
TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_p158_20260909_test npx --no-install vitest run test/grant-revocation-pg.test.ts test/auth-repository.test.ts test/workspace-sync-repository.test.ts test/account-mute-repository.test.ts test/admin-members-repository.unit.test.ts --maxWorkers=1
npm run typecheck
npm run lint
npm audit --omit=dev --offline --json
# 覆盖参数追加 --coverage --coverage.include=src/auth-repository.ts --coverage.include=src/workspace-sync-repository.ts --coverage.include=src/account-mute-repository.ts --coverage.include=src/admin-members-repository.ts --coverage.reporter=text

# apps/worker，另一隔离合成库；server仅本机临时端口
TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test npx --no-install vitest run test/business-read-session-pg.integration.test.ts test/workspace-sync-service.test.ts test/session-auth-service.test.ts test/account-mute-service.test.ts test/admin-members-http.test.ts --maxWorkers=1
npm run typecheck
npm run lint
```

## 未关闭项——必须交 arch，不跨文件所有权

be2以下SQL实读仍无 revoked_at 过滤；**不能认为修Session就替代所有repository live检查**，它们有独立查询，需按真实入口权限重新验证：

- `packages/db/src/r014/account-pipeline-repository.ts:25,105`。
- `packages/db/src/r014/external-change-repository.ts:55`。
- `packages/db/src/r014/me-workspace-repository.ts:93,103`。
- `packages/db/src/r014/search-repository.ts:68`。
- `packages/db/src/r014/user-watchlist-repository.ts:44`。

这些只有源码确认，不宣称已对be2各HTTP复现；请求arch派修并补软撤权/同号跨媒体反例。P166本人四入口已实证修复；不代表全产品账户交接验收完成。

另：bootstrap 的 `INSERT ... ON CONFLICT DO NOTHING` 不会清 revoked_at，旧seed重放不重新授权；但其 `bootstrap-grant-count` 仍计历史行，属于可用性后续项。本轮未改变bootstrap语义。已经排队的ETL job持有固定授权快照，其运行时撤权复核需另审，不能用本次tick过滤宣称已验证。

磁盘一度仅2.3GiB、末次约4.3GiB，低于8GiB全量门槛；未跑全包/build，未清缓存/数据库，未启动业务worker/beat。无生产性能/内网部署/真实源证明。整个信箱目标仍active；其余契约阻断保留P160/P163/P164。
