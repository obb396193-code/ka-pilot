# 工作项驳回边界 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按已冻结 api.md:574 收口 processing 才能 reject、原因必填，并保证更新失败不提交事务。

**Architecture:** 复用 Domain 状态机和 WorkItemRepository 行锁事务；原因在取连接前检查，状态在锁定真实行后检查，UPDATE RETURNING 成功转记录后才 COMMIT。不新增 HTTP/公开 DTO，不开放媒体写。

**Tech Stack:** TypeScript、Vitest、node-postgres。

---

这是 R010a2 已批准需求的修错子批，不代表完整处理闭环。`superpowers:executing-plans` 本会话不可用，主 agent 按以下 TDD 顺序直接执行，不派子 agent。

## Task 1: Domain 状态机

- Modify `packages/domain/src/work-items.ts`；Test `packages/domain/test/work-items.test.ts`。
- 先将旧 open→reject 正向用例改为 processing→reject 正向，并覆盖其余七个状态 reject 拒绝；跑 `npm --prefix packages/domain test -- --run work-items.test.ts`，应先红。
- 删除 open/escalated 的 reject 路径，其他动作不改；重跑定向。

## Task 2: Repository 原因与提交边界

- Modify `packages/db/src/work-item-repository.ts`；Create `packages/db/test/work-item-transition.unit.test.ts`；Extend `packages/db/test/work-item-repository.test.ts`。
- 单测先断言 undefined/null/空串/纯空白/非string rejectReason 不申请连接；合法原因 processing 成功；open/escalated 驳回 rollback 且不发UPDATE；UPDATE无返回行 rollback，不COMMIT。
- 跑 `npm --prefix packages/db test -- --run work-item-transition.unit.test.ts`，应先红。
- `action==='reject' && (typeof rejectReason!=='string' || rejectReason.trim().length===0)` 抛无敏感详情错误；`requiredRow`移到COMMIT之前。
- PG 验证缺原因保原状态、open拒绝、processing合法原因落库；用显式隔离库，不写默认共享库。

## Task 3: 门禁与留痕

- Domain 定向+全量；DB全部unit+修改PG套件；Worker非PG全量；Domain/DB/Worker typecheck/lint。
- 质量检查覆盖修改逻辑、参数化SQL、无新日志/凭证、无新增依赖；offline audit明确不是实时情报。
- PG仍不可达则记实际错误/跳过数，不报通过。现存Domain权威fixture漂移如仍失败，单列，不改Contract。
- 路径限定代码commit+独立台账/信箱commit，不push。

## 同时交 arch 的契约缺口（本批不实现）

- v1.3 动作写 `dispatched`，schema.sql WorkItemStatus及现有CHECK没有该状态；012 partial unique列dispatched却漏escalated。需统一有效状态集合。
- API要求跨级重弹关闭旧条并用superseded_by指向新条，但work_items无此列（assets有同名列，不可混用），旧Repository原地升级。需arch补列/迁移归属和旧记录终态，之后一次落实计数/重弹/派发，不能先自造DDL。
- 拒绝请求正文旧段仅note，v1.3要求reject_reason；其HTTP命名由arch统一后接线，本批只内部rejectReason。

## 实际结果与质量检查（2026-09-06 14:48）

- 代码 `122aa6f`，5文件；无Contract/HTTP/视觉/媒体写改动。Domain仅processing允许reject，Repository原因在连接前校验；条件更新仍按workspace/id/旧status，返回记录校验先于COMMIT。事务rollback失败不覆盖原异常。
- TDD：Domain先2失败/19通过，Repository先10失败/7通过；修后Domain21通过，Repository最终23通过（含共享映射/插入/同级合并/find回归，不代表计数/重弹已实现）。
- DB纯逻辑全量21文件231通过，`/tmp/ka-reject-db-unit-final.log`；Worker非PG102文件1055通过+2外部opt-in跳过，`/tmp/ka-reject-worker-full.log`。Domain全量50文件通过/2文件失败，633通过/10失败，`/tmp/ka-reject-domain-full.log`；全部10个仍是P073已列权威v2/priceSource样例漂移。本批不删旧fixture断言，不改Contract。
- Domain/DB/Worker typecheck、lint通过；DB offline production audit0（非实时漏洞情报）。无新依赖、env、日志或URL；SQL仍参数化，事务无新增网络/模型调用、无N+1。只有本地状态写，不开媒体操作。
- 代码质量技能覆盖检查：Domain工作项96.92%行/92.85%分支；Repository99%行/90%分支，`/tmp/ka-reject-{domain-coverage,db-coverage-final}.log`。初次仅transition用例整模块42.57%行低于80%门，补共享helper回归后重跑通过；不是调低阈值。未覆盖的Repository两行是旧活动项severity缺失异常，非本次修改。
- 真PG命令显式 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r013_20260906 PGCONNECT_TIMEOUT=3 npm --prefix packages/db test -- --run work-item-repository.test.ts --hookTimeout=10000`。实际ECONNREFUSED，一套初始化失败/8跳过，`/tmp/ka-reject-pg.log`；新增PG反例已写未运行成功，不使用默认共享库、不重启Docker。
- 状态candidate_non_pg_verified/pg_pending，未合流/部署/push。**R010a2整体仍未实现**：缺计数/跨级重弹/静音/HTTP DTO/派发等；上方契约问题报P083，后续按arch冻结继续。
