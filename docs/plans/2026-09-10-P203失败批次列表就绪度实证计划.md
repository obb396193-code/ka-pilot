# P203 失败批次列表与就绪度 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在打开 P176 Full/Incr 容错前，以真实 PG 复现账户/任务读取旧值与首次同步误就绪的剩余缺口，给 arch 可复验的派修依据。

**Architecture:** 沿用 P176 已批准的失败 tuple-day 为 missing、后续真实补采并重算才恢复的设计。不改 be2 列表文件。独立诊断脚本在显式本机合成库单事务内造数，直接执行当前生产 SQL；用现成 `etlBatchReadableSql` 作为修复方向的对照，全部回滚。诊断成功不代表功能正确或正式门禁通过。

**Tech Stack:** TypeScript、pg、真实 PostgreSQL、共享 SQL helper。

---

1. 新建 `packages/db/scripts/probe-list-batch-readability.ts`；拒绝非 localhost:55432 / ka_*_test，固定 synthetic 字段、随机 workspace。
2. 同号跨 workspace/media，各一条 canonical；在目标 tuple 加 typed failure 事实，并创建 done full run。
3. 执行 ACCOUNT_LIST_COUNT/PAGE、TASK_LIST_PAGE、WORKSPACE_SYNC_READINESS SQL，记录失败旧值与误完整；对照 shared helper 验证 tuple/date 隔离、fresh Raw 不足以恢复、recompute 才恢复。
4. 命令：在 packages/db 执行 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test node --import tsx scripts/probe-list-batch-readability.ts`。显式断言保证反例真的触发；任何意外结果报错而非静默绿。
5. 单独 tsc/eslint、共享读取回归；磁盘不足 8GiB 不启动全包/构建，不清理用户文件。代码路径限定 commit 后将结果和待派修文件写入本人状态、inbox-arch。

执行子技能不可用；按持续授权本人执行。Coze 会产生额外云项目，不符合本次既有本地后端修复范围，不使用。
