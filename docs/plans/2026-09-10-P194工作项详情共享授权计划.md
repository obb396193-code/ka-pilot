# P194 Work Item Detail Authority Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 详情与P192列表共同遵守v1.9.11三类矩阵，关闭任务型列表可见/详情403的断路。

**Architecture:** WorkItemRepository 新 findForRead(ws,id,auth) 专供HTTP；原find仅内部job。RR/RO先SELECT最小身份+共享workItemScopeClause的可见性+任务获授权tuple证据；拒绝不加载evidence/diagnosis，允许才在同snapshot读正文。返回内部 {record,taskScopeAccount}；Service核身份/结构/tuple，公开DTO不变。

**Tech Stack:** TypeScript, PostgreSQL, shared authority helper, Vitest, existing HTTP composition.

### Task 1: 红绿数据库反例
- 新建 `packages/db/test/work-item-read-authority.test.ts`：独占合成库、套件自主迁移；账户/任务/私人各型、跨媒体同号/跨workspace、空grant、team排私人、撤权/断关系、去掉共享谓词暴露错误行，正文未查询证明。
- Run（packages/db）：`npm test -- --run test/work-item-read-authority.test.ts`；先方法不存在红，后绿。

### Task 2: 最小实现
- 修改 `packages/db/src/work-item-repository.ts`：导出WorkItemAuthorizationError/WorkItemReadResult；新增findForRead，不改原find/transition/job语义。
- 修改 `apps/worker/src/data/read-detail-service.ts`：WorkItemDetailPort必须findForRead；处理typed403、内部证据，不再把全部双null当私人。
- 修改使用此port的测试：read-detail-service、data-api-http、各list-http、business-read-session-pg；composition原来直接new Repository无需改。

### Task 3: 联合验证与交付
- Service恶意port：错workspace/id、半空tuple、错误proof、无proof任务、team私人；真实PG+Session HTTP加入任务型详情200、隐藏任务403和撤权后403。
- 原details/work-item/list/data/session/changeset/16MiB/requestId回归；DB/Worker type/lint、缓存audit。disk<8GiB不跑全五包，明确targeted。
- 独立代码SHA、质量报告、R010状态/inbox-arch。未合流/部署/媒体写、不push、不改前端/arch台账/合同。

执行子技能不可用，按持续目标逐步直接实施。rerun sourceRunId冲突待arch期间推进这一已冻工作。
