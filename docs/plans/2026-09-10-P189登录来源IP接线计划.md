# P189 登录来源 IP Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 arch P189 把可信 BFF 传入的首个 x-forwarded-for 地址传给 SessionHttpService.login 的第三参。

**Architecture:** Data API 已先验 internal bearer；只在该认证后的登录分支读取首段合法 IP，否则回落 socket.remoteAddress。不修改 be2 的限速器/Guest DTO/身份逻辑；匿名互联网客户端不能绕过 internal bearer。BFF/反向代理必须替换而非盲信客户端转发头，这个部署信任前提在交接明确。

**Tech Stack:** Node http/net、Vitest、既有 SessionHttpService。

---

executing-plans 未提供，依用户持续实施授权本会话直接执行，不派子 agent。使用 writing-plans + code-quality-checker。

1. 新建 `apps/worker/test/session-client-ip-http.test.ts`：真实 HTTP 壳 + 真实 SessionHttpService/GuestLimiter，假合成 Session 存储计数；20 次来源 A 第21次挡下，来源 B 仍进入存储；测试第一段/IPv6/socket/非法头/缺 bearer/非法方法。不得把 guest 当前未完整 resolve 当上线成功。
2. 修复前 `npm --prefix apps/worker test -- run test/session-client-ip-http.test.ts` 应在第二 IP 和 login 第三参断言失败。
3. `apps/worker/src/data/http-server.ts` import isIP，登录前首段 trim + 格式校验；调用 `login(body,requestId,clientIp)`。不改变其他 Session 路由。
4. 同文件 + session-http + session-auth + etl-runs-http 定向回归；Worker typecheck/lint、diff check、离线依赖审计。磁盘<8GiB 不跑全量、不声称全量绿。
5. 独立代码 SHA + R010-状态/inbox-arch 留痕。F-P179-Q 已发 rerun 两点待裁（不占用 arch 新派 P191 viewer），其余队列继续，不开放媒体写。
