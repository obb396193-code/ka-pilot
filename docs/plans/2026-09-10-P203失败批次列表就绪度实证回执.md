# P203 失败批次列表/首次就绪度实证回执

- 代码/计划 SHA：`16b86764`。状态：**缺口已复现，未修复；不是部署完成或门禁全绿**。
- 背景：P176 Task2 明确要求所有读取屏蔽失败 tuple-day 后，才接 Full/Incr query 重试耗尽后继续。现有 semantic/pivot/规则已接共享屏蔽；账户/任务读取由 be2 维护，不在本批跨域修改。

## 真实 PG 结果

`packages/db/scripts/probe-list-batch-readability.ts`，在显式 `ka_be_r010_p194_test` 执行；生产 SQL 原样读取，不靠 mock。单事务插入合成空间/账户/任务和失败事实，退出全部 ROLLBACK，验证空间不存在。

| 实测对象 | 当前错误结果 | 对产品影响 |
|---|---|---|
| account-list PAGE | 失败 tuple 仍读到旧 cost=10 | 账户列表与数据透视产生矛盾 |
| account-list COUNT | metrics_complete=true | 旧 computed_at 非空即可冒充完整 |
| task-list PAGE | spent=10，仍计入失败数据 | 任务消耗/达成进度可能基于旧批次 |
| initial full readiness | 唯一 full run 含失败事实仍 true | 部分成功首次同步被当完整就绪 |

共享 `etlBatchReadableSql` 对同一失败 tuple 返回 false；跨 workspace、跨 media 同号都 true；同资源后续 Raw 补采后旧 canonical 仍 false；computed_at 晚于新 Raw 才 true。八项布尔断言+回滚验证全部满足。脚本成功仅代表**稳定复现缺陷及共享 helper 对照成立**，不能把这些缺陷计作测试通过的产品功能。

## 精确位置与建议派修

- `packages/db/src/account-list-sql.ts:50` metrics_complete 子查询、`:158` daily LEFT JOIN 未接 `etlBatchReadableSql("metric")`。LEFT JOIN 应加 ON，不能在外 WHERE 把账户行消掉。
- `packages/db/src/task-list-sql.ts:193` aggregate 未接失败屏蔽；修时保留 expected 账户/日期完整性，不能只过滤失败行后把剩余的 sum 伪装完整。调用方的 coverage/dataState 要共同验证。
- `packages/db/src/workspace-sync-readiness.ts:22` 只检查有同用户同媒体 done full，不核 batchFailures。首次唯一 full partial 不能直接就绪。请 arch 明确“full 部分失败，后来 incr 同资源补采并 canonical 重算”是否可完成首次 readiness，或必须后续无失败 full；本人可接 helper 修复但不替契约选一种。
- scope/失败时间/历史数据隔离延续 P176；不改状态枚举，不删除旧 Raw/canonical，不清 warning 假恢复。余额/开户基建 readiness 是另一个业务概念，不混为拉数完整性。

## 检查与边界

- 真实诊断命令：`TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test node --import tsx scripts/probe-list-batch-readability.ts`（cwd packages/db），exit0，rolledBack=true。
- `vitest run test/etl-batch-readability.test.ts test/workspace-sync-readiness.unit.test.ts`：**15/15**（10 PG +5 unit）；后者既有测试未覆盖 partial full，所以绿不证明缺口不存在。
- 新脚本独立 strict NodeNext tsc、eslint、git diff --check 通过；DB offline production audit0，仅缓存证据。
- 安全：随机合成 ID、显式本机隔离库、参数化 SQL、10s statement/3s lock timeout、finally rollback；不输出私有行/凭证，不起队列、不接真实源。
- 磁盘实测3.8GiB，小于8GiB，不跑全包/构建，不清用户缓存。无正式代码改动，无前端改动，无 migration，无 push/部署。
- 下一步：等待 be2 读取接线与 arch 首次同步恢复口径后，才启用 P176 Runtime 容错；其余待裁迁移014/021与F-OS-004仍保留，不称全部完成。
