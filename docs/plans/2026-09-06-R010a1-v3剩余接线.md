# R010a1 v3 Remaining Wiring Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 arch main@b8d2b0b v1.7.4 完成 v3 的价格来源、真实可判定账户达标率、窗口比较、两 Adapter 与公开 HTTP/BFF 同步切换；不以内部计算完成替代公开能力。

**Architecture:** 共用 Query Registry 与三态 MetricSet。个人在同一RR/RO快照读现金、历史价格、达标账户计数；团队使用受控 reader 的逐日 cash_assessment。公开响应通过同一严格v3边界，来源只能由服务端session决定，缺数与截断不得出完整金额。

**Tech Stack:** TypeScript、Zod、PostgreSQL、SQLite测试、Vitest、现有非视觉BFF。

当前 executing-plans 子技能未提供，按本计划直接逐步TDD执行；老板已授权连续推进，不另外创建会话或委派子agent。

### 1. 可判定账户达标率（本次先行）

- 修改 `packages/db/src/window-assessment-repository.ts`：新增bounded SQL聚合查询账户级窗口Σ现金与Σ每日价格×真实转化，返回total/determinable/onTarget三个整数；沿用expected account-day与三键scope。无grant总数0；部分账户日缺价格/现金/转化不进可判定分母；NaN/Infinity拒绝，不把损坏当正常。
- 测试 `packages/db/test/window-assessment-counts.test.ts`（mock SQL contract）与 `packages/db/test/window-assessment-repository.test.ts`（真实PG）：分母是可判定账户，不是账户日/行；跨媒体同号与跨workspace、不带grant、零真实值、缺日、有效价变更。
- 修改 `apps/worker/src/data/platform-window-query.ts`：同快照查询本窗/上一等长窗口counts，确定count不得超过批准scope；`onTargetRate = onTarget / determinable`，零分母undefined。today仍无同时段快照，全部compare undefined，不查昨日全天。输出warnings包含BUDGET_SOURCE_NOT_READY，不造预算。
- TDD：先写缺数分母/比较窗测试，`npm --prefix apps/worker test -- --run test/platform-window-query.test.ts`应红；实现后定向与全量过，真实PG不可用则仅标pending，不删测试。

### 2. 价格来源与团队逐日计算

- 修改 `packages/domain/src/summary-window.ts`、`window-assessment.ts` 与测试：priceSource history/ka_daily严格；null effectiveDate仅ka_daily；团队多值按不同值数而非天数计priceVersions，warning ASSESSMENT_VERSION_UNKNOWN。个人仍真实版本ID。
- 修改 `apps/worker/src/data/query-registry.ts`、`ka-data-client.ts`：注册受控内部窗口查询，不开放raw SQL；逐日Σ现金/真实转化/日价不能拿最后一天代替全窗；限制10k/16MB和已知硬截断。对query输出的workspace始终由可信context注入。
- arch fixtures还缺priceSource，P068请arch更新，禁止be编辑Contract。写失败样例验证，再实现、运行Domain/两Adapter parity/SQLite实际查询测试。

### 3. 公开v3一次切换

- `packages/domain/src/data-query-rows.ts`、`data-query-contract.ts`、`apps/worker/src/data/{query-registry,canonical-query-rows,platform-data-source,ka-data-client,query-service}.ts`：summary=v3、trend=flat metrics，保留lineage/window/authority/coverage/requestId；无第二套Query ID。
- `apps/web/lib/data/{contracts,adapters,types,bff}.ts`及现有测试：只改非视觉解包与严格响应，不兼容伪造v2、不信浏览器dataView，不改React/样式。
- 现有HTTP测试补personal/team、date_from/to、范围冲突、坏日期、空源、截断、unknown lineage、失效session；团队priceSource不能漂移到个人。

### 4. 余下query能力与系统只读

按原R010a1继续trend/dimension/pivot2、account detail、systemhealth/etl-runs；只引用已冻结DTO，缺枚举由arch裁。完成按信箱全项对账，不能把本计划1–3当作整个R010完成。

### 5. 门禁和交接

每子批独立代码SHA与留痕；Domain/DB/Worker test+typecheck+lint、相关Web非视觉测试、coverage核心>80%。真PG必须指定本任务合成库，串行运行；Docker故障原样记录。任何新外部依赖问arch但继续独立事项。无push、媒体真写、真实凭证或上线声明。
