# 自查-20260910-01：F-OS-004 Task3 HTTP/登录交审

代码 **ec221ecc**，18文件383+/45-。这是开户Task3，不是arch新派的P-211看板API。候选实现、本机合成数据验证；未arch接受/合流/部署/push，未使用真实凭证或媒体写。

## 实现与边界

- 实际data-api装配全局成员仓储，GET成员、POST开户201、POST reset-password200；局部grants只读接口保留旧范围，未开放授权修改。
- 可信Session定位当前身份，DB复查任一active team admin；当前personal角色不是admin也可被治理授权，单纯自报role/admin或普通成员不能越权。viewer中央403保留。
- 开户密码只在创建响应回一次，reset生成新密码并原子撤销全部session；响应no-store，固定错误、不输出SQL/hash/凭证配置。
- 数据库用户名登录不再依赖ENV成员清单；active internal_test查找，已存密码优先且不回落旧ENV，功能关闭仍拒绝。异步KDF复用be2实现。
- 新开户输入与现有Session一致：名字200、密码512上限，超长拒绝。P209用存储1024/名字256做边界不能证明登录可用，本批纠正并实测上下界。旧自助改密1024不在本批改，交arch协调be2统一路由与Session边界。
- 保留requestId、严格请求/响应、405、请求容量、响应exact上限fail-closed；新增边界用较小配置复现exact，不宣称新分配了16MiB测试载荷。移除已落地reset-password的PENDING，BFF扫描3/3绿。

## 本机证据（合成隔离PG：ka_be_r010_p194_test）

| 检查 | 实跑结果 |
|---|---|
| Domain command + old members | 57/57，typecheck/lint通过 |
| DB provisioning PG + unit + must-change-password | 39/39，typecheck/lint通过 |
| DB新增用户名lookup边界 | 11/11，typecheck/lint通过 |
| Worker command HTTP / PG生命周期 / old HTTP / old PG / provider / password routes / BFF inventory / viewer inventory | 8文件134/134，typecheck/lint通过 |
| 实际data-api子进程启动 + Session HTTP | 2文件14/14；KA off启动health200/session401，新建/reset无Session401，不启动consumer |
| Worker production offline audit | 0已知漏洞（缓存审计，非在线全库保证） |
| changed service/route/provider coverage | 行98.73%、分支93.52%、函数100%；service100/86.79、route100/96.92、provider97.01/96.15 |
| 安全静态与提交范围 | diff check通过，新生产路径无console/scryptSync/明文ENV写；apps/web与视觉零改动 |

真实PG+HTTP生命周期：治理管理员开户→无ENV新用户名登录→本人改密→旧密码401/其它旧会话401→重置→所有旧会话401→新密码登录。真实无权限身份403/治理membership撤销403/伪造header无效；名字200与密码512可实际登录。共享测试库未清、仅清本suite随机合成对象。

### 未掩盖的失败与限制

- 首轮新HTTP测试14红（旧global gate403/reset404），接线后绿；PG第一轮login漏provider是测试输入错误，已补，不算生产红绿证据。
- 一次组合coverage跑在自助改密出现500，隔离重跑、带诊断组合重跑、最终八文件标准组合均未复现；**根因未证明**，保留为arch复验观察项，不声称已定位/修复该偶发。
- 磁盘3.6GiB，依arch低于8GiB规则未跑五包全量/build；上述全为定向门禁，不称全门禁。真实OS用户名/内网部署/浏览器BFF点选未验证。
- production启动smoke只证明真实入口和无Session边界；完整登录生命周期用相同HTTP factory+真实repositories，但非部署现场浏览器。

## 后续顺序

已读main cb9e368c v1.9.23：立即转P176 Task2/3→readiness→025→arch P211看板API；本Task3提交仅保存已完成现场，未抢新的开户范围。024与sop-run后置。P-2xx由arch派，本人后续自查按日期编号。
