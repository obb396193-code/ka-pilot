# 两个后端的长期目标队列（arch 2026-09-07；老板：挂个目标让他们一直做下去，不要停下来等）

> **契约已无未定义功能**（缺口地图「待契约补 = 0」，v1.8 是最新）。所以下面每一批都能直接开工，**不需要再问 arch 要契约**。
> 规矩不变：一批一交审、交审后停手等 ✅、不 push、路径限定提交。**但"等审"不等于"停工"——交审后立刻开下一批**，我审出问题回头修即可。

## 一条铁律（这次新加）

**任何一批做不下去时，不要停下来等，按下面的规则自己往下走：**
1. 缺契约 → 不可能（契约已冻全）。真遇到就写信箱，**同时跳过这一条继续本批其余部分**。
2. 缺别人的东西（缝、别人的文件、别人的表）→ 写信箱，**跳过该条，继续本批其余**，不要整批停。
3. 缺外部（PG 挂了、OS 没给东西）→ 换成不依赖它的批次往下做。
4. 一批全被挡 → **直接开队列里的下一批**，被挡的那批记进状态文件「阻塞」。

## be（Codex）队列 —— 按顺序做，做完一批直接开下一批

| # | 批 | 内容 | 迁移 |
|---|---|---|---|
| 1 | **R-010a1 收口** | 剩余维度（agent_type / deduction_range / 资源位团队源 / hourly / gap 进 Registry）、pivot2、system/health、system/etl-runs、两 Adapter 全切 v3、删 v2 边界 | — |
| 2 | **R-010a2 收口** | work_items 四动作 HTTP、dispatched/superseded_by（013 列）、changesets 全流程 HTTP（preview/confirm/execute/retry/rollback）、rollback 三表、静音 HTTP、规则解释器 + explain 端点、SLA 暂停 | 013 的列 |
| 3 | **R-011** | 团队 KA 快照方案 A：五表 + `account_metrics_daily` 三列、staging→publish 事务、TeamSnapshotDataSource、team readiness、`sourceBatch`/`stability` | **013** |
| 4 | **R-012** | 警报流/协作/六页签/漏斗/时间线/8 维（bid_tool 六 raw 列）/task_budget_history/卡片/订阅 | **014** |
| 5 | **R-015** | 素材域（拆片链路：下载→抽帧→ASR→LLM 结构化→相似→brief→谱系）、结算、治理后台其余 | **016** |
| 6 | **R-010b** | 工作流/规则/Agent 剩余 HTTP、`GET /workflows/runs` 加 taskId、原子能力 | — |

## be2（Claude Code）队列

| # | 批 | 内容 | 迁移 |
|---|---|---|---|
| 1 | **R-014**（在做） | S1 迁移 015 ✅ → S2 独占 11 表仓储 + DTO → S3 只读聚合 → S4 HTTP（约 30 条）→ S5 BFF → S6 交界字段（accounts/tasks 列表六文件已临时移交你） | **015** |
| 2 | **R-017** | 账户昵称解析 + 归属清洗页后端（两端锚定解析、按 media 版本化规范、冲突不静默、override 永久优先、六端点、十个维度切来源） | **018** |
| 3 | **R-016** | 策略方案对象 / 归因树 / 竞情 / Shadow / AI 提效 / 周报 / 任务复盘 / 知悉流 / 月推 | **017** |

## arch 同时在做

- **联调**（本地先跑通：建库 → migrate → seed → 起 data-api → 起 web 打真接口），跑通的部分逐页验收；不等你们全做完。
- 共享结构开缝（`extraRoutes` 入参 + handler spread + 空的 `r014/{routes,handlers}.ts`）——开完通知 be2，**be2 在此之前先做不需要缝的 S2/S3**。
- fixture 补齐、契约裁决、合流与门禁。

## 交审节奏（不变）

一批交审 → 写回执（be 用 P-xxx，be2 用 Q-xxx）→ **立刻开下一批**，不等我的 ✅。我审出问题写信箱，你回头修，不影响你正在做的批。
