# P189 登录来源 IP 接线质量回执

代码 `2724de9c`，基线 main@5682a509 + P188测试修复；implemented + 本机 HTTP verified，待 arch 合流；不是访客功能已上线。

## 结果

- 源码实际路径为 `apps/worker/src/data/http-server.ts`（非 src/http-server.ts）。内部 bearer 验证后的 login 分支，把 x-forwarded-for 首段 trim、合法 IP 才采用，否则 socket.remoteAddress，作为 login 第三参。
- 不读取 body 自报 IP、不记录 IP，不改 be2 SessionHttpService/限速器；更换尾部代理不产生新桶；无 bearer/错 bearer/GET 不进入 login。
- 新 `session-client-ip-http.test.ts` 用真实 HTTP 壳和真实 GuestLimiter，synthetic issueSession 返回 false，仅计数是否穿过限速，不伪造可用 guest 会话。修复前 9红1绿（第二 IP 被错误全局桶挡下、第三参缺失），修复后10/10。
- 五文件回归 **123/123**：来源IP10 + data-api-http57 + etl-runs-http36 + session-http11 + session-auth-service9。保留 exact16MiB、requestId、旧路由/session/登录失败同形等已有断言。
- Worker typecheck/lint、git diff check；cached production audit0。仅改7行HTTP壳（6增1删）+新增测试；未改数据库/迁移/依赖/前端。没有真实PG新增逻辑，此批不声称PG新证明；P188之前的49项是另一批证据。

## code-quality-checker 与外部缺口

1. **前端/部署仍缺一跳**：实读 `apps/web/lib/data/session-bff.ts:132-134`，目前只调用 internalApiHeaders(config/requestId/session/json)，没有传来源 IP。本批只完成后端接收；实际浏览器经这个 BFF 仍会落在 BFF 的 socket 地址，不能宣传“生产按用户IP已经限流”。已抛给 arch/fe，不越权改前端。
2. **信任前提**：反向代理/BFF 必须覆盖外来 XFF；具有 internal bearer 的调用方属于受信服务。不能直接把客户端伪造首段转进后端。负向测试证明无有效 bearer 不执行 login，但不代替部署代理安全核验。
3. be2 现有 GuestLimiter 仍为单进程 Map，非多实例共享限速；本批没有改其窗口/容量/IPv6地址文本归一化策略。若部署多副本需另批集中限流，不宣称跨副本准确。
4. 磁盘<8GiB，按门规仅定向与静态，未跑五包全量。

## 交接

F-P179-Q 询问 rerun CONFLICT jobId 严格形状及 timeline 落点，未发明公共 DTO。P190 BFF覆盖绊线、P191 viewer统一写边界已收到，依序推进。生图按用户取消，旧图片保留。未 push/部署/运行真实队列。
