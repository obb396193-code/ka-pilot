# P166 软撤权全入口接线 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 让 018 的 revoked_at 在本人负责的 Session、同步候选、静音及成员权限显示中真正生效，避免已交接账户仍被原授权使用。

**Architecture:** 复用现有 SQL 查询，在聚合/锁前过滤 `(to_jsonb(grant_alias)->>'revoked_at') IS NULL`。兼容既有迁移窗口测试（018 前无列即未撤销），018 后非空一律撤权，不用未来时间恢复授权。team 保持不解析 grants；不改 Contract、be2 或媒体执行。

**Tech Stack:** TypeScript、现有 pg/Auth/WorkspaceSync、Vitest，隔离本机 PostgreSQL。

---

未安装 executing-plans 技能；按用户持续授权本会话执行 TDD，不派子 agent。

## Task 1：真实 PG 先红

新增 `packages/db/test/grant-revocation-pg.test.ts`，仅显式本机 55432 ka_*_test，随机 workspace/identity，清理仅本例对象。

- 同号跨 media：撤 KUAISHOU 后个人 Session 只剩 TENCENT，全部撤后仍可登录但 scope=[]。
- team 不受个人撤权干扰，保持 team_workspace_readonly。
- 1001 已撤销历史行不占 active grant 的 1000 限额。
- 同步候选不带撤销户，不能从历史 grant 恢复范围。
- 管理成员 active grantsCount 与权限明细一致。
- 已批准旧上下文不能绕过静音 live authority；拒绝后原静音行不改变。

Run：`TEST_DATABASE_URL=<专用测试库> npx --no-install vitest run test/grant-revocation-pg.test.ts --maxWorkers=1`，预期 Session/同步/成员/静音撤权反例红。

## Task 2：最小 SQL 修复

修改 `packages/db/src/{auth-repository,workspace-sync-repository,account-mute-repository,admin-members-repository}.ts`。所有授权过滤在 JOIN/WHERE 中、聚合前；参数化、tuple、锁及事务不改。admin grantsCount 只计当前有效授权，不把历史审计行当可用范围。

`bootstrap-seed-repository` 的幂等插入 DO NOTHING 不恢复撤销标志，本批不改变重新授权语义。它的历史行限额另交 arch；be2 r014 的多处 live-grant 查询交 arch 派修，不能说全仓撤权已收口。

## Task 3：回归/交审

跑新 PG 与既有 auth、sync、mute、admin unit/HTTP 回归，以及 DB/Worker typecheck/lint。磁盘不足8GiB不跑全量，不清理其他数据。新代码独立 SHA，P166 回执列本人已关与 be2 未关入口、真实红绿结果及部署边界。不得称整个交接功能安全验收完成。
