# 数据运行时批次隔离 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在自查06日期就绪接齐后，将P176已验证批次容错装入正式Worker Runtime，验证取数→真实队列→PG→公开数据API。

**Architecture:** Runtime只给full/incr注入EtlBatchFailureRepository；记录成功后跳过失败批，成功批继续，失败格在所有下游被屏蔽。账户发现、身份、越权行、数据库/lease/记录错误仍fail-stop；历史回填与媒体写不变。

**Tech Stack:** TypeScript、Vitest、fake-fetch、隔离PostgreSQL、临时本机HTTP端口。

---

执行技能未提供，按老板持续实施授权逐项TDD。老板本轮明确数据分析/看板/相关接口全部优先，开户、dispatches、sop-run后置；只向arch回报，不改公共台账。

1. 新 `apps/worker/test/etl-partial-runtime-pg.integration.test.ts`：真实consumer分别跑full/incr，一批50失败、1成功；验证失败ledger、canonical屏蔽、readiness false；补同scope成功incr→canonical→readiness true→HTTP数据查询覆盖完整。账户发现失败仍保留job重试；不运行常驻worker、不接真实credential。
2. 跑上述红测；最小改 `apps/worker/src/runtime.ts`，注入同一个failure repository给full/incr；更新partial-query的历史注释。
3. 回归partial、protocol、identity、fencing和HTTP；DB/Worker类型/lint，缓存audit，保留真实失败；独立代码SHA和质量回执。
4. 下一批P211：优化师/目标/版位、多值filters、三BI指标、级联filters接口。旧window与鉴权/截断继续复用，不能引入第二套source优先级。新fixtures从本机合成PG/SQLite真实响应生成，标明不是OS生产数据。
