# 规则现金口径 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按metrics.md:48-51/85与老板现金考核裁决，超成本规则不再把realCpa与现金考核价混比。

**Architecture:** OverCostRampInput只接受cashCost/realConversion作为成本事实，Domain复用safeDivide计算cashCpa。未知现金绝不回退账面；阈值数值3000、1.2、冷启动1.5及10样本不改，minimum spend用现金口径并在trace明确。

**Tech Stack:** 现有TypeScript Domain/Worker、Vitest，无新依赖/HTTP/迁移。

---

主agent沿冻结规格TDD，executing-plans不可用，不派子agent。

## Task 1：先红

- 新`packages/domain/test/alert-rules-cash.test.ts`：账面高现金低不触发、账面低现金高仍触发（旧属性仅反例）、旧输入无现金不fallback、缺现金/转化、零转化、冷启动9/10、有限边界/溢出、3000现金边界。
- 运行`npm --prefix packages/domain test -- --run alert-rules-cash.test.ts`，预期旧实现失败。

## Task 2：最小接线

- `packages/domain/src/alert-rules.ts`移除over-cost旧realCpa/cost入参，改cashCost；safeDivide复用，负/缺转化不可判；计算溢出不可判；trace明确现金成本/现金消耗。zero_delivery/spend_cliff不改业务定义。
- `packages/domain/test/alert-rules.test.ts`原6类超成本用例改为自洽cashCost/volume，不保留互相矛盾的独立CPA。
- `apps/worker/test/rule-scan-handler.test.ts`候选显式cashCost及其引用指标；`data-pipeline-pg.integration.test.ts`从已有canonical cashCost读取，不本地硬编码折算系数/同名覆盖realCpa。

## Task 3：验证/留痕

- Domain/Worker全量、DB纯逻辑、三包type/lint、核心覆盖>80%、offlineaudit；隔离PG尝试，不可连仍标blocked。
- P095+R010状态+本计划质量表，代码/文档分SHA；不改arch Contract/前端/真实媒体写。正式provider、SLA、rule explain仍待，不称规则产品已部署。

## 交付与质量报告（2026-09-06 17:17）

代码`f3ee185`，5文件；candidate/claude_review_pending，未merged/deployed/push。writing-plans先写实施计划，code-quality-checker完成类型/范围/覆盖/依赖/性能检查。

- 规则只收cashCost+realConversion，safeDivide计算现金CPA，删除旧realCpa/cost候选字段。旧JS调用只传账面字段时insufficient，不静默fallback。现金与转化缺失/非法、负转化、比率溢出、考核阈值溢出都不能触发。
- 保留1.2/冷启动1.5与少于10样本护栏，正现金/零转化仍infinite；0/0不可判。起量floor3000按metrics.md“账面cost只展示”落为现金消耗；trace明确“现金”，数值未改。这个口径变化交arch明示复核，不再沿用旧PRD展示“真实CPA”的历史文案。
- PG原样例账面5000/系数2=现金2500，故原“触发1次”期待改为不命中/不生成/不合并/不推送；未修改金额或系数来掩盖口径变化。新增明确cashCost2500断言，source query使用approved media/account tuple。PG实际不可连，数学与pure测试通过不等于DB管线通过。

| 门禁 | 本轮结果 | 日志 |
|---|---|---|
| TDD红 | 现金/旧账面混用等10红 | `/tmp/ka-rule-cash-red.log` |
| 新现金反例 | 16过；连原规则和readiness共45过 | `/tmp/ka-rule-cash-focused.log` |
| 核心覆盖 | 两套28过；alert-rules97.14%行/95.06%分支 | `/tmp/ka-rule-cash-coverage.log` |
| Domain全量 | 710过/10旧P073fixture失败，未动契约 | `/tmp/ka-rule-cash-domain.log` |
| Worker非PG全量 | 1099过+2外部opt-in skip | `/tmp/ka-rule-cash-worker.log` |
| DB纯逻辑全量 | 370过 | `/tmp/ka-rule-cash-db.log` |
| 三包typecheck/lint | 通过 | 终端原始输出 |
| Domain离线生产audit | 0，缓存非在线最新审计 | 终端原始输出 |
| 真实PG综合管线 | 55432拒连，1例未执行 | `/tmp/ka-rule-cash-pg.log` |
| diff/路径 | diff --check通过，Contract/前端/依赖0改动 | `git show --stat f3ee185` |

命令：Domain `npm --prefix packages/domain test -- --run`；Worker `npm --prefix apps/worker test -- --run --exclude '**/*pg*'`；DB `npm --prefix packages/db test -- --run unit.test.ts window-assessment-counts.test.ts window-assessment-boundary.test.ts semantic-dimension-boundary.test.ts`。三包`run typecheck`、`run lint`。覆盖`npm --prefix packages/domain test -- --run alert-rules-cash.test.ts alert-rules.test.ts --coverage --coverage.include=src/alert-rules.ts`。PG显式`TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r013_20260906 PGCONNECT_TIMEOUT=3 npm --prefix apps/worker test -- --run data-pipeline-pg.integration.test.ts --hookTimeout=10000`。

风险/剩余：函数固定次数计算，无新增网络/SQL/依赖；沿用number金融表示，精度合同未扩大。仅日粒度over_cost_ramp修正，窗口多考核价不能由caller用末日价冒充，未来provider必须用冻结逐日公式。正式condition_tree→源/版本读取、SLA持久化、public explain仍待，P093/P092契约与PG阻断未解决。未发现本批新增Critical/High；不能因此把全部R010a2标为完成。
