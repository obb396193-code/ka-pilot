# P-192 未开放入口 Implementation Plan

> **For Codex:** 使用 writing-plans 小步 TDD；executing-plans 当前不可用，沿本会话按步骤实施。

**Goal:** 按 arch v1.9.44 为七条新路径提供可信登录后的 501，回归已有 review，共八条；不实现实际写动作。

**Architecture:** R010 无业务依赖路由，经现有 Data API session/viewer 防线注册；对应 Next BFF 复用既有 forwarder 的服务端凭证、requestId、响应大小与错误校验。P199 的 test-send/on-call 不纳入。

**Tech Stack:** TypeScript、Vitest、Node test、Next BFF。

## Task 1：HTTP 红绿

- 新增 `apps/worker/test/deferred-actions-http.test.ts`：七条新路径与已有 review 501；未登录401、错误 bearer403、viewer403、非冻结 method405、额外 query400；业务 service spy 必须不调用。
- 新增 `apps/worker/src/r010/deferred-action-routes.ts`：固定白名单 method/path，无 Repository、Agent、网络依赖；错误形复用 R010。
- 修改 `apps/worker/src/r010/http.ts` 增 NOT_IMPLEMENTED 错误；`src/data/http-server.ts` 只追加注册/派发 hunk。
- 先 `npm test -- --run test/deferred-actions-http.test.ts` 得到缺路由红，再最小实现转绿。

## Task 2：BFF 红绿

- 新增 `apps/web/lib/data/deferred-actions-bff.ts` 与测试、server-only re-export，以及七个 `app/api/internal/**/route.ts`。
- 明确固定方法和 encodeURIComponent 路径参数；复用 r014/forwarder，不透传浏览器 scope；501 原样回前端。
- 测七路路径/方法/501与requestId；未登录不 fetch、伪造2xx/204拒绝、无秘密错误、错误相关ID拒绝。
- 跑 `node --test lib/data/deferred-actions-bff.test.ts` + Worker 双向路由覆盖。

## Task 3：验证与交审

- Domain/DB/Worker 全量 test、typecheck、lint；DB 只使用 `ka_be_*_test` 合成库，禁止消费真实队列；Web full tests/typecheck/lint。
- 无新迁移/DB查询，真实PG由原有全量覆盖，不把HTTP mock称为真实登录联调。
- code-quality-checker 检查写前鉴权、无业务调用、方法边界、路径编码、稳定错误、无依赖升级和视觉变更。
- 交审前 merge main；路径限定提交，不push；inbox-arch 写 exact SHA、测试实际结果、P198故障窗口尚待裁决。

## 复工说明

- arch `94e9dc25` 已回复“直接往下做”。信箱冲突为两侧纯追加，保留双方全文且按原顺序的脚本校验通过；合并提交 c87fae03 / d8dfb95d。主线带入的 be2/fe 文件不是本批改写。
- 旧 P194 实现按写入时间取旧值尚待改成 v1.9.44 生效版本口径，本批不夹带修改。
