# R010a2 忽略与静音原子事务 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 当前无该子技能；按老板连续执行指令，在本独立worktree本地TDD，不另开Coze或子会话。

**Goal:** 落实已冻P096天数/03:00截止，以及工作项ignore与account_mutes同事务，不能半成功。
**Architecture:** Domain负责严格输入和日期换算；既有AccountMuteRepository增加同事务组合，复用活动授权锁与mute UPSERT；不触媒体、队列或共享HTTP结构。服务端入口后续接此原子操作，不能调用两个各自提交的方法组合。
**Tech Stack:** TypeScript/Zod/PostgreSQL/Vitest。

### Task 1 日期与输入

- 新建 `packages/domain/src/account-mute-policy.ts` 与 `test/account-mute-policy.test.ts`。
- 1/3/7唯一合法天数；复用shanghaiTaskBusinessDate，再加天数得到DATE存储；响应转成该日03:00+08:00瞬时。覆盖02:59:59/03:00、跨年、闰日、非法日期/天数，不取UTC自然日。
- muted_until是DATE（012），不改表或假存时间；抑制P0例外与含尾截止由此策略表达，真正RuleScan接线不冒充完成。
- 先跑测试缺module RED，再实现、定向PASS。

### Task 2 原子存储

- 修改 `packages/db/src/account-mute-repository.ts`；新建 `test/account-mute-ignore.unit.test.ts`，扩展既有 `test/account-mute-repository.test.ts`。
- 新ignoreAndMute只接workItemId/date/reason，不接用户自报账户；锁workspace内工作项，从真实行取tuple，复核personal批准grant与活动identity/member/user/account。双null无账户项不可静音。
- 复用assertWorkItemTransition，不擅自扩状态机。UPDATE工作项ignored/ignore_reason/resolved_at，然后同连接UPSERT账户静音；任一步失败ROLLBACK。废弃work_items.muted_until仍不写。
- 独立合成PG反例：跨媒体/跨workspace、失效授权、非法状态、同时调用、强制静音失败后忽略也回滚。只用显式55432隔离测试库，不改共享库。
- 模拟数据库篡改tuple/status/rowCount也拒绝；错误只带控制码，不带原始SQL/数据。

### Task 3 验证与交审

- 新建 `apps/worker/src/work-items/account-mute-service.ts` 与对应test：服务端clock把days转DATE/03:00响应；静音和ignore+mute分别调用正确store接口，后者不能拆两次提交；严格auth/输入/输出与安全错误，不用Promise.race令写入在超时返回后继续。

- Domain新测试、DB单测/真实PG；相关全量与三包type/lint按磁盘门禁运行，不能引用旧全量冒充本批。
- `git diff --check`，扫描变更安全/复杂度；代码/留痕独立路径限定SHA，P131通知arch。
- 未完成：HTTP/BFF接缝、真实规则抑制+occurrence、SLA及ignore无mute入口，不用基础建设冒充每天能处理全部完成。013类型冲突仍由arch裁，不堵本条012基础操作。
