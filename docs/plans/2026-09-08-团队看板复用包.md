# 团队看板复用包 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 本次为用户批准的本地整理预览；该子技能当前未提供，按下述显式步骤自行执行，不派生任务、不提交、不上传。

**Goal:** 给同团队腾讯数据看板开发提供已有页面、组件、使用约束和脱敏取数知识，不影响现有开发。

**Architecture:** 从固定 Git SHA 读取白名单文件，复制到全新的 output/team-dashboard-kit-2026-09-08-v1。运行母版与依赖项目上下文的参考组件分开。仅在副本生成构建与压缩包，不操作当前应用、服务、依赖或分支。

**Tech Stack:** 已有 React/Vite/Tailwind 页面；Node.js 机械导出；Markdown 交接文件。

## 范围与执行步骤

1. 只读核验 HEAD/status、现有母版、Claude 组件、标准及取数证据；固定源版本 c3451db3c7a846a7b455dd6acd654b377d250623。
2. 在独立 output 目录创建 README、Agent 约束、接入步骤、腾讯取数手册、验收单、机械导出脚本。禁止覆盖旧导出包。
3. 脚本仅复制 Git 白名单；母版只保留 sidebar 入口。头像使用原组件现成 fallback，不带本地参考人物资产；其他视觉不变。
4. 导出既有可复用组件和既有标准，标记引用快照的缺失依赖；不得声称整个生产前端已独立运行。
5. 在副本复用已安装依赖执行 typecheck、母版测试、build；哈希核对只允许已登记的副本适配。验证 ZIP 无 node_modules、凭证、真实业务缓存及头像资产。
6. 回报文件清单、限制与预览入口。仅交付本地候选，不 push、不合并、不重启原服务。

## 验收与留痕

- 工作区初始 clean；本批唯一已有文件追加目标为工作台账，应用与 Contract 零修改。
- output 包内维护 manifest.json、VERIFICATION.md、MAINTENANCE.md 和引用路径；静态预览数值全部为原有演示数据。
- 数据证据为仓库历史实证，不代表本次登录腾讯后台或实跑取数。冲突、未证实与待同事验证项明列。
- 运行结果以包内 VERIFICATION.md 为准；此计划不作为已完成证明。
