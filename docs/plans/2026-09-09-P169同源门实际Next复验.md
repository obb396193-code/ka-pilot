# P169：F-P157-1 已修代码与实际 Next HTTP 复验

日期2026-09-09。本人 `be/r010` 已 fast-forward 到 arch main **a192c28**，包含P166/P167/P168合流、D7与be2软撤权修复。没有重开生图、没有覆盖其他工作树。

## 为什么本次不重新改 BFF

最新信箱仍称 F-P157-1 最急，但源码已在 **86ebd17（P161）** 修复，并已随 P160–P165 合main。
`apps/web/lib/data/r010-command-bff.ts:89–90` 先判断 Sec-Fetch-Site，仅same-origin通过；缺该头才比较Origin与内部URL；不信任forwarded host。此次 main 与本人对应文件一致。

## 真实 HTTP 探针

没有新建/重启服务。只读 `lsof` 确认既有3411端口PID84601，cwd=`/Users/aik/ka-arch-integ/apps/web`；该工作树HEAD **3dfd867**，磁盘`.next/BUILD_ID`为`JlrQ6iY8OrR6VUte7_Tw7`。后两项是现场元数据，不能单独证明内存bundle对应哪个SHA；以下网络行为为直接证据。

向 `http://127.0.0.1:3411/api/internal/changesets/00000000-0000-4000-8000-000000000001/dry-run` 发6次POST，Content-Type=application/json、body={}，**全部不带Cookie/真实凭证**。因此BFF在同源检查或Session缺失处终止，不发业务后端请求、不调用媒体、不改对象。

| 头组合 | 实际结果 | 判断 |
|---|---|---|
| Origin=https://synthetic-proxy.example + Sec-Fetch-Site=same-origin | 401 UNAUTHORIZED | 通过同源门，进入正常Session缺失判断；不是旧403 |
| 同上Origin + cross-site | 403 FORBIDDEN | 跨站仍拒绝 |
| 同上Origin + same-site | 403 FORBIDDEN | 同站不等于同源 |
| 同上Origin，无Fetch Metadata | 403 FORBIDDEN | legacy须严格匹配 |
| Origin=http://localhost:3411，无Fetch Metadata | 401 UNAUTHORIZED | legacy匹配进入正常鉴权 |
| 公网Origin + x-forwarded-host=synthetic-proxy.example + x-forwarded-proto=https，无Fetch Metadata | 403 FORBIDDEN | 不信转发头扩大信任 |

六响应body.error.requestId均与x-request-id一致。此为真实本机Next HTTP + 手工构造浏览器头，**不是浏览器公网port-mapping端到端或已登录dry-run验证**。这些请求不带session，不能用401证明媒体source-off成功。

## 定向回归

`apps/web`：`node --experimental-strip-types --test lib/data/r010-command-bff.test.ts`，**19/19通过**，覆盖三命令转发、代理URL、same-site拒绝、typed三值、exact16MiB与requestId；mock源不是真实媒体。
Node22产生既有MODULE_TYPELESS_PACKAGE_JSON警告；没有为消除警告改前端package。未运行新全量/build/typecheck，因为本批没有生产代码变化；不引用旧门禁冒充这轮全量。

按code-quality-checker复核：本次仅留痕，未引入依赖/SQL/认证变更；无新增覆盖要求。没有push、媒体写、UI或候选图片改动。

## 剩余范围

- 请arch把“F-P157-1待修”纠正为“代码已合、本机Next同源门已验；公网已登录链路部署侧再验”。
- F-P153-1/2仍待P163的账户hh采样/落库证据与Gap规则集版本选择；本次v1.9.2没有改变这两项。不能把D7日报新DTO当作该源证据。
- 013 BIGSERIAL/UUID冲突、014归属/raw列、详情decision/ETL未知态、工作流taskId/SSE样例分歧仍按P160/P164请求处理。
- D5权限已由arch派be2 Q020；日报和KB/密码Q021属于be2当前分工，不因新信箱内容重复开线。任务详情的真实漏洞修复尚未在本轮验收。
- 主线已合P166–P168是新事实；本地/内网部署证据仍分别表达，不用“已合流”冒充“全部目标完成”。
