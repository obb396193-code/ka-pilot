# P171 奇航协议重试 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 先修F-OS-001的协议抖动重试和可安全定位的错误，不把凭证失败当暂时抖动。

**Architecture:** Client只增加typed protocol error并走原maxRetries指数退避；diagnostic只选白名单参数、批次计数及指纹，响应按类型/字节数/摘要记录，不原样记录任意HTML/JSON或错误cause。单批失败继续执行与缺源覆盖标识另子批，不能让finishRun成功掩盖partial。

**Tech Stack:** TypeScript、Node crypto、fetch、Vitest。

---

## Task 1：TDD修协议重试

- New `apps/worker/test/qihang-protocol-retry.test.ts`：200非JSON一次→成功、坏信封一次→成功、4次失败有resource/ds/批次定位、401/403/业务失败/超限不重试、诊断不得含userId/URL/token/原body。
- Modify `apps/worker/src/qihang/client.ts`、`errors.ts`；New `qihang/protocol-diagnostic.ts`。
- 先跑测试红；添加typed error，仅已识别protocol与既有网关/network重试；穷尽错误message含安全诊断，避免只存在cause导致jobs.last_error不可见。
- 运行新旧client/ETL/consumer回归、Workertype/lint/audit，限定SHA后交审。

## Task 2：批次容错（此计划列范围，不声称Task1即完成）

- 审计full/incr/backfill各入口：实读full只有账户元数据/账户离线/实时，150次广告分批在incr，不沿用OS口述定位替代源码。
- 失败批继续其他批，但failed/missing必须带resource+ds+批准tuple，不抹旧完整性，不将partial finish伪成ready。
- 待确定既有EtlRunStore扩展的可追溯实现后单独代码/真实PG；身份失败、越权、截断仍fail closed。

## Task 3：身份绑定/重排/supervisor

- 后续F-OS-002/003独立子批；不删除历史job，不换credential owner，不接触真实身份/凭证。
- 本轮生图取消。先修OS首跑P0，再021/hourly、Gap。code-quality-checker提交前执行；执行子技能不可用，按用户持续授权直接推进。
