# P193 覆盖待办到期门质量回执

- 代码 **96c6c789**（3 个测试基础设施文件）；依据 arch main `5586efa9` 的 v1.9.19 / P190 裁决。
- **状态：implemented、targeted verified with known failure；未合流/部署/推送。** 这不是后端功能补全，没有把缺路由变成可调用接口。

## 精确范围

仅登记 arch 明确交给 fe F8-15 的两个 `backend_to_bff` 路径：

| 路径 | owner | 到期 |
|---|---|---|
| /api/v1/system/etl-runs | F8-15 | 2026-09-12 00:00:00 Asia/Shanghai |
| /api/v1/admin/data/reconcile | F8-15 | 2026-09-12 00:00:00 Asia/Shanghai |

没有 BACKEND_ONLY 永久豁免。到期时刻及之后，缺口重新失败；不使用可由 ENV 设置的跳过开关。已接通却残留登记也报错，要求删除登记，防止未来回归被旧白名单覆盖。路径/方向精确匹配；禁止通配符、空 owner、非法时间和重复条目。

## 真实结果

在 `apps/worker` 执行：

`npm test -- --run test/r010/coverage-pending.test.ts test/r010/bff-coverage.test.ts test/r010/route-path-inventory.test.ts`

- 新到期门 **9/9**、路径解析 **5/5**、实际覆盖 **2/3**：总 **16 passed / 1 failed**。
- 失败保持：`/api/v1/admin/members/:p/reset-password` BFF 有、后端无。其归属 F-OS-004，不在 arch 两项 F8-15 到期登记之内。**不能声明分支门禁绿。** 已发 F-P179-Q3 请求明确先做 F-OS-004 或补第三项临时登记裁决。
- 写实现前新测试因模块不存在失败；实现后新测试全绿。恰好到期 `2026-09-11T16:00:00Z` 及到期后均不抑制缺口。
- `npm run typecheck && npm run lint && npm audit --omit=dev --offline` exit 0、缓存漏洞0；`git diff --check` 通过。
- 本批没有生产 DB/HTTP 变更，不另跑 PG；没有把上批206项重复算成本批测试。

## 其他信箱接缝

- rerun 两问已收到：成功202、CONFLICT details.jobId、audit_log 指定落点。
- 新 fixture 的 sourceRunId=UUID 与实际 etl_runs.id BIGSERIAL / 已冻列表十进制字符串冲突。具体证据已回 arch F-P179-Q3，不修改 arch fixture/API，不发明 UUID 映射。
- P178 detail 下一批仍未接；P192列表成功不代表详情已成功。014 dispatches、021/hourly索引、F-OS-004、sop-run、P176容错继续在全目标内。
- code-quality-checker 按精确匹配、到期恢复、已完成清理、反向缺口保留和依赖进行检查。当前已知阻断如上；未宣称整仓可部署。
