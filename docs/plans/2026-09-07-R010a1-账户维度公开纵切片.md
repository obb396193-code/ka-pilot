# R010a1 账户维度公开纵切片 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将已冻结 account.dimension/v3 的个人账户维度接入唯一 Registry、真实 PG 窗口计算、Service/HTTP/BFF；缺源维度不伪造。

**Architecture:** 沿用 session explicit tuple scope 和 RR/RO snapshot。一次批量按账户读取生效考核价历史，与同快照账户聚合结果组合，禁止逐账户 N+1 summary。公开行按 arch 冻结 key=media:accountId；数据库输入/内部结果仍验证 workspace/media/account 三键。

**Tech Stack:** TypeScript、Zod、PostgreSQL、Vitest；不加依赖、不改 arch Contract/前端视觉。

执行技能未安装：由本会话按 TDD 分段执行，不派新角色。基线 100cc14（已合 main adc415d）。其他维度/team/pivot2/health/etl-run 仍在总信箱，不能把本片算 R010a1 全部完成。

## Task 1：账户级考核历史批读

- 修改 `packages/db/src/window-assessment-repository.ts`，新增 `loadByAccount(scope)`，复用 expected account-day、effective history、10k sentinel/16MB guard。
- 测试 `packages/db/test/window-assessment-account.test.ts`：先写失败测试，验证跨媒体/跨 workspace/空 scope/缺日/多价/未来价/坏数/超上限；然后实现。
- 每行必须带受信 workspace/media/account；仅按授权 tuple 读，不逐账户查。
- 运行定向真实 PG、DB typecheck/lint，再限定提交。

## Task 2：窗口组合与公开接线

- 新建 `apps/worker/src/data/platform-dimension-query.ts` 与测试；复用 canonical metrics/computeWindowAssessment/dimensionWindowRowsSchema；同 RR 内批读 dimension/history/lineage，校验两侧键集合及现金/转化合计。
- 扩展 Domain data-query-contract/data-query-rows、Registry、PlatformDataSource/query-service；公开 dimension 属性强校验，不把已实现 account 能力冒充其他维度。
- 更新实际 production composition，保留 unavailable/strict error/16MB/requestId；旧 request syntax 共用 Registry。
- 测试先 RED，再实现、GREEN；包括 source malicious tuple、空授权、跨媒体同号、过期 metadata、缺历史、非法 row。

## Task 3：纵向门禁与交接

- Domain/DB/Worker/Gateway/Web 非视觉测试；真实 PG 固定合成库串行，不建新库、不碰共享 ka。
- Typecheck/lint、核心覆盖 >80%、安全扫描；环境失败如实留痕。
- code SHA 与 P 编号回执/状态独立提交；不 push、不部署、不开放媒体写。
