# P198 持久化基础质量回执

状态：基础代码候选，**不是出站功能完成**。仅使用合成数据，没有真实钉钉发送、媒体写、部署或push。

## 提交与边界

- `9a037978`：7个代码/测试文件，235新增、2删除；030迁移、三类业务键准备和terminal原因。
- `c4272574`：计划、环境风险与恢复经过。
- `7c50dec9`：同步主管main@2b0e1694（含A46/v1.9.49）。三份信箱/台账冲突依arch授权仅删标记、双方正文保留；非本角色实现的合流文件不是本批代码成果。
- A40：新增私有Domain导出，公开HTTP/BFF响应没变。030新增去重键、重复引用、延后时间、租约与连续未知计数；旧行不填假业务键/时间。down遇管理元数据拒绝有损降级。
- A44：基础代码提交后apps/packages未跟踪源码为0；旧output日志/审查文档保留，没有夹带提交。

## 验证与失败轮

| 门禁 | 当前证据 |
|---|---|
| Domain定向 | 2文件59项通过；100%语句/分支/函数/行覆盖；typecheck/lint通过 |
| DB合并前全量 | 新合成库163文件1874项通过，含030真实up/down/up及旧queued/sent历史保留、跨空间dedupe FK反例；typecheck/lint通过 |
| Domain合并后全量 | 105文件1683项通过，typecheck/lint通过 |
| Worker合并后全量 | 首轮2488通过、1失败、2外部opt-in跳过；BFF子进程缺zod与本Agent并行npm ci重叠，整轮不计通过，依赖稳定后重跑 |
| DB合并后全量 | 待Worker结束后串行复跑 |
| Web A46编译兼容 | 首轮缺锁定依赖导致失败；npm ci按现有lock恢复1068个包，不运行脚本、不改package/lock；复跑typecheck通过、349测试通过、lint0错误26既有警告 |
| 静态安全 | diff/check无问题；新增SQL只有静态DDL，无请求拼接；新增实现无URL/密钥；DB离线缓存audit0（不是最新联网漏洞保证） |

原始日志：`output/p198-v1948-domain-focused-final.log`、`domain-coverage.log`、`domain-postmerge.log`、`db-recheck.log`、`worker-postmerge.log`、`web-typecheck.log`、`web-deps-locked.log`、`web-final.log`（除首个完整名外均加`p198-v1948-`前缀）。

失败轮必须保留：旧并行DB90文件失败/6测试失败/720未执行，首条迁移30秒超时引起后续锁/缺表连锁；Worker旧日志无完整终态。磁盘当时582MiB，恢复后7.7GiB；没有擅自清理或调宽超时。新库串行原迁移用例约2.5秒通过，不能据此宣称所有旧失败原因均已单独证明。

第二轮Worker失败证据：`work-item-list-http.test.ts`真实BFF子进程在Web依赖恢复期间报`Cannot find module 'zod'`。这是本Agent把依赖安装与跨包测试并行的安排问题，不能把2488通过称全绿；保留原日志，后续所有依赖安装与依赖它的测试必须串行。Web已独立全绿不替代Worker完整复跑。

TDD补漏：稀疏数组被旧map跳过并哈希成null，新增反例实红，再用Array.from遍历拒绝undefined并全绿。030测试标题原写保留queued/sent但只插queued，本轮补真实sent行和回放后时间不变断言。

## 尚未完成（下一步继续，不算阻断）

1. 实际Repository的claim/finish/fencing/未知恢复/24h sent去重和旧queued补键尚未接；仅迁移与纯函数不是持久化消费证明。
2. 收件人解析、按空间群配置、DM映射、白名单模板与Transport尚未接；发送超时/重定向/异常body仍需真实HTTP mock验证。
3. 单次CLI入口/diagnose、daily report的deduplicated展示仍未改；不能称告警可投递。
4. 本批还未交arch最终验收，等待合并后全门禁收口；总体目标继续P198→P193→P194→P199及后续队列。
