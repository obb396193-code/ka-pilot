# OS 探针清单

> 用途：直接复制发给 OS（Multica）上那个 agent，让它**实际跑命令**回答，不要推测。
> 回答拿到后贴回来，我更新 `docs/02-技术侧现状.md` 并记台账。
> 全部只读探测，不碰任何写操作。
>
> **当前状态：探针 A 待老板发送**（一次问全，避免多轮往返）

---

## 探针 A（当前主力，一次问全）

复制下面整段发给 OS：

```
你上一轮关于 Multica / Claude Agent SDK / skill / CLI / MCP / devix / cloud-cli 的说明我收到了，很清楚。

背景：我是 KA 部门快手渠道优化师。我要做一个面向 KA 多角色（优化师/运营/KA负责人/更高层）的投放提效产品，有自己的前端和多用户账号体系，跑在 Multica 沙箱之外。它不是一个 skill，是一个独立产品。我的定位是做"决策层"：判断该动哪个账户、给建议、回收效果；执行仍然落到你们已有的原子能力上。

所以我要摸清"业务侧能合法拿到什么"。以下每一条请**实际跑命令**再回答，把命令原文和原始输出一并贴出来（凭证类的值打码但保留字段名/变量名）。**不要推测，跑不了或看不到的就直接说"无权限/看不到"，这个答案对我同样有价值。**

一、kuaishou-cli 的鉴权与可移植性（最重要）
1. which kuaishou-cli、ls -l 结果、版本号。
2. 它的凭证从哪来？请具体区分：(a) 沙箱注入的环境变量（列变量名，值打码）(b) 本地配置文件（路径 + 字段名）(c) 需要用户自己登录（登录命令是什么、登录态存在哪、有效期多久）。
3. 它调用的服务端域名/接口地址是什么？是公网 MAPI 还是阿里内网域名？
4. 关键判断题：把这个二进制拷到沙箱外的一台普通机器上（公司电脑或云服务器），用我自己的账号/MAPI 凭证，它能不能跑通一条只读命令？依赖哪些环境变量、内网域名、证书？请给判断依据，不要只给结论。
5. 如果不能直接用，业务侧想合法拿到快手 MAPI 的读权限，正规路径是什么？（申请什么权限、找谁、走什么流程、是否必须挂在 Multica 下）

二、鉴权能不能"过 OS"给外部产品用（架构关键）
我的产品要做多用户登录，理想状态是：用户在我的产品里登录，鉴权透过 OS/Multica 换取媒体侧权限。请回答：
6. Multica 有没有对外开放的 API / OAuth / 开放平台？外部系统能不能以"应用"身份接入（而不是必须跑在沙箱里当 agent）？有没有 App ID / Secret 这类机制？
7. multica CLI 的 MULTICA_TOKEN 是怎么签发的、有效期多久、能不能给外部服务申请长期或可刷新的 token？
8. 有没有"工作流网关"或对外触发入口（你提到 /opt/cloudcli 有 workflow-gateway）？外部系统能不能 HTTP 触发一个 Multica 任务并拿回结构化结果？如果能，鉴权怎么做、有没有文档地址。
9. 如果外部产品完全不能直连，那么可行的替代形态有哪些？请按可行性排序，并说明各自的限制：(a) 我的产品作为 skill 装进沙箱 (b) 外部产品 HTTP 触发沙箱任务、结果回调 (c) 只在沙箱内跑、把结果产物导出到外部 (d) 其他。
10. 沙箱的出网限制：能不能主动访问外部我自己的服务（比如我部署在公司内网或公网的一个 API）？有没有白名单机制、怎么申请？

三、BI 真实转化 / FBI（业务价值的地基）
我做的成本口径必须用后台 BI 真实转化，不能只用媒体账面转化。
11. 现在能查 BI 真实转化的 skill 有哪些？把它们的 SKILL.md 全文贴出来（不要摘要）。
12. 这些 skill 底层调什么：CLI / HTTP 接口 / 数仓（dataworks？）？鉴权怎么来（变量名、配置路径，值打码）？
13. FBI 平台能不能从这里查？用什么方式？我这种业务侧账号默认有没有权限，没有的话怎么申请？
14. 能拿到哪些 BI 字段、最细到哪个维度（账户/计划/任务/日期/小时）、能回溯多少天？请给字段名清单。
15. 这些 BI 数据能不能出沙箱（导出成文件、或被外部服务查询）？合规上有没有限制？

四、可用能力全清单
16. 把当前可用的**所有广告投放相关 skill** 列成表：名称、一句话作用、读还是写、底层调哪个 CLI/接口、是否需要人工确认。项目级（.claude/skills）和用户级（~/.claude/skills）都要列，description 原文一并给。
17. .claude/skills/ks-data-queryer/ 的完整目录树 + SKILL.md 全文 + connectors/ 里每个文件内容（凭证打码）。
18. kuaishou-cli 所有**只读**子命令清单及参数；报表类命令的完整指标字段名列表；支持的维度拆分；时间粒度与历史回溯上限；单次条数上限与频控。
19. 实际跑一条最小范围只读命令（比如某个账户昨天的消耗），把命令和返回的 JSON 结构贴出来（字段名保留，数值可打码）。
20. 启航cli 现在覆盖哪些能力（创意/文案/DP 供给）？也给只读命令清单。

五、别人已经做到哪一步（避免重复造）
21. 这套体系里现在有没有已经做成"看板/报表/日报"形态的东西？谁做的、在哪看、覆盖哪些渠道？
22. 有没有已经在跑的自动化诊断或调控（定时任务、自动改价、自动预警）？规则是什么、谁在用？
23. 业务侧（非技术部门）的人，现在有没有人在这套体系上自己做东西？做的是什么形态？

请按编号回答，能跑就跑、跑不了就明确说不能。我最需要的三个答案是 4、9、13。
```

---

## 备用探针（A 回来后按缺口追问）

### 探针 B：如果 kuaishou-cli 可移植（走独立产品路线）
```
请给出在沙箱外部署 kuaishou-cli 的完整前置条件清单：二进制获取方式、依赖、环境变量、证书、内网访问要求、凭证申请流程。以及：是否有版本升级机制，我如何保证外部副本不过期失效。
```

### 探针 C：如果必须寄生（走 skill 路线）
```
如果我把产品做成 skill 装进这套体系：
1. skill 目录能不能包含一个长期运行的服务（不只是脚本）？
2. skill 能不能有自己的持久化存储（数据库/文件），跨 run 保留状态？沙箱重启后还在吗？
3. skill 能不能对外暴露一个 Web 界面让人用浏览器访问？
4. 一个 skill 怎么发布给部门其他人用？审核流程是什么？
5. skill 里能不能调用外部 LLM（我自己的 API key）？出网限制是什么？
```

## 回收记录

| 探针 | 发送日期 | 回收日期 | 结论落到哪 |
|---|---|---|---|
| A | 待发 | | 02 文档全面修订 + 新建 04-可用能力清单.md |
| B | 视 A 结果 | | |
| C | 视 A 结果 | | |
