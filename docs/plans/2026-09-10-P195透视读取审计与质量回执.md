# P195 透视读取审计与质量回执

## 交付

- **代码 8cc6a367**，2文件，76增1删；production仅3行：透视daily LEFT JOIN复用 `etlBatchReadableSql("metric")`。
- 本机 implemented + targeted verified；待arch审查，**未merged/deployed/push**，不代表P178全域完成。
- 生图按老板取消，原图和输出文件保留；不动前端/合同/arch工作台账/其他agent源文件。

## 本轮审计结论

行号基于8cc6a367。未引用共享权限函数不能直接当“已证越权”；以下三条在实际PG同号跨媒体/跨workspace测试中没有复现泄漏。

| 文件/入口 | 实际范围与证据 | 结论 |
|---|---|---|
| platform-pivot-sql.ts:7 / platform-pivot-repository.ts:66/84 | expected仅来自Session获授tuple数组；account/metric/relation连接均三键；库后再核ws/pair/day与完整行数 | 结构性授权成立，不是假定全workspace可见。仍未收敛为共享权限helper，不能称P178规范全部完成 |
| rule-definition-repository.ts:48/62 | target先命中personal scope再查同workspace规则；账户/任务适用范围按准确pair及有效日；team定义可读但不借个人grant | 没有全账户指标查询。规则定义本身是workspace资产，当前未找到Worker公开消费它的入口，不擅自扩大公开规则可见性 |
| rule-evidence-repository.ts:21/28/40 | scope进一步缩成目标单户，definition/指标/考核价同RR/RO；team显式source unavailable | 真PG确认同号不串数、缺日不可判定，不是团队取证功能完成 |
| semantic-query-health.ts:186/207；semantic-query-repository.ts:169 | ETL状态/质量计数仍workspace/date级；Worker production检索无queryHealth调用 | 保留内部风险登记，不能作为个人公开健康接口直接挂上；本批没有改它或宣称已授权 |
| work-item-command-repository.ts:32/43；account-mute-repository.ts:50/150 | 预取最小item后比Session tuple，再锁live membership/grant（含revoked_at）；最终更新绑定对象 | 当前不放宽命令到新版所有可见任务/私人项；共享化/命令语义不混本批 |

其余changeset后台claim/follow-up/execution队列与quality后台只按workspace的调用链仍须继续完整审计，不以静态关键词覆盖冒充安全证明。

## 本轮发现并修复：失败批次旧数在透视中重新可用

这是一处**数据一致性缺口，不是新证明的越权**。P179已让Semantic读取屏蔽存在失败台账且尚未恢复的tuple-day；pivot的daily JOIN漏了同一屏蔽，规则取证复用pivot也受到影响。

- 真实PG红：两户中一户已记BATCH_FAILED，预期observedAccountDays/observedAccounts=1，旧实现仍回2；新负对照也因找不到屏蔽谓词而红。旧7项过、新2项红。
- 修复把共享屏蔽加在**LEFT JOIN ON**，不是WHERE。失败户expected仍保留，observed=false、金额missing、computedAt=null；其余户保留原数据，不能把失败户当0或直接删掉。
- 对应资源/日期/媒体/workspace的成功Raw到达仍不足以恢复旧canonical，必须重算；完全复用既有helper，不另造恢复判据。
- 新第3项真PG：规则原先pass=true → 失败日pass=null/METRIC_MISSING → 新Raw仍null → canonical重算后true。不生成虚假“不触发/触发”判断。
- 测试代理只摘共享屏蔽便再次读到失败户旧值10；这是合成数据负对照，生产SQL未去掉防线。
- 不开启Full/Incr按批继续执行，不改warnings/coverage公开契约，不清除存量指标，也不改变规则定义、账户授权或任务关联。

## 命令与结果

均在本机独占合成库 `ka_be_r010_p194_test`（localhost:55432）；每套件自主迁移，仅清自身随机workspace。没有公司数据或凭证，没有实际媒体/ASR/Agent调用。

### DB：7文件117/117

在packages/db，TEST_DATABASE_URL指向上述库：

```sh
npm test -- --run test/etl-batch-readability.test.ts test/platform-pivot-repository.test.ts test/platform-pivot-repository.unit.test.ts test/rule-definition-repository.test.ts test/rule-definition-repository.unit.test.ts test/rule-evidence-repository.test.ts test/rule-evidence-repository.unit.test.ts
```

最终摘要：7 passed；117 passed；30.99s。包含readability10、pivot7+45、rule-definition11+22、rule-evidence6+16。初次107、两新例版116、unit83均为中间重复跑，不再累计。

### Worker：4文件65/65

在apps/worker，相同显式合成库：

```sh
npm test -- --run test/pivot-query-pg.integration.test.ts test/platform-window-query-pg.integration.test.ts test/platform-pivot-query.test.ts test/pivot-public-query.test.ts
```

最终摘要：4 passed；65 passed；27.03s。真PG pivot HTTP/factory3、window9；其余为映射/边界/合成reader。没有把synthetic reader BFF测试说成真实PG全链。

### 质量门与限制

- DB/Worker各自 `npm run typecheck && npm run lint && npm audit --omit=dev --offline`：exit0，缓存production漏洞0，不等同实时漏洞库审计。
- `git diff --check`通过；代码路径限定commit并`git show --stat`核验，只有两个本人DB文件。
- **182个不同定向测试**，不算全仓门禁；Domain未变，未额外重复全包。未单独生成本批覆盖率报告。
- code-quality-checker核查SQL参数化、代码固定alias、三键不变、LEFT JOIN保留缺数、无secret日志。复用相关NOT EXISTS会增加查询工作量；小合成PG通过，**未做10k行真实PG性能基准**，不宣称性能无影响。
- 磁盘从4.4GiB降至2.6GiB（与其他工作并行，未调查归因）；低于8GiB全门禁阈值，不启动全五包/构建，不擅自清缓存。第一次PG请求自动审批超时，重试一次获准并真实完成，不标pg_blocked。

## 接续与未完成

P178其他仓储/共享权限收敛继续；rerun fixture sourceRunId UUID/BIGINT仍待F-P179-Q3，未改archfixture。014 dispatches、021/hourly索引、F-OS-004、sop-run、P176 Full/Incr运行时、P187其余公开fixture对拍均未被本批替代。总信箱目标保持active。
