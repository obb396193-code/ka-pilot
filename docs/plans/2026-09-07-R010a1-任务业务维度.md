# R010a1 任务与业务维度 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 接通个人 task/biz account.dimension/v3，从受信授权账户的历史生效归属生成窗口指标与考核，不复用当前挂载或平均CPA。

**Architecture:** 复用已交付账户维度 RR/RO snapshot、一次批量账户日/历史读取；为内部历史行附当天 taskId/bizName，按它们分组，和数据库维度聚合核对键集合/总量。所有数据仍来自批准workspace/media/account。未知任务/业务保留null分组；缺主表标签不丢事实关系。

**Tech Stack:** TypeScript、PG、Zod、Vitest，无新依赖。

技能 executing-plans 不可用，主会话按 TDD 执行；不派子角色。不改 arch Contract/视觉/真实媒体写；总信箱范围不变。

## Task 1：内部历史归属与真实 PG 反例

- 修改 `packages/db/src/window-assessment-repository.ts`：loadByAccount 附 relation.task_id/tasks.biz_name（LEFT JOIN）。新字段严格类型；不把 task/ad/material 对象假定成账户同键。
- 测试 `packages/db/test/window-assessment-account.test.ts` 先 RED；补缺任务主表、跨日更换任务、重叠日期的用例。
- 同workspace跨media同号、foreign workspace、空scope继续通过；参数化输入，未知标签null，不回退到整workspace。

## Task 2：分组窗口与公开 Registry

- 修改 `apps/worker/src/data/platform-dimension-query.ts`，新增 group(input)；跨日归属从内部真实行读，允许一个账户跨日进入不同任务，但每账户日只一次。
- 修改 Domain 仅导出已有 grouped row schema；Registry启用task/biz；PlatformDataSource按dimensionType调用统一窗口组合；BFF现有strict union不另造接口。
- 先写 `apps/worker/test/platform-group-dimension.test.ts` 失败测试，核实类型/缺日/重复/金额差异/无授权/未知分组/账户多日切任务；再实现。
- 真实 PG→HTTP 验证真实维度数据及requestId；不得仅mock声明完成。

## Task 3：验证与留痕

- `npm --prefix packages/domain test -- --run`，DB/Worker/Gateway串行固定合成库，Web非视觉测试；typecheck/lint/coverage/offline audit。
- 核心coverage>80%；接口旧账户维度和summary/trend/table全部回归；显式记录既有Web依赖缺口。
- 限定代码SHA + P编号回执 + R010状态，不push。team/agency/扣量/版位/出价/pivot2/health/ETL和其余信箱继续保留未完成。

## 开发中收到 arch 更新（cfb0d50）

- 已快进 main@cfb0d50，P106 上批已获审；56fd109 补齐维度完整 lineage，Domain 直接验权威 source，不再合成 unknown metadata。
- F-P110-1：仅 Domain 10k 哨兵 / DB 完整迁移回放两个重用例设30s，不改全局、不删断言；以独立 SHA 交付。
- 本批 final 交审后冻结 be/r010，不再增加提交直至 arch ✅/❌。总信箱目标仍未完成；本片完成不能替代后续任务。
- 自审发现有 observed rows 却 accountCount=0 的 mock 可过，先 RED 复现后补 fail-closed；全量结束前不再编辑代码/测试。
