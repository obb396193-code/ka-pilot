# B21 状态

> 分支：`codex/b15-material-teardown-semantics`  
> 基线：B20 最终交接 `b77076e`  
> 当前状态：工作流未触发诊断纯 Domain 内核完成；未合并、未部署、未接 Scheduler/DB/API/前端。

## 已完成

- 绑定 workspace/workflow/version/trigger/evaluation 的评估快照。
- 有序 gate checks：passed/blocked/not_evaluated。
- eligible/blocked/incomplete 三态；全部阻断和第一阻断。
- 12 个既有 WorkflowBlockReason 对应稳定 nextActionCode。
- blocked 证据引用、可选未来 retryAt、数据截止时间。
- 排序/唯一性/连续顺序/时间边界、稳定指纹、深冻结和语义完整性门。

## 明确语义

- eligible 只表示本次门禁全部通过，不代表 Run 已创建或动作已执行。
- Domain 不计算新鲜度、样本、冷却、日上限、冲突和权限，只归一真实执行器给出的 check facts。
- 不包含用户话术；前端按 reason/action code 映射人话。

## 提交

- 设计与计划：`17b54f5`
- 领域内核：`f985923`
- 质量与交接：待回填

## 验证

- B21 定向：16 passed；模块 98.54%/91.02%/100%。
- Domain：31 files / 397 passed；97.34%/87.84%/99.57%。
- DB：19 files / 92 passed；真实 PostgreSQL/migration 通过。
- Worker：48 files / 301 passed + 1 skipped；PG/gateway/FFmpeg 通过。
- DingTalk：5 files / 19 passed。
- Claude Agent SDK opt-in：1 passed。
- 四包 typecheck/lint/audit、复杂度、安全和冻结目录门禁通过。

## 等待 Claude/arch

1. Scheduler/Trigger 评估记录何时落库、evaluation 幂等键和 retention。
2. 12 类 check fact 的生产者、证据对象和 data cutoff 来源。
3. API DTO、运行中心查询与权限；原始运行记录仅授权角色可见。
4. reason/action code 的最终人话、跳转入口和 retry UI。

