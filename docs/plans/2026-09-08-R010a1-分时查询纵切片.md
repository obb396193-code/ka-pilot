# Account Hourly Query Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 当前无该子技能，按已授权持续目标在本人worktree执行。

**Goal:** account.hourly 从strict参数进入Registry、Session授权Service、HTTP/BFF；真实reader缺席时503，不用日累计伪造小时成功。

**Architecture:** 复用P138参数、P130行投影。Registry列出固定capability；独立可信hourly port保留内部workspace证明，公开rows沿冻结media/accountId/hh。输出再次核对完整tuple/窗口/小时/coverage/上限，统一meta由已验证source派生。runtime不注入未证实Provider。Gap随后单独批次处理规则版本。

**Tech Stack:** TypeScript/Zod、现DataQueryService、Node BFF、Vitest、独立PG会话联测。

### Task 1：RED与Domain/Registry

- Domain data-query-contract/data-query-rows接hourly ID、严格rows集合、hourly meta。request复用accountHourlyParamsSchema；旧八查询不变。
- Worker query-registry增加hourly定义、0..24参数、单日；无KA SQL生成器。
- 新hourly-public-query.test先测合法请求不再404、非法hour/date/scope拒绝；缺port503，不触发日表fallback。

### Task 2：输出守卫与Service

- 新hourly-public-source.ts：内部workspaceId proof、sourceQueryResult strict、请求date/media/hour/账户集与返回coverage/rowcount一致；未知lineage不能编造。复制auth入port，恶意修改不扩权。
- Service通过原Session来源选择与scope交集后调用hourly port；无port/非personal明确不可用，team不fallback个人。source未接也要先完成请求与授权检查。
- 无可信total命中10k/任何truncated拒绝，可信total恰好上限且完整才允许；外层HTTP exact16MiB继续复用。

### Task 3：BFF与验证

- Web canonical-query-rows/contracts消费hourly严格行及meta，复用已有BFF/Session。对Domain/Web schema作永久parity，不能仅手抄不验证。
- 现hourly fixture有`partial:true + wholeResultTotal.available`及meta/source时间不一致：不放宽线上校验、不改arch文件；测试仅引用其行投影，另构造标记为synthetic的自洽Envelope，向arch报矛盾。
- Domain/Worker定向与类型/lint；Web全node与定向类型；真实主进程PG验证hourly无源503、越权403、logout401；更新状态、P139交审。真实小时Provider、Gap、真实Next页面仍不称完成。
