# B7 工作流可靠执行内核状态

> 分支：`be/b7a`
>
> 基线：B6 `57c5773`
>
> 当前状态：设计已批准，逐文件实施计划编写中。

## 已确认范围

- 方案 B：B7a 工作流可靠执行内核完成后，再做 B8a 知识库领域底座。
- B7a 可实现领域、Repository、Worker 和内部端口。
- 不改冻结契约/迁移，不碰前端，不编造真实 Multica/OS 协议。
- 媒体写操作继续强制 B3 变更集、dry-run、确认和 UNKNOWN 对账。

## 当前依据

- `packages/contract/schema.sql` 已有 workflow definitions/versions/runs/events 最小表。
- B1-B6 已有 jobs、规则、工作项、变更集、Agent 和数据事实可复用。
- 公开图 Schema、节点/事件 DTO、确认恢复 API、触发幂等和真实 OS 接缝尚待 Claude/arch 裁决。

## 下一步

1. 写逐文件 TDD 实施计划。
2. 先做 Capability Registry 和严格图编译。
3. 再做状态回放、Repository、Simulation 和 Worker。
4. 跑全量门禁并写 P-011 审查回执。
5. 随后进入 B8a 知识库领域底座设计。
