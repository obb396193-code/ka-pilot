# P185 运行记录响应基础质量回执

## 状态

代码 **70187851**，F-P179第一子步：Domain严格响应，**不是GET已接通、更不是rerun已开放**。
唯一权威fixture `packages/contract/fixtures/system/etl-runs.json` 未修改；测试只删除其文档用meta._note，逐字段对拍。
新schema从Domain index真实导出，测试由index导入验证，不只测私有文件。

## 已实现

- runId正int64字符串，覆盖2^53相邻值及9223372036854775807，拒数字化/越界/前导零。
- 一行一次attempt，正安全整数，不按job合并；重复runId拒绝，不静默去重。
- jobType复用现有withEtlAttempt六类，status仅running/done/failed，不造新状态。
- rows按当前冻结的完整对象或null校验，真正0保留，未知不填0；各字段null建议已向arch另问，当前不越权放宽。
- 公开warning复用etlBatchFailureWarningSchema；旧字符串只放行fixture已有BLOCKED_AUTH；私有media/failedAt/原错误体字段拒绝。
- 时间为真实日历日期，结束不早于开始，running无finished、terminal需finished；strict响应、requestId、meta、稳定错误envelope。
- 10000条为Domain防御性输出上限，不代表来源完整、SQL已分页、16MB传输已验。后续Runtime仍必须独立执行截断与安全字节上限。

## 验证

| 检查 | 结果 |
| --- | --- |
| 实现前响应测试 | 36红，缺真实导出；没有用无效导出绕过 |
| 最终响应38 + 批次warning19 | 57/57 |
| 新模块覆盖 | 行/函数/分支100% |
| Domain/DB/Worker typecheck、lint | 三包通过 |
| Domain `npm audit --omit=dev --offline` | 缓存0，不代表在线刷新 |
| diff check | 通过 |

本轮纯Domain没有SQL改动，不运行/冒报真实PG；没有启动服务、队列或付费调用。
磁盘约7.4GiB低于arch全包8GiB门槛，以上为定向门禁，不报全包绿。

```sh
# packages/domain
npm test -- --run test/etl-run-list.test.ts test/etl-batch-failure.test.ts --coverage --coverage.include=src/etl-run-list.ts --coverage.reporter=text
npm audit --omit=dev --offline
# Domain/DB/Worker分别
npm run typecheck
npm run lint
```

## 必须保留的未完成项

inbox-arch P185四问已发：旧无execution快照记录的attempt；分离job的raw/canonical计数；GET分页/排序；rerun身份/状态/幂等。
不拿当前jobs.attempts代替历史，不跨run拼计数，不用响应时钟证明canonical新鲜度。
这些源/规则未明前，不把严格schema通过包装为可用接口。
后续Repository+HTTP、P176容错、密码与小时源等仍待做，全部信箱目标active。
P-178工作项等待be2 Q-027共享helper合main；其他授权检查未全部完成。
本候选未合流/部署/未push，无前端/真实媒体写，生图停止。
