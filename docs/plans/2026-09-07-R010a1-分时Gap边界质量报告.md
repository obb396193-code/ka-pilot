# R010a1 分时 / Gap 数据边界质量报告

## 状态

- 代码`4041f26`：agent_type增加unknown，权威样例原样parity；代码`ad19c08`：分时/GAP版本化严格rows边界。合main@`1c622d0`后HEAD`3d4df55`。
- 本批只完成Domain边界，不是hourly/gap公开功能完工。Registry、真实源reader、完整envelope、HTTP/BFF仍待接。无修改Contract/前端视觉/DB/媒体写/依赖；未push/部署。
- 使用writing-plans按TDD拆步；code-quality-checker自审类型、安全、缺数、覆盖。主会话独立执行，无子Agent。按最新长期队列，**交审后继续下一项，不再等审停工**。

## 实现证据

- unknown不是self默认值，不补代理名称；最新昵称解析主源由be2 R017承担，旧custom_tags只对照；此处不抢解析器或accounts新列。
- 两种输出逐字段strict，复用canonical MV三态/RV有限、无穷、未定义，hh整数0..24；24为显式全天，不当第25小时。重复(media,accountId,hh)拒绝，同ID跨media允许；Gap重复group.key拒绝，null与字面"null"分开。
- 缺当前累计数不能伪造delta；已知相邻前一小时缺数时后一个delta不可available，按tuple匹配且与数组顺序无关。返回窗口没包含前一行不等同源缺采样，不擅自拒绝已有独立差分证据。
- 缺基础数不可给有限CPA/Gap或normal/high；零分母不可有限。规则high阈值、现金系数、差分/速度/预测计算、可信scope和完整性仍是后续reader责任；不手抄比率算法/硬编码规则。
- 数组≤10k只做row边界，不证明源完整、不替代HTTP exact16MB。外层版本化投影只含queryId/rowSchemaVersion/rows（Gap加groupBy），不是完整source response。

## TDD / 门禁

- unknown先RED：46例中3失败，原因旧enum不接受新fixture第三行。修改enum后回归通过；不是删样例或把unknown当self。
- 新rows先RED：模块不存在；实现后与六份权威维度样例/现有query契约一起115/115通过。后加2个负向测试，最终覆盖运行2文件85/85（维度46+操作rows39）通过；不是两组数字相加。
- 最终两个被测文件语句/分支/函数/行均100%；仅这两个Domain文件，不宣称整个后端100%。
- Worker定向6文件121/121通过：Registry、语法、canonical映射、账户维度、task/biz维度；Domain及Worker typecheck/lint均通过。Domain production offline audit0（缓存证据）。
- 合main@1c622d0只增加arch队列/台账等文档，`git diff c508169..main -- packages/domain apps/worker packages/contract/fixtures`为空；上述源码测试适用合并头，未声称重新全量。
- 全量空间门不满足：本轮4.8→3.8→1.7GiB，不跑全量、不新建PG库、不清缓存。本批无DDL/DB改动，未跑新的真实PG，旧批PG不能替代本批证明。
- 日志在`output/r010a1-operational-{rows-focus,coverage-final,worker-regression}.log`，早期RED见工具回显。实现计划含逐文件范围。

## 后续依赖，不阻断整个队列

1. 分时源：当前Qihang client的account_realtime类型/URL无hh，incr只对广告做相邻hh采样；账户仅拉当前全天。需要OS实证account_realtime是否真正支持hh，不能自行假定。既有广告小时表没有lastSyncTime/修正字段，不拿它伪造账户分时。
2. 扣量源：当前canonical原样存account_deduction_rate；规则桶按百分数，而Gap RV样例用0.1。需要确认存储单位及多日窗口按account-day归桶还是账户代表值；不重复裁决产品是否要做，只补可执行口径。
3. ETL列表：旧startRun把BIGSERIAL转Number且scope未固化attempt；新只读列表必须保留字符串与真实attempt证据，不能用jobs当前attempt数填历史记录。
4. 下一步继续R010a1 system/health等不依赖昵称与分时源的功能；需要共享结构时用arch开缝，不抢be2列表六文件。013/014/016从Contract切片。所有未实现项仍保留总目标。
