# R010a1 健康覆盖率读仓储 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 给 system/health 提供真实、按批准账户范围隔离的 canonical 覆盖率与更新时间观测，不把缺数变成健康。

**Architecture:** 新建独立 PlatformHealthRepository；输入只接受服务端 ApprovedWorkspaceAuthContext 和已解析业务日。在 RR/RO 事务内用参数化 SQL 同时统计账户分母、当日有行账户、缺 computed_at 的账户与 MAX(computed_at)。不复用 workspace-only 的旧 ETL/quality 聚合，不修改 be2 六个列表文件。

**Tech Stack:** TypeScript、现有 Domain Zod、pg、Vitest。

---

执行由当前会话连续完成；executing-plans 不在可用技能中，按本计划 TDD。无需等待交审结果再做下一项。

### Task 1: 账户范围与观测仓储

**Files:** Create `packages/db/src/platform-health-repository.ts`; Test `packages/db/test/platform-health-repository.unit.test.ts`; append own export `packages/db/src/index.ts`。

1. 写失败测试：personal tuple 参数化、team 无 grants、空 grant、重复 grant 拒绝、非法日期/认证 DB 前拒绝。
2. 在 `packages/db` 跑 `npm test -- --run test/platform-health-repository.unit.test.ts`，确认新模块缺失的 RED。（根目录没有 npm workspace，不能用 --workspace。）
3. 实现 SQL：personal 分母来自明确 grants（未同步主表也计缺数），team 来自当前 workspace accounts；metrics 以 workspace/media/account/ds 连接；COUNT FILTER + MAX 仅在批准范围。
4. 校验 PG count 为安全非负整数，withData 不超 accounts，missingSyncTime 不超 withData；时间必须来自非空 computed_at，非法值回 typed error，不带原始 DB 值。
5. 加 rollback/release 与未知时间反例，定向绿后路径限定提交。

### Task 2: 真实 PG 与质量门

**Files:** Create `packages/db/test/platform-health-repository.test.ts`。

1. 固定本后端合成测试库；不连共享 ka，不建临时新库，不启动 Worker 消费。
2. 验证同账户号跨 workspace/media、空 grant、未同步主表、team 全 workspace 但不跨空间；computed_at null 保留未知；授权外最新时间不能污染 dataAsOf。
3. DB typecheck/lint、定向与相关回归；磁盘低于 8 GiB 不跑全量。PG 若不可用如实登记，不能用 SQL 文本测试冒充。

### Task 3: health 上层接线（后续，仍属总目标）

- Domain 公共 DTO、health Service、Session extraRoutes、BFF/16MB/HTTP 联测仍要做。本仓储不等于 system/health 上线。
- Contract api.md:1014 的 agent.total 与 fixture.agent.instances 不一致；请 arch 统一。healthScore 算法未见冻结，不自行给 98/100 分；缺探活来源一律未知。
- ETL attempts 必须来自不可变 attempt 证据；旧 etl_runs 无 attempt 列，不用 jobs 当前 attempts 或 ROW_NUMBER 假造。
- 实施时已读代码无 extraRoutes，依 arch 开缝接线，不抢共享结构。缺口发 inbox-arch，同步继续其他可做项。
