# P130 分时累计投影质量报告

## 交付边界

代码 `7ddadb4`，基于已合 main@e9df460；交审前再合 main@621faad（合并2304379，仅前端/文档变动）。
仅 `packages/domain/src/hourly-projection.ts`、本人index追加、33项测试。不改Contract、be2文件、前端视觉、真实写、凭证或运行中服务；没有push。
状态为 implemented/candidate，本机计算与DB回归 verified；不是 account.hourly 查询/真实源/部署完成。

## 计算与自审

- 请求manifest限定同workspace/真实日期/批准media+account及小时范围；多媒体同号独立，输出稳定排序，私有字段不外泄。
- 只接受规范化累计值。0小时累计零基线；普通小时必须有对应前驱；24全天不产出伪小时差值。
- missing不补0，负差值标error而非吞掉修正；每指标独立传播。现金值由reader供应，不默认现金系数。
- CPA复用RatioValue（正/0无限，0/0未定义）；预算缺失未定义。速度只有完整一小时证据才出；预估只有真实时间占比才出，绝不从hh、机器当前时间猜。
- exactly10000期望格可计算但不证明来源完整，10001拒绝；观察上限在schema递归前拦截。字段存在但非法、重复、跨日/越权、数值溢出全部拒绝。
- 不修改调用方对象；safe error不带原始观察。安全扫描命中仅测试的synthetic token拒绝反例，无HTTP、Shell、日志或secret处理。

## 真实门禁（本批）

| 检查 | 结果 |
|---|---|
| TDD RED | 新module不存在，原始 red.log 保存 |
| Domain定向 | 33新 + 39旧 = 72/72 |
| 新模块coverage | 行/语句/函数100%，分支97.1% |
| Domain全量 | 71文件：1007通过、1失败（be2 search v1.9漂移，见下） |
| DB全量真实PG | 84文件881/881，独立ka_be_r010_20260907_test@55432 |
| Worker定向 | platform-pivot35 + data-query15 + qihang-client38 = 88/88 |
| Domain/DB/Worker typecheck + lint | 全部exit0 |
| diff --check | 通过 |

命令：`npm --prefix packages/domain test -- --run`；DB同命令在 `packages/db` 且显式TEST_DATABASE_URL；Worker `--run test/platform-pivot-query.test.ts test/data-query-service.test.ts test/qihang-client.test.ts`。
日志 `output/qa/p130/{red,focused,coverage,domain-final,db,worker-focused}.log`；首轮test下标TS错误已修（非业务边界问题）。
**全量未全绿**：search-contract.test.ts:15仍要求subtitle、拒新版fixture.meta，已在P129报arch/be2；不删断言，不改他人文件。Domain首轮1005/1，新加两个边界后最终1007/1。
DB开测前9.4GiB、跑完期间可用空间最低4.3GiB，后回6GiB；低于8GiB不再启动Worker全量PG，无新Worker全量/线上证据。未清文件/缓存/数据库，不沿用P129全量数字冒充本轮。

## 下一步精确依赖

1. `apps/worker/src/qihang/client.ts:44-47` account_realtime类型无hh；`:298-300`只序列化ds。`etl/incr-handler.ts:82-88`账户请求未传hh；`etl/query-observation.ts:14`仅ad携带hh。
2. `refs/ks-data-queryer-v1.0.2/ks-data-queryer/connectors/account-realtime.md`参数表也无hh；OS轮2/3证据是ad_realtime。请确认账户hh实际生效，而非忽略参数。若确认可用，接client/ETL/观察记录与raw reader；若不支持，要按完整广告快照manifest聚合，不能聚合不完整raw集合。
3. last_sync_time是同步时间，不自行等价为累计截止精确时刻；reader无法证明completeHour/elapsedDayFraction时传false/null，本层给missing。现金补偿/系数必须有真实有效版本。
4. GAP规则有效版本与归因/扣量事实仍需真实reader；公开Registry/服务/HTTP另批，不把仅strict rows当完成。
