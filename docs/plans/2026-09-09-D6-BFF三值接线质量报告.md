# D6 BFF 三值接线质量报告

代码 **693614e**，be/r010，已包含 main@7dd47e6。候选已提交，未审合、未push、未部署。

## 实现及范围

- `r010-command-bff` 仅给 dry-run 接合法三值200响应；核对 upstream/header/body requestId、path changesetId、逐项三值及summary/confirm/时间，拒绝旧缩略成功与extra字段。mute/ignore边界保持。
- 共用Domain的strict schema，不在BFF复制DTO。原Domain `.js` 引用链不能由网页原生Node TypeScript测试直接解析，因此抽出 `changeset-preflight-wire.ts` typed factory：Worker旧入口与BFF都给同一 ChangeValue validator/equality 实例。无loader/tsconfig/新依赖改动。
- equality/canonical serialization从原文件机械移到纯 `change-value-schema.ts`，旧 `changeset-values.ts` 兼容导出，hash继续使用同一序列化。BIGSERIAL上限改用BigInt构造器以兼容Web ES2017编译目标，仍精确无损。
- 全响应共享校验增加 `dataAsOf <= checkedAt`（HTTP上轮已检查）；null保持未知，拒绝未来源时间。预检三值不由BFF计算/补值。
- 真实 BFF函数→localhost HTTP→独立PG已实测，回传unknown观测与execution_runs快照逐项一致，confirmAllowed=false、无媒体jobs。session解析器及媒体Provider为合成测试替身，不是公司权限/媒体通路证据，也不是已运行Next/browser。
- 只改9个代码/测试文件；Contract、页面、组件、主题、Next路由、生产composition、DBDDL、be2服务0 diff。

## 命令与实测

| 范围 | 命令/结果 |
|---|---|
| BFF红灯 | `node --test lib/data/r010-command-bff.test.ts` 改实现前2 fail/16 pass：旧BFF不接合法success |
| Web定向 | `node --test --test-reporter=spec lib/data/r010-command-bff.test.ts lib/data/change-value.test.ts lib/data/internal-api-bff.test.ts lib/data/admin-calendar-bff.test.ts lib/data/work-item-list-bff.test.ts`：39/39 |
| Web覆盖 | node `--experimental-test-coverage --test-coverage-include=lib/data/r010-command-bff.ts`，command+value20过；BFF100%行/94.06%分支 |
| Domain | vitest `changeset-preflight-presentation / changeset-values / changeset-typed-flow / changesets` 四文件82/82；wire100%行/分支；value-schema100%行/98.14%分支 |
| Worker/PG | 显式独立TEST_DATABASE_URL，vitest `changeset-dry-run-service-pg.integration / changeset-dry-run-http / changeset-dry-run-service`，97/97（其中10真实PG）；maxWorkers=1，无共享ka库 |
| DB | `changeset-observations.unit / changeset-dry-run.unit`：52/52 |
| Domain+DB | 各自typecheck/lint通过 |
| Worker | lint通过；typecheck仍4项account/task旧fixture缺be2新增列，无本批文件类型错 |
| Web | 三个改动文件ESLint通过；整包typecheck75项错误，无本批文件错误。缺shiki/ai/streamdown/motion/xyflow/blocknote等运行依赖，引发组件相关类型错误；未安装依赖或更改视觉来绕过 |
| Git | merge main已最新；diff --check通过，路径限定9文件提交；未push |

共 **270项独立定向通过**（82+97+52+39），覆盖率重跑不重复计数。DB最初误用不带.unit的文件名，vitest报告No test files found；随后按实际文件重跑52通过，不把首次命令算绿灯。Web新增BigInt编译错误已修，最终75项全在本批外的组件域。Node有既有MODULE_TYPELESS_PACKAGE_JSON warning，未为消警告改package.json。

原始输出在本会话工具结果，未假称落盘完整日志。磁盘7.7~7.8GiB<8GiB门槛：未跑全量、未Next build/浏览器验收、未清缓存。未新增依赖，未额外联网audit。

## 质量判定和下一步

按code-quality-checker检验：本批无发现新的权限/SQL/凭证/媒体写漏洞；同源CSRF、session唯一cookie、server bearer、拒x-ka自报、status与requestId一致性、exact16MiB、超时及安全错误均有测试。核心覆盖>80%。这只是本批候选质检，不替代arch全量门禁。

前端资产规范已读。本批只有服务端BFF，不引入UI资产、视觉/响应式/主题/动画/组件变化，截图与视觉评审不适用；不冒称前端页面验收通过。全量build/类型门禁仍需依赖和交界fixture收口。

- D6的Domain→Service→PG→HTTP→BFF成功态软件接线已完成（仅合成provider验证）；内测按arch继续无真实Provider返回503，confirm/execute保持关闭。
- 请arch协调be2修4个Worker旧fixture，并由前端线补当前工作树缺失依赖；不得把本批定向测试外推整包可部署。
- 后端总目标未完成：继续R010a2公开详情/decision、其余已冻结功能和R011/R012/R015/R010b；迁移/字段/fixture待裁决项仍在台账，不能用D6代替整体完成。
