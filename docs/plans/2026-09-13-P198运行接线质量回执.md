# P198 运行接线质量回执

状态：代码候选已保存，本批全量门禁通过，待arch独立复审。不是P198完成证明，不是部署/真实钉钉发送证明。

## 版本与范围

- 代码 `13dea23a`，20文件、536增/5删；依赖仓储 `d66f5dbc` 与HTTP模块 `2262302b`。
- 同步主管 `main@ab83a789`，合并 `69f86e65`。只同步主管前端与文档；本代码提交对apps/web、packages/contract零diff。冲突仅三份信箱/台账，按批准并集保留双方原文。
- Domain私有上下文shape；同空间active workspace/user的DM映射；固定三类模板；受监管 `outbound:once` CLI；诊断group/dm存在状态；日报识别deduplicated；ENV/runbook。
- child只继承当前空间群凭证与必需运行配置，不继承别的空间群、ETL身份、trigger token或NODE_OPTIONS。SQL参数化、输入/响应有界；模板不包含原始error/任意URL。

## 本轮实证

| 门禁 | 实际结果 |
|---|---|
| Domain全量 | 107文件1714项；typecheck/lint通过 |
| DB全量（独立新合成库） | 165文件1891项；typecheck/lint通过，含迁移回放与新增上下文3项 |
| Worker最终定向 | 4文件60项，通过；typecheck/lint通过；`output/p198-wiring-focused-final.log` |
| 配置/路由覆盖率 | 100%行/100%函数/90.8%分支；12项测试，最后补了未知run/step/media断言 |
| Web typecheck | 代码提交前与合main后均通过（不是后端自行实现视觉） |
| Worker依赖audit | 离线缓存结果0漏洞；不是实时远端advisory查询 |
| Worker全量首轮 | 1失败、2548通过、2外部opt-in跳过；不算全绿，日志保留 |
| Worker全量R2 | 69f86e65上215文件2549项通过，2外部opt-in跳过；typecheck/lint通过；`output/p198-wiring-worker-full-r2.log` |
| 独立入站网关回归 | 8文件36项通过（含2项合成PG）；typecheck/lint通过 |

命令：各包 `npm test -- --run && npm run typecheck && npm run lint`；Worker/DB使用显式 `TEST_DATABASE_URL`。DB新库 `ka_be_r010_p198_wiring_db_test`，Worker另库 `ka_be_r010_p198_wiring_worker_test`；不在Worker遗留数据上回放DB降级，不删旧失败证据。

真实PG+运行入口测试覆盖：去重产生的审计行进入日报且at为null；同空间解析/外空间隔离/重复或inactive映射拒绝；无群与未知kind只失败该行、inbox不动；无Qihang/KA凭证真实CLI启动；错误参数固定输出；监督超时后子进程实际不存在；进入PG锁等待后失去父IPC，在1秒内退出。

## 红→绿与未完成项

- 诊断缺group/dm与日报拒绝deduplicated分别先红再修。
- 第一轮本机连接被工具沙箱EPERM拒绝，9项未执行；获准本机合成测试权限后实跑，不把跳过算通过。
- 孤儿最初只置AbortSignal，PG阻塞不会退出，真实反例返回still_running。改断IPC立即exit1并限制查询预算；sending留租约供unknown恢复。测试清理最初误等主动断IPC后的close，改观察exit；冷启动与退出计时随后分离，beforeEach先确认PG锁等待，再在主体限定1秒退出，全局test/hook超时未改。
- 首轮全量中的失败是上述旧启动观测版本，修后定向60通过，随后R2完整2549通过；没有拿定向抵全量。
- P198仍缺主管最新点名的**既有worker HTTP入口接线**：需要防HTTP外层锁与child同锁互锁，不能以CLI替代。随后才进入P193等既定队列。
- FE日报镜像/文案需要接 `status:"deduplicated",at:null`，显示「已去重（同内容已发）」；媒体写继续关闭。
- 不push，未使用真实凭证、未真实发消息、未部署；不承诺exactly-once。
