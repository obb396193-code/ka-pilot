# R010a2 废弃工作项静音写入封锁

## 冻结依据与范围

- `schema.sql:457`、arch P005：户级静音放 `account_mutes`，`work_items.muted_until` 废弃不再写；旧历史值仍可读，不清除历史证据。
- 现场 `WorkItemRepository.transition()` 仍在 ignore 分支写旧字段。先关掉这一条错误写路径；不能把一个工作项静音伪称全账户静音。
- 本子批不新增公开 DTO/HTTP、无媒体写、不实现尚未明确的天数边界或规则抑制语义。

## TDD 计划

1. 增加单测：旧 `mutedUntil` 非空输入在连接数据库前拒绝（包括运行时非法值）；普通 ignore 只写忽略原因与终态，历史静音日期保持不变。
2. 以专用错误标记旧路径不可用，移除 UPDATE 中的旧字段赋值和参数；不静默吞掉旧静音请求。
3. 更新真实 PG 反例：无日期 ignore 正常；旧非空日期拒绝且工作项不改变；历史日期不覆盖、不清除。
4. 跑 DB/Worker/Domain 回归、typecheck/lint、核心覆盖、安全扫描和隔离 PG；拒连就明确未验证，不用 mock 替代。
5. 独立代码 SHA + 回执 P096。向 arch 请求 `days` 的业务日/含尾规则、静音作用域以及账户 mute 响应冻结；随后继续其他合法任务。

## 当前状态

代码 `5940032`，candidate / claude_review_pending；未合流、未部署、未开放写接口。没有更新共享工作台账或 Contract。

## 质量与证据

- 红测10失败/22过→修复后32过。核心Repository行99.05%、分支90.74%、函数100%。`/tmp/ka-mute-red.log`、`/tmp/ka-mute-coverage.log`。
- DB纯逻辑379过：`npm --prefix packages/db test -- --run unit.test.ts window-assessment-counts.test.ts window-assessment-boundary.test.ts semantic-dimension-boundary.test.ts`，`/tmp/ka-mute-db.log`。
- Domain全量710过/10旧P073 fixture失败；Worker `--run --exclude '**/*pg*'` 1099过/2真实源opt-in跳过。`/tmp/ka-mute-domain.log`、`/tmp/ka-mute-worker.log`。没有把Domain写成全绿。
- 三包typecheck/lint最终通过；首次DB检查发现测试显式undefined不符合exactOptionalPropertyTypes，改为缺省键后重新全跑。`/tmp/ka-mute-types-lint.log`。
- 隔离合成库 `ka_be_r013_20260906` 的work-item-repository PG测试在beforeAll报 `ECONNREFUSED 127.0.0.1:55432`，9例未执行。`/tmp/ka-mute-pg.log`。未改用共享数据库或启动服务。
- `npm --prefix packages/db audit --offline --omit=dev` 0 vulnerabilities，仅缓存审计，不当作在线更新检查；`git diff --check`通过。
- code-quality-checker检查：参数仍绑定SQL、不新增秘密或日志、不回显用户日期，旧路径拒绝在连接前；更新减少一个字段/参数，不增查询或锁。事务/旧历史证据用单测覆盖，但真实PG证据待补。无新增依赖/迁移/前端改动。

## 待arch裁决 / 后续实现边界

1. days=1/3/7是严格允许值还是UI快捷项？`muted_until DATE` 按自然日还是上海03点业务日计算，截止日包含与否？不能从DATE推断时间边界。
2. 静音仅压通知还是同时不生成P1/P2/机会工作项？P0突破已明确；不得把户静音混成规则静音，也不能随意消触旧工作项。
3. 账户mute公开响应结构与ignore附带mute的原子要求请冻结。建议同事务忽略+写account_mutes，任一失败均回滚；仅建议未先造契约。
4. 本批内部 `ACCOUNT_MUTE_REQUIRED` 不属于新增公开error envelope；尚无公开写路由暴露。整户静音存储/规则读取/HTTP尚未交付，不能把本修复称作户级静音功能完成。
