# 自查05 小时采样原子落库质量回执

代码 **da17ce0a**，10文件498+/97-。边界：Domain私有存储校验、DB事务仓储、Worker既有纯函数兼容重导出；没有HTTP/消费者/真实凭证/媒体写。未合流、未部署。

## 变化与证据

- 50账户/批，输入JSON字节≥16MiB拒绝；字段严格类型、工作空间/媒体/账户/日期/小时/run一致。输入在第一次await前复制，防外部调用方异步改对象改变写入范围。
- 原normalizeAccountHourlySample机械移至Domain（逻辑不变），Worker保持原导出；DB与Worker用同一函数复核Raw和normalized值/时间。新增「Raw消耗0、normalized99」曾实红，再修为拒绝，不只验账户归属。
- DB短事务锁真实job/run和workspace/user，核对running incr、lease token/到期、attempt、owner active、personal active、冻结ds/accounts/hh；提交前重新查租约时钟。没有服务身份或他人凭证fallback。
- 参数化Raw+hourly两笔同事务；source与workspace/media均受可信批次约束，Raw保留hh/业务参数与真实owner，不写userId凭证。缺行不造0；已存同号跨workspace/media数据不受影响。
- 小时快照五键upsert，新sampled_at才允许更新且last_sync_time不得倒退；重放不会重复快照，Raw追加保留历史。未实现run完成/定时器，调用层仍需记录缺行/observations；此仓储不声明整体coverage。
- Raw之后故障、写完租约过期，实PG证明两表均回滚；rollback失败丢弃连接；异常只固定文案，不透出SQL/body/身份。

## 门禁原始摘要

执行目录本人worktree；数据库只使用 `ka_be_r010_p194_test`（合成fixture、随机workspace，非共享ka）。

| 命令/范围 | 实际结果 |
|---|---|
| Domain `npm test -- --run test/account-hourly-storage.test.ts` | 初次缺文件红；后33/33；新增Raw矛盾测试先32绿1红，再33绿 |
| Domain同命令加 `--coverage --coverage.include=src/account-hourly-storage.ts` | 行/函数100%，分支89.18% |
| DB `npm test -- --run test/account-hourly-write-repository.test.ts test/account-hourly-read-repository.test.ts --coverage --coverage.include=src/account-hourly-write-repository.ts` | 2 files / 73 passed（24新写+49读），3.22s，行/函数100%、分支89.47% |
| Worker `npm test -- --run test/account-hourly-sample.test.ts test/qihang-account-hourly.test.ts` | 2 files / 69 passed；证明迁移纯函数后行为未变 |
| Domain/DB/Worker `npm run typecheck`、`npm run lint` | 三包均退出0 |
| DB `npm audit --omit=dev --offline` | 0 vulnerabilities；仅缓存证据，不称联网最新通告 |

首轮PG20绿1红在fixture把job改done但没有清lease，被既有jobs_active_lease_guard拒绝；修成与真实终态相符的done+lease NULL后，73全绿。没有删除/绕过约束。

质量分类：新增阻断项0；保留已知边界——没有模拟提交成功但响应丢失的网络不确定性，重试允许Raw历史重复、快照不重复；没有全量五包门禁（磁盘约3.6GiB按arch规则不build/全量）；未真实OS执行。

## 新裁决接收与下一步

工作中收到main d2ab7ab9：v1.9.24页面业务日readiness已裁、三调用点hunk已授权；v1.9.25迁移号只增，025可单发，dispatches届时026或以后。已更新025代码注释，覆盖自查04的旧发布阻断。本批收口后按新序先readiness→P176 Runtime→P211，不继续扩小时job。

技能影响：writing-plans让新边界逐项红绿；code-quality-checker补原始/整理值矛盾、异步输入突变、回滚连接销毁等反例。Coze仅检查适用性，云端项目/源码上传不在授权范围，未调用。
