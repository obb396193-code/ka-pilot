# P170 维度来源取数 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 v1.9.3 透视表来源改造先提供同快照、三键限权的解析事实读取；不把内部 reader 当成公开功能完成。

**Architecture:** 复用 018 的 account_name_parses，单次参数化查询只读批准的账户 tuple。reader 接收现有 RR/RO connection，返回原始解析/人工覆盖/冲突事实，不抢 be2 的 T5 resolver、不替 arch 决定混合来源分组规则。

**Tech Stack:** TypeScript、pg、现有 Domain parse schema、Vitest、真实 PostgreSQL。

---

基线 be/r010@92b5d24，已含 arch eae4c60。writing-plans 已读取；执行子技能当前不可用，按用户持续执行授权在本 worktree 逐步 TDD。生图取消、前端视觉/Contract/be2 所有权文件不动。

## Task 1：先封住待裁决语义

- 追加 `docs/relay/inbox-arch.md` P170：同值混合来源能否返回 null，还是拆分 key；account/task/biz 行来源含义；现有 agent_type 三枚举与昵称中文裸值映射；fixture 升级范围。
- 在未裁之前不把现有 v3 strict schema 改成 optional source，不通过全填 null 绕过真实来源。

## Task 2：内部读取与测试

**Files:**
- Create `packages/db/src/account-dimension-evidence-repository.ts`
- Create `packages/db/test/account-dimension-evidence-repository.unit.test.ts`
- Create `packages/db/test/account-dimension-evidence-repository.test.ts`
- Modify `packages/db/src/index.ts`（仅追加本人导出）

1. 写失败测试：空范围不访问数据库；三键不匹配/重复/多返回/坏 JSON 拒绝；解析缺失与冲突不能变成默认来源。
2. `cd packages/db && npx --no-install vitest run test/account-dimension-evidence-repository.unit.test.ts`，记录缺实现红灯。
3. 实现 `load({workspaceId,accounts})`：最多1000个非重复 tuple；以 accounts 主表为存在性事实，LEFT JOIN account_name_parses 的三键；LIMIT 1001；源 JSON 字节总额 SQL 哨兵及结果 exact16MiB 拒绝。无任何 fallback、token、任意 SQL 或媒体调用。
4. 真实 PG：同号跨 media/跨 workspace、重复回放、未解析账户、人工 override 不覆盖原 segments、RR 快照内并发修改不可见、非法存储形状 fail closed。
5. 运行定向、覆盖率、DB typecheck/lint/offline audit；磁盘足够再跑全量，不引用旧数字冒称本批通过。

## Task 3：交审与公开接线边界

- 独立路径限定代码 SHA；报告附真实 PG / unit 原始摘要和未完成项。
- 更新 `docs/plans/R010-状态.md`，信箱回执。
- 得到 Task1 语义裁决和 strict fixture 后，另批接公开 dimension/v3 + BFF parity；本批不宣称已对用户展示 source。
