# Workspace Source Routing Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** R009#8按服务端Session绑定personal→platform/team→ka_data，普通输入不能选源，对账只走独立entitlement端点，BFF不再注入旧dataView。

**Architecture:** 先分离普通/诊断严格请求及纯路由策略，随后接Service/HTTP和团队KA reader，最后BFF及双空间真实PG。每子批独立SHA；中间纯内核不称路由上线。部署共享reader须绑定指定team workspace，不能仅凭任意team Session跨空间访问该凭证的数据。

**Tech Stack:** TypeScript/Zod、现有SessionAuthService、Registry、两DataSource、Next BFF、Vitest/PostgreSQL。

权威为main@cda3303的DATA-ROUTE-001、R009及A-001。@writing-plans / @code-quality-checker。老板已选择本会话连续执行；executing-plans不可用，按现有独立工作树逐步实现，不另派写文件agent。

## Task 1 — 严格请求和路由策略内核

- 修改`packages/domain/src/data-query-contract.ts`：新增ordinary与admin两种严格请求；普通5个Query ID、诊断仅reconcile.account_daily；顶层只有queryId/params，不接受dataView/data_view/scope/entitlement。旧入口在Task3统一替换，过渡期不宣称新HTTP已生效。
- 新`apps/worker/src/data/data-source-routing.ts`、`test/data-source-routing.test.ts`；纯策略输入仅可信Auth+部署policy，返回selectedSource/workspaceKind/reason，错误为稳定code，不携payload。
- TDD：personal→platform（KA关也可用）、team→ka_data、team KA关503语义；ordinary reconcile拒绝；所有role无entitlement拒绝诊断；flag off、KA off、wrong tuple拒绝；浏览器多余字段400。先`npm test -- --run test/data-source-routing.test.ts`红，再最小实现到绿。

## Task 2 — 团队源与来源身份

- 修改`apps/worker/src/data/ka-data-client.ts` / `query-registry.ts`及tests：团队只读执行server配置绑定的workspace，不能从个人grants推成全量；media过滤与业务账户过滤固定模板。不接受浏览器SQL/共享reader。
- 团队查询覆盖未知不可编造requestedObjects；强制source metadata来自真实source，`lineage.workspaceKind`来自已批准Session，不靠上游自报。KA关闭/地址失效503且不回退platform；账户级三键继续由可信adapter注入后守卫。
- 新配置名称和多团队映射边界交arch留痕；一期单共享reader可绑定一个team workspace，未配置明确unavailable，不以任何team身份默认放开全量。

## Task 3 — Service / HTTP唯一接线

- 修改`query-service.ts`：execute ordinary与executeReconcile分开，策略由部署依赖注入；移除浏览器dataView的驱动，guard源输出三键/预算/版本，审计仅selectedSource/reason/requestId。
- 修改`http-server.ts` / `data-api.ts` / `data-api-config.ts`：注册`POST /api/v1/admin/data/reconcile`；Session+internal bearer不变，诊断policy下移Service禁止旁路。移除旧applyDataQueryAccessPolicy强塞platform路径；普通额外dataView明确400，不静默删除。
- HTTP测试重写旧视图选择happy path：登录personal/切team/旧token401/伪造x-ka不改变scope/logout后401；非法Query/params、各role无entitlement、flag off、KA off、requestId/16MB exact边界/405/超时全回归。

## Task 4 — 非视觉BFF与v2消费

- 先读`apps/web/AGENTS.md`必读资源（不引新UI或视觉）；修改`apps/web/lib/data/{canonical-query-rows,contracts,adapters,bff,client,fixtures}.ts`中实际需要的非视觉部分及tests，api路由按已冻结派工。
- v2只解包available，missing/error状态不丢；CPA直接用RatioValue，不能前端重算；strict fixtures对照后端文件，不再仅用自有v1样本自证。
- BFF普通只发queryId/params，按服务端Session workspaceKind核对响应mode，不接受浏览器自报workspaceKind；不能为去dataView而放开reconcile。若现页面调用还携旧参数，仅改非视觉client统一清理自己的构造，外来HTTP仍400。

## Task 5 — 验证与审查回执

- Domain/Worker/Web全量+type/lint；真实PG恢复后串行DB全量及WorkerPG+HTTP；当前55432拒连只能non_pg_verified，不降低PG门禁。
- SQL/凭证/16MB/跨空间静态审核、核心coverage>80%、依赖audit；diff/frontend视觉0检查。
- 代码/留痕独立SHA，P044继续回执，接双空间→折011→merge main。任何中间可测试函数均不冒充#8整项完成。

## 执行现场（2026-09-05 21:58）

### 22:21 增量

- Task3可独立部分先于Task2完成，代码9b7968b：普通/admin HTTP已接Service策略，旧dataView入口移除；Domain514、Worker非PG683+2skip，type/lint通过，75核心定向行90.89%/分支89.47%。
- Task2受信reader映射/lineage与Task4 BFF仍待完成，不能独立部署中间状态。PG55432实测拒连；集成旧team platform断言改为KA关闭503，未用假KA模拟完整团队源；启用KA的双空间测试仍是独立待办。

- Task1代码`7ced49d`：普通/诊断两个新增严格schema + selectDataSourceRoute内核。新22测试，核心行/分支/函数100%；Domain514、Worker非PG670+2外部skip，两个包type/lint通过。无依赖/前端/DB变更；PG仍不可用，未以非PG门禁替代。
- **HTTP还没接本内核**，旧dataQueryRequestSchema/HTTP applyDataQueryAccessPolicy尚待Task3一起移除。未称#8完成，不可单独作为对外路由交付。下一实际动作Task2团队源（需要server部署workspace绑定），再Task3/4切换并回归。
- 读取主仓私有资料`ka-src-0011/source.txt:13`确认服务允许单条SELECT/WITH、写/DDL/PRAGMA拒绝。因此上批CTE属于文档支持范围；本机真实SQLite已通过，内网新模板端到端执行/性能仍需OS，不再把WITH支持本身列为未知。
