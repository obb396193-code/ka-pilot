# R010a1 任务 / 业务维度公开窗口质量报告

## 范围

- 基于 arch main@cfb0d50（含 P110、56fd109 完整 source fixtures），仅个人 task/biz 维度 account.dimension/v3。原 account 维度、summary/trend/table 保持兼容。
- DB loadByAccount 添加当天有效 taskId 与任务主表 bizName；LEFT JOIN 保留孤儿 task relation。bizName 来自当前任务主表，不声称已有业务名称的历史版本表。
- PlatformDimensionQuery.group 同一 RR/RO 连接：lineage、dimension（含一条 overlap probe）、批量 effective history，共四条 SQL，不按账户 N+1。跨日任务变化按日期分组；缺日/缺价保留未知；cashCost/realConversion 两侧合计核验，再按 Σprice(d)×conv(d) 判达标。
- 唯一 Registry 放行 task/biz，31天窗口、1000批准账户、10k哨兵及exact16MB继续有界；不因拆多个任务组虚增 lineage.returnedAccounts。
- 公共 dimension 行 / BFF 沿用 arch v3 DTO。DB/internal 仍按 workspace/media/account 三键验证，来源限定 personal explicit_accounts；team 无live fallback。没有改packages/contract、视觉或媒体写。

## 质量检测与实证

执行 writing-plans 的先计划/TDD，code-quality-checker 的安全/性能/覆盖/失败记录。executing-plans 不可用，主会话执行，未派子角色。

- DB RED：3项失败（新task/biz字段缺失与非法类型未拒）→18项通过（含真实PG3项）。同ID跨media/workspace、空grant、任务换绑、主表缺行均实证；未删除FK或排他约束。
- Worker RED：13项缺group方法→实现；自审追加zero-account-count：有observed行却账户数0可穿透，RED实证后修为fail-closed。
- 最终组合定向：14 group +8 account，22/22；核心行/语句95.52%、分支91.34%、函数90%。未覆盖行主要为生产factory，另由真实PG8项覆盖。
- PG→Session HTTP：account/task/biz均读真实合成库；伪造workspace/全scope头不能扩大范围，跨workspace金额900和未授Tencent不混入。两媒体同时获授时任务组现金1822，单媒体换绑前后为22/3。空scope为空；缺日为missing，孤儿任务标签null；重叠关系由真实PG exclusion constraint拒绝。
- 实际Web BFF→loopback HTTP：summary/trend/table/account dimension/task dimension/biz dimension六种通过；这条使用合成auth/data ports，**不能与PG→HTTP组合冒称真实登录/真实上游端到端**。
- Domain 直接读arch补齐的完整维度source，不再合成unknown metadata。原异常scope/错维度/坏数/重复组/exact响应字节回归仍在HTTP56项中。
- SQL参数化；无凭证、SQL文本、上游body进入public输出；无新依赖。三包production offline audit0，只是缓存审计，不声明在线fresh漏洞检查。
- F-P110-1独立SHA **6738178**：仅两个重用例30s，保留10k额外行拒绝与12迁移回放所有断言，不改全局timeout。

## 中间失败记录

- 首轮 Domain 769过/1超时（10k行5467ms > 默认5s）；无改断言重跑770过，后收到arch F-P110-1才单独设30s。
- 本批最初测试错误写price为数字，实际Contract是{value,effectiveDate}；已按冻结结构修用例。PG排序按cost/key而非现金大小；改按key核对，并未改生产排序。
- PG重叠测试造不出非法行，因为现有exclusion constraint先拒绝；改验证23P01及原数据不变，未卸约束。内存恶意reader重复日另有反例。
- typecheck发现Zod issue跨类型不匹配，转为相同path的custom issue；lint发现unused destructuring，改显式选择输入。不关闭lint/type规则。
- 第一轮Worker全量1188过/1红，命中开发中新增的zero-account-count反例；修复后全套重新串行，不把局部绿当最终全量绿。

## 全量门禁

代码SHA **18fdebd**（12文件257+/35-），依赖独立测试修复 **6738178**。固定合成库 `ka_be_r010_20260907_test`，不新建库、不用共享ka。Domain/DB/Worker/Gateway相关测试串行；Web非视觉测试单独。

| 门禁 | 最终真实结果 |
|---|---|
| Domain | 58文件770/770 |
| DB | 70文件710/710，含真实PG，155.71s |
| Worker | 118文件通过+2外部opt-in文件跳过；1189过+2跳过，含真实PG/HTTP，235.60s |
| Gateway | 8文件36/36，含真实PG2，5.38s |
| Web非视觉 | 143/143 |
| Domain/DB/Worker/Gateway typecheck/lint | 通过 |
| Web typecheck/lint | 既有FE缺依赖，typecheck未通过；lint0error12warning |

命令各包 `npm --prefix <path> test -- --run`，PG包显式 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test PGCONNECT_TIMEOUT=3`；允许loopback权限。末轮代码与测试不再改动，DB→Worker→Gateway同一串行命令exit0。

- 日志：`/tmp/ka-p111-{domain,db,worker,gateway,web}-final.log`，coverage为`ka-p111-dimension-coverage-final.log`。前缀ka-p107的文件为早期运行，保留失败证据，不能冒充最终。
- Web typecheck本机仍失败：合流前端已有shiki/ai/ReactFlow/BlockNote等依赖缺失；lib/data无诊断。未npm ci、未改前端隐藏错误。Web lint0 errors/12已有warnings。

## 未完成 / 交审边界

- 本批只接个人任务/业务维度，不等于全部R010a1。其他维度、team快照、pivot2、health/ETL、a2、013与其余信箱仍未完，不宣称上线或媒体生产验证。
- 1000户×31日历史>10k会显式拒绝，未证明该容量；后续优化需同快照有界聚合，不能静默截断。
- arch最新main@63a5fd8的OS八条/R013b HTTP trigger、R011 sourceBatch/stability、R012 bid_tool已只读收到；不混进当前维度批，后续按新契约读取实施，不再写“f.yml未知”。
- 收到arch cfb0d50交审冻结要求：最终回执提交后不再往be/r010增加提交，直至✅/❌。总目标仍active，未将剩余任务裁剪或标完成。
- R-FE-IMG-002已生成三个浅色方向，路径/完整提示词见同日候选记录；1672×941为模型实际尺寸，正式2400×1350和视觉批准尚未完成；未改public/提交图片。
