# P177/P178 审查退修回执

## 独立提交

- **495ea1c9 / P177**：仅`qihang-protocol-pg.integration.test.ts`，纠正跨时钟“立即重领”测试假设，新增Node领先60秒反例。
- **16679e7a / P178**：9个测试套件的10个库名guard由`ka_be_*_test`放宽为arch冻结的`ka_*_test`，保留localhost/127.0.0.1:55432；新增10项静态防回退/库名正反例。覆盖原P170–174七套件及P175诊断两套件。
- 两笔均为测试修复，不改生产重试/权限/读写开关，不push。main已合入本人分支的同步SHA为**2534d684**（合main69b1582）；合并只对DB三个独立导出与信箱追加区人工保留双方。上游FE naming-tab已有EOF空行未越权改动。

## P177 复现与证据边界

1. 当前分支原用例单跑1/1过；`git diff 400b63e1 c40755a8 --` 检查consumer、job-repository、full-handler、QihangClient与该测试，**这些文件零diff**。该测试未调用P175 supervisor/child。
2. 原consumer用Node `now()`生成retryAt，PG `leaseNext`用DB `now()`判断到期。加入Node领先60000ms，保留原立即第二次processOnce断言：0ms通过、60000ms稳定失败，正是`expected false to be true`（原第二次领取行）。
3. 修后先断言未来retry仍queued/attempt1、source调用仍4次、不提前领取；随后在本例id/workspace/status/attempt限定下用PG clock把retry标为已到期，再验证原job领取、attempt2、done、两个ETL attempt历史与安全诊断。
4. 修复属于测试时间前提明确化，**不能断言arch那次失败一定是此原因**：未取得那次失败时的双时钟快照。请arch在原独占库exact-SHA复验；若仍失败，应回传具体断言行/该合成job的run_after与DB now，而不是猜IPC。
5. 本机只读时钟探针当时DB相对Node中点约+40.5ms；这是一次快照，不代表arch失败当时/未来时钟同步。

## 最终实跑

| 范围 | 结果 |
|---|---|
| P177 Worker六文件（协议+实际once重试+HTTP/CLI+安全码） | 32/32，Worker type/lint exit0 |
| P178 guard先红 | 9红1过；改后10/10，另修静态测试误把pathname赋值当guard的误报 |
| P178 新CI前缀独占库 DB五文件 | 48/48，真实PG/CLI |
| P178 同一独占库Worker四文件，DB结束后串行启动 | 7/7，真实PG/CLI（含故意不存在DB的失败诊断） |
| DB/Worker typecheck/lint/offline production audit | 均exit0，缓存audit0，不声称在线最新advisory |

专用库为**ka_ci_be_r010_p178_test**（localhost:55432），本轮新建、本人独占，不共用arch/be2测试库。新库跑了迁移；各套件按随机workspace清理自身合成记录，不清全库/全队列。数据库保留供后续复验，无真实凭证/业务记录。测试守卫扩大的是命名兼容，不授权到生产主机。

```sh
# P177 apps/worker，TEST_DATABASE_URL=本人旧专用合成库
npx --no-install vitest run test/qihang-protocol-pg.integration.test.ts test/worker-once-retry-pg.integration.test.ts test/worker-once-failure.test.ts test/worker-once-http-pg.integration.test.ts test/worker-once-diagnostic-cli-pg.integration.test.ts test/qihang-protocol-retry.test.ts --hookTimeout=30000
# P178 packages/db，新CI前缀专用库
npx --no-install vitest run test/account-dimension-evidence-repository.test.ts test/qihang-identity-seed.test.ts test/qihang-job-recovery.test.ts test/scheduled-job-replay.test.ts test/worker-once-diagnostics.test.ts --hookTimeout=30000
# P178 apps/worker，同一库，DB套件完成后运行
npx --no-install vitest run test/qihang-protocol-pg.integration.test.ts test/worker-once-retry-pg.integration.test.ts test/worker-once-identity-recovery-pg.integration.test.ts test/worker-once-diagnostic-cli-pg.integration.test.ts --hookTimeout=30000
npx --no-install vitest run test/pg-test-database-guards.test.ts
# 两包分别
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

## 后续

- 本机余量约5.7GiB，低于8GiB全包门；没有宣称全包绿，未清缓存、未覆盖他人现场。P175诊断仍等arch退修复验，不冒称合流/部署。
- P176失败ledger内核已交，缺数读取与Full/Incr接线仍未完成，下一批继续，尚不能把单批失败当done交给OS。
- main已带be2 020/identity-password-repository，F-OS-004的仓储依赖已解除，后续按队列接管理员初始密码/重置；不是继续等旧阻断。BI001/002、021/hourly、Gap、source仍未完成；BUC/Pod仍须相应真实条件。
- 生图取消持续，全信箱目标active，不等待Claude审完才推进可独立项。
