# P194 纯忽略 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. 当前技能不可用，按本计划直接执行TDD，不另起会话。

**Goal:** 按arch v1.9.44让不带mute_days的忽略真实落库且不静音；两分支返回真实工作项ID与持久化时间，贯穿Worker/BFF。

**Architecture:** 复用AccountMuteRepository既有工作项锁、live授权核验和短事务；同一内部命令按可选mutedUntil决定是否写account_mutes。不新增队列/媒体操作。Service按请求选择strict结果；BFF按请求分支校验，不接受错对象或伪静音。

**Tech Stack:** TypeScript、Zod、PostgreSQL、Vitest、Next BFF node:test。

## 依据与边界

- main@94e9dc25、api.md v1.9.44③明确两份DTO。已有ignore+mute实际只回mutedUntil/scope，需一起补workItemId/status/ignoredAt；回执声明发出形状变化。
- ignoredAt来自UPDATE RETURNING resolved_at，不用HTTP返回时刻冒充落库时间。重复终态仍409，不产生第二次静音/审计。
- 不改变原有personal+显式账户写权限；team/viewer/撤销grant拒绝。无账户私人项本批不扩写权限，沿用既有命令边界。
- 纯忽略既不新建也不改已有account_mutes；不改变legacy work_items.muted_until。事务中记录审计，审计或静音失败整体回滚。
- 只改account-mute命令和获准BFF schema/handler/tests；不碰be2 query/ETL。

## Task 1 — strict wire红绿

- Create `packages/domain/src/work-item-ignore-wire.ts`（无相对.js import，Web可直接读）及 `test/work-item-ignore-wire.test.ts`；index导出。
- 测plain无静音字段、有静音必须齐全、非法日期/对象ID/额外scope/无效天数拒绝；测试先因缺export红。
- 两个结果schema共享`{workItemId,status:ignored,ignoredAt,reasonChip?}`，with-mute增加mutedUntil/scope。请求`{mute_days?:1|3|7,reason_chip?:string}`。
- `npm test -- --run test/work-item-ignore-wire.test.ts`预期全绿。

## Task 2 — DB/Service/HTTP红绿

- Modify `packages/db/src/account-mute-repository.ts`、`apps/worker/src/work-items/account-mute-service.ts`、`src/r010/account-mute-routes.ts`。
- 新PG HTTP反例先跑：plain {}当前503→预期200；实际持久化resolved_at等于响应ignoredAt；mute表行数/旧记录不变。
- Repository共享事务中UPDATE work_items并RETURNING resolved_at；条件写mute；audit同事务；返回内部tuple/真实时间/mute|null。Service不能接受与请求分支不一致的repo结果。
- 修旧mock补真实Date字段，不放宽输出校验；补错workspace/id/tuple、坏日期、audit失败回滚、重复终态、撤权、同ID跨媒体/空间。
- 真实HTTP继续测bearer+session、requestId、405、exact16MiB；后端没有新路由注册，现createAccountMuteRoutes继续接管。

## Task 3 — BFF两分支

- Modify `apps/web/lib/data/r010-command-contracts.ts`、`r010-command-bff.ts` 与测试，零视觉改动。
- 共享domain wire schema。mute账户路径仍仅旧mute响应；ignore必须按请求有无mute_days挑对应shape并核对path workItemId。
- 真实PG→HTTP输出保存 `packages/contract/fixtures/work-items/ignore-plain.json` 与 `ignore-mute.json`（只用合成身份与已捕获真实响应，不手造时间），用BFF永久parity测试读取。

## Task 4 — 交付

- Domain/DB/Worker全量tests/typecheck/lint；DB迁移全量用新空合成库，Worker在其后跑，避免前轮残留污染；Web npm test/lint，依赖不齐的typecheck如实记录。
- 使用code-quality-checker复查安全、事务、性能、分支覆盖与依赖；无真实发送/媒体写/运行生产worker。
- merge main→路径限定代码SHA→质量回执/两个信箱独立SHA；不push。change-log迁移与查询作为下一独立批次，不混纯忽略。
