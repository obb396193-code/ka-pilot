# P205 账户小时采样边界质量回执

代码SHA：`3065603e`。本批纯Worker适配器与测试，**未落库/未启采样job/未接公开reader/未部署**。

## 已实现

- `normalizeAccountHourlySample`消费真实Client的rows与我方observedAt，映射已实证六个账户累计指标：cost/exposure/click/conversion/realConversion/budget；不是广告求和。
- 受信workspace/media/accounts/date/hour固定，source_run_id保留十进制字符串，超过JS安全整数仍不丢精度。源自报workspace不覆盖调用方；账户越界/重复、media/date不符整体拒绝。
- 缺行返回missingAccountIds，字段absent/null保留null，0保留0；字符串数值严格有限/非负，计数必须安全整数。present-invalid不静默填0。
- last_sync_time原始无时区格式必须用显式sourceUtcOffset转TIMESTAMPTZ；输入无默认，不借进程时区或当前时间编新鲜度。部署接线时此值须来自受信source配置，不能来自浏览器。
- complete严格按冻结「采样时间≥源日该小时结束+5min」；HH23跨自然日处理，不套任务03:00日切。这个complete仅指采样时刻，不代表last_sync_time新鲜或指标齐全，公开reader仍需独立lineage/availability判断。
- hh只接受0..23；Client允许的24不会冒充小时表23。输出只含白名单，稳定错误不泄漏源body/qid/私有上下文。

## 测试与质量

- TDD先新测试缺模块红（未把它计成46行为红），实现后46绿；补不可变排序与真实QihangClient假fetch串联后**新48/48**。
- 六文件不同定向**149/149**：新sample48、account-hourly21、QihangClient38、Full/Incr18、canonical8、batch-failure16。
- Worker typecheck/lint通过；offline production audit0（缓存证据）；diff check通过。
- 新模块覆盖：行100%、函数100%、分支98.24%。剩sort比较器相等分支在重复ID先拒绝后不可达；不加ignore压绿。
- 本批没有DB或HTTP路由修改，**没有声称本批真实PG/OS源验证**；Client串联使用合成fetch响应。前批P204的40项PG套件证据不重复计入本批149。
- 安全/性能自审：有界rows≤10000、accounts≤1000、数值文本≤64字符；不执行不可信代码、无SQL拼接/外部IO、不输出额外字段。一次映射+集合校验+排序，未引入N+1；不是生产性能基准。

## 仍待完成

- 迁移编号/升级路径P201、014复合FK；F-OS-004权限/fixture P202；P176列表屏蔽与首次full恢复P203仍待arch。
- 021小时表/采样job/store原子性与幂等/批准tuple reader/factory/data-api接线仍需继续，不能把纯适配器算成小时功能完成。
- 函数输入是内部可信请求契约，**不是授权解析器**：后续job必须先解析真实身份/授权并冻结，再调用它。未新增公开DTO。
- 磁盘3.0GiB，小于既定8GiB门；未跑全五包/构建，未清缓存。未改前端/Contract/主台账，未push/部署/真实媒体写。
- 使用writing-plans与code-quality-checker实施与验收。
