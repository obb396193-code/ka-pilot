# P193 内核质量回执（未注册HTTP）

## 交付边界

- 代码SHA `0b20dfc1`，分支 `be/r010`，交付前merge本机main@94e9dc25显示Already up to date；待arch审查，未合流/未部署。
- Domain strict PATCH/PUT请求 + 单成员V195响应；未改变Contract文件。
- 新 `AdminMemberManagementRepository`：workspace-local列表、授权读取、角色/成员状态PATCH、授权整批替换；尚无生产调用者。
- 复用原治理鉴权，仅导出重命名 `requireMemberGovernance`；旧全局create/reset/list实现不改行为。
- 与上一笔相比 `apps/worker` / `apps/web` **零diff**，无HTTP/BFF开放，无媒体写、无真实凭证、无push。
- 本批为P193内核候选，**不称完整功能完成**。总列表范围、管理员如何切入其他个人空间、停用会话作用域待arch明确；试做接线已撤回，patch保存在output。

## 验证记录

| 范围 | 本轮证据 |
|---|---|
| TDD Domain | 新测试先13红（缺导出），实现后13/13绿 |
| Domain full | 102文件/1592绿；typecheck/lint绿 |
| DB 定向真实PG | 新内核10 + 原provisioning19 + unit16 = 45/45绿 |
| 新内核V8覆盖 | statements/lines/functions 100%，branches89.65%；不是整个系统覆盖率 |
| DB full | 新建空合成库后160文件/1791全部通过；typecheck/lint绿。首次复用旧测试库8条降级用例失败，见下方隔离说明 |
| Worker full | 205文件/2374通过，另2个外部凭证opt-in跳过；typecheck/lint绿。接线撤回后在p193合成库跑旧路径回归 |
| Web | `npm test`308/308绿；本批web零diff。前轮缺主线依赖的typecheck阻断未修复，不声称当前web全门禁绿 |

测试只操作本机55432的合成隔离库，定向测试先用 `ka_be_r010_p194_test`，最终DB全量改用新建空库 `ka_be_r010_p193_test`。随机workspace/identity前缀，afterAll清理本套用例数据；未连接真实内网数据。日志 `output/p193-*`；覆盖率JS是生成资源，不是漏提交的产品源码。

### DB全量第一次失败与复验

- 旧库曾供Worker全量使用，第一次DB全量1783通过/8失败，全部是既有 `r014/contract-v1-5-migration.test.ts` 降级时命中027保护：`tasks still carry v1.9.28 fields; cannot downgrade losslessly`。
- 本批代码不写tasks；没有清除旧任务字段、删除其他测试库或放宽迁移保护来过门禁。新建此前不存在的空库 `ka_be_r010_p193_test`，同代码重跑160文件/1791全绿。
- 原失败保留在 `output/p193-db-full.log`，成功复验 `output/p193-db-full-r2.log`。后续迁移全量应使用空的独立测试库，避免Worker造数与迁移降级测试互相污染。

## 自审（code-quality-checker）

- ✅ 参数化SQL，workspaceId仅取approved上下文；目标identity成员关系和账户(media,accountId)全部在同workspace核验。
- ✅ 两种媒体同号可同时授权，外空间同号不串；无效账户在写之前整批拒绝，事务/audit失败回滚。
- ✅ 授权用revoked_at留痕、重新授予服务端写created_at。没有保存session明文、密码、secret引用；audit只记录授权变更和actor。
- ✅ 空替换后**同一现有token**经真实AuthSessionRepository解析得到空账户范围；无需重新登录。成员停用候选只撤当前workspace会话，其他workspace保持有效（语义待arch确认）。
- ✅ 每workspace短事务advisory lock先于共享治理行锁，避免同空间并发自更新时SHARE→UPDATE升级互锁；target membership再FOR UPDATE。并发两次替换只留下其中一整套，不混集合。
- ✅ team不读/写grant表；team元数据grantCount=0；个人admin自报不能取代live team admin治理资格。
- ✅ 1001成员真实PG sentinel拒绝全量溢出，其他workspace仍正常；超长存储名称拒绝而非截断伪造。
- 性能边界：列表最多1001行，grant最多1001行；整批<=1000，固定SQL调用数；已测事务并发正确性，未做生产高并发压测。锁超时/死锁映射受控timeout，不静默半成功。
- 依赖清单/lock未改；`npm audit --omit=dev --offline` 0 vulnerabilities（缓存审计，不代表联网最新漏洞库）。
- 未完成：HTTP/Service/BFF、端到端浏览器撤权流程、arch对所有治理入口范围统一裁决、真实内网验收。
