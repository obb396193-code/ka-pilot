# 025 account_metrics_hourly Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 arch v1.9.4/v1.9.21 已冻结的账户小时累计表落到迁移 025，保持三键隔离、缺失值和真实源时间。

**Architecture:** 仅 DDL 与迁移回归，不改变现有广告小时表，不开放新路由。复用现有月分区维护函数，增加账户小时分区并可无损降级；运行记录分页索引同批落地。

**Tech Stack:** node-pg-migrate / PostgreSQL / Vitest。

---

## 现状与发布边界

- 起点 be/r010@76d1ca1c；本机 main@950b45e9 信箱排序先 025、后 024。024 尚不存在，025 由 arch 指定，不擅自改号。
- `src/migrate.ts` 的 `checkOrder:true` 不可关闭。**开发可以先交 025，但正式部署必须等 024 文件齐全、按 024→025 执行**；已经执行过 025 再插入 024 会触发乱序校验。交 arch 合流/部署裁定，不发明空的 024 占位。
- 此批真实 PG 验证可在专属测试库上执行当前文件集合的 up/down/up；这不证明未来 024 已验证或 025 可以先部署。
- 现有维护器为一个数据库函数，025 扩为四表，down 恢复原三表。原有表的数据/约束不改。非空小时表拒绝 down，不 DROP CASCADE。
- 当前没有 executing-plans 技能，按用户已批准的连续实现方式逐步本地执行；不另派 Agent。

## Task 1：先立迁移红测试

**Files:** `packages/db/test/account-hourly-migration.unit.test.ts`。

1. 用 createRequire 读取 `025_account_metrics_hourly.cjs`，收集 up/down SQL。
2. 与 schema.sql 的账户小时表完整 DDL 去注释空白后对拍；核对唯一 025、0..23、三键 FK、五键 PK、nullable 指标、必要时间列。
3. 检查分页索引、四表分区、超时、非空降级拒绝、旧维护函数恢复、不改旧数据。
4. `npm test -- --run test/account-hourly-migration.unit.test.ts` 应因缺迁移红。

## Task 2：最小 DDL

**Files:** `packages/db/migrations/025_account_metrics_hourly.cjs`。

1. up 设置 lock_timeout=5s、statement_timeout=5min，创建冻结表和 `etl_runs(workspace_id, started_at DESC, id DESC)` 索引。
2. 更新月分区函数；创建既有策略的过去三月至下月分区，后续复用 `ensureMetricPartitions`。
3. down 在丢表前检查无数据，恢复三表函数，删除本人新增表/索引，不改变其余数据。
4. 重跑 Task1，DB typecheck/lint。

## Task 3：真实 PostgreSQL

**Files:** `packages/db/test/account-hourly-migration.test.ts`。

1. 套件自己 runMigrations，不依赖别的套件；使用显式 TEST_DATABASE_URL，只允许测试库，不读取真实凭证。
2. 用随机 workspace 测同 workspace 跨媒体同号、跨 workspace 同号、孤儿 FK、0/23 与 -1/24、重复五键、null 数值保留、源时间和采样时间不串。
3. 真实 down 非空拒绝且数据保留；只清自己 fixture，025 down/up 窗口回放，维护函数在 down/up 两状态都能创建未来分区，原三个表仍保留。
4. 定向加现有小时 reader 回归；记录测试、安全与性能风险。实际时长记录但不称在线低锁；首次/停写维护窗口执行。

## Task 4：独立交付

代码与质量/信箱留痕分提交。仅本人新增文件和 `R010-状态.md` / inbox-arch，不改公共 Contract、前端、主台账。生产部署/OS 真数据/小时 job 未完成继续明示；后续按 arch 队列推进。
