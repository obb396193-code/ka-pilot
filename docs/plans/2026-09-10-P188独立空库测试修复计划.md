# P188 独立空库测试修复 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 消除近期本人新增 PG 套件依赖其他套件先迁移的顺序问题。

**Architecture:** 使用既有 runMigrations，测试目标 localhost:55432 / ka_*_test 校验通过后，beforeAll 先迁移再建 synthetic fixture。每个文件在不同的全新空库独立证明；不改业务实现、迁移内容或 arch 已修的失败台账套件。

**Tech Stack:** Vitest、node-pg-migrate、pg、TypeScript。

---

当前从 arch main@5682a509 fast-forward；用户已授权持续实施，executing-plans 技能未提供，按本文步骤本会话执行，不派子 agent。采用 writing-plans / code-quality-checker；不修改前端和 arch 台账、不 push。生图已取消。

### Task 1：复现和审计

检查 7 套无迁移的候选：etl-batch-failure 已由 arch 修好；其余 6 套仍缺。
`etl-run-bigint-pg.integration.test.ts` 的临时表通过 `LIKE public.etl_runs` 建立，仍依赖公共迁移，不属于豁免。

空库命令（预先确认 public tables=0）：
`TEST_DATABASE_URL=<独占本地测试库> npm --prefix packages/db test -- run test/etl-run-bigint-pg.integration.test.ts`。
预期修复前 relation public.etl_runs does not exist；不通过预置表掩盖失败。

### Task 2：最小修复

修改文件：
- packages/db/test/changeset-read-authority.test.ts
- packages/db/test/etl-batch-readability.test.ts
- packages/db/test/etl-run-bigint-pg.integration.test.ts
- packages/db/test/etl-run-list-repository.test.ts
- packages/db/test/semantic-authority-regression.test.ts
- apps/worker/test/etl-runs-http-pg.integration.test.ts

DB 文件 import `{ runMigrations } from "../src/migrate.js"`，Worker 从 `@ka/db` 导入。URL 守卫后执行 `await runMigrations({ databaseUrl })`，然后 pool / fixture；同步 beforeAll 改 async；hook timeout 30_000。其余用例、隔离清理、断言不放松。

### Task 3：验证与交接

为每套创建不同且不存在的 ka_be_r010_p188_*_test 库，确认 public tables=0，分别单文件运行，记录返回码和数量；不删除其他库。Worker 用例仅启动真实 Data API 测试子进程，环境白名单无真实凭证、不启 Worker/Beat。

`npm --prefix packages/db run typecheck` / `lint`；Worker 同命令；`git diff --check`；cached production audit。磁盘不足 8GiB 时不跑五包全量、不声称全门禁通过。按 code-quality-checker 检查迁移顺序、目标守卫、清理范围、异常和依赖。

独立路径限定代码提交，随后质量回执 + R010-状态 + inbox-arch 留痕提交。报告 P188 已由 arch 完成部分与本次同类修复区别。之后继续 rerun，不自行扩大契约。
