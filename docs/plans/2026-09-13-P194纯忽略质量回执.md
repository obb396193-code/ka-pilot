# P194 纯忽略质量回执

## 范围与当前状态

- 本批仅P194①：纯忽略、忽略静音的完整DTO、既有Worker/BFF接线。change-log迁移与查询仍是后续独立批次。
- 代码SHA `ea694595`，交付前merge本机main@94e9dc25（Already up to date），路径限定提交后show --stat/--check通过；交arch待审，不称已合流/部署，不push。
- 按writing-plans分步TDD；本记录按code-quality-checker覆盖安全、事务、边界、测试与依赖。

## 发出形状变化（A40）

- 不带mute_days：原503改为真实200，`{workItemId,status:"ignored",ignoredAt,reasonChip?}`；不含mutedUntil/scope。
- 带mute_days：在实际旧响应mutedUntil/scope上补上工作项ID/status/ignoredAt/reasonChip?，对齐v1.9.44。账户单独mute响应不变。
- ignoredAt来自数据库UPDATE RETURNING resolved_at；未用Service/HTTP当前时钟代填。reason_chip未传则不回reasonChip，空字符串按契约保留。
- 两份fixture来自真实合成PG→生产HTTP shell的输出（日志 `output/p194-ignore-fixtures.log`），不是手写示例；Service日切测试时钟固定2026-09-13 03:00+08，数据库resolved_at为执行时刻，二者在用例中分别核验。
- fixture路径：`packages/contract/fixtures/work-items/ignore-{plain,mute}.json`。BFF永久parity用例读取原fixture，按其ID/requestId发请求，不改响应内容来过校验。

## 已完成验证

| 范围 | 证据 |
|---|---|
| TDD | Domain缺wire模块先红；真实PG/HTTP最初3红（两条plain503及一条mute缺字段），实现后绿 |
| Domain全量 | 103文件/1601绿，typecheck/lint绿 |
| DB定向/全量 | 定向73/73（真实PG15 + 两套unit58）；新空库全量160文件/1797绿，typecheck/lint绿 |
| Worker定向 | 第一轮Service/route/HTTP68绿；新增边界后pure64/64，PG HTTP6/6；最终全量另列 |
| Web | 全量310/310，lint0error18既有warning；typecheck85条既有缺依赖及连带错误，ai/shiki/streamdown等缺失；本批路径无新增TS错误 |
| 覆盖 | DB repository statements/lines/functions100%、branches96.58%；Worker两文件statements/lines100%、branches93.7%；均为局部覆盖，非全仓 |
| 安全 | diff --check绿；本批新源文件已stage用于A44/tracked-imports；无新依赖，offline audit生产依赖0（不是最新联网漏洞库） |

DB全量已完成（空隔离库ka_be_r010_p194_ignore_test）。Worker停止代码编辑后的最终全量复跑已完成：205文件通过、2外部opt-in文件跳过，2388测试通过、2跳过（另一隔离库ka_be_r010_p193_test，389.61秒）；typecheck/lint随后通过，进程exit0。首轮失败仍完整记录如下。

### Worker首轮失败与修复记录

- 首轮2383过/3失败/2外部跳过：production-composition端到端30秒超时；旧worker-once硬截止测试在预算内未进入lease（0而非预期1）；Web subprocess静态守卫只认旧execFile。
- 原端到端测试每次点击都冷启动一个tsx/Node进程；改成整套测试复用一个真实BFF子进程，仍通过真实HTTP访问独立data-api进程及PG，会话切换/旧token/跨媒体/退出断言完整保留。合成凭证改走IPC，不再放进子进程argv。
- 静态守卫扩展识别这条bffChild spawn路径，仍强制`--import tsx`，**11个探针数量不变**，另补两种入口缺tsx反例。未降低守卫要求、未提高30秒测试时限。
- 定向隔离复跑两文件4/4过，端到端测试体1165ms（整文件含启动3908ms），旧硬截止测试也过。日志 `output/p194-ignore-worker-isolated.log`；typecheck/lint绿。
- 首轮期间为定位超时改了测试harness，原始失败日志仍保留，不能作为最终稳定现场门禁；最终Worker全量在停止代码编辑后重新运行，写 `output/p194-ignore-worker-full-r2.log`。

## 安全/性能自审

- 当前personal workspace +批准tuple +live membership/identity/user/grant复验并上锁；不同workspace/不同media同号、撤销grant均拒绝。
- 纯忽略不查写account_mutes，已有静音完整保留；ignore+mute仍同事务，两分支audit同事务。真实PG注入audit失败，work_items状态回滚、旧mute不变。
- 终态重复仍409；并发锁/状态机沿用原实现，不能重复执行并延长静音。
- BFF只透传服务端bearer/cookie，拒浏览器scope；按请求挑strict DTO，错ID/错分支/非法日期拒绝，requestId一致，exact16MiB保守拒绝。团队/viewer媒体写未开放。
- 查询/写入固定次数，无N+1；上限reason4096字符，锁/statement timeout沿用3s/5s；无重试媒体动作，无外网发送。
- 保留原账户型工作项写权限：无账户私人项仍不开放写，本批不据只读权限擅自推导写权限；请arch如需开放另冻规则。
- BFF schema/handler/test获派单授权，零视觉/页面改动；be2 query/ETL逻辑零diff。生产composition测试仅改ignore段与自己的合成清理，不动数据断言。

## 剩余边界

- 待arch独立审查；浏览器表单交互与内网部署未验收，Web完整typecheck仍受既有缺依赖阻断。
- P194②change-log尚未完成，不能把本批记为整个P194完成；P193范围/P198未知发送窗口仍待arch回信。
