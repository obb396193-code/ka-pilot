# R010a1 Migration 012 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 落地 arch 已冻结的 v1.3 数据库变更及指定进入012的系数op、缺数抑制列，为seed与后续HTTP铺底，不提前开放业务写。

**Architecture:** 从保留的 `be/r009@b8f87d3` 拉 `be/r010`（已包含main@f841da4）。012为单一停写维护窗口事务，先检查遗留孤儿消息，再按Contract建列/约束/表；旧TEXT值严格作为JSON字符串保存，不推断业务type。R009 PG最终门仍未关闭，此独立建设不冒充R009验收或可上线。

**Tech Stack:** PostgreSQL / node-pg-migrate / Vitest / TypeScript。

执行方式：当前会话连续实现；executing-plans不可用，按本计划逐项执行。不启用Coze外部上传，不改Contract/React/媒体执行入口。

## Task 1 — 012结构反例（先红）

- 新建 `packages/db/test/migration-012-bundle.unit.test.ts`，实际加载012的up/down回调；严格对照 `packages/contract/schema.sql` v1.3、op和规则缺数附加段，不漏表/列/索引。缺文件先红。
- 测：唯一012文件、账户mute三键FK、两session FK、seq/client id唯一、active dedupe条件含dispatched、9个run列、4张新表、op默认divide、缺数suppress默认、JSON转换与回退保护。
- `cd packages/db && npm test -- --run test/migration-012-bundle.unit.test.ts`；这是JS/包结构证明，不是SQL执行证明。

## Task 2 — 迁移与必要旧内核存储桥

- 新建 `packages/db/migrations/012_contract_v1_3.cjs`，逐项复制冻结DDL；不把014/015列混入。`SET LOCAL lock_timeout='5s'` / statement_timeout='5min'。
- up先检查agent_messages/context的session孤儿并抛固定错误，不删除历史行。seq/client id保持冻结的可空，旧消息不自造序号。users.idealab_ak_ref保留，不自动把未知provider凭证搬库。
- from/to使用冻结的 `USING to_jsonb(from_value)`，保留“001”“true”“{...}”作为原字符串；a2再实现严格typed value业务，012不宣称完成typed DTO。
- down先检查JSONB是否只含SQL NULL/JSON字符串；出现typed对象/数字等则拒绝丢类型回退；出现multiply系数也拒绝回到旧divide语义。兼容原字符串用 `from_value #>> '{}'` 还原，不用 `::text` 加额外引号。
- 回退保护仅覆盖旧值类型和系数语义，不代表整批down无损：down会删除新增表/列及其中的新数据。只在已确认可丢弃的测试库回放；业务环境先备份、停写并由arch批准回退方案，不自动执行down。
- `packages/db/src/changeset-repository.ts` 必要存储桥：旧string/null入参使用 `to_jsonb($8::text),to_jsonb($9::text)` 写入，防止"100"被PG解释成JSON数字，普通文案不因不是JSON而报错。只改序列化，不扩业务DTO或写路由；迁移012后才能运行此版本。

## Task 3 — 真实PG测试（环境恢复后必须执行）

- 新建 `packages/db/test/contract-v1-3-migration.test.ts`：012 up/down/up、历史字符串逐值原样往返；null/文字/leading zero/JSON-looking字符串不推断；新typed对象阻止down、multiply阻止down；孤儿消息/上下文阻止up且事务不留下半建列；seq/client id按session隔离、active dedupe按workspace隔离，非活跃重复允许；mute同号跨媒体/跨workspace、orphan FK；provider凭证cross workspace user FK。
- `packages/db/test/changeset-repository.test.ts`：新增真实JSONB string/null往返反例（旧内核仍用原string，而非JSON数值/布尔）。
- 回放计数逐文件更新：auth5/workspace-sync4/workspace-kind3/contract-v1.2 2/backfill 2；migrations.test增加012一步、full replay12；011包结构检查不变。禁全文件盲替数字。
- 专用 `ka_r009_test` 当前连不上且可能含旧双011历史，先按R009折011计划核对历史；不得修改pgmigrations、不得对共享业务库清schema。未恢复只能记录pg_blocked。

## Task 4 — 自检/交付

- DB纯逻辑+typecheck/lint/coverage/audit；Domain/Worker非PG回归，真实PG恢复后DB全量+业务Session/Changeset PG门禁。只生成SQL字符串的覆盖率不冒称SQL已验证。
- 代码独立SHA、文档独立SHA；`inbox-arch`新增P046（P045保留给R009最终整批），说明从未合流候选继续、数据库不可用、JSONB兼容桥必要性。此交付验收句是“数据库准备好支持规则去重/会话幂等/系数初始化”，属于基础建设，不算用户功能已可用。
- 下一项R013 bootstrap/discover与coefficients；系数生效日期仍不能凭空填写。真实媒体写关闭，不push。

## 2026-09-06 实际交付

- 代码 `eaca65e`，11文件，351新增/19删除；未合流、未部署、未执行任何数据库迁移。
- 012包结构10项先红后绿；DB纯逻辑47、Domain518、Worker非PG724（另2外部opt-in跳过）通过，三包typecheck/lint通过；DB离线production audit为0。
- 012 JS up/down回调覆盖100%，不是SQL执行证明。真实PG反例已写但55432于02:16拒连，状态pg_blocked；旧双011历史必须在恢复后先核对，不篡改迁移记录。
- 当前分支be/r010，起点be/r009@b8f87d3，包含main@f841da4；be/r009保留待R009最终PG验收，未将其冒充已接受基线。
