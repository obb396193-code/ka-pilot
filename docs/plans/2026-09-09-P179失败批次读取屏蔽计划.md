# P179 失败批次读取屏蔽 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 接P176 Task2，让已记失败的tuple-day不再由旧Raw/Canonical表现为正常数据，summary/table真实coverage不足。

**Architecture:** 共享DB SQL谓词按失败事实、后续同资源采样时间、Canonical computed_at判定可读。只保留原始历史，不删Raw；失败后的真实采样尚未重算时仍屏蔽旧Canonical。ad批额外保留私有hh/adIds过滤范围，不能用别的小时/别的广告子集洗掉失败；公开warning不增加字段。

**Tech Stack:** PostgreSQL correlated EXISTS、Domain zod、现有Semantic/WindowAssessment仓储、Vitest真实PG及公开DataSource验证。

---

1. 先写真PG红例：已有旧Raw+Canonical→记失败→table无旧行、summary含missing而非部分求和冒充完整、lineage少account-day；跨workspace/media同号不受影响。
2. 新 `packages/db/src/etl-batch-readability.ts` 共享谓词；Domain私有evidence/record input支持ad过滤范围；ledger检查字段合法性与重复冲突。
3. 接 `raw-metrics-repository.ts` loadMergeInputs，`semantic-query-metrics.ts` expected left join，`semantic-query-repository.ts` table/count/lineage。expected account-day不能被WHERE过滤掉，否则缺数会被静默忽略；WindowAssessment/Dimension通过共用expected CTE同步获得missing。
4. 后续新Raw仅能解除原始输入屏蔽，不能直接恢复旧computed_at；新Canonical必须computed_at覆盖成功采样。ad hh不足、ad子集不覆盖、错误resource、跨日/跨空间媒体均不解除。真PG before/after证明旧值不读、真实重算后恢复。
5. 本批不接Full/Incr容错，不修改be2账户/任务列表。公开summary/table数据源定向+真实PG回归、三包type/lint/audit、范围覆盖；独立code/留痕SHA。性能说明包含失败日志历史扫描代价，不承诺无限历史常数时间。
6. 随回执交arch be2接线入口；后续P176 Task3还须canonical写入并发防线、readiness/warnings、Full/Incr/runtime完整闭环，不能把读取屏蔽冒称整个批容错已完成。

执行子技能当前不可用，沿用持续授权本人执行。保留媒体写关闭、不push、不生图；只用本人独占测试库，不修改共享库。
