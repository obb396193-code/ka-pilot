# I-002 工作项列表BFF修复计划

arch@f4205ce新派联调P1优先。只补`GET /api/internal/work-items`与服务端薄转发，不改视觉、列表后台权限或媒体写。

1. 按tasks同路径使用internal-api-bff的cookie/服务token/requestId/10s/16MiB工具；新增work-item-list-contracts、bff、server-only wrapper和route。
2. query严格白名单/不重复/规范数字；cookie仅ka_session，browser Authorization/x-ka-*不透传。不接旧scope resolver，不设免鉴权demo fallback。源固定GET /api/v1/work-items。
3. 当前已运行Domain WorkItemListResponse逐字段验证，直接读取ready/empty/partial/stale/errors权威fixture，成功/失败requestId与HTTP状态匹配。响应达到16MiB返回502 SOURCE_TRUNCATED；非法响应固定502，禁止源body/异常泄漏。
4. 当前服务仍coverage.complete；v1.3三态coverage完整流水尚属R010a2，不能在BFF自行合成checked/pending。I002先接真实已有envelope，后续A2同时升级，留账不冒充完成。
5. TDD及回归：无session/伪造scope、筛选/分页、错误/超时/源缺失、exact16MiB、非法状态/字段、只读method、route存在性；Web非PG测试与lint/typecheck。若全Web类型有旧FE缺依赖，记录原始结果，不安装依赖/改视觉掩盖。
6. 遵循apps/web AGENTS已读目录规范；本批无UI组件引入，因此无视觉选型/第三方源码/截图变动。独立code SHA+P127质量回执；真正Next→Worker→PG联调待arch复跑，不用mock HTTP替代。
