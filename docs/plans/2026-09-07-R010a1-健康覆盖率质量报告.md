# R010a1 健康覆盖率读仓储质量报告

## 当前交付与边界

- 代码 `fcc2f76`；已合 main@4222d4e，合并头 `1375eed`。合入增量仅 arch 本地联调 runbook，不影响本批代码/测试。
- 新增 PlatformHealthRepository（96 行），输入现有 ApprovedWorkspaceAuthContext；personal 显式 tuple、team 当前 workspace 全账户；不读取 team grants。private observation 不等于公开 health DTO。
- 同一 RR/RO 内查询 COUNT 与 MAX(computed_at)，三键 + 业务日绑定。已获授但未进 accounts 的对象仍计分母；空 scope 是 0/null；缺 computed_at 显式计数，不补当前时间。withData 仅表示 canonical 有行，不证明全部指标/分区完整、首次 full 成功或 SLA 新鲜。
- 未修改 Contract、be2 列表六文件、前端、运行时入口、依赖、migration；无 push/部署/媒体写；没有开任何消费者或清缓存。

## 门禁实测

| 命令（cwd=packages/db） | 本次真实结果 |
|---|---|
| `npm test -- --run test/platform-health-repository.unit.test.ts`（实现前） | RED：模块缺失；不是业务失败 |
| 同命令（实现后） | 20/20；加质量负例后 23/23 |
| `npm test -- --run test/platform-health-repository.unit.test.ts --coverage --coverage.include=src/platform-health-repository.ts --coverage.reporter=text` | 23/23；行/语句/函数 100%，分支 97.77% |
| `npm test -- --run test/platform-health-repository.unit.test.ts test/semantic-read-snapshot.unit.test.ts test/auth-repository.unit.test.ts test/workspace-sync-readiness.unit.test.ts test/task-query-lineage.unit.test.ts` | 5 文件 57/57（包含上述23，不能累加） |
| `npm run typecheck` / `npm run lint` | 分别 exit 0，两轮均过 |
| `npm audit --omit=dev --offline` | 缓存报告 0 vulnerabilities，非新鲜网络审计 |
| `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test PGCONNECT_TIMEOUT=3 npm test -- --run test/platform-health-repository.test.ts` | **exit 1；beforeAll ECONNREFUSED 127.0.0.1:55432；6 用例未执行，不是通过** |
| `git diff --check` / 路径与静态安全检查 | 通过；生产代码无写 SQL/真实标识/凭证/输出日志/动态 SQL 拼值/Date.now |

最初误用根目录 `npm test --workspace=@ka/db`，项目无 npm workspace，exit1；已改包内运行，不当作 TDD RED。测试结果取实际命令输出，本批无全量通过声明。

## 风险与未完成

1. **PG 阻塞**：只读 docker ps 显示仅 ContentRadar PG/Redis/MinIO，KA 55432 无监听。未借用 ContentRadar 5432。磁盘从197MiB/116MiB一度恢复12GiB，随后3.5～5.5GiB；不满足8GiB全量门。PG6项用例已写：三键/跨日最新时间隔离、双媒体显式授权、缺账户分母、空scope、team范围、未知时间，但尚未获真实结果。
2. **暂不宣称 health API 完成**：还缺上层 health 状态与 Service、受 Session 保护的 extraRoutes、BFF/16MB 联测。共享结构未见 extraRoutes，不独立改造。
3. **需 arch 一致化**：api.md:1014 写 agent.total，而 health fixture 使用 agent.instances；healthScore 未见算法；全未知分项下 overall/note 的明确表达需确认。先交真实 observation，不自造高分或健康色。
4. **ETL 列表独立依赖**：etl_runs 无不可变 attempt 列，旧 writer 把 BIGSERIAL 转 Number；后续不能用 jobs 当前 attempts 或行号冒充历史 attempt。
5. quality-checker 检查：SQL参数化/无N+1/错误计数安全整数、scope严格拒绝重复tuple和跨kind、失败rollback/release已单测；SQL语义须PG补证，单测不能替代。

继续 R010a1 其余与长期队列，不等本批审查；以上依赖不从总目标删除。
