# 下一批：ETL大ID与attempt读取

当前P139/P140已交审，继续R010a1。以live代码为准，不复用旧“共享runtime不能改”的阻断：arch已确认本人拥有runtime.ts。

## 已核实事实

- EtlRunRepository.startRun仍把PG BIGSERIAL `id` 转Number；超过安全整数有失真风险。
- EtlRunStore/full/incr的runId和runtime.recordObservation仍number；仅改公开DTO为string不能修复写入闭环。
- P121六入口scope.execution已有不可变job/attempt快照；历史缺execution不能填1或取jobs当前attempts。
- P120健康读仓储此前PG拒连：2026-09-08本人合成库已补6/6，相关unit27/27；它只是coverage观察，不是完整公开health。

## 实施顺序（TDD）

1. DB start/record/finish/fail全链使用规范十进制BIGINT字符串；最大PG bigint和>2^53实证，不把非法输入送SQL。保持run_kind/scope语义，不修改外部Contract，不改队列或媒体调用。
2. Worker EtlRunStore及full/incr/canonical/quality/backfill/runtime的显式runId类型同步string；真实handler/mocks保持同类型。核准所有引用再机械修改，不改别的计数/rows的number。
3. 在本人PG库用显式插入合成大ID行验证观察/终态只命中该ID，不推进共享sequence到极值。同步验证scope.execution旧attempt不随Job重试改变。
4. 独立SHA交审；旧行attempt未知、rows阶段计数缺证仍阻止公开伪结果。下一子批才做system/etl-runs的只读Repository/HTTP，不能用一个rows_ingested填raw和canonical两项。
5. 主库/arch库/别的worktree不动，测试只本人库；恢复后PG门禁已不再blocked，但磁盘4.5GiB继续限制全量负载。
