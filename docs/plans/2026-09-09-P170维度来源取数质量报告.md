# P170 解析来源事实读取质量报告

## 交付与边界

代码 **ceff217**，4文件293行新增（实现116行，测试175行，导出2行）。基线92b5d24；内部读取已实现/本机合成PG verified，**公开 account.dimension/v3 source 未接、未审合、未部署**。

- 从既有018解析表一次三键 JOIN 读取，批准scope私有副本；最多1000 tuples，空scope不发SQL、不自动发现账户。
- 原segments、override、conflicts分别保留，nameMatches揭示旧昵称解析；缺解析null、未知时间null，不默认nickname/当前时间。
- 接收同一RR/RO connection，不自己换pool连接。SQL字节总额哨兵先拒过大JSON，传输结果exact16MiB再次拒绝；非重复scope/result，非法行和越权行统一typed error，无原始数据进错误。
- 不包含来源分组/冲突裁决/前端展示；不修改be2 resolver、T5列表、登录或Contract。P170四项口径问题已交arch。

## 本轮实测

| 门禁 | 结果 | 证据边界 |
|---|---|---|
| TDD初次unit | 缺实现模块，1 suite failed | 真红，不算通过 |
| 初次PG | EPERM连接被沙箱拒绝；31 unit过/6 PG未执行 | 不伪报PG过 |
| 允许隔离库后定向 | **37/37**（31 unit + 6真实PG） | 同号跨空间/媒体，RR并发修改，未解析/非法/大JSON |
| 覆盖率 | **100% lines / 95.65% branches** | 仅新reader模块，不冒称全仓 |
| DB全量 | **113 files / 1262 tests passed**，80.44s | 混合unit+真实PG，不是1262全PG |
| DB typecheck/lint | 两项exit0 | 当前代码 |
| DB audit | offline production **0 vulnerabilities** | 本机缓存，不是在线最新公告 |

定向命令（packages/db）：
`TEST_DATABASE_URL=<local dedicated ka_be test database> npx --no-install vitest run test/account-dimension-evidence-repository.unit.test.ts test/account-dimension-evidence-repository.test.ts --hookTimeout=30000 --coverage --coverage.include=src/account-dimension-evidence-repository.ts`。

全量：`TEST_DATABASE_URL=<same isolated database> npx --no-install vitest run --maxWorkers=1 --hookTimeout=30000`，原始日志 `/tmp/ka-p170-db-full.log`。测试库限定本机55432、`ka_be_*_test`；专用合成数据，清理仅随机workspace。首个git add被沙箱拒绝，无文件损坏；获准后路径限定提交成功，不push。

## 自审（code-quality-checker）

- SQL输入全参数化，无拼接账户ID；恒定ORDER BY COLLATE C，最多一次查询，无N+1。
- 已显式验证 row.workspace_id/media/account_id，错误不带请求、SQL、response body、昵称或凭证。
- SQL返回JSON总量守卫避免巨大JSON进入应用，再检查总响应边界；1000有效tuple不误报截断、1001输入拒绝。
- 无依赖变化/迁移/媒体操作/视觉diff；不能替代调用方 ApprovedWorkspaceAuthContext 授权或公开source schema。
- 剩余：source混合分组及旧维度字段语义待arch裁，不扩大为第二套T5。

## 新优先级

测试期间arch main新增 **1e4ac70 v1.9.4**（021小时数据与Gap规则解阻）及 **e29a5c3 F-OS-001～003**（P0优先021）。P163旧阻断快照已过时，接下来同步主线，先修OS首跑，再021/hourly→Gap→source；本批reader收好不继续抢前序。生图仍取消，完整目标未完成。
