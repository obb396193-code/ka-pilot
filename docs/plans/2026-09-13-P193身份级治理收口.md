# P193 身份级治理 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 依arch v1.9.46将旧workspace-local候选改成身份级治理，最终接通PATCH成员、GET/PUT授权及BFF。

**Architecture:** 复用live team-admin治理鉴权；服务端解析目标identity唯一有效personal workspace。修改在事务内执行，身份停用撤全部session，撤grant后原session重新解析立即缩权；审计同时记录actor identity/target identity/target workspace。

**Tech Stack:** TypeScript、Zod、PostgreSQL、既有HTTP/BFF，合成PG与Vitest。

## 依据

main@1e304507，be/r010@bf4a480f干净。上轮P198内核已提交，属于实际进展；未解决的接收人/模板问题已回arch。本笔不混P198。

brainstorming所需的设计批准依据是用户按Claude派单实现的授权以及v1.9.46：拒绝伪造session/给管理员塞personal成员，采用服务端按identity定位，不重新选择产品权限架构。writing-plans的executing-plans子技能未提供，继续逐项TDD。

## 1. Repository（独立提交）

文件：packages/db/src/admin-member-management-repository.ts、admin-member-provisioning-repository.ts（仅治理helper返回已验证actor identity）；packages/db/test/admin-member-management-repository.test.ts与同名单测文件；packages/domain/src/admin-members.ts注释。

先改旧测试形成红：当前空间不同仍能管理目标personal；修改role只更新该identity的personal membership/user（role实际存储位置，team角色不在此授予）；停用改auth_identities并撤所有session；明确目标同名跨workspace/media隔离；无team-admin资格拒绝。

随后实现：目标identity行锁、唯一有效personal查找LIMIT2、拒绝共享/歧义personal，不把auth.workspaceId当target；固定锁顺序避免互相停用/自改导致锁升级死锁。grant全量替换、audit同事务；不创建或激活目标空间、不建不存在的账户。

新增真实PG：管理员从personal/team均可管理同一目标；没有/多个有效personal、共享personal、inactive目标空间、账号仅在管理员空间存在、两媒体同号、并发全量替换、审计失败回滚、停用后两空间session失效、重新启用不复活旧token、live team-admin撤销立即生效。全局GET复用既有provisioning reader，不倒退workspace-local。

门禁：定向真实PG红绿，然后Domain/DB/Worker三包全量test/typecheck/lint。将旧测试scope断言明确更新为新契约，不通过删权限用例放行。

## 2. Service/HTTP（独立提交）

文件：apps/worker/src/admin/members-service.ts、r010/admin-members-routes.ts；data/http-server.ts仅选项类型注册hunk；data-api.ts仅注入；对应Service/HTTP/PG测试。

严格request/response和目标identity回读校验；服务端target workspace不接受浏览器提供；保留viewer默认写关闭和媒体写关闭，不扩大公共写权限。401/403/400/404/409/502/503/504/500、requestId、exact16MB、异常净化、oldtoken失效通过真实HTTP+PG证明。

## 3. BFF与回执（独立提交）

完整阅读Web规则及必读文档后，只改授权BFF路径/请求透传，不改视觉。复用Domain strict schemas，补cookie/server bearer、csrf、请求/响应shape与requestId、目标一致性/错误码/体积边界。真HTTP合成fixture供前端parity，不再手抄第二套。

交审前merge main、源码全跟踪；[codex]路径限定提交、不push。A40声明旧grants的workspace-local语义被v1.9.46替换；公共字段不自行增targetWorkspace。未完成一步时分层报告，不把Repository称API已完成。

## 现场进度（2026-09-13）

- 第1步仓储已改：治理资格按live team-admin；目标唯一personal服务端查找；grant整体替换只写目标三键；停用全session；actor/target审计同事务。单目标回读LIMIT2，缺失或重复不作任意首行选择。
- 先改旧PG用例复现5红；实现后原10项通过，再扩到17项真实PG。新增27项mapper/错误边界用例；首轮其中两项空数组参数被it.each展开，修正测试参数包装后44/44全绿，不把那次失败算通过。
- 定向仓储覆盖：行/语句/函数100%，分支98.23%；typecheck/lint绿。Domain104文件1650项及typecheck/lint绿。
- 第1步代码`39bf2683`：DB162文件1866项、Worker209文件2475项+2外部opt-in跳过，三包typecheck/lint均通过；第2/3步Service/HTTP/BFF未改，尚不能称治理API可用。只在本地合成PG验证，没有真实业务授权变更。
- 审查重点：PATCH role只改目标personal membership/user，不授予或改写team角色；target缺失/歧义/共享明确拒绝。调用方workspace保留在audit列，目标workspace在detail字段；全局低频治理锁是跨调用空间的串行边界，不宣称高吞吐。
