# P198 发送HTTP模块质量回执

日期：2026-09-13。代码`2262302b`，私有模块，尚未注册实际投递入口；全量门禁通过、候选交审，不是完成P198。已同步main@9d6958b8，merge `00339164`只保留双方追加原文并移除冲突标记，未改实现文件。

## 实现与安全边界

- `apps/worker/src/notifications/dingtalk-outbound-http.ts`：群HMAC-SHA256签名、单聊token/OTO请求、内存token缓存；不新增依赖，不import入站网关启动器。
- 群目的地复用既有HTTPS钉钉域规则，再限制robot/send、access_token，无userinfo/自定义端口/fragment；redirect=error。DM地址代码固定，调用方不能指定主机。
- 全链10秒超时（token/发送分别有界且总和低于60秒租约），响应流/Content-Length严格小于256KiB。消息4KB本地上限，超长拒绝不截断；该值是客户端保守上限，不宣称钉钉官方限制。
- 成功必须对应有效ack；OTO的filtered/invalid/flowControlled列表逐目标检查，不因HTTP200就记成功。未知响应/连接中断→unknown；token请求本身不能发送消息，失败不计模糊发送成功。
- 无日志输出provider body/URL/token，无网络自动重试；重试完全交已冻结DB状态机。公开sent语义仍是明确API接收，不是用户已读。
- 锁/取消后不得再发请求；mock先复现abort信号已设置仍调用fetch，修为post入口立即拒绝。所有测试凭证均synthetic。

## 定向门禁

- 32/32 fake-fetch用例通过，覆盖行/语句/函数100%，分支85%；Worker typecheck/lint通过。
- red轮：缺模块；新增cancel-before-send反例先红；首次lint触发控制字符regex规则，改字符码校验后绿，无ignore/disable。
- 原始定向日志`output/p198-transport-focused.log`；全量Worker/gateway日志`output/p198-transport-worker-gateway-full.log`，终态后追加，不用进行中日志充绿。
- Domain/DB代码相对仓储笔没有变化，最新全量证据仍1699/1888及两包typecheck/lint；Worker变化必须单独全量复验。

最终终态：串行会话28241 exit0。Worker213文件2526通过+2项外部opt-in跳过，gateway8文件36通过（含真实PG入站2项）；两包typecheck/lint全绿。`git diff --check`通过，apps/web/视觉/冻结Contract/package-lock零diff。新源码均已跟踪。没有钉钉真请求，没有媒体写，没有push。

## 未完成

服务端群配置解析、同workspace DM身份反查、三种白名单模板、真实单次CLI注册、diagnose、日报deduplicated显示尚待下一接线笔。当前不会从业务队列自动发消息；不称已接通/部署/真实发送。
