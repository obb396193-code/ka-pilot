# R013 Coefficient Seed Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 交付独立seed:coefficients部署命令，按显式空间与生效日一次初始化四渠道折算配置；重跑幂等、不覆盖历史、不自行决定生效日期。

**Architecture:** Domain严格输入及四条已冻结初始配置→DB单事务锁active personal workspace、读取最多5条四渠道已有版本→仅首次插入缺行或精确重放；发现不同版本/值拒绝，后续版本变更留给R012设置与重算链。CLI只取一个JSON参数和DATABASE_URL，不创建空间、不接媒体/默认凭证、不调用012迁移。team现金已由源算好，禁止seed二次系数。

**Tech Stack:** TypeScript/Zod、node-pg、Vitest；沿用be/r010，当前main@2605099新增v1.5.1只读登记，不混入本批。executing-plans不可用，按用户已授权的当前会话连续执行。

## Task 1：Domain输入与初始配置

- 新增packages/domain/src/coefficient-seed.ts、test/coefficient-seed.test.ts并导出index；`{workspace_id,effective_date}`严格无多余字段，真实日历校验。日期必须明示，不能默认今天。
- 四条仅属于初始化配置：KUAISHOU multiply 0.7812，TENCENT divide 1.045，TOUTIAO divide 1.09，BAIDU divide 1.51；运行时计算继续读DB，不依赖这个常量。
- 先失败测试再实现；非法日期/凭证/自选系数/无scope拒绝，返回固定错误不带输入。

## Task 2：DB首次写入事务

- 新增packages/db/src/coefficient-seed-repository.ts、test/coefficient-seed-repository.unit.test.ts和真实PG test/coefficient-seed-repository.test.ts。
- SERIALIZABLE + workspace行FOR UPDATE，锁等待5s/语句30s；无空间/停用/team拒绝；固定四媒体参数化读取LIMIT5，避免全量历史materialize；任何已有多版本或不同值/日期拒绝，不偷偷回溯改口径。
- NUMERIC按十进制字符串规范化精确比较，不用浮点把临近不同值当相同；所有现存行先验证再插入，缺行同事务补齐，不更新/删除任何历史。四行初始changed_by=NULL（操作者不是产品session，不冒用老板actor）。
- 并发/重试通过事务与空间锁保证；SQL/DSN/上游错误不暴露；真实PG不可用时明确未跑。
- PG反例：空空间四行、重复两次、双并发只四行、不同空间隔离、错误日期/值/多版本不写、停用/team/不存在拒绝、异常后全回滚、既有业务数据不受影响。

## Task 3：CLI与部署说明

- 新增packages/db/src/seed-coefficients.ts，package.json注册seed:coefficients；一个JSON参数，无参数则退出且不连接DB；固定失败文本，stdout仅inserted/skipped计数；无自动迁移/真实凭证。
- unit注入执行port验证参数/DB-before校验，真实CLI缺输入/坏JSON/缺DSN均拒绝；runbook §2.5说明显式日期示例为占位不得直接上线。
- Domain/DB纯逻辑+type/lint、Worker回归、覆盖与audit；独立代码/留痕SHA。PG恢复后在已核定隔离测试库跑真实PG。bootstrap三项待裁不改，本批不代替其完成。

## 实际交付与部署阻断

- 代码d16906a，Domain24新例+DB/CLI30新例；Domain579、DB unit77、Worker非PG761+2外部opt-in跳过，三包typecheck/lint，DBoffline audit0。Domain核心100%，DB Repository行98.66%/CLI83.87%，合计94.33%。SQL真实PG7例已写，02:51获准TCP仍ECONNREFUSED，未执行。
- 仅40001且ROLLBACK成功会使用同一输入最多3次新事务重试；其他错误不重试，rollback失败销毁连接。没有自动扩大scope或变更版本。
- **当前不可部署系数seed后运行旧canonical**：实读metrics.ts:67仍写死除法，metrics-repository未select op。紧接着按已冻R010a1补op快照→Worker→Domain与现金onTarget；此依赖不能只留注释后上线。

## Task 4 — 紧接着收口op依赖（独立代码SHA）

- packages/domain/{src/types.ts,src/metrics.ts,test/metrics.test.ts}：新增可空方向；缺方向不默认divide；cash_cost按乘除，onTarget用cashCpa与现金考核价，缺价/缺现金不判达标。
- packages/db/src/metrics-repository.ts：生效版本同时读coefficient/op，保持三键+截止日，不依赖媒体名推断方向；缺整条配置返回null，present-invalid配置拒绝。PG测试用同媒体两日期op不同，另空间不串。
- apps/worker/src/etl/canonical-handler.ts接settings；修对应测试/benchmark固定假配置显式divide；新例multiply补偿后现金正确、缺op不落假值。不扩大到v3窗口HTTP，此步骤只是现有canonical计算正确性。
- 定向先红→实现→Domain/DBunit/Worker全量type/lint，PG如仍不可用如实保留；完成后P050补回执。
