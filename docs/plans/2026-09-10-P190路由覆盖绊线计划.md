# P190 R010 BFF 覆盖 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为本人路由增加浏览器可达性双向检查；失败暴露缺口，不假装完成前端接线。

**Architecture:** TypeScript AST 读取 r010 路由、Data API shell 和 shell 导入的 HTTP_PATH 常量；反向检查包含实际注册的 r014 路由。递归扫 web/lib/data 非测试 TS，排除注释，用模板/条件分支解析路径。少数动态结构段严格登记源码锚点，源码变化就红，不把未知路径当成功。

**Tech Stack:** 已有 TypeScript 编译器 AST、Vitest、node:fs；只测试与文档，不改前端和运行时。

---

executing-plans 未提供，沿用户持续授权本会话执行，不派子 agent。本批使用 writing-plans / code-quality-checker；rerun 的 F-P179-Q 两点待 arch，先做无该依赖的检测门。

1. 创建 `apps/worker/test/r010/route-path-inventory.ts`：抽取字符串/正则/模板路径，输出来源文件；处理导入常量、条件 suffix、参数归一化为 :p；未知动态路径报错，不隐性丢弃。
2. 创建 `apps/worker/test/r010/route-path-inventory.test.ts`，样例证明字符串、正则、模板、条件、注释排除、坏形状拒绝；真实库存数量下限与 sentinel 路径确保不是永绿。
3. 创建 `apps/worker/test/r010/bff-coverage.test.ts`：own→BFF、BFF→全部实际路由；BACKEND_ONLY 仅明确不面向浏览器接口且必须说明理由。已知漏网不得塞豁免。
4. `npm --prefix apps/worker test -- run test/r010/route-path-inventory.test.ts test/r010/bff-coverage.test.ts`；若检出真实缺口，保留红闸与清单回 arch，不同批顺手修前端。
5. Worker typecheck/lint、diff check、缓存audit；质量报告说明这是静态路径可达性下限，不等于方法/DTO/权限/部署验收。单独SHA和审查回执，后续P191 viewer门仍需真实HTTP方法枚举。
