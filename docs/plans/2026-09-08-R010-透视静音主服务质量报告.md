# P136 透视与静音主服务接线质量报告

## 范围与结果

- 代码 **da7d5ac**，6个Worker文件；已含main@0d358d8。按arch文件owner澄清直接接主HTTP，不改be2注册数组、Contract、前端或媒体执行器。
- `data-api.ts`真实启动注入 `createPlatformPivotQuery(pool)` 和 `AccountMuteService(AccountMuteRepository(pool))`。主壳在Bearer+Session之后处理两个静音路径；缺服务配置503，不再无入口404/假成功。
- 透视真实读SQL，taskIds选中真实日归属；静音与ignore+mute沿用已通过锁内授权/原子事务的Repository。纯ignore不带mute_days仍503；规则扫描生产静音过滤/通知抑制仍未接，不把存储静音配置说成全链路生效。

## 本批实际门禁

| 项目 | 结果与日志（output/qa/p134/） |
|---|---|
| 主壳静音TDD RED | 4失败，旧壳404；composition-red.log |
| 第一次GREEN尝试 | 36功能测试过但PG afterAll失败，**不是全绿**；composition-green.log |
| 最终真实PG集成 | **8/8**，包含实际data-api子进程1、主壳Session静音4、透视SQL3；composition-pg.log |
| 旧/新HTTP联合 | **148/148**，DataAPI56、startup3、Session11、D6 23、静音33+2、R010响应5、透视公开15；composition-regression.log |
| Worker typecheck/lint | 最终exit0；composition-type-final.log/composition-lint-final.log |
| 静音route覆盖 | 行/语句/函数100%，分支91.66%；composition-coverage.log |
| Worker生产offline audit | 0；缓存公告不是在线最新风险保证 |
| diff/static边界 | diff --check过，未新增媒体/Job调用、敏感日志、动态输入SQL或浏览器授权协议 |

真实PG命令：`TEST_DATABASE_URL=<本人55432隔离库> npm --prefix apps/worker test -- --run test/account-mute-http-pg.integration.test.ts test/r010-production-composition-pg.integration.test.ts test/pivot-query-pg.integration.test.ts --maxWorkers=1 --testTimeout=30000 --hookTimeout=30000`。

新生产composition测试不是另造harness：真正spawn `src/data-api.ts`，KA/diagnostic/internal-test-login均关闭，通过DB发放的session cookie调用；同account_id的其他media/其他workspace含不同指标，真实pivot仅返回已授10而不是其他900；未知task空格子；静音只写本人KUAISHOU三键；ignore状态变ignored。HTTP切team后动作403且旧token401；HTTPlogout后三路全部401。没有运行worker/beat/媒体通道；测试child结束后停止，不影响arch联调实例。

## 收尾错误与修复

- 测试升级接入真实Session后，afterAll误用auth_sessions.workspace_id（真实列为active_workspace_id），导致功能通过但套件失败。
- 修为按本测试identity_id清Session后再清membership。只读核对本人库遗留8个合成workspace及其identity，用显式8个UUID+测试名称双守卫在事务内清理；无用户数据、无共享库清理。测试fixture可重建，最终回归收尾成功。
- 空间约3.6GiB，低于全量8GiB门槛，未全DB/Worker。不复用arch或前批全量结论。

## 未完成与下一步

- 已本地进程+HTTP+Session+PG验证，仍待arch exact-SHA审核合流/重启联调；未部署、未声称production-verified。
- 静音BFF/纯ignore成功契约/规则扫描runtime静音消费尚未完成。已有pivot BFF走通用query，这次未改UI。
- D6仍source-off，013/014等待裁决与真实源项不抹掉，总信箱目标继续active。
