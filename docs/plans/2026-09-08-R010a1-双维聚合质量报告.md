# R010a1 双维聚合质量报告

代码 **f287252**；main **4222d4e** 已同步。本批为 pivot2 聚合阶段，不是公开查询已接通。

## 检查结果

- 新核心110行：strict `account.pivot2/v1` a/b+v3 metrics/assessment；保留null分组，拒重复cell、同key异label、同维两轴不同key、account轴缺media。
- 内部输入仅account_day粒度：history/ka_daily区分、workspace/tuple/日期完整集合校验；每成员只进一个cell，不丢缺数账户日；跨账户同号不碰撞。≤1000账户/31日/10000成员，先做总成员预算预检，避免多层数组耗尽资源。
- 调用既有 aggregateWindowMetrics 重算ratio；逐日价格+转化加权成本空间，不平均CPA/考核价。双份现金/转化证据必须一致；输入旧costSpace不作为判定依据。预算仍undefined，KA混价保留ASSESSMENT_VERSION_UNKNOWN。
- tuple-key使用JSON数组编码，文本冒号不产生分组碰撞。标签真实性/粒度分配/授权/来源coverage仍由可信reader负责，不把纯函数当来源准入。

## 实际门禁

| 范围 | 命令 | 结果 |
|---|---|---|
| TDD RED | Domain `npm test -- --run test/pivot-window.test.ts` | 模块不存在，1 suite失败/0 tests |
| 初轮 | 同上 | 24 passed |
| 最终Domain+覆盖 | `npm test -- --run test/pivot-window.test.ts test/summary-window.test.ts test/window-assessment.test.ts test/ka-window-assessment.test.ts --coverage --coverage.include=src/pivot-window.ts --coverage.reporter=text` | 4 files/84 passed，新38；核心行/语句/分支/函数100% |
| 上界 | 最终38项中的10000完整account-day输入 | 实际通过，约532ms；用例单独30s上限，未修改全局 |
| Worker回归 | `npm test -- --run test/platform-dimension-query.test.ts` | 8 passed |
| 类型/规范 | Domain、Worker各 `npm run typecheck && npm run lint` | 最终均exit0 |
| 依赖 | Domain `npm audit --omit=dev --offline` | 缓存audit0，非在线最新漏洞证明 |
| 静态 | `git diff --check`/逐行复核 | 无空白错误；无eval/SQL/网络/secret；未改Contract/DB/前端/媒体写 |

首次typecheck曾报TS18046（条件联合对象warnings为unknown），已改为KA分支内取typed warnings并最终复跑通过；未掩盖失败。

## 未完成/下一步

- DB pivot reader、Window Service、完整source envelope、Registry/Adapter/HTTP/BFF与cellCoverage尚未实现。新文件只导出投影与内部聚合，不把account.pivot2偷偷注册成假ready。
- 需要真实历史任务/业务/账户日及价格在RR/RO同快照读入；team按R011发布快照；8维真实性仍依赖R017/014及OS，不用custom_tags/空bid_tool制造分类。
- 当前1.6GiB低于8GiB全量门；本批未跑全量/新PG。隔离PG前批仍ECONNREFUSED，此处没有新的PG通过声明。
- independent implemented/non_pg_verified，尚待arch审，未merged/deployed/push；总目标继续，不以聚合函数替代全信箱完成。
