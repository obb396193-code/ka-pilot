# P176 单批取数失败隔离 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按v1.9.8完成F-OS-001③，耗尽的取数批不拖垮其他批，失败tuple-day为missing，公开warnings为BATCH_FAILED，不能把旧canonical当ready。

**Architecture:** 先实现strict失败事实和受fencing保护的有界记录内核，再接失败屏蔽/coverage读取，最后才允许Full/Incr在重试耗尽后继续。沿用etl_runs.scope JSON和现有lease，不造新表/新run状态。批次异常只包住源query，持久化/鉴权/截断/非法scope仍失败。

**Tech Stack:** Domain zod、Worker QihangClient、pg事务/行锁、Vitest真实PG与假上游。

---

## Task1 独立内核SHA（本子批）

1. 新 `packages/domain/src/etl-batch-failure.ts` 及 `test/etl-batch-failure.test.ts`：公开warning固定code/resource/ds/accountIds/fingerprint；私有batchScope为可信workspace/media/accounts/dateFrom/dateTo，真实日历/唯一账户/上限校验。先测试红，再实现。
2. 新 `apps/worker/src/etl/batch-failure.ts` 及对应测试：只将RetryExhaustedError映射失败warning；BlockedAuth/业务错误/截断/原始任意错误不吞；单日显式账户范围，SHA指纹不含qid/原始body。原Client仍负责重试，不新增重试层。
3. 新 `packages/db/src/etl-batch-failure-repository.ts` 与unit/PG测试：仅运行中full/incr、同workspace/job/attempt/current lease、已冻结batchScope内可追加；短事务FOR UPDATE，10000记录/16MiB边界，重复同指纹幂等且冲突拒绝。时间取DB clock，记录只含白名单；不接Runtime、不改变canonical/jobs状态。
4. 真PG红绿：同号跨空间媒体、旧lease/旧attempt、非running、非法scope/日期/fingerprint、并发追加不丢、重放不重复、失败rollback。所有样本专用本机库/随机空间，不读业务数据。
5. 三包定向type/lint/audit/coverage→路径限定code commit→质量报告/信箱receipt commit；全包仍遵守8GiB门。

## Task2 失败读取屏蔽与就绪度（后续独立SHA，未完成）

- 原 `RawMetricsRepository.loadMergeInputs` 会读旧Raw，canonical依任务异步重算；不能仅把failed tuple从本次数组过滤就宣称missing。必须据失败时间与后续同资源成功采样判断旧Raw/Canonical是否仍有效，并覆盖summary/table/window读与初次同步readiness。
- 新失败记录保留原完整事实，不删除历史Raw。后续成功必须用真实新采样解除失败，不能用当前响应时间伪造freshness。canonical聚合需要范围屏蔽，旧canonical保留也不能在公开源当ready；其他媒体/空间不能受影响。
- account/task列表SQL当前属be2：有共享读helper则只改本人helper；确需改对方文件先交arch派修，不越权。最终交付需明确哪条公开读取仍未接。

## Task3 Full/Incr及Runtime接线（Task2守卫后）

- startRun冻结batchScope，query失败record后继续下个明确账户/日期批。account metadata发现失败不容错（主表/FK前提未知）；空scope不发全量源请求。
- 历史offline失败不等同真空分区；ad前/后小时任一批失败，相关账户不派生假delta。安全失败集合只从typed重试耗尽获得，持久化失败不吞。
- `etl-runs`按冻结warning输出、run done、lineage partial，测试一次抖动恢复/预算耗尽/其余批继续/旧canonical拒绝/后来真实成功恢复。真实PG+假上游走原runtime，不只mock handler。

执行子技能不可用，沿用老板持续授权本人执行，不额外派子agent；本Task1完成只报内核，不把整条F-OS-001③勾完。

## Task1执行快照

42e98717已提交；Domain18、DB54（23PG）、Worker68通过，三包type/lint/offline audit通过。Task2/3未完成，Full/Incr/Runtime没接内核；详细边界见`2026-09-09-P176批次失败内核质量回执.md`。
