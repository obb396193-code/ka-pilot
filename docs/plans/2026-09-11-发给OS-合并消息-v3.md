# 发给 OS 与 devix（2026-09-11 合并版；取代 09-10 v2）

两位的回执都收到，四个根因（8192 URL 上限 / ka-data 护栏拒 `replace()` / 022 SQLSTATE / 登录 Router Cache + 任务页 period null）全部已在 main 修掉。老板拍板：**内网不灌假数据，直接抓真数**。

## 一、现在就做（两套部署都做）
1. **拉 origin/main 最新**：`git pull origin main`，`git log --oneline -1` 的 SHA 与 arch 在群里给的一致（含 `[arch] 热修 F-OS-006`）。依赖不用重装（dependencies 零变动）。
2. **升库**：`cd packages/db && npm run migrate`，自检 `select count(*) from pgmigrations` = **20** = migrations 文件数（比上次多 025 小时表、027 任务管理）。022 已能在缺 pg_trgm 的库上降级，不再整批回滚。
3. **不要跑 `scripts/seed-demo-data.py`**。devix 那套已灌过合成数据的库：清掉再抓真数（最简单是 `./stop.sh --pg` 后重新 initdb + migrate；或把 workspace `00000000-0000-4000-8000-00000000009x` 系的假空间/账户/事实行整体删除）。页面上「示例个人空间（假数据）」「示例优化师」这些名字都来自 seed，抓真数后自然消失。
4. **重启 data-api + worker-http**，然后跑一次 `worker:once`（etl_full）。这版 `resource=account` 不再带 accountIds，URL 172 字节；200+text/html 会立刻判 `UNEXPECTED_CONTENT_TYPE` 不再重试 4 次。回我 `etl_runs` 三列 + `account_metrics_daily` 行数 + 账户数。jobs 表那条 exhausted 的不用动，queued 的会自然消化；如仍失败把新的 `error_summary` 贴我。
5. **团队空间**：`replace()` 已全换 `strftime`、ds 按 TEXT join。登录后切团队空间，`POST /api/internal/data-query {"queryId":"account.summary","params":{"dateFrom":"2026-09-03","dateTo":"2026-09-09"}}` 应返回 `ok:true` 且 `rows[0].metrics.cost` 有值（OS 实测 09-09 单日 318 户 / 46 万）。仍 503 把 data-api 日志里 ka-data 的原始 error 贴我。
6. **web**：等 CI 重推 `deploy/web-standalone`（约 15 分钟，`SOURCE_SHA` = 第 1 步的 SHA）再 `git fetch` + 重启；I-010 登录卡住、I-011 任务页崩两处已在 main，devix 本地那两笔补丁可以丢掉。**`KA_READ_MODEL_DETAILS_ENABLED=true` 正式列入必配**（devix 验证 data-api 已能返真详情），runbook 环境变量表补这一行；`KA_DATA_SERVICE_TOKEN` 必配（与 `DATA_API_INTERNAL_TOKEN` 一致）。

## 二、回答两位的问题
- **devix 问「真数据落进带公网入口的沙箱能不能做、用谁的 userId」**：老板定。arch 建议：OS 那套（已绑白鹏杰启航身份、deploy.sh、看门狗）为主环境抓真数；devix 那套先不绑身份、只做浏览器点测环境，等老板拍板要不要并成一套。
- **页面大部分是 fixture**（devix 改库探针，账户池 39 户 vs 库里 6 户）：属实，是前端阶段状态不是你们没接上。已立规矩 A35：真实模式下不得渲染 fixture，没接的容器显「接口未接入」；fe 正在按页接真接口（F8-25），顺序：工作台 → 账户池 → 任务列表 → 数据分析各 tab → 归属清洗 → 报告。每合一批我在这里通知。
- **UI「变形」**：老板那张截图对比本机同页面，结构一致；差别是顶栏切成了「全彩」+粉色色相、浏览器缩放 80%。容器里图表中文变豆腐块是容器没装中文字体（`fc-list :lang=zh` 空），老板机器不会。ingress 给浏览器 UA 注入三个脚本并留下重复 `</head>` 这条记下，暂不处理。
- **`conv IS NULL` 语义**：采纳——`fact_conv_daily` 该 ds 有行则 NULL = 0（无归因），无行才是「待到」。契约 v1.9.36 已改，后端按此实现。
- **`qihang_account_report_hour`**：cost 单位与同账户同小时多行的去重口径，等车程确认前不接。三个旧表名作废。
- **给车程的清单**：按 OS 实测版发（护栏误杀 `replace()` 升 P0、7 表已可查只确认支持范围、只补 `is_ubp`/`ocpx_gap` 两列、小时表单位、conv NULL 口径）。
- **仓库公开/密码/PAT**：老板已定不处理，不再列。
- **Multica issue**：等老板建，建好在那儿回帖；回执仍走 `docs/plans/`。

## 三、回执格式
三列（etl_runs status / rows_ingested / step_failed）+ `account_metrics_daily` 计数 + 团队空间 summary 的 `ok/cost` + `SOURCE_SHA`。双空间各截一张大盘。
