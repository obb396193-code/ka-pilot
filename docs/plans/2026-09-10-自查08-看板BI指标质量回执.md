# 自查08：看板三 BI 指标计算

代码 `7f407c7e`，本地候选，未公开接线/合流/部署/push。

- `computeDashboardBi` 只依赖完整 expected account-day 输入，不自行判权限/覆盖。历史价格与KA每日价格两条均复用现有窗口assessment算法；`bi_conv`用已确认realConversion，绝不以账面conversion替代；`bi_cash_cost`先求和再相除；`over_cost`反向costSpace，正数超成本。
- 现金/BI/价格任一缺项保留missing；真实零与缺失区分，正现金/零BI返回infinite、零/零undefined；超成本精确零不输出负零。不四舍五入、不前端分摊、不增加源优先级。
- strict输入/输出；10000成员上限、真实日期、生效版本不能来自未来；溢出和present-invalid拒绝。两类源数值一致，多个账户同日不同价格不被压成一个代表价。

## 门禁

Domain：`npm test -- --run test/dashboard-bi.test.ts test/summary-window.test.ts test/window-assessment.test.ts test/metric-value.test.ts --coverage --coverage.include=src/dashboard-bi.ts --coverage.reporter=text` → 4 files / **73 passed**（新24），模块行/分支/函数100%。`npm run typecheck`、`npm run lint`通过；`npm audit --omit=dev --offline`缓存0漏洞。先执行测试得到缺模块真实红，再最小实现转绿。

无DB/HTTP变更，故本批不冒称PG/接口验证；无新增依赖/SQL/网络/密钥，循环O(n)，数组≤10k。低磁盘不跑五包/build。使用writing-plans明确范围、code-quality-checker核验strict/溢出/覆盖和依赖。

## 未完

P211完整公开接口仍需维度、filters与源接线、实际响应fixture。文档view入参与真实queryId入口的差异已在arch信箱问；既有v1.9.8已明确resource_position统一placement，会按此既有裁决推进，不造第二个来源。此纯计算交付不等于看板功能全完。
