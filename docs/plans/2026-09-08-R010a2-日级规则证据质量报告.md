# P144 日级规则真实证据质量报告

候选代码：Domain **5c7168f**、DB **0e7366c**，依赖前批05fb748。main仍c3451db，无新增待合契约。未合流/部署/push/生产验证。

## 交付内容

- `planRuleDailyEvidence` 将显式日期窗、每叶window_hours、consecutive_days转为有界查询计划；一天一个日累计，不冒充任意滚动小时。最宽跨日范围≤31天，读取前拒绝小时需求和超范围。
- `evaluateRuleDailyEvidence` 复用原条件树判真器；日集合必须完整（允许显式missing行），不丢缺日、不补0；逐日考核价按真实转化加权。现金CPA、零分母、成本空间都由已有现金口径函数计算。未知未实现指标missing，不自取别的源。
- `RuleEvidenceRepository` 从批准personal上下文收窄到当前tuple，规则/适用性/实际canonical/生效历史价共用同一RR/RO连接。复用原SQL和decoder，老Repository.read继续拥有自己的事务入口。team发布源未接→明确TEAM_SOURCE_UNAVAILABLE，不借个人授权。
- 禁用/不适用/null legacy树分别表达，不以condition_false混淆；整个结果为**内部证据观察**，没有triggered/伪造dataAsOf。firstFull、源新鲜度、冷启动、静音、dedupe等后续未接，不能拿evaluation.pass创建工作项。

## 精确门禁

本人 `output/qa/p144/` 保存日志。只用localhost:55432/ka_be_r010_20260907_test合成库，DB/Worker串行。测试中的表删除都限定当次random workspace，无真实数据/媒体写。

| 项 | 结果/证据 |
|---|---|
| Domain全量 | 77 files / 1131 pass，domain-full.log |
| 日证据新增 | 16 pass，domain-focus.log；覆盖100%行、94.23%分支 |
| DB全量最终 | 92 files / 991 pass（含unit+真实PG），db-full-final.log |
| 三仓储定向unit | 83 pass，新仓储16；db-cover-final.log |
| 三仓储真实PG | 24 pass，新组合6；pg.log |
| Worker全量 | 142 files pass / 1541 pass + 2外部opt-in skip，worker-full.log |
| Web node回归 | 196/196，web-test.log；未做Next build或声明全UI类型通过 |
| Domain/DB/Worker typecheck、lint | 通过（各type/lint日志） |
| production离线audit | 0，audit.log；不是最新线上advisory扫描 |

真实PG并发反例：本次旧规则+cash+生效价保持旧快照；另一事务一次修改三表后，下一次读看到全部新值。跨workspace、跨媒体同accountId逐行断言；缺日输出missing；PG NUMERIC NaN拒绝。新组合核心100%行/分支，三个仓储合计100%行/97.98%分支。

TDD：Domain/DB新模块分别先缺文件RED后GREEN；第一次Domain命令误用根workspace参数（项目不是npm workspaces），改在子包执行；DB首轮lint未使用参数失败，修正后全门禁通过。中间989全量为未补两安全反例的快照，最终991不混计。无测试失败被藏为skip。

质量检查重点：参数化SQL、三键授权、exact16MiB、控制查询量、单RR/短事务固定超时、解析后私有副本、安全错误。新增加权金额溢出与非法返回规则反例，不能500泄露源body或伪判false。未改Contract/be2保护文件/视觉/依赖。

## 未完成项与下一步

R010a2公开explain/生产动态规则provider/静音与SLA依然未完成，不以此基础建设占用可用功能验收。公开API默认窗口、失败/未适用等状态必须贴合arch Contract；本内核要求明确window，不替公有语义拍板。规则ID内部字符串，公开numeric边界不得丢精度。

`PlatformHealthRepository`/semantic lineage当前取computed_at，是转换时间；不能拿它证明源last_sync_time。后续真实freshness证据必须保留source时间/身份，不把当前时间或重算时间当新鲜。013迁移冲突/ETL三字段待裁仍保留；全信箱目标active，继续其余已冻工作，不等待本批审完才动。
