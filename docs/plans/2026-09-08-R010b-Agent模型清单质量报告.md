# P146 Agent 模型清单纵切片质量报告

代码 **6d9800b06844ccbb0f69ece99b269ee2df38fbb1**；本人 `be/r010`。交审前 `git merge main` 已 up-to-date（main c3451db）。候选已本地验证，**未合流、未部署、未生产验证**；未 push。

## 实际接通的范围

- `provider_model_capabilities` → DB RR/RO reader → Service → 真正 `createDataApiServer` / `data-api.ts` → `GET /api/v1/agent/models`；对应 `/api/internal/agent/models` BFF 已加入。
- Domain strict schema 直接供新 BFF 使用；arch fixture 去掉文档 `_note` 后通过同一 schema。不是另一套手抄 DTO。
- internal bearer + 当前 Session；个人空 grant 和团队均可读**全局非业务能力目录**。不读取任何用户凭证；不接受浏览器 provider/workspace/source 选择。
- `(provider,id)` 唯一；同模型在不同 Provider 下可共存；完整读最多1000行，第1001行是 overflow 哨兵。SQL 在传回应用前限制标识符/版本字段大小，非法大字段不透传；只 SELECT 明确五列，不读 error_summary/secret_ref。
- 请求与响应 requestId 关联、no-store、只读 GET、405 Allow=GET、未知 query 400、安全错误；HTTP/BFF exact16MiB 拒绝。共享 helper 仅增加 Allow 参数，原 POST 默认不变。

## 真实状态映射与未冻结配置

当前表没有 `label`、默认模型配置：label=原始 model ID，**default=false**；不把 fixture 中示例的默认模型移入生产。`failed`→`disabled`；`verified` 缺 tested_at/test_version → `documented_unverified`。present-invalid 不当 absent/null，整次502。

请 arch 审查上述保守映射；若要求默认项，需明确服务端的默认 provider+model 来源与现 Router 的一致性。不能把“清单 verified”解释成当前用户拥有有效 AK、SDK 当前已就绪、或允许调用媒体。原运行时 Router/凭证/工具权限检查均未绕开。

## TDD、门禁和质量检查

原始绿灯日志在 `output/qa/p146/`；前置缺模块 RED、首次本机监听 EPERM、错误测试列名及 lint 红灯保留在本轮工具记录，不能把这些第一次运行称为通过。EPERM 后仅提高 localhost 测试权限；成员列按真实 `is_active` 修正；未改生产 auth schema。

| 门禁 | 结果 |
|---|---|
| Domain 全量 | 78 files / **1143通过**；typecheck/lint通过 |
| DB 全量 | 94 files / **1012通过**；unit+实际PG混合套件，不是1012项PG；typecheck/lint通过 |
| Worker 全量 | 144 files通过、**1560通过+2外部opt-in跳过**；typecheck/lint通过 |
| Web 数据层 node | **201通过**；新BFF定向typecheck与四文件lint通过 |
| 新增定向 | Domain12；DB13unit+4实际PG；HTTP17+2实际PG；BFF5 |
| 核心覆盖 | Domain100%行/分支；DB100%行、92.3%分支；Worker新Service/Route100%行、88%分支 |
| 依赖检查 | 本批无依赖变更；Worker production **offline/cached audit0**，不是新在线公告核验 |

Worker未覆盖分支主要是直接绕过壳层的非法内部auth/坏service输入；真实壳层已在repo之前拒绝无Session/非法bearer。DB单元未覆盖的Date对象分支已由实际PG驱动执行。不得把部分分支覆盖报100%。

DB/Worker完整套件使用显式隔离库 `ka_be_r010_20260907_test`，串行，不交叉跑迁移。新增测试拒绝共享ka；afterAll只清理本文件新建合成 provider/identity/workspace。实际PG测试包括：未验证/失败映射、超过1000行、16MiB字段SQL拒传、personal空grant、team切换、旧token失效、退出401、成员撤销403且repo未执行。

质量自查：SQL只读且没有用户SQL/动态表名；源错误和私有字段不进DTO；GET路由没有Job/Agent/媒体副作用；内部请求只由服务端origin/token+Session构造；域schema和fixture一致；无arch Contract/be2保护文件/前端视觉改动。`git diff --check`及提交后`git show --stat`通过。

## 尚未完成与交接

- 未做 Next production build/完整前端类型门禁或浏览器验收；本批不处理其他人的UI依赖。
- 尚无真实生产模型探测、配置写入、默认模型选择、用户AK绑定、Agent会话/messages/SSE/run续传；仅模型清单链路可测试，不称“Agent可对话”。
- R010a1/a2源freshness/公开规则解释、013冲突、team ingestion、其余R012/R015/R010b仍留在长目标；本批未替代/关闭这些项。按arch纪律交审后继续未阻塞任务，无需等待本批✅。
