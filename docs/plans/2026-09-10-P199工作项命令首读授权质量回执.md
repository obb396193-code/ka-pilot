# P199 工作项命令首读授权质量回执

## 状态与边界

代码 **f022b0fe**，5个本人DB源文件/测试。implemented + targeted_verified，未arch验收合流、未部署、未push。仅本地工作项状态与告警静默存储，不调用媒体provider、不跑队列消费者。

P178审计发现两处首读按workspace/id取status与tuple，然后JS/live grant拒绝。**没有证明响应泄漏**，但不满足首读SQL先限权规范。AccountMute.ignoreAndMute与WorkItemCommand.apply现复用accountScopeParams/accountScopeClause；会话快照限定SQL后仍锁实时identity/membership/grant/account，撤权不被旧Session绕过。不把任务/私人工作项的只读矩阵放大为写权限，不开放team写。

无授权命中时只查同workspace的id存在性，区分404/403；无正文、状态和tuple进入该查询。原事务/状态转换/审计/回滚/输出守卫保留。

## TDD及门禁

新增两个真实PG反例，观察数据库驱动实际返回行：Session仅KUAISHOU，目标为同号TENCENT，拒绝且首读不返回TENCENT元数据。原代码两条红，修后绿。快照unit的SQL新增参数断言同步为完整四参，未降低测试。

| 包/命令 | 结果 |
|---|---|
| DB `TEST_DATABASE_URL=<本机专用合成库> npm test -- --run test/account-mute-ignore.unit.test.ts test/account-mute-repository.test.ts test/work-item-command-repository.test.ts test/account-mute-repository.unit.test.ts` | 80/80，其中真实PG27 |
| Worker `TEST_DATABASE_URL=<同库> npm test -- --run test/account-mute-http-pg.integration.test.ts test/r010/viewer-readonly-http.test.ts test/account-mute-http.integration.test.ts test/account-mute-service.test.ts test/account-mute-routes.test.ts` | 129/129，其中真实PG+HTTP4 |
| DB、Worker `npm run typecheck` / `npm run lint` | 全部通过 |
| DB `npm audit --offline --omit=dev` | 0；缓存审计，不冒称在线最新情报 |
| `git diff --check` | 通过 |

共 **209项不同定向**，不重复计覆盖率重跑。DB覆盖命令在上述四文件后附 `--coverage --coverage.include=src/account-mute-repository.ts --coverage.include=src/work-item-command-repository.ts --coverage.reporter=text --coverage.reportsDirectory=../../output/p199-db-coverage`：AccountMute行100%/分支97.45%，WorkItemCommand行100%/分支84.78%，合计行100%/分支93.9%。

采用writing-plans编排已冻任务、code-quality-checker检查：动态内容均值参数；谓词仅来自已有代码helper；正常路径无新额外查询，仅拒绝路径多一个按主键存在性读；无新依赖/凭证/前端变更。未引入等待外部调用的长事务。

## 待办

磁盘3.2GiB，仅定向门禁，未五包全量/build/清理。P178余下结构性限权路径和后台例外仍要统一审计回执，不用本批209项代表所有后台已测。014/021/F-OS-004/sop-run/P176继续按arch队列。P196已见main f06fadac合入；P197/P198/P199仍候选，不据本地交付标生产可用。
