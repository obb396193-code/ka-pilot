# R010 恢复门禁质量报告

日期：2026-09-07。后端分支 `be/r010`，已同步 arch main 的 v1.7.5 及 P104 合流回执。本报告只记本机实跑，不借用 arch 的独立数字。

## 代码与边界

| SHA | 变更 | 当前证据 |
|---|---|---|
| c30f6f1 | 维度行严格校验；复用 v3 现金考核；账户 key=media:accountId；非账户不冒充账户 | 45新增用例；六份权威样例直接读；行边界基础，不是维度查询已公开可用 |
| e1702e3 | 静音/变更集/T1 PG套件只清自己创建的UUID | DB真实PG全量通过；未改生产FK/降级保护 |
| b46ae5d | 旧v2 PG快照测试转到真实v3窗口reader | 新旧快照金额10→90/green→red/time成套，未改期望为unavailable |
| 1f4a974 | F-P103-1原始异常测试观测 | 捕捉 `Window reader unavailable` 原始堆栈；证明旧构造器缺window reader，普通snapshot从未调用；finally恢复Error全局 |

arch P104 已把前三笔合 main@7691819；最后一笔为独立补充候选。本轮没有自改Contract、React/视觉、依赖/lockfile、真实凭证、媒体操作或push。合main带入前端文件不等于be改前端。

## 门禁

| 命令/范围 | 实跑结果 |
|---|---|
| `npm --prefix packages/domain test -- --run` | 765/765；旧ready/unknown-lineage失败随arch新fixture解除 |
| DB full，显式TEST_DATABASE_URL | 69文件、692/692（包含真实PG，不是692个全为PG） |
| Worker full，显式TEST_DATABASE_URL | 115文件通过；1142通过，2个真实外部源opt-in skipped |
| Gateway full，显式TEST_DATABASE_URL | 8文件、36/36，含2个真实PG入站恢复例 |
| `npm --prefix apps/web test -- --run` | 140/140 |
| Domain/DB/Worker/Gateway typecheck、lint | 通过 |
| Web typecheck | 当前be工作树缺新前端依赖（shiki/ai/ReactFlow/BlockNote等），失败；package已声明，不擅自重装/修改前端 |
| Web lint | 0 error / 12现有warn |
| Domain/DB/Worker `audit --omit=dev --offline` | 缓存结果0漏洞，不代表实时远端审计 |
| 核心覆盖 | 维度模块100%行/分支；连共享窗口模块97.31%行/93.47%分支，67项定向 |
| diff check | 通过 |

### 真实PG重点回补

- DB：变更集41、T1持久调度6、账户静音8、session清理9、窗口历史5、维度三键4、012真实迁移4（内含多次up/down/up与孤儿/JSON类型保护断言）。
- Worker：单轮worker3、session清理CLI5、真实快照2、窗口4、table任务scope5、业务Session链2、bootstrap2、变更授权4、工作流5、backfill3、pipeline1、benchmark4（只含100账户1轮真实样本，不承诺1000/5000性能）。
- PG测试命令统一：`TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test PGCONNECT_TIMEOUT=3 npm --prefix <package> test -- --run`。这是本轮新建合成库，不是共享业务库。DB→Worker→Gateway串行，均不连接真实启航/KA/钉钉群。

## 失败记录不可隐去

1. 首轮DB684过/8失败：测试残留typed JSON阻止迁移down，account_mutes FK阻止旧套件清账户；保留迁移保护，按本套件UUID做teardown。
2. 初版teardown错误假设execution_runs有workspace_id，真实PG失败；改经changesets关联范围，再完整DB692通过。
3. 首轮Worker1140过/1失败：旧快照测试未注入v3 reader。修复后定向5过；原始异常补充诊断2过。
4. 中间一次Worker全量遇ENOSPC与55432拒连：28文件失败，914通过/3用例失败/3跳过。不是代码验收成功；没有改超时或降低断言。宿主空间恢复约3.9GiB，重跑1142+2过。
5. F-P103-2：DB/Worker原有vitest配置均为 `fileParallelism:false`，本轮没有依赖手动maxWorkers。不同命令对同一个库同时回放DDL仍不支持，必须隔离库/串行；不提高锁超时遮盖竞态。

日志（均为小型合成测试输出）：`/tmp/ka-p104-{domain,web,db-pg,db-pg-fixed,db-pg-final,worker-pg,worker-pg-final,worker-recovered,gateway-pg,raw-cause}.log`；覆盖 `/tmp/ka-dimension-rows-coverage.log`。最后通过为db-pg-final与worker-recovered，不能拿名字带final但ENOSPC失败的日志当成功。

## 质量与下一步

- 安全：没有新增网络/SQL/媒体执行；所有测试清理由本套件实际seed UUID定位，表名为固定常量；新原始异常观测仅测试环境，finally恢复，不降低生产保密边界。
- 性能：行解析bounded10000，额外行拒绝；没有新增N+1/媒体查询。JSON总字节守卫与服务端scope仍由现有source/HTTP负责，Domain行校验不冒充授权。
- 仍未完成：公开dimension/其他R010a1；a2动作/rollback/规则解释/静音HTTP；013团队快照及后续R012/014/015/016/R010b；OS f.yml/真实部署。按最新arch顺序继续，目标active，非complete。
- 品牌图收到R-FE-IMG-002：旧暗色候选被否，后续按新浅色三方向与参考图重做，仍先候选拍板、不直接替换public。此报告不把旧候选记作交付完成。
