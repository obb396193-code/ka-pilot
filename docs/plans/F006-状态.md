# F006 状态（前端 fe · 分支 `fe/f006` · 从 `main@v0.2-unaudited-baseline`）

> 2026-09-05 开工。规则：做完一条更新一条；compact/重开先读本文件。视觉老板看页面拍板，功能交 SHA 给 arch。
> 本地起法：`cd apps/web && NEXT_PUBLIC_KA_DATA_PROVIDER=mock KA_DATA_DEV_AUTH_CONTEXT_ENABLED=true KA_DATA_DEV_WORKSPACE_ID=00000000-0000-4000-8000-000000000024 KA_DATA_DEV_USER_ID=demo-user KA_DATA_DEV_ACCOUNT_SCOPE_JSON='[{"media":"KUAISHOU","accountId":"demo-account-07"}]' npx next dev -p 3400`

## 全局底座（页 1 顺带落地，全站复用）

| # | 项 | 状态 | 说明 |
|---|---|---|---|
| G1 | 字体切到模板默认 Geist + 系统中文（next/font/local，woff2 从 geist@1.7.2 OFL 复制到 `app/fonts/`） | ✅ | 之前 apps/web 用 Inter（Google 拉取，内网不可用）；老板字体拍板前不再动 |
| G2 | 语义状态/图表 token 提升到 `globals.css`（`--kp-status-*` / `--kp-chart-*`，light+dark；Tailwind `text-status-*`） | ✅ | 原来只在 `neutral-surface.module.css` 里，脱离 PageShell 就失效 |
| G3 | 侧栏：九项平铺 + 母版图标 + 「快速新建」下拉（三项 disabled 注明写接口未开）+「更多」；「设置」「治理后台(admin)」按 PRD 2.1 进头像菜单 | ✅ | 导航项/路由不变；放侧栏底部时 1366×768（视口 662px）会溢出滚动，故进头像菜单；badge 插槽保留、不写死示例数 |
| G4 | 侧栏底部「个人空间 ⇄ 团队数据」切换（Select，接 `POST /api/internal/auth/workspace`；切换后内容区按 workspaceId 重挂 = 重新查询） | ✅ | session 契约/客户端 = 从 `codex/fe-task5-session-bff@c3ed7b3` 原样复制 `session-contracts.ts`/`session-client.ts`/`session-client.test.ts`（内容逐字节一致，R-009 合入时 add/add 自动消解）；mock 模式用契约冻结 fixture |
| G5 | SiteHeader：面包屑=当前页名 + 数据健康 pill（绿/黄/红/灰）+ 降级时整页黄红条 + ⌘K（侧栏搜索钮同一面板） | ✅ | `GET /system/health` 未有 BFF 路径 → mock 走 fixture，真实模式显灰「待接入」；缺口写 inbox-arch |
| G6 | 内容区外壳对齐母版（`py-4 md:py-6 gap-4 md:gap-6`，px-4 lg:px-6） | ✅ | 放在工作台组件内，不动 layout，避免其他页双重 padding |

## 页 1 工作台 `/`

| # | 项 | 状态 | 说明 |
|---|---|---|---|
| 1.1 | 删「KA Data 权威版/自建平台版/双源对账」三 tab + 页面不再读 `data_view` | ✅ | 切空间即切源；`lib/data/data-view.ts` 形状不动，其他页仍用（页 3/4 再删） |
| 1.2 | 六 KPI 用母版 SectionCards 结构（label / 值 / 环比 badge / footer 副注 + 口径短公式，hover 全公式+来源） | ✅ | 缺数显 `−`；环比后端未给 → 显「环比待接」不造数 |
| 1.3 | 趋势卡：母版 ChartAreaInteractive 外壳（标题/7·30·90 切换）+ 图体 ECharts（沿用 `spend-real-cpa-trend`，X 轴改 MM/DD） | ✅ | 时间段切换写 URL `date_from/date_to` |
| 1.4 | 待处理队列：Tabs 全部/P0/P1/机会（带计数，窄屏 Select）+ 四件套卡（严重度色条）+ 按钮组（生成变更集/查看证据/忽略+原因 chip/静音 3 天/跳后台 disabled）+ 底部健康行 | ✅ | 删「自定义列」「新建任务」；within_threshold_count 契约未冻 → 显「未返回的账户不自动判定健康」 |
| 1.5 | 右栏：警报监控（值班/P0 未确认/升级中三格 + 列表）/ T+1 回收 / 我的待办 / AI 早报（发群 disabled） | ✅ | 8/12 + 4/12 |
| 1.6 | 血缘条收成一行紧凑页脚（来源·截至·覆盖·版本；`DataStateFrame lineage="inline"`） | ✅ | 其他页仍用完整版 |
| 1.7 | 本地 mock 跑通 + typecheck + lint + 测试 + 1440/1366/390 无横向溢出 + dark | ✅ | tsc 0 错、eslint 0、node test 77/77（合 R-009 后 77）；1366×662 侧栏不溢出；控制台 0 error；`echarts/zrender` 主目录 node_modules 原本没装，已 `npm install --no-save` 补上（lock 未动） |
| 1.8 | 团队只读空间（AUTH-001 无 execute grant）禁用「生成变更集」等执行入口，提示切回个人空间 | ✅ | 数据门与只读门分开提示 |
| 1.9 | 设计规范落代码后页 1 重刷：三模式开关 + 色卡 + 简洁 B KPI + 点彩 chip + 字体栈；tsc/lint/77 测试全绿；1440/390 验过；全彩+主色持久化验过 | ✅ | 页面 http://localhost:3401/ |
| 1.10 | 老板看演示后要求对齐密度：KPI 六张一排紧凑、开关胶囊化、页 1 卡片内边距 16–20px、总览数字去 .00；全彩加 12px 圆角与淡底环比/chip | ✅ | |
| 1.11 | 老板反馈「选个荧光色黑白模式也跟着难看、开关变色很丑」→ ①开关永远黑白（选中态实心黑，只有色点显示主色）②主色驯化：黑白·点彩用「墨水版」`--kp-ink`（oklch 亮度夹到 0.42–0.6、饱和度封顶 0.13），黑白+彩/全彩用 `--kp-tone`（饱和度封顶 0.2）；③环比缺数显「—」灰胶囊对齐演示版式 | ✅ | 实测 #22e05a 荧光绿 → 黑白下 oklch(0.6 0.13 147)，全彩下 oklch(0.72 0.2 147) |
| 1.12 | 老板看页面拍板：三段开关/色卡/主色驯化「都可以」 | ✅ | 默认模式仍为黑白·点彩 |
| 1.13 | 老板：「搜索与 Agent」入口和抽屉丑 → 拆成两件：**⌘K 命令面板**（shadcn 官方 Command/cmdk：最近访问·页面·动作·「问 AI」行，键盘 ↑↓↵esc；对象搜索接口 `GET /search` R-010 前用示例并标「示例」）+ **AI 助手悬浮窗**（老板定：不放顶栏，右下角悬浮球 + 浮层面板；面板全部用 Vercel AI Elements 官方件 prompt-input/suggestion/conversation：上下文 chip 页面/空间/+账户、高频问题、模型选择器、发送；后端 = Claude Agent SDK 经 CC Switch 网关切模型，接入前诚实空态不伪造回答/模型清单） | ✅ | 新增依赖 cmdk / ai / nanoid / use-stick-to-bottom；shadcn 新增 command/dialog/hover-card/input-group/textarea/scroll-area 官方源码，全部登记 manifest；旧 `agent-command-shell.tsx` 删除 |
| 1.14 | 老板看页面拍板 → commit | ✅ | 存档点 `449ccec`（2026-09-05，老板口头「做一个存档点」），路径限定提交 145 文件，不含 node_modules / fonts-demo / misans 生成物 |

## 环境/协作记录
- **git 事故（已理顺）**：arch 与 fe 共用主目录，我 checkout `fe/f006` 后 arch 三笔 commit（d070c1d / ad5b701 / d7b6260）落在我分支；已 `git branch -f main d7b6260`（纯快进）、主目录清干净回 `main`，`fe/f006` 改在独立工作树 **`/private/tmp/ka-fe-f006`**（node_modules 软链主目录）。详见 inbox-arch F-006-Q1。
- 本地端口 **3401**（3400 被退役的 Codex root 会话 dev server 占着，未杀）。
- mock 下切到「团队数据」会按 DATA-ROUTE v1.2 模拟 team→ka_data（页脚血缘显 KA Data）；真实模式浏览器固定发 platform，由服务端路由。

## 设计规范拍板记录（老板一对一，2026-09-05 起）

拍板小样：`docs/frontend/ui-assets/decisions/style-directions-v2-2026-09-05.html`（主题三版 + 页内右上角开关 + 冷暖色系开关 + KPI B/C 开关）、`font-directions-2026-09-05.html`（字体 F1–F6，含「谁最像苹方」并排）、`misans-fixed.css`（MiSans 切片字重映射修正：330/380/520/630 → 400/500/600/700）。静态服务 `python3 -m http.server 3402 --directory /private/tmp/ka-fe-f006`，字体资源软链 `fonts-demo/` → 主目录 `apps/ui-layout-demo/public/font-comparison`。

| 项 | 结论 | 状态 |
|---|---|---|
| 壳 | 侧栏/顶栏/卡 = shadcn 母版结构，真黑真白，灰只做发丝线（老板：永远不要「灰灰的」） | ✅ 定 |
| 主题 | 三套同时做、同一套组件换变量：1 黑白·点彩 / 2 黑白+彩 / 3 全彩（单色系） | ✅ 定 · **已落代码**：`lib/theme/theme.ts`（模式+主色+12 预设+首屏脚本）、`components/business/theme/{theme-provider,theme-switch}.tsx`、`globals.css` 三模式 token（`data-theme-mode` × `--hue` 派生 chart/primary/sidebar） |
| 默认主题 | 1 黑白·点彩（颜色只做点染：chip 一粒色点、图表第二条线一个点缀色、红绿黄只表状态） | ✅ 定 |
| 切换位置 | 顶栏右上角、搜索框左边，一点即换（不进设置）；胶囊分段样式对齐演示 | ✅ 定 · 已落代码（修过一个 Radix Tooltip 覆盖 Toggle data-state 导致选中态不显示的 bug） |
| 主色 | 老板要「真正的色卡 + 吸管」→ 弹层 = 8 预设 + 明度饱和面 + 色相条 + hex + 吸管（EyeDropper API，Chrome/Edge）；一个 `--hue` 变量派生图表三色、按钮、选中态。正式版组件：**Kibo UI Color Picker**（MIT，仿 Figma，带吸管，`source-cache/kibo-ui/color-picker.json` 已缓存）；备选 Dice UI color-picker（public）。先例 shadcn Theme Customizer / tweakcn。**两家官方源码已并排跑在同一容器**：`apps/ui-layout-demo/color-picker-compare.html`（fe 工作树，vite 5174；Kibo 只改 import 路径 + 面板需父容器给高；Dice 改 import 路径 + 补 shadcn popover/select 官方文件 + 3 个小 hook；Kibo 需 `color` 包，已 `npm install --no-save`）。预设候选带出处：shadcn 8 / Radix 9 阶 8 / Tailwind 600 8 / tweakcn 25（jnsahaj/tweakcn theme-presets.ts 抓取）/ 品牌 8；我推荐 12 个在 `src/color-picker-compare/presets.ts`。**老板定：取色器用 Dice UI、12 预设两排；默认主色先定靛蓝，看橙色全彩演示后改为 D-CON 橙 #ff6a2c（= PRD 5.1 品牌色）**（2026-09-05）。**已落代码**：`components/dice/`（官方源码，登记在 source-download-manifest）+ `components/ui/popover.tsx`（shadcn 官方）；顶栏色点弹层 = 12 预设 + Dice 面板 + 吸管；选择存 localStorage（账号级持久化 = 契约缺口 F-006-Q2） | ✅ 定 |
| 全彩质感 | 老板两次说没质感 → 改成单色系：一个主色 + 中性色，按钮实色、页底近中性、白卡软阴影、图表同色相深浅两线；去掉多色卡片和标题色条 | ⏳ 待老板看第 3 版 |
| KPI 卡 | 老板二次改定：**C 组合但只留趋势线**——标签+ⓘ口径 / 数字+环比 / 一条往日走势线，**不放说明文字**，不放分段条进度条；六张一排、紧凑 | ✅ 定 · **已落代码** `kpi-cards.tsx`（走势线只画后端有日序列的消耗/CPA，其余画虚基线占位不造数）；严重度 chip 点彩式 `severity-badge.tsx` |
| 字体 | ✅ 老板定：Mac 用苹方、Windows 用最像苹方的自带字体。**已落代码**：依赖 `misans@4.1.0` + `scripts/fonts/prepare-misans.mjs`（postinstall 生成 `app/fonts/misans/`，gitignored，字重 330/380/520→400/500/600，300 切片 6.7MB 按需下载）；字体栈 Geist → PingFang SC → MiSans → Microsoft YaHei，Mac 零下载 | ✅ 定 |
| 字重坑 | misans 切片把常规体登记为 330，浏览器要 400 抓到 Medium → 显得粗；进仓时必须用修正后的映射 | ✅ 记录 |

定案后落真代码顺序：globals 三套主题变量 → 顶栏开关组件（从组件库取 ToggleGroup/Segmented）→ 字体切片进 `apps/web/app/fonts` + 字体栈 → 首页 KPI 换简洁 B 结构 → 页 1 重刷给老板看。

## 全站底座（页 2–4 批次顺带落地）
| # | 项 | 状态 | 备注 |
|---|---|---|---|
| G7 | `PageHeader`/`PageBody` 统一页头（标题/一句口径/右侧动作/「脱敏 Mock」角标）+ 卡密度覆盖；`PageShell`/`DataViewSwitcher`/`AnalysisTable` 三旧件删除，诊断详情页/占位页迁到新壳 | ✅ | 页面壳 = 母版 SectionCards 的 padding 节奏 |
| G8 | `DataGrid` 通用表壳：**TanStack Table v9**（母版同款）+ shadcn Table；粘性表头、排序、「自定义列」显隐、分页（每页行数/首末页）、`MissingValue` 缺数 `−`；`format.ts` 只做展示格式化（¥/千分位/百分比/日期，**不算数**） | ✅ | 列显隐初始态必须走 hook 参数（曾在渲染中 setState → hydration 不一致，已修） |
| G9 | 面包屑：分隔符 `<li>` 与 `BreadcrumbItem` 平级（原嵌套 `<li>` 在二级页 hydration 报错） | ✅ | 影响所有 `xx › 详情` 页，已全部 0 console error |
| G10 | `middleware.ts`：无 `ka_session` cookie → `/login?next=`；`NEXT_PUBLIC_KA_DATA_PROVIDER=mock` 时跳过；matcher 排除 api/_next/静态 | ✅ | 真实模式生效；mock 本地可直进 |
| G12 | 老板 09-05 二次拍板：**全部表格统一母版 DataTable 格式**（勾选框列 / 类型 chip / 带图标状态 chip：绿勾·红叉·黄叹号·转圈·时钟·虚线圈 / 行末 ⋮ 菜单 / 「已选 N 条，共 M 条」页脚 + 勾选后批量动作）→ `DataGrid` 加 `selectionColumn`/`actionsColumn`/`StatusChip`/`TypeChip`/`bulkActions`/`renderExpanded`；账户池、投放任务、数据总表、维度透视四张表已换（透视勾选后按钮变「分析所选 N 个」）。不做拖拽把手（母版拖拽是手动排序，后端无排序接口） | ✅ | 账户达标状态映射集中在 `accounts/account-status.ts`，全站一份 |
| G11 | AI 悬浮球换「B 光晕球」：conic 渐变慢转 + 外圈呼吸辉光（`@property --kp-ang`，`prefers-reduced-motion` 关动画）；浮层玻璃发丝边 `.kp-glass` | ✅ | 老板拍板 B（小样 `agent-launcher-directions-2026-09-05.html`） |

## ★ 老板拍板清单（冻结，2026-09-05；后续任何开工提示词与此冲突以此为准，冲突点由 fe 标出给老板看）

| # | 项 | 老板定的 |
|---|---|---|
| D1 | 默认主题 | 黑白·点彩，不默认全彩；三模式右上角一点即切；主色色卡 Dice UI，默认 D-CON 橙 |
| D2 | 底子 | 真黑真白不要灰；颜色只做点染；全彩要单色系克制 |
| D3 | 字体 | Mac 苹方 / Windows 最像苹方的自带字体（Geist → PingFang SC → MiSans → 雅黑） |
| D4 | KPI 卡 | C 组合只留趋势线、不放说明字；六张一排；**全站同一种卡**（账户小传展开区也用它） |
| D5 | 组件 | 绝不自写，组件库官方源码原样进仓；同能力多库并排给老板挑 |
| D6 | 表格 | **全部表格 = 母版 dashboard DataTable 一模一样**：拖拽把手 / 勾选框 / 名字一行（不放 ID 副行）/ 类型 chip / 绿勾红叉黄叹状态 chip（固定色）/ 行末 ⋮ / 「已选 N 条」页脚 |
| D7 | 详情打开 | 行内展开（点账户名在行下方展开）；「打开完整账户页」按钮保留 |
| D8 | 账户池 | 定义 = 全量账户按生命周期分层看；字段叫**产品名**（账户级，淘宝 / 手淘软件…）；分层卡 / 流程条 / 看板三种都留用户自己切，矩阵不要；八档 = 基建中 / 等待 / 冷启动 / 起量 / 稳定跑量 / 掉量 / 待关 / 已关（阶段判定在后端，F-006-Q4） |
| D9 | 登录 | 账号密码为主 + BUC 置灰在下；布局左右分屏；左面板不要模板动效，照大厂做「品牌图 + 表单」；图由 Codex 生（F-006-Q5，经 arch 派）；图到之前正式页用「光谱」顶着 |
| D10 | AI 助手 | 右下角悬浮球（B 光晕球）+ 浮层，Vercel AI Elements 官方件；后端 = Claude Agent SDK 经 CC Switch 网关；接入前诚实空态 |
| D11 | 搜索 | ⌘K 命令面板（shadcn Command），顶栏搜索框是入口 |
| D12 | 数据纪律 | 前端永不算数；缺数显 −，分母 0 显 −；不造真实感假数据，demo 标「示例 / 脱敏 Mock」 |
| D13 | 流程 | 视觉先给老板看页面再 commit；被否整颗回退不辩护；老板对话拍板 > 开工提示词 |

**待老板定（保持现状不动）**：登录最终方向（等图）；分层卡 vs 流程条最终留哪个。

**下一阶段（老板 09-05 口头）**：arch 会出新目标 = 前端能做的全部做出来，然后逐块对比精修。fe 的候选清单见本文件末尾「全前端铺开清单」。

## 页 2 登录 `/login`（2026-09-05 做完，待老板看）
| # | 项 | 状态 | 备注 |
|---|---|---|---|
| 2.1 | 候选：shadcn 官方 Blocks login-01…05 原样渲染在 `/login/candidates`（`components/blocks/login-0N/`，官方源码只改 import 路径；02/04 引用的 `public/placeholder.svg` 补了一张中性占位） | ✅ | 老板挑壳；候选页只在 dev 用 |
| 2.2 | 正式页 `/login` = login-03 壳（muted 底 + 居中卡 + 顶部 logo）业务适配 `components/business/auth/login-form.tsx`：BUC 登录按钮 disabled 注明「内测后开放」、用户名/密码走 `loginSession({provider:"internal_test"})` → `readSession` → 回跳 `?next=`；mock 模式按钮文案「进入工作台」 | ✅ | 表单件 = shadcn `field.tsx` 官方新件；候选选定后可一换壳 |
| 2.3 | 老板：「登录页还有别的吗？看看好的 2B 登录页什么样」+ 定「账号密码为主 + BUC 次」→ `LoginForm` 改为表单在上、BUC 置灰在下（/login 同步生效）；三种 2B 主流方向做成整页真页 `/login/directions`：A 极简居中（Linear/Vercel）、B 左右分屏（Stripe/Retool，黑色品牌面板 + 主色微光 + 三条能力）、C 品牌底 + 玻璃卡（Clerk 演示风，主色晕染 + 颗粒 + 毛玻璃）；同一个表单只换壳，无新依赖 | ⏳ 待老板挑 | 挑定后正式页换壳、其余两个删；五块官方原样 Block 仍在 `/login/candidates` |
| 2.4 | 老板第二轮：定「左右分屏」，但左面板「不高级、没质感、像模板动效」→ 左面板重做四种，全部用组件库官方背景件（源码进 `components/magicui/`、`components/aceternity/`，登记 manifest + source-cache）：B1 粒子（Magic UI Particles，鼠标跟随）/ B2 光束（Aceternity Background Beams，需 `motion@13.2.0`）/ B3 星空（Aceternity Stars Background）/ B4 素格（Magic UI Interactive Grid，浅底）；文案收成一句话 + 一行注，去掉网格 + 光斑 + 三条能力；入口 `/login/directions`，`?panel=particles|beams|stars|grid` | ⏳ 待老板四选一 | 极简/玻璃两个落选方向仍留作对照；stars-background 唯一改动 = React 19 的 RefObject 类型 |
| 2.5 | 老板第三轮：四种面板仍不喜欢（「你自己想的特效」），要看大厂真实登录页（图片 / 生图 / 炫动效 / 交互）→ 用老板 Chrome 实测巨量引擎、千川、磁力金牛、腾讯营销、飞书、Stripe、Figma、Linear（笔记 `decisions/login-references-2026-09-05.md`，含生图 brief）；结论：投放平台 = 一张高质量品牌 3D 图撑全场 + 浮卡。本轮候选 6 个：C1 全幅图 + 浮卡（巨量式）/ C2-1 分屏 + 图（飞书式）/ C2-2 液态金属 / C2-3 光谱 / C2-4 光球 / C2-5 线场（后四个 = React Bits 官方 WebGL 件，`ogl` 依赖，`components/reactbits/`，登记 manifest）；图用 Unsplash 免费 3D 渲染占位标「示例图」。粒子/光束/星空/素格四种连同 magic-ui / aceternity 源码和 `motion` 依赖整颗删除 | ⏳ 待老板挑 | 入口 `/login/directions`；截图工具落不了盘，参考站只留文字笔记 + 链接 |
| 2.6 | 老板：图交给 Codex 生（经 arch 派活）→ inbox-arch **F-006-Q5**（两张尺寸 / 落盘路径 / brief / 交付方式）；图到之前正式页 `/login` 先用「光谱」分屏顶着（老板定） | ✅ | 图到后换 `login-directions.tsx` 里两个占位 URL 并把正式页切到图版 |

## 页 3 账户池 `/accounts` + 数据分析 `/data`（2026-09-05 做完，待老板看）
| # | 项 | 状态 | 备注 |
|---|---|---|---|
| 3.1 | 账户池表：DataGrid，列 账户/负责人/阶段/消耗/真实 CPA/考核价/达标 chip/余额·断量/关联任务；阶段·余额断量·关联任务契约未给 → 整列 `−` 不造数；负责人筛选可用，生命周期/星标/导入账户 disabled 注明接口 | ✅ | 数据形状沿用 `lib/data`（`account.summary`）|
| 3.2 | 点行开右侧抽屉（Sheet 480px）：消耗/真实 CPA 双卡、CPA 与考核价趋势（ECharts 走 token）、当前诊断、余额·断量/计划层占位、「打开完整账户页」→ `/accounts/[id]` | ✅ | 抽屉数据 = `account.detail` + `adaptAccountDetail`，与全页同源 |
| 3.3 | 账户全页 `account-detail-view.tsx` 迁到新壳（去 PageShell/三 tab） | ✅ | |
| 3.4 | 数据分析改三子页线型 tab（DATA-ROUTE v1.2 删三 tab）：**大盘** `/data`（六 KPI + 消耗/CPA 趋势）/ **数据总表** `/data/table`（账户 × 全部可得字段，KA 侧列默认隐藏用「自定义列」打开，导出 Excel disabled 注明 R-010）/ **维度透视** `/data/pivot`（8 维 chip：账户可用；资源位/出价工具标「来源待接」；其余「未开放」；「分析这 N 个」直接开 AI 悬浮窗带上下文） | ✅ | 三子页共用 `app/(main)/data/layout.tsx` |
| 3.5 | 老板问「为什么点开是右侧弹出」→ 做了抽屉/面板/行内/全页四种 demo，**老板定：行内展开**（点账户名在行下方横排展开小传，「打开完整账户页」按钮保留在展开区和 ⋮ 里）；其余三种和切换下拉已删，`account-peek.tsx` 只剩行内版 | ✅ | 旧 `account-drawer.tsx` 已删 |
| 3.6 | 账户池表换母版格式：状态 tab 带计数（全部/超考核/观察/达标）、负责人筛选、账户名 = 母版 link 按钮开小传、⋮ = 查看小传/打开完整账户页/问 AI/（分配负责人·星标·加入任务置灰注明接口）、勾选后批量动作置灰注明接口；生命周期筛选暂撤（字段未给） | ✅ | |
| 3.7 | 老板看图说「和母版不一样」→ 逐项对齐母版 DataTable：加拖拽把手列（dnd-kit，母版同款，只改本地顺序）、账户名只留一行（去掉 `KUAISHOU · demo-account-07` 副行，媒体变「类型 chip」显中文「快手」，账户 ID 变默认隐藏列）、状态图标改母版固定色 `fill-green-500` / `fill-red-500` / `fill-amber-500`；任务表同样处理（任务 ID 隐藏列）；数据总表/透视同步 | ✅ | `DataGrid` 新增 `dragColumn` / `onReorder` / `useLocalOrder`；`actionsColumn` 回调多给表格行以便 ⋮ 里「展开小传」 |
| 3.8 | 老板：账户池定义 = **全量账户按生命周期分层看**（基建 / 等待按产品分 / 起量 / 跑量 / 掉量 / 待关），之前只做了表壳 → 加分层：顶部生命周期条（全部 + 8 档带户数，点一档即筛）、产品 / 负责人筛选、「按产品分组」（每产品一段：户数 + 阶段分布 + 自己的表）、表加产品 chip 与阶段 chip（点彩色点）。阶段 / 产品字段契约没有 → 真实模式显「未接入」，mock 用 `lib/data/fixtures/account-lifecycle.mock.json` 覆盖层，mock 账户从 3 户补到 24 户（4 产品 × 8 阶段，等待/基建/已关户消耗为真 0）；契约缺口写 inbox-arch **F-006-Q4**（stage/since/reason/product/tags/balance，判定规则在后端） | ✅ 待老板看 | 阶段 enum/文案/颜色在 `lib/data/account-lifecycle.ts`；`DataGrid` 加 `showColumnPicker`/`showPagination` 供分组段用 |
| 3.9 | 老板：展开区的 KPI 卡「怎么又变了」→ 行内小传改用工作台同一个 `KpiCards`（六张一排：消耗 / 真实 CPA / 阶段 / 余额·断量 / 计划层 / 关联任务，CPA 走势线取 trend），下面趋势 8/12 + 诊断 4/12；「打开完整账户页」按钮保留（老板定） | ✅ | `KpiCards` 加 `className` 供覆盖栅格内边距 |
| 3.10 | 老板：「产品」→ **产品名**（账户级字段，如淘宝 / 手淘软件，每个任务投的产品名不一样）；「账户池能换点别的表达方式」→ 分层做成四种可切（右上 ToggleGroup，`?view=` 可直达）：分层卡 / 流程条（开户→关户从左到右，段宽随户数 + 分布细条）/ 产品名 × 阶段矩阵（格子是户数，点格子同时筛产品名和阶段）/ 看板（一档一列，账户是卡，替代表格）；mock 产品名改为淘宝 / 手淘软件 / 闲鱼 / 淘特；Q4 字段名改 `productName` | ⏳ 待老板四选一 | `accounts/lifecycle-views.tsx`；定一种后删其余 |
| 3.11 | 老板问「字体和数字字体是不是变了」→ 实测 DOM：body / 标题 / 数字 / chip 的 font-family 仍是 geistSans → PingFang SC → MiSans，数字 `tabular-nums`，Geist 与 MiSans 切片文件 09-04 23:46 后未变；MiSans 只为苹方缺的个别符号兜底加载 | ✅ 没变 | 老板浏览器当前主色是他自己选的绿 #059669，不是字体变化 |
| 3.12 | 老板：「产品名 × 阶段」矩阵不要；分层卡 / 流程条 / 看板**三种都留、用户自己切**（他还在纠结，先保留）；分层卡再打磨 → 左边「全部账户」深色锚点卡，右边八档中屏 4×2 / 宽屏一排 8，每卡底部一条按户数占比的细线（只是视觉占比，不显百分数）；切换器去掉「演示」字样，正式叫「生命周期分层」 | ✅ 待老板再看 | `?view=tiles|pipeline|kanban` 可直达 |

## 页 4 投放任务 `/tasks`（2026-09-05 做完，待老板看）
| # | 项 | 状态 | 备注 |
|---|---|---|---|
| 4.1 | 列表：TASK-LIST-001 四态 fixture（`lib/data/fixtures/task-list/*.json`，mock 用 `?state=ready|partial|stale|empty|401` 切，右上 Select 可切）；真实模式 `GET /api/internal/tasks` 经 `taskListResponseSchema` 校验；partial/stale 顶部黄条按契约文案；401 用错误信封 | ✅ | 页面 `/tasks`；四态截图都验过 |
| 4.2 | 表列：任务/状态 chip/周期/目标/已完成/达成·pacing 条（后端 `pacing` 结果直画，含时间进度刻度与预计缺口）/考核价/达标率/账户数/待处理 P0·P1；tabs 全部/进行中/准备中/已结束；「新建任务」disabled 注明写接口 | ✅ | pacing 全由后端，前端只画 |
| 4.3 | 详情 `/tasks/[taskId]` 六页签（PRD 2.3.2）：总览取列表同一 pacing（目标/已完成/时间进度/预计完成/需日均/考核价/挂载账户/待处理 + 动作分叉卡）；数据/账户/素材/时间线/复盘 = 诚实空态注明各自接口 | ✅ | `GET /tasks/:id` 未开放前不伪造 |
| 4.4 | 验收：tsc 0 错、eslint 0 错（5 条 `<img>` 警告在官方 blocks/AI Elements 原样件）、node test 77/77、全部新路由 200、控制台 0 error（含二级页）| ✅ | 2026-09-05 06:5x |
| 4.5 | 老板逐页看 → 拍板 → 一次路径限定 commit | ✅ | 存档点 `449ccec`（含页 1–4 + 设计规范 + 底座）；后续精修在其上继续，SHA 已写 inbox-arch 交 arch cherry-pick |

其余五个导航项（自动化/商品素材/报告/知识库/集成与通知）仍是 `ModulePlaceholder`（已迁新壳），按页面规划待后续批次。

## 契约缺口（已写/待写 inbox-arch）
- [ ] 数据健康横幅：`GET /system/health` 需要 BFF 同源路径（建议 `GET /api/internal/system/health`），R-010
- [ ] 六 KPI 环比 + 考核价副注：`account.summary` 行无环比/考核价字段，前端显「待接」

## 全前端铺开清单（fe 按 PRD 2.2 整理，等 arch 正式目标；★ = 有契约/mock 能出真页面，○ = 只能出诚实空态壳）

| 域 | 页面 | 路由 | PRD 级 | 现状 | 能出到什么程度 |
|---|---|---|---|---|---|
| 工作台 | 工作台 | `/` | P0 | ✅ 已做 | ★ |
| 登录 | 登录 | `/login` | P0 | ✅ 已做（等图） | ★ |
| 任务 | 任务列表 / 任务详情 | `/tasks`、`/tasks/[id]` | P0 | ✅ 已做 | ★ 列表；详情五页签 ○ |
| 数据 | 大盘 / 数据总表 / 维度透视 | `/data`、`/data/table`、`/data/pivot` | P0 | ✅ 已做 | ★ |
| 数据 | 盯盘 / Gap 对账 / 自助报表 / 策略分析 | `/data/live`、`/data/gap`、`/data/reports`、`/data/strategy` | P1 | 未做 | Gap 对账 ★（reconcile 契约在）；其余 ○ 壳 + 示例 |
| 账户 | 账户池 / 账户详情 | `/accounts`、`/accounts/[id]` | P0 | ✅ 已做 | ★（阶段/产品名等 Q4） |
| 账户 | 基建管理 / 开户测试跟踪 | `/accounts/infra`、`/accounts/testing` | P1 / P2 | 未做 | ○ |
| 报告 | 日报 | `/reports/daily` | P0 | 未做 | ○ 12 模块壳 + 一键发钉钉置灰（日报接口未开） |
| 报告 | 周报复盘 / 结算对账 / AI 提效 | `/reports/review`、`/reports/settlement`、`/reports/ai-impact` | P1 / P2 | 未做 | ○ |
| 自动化 | 自动化中心 / 工作流画布 / 运行中心 / Agent 运行监控 | `/automation`、`/automation/workflows/[id]`、`/automation/runs`、`/automation/agent-runs` | P1 | 未做 | 画布 ○（需选画布库，先查库）；其余 ○ |
| 商品素材 | 素材池 / AIGC 下单 / 商品池 / 拆解相似 | `/materials`、`/materials/order`、`/materials/products`、`/materials/analysis` | P2 | 未做 | AIGC 下单 = iframe 内嵌（可真做）；其余 ○ |
| 知识库 | 知识库 | `/knowledge` | P1 | 未做 | ○（复刻 CR 编辑器/文档树/@双链，量大，单独排） |
| 集成 | 集成与通知六件 / 卡片中心 / 值守告警 | `/integrations`、`/integrations/cards`、`/integrations/alerting` | P1 | 未做 | ○ |
| 协作 | 协作中心 | `/collab` | P1 | 未做 | ○ |
| 资产 | 公共资产治理 | `/assets` | P2 | 未做 | ○ |
| 头像菜单 | 设置 / 治理后台 | `/settings`、`/admin` | P0 / P1 | 未做 | 设置 ★（偏好/凭证绑定按 AUTH-001）；治理后台 ○ |
