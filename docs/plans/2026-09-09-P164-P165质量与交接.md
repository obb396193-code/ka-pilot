# P164 / P165 质量与交接

日期：2026-09-09。合流源 `be/r010`；仅后端，无媒体写、真实凭证、前端视觉、Contract 修改或 push。

## 独立提交与状态

| 批次 | 代码 SHA | 状态 |
|---|---|---|
| P164 返点版本只读仓储 | c2f73f937249776fd723df81f7f79bf50558f3a3 | implemented / focused PG verified / 待 arch 审；未接 HTTP、未部署 |
| 同步主线 | 1342f6f | 已含 arch 对 P161–163 的合流及 be2 S6c/018；不是新功能交付 |
| P165 S6 合流 fixture 去重 | cad5854aa2d574be82a8b1f333ea9c4dffd909da | 4 个 be 自有 Worker 测试修复；待 arch 审 |

## P164 已实现

`CoefficientReadRepository.read(auth, businessDate, media?)` 是内部读取模型，非另造公开 DTO。读取现有 `channel_coefficients`，与 R012 整体变更记录所需的 014 不同，本能力可以独立实现。

- 先解析唯一 ApprovedWorkspaceAuthContext，再在 RR/RO 同一快照复核 active personal workspace、workspace-local user、admin membership、identity；team/nonadmin 在连接前拒绝。这里是配置读取，不派生账户 grant，不给空 scope 增权。
- 版本按 media / effectiveDate / BIGINT id 稳定排序；当前值选不晚于业务日的最新生效日。未来版本留在历史，不伪装当前。同一当前生效日多版本 fail closed，不任取一行。
- 关联修改人限定 workspace；seed 的 NULL changed_by 为 unrecorded，外空间/孤儿引用为 unavailable，不暴露其用户 ID/姓名。现表没有 evidence_url，内部明确 not_stored，不编链接。
- 两次固定查询，无 N+1；参数绑定 workspace/media，LIMIT 10001 为哨兵，已知完整 10000 行可返回。长列在 SQL 侧限界，UTF-8 返回体精确达到 16MiB 即拒绝；NaN、非法日期、重复 ID、外 scope、错误 op 均拒绝。
- 异常只抛稳定 typed code，不带 SQL、上游 body、token 或个人身份值；57014 单独归超时。未新增依赖、缓存、业务写或公开路由。

## TDD 与实际门禁

起初新测试因模块不存在失败；实现后 strict typecheck 的 6 个 narrowing 错误修复，最终结果如下。合 main 后又复跑了同一组 37 项及 DB 门禁，不借用旧分支结果。

| 门禁 | 实际结果 |
|---|---|
| 新仓储真实 PG | 10/10 |
| 新仓储 unit | 19/19 |
| 既有 coefficient seed PG 回归 | 8/8 |
| DB typecheck / lint | 通过 |
| 新仓储覆盖率 | 行/语句/函数 100%，分支 95.4%；覆盖采于合 main 前相同 P164 代码 |
| DB production audit | `npm audit --omit=dev --offline --json` 缓存报告 0；不是线上实时漏洞核验 |
| P165 账户/任务 service + HTTP | 4 文件 56/56 |
| 合流修复后的 Worker typecheck / lint | 通过 |
| diff --check | 通过 |

PG 命令（在 packages/db）：

```sh
TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_p158_20260909_test npx --no-install vitest run test/coefficient-read-repository.test.ts test/coefficient-read-repository.unit.test.ts test/coefficient-seed-repository.test.ts --maxWorkers=1
npm run typecheck
npm run lint
```

以上 URL 是本机隔离合成测试库，不是内网凭证。新用例在原隔离库执行当前 migrations（含 018），使用随机 workspace/identity，清理仅本用例对象；没有新建/删除数据库。

Worker 命令（在 apps/worker）：

```sh
npx --no-install vitest run test/account-list-http.test.ts test/account-list-service.test.ts test/task-list-http.test.ts test/task-list-service.test.ts --maxWorkers=1
npm run typecheck
npm run lint
```

## P165 合流红灯及修复

合 main 后 typecheck 真红：四个测试文件共 14 个 TS1117。原因是 P158 的临时必填字段与 be2 S6c 的完成版字段同时进入对象。删除旧重复字段，保留 S6c stage/poolStatus；readiness 保留样本实际已有的一个 account，不把 linkedAccountCount=1 的样本写成零账户。没有改 be2 生产服务或字段语义。修后 56 项和 Worker typecheck/lint 均绿。

## 质量审查与剩余边界

- 安全：本次代码静态检查未发现新增 Critical/High；可信 auth 与库内重新授权、SQL 参数化、返回体限额、跨 workspace 作者隔离均有测试。无新增 HTTP，所以本批不存在新 CSRF/BFF 边界。
- 性能：最多 10001 行、两次查询、O(n log n) 排序，复用事务超时；尚未做真实高并发负载测试。10000 行 PG、16MiB 单元边界已实测，不能代表线上延迟承诺。
- 可维护性：新生产文件 105 行；解析独立，未新增圈复杂度分析工具，不声称精确圈复杂度通过。
- 磁盘从约 7GiB 降至 4.3GiB，低于仓库 8GiB 全量门槛；本批未重跑全包 test/build，没有清缓存或启动业务 worker/beat。相关定向 PG 通过不等于全量/部署验证。

## 请 arch 冻结后再接公开入口

1. `api.md` 口径设置 prose 与唯一 fixture 的字段命名不同，以 fixture 为准；但 fixture 只有非空 changedBy。初始 seed 和历史孤儿如何公开表达？建议允许 null + 明确缺失状态，不能伪造管理员/系统修改人。
2. 现 SQL 表缺 evidence_url / created_at，API prose 称三张版本表均有。请明确所需 DDL 切片与迁移归属；读取不得凭空返回来源证据。
3. fixture 指 team editable=false，但 team cash 来自外源且不允许个人系数 seed；请明确 team GET 的只读来源/空态以及非 admin 读取权限。当前内部仓储仅 personal admin，尚未冻结成产品路由。

P161–163 已随 main 合入；be2 S6c 和 018 也已在此基线，不再以“尚未合入”作为等待理由。hourly/Gap 的真实源和规则裁决仍见 P163。P164 不等于 R012 全部完成，整个信箱目标保持 active；生图按老板要求取消。
