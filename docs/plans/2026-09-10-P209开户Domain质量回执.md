# P209 开户 Domain 质量回执（2026-09-10）

## 交付状态

候选代码 `f38a88fa`，main f074992e已通过merge ba2b4700同步。仅F-OS-004 Task1 schema实现，未DB写/HTTP装配/真实登录验证，未合流/部署/push。用户取消生图，旧output不动。

按 writing-plans 拆任务并按 code-quality-checker 做边界/回归/依赖/覆盖检查。冻结要求来自api v1.9.21与v1.9.5，不改公开Contract/fixtures。Skill executing-plans不可用，自执行无子agent。

## 文件与行为

- `packages/domain/src/admin-members.ts`：36行新增。复用原成员字段、meta、错误结构；新增开户input provider判别式，BUC拒绝密码字段，internal_test12..1024字符可选初始密码。用户名128字符上限与ASCII允许集且禁止末尾换行。reset input严格空对象。
- created响应internal_test必有一次性密码、BUC禁止；reset响应identityId+initialPassword+安全非负sessionsRevoked；v195 list要求mustChangePassword且不许密码字段。created/list不接受额外scope/token属性。错误新增CONFLICT/READ_ONLY_ROLE，无SQL/上游正文槽。
- 新测试 `packages/domain/test/admin-member-commands.test.ts`40项；旧17项未改。三份arch fixtures仅剔除meta._note文档注释，不手抄样例。
- 原只读schema/路由暂保留；后续Service同批切v195，不声称现列表已带新字段。

## 真实门禁

1. TDD首次40/40失败：新增导出尚不存在（不是行为已实现后的假红）。加实现后通过。
2. Domain `npm test -- --run test/admin-member-commands.test.ts test/admin-members.test.ts --coverage --coverage.include=src/admin-members.ts`：57/57；该文件行/分支/函数/语句100%。
3. Domain `npm run typecheck && npm run lint && npm audit --omit=dev --offline`：通过、缓存audit0（不是实时联网漏洞数据库）。
4. Worker `npm test -- --run test/admin-members-http.test.ts && npm run typecheck`：18/18旧HTTP回归、类型通过。使用合成认证/仓储，不算PG/OS证明。
5. `git diff --check`通过；无依赖变化/同步KDF/网络请求/SQL/日志/真实凭证。新增生产代码36行，schema数组有1000上限，字段有长度上限。

合计75项不同测试；本次没有真实PG、没有五包全量/build。磁盘约2GiB低于arch8GiB门，未清缓存，未降低最终门禁。

## 后续

F-OS-004 Task2必须证明任一有效team admin身份的全局授权及并发原子性；复用密码仓储可事务执行器；不能直接先建人再独立setPassword。Task3真实登录/改密/重置session闭环仍未完成。之后024→025→readiness→sop-run→P176 Task3，不再等已裁P201/202/203/206。
