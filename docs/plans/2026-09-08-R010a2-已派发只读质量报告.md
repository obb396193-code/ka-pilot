# R010a2 dispatched 只读质量报告

代码：`159f345`。基线 main `4222d4e`；交审前 merge main 显示 Already up to date。

## 实现与边界

- Domain 共用 WORK_ITEM_STATUSES / ACTIVE_WORK_ITEM_STATUSES；列表请求、列表项和详情只读 schema 接受 dispatched，未知状态仍拒绝。changeset 枚举未扩大。
- count/page 同一 CTE 默认包含 open/processing/dispatched/escalated；SQL 插值仅来自静态状态常量，请求仍参数化。workspace、media/account tuple、本人无账户项、team 排除无账户项的条件未改。
- WorkItemStatus 类型新增 dispatched，但未给它添加写 transition，当前写入口仍关闭。旧 createOrMergeAlert 的升级/去重行为未改，不宣称全活动态生命周期完成。
- be2 拥有的 task-list-sql 两处活动态尚需同步；013 的 CHECK/partial unique、superseded_by 和 SLA 另批。
- PG 测试新增两例，覆盖授权/跨 media/跨 workspace/空 grant/team/超末页。原文件共享 ka 默认回退已删除，只允许显式本地 55432/ka_*_test。

## 本机实际执行（2026-09-08）

| 门禁 | 命令（在对应包 cwd） | 结果 |
|---|---|---|
| Domain RED | `npm test -- --run test/work-item-list-contract.test.ts test/read-detail-contract.test.ts` | 2 failed/7 passed；两处 dispatched 被旧 enum 拒绝 |
| DB RED | `npm test -- --run test/work-item-list-repository.unit.test.ts` | 1 failed/3 passed；旧 count/page 缺 dispatched |
| Domain GREEN | `npm test -- --run test/work-items.test.ts test/work-item-list-contract.test.ts test/read-detail-contract.test.ts` | 3 files/31 passed |
| DB unit | `npm test -- --run test/work-item-list-repository.unit.test.ts test/work-item-transition.unit.test.ts` | 2 files/36 passed |
| Worker | `npm test -- --run test/work-item-list-http.test.ts test/read-detail-service.test.ts test/work-item-list-service.test.ts` | 3 files/45 passed |
| HTTP 回归 | `npm test -- --run test/data-api-http.test.ts` | 56 passed，含真实本机 BFF→Session→query HTTP 组合（mock来源） |
| 类型/规范 | Domain、DB、Worker 各 `npm run typecheck && npm run lint` | 全部 exit 0 |
| 覆盖率 | Domain 上述31项，`--coverage --coverage.include=src/work-items.ts --coverage.include=src/work-item-list-contract.ts --coverage.include=src/read-detail-contract.ts --coverage.reporter=text` | 总行/语句99.14%，分支94.44%，函数100%；两个schema各四项100% |
| 依赖 | DB `npm audit --omit=dev --offline` | 缓存审计0漏洞，不是最新在线审计 |
| 静态 | `git diff --check`、逐文件 diff | 无空白错误；无secret/新网络/动态请求SQL/前端视觉改动 |

## 未验证与明确问题

- 隔离 PG 探针：在指定 `ka_be_r010_20260907_test` 发起连接后拟 `SELECT 1`，实际连接阶段 **ECONNREFUSED**，exit1；两条新 PG 用例没有运行，不报 PG 通过。
- 磁盘仅1.6GiB，低于 arch 的8GiB全量门槛。未跑三包全量/迁移全量，没有清用户文件，没有借 ContentRadar 库。
- migration013 定义冲突：schema.sql 的 changeset_items.id 为 BIGSERIAL，而 changeset_reversal_items 两个 item_id 与 execution_run_items.item_id 是 UUID。向arch提交P123，未私改Contract或生成不兼容迁移。
- 任务汇总两处遗漏 dispatched 属 be2 临时文件所有权，需协同；本批只修工作项读链，不声称任务计数已一致。
- 状态为 implemented/non_pg_verified/待审，非 merged/deployed；真实媒体写仍关闭，无 push。
