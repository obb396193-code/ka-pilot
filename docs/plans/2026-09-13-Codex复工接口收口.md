# Codex 复工接口收口 Implementation Plan

> **For Codex:** 按 writing-plans 的小步 TDD 顺序执行；executing-plans 技能当前未安装，沿用本会话执行与逐批交审。

**Goal:** 按 arch 2026-09-12 的 P-192…P-197 补真实接口缺口，不重做 be2 数据链。

**Architecture:** 沿用 session 授权、Domain 严格响应、DB 参数化查询/短事务、R010 HTTP 与 BFF。Contract 仅 arch 修改，未冻结响应/路径先提问，不以猜测代替。

**Tech Stack:** TypeScript、Zod、PostgreSQL、Vitest、Next BFF。

## 2026-09-13 恢复核验

- 工作树 be/r010：078c940c → 本机 main 802e31a4，fast-forward，无冲突。
- 仅存在历史未跟踪文档/output，无未提交源码；保留所有历史产物。
- P-195 三路径已在 main：etl-runs BFF、admin/data/reconcile BFF、reset-password 后端。先复验，不重复编码。
- P-192 清单多数仍写「? 契约无端点」，v1.9.31 只冻结 501，无方法/路径；待 arch 补齐。
- P-194 纯忽略的内部 WorkItemCommandRepository.apply(ignore) 已在；公开成功响应尚未给出完整形状。已有静音响应是 mutedUntil/scope，并非 mute_days；待澄清再接公开路由。
- 新 R010 路由唯一生产挂载点为 apps/worker/src/data/http-server.ts（新边界禁止修改）；请求 arch 授权精确注册 hunk 或代接。

## Task 1：P-195 覆盖回归

**Files:** apps/worker/test/r010/bff-coverage.test.ts、admin-member-commands-http.test.ts、etl-runs-http.test.ts（现有测试）

1. 运行现有三路径回归，验证 PENDING 为空仍通过。
2. 核对 reset-password 的真实 PG 测试，不把 route 存在当作业务完成。
3. 回执交 arch，说明该项为最新基线已有实现。

## Task 2：P-194 变更记录内核

**Files:** packages/domain/src/settings-change-log.ts、packages/domain/test/settings-change-log.test.ts；packages/db/src/settings-change-log-repository.ts、packages/db/test/settings-change-log-repository.test.ts；apps/worker/src/admin/settings-change-log-service.ts、apps/worker/src/r010/settings-change-log-route.ts 与对应测试；之后接对应 BFF。

1. Domain 测试先读 Contract settings/change-log.json，固定 camelCase（fixture 是权威）；测试非法 kind/日期/值与游标。
2. DB 三版本源 UNION，RR/RO 快照、参数化 scope/filter/cursor、稳定复合排序，空 grant 无业务数据。旧值从同一 scope 的前一版本取得，不能从页面前一行猜。
3. 真 PG 测跨 workspace/同号跨 media/无授权任务/同时间同 ID 跨源分页/回溯版本/非法数值。
4. Service/HTTP 测 requestId、401/403/400/502/503/504、exact16MB；取得注册授权后接生产 composition 和 BFF。

## 后续批次

- P-192：arch 定端点后，501 不调用任何业务/媒体服务，补 method/auth/BFF 回归。
- P-193：arch 澄清 PATCH 响应与 grants 请求体后，治理身份回查、唯一 personal 空间、短事务替换+审计、实时撤权与 session 回归。
- P-194 纯忽略：确认成功形后复用既有命令仓储，不产生 account_mutes，不改媒体写。
- P-196/P-197：按归因树/报表冻结 DTO 单独交审，不自行扩 ETL。

## 门禁/交付

- 定向 TDD → Domain/DB/Worker test + typecheck/lint（串行控制负载）；需要真实 PG 的项明确列执行证据。
- BFF 修改额外跑 web 测试（防源码 import 漂移）。使用 code-quality-checker 检查权限/信息泄露/分页/覆盖。
- 每笔提交前 merge main，路径限定 [codex] 提交；不 push。结果、响应形变、未完成项写 inbox-arch。

## 实际推进（2026-09-13，仍未挂公开接口）

- P-195：路径盘点/ETL HTTP/成员命令 53 项通过；真实 PG+HTTP（重置密码→旧会话401、真实入口启动/ETL记录）11 项通过；BFF 定向20项通过。没有重复修改旧实现。
- P-194：Domain 严格 schema 对照 arch fixture；Repository 考核价单源读取+游标（保留微秒，三元排序时间/kind/id）与 Service 完成；新测 Domain31、DB unit13/PG9、Service11 通过。
- 数据实证新增：预算历史表尚无迁移；系数缺审计时间/证据字段；作废记录 wire key 未冻结。缺源报明确错误，不补假表、不替换时间、不丢作废当普通 set。默认全种类暂仍 unavailable；只传 assessment_price 可读取无作废的真实历史。
- 没挂 HTTP/BFF：唯一生产挂载点 data/http-server.ts 在本批禁止修改范围，已报 arch 等精确授权；纯忽略响应也待完整 fixture。不能将此内核称为用户已可点击的 P-194。
- 全量首轮发现并修复：tuple SQL 改引用唯一 accountScopeClause；历史查询不是「选当前考核价」，按既有豁免机制给 assessment-price-selection 测试添加一条带理由记录，不改 shared helper、不改生效口径。另首轮最先运行的迁移因前一轮失败测试撤权残留拒绝降级；隔离库后续完整回放清理后重跑，保留两轮原始日志，不隐藏首轮红。
- 代码提交 **9ed8ec5c**，交付前 merge main 无新提交。全量 Domain1575 / DB1773 / Worker2285（另2 opt-in skipped），三包 typecheck/lint 全通过；P194仅内核完成。质量回执 `docs/plans/2026-09-13-P194内核质量回执.md`。
