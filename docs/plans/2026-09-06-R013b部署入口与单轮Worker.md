# R013b Deployment Entrypoints Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现arch已派的生产启动硬门、可打包安装顺序、FaaS三入口与单轮Worker；不在本机真实队列启动或部署，不把骨架视为已上线。

**Architecture:** 在现有Data API/Worker入口前统一配置门；复用WorkspaceSyncTickService与JobConsumer/lease，不发明队列；先逐项审计单轮中断和租约恢复边界，再接线。生产禁止任何KA_DATA_DEV_*，不能只是忽略。安装按Domain→DB→Worker/Web，保留各子包锁文件，不擅升前端依赖。

**Tech Stack:** TypeScript/Vitest、node-pg、shell/npm ci、FaaS f.yml样例；以现有be/r010接续，最新main@71b9231未来R014–R016不混本批。executing-plans不可用，按用户已授权连续执行；writing-plans与code-quality-checker落实计划/门禁。

## Task 1：生产环境硬门（独立代码/留痕）

- 新apps/worker/src/production-environment.ts，由config.ts、data/data-api-config.ts及createKaDataClientFromEnv共同调用；任何以KA_DATA_DEV_开头的自有env键，在NODE_ENV=production时失败，空值/false也不放行；固定错误不打印键和值。
- 新test/production-environment.test.ts：Worker/Data API/client三入口负向、非生产未开启额外权限；test/data-api-startup.test.ts实际子进程验证KA关闭时仍拒违规配置，且不读DB/开监听；保持无KA健康启动测试。
- 先红后绿、Worker全非PG/type/lint与核心覆盖；独立提交P051。不会改监听行为或真实数据源。

### Task 1 实际回执

- a77b224，6文件；三入口统一固定错误，不输出dev键或值。10新增逻辑例（首轮6断言失败/4通过→全绿）+1实际Data API子进程例；无KA仍健康启动/显式缺配置失败回归保留。
- Worker非PG774+2外部opt-in skipped、typecheck/lint；核心新文件行/分支100%。首次非PG排除模式遗漏benchmark-data-pipeline-pg，真实连接ECONNREFUSED导致1失败；保留/tmp/ka-production-guard-full.log，完整排除PG文件后非PG重跑/tmp/ka-production-guard-full-retry.log全绿。不把此重跑称真实PG通过。
- main@a7f98a8已答P047三问：优先接回bootstrap完整事务/CLI，再继续本计划Task2–4。新增R-FE-IMG-001纳入空档任务（先出候选供老板拍板，不先替前端定视觉）。

## Task 2：安装与FaaS骨架

- 仓库根package.json、scripts/install-all.sh与自动测试：Domain→DB→Worker/Web按各自lock npm ci；不处理真实.env、不上传node_modules，干净导出副本验证file:依赖解析。
- deploy/faas/{web,data-api,worker-once}：bootstrap与f.yml示例、空值env清单；引用OS证据，不捏造已部署应用；runbook补§7。实际a1发布由OS和老板授权。

## Task 3：单轮Worker

- Task2先交e6bfc45安装入口：六个fake-npm流程测试+bash -n通过；root空依赖lock离线生成。真实clean clone/npm ci受磁盘2.7GiB余量暂缓，不算file:部署验证。f.yml仍待OS模板。审批工具短暂额度错误后已通过原路径恢复，未绕审批写文件。
- Task3逐文件细化：DB JobRepository增加可选且严格验证的workspace/jobTypes领取与过期恢复范围，旧默认调用兼容；真实PG证明其他workspace/不支持jobType不被领取或回收、并发fence仍有效。Worker runtime显式限定只读ETL/quality job类型与当前workspace，service identity fallback禁用；tick被blocked/无授权不消费旧job。
- 硬截止采用父进程监管子进程：只通过校验后的IPC输出job id/type/state，到max时终止子进程并等待退出，不用Promise.race留下运行的handler/heartbeat。被中断job留lease由下一轮重领，不假标done；子进程不自动migration、不启动循环分区维护、不打开媒体写。单轮需要分区时只执行既有受控分区ensure，无周期timer。

- 先审读runtime.ts、jobs/consumer.ts、JobRepository和sync-tick-cli：processOnce目前全队列领取，handler没有AbortSignal；不能仅Promise.race超时后连接还跑/心跳不退出。需明确runtime隔离与硬截止处理，不静默把任务标done。
- 新worker:once命令必须严格服务端指定workspace/media并调用现有tick授权；缺配置先失败，授权身份固定；消费已有handler支持的job至当前无可领取/10分钟上限，遇未到retry时间退出交下轮。租约中断沿用恢复，不重新猜身份。
- 日志仅job id/type/state，无payload/SQL/上游body；测试fake ports、实际子进程无配置不联网，再真实PG isolated队列测试；不启动用户现有队列。
- 如果边界与冻结worker一次要求冲突先P信箱提问，不擅扩媒体写/新调度契约。

## Task 4：30天Session清理

### 2026-09-06 单轮子批回执

- 162e7cf两文件、bcb67e7十五文件；分离DB租约范围与Worker运行入口。DB全量336/Domain607/Worker828+2真实外部opt-in skip、type/lint全过；真实PG目标分别ka_be_r013_20260906与ka_be_r010_test，仅本次合成账户/任务。Gateway/Web未在本批重跑。
- 边界：父进程到deadline硬杀并等close；child不运行migration、不起daemon分区timer；tick无授权不prepare/consume；6种ETL/quality白名单，受信workspace限定，source身份只从DB本人解析。失败旧lease不标done，现有fence恢复；未知job不被领取/回收，真实写仍关。
- 3个实际PG/进程例：真实CLI空grant、真实runtime合成源完整ETL/下游、真实子进程死后PG留lease并换token重领。其他workspace和changeset_execute未消费；全stdout事件仅jobId/jobType/status，固定stderr无凭证。
- 原始失败日志保留：DB两例（5s迁移超时、即时runAfter依赖）重跑明确已到期fixture/30s门禁后336；宿主/PG时间测量不支持声称时钟漂移。首次PG EPERM为sandbox边界，审批后通；新markDone测试签名错误、mjs lint未import Node globals已修。`/tmp/ka-once-worker-full.log`、`/tmp/ka-once-domain-full.log`、`/tmp/ka-lease-scope-db-retry.log`；核心覆盖98.86行/85.18分支；Worker offline audit0。
- 收到arch@829f2bc新口径后eb109fd合main，先回R010a1公开v3，清理job不抢开。FaaS/真实源/干净安装仍未验证。

- 审计FK与审计留存后增加有限批次expired/revoked>30天清理job和测试，不删除活动session；参数来自受控时钟与配置，不接受浏览器阈值。
- 真实PG验证只能在明确隔离测试库；现55432拒连，保持未验证。清理脚本不在本机会话实际运行。
