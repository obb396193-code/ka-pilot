# R010a1 账户维度公开链路质量报告

## 范围与依据

- arch main adc415d，be 合流 100cc14；批读历史代码 e398f24。沿用 v1.7.5 account.dimension/v3，不修改 packages/contract。
- 个人账户维度通过唯一 Registry + PlatformDimensionQuery + RR/RO PG + Session HTTP + 非视觉 BFF；只开放 account dimension。team/其他维度不冒充已接好，继续原计划。
- 实际执行技能：writing-plans（先计划与失败测试），code-quality-checker（安全、性能、覆盖与门禁）。未安装 executing-plans，由主会话 TDD 执行。

## 实现要点

- `loadByAccount` 一条参数化 SQL 批读 expected account-day 与当天已生效历史；拒绝重复日、三键越界、NaN/Infinity、未来价、10k overflow sentinel、exact16MB。缺日保留 null，不补0。
- `PlatformDimensionQuery` 同一 RR/RO snapshot 内三次批读（lineage、dimension、history），逐账户核对两侧键和现金/转化合计，窗口达标使用真实日价，不读缓存 costSpace、不平均 CPA。
- public key=`media:accountId` 按 arch DTO；workspaceId 不额外塞进公开行。DB/internal row 明确验证 workspaceId，Service 验证 session 授权 pair；原 account_rows 三键守卫不放宽。
- Domain/BFF 严格 dimension/row/version/priceSource 校验；旧 query_type 与 canonical queryId 共用 Registry。BFF 复用其已有 v3 metrics/assessment，不引入 web 未声明的 @ka/domain 依赖。
- 不支持维度422 DIMENSION_UNSUPPORTED；未接team返回VIEW_UNSUPPORTED，不回退个人。未知lineage metadata继续null，不用当前时间/部署默认值。

## 已完成定向证据

- TDD RED：DB13项缺方法失败；Worker窗口测试缺模块失败；Registry9项缺注册失败；Domain新入口拒绝失败。
- DB新批读15项 + 原窗口5项 = **20/20**（含真实PG）；固定合成库 ka_be_r010_20260907_test，无共享 ka/新建库。
- 窗口组合8项 + 真实PG/HTTP6项 = **14/14**；新核心 `platform-dimension-query.ts` **100% lines/functions，93.84% branches**。
- 实际BFF→HTTP→Registry loopback已覆盖summary/trend/table/dimension；恶意跨媒体tuple、错误dimension、坏数、重复组、超响应大小拒绝。exact byte边界已补，全量中复验。
- Domain/DB/Worker typecheck/lint通过；Web非视觉 **143/143**，lint0error12既有warning；Webtypecheck仍被既有前端缺依赖阻断，本次lib/data没有诊断行。
- Domain/DB/Worker production audit --offline 0：仅缓存审计，不声称在线最新安全报告。无依赖改动、无凭证、无任意SQL/媒体写；diff --check通过。

## 失败记录（不能删成全绿）

- 首次全量Domain保留六Query断言（770中1失败）→更新新增dimension的枚举与版本计数。
- 首次Worker非PG命令未允许loopback，产生listen EPERM（119失败/998通过/2skip），不是业务绿灯；改用允许loopback的隔离PG全量重跑。
- 首次BFF尝试直接导入未声明@ka/domain，原生Node无法解析 → 删除该依赖假设，复用BFF已有schema并增加parity；未重装/未改package。
- 接口枚举增加后的旧mock全集测试未认识新dimension（Web3失败）→新增真实arch行fixture测试路径，不伪造mock功能。

## 全量门禁

最终代码 **fdc5f5b**，依赖批读 **e398f24**；2026-09-07 本机串行 Domain→DB→Worker→Gateway 完成，exit0：

| 门禁 | 真实结果 |
|---|---|
| Domain | 58 files，770/770 |
| DB | 70 files，707/707，包含真实PG |
| Worker | 117 passed files + 2 skipped；1173 passed + 2 外部opt-in skipped，包含真实PG/HTTP |
| Gateway | 8 files，36/36，包含真实PG |
| Web 非视觉 | 143/143 |
| Domain/DB/Worker/Gateway typecheck + lint | 全通过 |
| Web typecheck | 仍失败：已有前端依赖缺失；lib/data 无诊断行 |
| Web lint | 0 errors / 12 既有 warnings |

命令：各包 `npm --prefix <path> test -- --run`；PG包显式 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test PGCONNECT_TIMEOUT=3`，允许loopback的本机权限。日志 `/tmp/ka-p106-{domain-green,db-full,worker-full,gateway-full,web-final}.log`；失败日志也保留，不能混用旧final文件。没有新建库。

## 未完成与需arch知道

- 本片不等于R010a1完成：task/biz/agency/deduction/placement/bid_tool、team维度、pivot2、health/ETL、table/detail新窗口链继续做；a2、R011(013)、R012/014/015/016/010b仍在范围。
- account fixture 的完整lineage仍缺datasetVersion/queryTemplateVersion/metricVersion/objectIdentity；直接使用其rows，测试显式合成unknown lineage，不改arch文件、不把缺失元数据当known。请arch补完整fixture，不阻塞后续代码推进。
- anomaly沿已有canonical data_anomaly聚合；考核超标由assessment.costStatus表达，未私设新规则。
- 没有push、部署、真实媒体写或生产数据验证；图片002与OS f.yml仍未完成。
- 大窗口账户日历史超过10k会明确拒绝，不截取后装完整；本批未证明1000户×31日容量，后续应在保持同快照/有界输入条件下做源内聚合或受控批读优化。
