# D6 Observed Preflight Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按arch修正fixture打通试运行三值结果；无真实Provider仍503，不凭草稿生成成功。

**Architecture:** 先建立Domain唯一strict DTO和观测判定函数，随后接Worker受控观测证据/持久化结果与HTTP/BFF。现P129 proof只有status/reason，缺observed，不强行映射；逐层保留授权、TTL/hash绑定与source-off。本计划首个SHA只交Domain，不能称公开成功态已通。

**Tech Stack:** TypeScript / Zod / Vitest / existing ChangeValue.

---

### Task 1: 唯一三值DTO与判定

- 新建 `packages/domain/src/changeset-preflight-presentation.ts`、`test/changeset-preflight-presentation.test.ts`，在index末尾be块导出。
- 先fixture/反例红；匹配BIGSERIAL正数字串、64hex、TTL、strict元信息、items三值和summary，unknown不得伪装ok；纯函数复用sameChangeValue，不靠字符串键顺序判断。
- 用真实fixture去除仅文档meta._note后严格校验；不改contract文件。运行定向/覆盖/type/lint，磁盘<8GiB不跑全量。
- 独立代码SHA、报告、P154。

### Task 2: 服务及HTTP/BFF

- 扩充受控只读proof为真实observed值，不能用旧status猜值；Service先校验绑定/目标/TTL，再持久化，再映射公开data。
- Worker route返回D6 canonical envelope；BFF复用strict校验。生产composition继续无Provider，仍503。
- 合成Provider的HTTP、真实PG回写、changed/unknown不允许confirm、超时/越权/无源反例；独立SHA。

executing-plans技能当前不可用，按现有本地TDD逐步执行；不创建Coze项目、不上传代码。

### Task 2a 实际交付（2026-09-09）

`a317f7c`：Worker dry-run-contract/service、observed-preflight新mapper；DB changeset-observations新validator、recordDryRun结果原子附带快照；Domain提供内部snapshot schema。服务/DB/旧HTTP/真实PG204定向过；Worker四个旧fixture类型错、磁盘不足全量仍挂账。**Task2b公开HTTP/BFF还未切新preview**，source-off保持。

### Task 2b HTTP 子批实际交付（2026-09-09）

`de63169`：公开 HTTP 切 preview，strict 三值及 lineage/path 绑定；87 Worker + 10真实PG定向过，HTTP覆盖98.38%行/90%分支，Worker lint过，旧四个类型错仍在。生产无Provider503不变。**BFF 尚未接成功态**，另一个独立子批继续；详见公开HTTP观测结果质量报告。

### Task 2c：BFF 成功态（逐文件计划）

1. `apps/web/lib/data/r010-command-bff.test.ts` 先加 canonical fixture 200/null lineage/路径ID/三值非法/16MiB/requestId/CSRF反例，`node --test lib/data/r010-command-bff.test.ts` 先红。
2. 复用同一 Domain schema，不能复制另一份字段。Node原生TS测试不能解析Domain的 `.js`→`.ts` 链；将纯三值schema提取到 `packages/domain/src/changeset-preflight-wire.ts` 的typed factory，原 `changeset-preflight-presentation.ts` 接同一factory，BFF接同一factory。typed values/equality从 `change-value-schema.ts` 导出，`changeset-values.ts` 继续兼容既有入口；不改tsconfig、不引loader/新依赖。
3. `r010-command-contracts.ts` 扩展成功响应联合类型；`r010-command-bff.ts` 仅为dry-run解析strict结果、校验同路径对象和相关requestId，保留error状态、same-origin/session/server bearer/16MiB。原mute/ignore不变。
4. Domain值/hash/三值测试、BFF定向、Worker已接HTTP回归；允许资源时全量。typecheck/lint/security/diff检验后代码与报告分别提交。无页面/视觉/Contract/媒体写修改。

Task2c代码 **693614e**：共享typed factory+BFF成功态+真实BFF函数→HTTP→PG联测；270定向过，Domain/DB类型和lint绿、Worker lint与Web改动lint绿。Worker四个旧fixture、Web缺依赖75项类型错、磁盘<8GiB全量/build未过仍挂账。内测source-off保持；本计划软件接线已交候选，不代表实际源/部署完成。
