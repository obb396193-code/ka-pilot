# P164 返点版本只读仓储 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为R012已冻结口径设置读取补真实版本仓储，不等待无关013/014，不实现改系数或重算。

**Architecture:** 复用ApprovedWorkspaceAuthContext及RR/RO事务，personal admin从指定workspace只读。按服务端业务日选每媒体当前行；未来版本只在history。保留内部证据缺失态，公开DTO缺失表达待arch裁决后接HTTP/BFF，不把仓储当页面完成。

**Tech Stack:** TypeScript、pg、Vitest、现有Domain auth/date。

---

执行技能未安装，按用户持续授权本会话自行TDD。

## Task 1：仓储反例先行

创建 `packages/db/test/coefficient-read-repository.unit.test.ts`，测试拒绝team/nonadmin/非法日期在DB前、参数化scope、RR/RO、无效数值拒绝、同日冲突拒绝、10001 sentinel、错误脱敏与取消超时。

创建 `packages/db/test/coefficient-read-repository.test.ts`，只用显式`ka_*_test`库、随机workspace隔离；真PG覆盖初始seed四系数、未来版本不泄入current、历史稳定排序/大ID、不串workspace、未知actor不造名字、不读他workspace用户姓名、RR快照期间并发新增不撕裂。

先运行测试，预期模块不存在为红。

## Task 2：最小实现

创建 `packages/db/src/coefficient-read-repository.ts`，末尾be区追加index export。

- 内部 `read(auth, businessDate, media?)` 返回workspace/date与每条版本的id文本、op、数值、effectiveDate、actor已知/未记录/不可解析、current/historyCount。不暴露公开HTTP DTO。
- SQL固定workspace过滤，所有值占位符；RR读取active personal workspace与workspace-local active admin actor，再限10001条版本数据。
- 每个动态长文本在SQL侧有界；返回16MiB前拒绝，NaN/Infinity/非法日期和标识拒绝。
- 同媒体最近已生效日存在多行→冲突错误，不LIMIT1猜；history按media C/date DESC/id数值DESC；current只指向真实已生效行。
- evidence_url当前列不存在，内部标not_stored而非假链接；seed changed_by NULL明确unrecorded，不编system或管理员名。

## Task 3：验收/交审

定向PG和unit、DB typecheck/lint、覆盖率、安全检查；磁盘<8GiB不跑全包，写明边界。路径限定代码SHA+留痕SHA，inbox-arch P164请求changedBy/evidenceUrl缺失公共DTO裁决。全功能仍未完成，不改Contract/前端/be2文件，不push/媒体写。
