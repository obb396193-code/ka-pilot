# R010a1 窗口查询实施计划

目标：按arch v1.4.1/v1.7.1接通普通POST /query与summary/v3，不再让前端累加日比率。起点4df6ab9（含main@ca11db0）。本计划不是交付证明。

1. Domain独立summary-window.ts/test：三份arch fixture的strict行/窗口schema；复用既有MetricValue/RatioValue；聚合先求和再相除、缺成员整体missing；compare按绝对值环比与比率差，零分母/NEW可表达；非法数值/日期/字段拒绝。先additive不提前切断现v2路由。
2. DB summary-window-repository + tests：受信三键scope、expected account-days、有效任务/逐日考核版本、相同比较窗口、当日卡；同一个RR/RO快照组合指标/lineage。team仍经KA reader，不经个人grants或系数二次折算。
3. Worker query registry/source/service/http：date与date_from/to排他归一、固定query_type映射、禁止自报scope/source；source adapter都输出同一v3；lineage带真实window，未知元数据null，不假造权威。query/system health/account detail按冻结DTO分独立提交。
4. 非视觉BFF/lib/data：透传session/requestId与v3 strict解包，Contract fixtures直接复用，不手抄另一套；不改React/样式。
5. PG/HTTP对照、权限/缺数/未来价排除/同号跨媒体/截断/exact16MB/比较零分母；四包+Gateway、typecheck/lint；候选与部署分开。

实现前需arch明确：summary多任务/多考核版本的单个price如何展示；多日compare窗口移动方式与同小时口径；v3 fixture缺现required authority该如何升级。未明确不猜这些公开语义；先写已明确的行schema/纯计算。

### 2026-09-06 P053已裁，继续实施

已完整读api.md v1.7.2/1.7.3与metrics.md窗口节；eb109fd合main@829f2bc。混合价格版本返回price=null+priceVersions≥2，达标/空间逐账户日Σprice(d)×真实转化−Σcash；dod/wow两端平移1/7日，today无同小时快照全undefined；authority必填、preset默认custom，共用Registry。先补严格Domain计算/混合价反例独立SHA，再接DB窗口数据和两Adapter/公开HTTP；不得用窗口末价代替逐日价格。Task bindings仍R014，不抢实现。

## 步骤1交付（候选，未切公开路由）

- 2790fc1，20新例，行/分支100%；Domain607/DB PG331/Worker PG798+2skip、三包type/lint过。严格schema直接对arch fixture行，compare使用既有compareAbsolute/compareRate，不做日CPA平均，先聚合9个三态指标再算7个比率。
- cash不可得但声明onTarget的反例实际先红后绿；起初模块缺失为加载失败，不声称20断言都跑过红。字段日期/额外字段/非数值/溢出/NEW均测。trend按v3 fixture为ds+flat metrics，不沿用v2嵌套summary。

## P053后子批（2026-09-06；仍未切公开路由）

aa577ac：多版本priceVersions/真实版本key，逐日加权及日聚合色标；preset默认custom；比较平移1/7日，today无同时段不可比。19359b0：个人tuple的expected account-day实际history查询，日期/ID倒序生效，缺日不丢；版本分组10k+1 sentinel、exact16MB、present-invalid失败。四PG/十DB unit；Domain617/DB350/Worker828+2外部skip，三包type/lint；核心30例覆盖97.35行/93.97分支。

当前history是单SQL一致快照，不宣称已完成summary/compare/lineage多查询RR。接下来新增受控RR/RO读会话，将现有semantic查询端口接受query-only连接以复用同一PG client；真实PG并发修改history/metric时一请求保持同一版本；异常rollback/release与只读拒写测试。无接口/Contract自定义字段。P058细口径等arch；预算卡属于014，不默认为tasks总预算。
- P053三问已经v1.7.2裁定；剩P058细口径待答。当前仍additive导出，不改变sourceQueryResultSchema的v2版本表，不能称公开窗口完成。

## 多查询一致快照子批（候选，2026-09-06）

b05ea60：withSemanticReadSnapshot检出一连接，RR/RO+statement15s/lock5s/idle15s；所有读完成且COMMIT成功后才返回。断连监听、防复用损坏连接、rollback失败销毁均测。SemanticQueryRepository及dimension/health helpers接受query-only连接，不用Pool强转。Platform adapter以snapshot runner包住lineage+数据+分页整条读链，真实Data API composition接入；不再发一个未及时await的lineage Promise，错误仍保持Canonical→顶层UPSTREAM_INVALID_RESPONSE、DB故障→SOURCE_UNAVAILABLE固定envelope。

真实PG证明同请求内刷新提交后summary/trend/table/history/lineage仍旧版本，下一请求才看到新版本；RO事务UPDATE被25006拒绝。Worker实际adapter路径跨媒体同号只返回授权KUAISHOU，旧timestamp+新cost混读反例通过。7生命周期unit+2DB PG+1Worker PG+3adapter新unit；内核覆盖100%行/86.66%分支。

全量首跑Worker831pass/1fail，定位既有即时enqueue与lease时钟不同；657803c独立修复并做3PG反例。最终Domain617/DB362/Worker832+2skip，type/lint全过，日志/tmp/ka-snapshot-{domain-final,db-final2,worker-final2}.log。下一步仍是公开v3与比较窗组合，不将当前v2事务接线误称v3完成。没有runbook/契约/视觉更改。
