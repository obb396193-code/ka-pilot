# R-009 状态

> 角色：be（Codex）  
> 审查/契约权威：Claude/arch  
> 分支：`be/r009`  
> 起始基线：`main@409d363`（`v0.2-unaudited-baseline=d121273` 只是 Claude 的审计起点，不是开发分支）
> 已同步 Claude/arch 基线：`main@f841da4`（合并提交 `11faf95`，2026-09-06）；方向是 main→be/r009，**不表示后端批次已合入 main**。
> 建立日期：2026-09-05

## 当前状态

- 2026-09-06 02:00：已clean合main（11faf95），仅两份追加文档冲突，双方留痕全部保留；最新v3 Contract作为后批目标保留，本批仍v2不双兼容。合后Domain518/DB纯逻辑37/Worker非PG724+2skip/Web111/Gateway非PG34，五包type/lint全过；真实PG仍拒连，未跑DB全量及Worker/GatewayPG，**不是五包最终门禁**。代码/文档均独立保留，无push/部署/媒体写。R009另有team anomalies未冻结源规则，已提arch，不能声称全部闭环；P045须待这些与PG证据收口。

- 2026-09-06：`85ea1bf`按arch要求完成折011（两CHECK+旧done重验完整保留，删除重复补丁文件、Git可恢复），其余P0约束不变。DB纯逻辑37/type/lint绿，callback100%只是包结构覆盖；01:53只读PG仍拒连，没动数据库历史/没执行回退。旧双011库的迁移维护边界已单列计划；下一步merge main，完整PG门禁仍pending。

- 2026-09-06：`3b76ed3`完成最终Service截断兜底；六Query超过本地预算或上游truncated都掩码为error，覆盖不足不误判。25新例先12红后绿，Worker非PG724+2外部skip，74定向核心行94.55%/分支88.12%，type/lint与offline audit0。最近真实PG01:37拒连，未重复也未冒称通过；继续折重复011、merge main，仍待arch整批审查和真实PG最终门禁。

- 21:58新增`7ced49d`：#8普通/诊断严格输入及服务端选择策略内核，22测试/核心100%，Domain514和Worker非PG670+2skip、type/lint绿。**尚未接HTTP**；待团队reader部署workspace绑定、Service/HTTP、BFF及真实PG联合证明。最新权威仍main@cda3303；Docker只读ps在本回合重查仍无返回，PG未恢复证据，未重启共享Docker。

- 2026-09-05 21:40：`bdc5273` 已提交六 Query v2 / 两 Adapter / SQLite 缺数及截断处理，18 文件；当前权威已只读更新至 main@cda3303，尚未 merge。Domain514 / Worker非PG648+2外部skip，type/lint绿；DB219在16:32真PG通过，但21:32 Worker真PG因55432 ECONNREFUSED失败，**当前pg_blocked**，不得沿用早前通过冒充最新全链验证。前端旧78测试/typecheck虽绿，直接解析新fixtures三个成功样本全部拒绝；按最新A-001在下一空间绑源批接BFF契约，不改视觉。未push/合流/部署，目标仍active。

- 2026-09-05 续工：P0-07 代码 `a044e54` 已完成 be 自验，回执 P-042；当前工作树仍在 be/r009。已只读核实 main@d3466c7：P-041 已由 arch 逐行通过，窗口化现金口径/settings 属 R-010a1/R-012，本批不做；Web 依赖告警已转 fe。尚未把该 main 合入后端分支，按 arch 要求批末合流前统一同步。

- 第二批续工：从保留的 `be/r009@d7b6260` 恢复后已同步 main@8b155a1。现工作树 `/private/tmp/ka-be-r009-20260905`；未改其他 worktree。main@677e4b2 已给 P-039 逐行通过并要求批末把追加迁移折回 011，尚待执行；不是已经合流。
- P2-1 已修：删除 `forwardDataQuery` 假 token fallback；缺失/空/畸形 Session 返回 401 且不请求后端。越权和版本漂移旧测试补齐真实 fixture Cookie 与关联 requestId，确保命中目标检查。Web 78/78、typecheck/lint 通过。
- 第二批执行顺序：P2-1 → P0-03 三阶段回灌与数据库 CHECK → P0-05 任务排斥错误 → P0-13 双主体 → P0-07 Workflow → P0-12 inbox → P0-04/v2 与空间绑源 → 双空间联合门禁。各子批独立代码 SHA，末尾集中完整回执给 arch。

- R-009 已从 Claude/arch 当前已提交 `main` 建立隔离分支；不再以 `codex/integration-control` 为基线。
- `packages/contract/` 只认 Claude/arch 冻结版本；be 不修改 Contract。
- 真实媒体写操作仍关闭。
- 旧 Task6 分支和未提交 migration WIP 已冻结为 Claude 审查候选，不自动合入 R-009。

## Claude/arch 已裁决的编号与顺序

当前 main 已有 migration 008～010。Claude/arch 已冻结：

- R-009 使用 migration `011`；
- R-010 使用 migration `012`；
- 旧 Task6 未提交的 `011_team_data_sync.cjs` 作废；
- 团队数据接入改列 R-011，使用 migration `013`；
- 实施顺序为 R-009 → R-010a → R-011 → R-010b。

## 交付清单

- [x] migration 011：P0-03/05/07/12/13 schema 变更；代码 SHA `351d039`。完整 migration 1→11 replay、011 up/down/up、DB 全量 32 files / 177 tests、typecheck、lint、production dependency audit 0 vulnerabilities 均通过。
- [x] R-009#12 hh 边界：`apps/worker/src/etl/payload.ts` 接受 `0..24`、拒绝 `-1/25`，与启航已实测累计小时语义及 client 边界一致；代码 SHA `7aea1dc`。
- [ ] P0-04 缺数三态：去除聚合静默补 0，对外统一 `{value,availability}`。
- [x] P0-05 任务账户区间排斥与 `TASK_ACCOUNT_OVERLAP`：`010e4bb`，be 自验通过，待 arch 终审；公开写路由仍未开放。
- [x] P0-07 Workflow executor fencing + `workflow_effects` 副作用 outbox：`a044e54`，Runner/真实 DB 联合反例通过，待 arch 审查；未开放媒体执行入口。
- [ ] P0-12 钉钉 durable inbox：先落库再 ACK、lease/重试/dead。
- [x] P0-13 Changeset 双主体与 item 账户三键权限：`5228b44`，be 自验与真实 PG 通过，待 arch 终审；媒体写仍关闭。
- [x] P0-03 Backfill raw → canonical → quality 三阶段状态机：代码 `5dfbbad`，be 自验与真实 PG 通过，待 arch 审查；不是 R-009 整批完成。
- [ ] 数据源绑空间：personal → platform；team → ka_data；普通请求拒绝 `dataView`。
- [x] 合入并复验 `codex/fe-task5-session-bff@c3ed7b3` 的 4 个 Auth BFF 路由：R-009 SHA `5e91a85`。
- [x] 在 Claude 指定的原 BFF worktree 收口 11 个 Session Cookie 迁移文件：原分支 SHA `c5df265`，R-009 SHA `2916a91`；Web 77/77、typecheck、lint 通过；未跟踪的 `bff.test.new` 经检查为被正式测试覆盖的临时缩减稿，未提交并已清除。
- [ ] 真实 PG + HTTP 双空间联合集成反例。

## R-009#13 四项代码定位（交 Claude/arch 复核）

以下仅表示 be 已定位并自查，不替代 Claude/arch 终审：

| 项 | 当前实现位置 | be 自查结论 |
|---|---|---|
| B4 pacing 零量日剔除 | `packages/domain/src/metrics.ts:122-127` 过滤非有限值与 `value === 0`；`packages/db/src/metrics-repository.ts:196-205` 历史窗口 SQL 同步排除 `NULL/0` | 领域函数与 SQL 口径一致，待 arch 复核 |
| B13 素材下载 allowlist 默认拒绝 | `apps/worker/.env.example:8-10` 默认空列表；`apps/worker/src/config.ts:82-90,164-171` 严格解析；`apps/worker/src/sources/material-source-probe.ts:193-208` 未显式命中即 `host_not_allowed` | 空 allowlist 无 host 可通过，待 arch 复核 |
| B23-C2 首次 full ready 门 | `packages/db/src/workspace-sync-repository.ts:163-175` 仅把同 workspace/credential owner/media 且 `etl_full + run_kind=full + status=done` 视为完成；`apps/worker/src/scheduling/workspace-sync-service.ts:40-60` 在成功 full 前自动选 full，并拒绝显式 incr | 代码已有门禁，待 arch 复核 |
| B10 离线分区有界回退 | `apps/worker/src/etl/full-handler.ts:34,161-182` 固定最多回看 3 日，命中首个非空分区即停，未命中返回 null | 回退有界且不硬编码 T-1/T-2，待 arch 复核 |

## 旧 Task6 冻结现场（仅供 Claude 审查）

- 分支：`codex/personal-team-task6-ingestion`，HEAD `4e67315`。
- `f009e19`：个人空间 session 候选 SQL 稳定 `LIMIT 2`。
- `6d02cfe`：source-neutral team ingestion Domain Contract。
- `feec2ec` / `4e67315`：分批 staging + 原子 publish 设计记录。
- 未提交：`packages/db/migrations/011_team_data_sync.cjs`、`packages/db/test/team-data-migration.test.ts`、`packages/db/test/migrations.test.ts`、`packages/contract/schema.sql`。
- 语义冲突：旧 Task6 按旧 root 清单设计；新 arch Contract 固定 team 为 `ka_data`。上述内容不得整体 cherry-pick。

## Claude 审查范围

`docs/plans/Codex后端交付总账.md` 是唯一总索引。Claude 按 `v0.2-unaudited-baseline` 起点审计 `P-004～P-035` 及后续 B23-C2/Task4/Task5/Task6 候选；必须区分 `codex_prechecked`、`claude_review_pending`、`merged`、`PG verified`，不能把候选交付当作终审。

## 验证记录

- `npm --prefix apps/web test`：77 passed / 0 failed。
- `npm --prefix apps/web run typecheck`：通过。
- `npm --prefix apps/web run lint`：通过。
- 首次测试因隔离 worktree 未安装 `apps/web/node_modules` 报 `ERR_MODULE_NOT_FOUND(zod)`；按该子项目 lockfile 安装后复跑全绿，不属于代码断言失败。
- `npm --prefix packages/db test -- --run test/migrations.test.ts`：1 passed；完整 1→11 replay 通过。
- `npm --prefix packages/db test -- --run test/contract-v1-2-migration.test.ts test/changeset-repository.test.ts`：10 passed；011 up/down/up 与 Changeset 三键写入通过。
- `npm --prefix packages/db test -- --run`：32 files / 177 tests passed，使用本机 Docker PostgreSQL `127.0.0.1:55432/ka`。
- `npm --prefix packages/db run typecheck`、`npm --prefix packages/db run lint`：通过。
- `npm --prefix packages/db audit --omit=dev`：0 vulnerabilities。
- `npm --prefix apps/worker test -- --run test/etl-payload.test.ts test/qihang-client.test.ts test/full-handler.test.ts`：3 files / 58 tests passed。
- `npm --prefix apps/worker test -- --run`（获准在沙箱外执行本机监听与真实 PG 路径）：79 files passed + 2 opt-in provider files skipped；623 tests passed + 2 skipped。
- `npm --prefix apps/worker run typecheck`、`npm --prefix apps/worker run lint`：通过。
- `npm --prefix apps/worker audit --omit=dev`：0 vulnerabilities。
- 代码质量自查：路径 diff、secret 模式、动态 SQL/注入面、production dependency audit 均未发现新增问题；这只是 be 自查，不替代 arch 终审。

## 第二批子批交付（2026-09-05，P-039 / P-041）

### 连续子批 A：P0-05（2026-09-05）

- 已同步 main@8b155a1，合并 SHA `48c79a7`。双方追加台账/信箱完整保留，be 上批账目改用 be-P039 标识避免占用 arch 的 #151。
- 代码 `010e4bb`：按 workspace/media/account 锁定任务归属，跨任务重叠统一 typed `TASK_ACCOUNT_OVERLAP` + `statusCode=409`；只捕获指定排斥约束的 23P01，不把 FK/连接等错误冒充冲突。SQL 全参数化。
- 考核价按当天唯一 relation→该任务内最新生效版本获取；不借其他任务价格，不读取未来版本；现有 dimension 已拒重叠、不含分摊分支，未重写。
- **HTTP 边界**：目前尚无公开 assignAccount 写路由，本次完成的是 Repository 错误契约；真实 HTTP 409 随 R-010 任务动作接线后验，不宣称页面可操作。
- TDD 首轮 2 红（跨任务暴露 DatabaseError、23P01 未映射）→修复后真 PG 8 个任务测试 + 3 个错误映射 unit + 6 个指标 Repository 测试通过。
- 本子批四包全量重新跑：Domain497、DB188真PG、Worker628+2 opt-in skipped、Web78；四包 typecheck/lint、diff check 全通过。
- 用户验收句：同一个账户同一天不能误绑两个任务；换任务后没有生效考核价就显示缺失，不串用旧任务的考核价。当前仍 be candidate/claude_review_pending，未合 main、未部署。

### 连续子批 B：P0-13（2026-09-05，代码 5228b44）

- 创建/确认/开始执行在事务内校验 personal workspace 与两个同 workspace active user；actors 去重、按固定 ID 顺序 `FOR SHARE` 锁定，撤销变更不能穿透该事务检查。team 即使有 active users 也拒绝。
- items 从父 tuple 写入；读取/确认/执行验证每条完整 workspace/media/account 三键，不通过过滤坏行掩盖损坏；三维各有真 PG 反例。
- Worker 新增强制授权预检，拒绝 store 返回错误 workspace/id；读当前值前和 UNKNOWN 回查前检查，beginExecution 再检。真实 PG+Worker 测试证明：已停用、读当前值期间停用、UNKNOWN 后停用、改为 team 均不调用媒体 executor，不生成 execution_runs。
- 已提交结果的 completeExecution/completeReconciliation 仍允许持久化结果，不因提交后撤销而丢审计；这是结果落账，不是授权继续发起新的媒体调用。此批未注册真实媒体写入口。
- TDD：新增 DB 首轮 15 tests 中 6 fail/9 pass（inactive/跨 workspace/items scope 原实现未拒绝），修后新增三维与 Worker 联合反例；最终定向 DB17、Worker11+PG4 全过。
- 核心 V8 覆盖率（本轮实测）：changeset-repository lines/statements **93.56%**、branches 76.92%；execution-handler lines/statements **93.91%**、branches 77.27%。不是全仓覆盖率，也不是 100% 分支覆盖。
- 事务只涵盖短数据库授权与状态更新，不把网络请求包在长事务中；不声称数据库撤销与远端已发出请求之间具备跨系统原子撤回能力。

### P-041 最新全量与质量结果

| 包 | 测试命令（在包目录） | 结果 |
|---|---|---|
| Domain | `npm test -- --run` | 45 files / 497 passed |
| DB | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 34 files / 196 passed，真实 PG（含纯逻辑测试） |
| Worker | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 82 passed files / 636 passed；2 外部 opt-in skipped |
| Web | `npm test` | 78 passed |
| 四包 | `npm run typecheck` + `npm run lint` | 全通过 |
| 后端三包 | `npm audit --omit=dev` | Domain / DB / Worker 均 0 vulnerabilities |
| Web | `npm audit --omit=dev` | **exit 1：5 vulnerabilities（4 high / 1 moderate），整体依赖门不通过** |

- Web 现有依赖审计项：fast-uri（GHSA-fph4-wmhf-6fwf / GHSA-jqff-g426-hqxp）、qs（GHSA-x5fp-wj9c-mxmx / GHSA-4mjr-xmp4-gh2g）、PostCSS（GHSA-qx2v-qp2m-jg93 / GHSA-6g55-p6wh-862q / GHSA-fxqj-rqcc-2cmp / GHSA-r28c-9q8g-f849）、sharp（GHSA-f88m-g3jw-g9cj），Next 为依赖链受影响节点。npm 建议部分需要 `next@16.3.4` 主版本升级；未运行 `audit fix --force`，交 arch/fe 单独评估。扫描结果是当前锁文件的 npm advisory 判断，不冒充线上漏洞利用实证。
- 本子批 apps/web、ui-layout-demo、packages/contract、package/lock 文件均 0 diff。完整质量自查：参数化 SQL、锁顺序、跨租户、typed error 不带 DB detail、凭证模式、边界反例、diff/check；未发现本次新引入秘密或动态 SQL 注入。actors 检查增加固定次数查询，不是逐 item N+1；性能没有做负载压测，不声称吞吐指标。
- 用户验收句：停用发起人/凭证所有人后，已确认的变更集不能继续借其身份执行；明细不能混入其他空间/媒体/账户。仍为 implemented + self-tested + claude_review_pending，未 merged/deployed/production-verified。

### P-039 历史快照：已做与 SHA

- `e69ea1e`：P2 假 Session token 兜底删除。Web 78/78；Cookie 缺失/空/畸形直接 401，不调用上游。
- `5dfbbad`：P0-03 + P2 status CHECK。由持久化 jobs 推导 raw/canonical/quality 全日期覆盖；同 workspace + batch + credential owner 限定证据；raw 完成只到 raw_done，canonical 完成只到 canonical_done，质量通过才 done。失败/blocked_auth 记录 failed_stage；待重试不提前报终态。并发刷新锁 backfill 行，finished_at 重放稳定。
- Runtime 四种 job 的成功/最终失败回调均刷新批次；回调异常沿用现有 error hook，启动恢复会重算 running/raw_done/canonical_done。质量不通过时保存检查记录并使回灌质量 job 失败，普通非回灌质量扫描行为未变。

### P-039 历史快照：迁移与部署条件（已被 arch 要求改为折回 011，待批末处理）

- 已审的 `011_contract_v1_2_p0.cjs` 未改写；新增 `011_r009_backfill_state.cjs` 作为 011 追加迁移，不占用 arch 为 R-010 保留的 012。node-pg-migrate 同编号按文件名稳定排序已实测，迁移文件总数变成 12；相关 down/up 计数同步。
- 约束拒绝 null/partial_failed/未知 status 和非法 failed_stage，不擅自清洗旧异常数据。旧 done 仅证明 Raw，因此迁移将其置 running 清 finished_at，启动后按 DAG 证据重验；down 不恢复错误的旧完成标记。
- **先停 Worker 再迁移，再启动恢复**，不是在线无锁迁移。lock_timeout=5s、statement_timeout=5min。历史 job 证据缺失时保守留 running，需要运维重跑/补证据，不虚报完成。
- Contract 文件 0 diff；未加公开写路由、未开媒体写、未改视觉。

### P-039 历史门禁（保留当轮真实数字）

| 包 | 命令 | 结果 |
|---|---|---|
| Domain | `npm --prefix packages/domain test -- --run` | 45 files / 497 passed |
| DB | `TEST_DATABASE_URL=…/ka_r009_test npm --prefix packages/db test -- --run` | 33 files / 182 passed，真实 PostgreSQL |
| Worker | `TEST_DATABASE_URL=…/ka_r009_test npm --prefix apps/worker test -- --run` | 81 passed files + 2 opt-in skipped；628 passed + 2 skipped |
| Web | `npm --prefix apps/web test` | 78 passed |
| 四包 | 各包 `npm run typecheck`、`npm run lint` | 全通过 |

- 新增真实 PG：补充迁移 up/down/up、非法状态 rollback、旧完成状态重验；Repository 并发/恢复/跨 workspace/其他凭证 owner 隔离；实际 createWorkerConsumer→协调器→Raw→Canonical→Quality 成功与失败各一条（上游 HTTP 使用合成响应，无真实媒体请求）。
- Docker 已恢复，投放 PG 容器启动；新建独立合成数据库 `ka_r009_test`。旧 PG benchmark 的安全白名单固定本机 ka，它仍只创建并清理自有 synthetic workspace；不能声称所有 PG 用例都落新库。
- TDD：新增状态约束测试先因 CHECK 不存在失败，补迁移后通过；此前 Domain reducer 缺模块红灯后补实现。Runtime 测试最初因宿主机/VM 毫秒级时钟差导致 job 未到期，现仅测试内以 DB 时间固定到期，无 sleep。
- 全量首轮 Worker 627 passed/1 failed：旧参数测试受 TEST_DATABASE_URL 覆盖，先报目标库错误而非预期 scale 错误；仅给该纯参数反例显式传安全 URL，未放宽生产白名单，复跑 628 全过。
- 本轮 `git diff --check` 与路径/注入/凭证静态自查通过；未重跑依赖 audit，不把上批 audit 0 当本轮结果。

### P-042 Workflow 可靠执行（2026-09-05）

**代码 SHA：`a044e54`，10 文件 443 insertions / 65 deletions。**

- `workflow-execution-repository.ts`：claimExecutor 原子领取新 UUID，续租/释放守 token；所有推进写操作先 `SELECT ... FOR UPDATE`，拿到锁后再读 DB wall clock 校验租约，避免“锁前检查有效、等待后已过期”误判。过期 token 不可续活，跨 workspace 拿不到租约。
- `workflow-repository.ts`：appendEvent/CAS status 的 token 变为强制字段；没有 legacy 无 token 入口。新 effect 使用既有011 `(run_id,node_id,attempt,phase)` UNIQUE，phase分别 preview/execute（reconcile存储能力预留）；先 pending再外调，完成写受token保护，同键终态只读回结果、不同key/result冲突拒绝。
- `run-handler.ts`：advance/confirm/pause/resume/cancel 都走命令局部租约，不在 handler 实例共享token；每轮续租、外调前覆盖timeout+30s，短DB事务不包网络调用。结果已存但事件未存时复用结果；pending/unknown不再调用，流程进入unknown，不偷换attempt重试。
- `postgres-run-store.ts`：真实Repository适配固定published版本编译与严格Domain事件；显式剥离持久化databaseId。新联合测试初跑就是在该边界失败，不能拿MemoryRepository测试代替真实接线。
- effect结果仅保存归一化预览/执行结果，已有output credential-shape/大小/schema门仍生效；上游异常只记 `{kind:'unknown'}`，不存错误原文。effect result大小另限1MB。

**真实测试过程与结果：**

1. DB 新测试首轮2红：claimExecutor不存在。实现后并发领取、过期接管、旧token event/status/effect/renew拒绝、missing token、跨workspace、effect pending/done重复与冲突均过；DB定向9/9（原6+新3）。
2. Runner PG首轮4红：真实Repository返回databaseId导致strict event拒绝，两个barrier测试因此超时并出现未消费rejection。改为显式事件适配，测试barrier也加入first promise竞速、捕获旧executor拒绝，避免未来同类失败掩盖原因。最终PG5/5 +旧Runner11/11，无timeout/unhandled rejection。
3. 新PG用例：双Runner advance/confirm竞争；正常confirm后执行一次；preview/execute结果落库后event写入崩溃，各恢复不重发；租约过期新executor把pending判unknown，旧executor回报被拒；preview异常仅保留sanitized unknown、终态重进不重发。全部为合成workspace/spy动作，未接真实凭证。

| 包 | 命令（包目录） | 最终结果 |
|---|---|---|
| Domain | `npm test -- --run` | 45 files / 497 passed |
| DB | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 35 files / 199 passed（含真实PG与纯逻辑） |
| Worker | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 83 passed files / 641 passed；2 外部opt-in skipped |
| Web | `npm test` | 78 passed |
| 四包 | 各 `npm run typecheck` + `npm run lint` | 全通过 |
| 后端三包 | 各 `npm audit --omit=dev` | 全部0 vulnerabilities；本轮重新查询 |

- V8定向覆盖：新execution Repository lines/statements100%、branches85%；Runner lines89%、branches73.91%；Postgres适配lines100%、branches94.44%。不是全仓100%覆盖；边界/失败测试与剩余未覆盖分支如实交审。
- 全量Worker首次640过后追加unknown脱敏回归，再完整重跑641过。DB和Worker PG串行，隔离库ka_r009_test；旧benchmark仍按既有白名单在本机ka自有合成workspace运行并清理。
- 质量自查：路径限定、diff/check、全参数化SQL、租户/lease scope、效果重放、防凭证原文存储、类型边界、池释放/短事务。核心租约模块独立文件控制复杂度；没有压力测试，不能推断部署吞吐。三包依赖0，Web已有5项告警不在本轮重扫/修复范围，arch已转fe，不能宣称整体依赖门已清零。
- **明确边界**：pending只证明可能发出，不代表done；恢复时保留pending记录并把run置unknown，等待后续回查链路，当前不自动重发/补偿。输出存储与媒体actions仍需生产实现方注入；本批增加真实DB适配与测试，没有注册公开写路由、没有接真实媒体ActionPort，不声称远程exactly-once或完整可部署工作流产品已完成。
- 用户验收句：重复点击或两台Worker同时接到同一流程，不重复创建变更集/发投放动作；结果不确定明确停未知态。状态为 implemented + self-tested + claude_review_pending；未merged/deployed/production-verified。

### 剩余与边界

P0-12 durable inbox、P0-04/v2缺数三态、按空间绑源及升级后双空间联合回归尚未完成；下一项P0-12。main@677e4b2要求批末折回011（总迁移回11）、team+KA关闭→503不回退platform、lineage增workspaceKind，均待本批后续完成。窗口化成本口径已由main@d3466c7之前的v1.4.1冻结，属后批，不在R009自行改公式。已有明确派工，不等待Claude才能继续；最终合流与审查仍由Claude/arch。

## P-043 · P0-12 durable inbox（2026-09-05）

**代码 SHA `c6603d3`**；19文件602+/257-，分支be/r009。开工与提交前核对main仍为d3466c7，arch顺序不变。本批前端/视觉/Contract/迁移/依赖锁0 diff；未push/merged/deployed/production-verified。

### 实现与自查

- Stream机器人callback改为持久化Promise成功后ACK。实读锁定SDK `dingtalk-stream@2.1.6-beta.1` 的 `onCallback` 只emit、不自动ACK（不是泛事件的onEvent自动ACK），并对生产callback注册补mock wiring测试。
- `InboundEventRepository`：INSERT幂等、全局external_event_id冲突必须同workspace/provider/kind，否则拒绝ACK；领取只认robot_message，workspace/provider限定，SKIP LOCKED原子attempts+1。冻结表无status/lease_token列，使用attempts作为fencing generation；任何checkpoint/renew/complete/fail先锁行，再用DB clock判断存活，旧代/过期代不能续活或覆盖。
- 初版dead由未processed、attempts耗尽与lease到期派生；**最新arch冻结后已用9715125对齐**：dead必须last_error=ATTEMPTS_EXHAUSTED，最终显式失败立即补标并失效lease，崩溃耗尽到期补标（含先前失败code），活跃最后租约不补标。中间失败仍只留固定PROCESSING_FAILED/INVALID_PAYLOAD，不删除记录、不伪造已处理。
- message与reply checkpoint使用AES-256-GCM加密，部署key必须显式注入，AAD绑定workspace/provider/event/purpose，不存明文webhook、不把上游错误原文输出日志。key及轮换约束登记.env.example和唯一runbook§6。
- 网关后台poll支持重启恢复；结果checkpoint后回复失败不重查，停机停止领取并等待当前处理。非文本/非法消息不ACK，card类型不会被文本worker消费。没有假定平台session reply有远端幂等；远端成功、本地complete前崩溃仍可能重复文本回复。
- 旧message-handler中的任务创建/Agent enqueue明确关闭（之前只是尚未接通的客户端/队列路径，不能借恢复机制自动开放真实执行）。需求不取消，等待后批冻结写链路接回。帮助文案不再声称这些能力可用。
- 网关不再启动时自动migration；与已有006停写迁移纪律一致，由部署维护步骤先迁移，再启动独立网关。

### 测试过程（不把工具故障当TDD证据）

1. 最初普通sandbox连PG为EPERM，批准后在ka_r009_test得到真正5红（receiveInbound/leaseInbound尚不存在）；实现后定向7过，再补事件类型/续租反例，最终DB新增6+旧2过。
2. 网关初次测试缺本地vitest，npm ci首次被sandbox网络阻断；批准后严格lockfile安装、ignore-scripts，未更新依赖。故网关第一轮不是有效业务红灯，不虚报TDD红绿。业务新增用例均在后续PG与callback测试中实跑。
3. 网关PG：落库后ACK抛错→重投仍一行→新worker恢复；双worker并发只有一次处理；处理失败跨worker重试；reply失败checkpoint恢复；五次耗尽dead且不删除、不泄露错误。mock补ACK前未落库/DB失败无ACK、租约丢失不发reply、旧payload拒绝、加密跨scope/purpose拒绝、空队列/停机/安全poll告警。
4. 第一次36测试通过后typecheck暴露测试直接import pg缺声明（原网关无@types/pg）；改用已导出的createPool，不增加依赖，再跑全量36+typecheck+lint全通过。

| 包 | 最终命令（在包目录） | 结果 |
|---|---|---|
| Domain | `npm test -- --run` | 45 files / 497 passed |
| DB | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 36 files / 205 passed（真PG+unit） |
| Worker | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 83 passed files / 641 passed，2外部opt-in skipped |
| Gateway | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run --coverage --coverage.include=src/inbox-worker.ts --coverage.include=src/inbox-codec.ts --coverage.include=src/message-handler.ts --coverage.include=src/dingtalk-adapter.ts` | 8 files / 36 passed，含2真PG；没有跳过 |
| Web | `npm test` | 78 passed |
| 五包 | 各 `npm run typecheck` / `npm run lint` | 全通过 |
| Domain/DB/Worker/Gateway | 各 `npm audit --omit=dev` | 本轮复查全部0 vulnerabilities |

- 核心V8：Inbox Repository行100%/分支97.29%；网关四核心文件合计行98.98%/分支94.44%，worker行96.22%；不是全仓覆盖率。DB/Worker全量串行，合成PG数据；旧benchmark的本机ka自有workspace行为未改。Web既有依赖问题由fe处理，本轮未重扫，不能称整个项目audit0。
- 按code-quality-checker检查SQL参数化/跨scope/陈旧代、重复副作用、凭证存储、错误日志、大小限制、轮询退出和依赖。自查未发现本批新P0/P1；非吞吐/灾备或真实钉钉验收，仍交独立审查。

### 未完成与下一步

- 旧ProductApiClient的Agent/query端点、DingTalk→正式Session/tuple授权桥接、临时webhook失效后的安全主动推送和细粒度撤销后通知策略未接通，归R010/R012；**本批只交可靠收件内核，不宣称真实群查数已部署可用**。非文本、卡片、自动死信管理UI未实现。
- 下一项P0-04缺数三态/row v2→按空间绑源→双空间集成→批末折011+merge main。P0-13已在P041实现并由arch认可。本批不是R009整批完成。
- 用户验收句：消息先保存再告知收到；进程中断能恢复，业务成功后回复失败不重查；重复消息不重建任务或发媒体写，耗尽失败保留证据。状态implemented + self-tested + claude_review_pending。

## P-044 · 缺数三态基础 / 全信箱持续目标（2026-09-05，进行中）

- 老板已明确要求持续完成所有Claude派工，active goal已建立；总对账表 `2026-09-05-Claude信箱全量执行目标.md`，保持arch唯一冻结/终审、分支独立SHA、无push/真实媒体写。
- 基础代码 `ee62db2`：普通可缺指标用 `CanonicalMetricValue` 严格三态，与原data-query来源health六态隔离。absent/null→missing，真0→available；present-invalid/NaN/Infinity拒绝；全部成员先校验再求和，missing/error或空集→missing，overflow拒绝；RatioValue分母0语义复用，缺分母不是infinite。
- TDD：缺module红灯→新增15过→Domain46files512过；`metric-value.ts` V8行/函数/分支100%。code-quality-checker检查无新依赖/网络/存储/凭证路径，纯函数输入严格校验；Domain npm audit --omit=dev为0。Domain/DB/Worker/Gateway/Web typecheck+lint全通过。
- 本提交只有Domain基础3文件90行，既有v1接口尚未切换；没有新增PG行为、不重跑PG、不拿P043的205冒充本轮PG验收。P0-04整体依然进行中，不能算用户可用能力完成。
- 接续位置：`semantic-query-metrics.ts`九个COALESCE与缺日expected member；`semantic-query-dimension.ts`、`task-repository.ts.queryTaskMetrics`共享aggregate；`report-facts-source.ts`还把普通值写finite；`data-query-rows.ts`与`canonical-query-rows.ts`待切v2。查询表只含已存在行，汇总必须以授权tuple×业务日期的expected集合检测缺日（任务按有效relation），不可只对现有行sum/null计数。
- P044向arch确认R013依赖012的顺序冲突，以及v2需同步web/lib/data非视觉数据适配的边界；都不阻塞当前后端实现。R011/R012先提案要求保留。

### P044 第二增量：SQL expected account-days / 报表缺数（3e7f932）

- 10文件147+/100-；只改DB运行码/测试与Worker一条真实PG联合测试，前端/Contract/迁移/依赖0 diff。仍未合main/部署，没有真实媒体写。
- `expected_metric`按当前查询范围账户×请求日期左连canonical；完整tuple JOIN并复用原approved账户/媒体/owner/有效任务过滤。日期用date+integer，不受PG会话DST影响。预期占位不计入rowCount/accountCount；没有ETL的整天仍可表达missing，授权空集不产生趋势/维度行。
- 九指标去COALESCE，列内有NULL则汇总NULL；空集NULL、真实0保持0。NaN/Infinity优先导致拒绝，不让NULL掩盖坏值。CPA仍由safeDivide取聚合分子/分母；比率不平均。TaskRepository复用相同趋势查询，空taskId新增前置拒绝，避免optional filter退化为全workspace。
- ReportFacts内部旧state结构已保留missing（不再finite(null)/补0），公开v2 availability封装仍待下一增量。维度missing排序在已知数后，任务按有效relation聚合；没有擅自把未知account有效期推断成真0。
- TDD：新增SQL三反例首轮3红；修后旧三项测试明确暴露旧语义（空总量0/缺日曲线消失/partial和装全量），已以真实0补齐完整窗口并改缺日断言。Worker全量第一次1红：旧测试只有accountId、另一媒体同号无数据被旧SQL忽略；现补明确tuple+混合媒体missing及隔离workspace缺日反例，报表范围也显式media。不是只改断言掩盖数据。

| 门禁 | 本增量最终证据 |
|---|---|
| Domain `npm test -- --run` | 46 files / 512 passed |
| DB `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 36 files / 211 passed，真实PG+unit（与Worker串行） |
| Worker 同上 | 83 passed files / 641 passed + 2真实外部opt-in skipped |
| Web `npm test` | 78 passed，无源码修改 |
| Domain/DB/Worker/Gateway/Web typecheck、lint | 五包全通过 |
| DB定向coverage | 4files/36tests；三运行核心文件行95.4%/分支86.3%/函数100%；不是全仓coverage |
| DB `npm audit --omit=dev` | 首次EPERM无结论，批准联网重试得到0 vulnerabilities；无新依赖，其他包此增量未重扫 |

- Gateway36含2PG为本轮9715125耗尽修复后实跑，SQL增量不动其运行依赖；benchmark依旧使用自有本机ka合成workspace，未冒称所有测试仅ka_r009_test。
- code-quality-checker自审：新增SQL标识符只有代码内固定列白名单，日期/范围/身份仍参数化，无token/上游body日志；tuple与空scope PG证明；真实0/缺成员/缺字段/NaN与任务有效期反例覆盖。Registry保留既有日期budget；未把本次小数据PG当吞吐证明。
- **剩余明确**：data-quality-repository还存在raw/canonical COALESCE与ELSE0，需要单独收口空对账不假通过；Domain六Query v2/两Adapter与fixtures未切；空间绑源/双空间/折011待后续。P0-04仍进行中，不将本提交当全信箱或整批完成。

### P044 第三增量：质量对账缺数 / 回灌unknown门（35d2482）

- 6代码文件140+/94-；独立SQL固定模板，raw/canonical按workspace/media/account并集核对，任一侧缺成员/字段总和为NULL，不把两侧不存在当0/0通过。明确field_sources=realtime/gap_filled或offline后不跨口径补位；仅无标记时保留既有offline优先、缺失再realtime的选择。已存在坏值拒绝，不拿另一来源掩盖。
- 普通对账结果内部nullable，总数缺失则差值/容忍值/passed均NULL；使用既有passed可空列表示unknown，无迁移/Contract变更。NaN/Infinity或PG numeric溢出转稳定SemanticQueryContractError，不回显原值。只有有限值双方才算0.1%容忍。
- Worker只把passed===true当对平；unknown检查真实入库并沿现有failed-check通知，回灌质量阶段失败而非done。普通quality job的done仍仅表示检查过程结束，不表示passed=true。未引入真实媒体动作。
- 预期范围是raw/canonical观测tuple并集，**不是全workspace账户完整性证明**（同步可能只有当前credential显式scope）；漏在两侧都未观测的账户仍需ETL/health coverage门。没有把对平结果冒充全范围齐数。
- TDD首轮3红：空两边、指定实时却无实时、同号跨媒体数相等；新增非法数值4种其中超大指数再红并修稳定错误；真实PG覆盖NULL不能掩盖NaN、真0、最新源、容忍边界、unknown落库。移除本测试全库DELETE，改每case独立workspace+限定读取并关闭pool。

| 门禁 | 结果 |
|---|---|
| Domain `npm test -- --run` | 46files / 512 passed |
| DB `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run` | 36files / 219 passed（含13质量真PG） |
| Worker 同上 | 83passed files / 643 passed + 2外部opt-in skipped；新增unknown回灌真实PG，三场景complete/mismatch/missing全通过 |
| Web `npm test` | 78 passed，未改前端 |
| Domain/DB/Worker/Web typecheck与lint | 四包全通过；Gateway本增量未动且未重跑 |
| DB核心V8 coverage | 93.05%行 / 73.91%分支 / 100%函数；超过核心行80门，不虚报分支80 |
| DB生产audit | `npm audit --omit=dev` 0 vulnerabilities，无新依赖/锁文件；其他包本增量未重扫 |

- code-quality-checker自审：SQL所有身份日期参数化，字段/资源/错误码固定，最新row排序明确；没有新增HTTP/外调/凭证日志；数量级仍为既有单日分组，无N+1。未发现本增量新P0/P1，待arch独立审。benchmark仍仅旧本机ka合成workspace机制，不冒称所有PG均隔离库。
- 代码35d2482未push/合main/部署。质量空对账现已修；接下来必须完成六Query row v2、两Adapter和fixtures，再空间绑源/双空间/折011，不得把内部nullable改动当公开三态完成。

## P-044 · 来源空间身份 8f28a2a（2026-09-05 22:30）

- SourceLineage `workspaceKind` 必填personal/team，缺省/非法值strict拒绝；来源metadataAvailability仍只看datasetVersion/dataAsOf/timezone/dayCut，不因有Session就宣称数据新鲜度known。
- 两Adapter由execution scopeKind输出；Service最终所有普通、诊断、失败降级分支由auth.workspaceKind覆盖，不依赖源名推空间。个人诊断KA来源仍personal，团队只读仍team；不读取或记录额外秘密。
- Domain红灯：旧schema无workspaceKind可通过；Worker红灯：adapter自报字段不能得到正确Session标识。修后追加上游自报、反向team覆盖、两侧故障、三成功fixture缺字段、HTTP模式对应断言。
- 验证：Domain46文件518；Worker非PG79文件686+2外部skip；两包typecheck/lint全过（Domain首轮unused变量报错已修）。116核心定向行90.45%/分支83.77%/函数100%；其中KA client分支77.33%、Platform分支78.57%，不冒充每个文件分支均80。Worker生产offline audit0，diff --check绿。
- 14文件代码SHA，不含前端/DB源码/迁移/依赖变更。当前PG最近实测55432拒连，本子批未重跑；未push/合main/部署。#8的BFF/团队reader尚未完成，因此不独立部署。继续A-001非视觉BFF与v2消费，再双空间/折011/merge main。

## P-044 · 空间绑源 Service/HTTP 接线 9b7968b（2026-09-05 22:21）

- 普通请求严格仅queryId/params；移除旧HTTP静默覆盖dataView逻辑。sourcePolicy位于DataQueryService，内置调用也不能绕过entitlement。personal固定platform，team固定ka_data，KA未配置team503且不回退。
- 独立POST /api/v1/admin/data/reconcile；普通路由不接受reconcile.account_daily，管理员端点不接受summary等普通查询；所有角色无明确entitlement403，KA off422，dataView/data_view或自报entitlement400。requestId原样安全透传；Session/internal bearer/405和exact-byte保护共用，没有开放媒体写。
- 服务端只记录selectedSource/reason/requestId。已有异常聚合对账测试不能再通过普通summary+dataView入口，已改为该入口拒绝且两Adapter均不执行；单侧缺源的canonical日对账回归继续保留。

| 门禁 | 本次真实结果 |
|---|---|
| Domain全量/typecheck/lint | 46文件514，通过 |
| Worker非PG全量/typecheck/lint | 79文件683通过+2外部opt-in skipped，通过；显式排除PG不计通过 |
| 定向覆盖 | HTTP34 + Service15 + 新接线4 + 策略22 =75；核心行90.89%、分支89.47%、函数100% |
| 启动 | 无KA配置可启动/显式开启缺配置拒绝，真实子进程smoke2通过 |
| 真实PG+HTTP | beforeAll/afterAll连接55432拒绝，1套件失败、业务测试未执行；当前pg_blocked |
| 安全 | Worker npm audit --omit=dev --offline：0（缓存审计非新远程数据库）；根目录误跑ENOLOCK已纠正；diff --check通过，无依赖或凭证变更 |

- code-quality-checker自审：新增路由无SQL/动态命令；特权来源只Session与部署配置；browser额外键在任何Adapter之前拒绝；方法/响应长度/错误信封共用，无新增循环或同步重计算。核心超过80%，待arch独立审查。
- 未完成：team受信reader绑定/lineage.workspaceKind、BFF v2与按Session验mode、KA启用双空间真实PG、折011+merge main；本SHA不能单独上线，因为旧BFF仍发dataView。保持全信箱目标active，不把中间接线称功能全完成。数据库不可用期间继续不依赖PG的工作。

## P-044 · 公开 v2 后端增量 bdc5273（2026-09-05）

- Domain六Query严格`<queryId>/v2`：九普通指标+daily五项用`{value,availability}`；RatioValue仍finite/infinite/undefined，rowCount/accountCount/anomalyRows为查询元信息不包装。旧v1版本、裸数字、非法availability和值组合均拒绝。三个成功JSON fixtures已升级，错误fixture不改。
- 两Adapter解析present-invalid先拒绝再归一化；真0 available、absent/null missing；transport截断后所有业务指标error、ratios undefined，identity/count保留。可信PG total==2000完整则不误置error，KA未知total 2000/10000及exact字节上限保守截断。
- KA固定SQL改为approved tuple×日期LEFT JOIN真实成员，COUNT只记observed；缺一天/任一成员NULL不算部分SUM。SQLite文本强转0、Infinity/SUM溢出先返回INVALID_METRIC哨兵，由typed adapter转稳定上游错误；不让序列化null掩盖损坏。summary/trend/reconcile模板版本同步v2。
- 多日summary额外内部`account_day_count`证明覆盖，不对前端发新DTO；trend校验日期窗口/重复。跨媒体同号仍以(media,account_id)限定，无自由SQL/用户列名。SQL请求仍backend=sqlite，无真凭证调用；CTE在本机Node22真实SQLite执行，内网服务SQL网关是否接受CTE仍需OS确认，不能当线上证明。

### 实测门禁和失败记录

| 范围 | 命令/结果 |
|---|---|
| Domain | `npm test -- --run`：46文件514；typecheck/lint通过 |
| DB真实PG | `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run --testTimeout=15000`：36文件219、type/lint通过（16:32）；首轮默认5s migration超时，其余218通过；重跑该迁移3.018s，本批无DB源码改动 |
| Worker全量含PG | 21:32运行：PG ECONNREFUSED，7文件失败/19测试skip含beforeAll失败，不能计为通过；Docker ps只读命令持续无输出，未重启共享Docker/业务服务 |
| Worker非PG最终 | `npm test -- --run --exclude 'test/*pg*' --exclude 'test/workflows/*pg*'`：77通过文件、648通过测试+2外部opt-in跳过；type/lint绿。被排除PG必须恢复后补跑 |
| 定向+coverage | canonical rows / Registry /真实SQLite / 两Adapter：84通过，核心4文件行91.85%、分支83.2%、函数97.29%；含六Query×两Adapter12组parity |
| Web只读检查 | 原测试78、typecheck绿；用web`dataQueryResponseSchema.safeParse`直接读contract fixtures：ready-lineage/unknown-lineage/reconcile-pending全false，stable-error true。**旧测试不证明v2端到端兼容** |
| 安全 | Worker`npm audit --omit=dev --audit-level=high` 0；diff --check通过，frontend/依赖/migration0diff；只读生产SQL固定标识符/参数白名单，测试仅内存SQLite合成INSERT |

- TDD：旧schema对v2红灯；截断mask新增7红；真实SQLite缺数/坏值7红；Inf/SUM overflow新增3红，修后通过。早期HTTP sandbox EPERM不算业务红灯。
- 后续仍为#8空间绑源+BFF →双空间真实PG→折011→merge main→P044整批回执；R013/R013b/R014新派工已登记总目标，不因单批提交缩小全信箱范围。

## P-044 · A-001 非视觉BFF/v2增量（2026-09-06）

### 团队reader最新子步be93018

- arch@f841da4已冻结一reader↔一team；配置与固定SQL已实现，team不解析grants、不匹配503；personal仍explicit scope。table/detail/summary/trend真实SQLite模板通过；源未知完整目录时coverage明确partial而非编造全量，传输截断仍error，日期类型显式转换。
- Domain518/Worker非PG699+2外部skip/Web111，Domain/Worker type/lint、60核心覆盖行90.21%/分支84.36%，offline Worker audit0。PG01:37:28仍拒连，两新开/关场景未执行。没有接真reader凭证、没有部署/合流/媒体写。
- #8还需account.anomalies团队unsupported语义收口（信箱已提，不自造）；#10真实PG恢复重跑；接着最终风险检查/折011/merge main和P045整批。此前“绑定待确认”已被本条覆盖，不重复等旧问题。

- 代码`2d6570f`：Web六Query消费严格v2/三态，直接读取后端正式fixture，旧v1拒绝；RatioValue不重算；按响应mode消费team结果。留痕`c4b2733`。
- 代码`053f9ea`：BFF不再注入dataView，普通五Query+两字段；服务器current Session决定来源，kind/source/workspace校验，旧token/logout不查数，无数据源fallback。复用Session请求安全约束、同requestId、两次查询10秒总预算、exact16MB。
- Web111/111、Domain518/518、Worker非PG686+2外部skip，三包type/lint、Nextbuild通过；BFF/client覆盖行96.84%/分支82.76%，v2schema100%。Web offline audit0，仅缓存审计。无新依赖/React/样式/DB/migration代码。
- **01:19:53真实PG重试仍失败**：55432拒连、suite1失败/业务case1未执行；不称PG或内网通过，没重启Docker。真实启用KA团队reader/双空间集成未完成。
- 旧源选择React容器可能仍由reconcile偏好产生普通路由400，视觉与诊断入口交fe/arch；不因此放宽普通接口。单shared-reader的team绑定待arch确认；之后启用KA反例→折011→merge main→整批审查。全信箱目标保持active；当前未push/合流/部署。
