# P148 重复告警次数质量报告

## 本批状态

- 代码 **e3cd971253380eceb7ac82eaff40fa045795aae2**；be/r010，main c3451db 已包含，提交前 merge main = Already up to date。
- `codex_prechecked`、`claude_review_pending`；未合流、未部署、未 push、未开放媒体写。
- 生产改动仅 WorkItemRepository 8增2删；新增10个真实PG测试。没有迁移/Contract/前端/be2文件改动。

## 缺陷与处理

012 已有 occurrence_count 和 last_triggered_at，但真实 createOrMergeAlert 不更新它们：每次并发重复依然 count=1、时间null。先写9个反例，旧代码9/9失败（不是连接失败）。修复后新增最大可表示值边界，共10项通过。

首次 INSERT 明确 count=1、last_triggered_at=now()；重复 UPDATE 在已有 advisory lock + FOR UPDATE 内 count+1，并记录拿锁后 clock_timestamp()。它是平台处理触发时间，不是数据源刷新时间。null/0/负数/INT溢出旧计数拒绝且整体回滚，不猜历史、不默默修复。

## 验证

| 门禁 | 真实结果 |
|---|---|
| RED | pg-red.log：新9失败，真实PG已连接并执行 |
| 初次 GREEN | pg-green.log：新9+旧12 = 21通过 |
| 最终定向/覆盖率 | coverage.log：新10+旧PG12+unit33 = 55通过 |
| DB全量 | 96文件、1028通过；包含unit及真实PG，不是1028项PG |
| Worker全量 | 144文件通过、1560通过+2外部opt-in跳过 |
| DB/Worker typecheck、lint | 全通过 |
| 仓储覆盖率 | 99.05%行、95.16%分支、100%函数；旧 severity=null 异常分支等仍未全覆盖 |
| 依赖安全 | 锁文件0 diff；Worker offline production audit0，不冒充在线最新审计 |
| 静态质量 | diff --check通过；SQL均绑定参数，无新动态SQL/网络/N+1/凭证输出 |

日志：`output/qa/p148/`。数据库限定 localhost:55432 的 `ka_*_test`，本次使用 ka_be_r010_20260907_test；测试清理仅自己的合成workspace。DB/Worker/覆盖率按序，不并发回放迁移。两包 type/lint 各保存原日志，真实媒体/队列不启动。

Domain/Web本批0 diff，未重跑；最近P146的1143/201不是本批新证据。没有浏览器/Nextbuild/真实内网验收。

## 自审与未完成项

- ✅ 同级/降级重复计数，不降severity、不重开processing/dispatched；10并发最终11，无丢数。
- ✅ 同ID跨媒体/空间不串计数；终态新建新系列，旧计数不覆盖；最大INT最后一步成功再下一步拒绝。
- ✅ 坏计数失败不更新title/status/time，没有半成功；没有更改现授权或加公共写入口。
- **仍缺**：旧严重度升级原地改行，不满足关旧建新/superseded_by，依赖已报的013；本批不得称升级闭环完成。
- **仍缺**：counter是成功处理调用的累计，不是有event ID的exactly-once外部事件数；没有新增事件幂等键或假装worker重试不会重复计数。
- **仍缺**：完整工作项详情/decision/动作公共DTO与处理按钮。现api.md v1.3详情、fixtures/work-items/detail.json、旧read-detail-schema不同；纯ignore成功DTO已登记待arch，不返回伪成功。
- 计划/质检依writing-plans、code-quality-checker执行；Coze外部托管不在本仓交付范围，未上传公司代码/数据或创建外部项目。

目标仍active，继续有效队列中无外部依赖项，不把本子批冒充整个信箱完成。
