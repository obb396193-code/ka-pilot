# 发给 OS：ka-pilot 确认 + 代码交付通道 + A–J 答复（老板复制整段；arch 2026-09-07）

```
一、ka-pilot 应用：确认，scaffold 12

1 应用名 ka-pilot ✅  2 仓库组 bpj02291154 ✅  3 仓库项目 ka-pilot（新建）✅  4 模板 scaffold-id 12（自定义框架应用 / nodejs20-basic）✅
你对齐的那点也确认：内测仍整套跑沙箱，这个 FaaS 应用是为正式化 + RDS 归属 + 域名提前铺路，不影响内测节奏。建吧。

二、代码交付：GitHub 私有仓（老板 2026-09-07 拍板改用 GitHub）

仓库 https://github.com/obb396193-code/ka-pilot.git （私有）
SHA   30ba1a733a2ad6a4272ff2b85d8a7d590f908b91  （main，五包真 PG 门禁全绿：domain 770 / db 710 / worker 1226 / gateway 36 / web 143）
按你的情况二走：我另发一个 fine-grained PAT，Contents: Read-only、只勾这一个 repo、有效期 7 天。你按自己说的方式用（token 从环境变量读，不写进 remote URL、不落日志、不回显），克隆完告诉我，我立刻吊销。
    git -c http.https://github.com/.extraheader="AUTHORIZATION: bearer $GH_TOKEN" \
        clone https://github.com/obb396193-code/ka-pilot.git /home/****/ka-pilot-src
    cd /home/****/ka-pilot-src && git checkout 30ba1a733a2ad6a4272ff2b85d8a7d590f908b91
仓里有 40 个分支（历史批次），你只用 main 那个 SHA，其余不用管。

三、五个最阻塞的

A1 代码：见上，GitHub 私有仓 + SHA，PAT 另发，现在就能拉。
A2/A3 三进程怎么起、端口：没有 pm2/Procfile，手写一个 deploy.sh，nohup 起三个 + 两个后台循环。就这五条：
  1) 先起库：/home/****/start_pg_tcp.sh
  2) 装依赖：仓库根 npm install（会自动跑 scripts/install-all.sh，按 lock 串行装五包，file: 依赖能解析）
  3) web：cd apps/web && npm run build && PORT=3000 npm start          → 0.0.0.0:3000（要给人访问）
  4) data-api：npm --prefix apps/worker run start:data-api             → DATA_API_HOST=127.0.0.1 DATA_API_PORT=3101
  5) worker：npm --prefix apps/worker run start:worker-http            → WORKER_HTTP_HOST=127.0.0.1 WORKER_HTTP_PORT=3102
  不要起 apps/worker 的常驻主进程（npm start），我们不用常驻消费者。
  两个后台循环（就是之前说的"部署脚本挂循环"）：
    while true; do curl -s -X POST -H "X-Worker-Trigger-Token: $TOK" http://127.0.0.1:3102/internal/worker/once >/dev/null; sleep 600; done &
    while true; do /home/****/pg_backup.sh; sleep 86400; done &
A4 Node 20.x 确认；用 npm（有 package-lock），不要 pnpm/yarn。**装依赖千万别加 --ignore-scripts**：前端的中文字体切片是 postinstall 生成的且不入仓，没生成会让整个前端编译失败、全站 500（我本地联调刚踩到）。装完自检一句 `ls apps/web/app/fonts/misans/misans.css`，不在就跑 `node apps/web/scripts/fonts/prepare-misans.mjs`，正常会输出「MiSans 就绪：300 个切片」。monorepo 的 file: 依赖我们验过 6 例，但没在干净克隆上验过——你 clone 后第一件事就是跑根 npm install，不通立刻告诉我。
B5 访问入口：内测先全沙箱，用你的 port-mapping URL，重启变了就重发给我，内测人少能接受。不要现在就把 web 拆去 FaaS——那会多一个"FaaS 到沙箱走 HTTPS"的变量，等 ka-pilot 应用和代码仓通了再试。
C6 DATABASE_URL：先建库再连，不要连 postgres 库。
  createdb ka_pilot（或 psql -c "CREATE DATABASE ka_pilot"）
  DATABASE_URL=postgresql://admin@127.0.0.1:5432/ka_pilot
D10 ka-data 是实时查还是同步进 PG：现在是实时查。个人空间的数据走奇航 ETL 落进自有 PG；团队空间目前是每次请求直接查 ka-data，不落库。你说的"快照同步进本地 PG"是后面 R-011 那批的事（五张表 + 每日发布指针），还没开工。所以这次部署你不用管同步，只要 ka-data 地址和 reader token 配好即可。

四、C/E/F/G/H/I/J 其余

C7 扩展：只要 pgcrypto + btree_gist，我逐个迁移查过了，没有 pg_trgm / uuid-ossp。gen_random_uuid 走 PG16 自带。
C8 seed：identityId 用你生成的那个（5e5ea046-...）。seed JSON 的结构在仓里 packages/contract/fixtures/ops/bootstrap-seed.json，字段是 identities / workspaces / memberships / grants 四个数组，照着换成真值即可。
C9 seed 是登录硬前置，确认——空库没有任何身份，登录必失败。顺序：建库 → migrate → seed:bootstrap → discover:accounts（只读列账户，人确认）→ 再 seed grants → seed:coefficients → 起 data-api。
E11 UBP：所有源都没有，前端永久空态，确认。
E12 出价工具：你这条是新证据，很重要——ka-data 的 bid_tool 整列是空的。那团队源就不能直接读它了，个人和团队都只能从 unit.bid_type 派生。我这边改契约，暂时保持"出价工具维度不可用"，等 ad_entities 把 bid_type 等六个原值存下来再开。
E13 代理/自投：按账户级 custom_tags 展示 + 空态，确认；无匹配的显"未标注"，不猜。
E14 码表：我们从官方文档冻，不用你找。你的样本（bid_type=10、ocpx=190、deep=0、unit_type=10、campaign.bid_type=0、auto_manage=1）我已经记进证据，正好用来验码表。
F15 写链路契约：payload schema 和结果 schema 都我定，已在仓里 packages/contract/api.md，不用你设计。
F16 UNKNOWN：我们的规则是"没有幂等键就先回读一次确认，再决定重试一次"。所以要你提供一个回读接口：给账户 + 对象 ID，返回当前实际值。这个等开写链路时再做，现在不急。
G17 scrypt：脚本在仓里 docs/runbooks/2026-09-04-DataAPI内网部署与环境变量.md 的「生成 scrypt 材料」那节，从 stdin 读密码不进历史。用户名密码老板给你。
G18 DATA_API_INTERNAL_TOKEN：对，web 和 data-api 两侧读同一个值，你自己填两边 config 就行，不用交给我们。
G19 完整 env 清单在同一份 runbook，必填七项 + 本次新增 WORKER_TRIGGER_TOKEN / WORKER_HTTP_PORT。缺什么对着 runbook 看，有疑问再问。
H20 FFmpeg：素材拆片是后面 R-015 那批，现在不用装。真做的时候要 ffmpeg + ffprobe 两个（抽帧要 ffprobe 拿时长和分辨率），你提前想想 ffprobe 怎么办。拆片在产品 worker 跑，产物先落沙箱本地。
I21 钉钉网关是我们自己的进程（apps/dingtalk-gateway），和你之前那个 a1-client-faas 网关不是一个，第二阶段再说，这次不部署。
J22 口径：团队空间直接用 ka-data 已对齐的 cash_yuan / cash_assessment，不再算一遍；个人空间才用我们自己的系数算现金。
J23 探针：data-api 有 GET /healthz，只返回进程存活。烟测打它。
J24 日志：结构化 JSON 打 stdout，暂不接 sunfire。

五、之前那条还没发的，一并确认

worker 定时按你说的 (a) 但更简单：不用 autopilot 拉 agent，就用上面那个后台循环 curl 打 HTTP 口（沙箱无 crond，这样最轻）。
备份：先只落沙箱本地，OSS 等云账号到手再补。恢复演练在部署后做一次（pg_restore 到新库 + 打 /healthz）。
RDS：我这边去 CloudCenter 提，需要先拿到 BU 云账号 uid。你把 ka-pilot 应用建出来后，RDS 归属就填它。RDS 一到手，第一件事是你从 FaaS 测 5432 通不通 + 出口 IP 固不固定。
ka-data 同步策略（每 ds 单条 SQL + updated_at 批次 + 近 3 天标临时）和 bid_type 码表我们冻——这两条不变。
```
