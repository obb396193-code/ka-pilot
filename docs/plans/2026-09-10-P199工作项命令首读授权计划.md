# P199 Work Item Command Read Scope Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按已冻结P178统一AccountMute.ignoreAndMute与WorkItemCommand.apply首读授权谓词。

**Architecture:** 已批准Session → accountScopeParams/Clause限定工作项首读并锁定 → 既有live authority锁 → 原状态迁移/静默事务。零命中再仅查同workspace的id存在性，维持404与403，不读取不获授媒体的status/tuple内容。现有JS二次守卫和live grant撤权校验继续保留。

**Tech Stack:** TypeScript、PostgreSQL、Vitest。

---

采用writing-plans；这不是新增权限设计，执行arch已批准共享谓词收敛。无执行技能可用，按用户授权本会话TDD，不新开Agent。

1. `packages/db/test/account-mute-repository.test.ts`、`work-item-command-repository.test.ts`各加真实PG反例：Session只允许KUAISHOU，源工作项是同号TENCENT；观察驱动返回的首读行不含未授权media，原代码红。
2. 两个对应src文件import共享helper，首读增加参数化predicate；无行先存在性轻查询，已有对象拒绝403，真正不存在404。保留所有写入/输出守卫，绝不让task/self只读可见性自动变成写权限。
3. 两类PG、相关unit/HTTP/service回归与DB/Worker type/lint；低磁盘仅定向，code-quality-checker审参数、事务/回滚与错误。无真实媒体/no push。
4. 独立代码SHA和质量留痕；区别“预读取不规范”与“响应已泄漏”，不夸大漏洞。
