# 自查06：业务日与冻结任务窗口就绪度

代码 SHA：`febe2af0`，基于 `0956af0f`（含 arch main `d2ab7ab9`），2026-09-10。本机合成 PG verified，candidate；未合流、未部署、未 push。

## 对账

- 按 v1.9.24 页面业务日，三个列表 repository 各只有一行日期传参，未改变 be2 的授权/SQL/映射。
- `initial_full_complete` / `hasSuccessfulFull` 保留兼容字段名，实际改为 expected scope × 日期全部 canonical 存在、computed_at 非空且 `etlBatchReadableSql` 可读。空 scope=false；不依赖历史 full/incr 类型或 done 状态。新 Raw 未重新计算不能解除失败屏蔽。
- scheduler 在同一 RR/RO 内读取身份、grants、日期就绪度；auto/incr 按将要排的 D-1..D，显式 full 按原默认 D-6..D。无浏览器日期/身份新增入口，无 private qid 输出。
- recovery 先验证并重建原 job payload，再按每个原 job 的 ds/offlineReconcileDays 和冻结授权 scope 判断。后来新增 grant 不扩大旧任务。一次批量 SQL，无逐 job N+1；更新/audit 保留原短事务与锁，不声称整个 recovery 已升级 RR。
- 窗口要求真实日历日期、含首尾 1–31 日；scope/job batch 最大1000，序号/数量/类型校验；编码16MiB边界拒绝。候选查询 LIMIT1001 后上限守卫，不静默截断。

## 可复验门禁

真实 PG 均指定 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test`，只写随机合成 workspace；没有真实源请求、没有常驻队列消费者。

| 包 / 命令（在各包目录） | 结果 |
|---|---|
| DB `npm test -- --run test/workspace-sync-readiness.unit.test.ts test/workspace-sync-readiness.test.ts test/workspace-sync-repository.test.ts test/qihang-job-recovery.test.ts test/grant-revocation-pg.test.ts` + 定向 coverage | 60/60 |
| DB `npm test -- --run test/workspace-sync-readiness.test.ts test/account-list-repository.test.ts test/task-list-repository.test.ts test/work-item-list-repository.test.ts test/account-list-repository.unit.test.ts test/work-item-list-repository.unit.test.ts` | 48/48（其中5项与上行重复；DB去重103项） |
| Worker `npm test -- --run test/workspace-sync-pg.integration.test.ts test/workspace-sync-service.test.ts` | 13/13（PG4，逻辑9） |
| DB / Worker `npm run typecheck`、`npm run lint` | 全过 |
| DB `npm audit --offline --omit=dev` | 缓存审计0；不是新联网审计 |
| `git diff --check` / 路径审查 | 通过；apps/web、视觉、公开Contract均0改动 |

覆盖：三内核合计行94.87%、分支89.47%；readiness新内核100%/100%；recovery100%/91.22%；既有scheduler repository86.66%/70%（遗留非法grant/rollback等分支非全覆盖，不冒称100%）。

## 真实失败及修复过程

1. 先红测11项中7红：缺日期参数/校验、无batch API、旧SQL仍依赖completed_job；新实现后绿，随后加边界到13项。
2. 首次 PG 46绿1红：新增测试漏 Raw.source NOT NULL，修合成 fixture 为 realtime，未放宽DB约束。
3. Worker 首次12绿1红：旧测试前序临时将priority改-32768，后序复用同job引起正确的幂等冲突；恢复测试为 JOB_PRIORITY.DEFAULT，生产冲突守卫不变，重跑13绿。
4. 根目录 `npm --workspace` 不适用本项目，改在包目录运行；计划已修。

## 安全 / 性能 / 未完成

code-quality-checker 自审：无新增 Critical/High；外部值均参数化，SQL复用静态谓词；空/重复/越界scope fail closed，输出无凭证。批量窗口一条SQL，真实RR并发发布反例通过。

磁盘4.6GiB，按arch低于8GiB不跑五包全量/build，不删缓存。Runtime **尚未注入** failures，仍fail-stop；下一独立批 P176 Task3 才启用并写明变化。OS真凭证 full→页面真数仍需外部实证，不以本机fake/fixture替代。历史blocked_auth除既定Qihang身份恢复路径外仍不擅自解除。
