# 演示就绪（Demo-Ready）目标定义（老板 2026-09-07：给技术同学看的要像成品，不要半成品）

> 这是 arch 循环工程的**靶子**。每一圈都问一句：离下面这条路径还差什么？差什么就先做什么。

## 一、判据：一条完整可演示路径能走通

登录 → 工作台看今日队列 → 账户池看九态与达标色标 → 数据分析看大盘/趋势/维度 → 任务详情看八页签 → 变更集试运行（不真写）→ 报告看日报

**每一步都必须**：真接口（不是 fixture）、真数据库、缺数显 −（不补 0）、颜色从后端 DTO 来（前端不自己算）、切团队空间能看到只读态。

## 二、必须完成的（P0，缺一不可）

| # | 内容 | 归属 | 现状 |
|---|---|---|---|
| D1 | 登录 / 会话 / 空间切换 全链路 | — | ✅ 已联调通过 |
| D2 | 账户池列表 + 九态 + 达标色标（BFF 路由补齐） | be2 | ✅ 全通：6 户真数据、九态/来源/最近动作/下一步建议均有值 |
| D3 | 工作台今日队列（BFF 路由补齐） | — | ✅ 通（灌 7 条工作项，角标 P0=1/P1=3）|
| D4 | 数据分析：大盘 + 趋势 + 至少 3 个维度 + 缺数三态 | Codex | 🟡 大盘三态 ✅、交叉表 ✅（task×biz/account×task）；hourly/gap 源未接（F-P153-1/2，v1.9.4 已裁源与规则，Codex 实施中：021 小时表 + Gap 规则版本派生） |
| D5 | 任务列表 + 任务详情（至少前四页签） | be2 | 🟡 列表通；详情后端 ✅ 合 main `24ede2d` 并实测通（键与 fixture 逐一致）；**前端接线派 fe F8-8**（BFF 透传 + overview 切真数据）；cost/预算七项恒 null 等 Codex 两个源 |
| D6 | 变更集试运行（预检 → 试运行 → 停在确认前，不真写） | Codex | ✅ 直连 503 source-off 与 fixture 逐字一致；BFF 同源门已修（Codex 86ebd17，Sec-Fetch-Site 优先）——arch 2026-09-09 用 127.0.0.1 + 真会话实测 same-origin 放行/cross-site 403；仅无 Fetch Metadata 的老浏览器仍需 localhost |
| D7 | 日报（12 模块，至少渲染出来） | be2 | ❌ 已补派 be2（优先于 R-017） |
| D8 | 一批**形状真实的合成数据**（账户/任务/指标/工作项），让每页都有东西看 | arch | ✅ `scripts/seed-demo-data.py` 一键（6 户/3 任务/186 指标/7 工作项/1 草稿/授权） |

## 三、加分项（P1，有时间就做）

- 归属清洗页（昵称解析）——这是差异化亮点，技术同学会问"你们怎么做归属的"。**2026-09-08 后端全链路 ✅**（规范/干跑/解析/列表），前端第七 tab 待接
- 知识库（复制 ContentRadar 编辑器，已铺前端）
- Agent 抽屉 + ⌘K
- 团队空间真数据（R-011 团队快照）

## 四、明确不做（演示时直接说明）

- 真实媒体写（全程只到试运行，写开关关死）
- 素材拆片（依赖 FFmpeg + ASR，未部署）
- BUC 正式登录（内测用账密）
- 移动端

## 五、给技术同学看的时候怎么说

一句话：「**内网数据拉进自有库 → 算达标和差距 → 给建议和一键操作 → 人确认后才写回媒体**。现在数据链路和建议链路都通了，写回media 那一段全关着，正好想请教怎么正规化。」

演示顺序按 §一 那条路径走，边走边说三件我们做对的事：缺数不补 0、所有归属都能人工改、写操作必须人确认。

> 2026-09-08 状态：**演示从 `http://localhost:3411` 打开**（习惯保留；F-P157-1 已修，现代浏览器从 127.0.0.1/沙箱域名打开写操作也通）；账号 demo / local-integration-test-only；演示前重跑 `python3 scripts/seed-demo-data.py`（草稿 TTL 2h）。
