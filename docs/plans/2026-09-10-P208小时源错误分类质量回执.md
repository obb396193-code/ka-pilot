# P208 小时源错误分类质量回执

代码 **`1d19bba9`**。发现 hourly port 抛出的可信 `HourlySourceError` 被 QueryService 的 catch 无差别改成 SOURCE_UNAVAILABLE；三条真实行为测试先红（403/502/502全变503），修复后绿。

## 修复

- 仅 HourlySourceError 保留类型后交原 mapError；FORBIDDEN→403、SOURCE_TRUNCATED/UPSTREAM_INVALID_RESPONSE→502。
- 私有 port 增加 SOURCE_UNAVAILABLE/UPSTREAM_TIMEOUT，对应现有 Query HTTP 503；超时 retryable=true，未改公共契约或其他路由状态映射。
- 文案固定，不转发错误 message/cause/stack。普通Error、伪造带code对象、null、字符串一律安全503，不按上游自报code获得特殊处理。
- 请求认证、账户范围检查仍先于port执行；没有失败后的daily/KA fallback。

## 门禁

Worker四文件 **129/129**：hourly-public-query32（本批新增11）、data-api-http57、data-query-output-budget25、data-query-service15。

```sh
npm test -- --run test/hourly-public-query.test.ts test/data-query-service.test.ts test/data-query-output-budget.test.ts test/data-api-http.test.ts --coverage --coverage.include=src/data/hourly-public-source.ts --coverage.include=src/data/query-service.ts
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

type/lint通过，缓存production audit0；新5种错误逐一经过本机真实 HTTP，requestId不丢，401/未授权账户403时port未调用；既有BFF/Session/10k/输出预算用例全部回归。源与Session解析为测试桩，**未声称真实PG或OS联调**。

覆盖率：hourly-public-source行100%/分支89.47%，QueryService行89.42%/分支85.77%，两模块函数100%。新增错误分支均被执行；未覆盖处主要既有source验证分支/非小时查询组合，不把总覆盖说100%。未改source成功envelope、字段校验、scope守卫、16MiB门、真实媒体写关闭状态。

只改两个生产文件+一测试+计划；未新增依赖，不触前端/Contract/DB；未做最新联网audit。磁盘约3GiB低于全包门，未五包构建，未清缓存。采用writing-plans/code-quality-checker。

## 最新依赖进展

本批结束前已收到本机main **`f074992e`** 的 v1.9.21：P201迁移编号改024/025与复合FK，P202全局治理管理员范围，P203按expected tuple-day可读性判首次就绪，P206 SOP一期内部模板/202响应等已裁。**不再把上述问题标成未答复**；下一批先同步main按原队列实现024/025，be2 Q036/Q037仍由be2处理。

P207 reader尚未装配到公开hourly，P208只是接线前的错误边界修复；生产采样/投影/factory/迁移仍要完成。未push、未合流、未部署。
