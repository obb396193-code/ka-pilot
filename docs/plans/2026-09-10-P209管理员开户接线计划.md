# F-OS-004 管理员开户接线 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 api.md v1.9.21 完成全局治理管理员的建成员、一次性初始密码和重置会话闭环。

**Architecture:** 从服务端已解析身份反查任一 active team admin membership；角色字符串本身不是全局授权。复用 identity-password-repository 的异步 KDF，身份/user/personal workspace/membership/password 在同一事务提交；重置密码与撤销全部 session 原子提交。公开 strict schema 与当前只读成员 schema 分阶段接线，未装配端点不得声称功能完成。

**Tech Stack:** TypeScript、Zod、Vitest、pg、现有 Session HTTP/R010 路由。

---

权威：本机 main f074992e，inbox-codex 最新序 F-OS-004 →024→025→readiness→sop-run→P176 Task3。executing-plans 技能当前不可用；按用户持续授权由本会话逐项执行，不另开会话/子 agent。生图已取消。磁盘约2GiB，遵守低于8GiB不跑五包全量/build；定向门禁不能冒充全门禁。

## Task 1：strict Domain 与冻结 fixtures（本次独立提交）

Files：修改 `packages/domain/src/admin-members.ts`；新增 `packages/domain/test/admin-member-commands.test.ts`。

1. 写红测试：internal_test 给定/省略 initial_password，BUC 禁止密码；拒绝客户端 scope/identity/workspace 注入、非法用户名/角色/日期/多余键。
2. 执行 `npm test -- --run test/admin-member-commands.test.ts`（packages/domain），记录真实红。
3. 新增 request discriminated union、created/reset response、members-v195 独立响应；复用旧 member 字段与 meta。示意：`z.discriminatedUnion("provider", [base.extend({provider:z.literal("internal_test"),initial_password:password.optional()}),base.extend({provider:z.literal("buc")})])`，所有 object strict。密码规则复用已冻12..1024，不限制已有生成 fixture 的长度到16。
4. 冻结 fixture 只去除文档注释 meta._note 后 parse；密码只允许 created/reset，不允许 list。响应 initialPassword 对 internal_test 必填，对 BUC 禁止。reset request 必须空 object。
5. 定向新旧成员 tests + typecheck/lint/cache audit，代码独立 SHA。旧只读 schema 暂不替换，等待 Task3 同批接线。

## Task 2：可信管理员与原子写入（下一独立批）

Files：新增 `packages/db/src/admin-member-provisioning-repository.ts`（新全局路径，旧局部grants仓储暂不动）、修改 `packages/db/src/identity-password-repository.ts` 的 `setPassword` 增加可选事务 executor、`packages/db/src/index.ts` 导出；新增 `packages/db/test/admin-member-provisioning-repository.test.ts`。

P210执行顺序：①写真实PG create/reset用例并跑红；②在同一个client上锁当前membership及identity、查任一active非demo team admin，写四对象与password；③错误回滚/并发重复/失活与伪造scope反例；④全局read同RR快照，identity的personal成员行去重/截断；⑤定向PG和类型/lint/缓存audit，独立交审。生成密码=`randomBytes(12).toString("base64url")`，16位；KDF只调用现有`setPassword(..., client)`，不复制。真实PG用既有迁移表，数据按本suite随机identity/workspace集合清理，不删共享fixture。

先审计 auth identity 如何由 workspace-local user 唯一定位、membership锁与身份并发约束。真实 PG 先红：personal admin 非治理管理员403；其他空间存在 active team admin 可操作；失活 identity/member/user拒绝；建四对象+password原子；重复409；密码失败不留孤儿；重置撤销全部session，BUC不得写password。密码仓储补可复用同事务执行器而非另写KDF，保持已有调用兼容；无明文落库/日志。GET成员按全局 identity 的 personal membership 单行输出（歧义 fail closed），MAX1000 + sentinel，不做 team grant聚合。grants子接口旧局部语义不擅自扩大。

## Task 3：Service/HTTP 接线与真实登录（下一独立批）

Files：`apps/worker/src/admin/members-service.ts`、`apps/worker/src/r010/admin-members-routes.ts`、`apps/worker/src/data-api.ts` 与现有 admin/login tests。

P211细化：保留AdminMembersService.read的局部grants路径；增加第三个依赖provisioning port，list/create/reset只走该全局port，缺依赖503而非回落局部list。Shell options扩充方法，路由表新增reset-password并传request字节限额。先新增 `test/admin-member-commands-http.test.ts` 真HTTP红测试；接线后更新旧成员测试到新全局list、维持局部grants负向断言；新增真实PG+HTTP注册/登录/改密/reset生命周期测试，所有新数据为随机合成fixture。不改变公开Contract和前端。

先写201/409/重置200、viewer统一403、401/405/非法JSON/query/UUID、requestId/exact16MB红测试。使用Task1严格schema，route不凭本地role推导全局权限；真实repo负责可信授权。创建返回一次initialPassword且no-store，列表仅mustChangePassword。与现有登录provider实际对拍：创建→登录→自改→旧密码无效→重置→旧session401。更新P190 reset-password暂挂只在真实route存在且扫描绿之后。

## 交审

各批路径限定提交，不push、不接真实凭证/媒体写；独占测试库 `ka_be_r010_p194_test`，不用共享业务库或启动队列。代码质量检查覆盖类型/SQL参数化/凭证泄漏/事务/容量/并发。报告列实际测试、未接线/未合流/未部署，不把schema阶段称开户完成。

### 2026-09-10 Task3 接线补证与优先级更新

- 上文 Task1 的存储边界12..1024不代表登录可用范围。实读既有Session契约：登录password最多512、identity.displayName最多200；新建输入直接复用这两条，避免开户成功但登录400/Session schema失败。已加512/200真HTTP成功、513/201拒绝。旧自助改密仓储1024是be2的既有边界，未越界修改，回报arch检查其路由与Session一致性。
- 新成员无ENV条目，旧provider只按ENV解析identity，真实链路不通；增加存储按active internal_test用户名查identity+hash，继续共用async KDF；关闭internal_test仍拒绝。旧ENV仅bootstrap回落，不写入或回显配置。
- 收到main cb9e368c：P-211是arch看板派单，不再把本Task3叫P-211；本批测试中的p211仅合成fixture标识。当前代码已在门禁收口，只保存独立交审，不继续拓展开户；随后立即按新序 P176 Task2/3→readiness→025→arch P211看板API。024/sop-run后置。编号以后由arch派，本人自查用日期序号。
