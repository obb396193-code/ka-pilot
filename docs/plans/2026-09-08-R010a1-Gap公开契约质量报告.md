# P140 Gap公开契约与缺源准入质量报告

代码 **f16cf95**；已合arch main@c3451db（本支merge cdbb856），包含I004 HTTP内测cookie修复。无Contract、be2保护文件或视觉变化；不push、不部署、不改媒体。

## 实现与未实现

- Registry固定account.gap，account/task/biz三分组、P138严格日期/媒体/账户参数、31日/10k限额；不支持KA SQL或浏览器选择源/规则阈值。
- Domain/Web Canonical account.gap/v1：source.groupBy、唯一组、缺数/零分母/三态、meta.ruleSetVersion严格且必填；未知时钟null，dataAsOf与source对应，businessDate取窗口尾日的当前fixture语义。未把fixture注释字段或错误时钟放宽到线上。
- Session范围校验后未接版本化Gap reader返回503 SOURCE_UNAVAILABLE；越权账户403、伪造阈值/规则版本400。不从旧表格返回一行假Gap，也不提供绕过成员授权证明的成功port。
- **不是Gap数据功能完成**：真实版本化规则来源/规则阈值选择、聚合账户×日成员证明、preDeduction归因量和扣量的窗口组合尚未落地。当前无生产ready成功，不会默认normal。未来adapter必须证明输入三键和窗口，再计算，不能凭group标签当授权。

## 证据

日志 `output/qa/p140/`（未入仓）。首次TDD5红：Registry404及Envelope拒绝；实现后全通过。

|门禁|当前结果|
|---|---|
|Domain全量|74文件1080/1080|
|Worker Gap/Registry/Hourly/data-api/Session|5文件117/117|
|Web node全量|196/196|
|实际spawn主服务+DB Session+合成PG|1/1；Gap503/未授权403、旧pivot/mute/ignore/team/logout回归|
|Domain/Worker typecheck+lint|通过；曾有control-regex lint，已用逐字符判定修复|
|Web本批定向typecheck+eslint|通过|
|Domain行契约定向99项覆盖|operational-query-rows 行/分支/函数100%；不是整个Service覆盖率|
|Worker production离线audit|缓存库0 vulnerabilities|
|diff/check、安全/保护路径人工核验|通过；版本不进日志，无新凭证/SQL/媒体动作|

合成PG唯一 `ka_be_r010_20260907_test`，未读真实账户/其他项目。新Web/Gateway测试是协议与BFF函数，不代表实际Next按钮/浏览器联调。

磁盘从6.6降4.5GiB，未运行全DB/Worker/Next build；全Web类型旧依赖问题仍未清，不宣称全绿。没有清用户缓存。arch的I004随基线合入，默认Secure的Session回归过；AUTH_COOKIE_INSECURE显式开关真浏览器实测归arch，本批未重复证明。

## 交接

- 当前Gap fixtures meta.dataAsOf与source不一致；本批只为测试构造明确synthetic时间关联，没有编辑权威fixture。
- 需要arch确认实际ruleSetVersion与可读阈值快照来源；现alert_rules.condition_tree有version字段描述，但无规则集生效版本Repository，不能把某条树version或updated_at擅当整套规则版本。
- 总信箱目标active。当前仅安全准入/缺源交付；下一步审计其他不依赖本源的R010a2/后续队列，不等本批审完。
