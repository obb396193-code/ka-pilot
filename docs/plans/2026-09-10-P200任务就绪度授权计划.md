# P200 Task Readiness Scope Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复P178实读发现的任务readiness聚合漏账户scope，保持当前冻结DTO与team只读语义。

**Architecture:** 继续现有Session→TaskListService→RR/RO Repository；仅在readiness账户集合生成处使用共享accountScopeClause，余额/unit联接自然收口。任务可见性、总关联数用于coverage、人工任务级标记不改变。不改be2拥有的Service，不新增接口。

**Tech Stack:** TypeScript、PostgreSQL、Vitest、真实本机HTTP。

---

已由arch P178授权设计：personal按tuple、team本workspace全量，直接引用共享谓词。本次不是重选产品权限。采用writing-plans整理实施；执行技能不可用，用户已要求原会话持续完成派单，不新开Agent。

1. 新建`apps/worker/test/task-readiness-authority-pg.integration.test.ts`。合成同workspace KUAISHOU/TENCENT同号与第三个私有账户，只有快手同号有余额/unit；用实际Session+HTTP证明旧查询把未授权户列进就绪度。切team、撤权、同号跨媒体与跨workspace回归。
2. 红测试后改`packages/db/src/task-list-sql.ts`：readiness内`account_task`加共享accountScopeClause，值仍为批准scope参数$3/$4；不复制权限逻辑。
3. 新PG/HTTP +旧任务、账户、规则、路由定向与DB/Worker type/lint；低磁盘不五包。code-quality-checker查SQL/事务/隐私/覆盖。
4. 独立代码SHA与回执。明确这是已通过真实响应证实的缺口，不与P199的进程首读规范问题混称。P178全域审计继续交汇总，014部署顺序问题另外报arch。
