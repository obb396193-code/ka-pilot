# 探针 B 原文（发给 OS，存档）

> 2026-08-13 生成。发送前不要改动，回收后把回答贴到本文件末尾。

```
你上一轮说的 a1 faas 和 autopilot webhook 我都收到了，很有价值。先明确我的判断：借用你的 token 这条路我不走，你拒绝是对的——建立在借来凭证上的产品随时会瘫，而且性质上就是业务侧绕权限，一旦被发现比慢几周严重得多。我要的是我自己的合法服务身份。

所以我的目标形态定了：把产品做成一个正规的公司内部应用（走 a1 faas / AoneFaaS），它有自己的服务身份、自己申请数据源权限、接公司统一登录给多个同事用。执行动作仍然落到你们已有的原子能力上，我做的是决策层。

以下请**实际跑命令查证**再回答，贴命令和原始输出（凭证值打码但保留字段名）。查不到或无权限就直接说，这个答案同样有价值。

一、FaaS 到底能承载什么样的应用
1. a1 faas template list 和 a1 faas template list --scope aone-platform 的完整输出，支持哪些运行时（Node/Java/Python/Go）和框架。
2. 它是"请求触发的函数"还是能跑常驻服务？有没有冷启动？单次请求超时上限是多少？能不能跑几分钟的长任务（我要定时批量拉数据做诊断）。
3. 有没有定时触发（cron）能力？怎么配。
4. 一个 FaaS 应用能不能对外提供多个 HTTP 路由（相当于一个完整后端 API），还是只有单一入口。
5. ACCESS_URL 是什么形态？内网域名还是公网？能不能绑自定义域名？公司内其他同事能不能直接用浏览器访问？

二、数据存哪（没有库就没有产品）
6. 有没有配套的数据库可申请（RDS/MySQL、PolarDB、Tair/Redis、表格存储）？申请流程是什么、找谁、多久？
7. FaaS 应用怎么安全地拿数据库连接凭证——有没有配置中心/密钥管理（a1 faas config vars 是不是就是干这个的，值加密吗）？
8. 有没有对象存储可用（存报表、导出文件、截图）？
9. 有没有更轻的方案能让我先跑起来（比如平台自带的轻量存储），不用等数据库审批？

三、前端怎么托管（这是我的核心差异化，必须能做得好看）
10. FaaS 能不能托管前端（Next.js SSR 或纯静态 SPA）？还是只能后端函数、前端要另找地方部署？
11. 如果不能，公司内部有没有前端静态站点托管服务（类似 OSS+CDN 或 aone 的静态部署）？怎么申请。
12. 内部应用能不能用公网 CDN 资源（字体、图表库）？还是必须全部本地化。

四、多用户登录（我不想自己造账号体系）
13. 内部应用怎么接公司统一登录（BUC / 域账号 / SSO）？有没有现成的中间件或 SDK，能不能给我文档地址或示例代码？
14. 登录后我能拿到用户的哪些信息——工号、姓名、部门、汇报关系（上下级）？有没有组织架构查询接口？我要用它做数据可见范围和任务分发。
15. 有没有现成的权限/角色管理服务可用，还是要自己在应用里实现。

五、数据权限（业务价值的地基，最关键）
16. 一个内部 FaaS 应用要拿**快手广告数据的读权限**，正规路径是什么：以应用身份申请 MAPI 权限？还是通过内部数据服务代理？找哪个团队、走什么审批流、大概多久？
17. FaaS 应用能不能访问外部公网 API（快手 MAPI 在公网）？出网策略是白名单制吗、怎么申请？
18. **FBI 平台 / BI 真实转化数据**：应用身份能不能申请查询权限？走什么接口（FBI 开放接口？dataworks？数据服务 API？）？我这种业务侧发起的申请通常能不能批下来？
19. 能查 BI 真实转化的那些 skill，底层实际调的是什么服务和接口地址（不用给凭证，给服务名和接口形态就行）？我想判断能不能让我的应用直连同一个服务。
20. 内部有没有已经沉淀好的**投放数据服务/数据集市**，业务侧应用可以直接申请接入，而不用自己对接每个媒体？

六、能不能不重复造轮子
21. Multica 的 issue 能不能被外部应用当作"任务系统"复用——我的产品要做任务分发（上级看到问题派给下级、下级处理回执）。外部应用能不能通过 API 创建/查询/更新 issue，还是只有沙箱内的 agent 能操作？
22. 这套体系里现在有没有已经做成看板/报表/日报形态的东西？谁做的、覆盖哪些渠道、业务侧能不能直接用？
23. 有没有已经在跑的自动化诊断或自动调控（定时预警、自动改价）？规则是什么、谁在用、效果如何？
24. **部门里有没有业务侧（非技术部门）的人自己建过内部应用**的先例？做的是什么、怎么拿到权限的、有没有踩坑经验可参考？

七、生产环境与流程
25. a1 faas deploy 只发日常环境（daily）。要发到生产（能给部门同事长期使用），完整路径是什么？需要什么权限、什么审批、有没有安全评审/发布窗口？
26. 建 aone-platform 应用会新建代码仓库和应用登记——这个动作需要什么前置（比如所属团队、成本中心、负责人审批）？会不会需要我的主管或技术部门同意？
27. 如果我先在 daily 环境做出可用版本给自己和几个同事试用，这在流程上合规吗？有没有人数或时长限制？

请按编号回答。最需要的五个答案是：16、18、13、10、6。
```

## 回收（待贴）
