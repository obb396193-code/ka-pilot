# R013 首次初始化与发现实施计划

目标：按inbox-codex R013修订及P044顺序，提供无默认账号、无默认授权的首次部署路径。当前be/r010@c469f70，012为PG待验候选；不把初始化当作已能上线。

## 分批路径与反例

1. Domain `bootstrap-seed.ts` + test + index：严格接收冻结的identities/workspaces/memberships/grants形状，所有层拒绝未知字段与密码/token/secret，有限数组；角色复用AuthRole，不发明readonly角色。personal/team授权由现有Auth判别式决定。完全相同重复输入可幂等，冲突同键拒绝。没有JSON不创建任何东西。
2. DB `bootstrap-seed-repository.ts` + CLI +真实PG测试：单事务、受控锁顺序/并发幂等、不更新现有行；解析用户与membership，拒绝两个personal或personal共享；grants只指显式账户，缺workspace整批回滚。DB FK要求账户先存在，首次新账户如何以已确认的grants建立最小主表行需在实现前对照arch边界并留痕。可选workspace/user ID要有可重复解析方案，不能每次重放随机新建。
3. Worker `discover-accounts.ts`：复用现有启航account资源和分页，服务端WORKER_SERVICE_QIHANG_USER_ID；stdout只输出指定五个字段，缺配置/坏值/截断不能假空成功；不写DB、不发job。只跑假Transport测试，不接真实凭证。
4. 独立coefficients命令：必须指定workspace和明确effective_date，四渠道系数/op按冻结值；同键冲突拒绝、不覆盖历史；不得擅填生效日。bootstrap不依赖012，coefficients依赖op。
5. fixtures/runbook/package scripts + PG/HTTP登录/切team联测；例子身份按最新R013b指令处理，但所有测试始终假数据，凭证不入文件。没有收到真实密码/启航身份，不宣称首次内网可用。

## 门禁

每子批先红后绿、代码与留痕独立提交；Domain/DB/Worker相应全量与type/lint，核心覆盖率>80%；真实PG恢复后再做空库migrate→seed→login/session/switch、重复与并发、回滚、空scope sync拒绝。当前PG拒连，不省略或伪报。媒体写/默认execute授权关闭，不改Auth行为或前端视觉。

## 2026-09-06 子批1实际交付

- `36c5c35`：Domain严格输入，37项反例通过、Domain全量555、typecheck/lint、核心覆盖100%。最初缺模块使套件加载失败，不声称37个断言均先跑红。
- 未接CLI/DB，不宣称seed命令已可用。引用其他已存在对象由Repository事务校验；Domain不强制所有对象都必须重复填入输入。
- 只读核实main已到abaac1d，新增账户池偏差提案/前端次序，没有改本批seed契约；尚未merge，不动未裁决账户池字段。
- 待arch确认实现缝隙：①seed显式grants的新账户是否允许先插仅三键、其他未知字段NULL的accounts占位（否则FK与先授权后同步形成闭环）；②新identity使用internal_test、provider_subject=id（现登录按identityId），不改已有provider；③省略workspace.id按kind+name在受控事务唯一匹配、歧义拒绝，省略user_id按membership唯一复用，否则新建；更推荐样例都写明确UUID。确认前不落这些DB语义；先继续独立discover命令。

## 子批2 只读discover交付

- `7c08b97`：Worker复用QihangClient account GET，严格服务端配置和media参数、50/页、稳定total/行数/页码/去重/媒体边界，完整成功才一次输出五字段JSON。不写DB/发job/授权，不依赖bootstrap裁决。
- 37测试含实际无配置CLI退出反例；Worker非PG761+2外部opt-in skipped，typecheck/lint与offline production audit0，核心行93.61%/分支89.15%。首轮完整回归因listen EPERM有100失败；申请允许本机假HTTP后同套件全绿，不按跳过处理。
- PG于02:37获准只读TCP探测仍ECONNREFUSED；没有调用真实启航。无秘密/前端视觉变更，未push/合流/部署；命令参数与stdout使用说明进唯一runbook §2.5。
- 下一步独立coefficients输入/幂等内核（要求显式有效日期）；bootstrap事务先等P047三项arch裁决，不等待数据库来做纯逻辑。

## 子批3 — P047采纳后Bootstrap DB/CLI（2026-09-06）

- b4c57d1已将arch@a7f98a8合到后端，信箱冲突只是双方末尾追加，全部保留。P047三项已采纳，不再待裁；新行internal_test/subject=id；显式grant账户占位只三键+status=NULL（绕开旧active默认，其他经营列不碰，未来015 pool_status默认available）；可选workspace按kind+name、可选actor按membership解析，歧义拒绝。
- 逐文件：packages/db/src/bootstrap-seed-repository.ts（单SERIALIZABLE事务+bootstrap专用advisory锁+identity/workspace行锁，最多3次40001回滚重试），src/seed-bootstrap.ts（一个严格JSON参数、输入先验后open DB、无默认账号）、index.ts/package.json；test/bootstrap-seed-repository.unit.test.ts +真实PGtest/bootstrap-seed-repository.test.ts、test/seed-bootstrap.unit.test.ts；fixtures/seed/bootstrap.example.json与runbook。
- 只insert，existing同scope/role主动验证、不更新name/provider/role/active/grants；停用身份/空间/actor/member拒绝，team grants不接受（团队只靠membership只读）；已有read/preview/execute grant不变，新grant显式read。
- 所有seed事务持同一个有界advisory锁防并发可选name创建；SERIALIZABLE防与非seed身份变更交错；错误整批rollback并固定文本。personal同identity最多一个active、同workspace最多一个active成员，检查LIMIT2，不全量materialize。所有外来字符串参数化，输出计数和已解析UUID，不输出provider_subject/秘密。
- PG反例：空库seed→现Auth issue/resolve/switch；同input重放/并发、跨媒体同号、跨workspaceactor误绑、两个personal、共享personal、歧义名称、停用链、缺workspace/grant整批回滚；空grant保留scope空。实际PG不可用时只称测试已备，不执行真实seed。

### 最终证据（03:50，覆盖此前pg_blocked快照）

- c7358fe，8文件；重试40001/23505只在rollback成功后、最多3次；新增1001st grant整批拒绝。示例改到docs/evidence/proposals/seed，由arch落Contract。
- 独立合成库ka_be_r013_20260906从001–012回放；原ka_r009_test双011历史完整保留。DB46文件331、Domain48文件587、Gateway8文件36、Web111；Worker90文件794+2外部skip，排除写死共享ka库benchmark。五包typecheck/lint通过；日志/tmp/ka-bootstrap-{db-final,domain-full,worker-pg-full,gateway-full}.log和/tmp/ka-r010-web-regression.log。
- 新DB/CLI37（12PG+14unit+11CLI），行96.34%/分支95.58%，Repository99.25%/CLI82.75%。Worker2个真实CLI→PG→HTTP反例：错密码401、personal登录、switch team readonly、旧cookie401、DELETE登出后401；空grant同步blocked_auth，不启消费者。
- 首轮PG42P08为同$1参数uuid/text类型推断，明确cast修复；两断言误用allowedAccounts改为冻结scope.accounts；HTTP误用POST logout导致404，改测试为现有DELETE；遗漏四个Service造成tsc失败，补真实composition依赖后全绿。失败保留，不用跳过抵消。
- 012真实PG4例（含up/down/up/事务/隔离/拒有损down），另外十例callback是纯逻辑；系数PG7+settingsPG7=14通过，包含op版本/未来版本排除/跨媒体/跨workspace。尚无真实启航/OS部署；benchmark单独安全收口。
