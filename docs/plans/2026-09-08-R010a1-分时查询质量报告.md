# P139 分时查询纵切片质量报告

代码 `b6fb474`；基线已包含 main@0d358d8，交审前 `git merge main` 返回 already up to date。仅本线代码、非视觉BFF适配；Contract、be2模块及六个保护文件0diff。不push、不部署、不接真实媒体。

## 现状

- account.hourly 进入严格Registry、Canonical行/版本、Session→tuple范围→Service→HTTP/BFF。hour0..24、单日参数，可信adapter独立workspace证明，输出再核账户/媒体/小时/覆盖与total；保护查询brand和授权快照不被provider变更。
- source.dataAsOf→meta.dataAsOf，未知保留null；不以响应时钟代替。无Provider实际启动503 SOURCE_UNAVAILABLE，非法授权账户先403，无日表fallback。team无个人hourly出口。
- 可信total且10k完整可通过；无可信total或truncated拒绝。HTTP/BFF exact16MiB保持拒绝；requestId/body/source日期对应。
- 真实小时reader未接，故这是“安全准入+诚实source-off”，不是可用分时数据页面。尚未验证OS账户级hh；已知广告级hh不能证明账户级资源同能力。

## 实跑门禁

日志 `output/qa/p139/`（不入仓、保留原失败日志）。

|命令/范围|结果|
|---|---|
|Domain `vitest run --maxWorkers=1 --minWorkers=1`|74文件1080/1080|
|Worker hourly-public-query + query-registry + session-http|3文件56/56；其中hourly21项含10k与真实BFF→HTTP合成source|
|Worker八文件HTTP回归|154/154：data-api、三列表、Session、dry-run、mute、r010|
|实际spawn data-api→真实DB Session/本人合成PG库|1/1；hourly503/越权403，原pivot/mute/ignore/team/logout回归|
|Web node全量|193/193；新增hourly metadata/requestId/exact16MiB|
|Domain/Worker typecheck+lint、DB typecheck|通过|
|Web本批定向tsconfig/eslint|通过；全Web类型前次失败为旧UI缺依赖，本批不报全Web类型绿|
|hourly-public-source覆盖|行100%、分支89.47%、函数100%|
|Worker npm audit --omit=dev --offline|缓存库0 vulnerabilities，不代表在线情报刷新|
|git diff --check/保护路径/人工安全审查|通过；无真实值/密钥/新SQL/媒体执行|

首次Domain枚举测试仍期待8个query、Worker测试窄化类型、Web mock参数缺dataView等已修，最终结果如表，不隐藏早期红。新schema和12类恶意Envelope双侧永久parity，不只手抄。使用code-quality-checker检查覆盖、安全/复杂度与scope失败分支。

## 未过门及向arch反馈

- 磁盘6.6GiB，仍低于本线8GiB全量运行门槛：未跑DB/Worker全量、Next build/实际浏览器。仅本人合成库的真实PG，不代替真实数据联调。
- `fixtures/data-query/hourly.json` 的 source.partial=true/coverage.complete=false 却wholeResultTotal.available；meta.dataAsOf=09:15 与source=08:00不一致，meta内还有说明字段。没有改arch fixture或放宽线上schema；测试借其冻结row，另构造明确synthetic自洽envelope。请arch修fixture。
- Gap fixture也有meta/source时钟不一致；后续独立批接groupBy/ruleSetVersion。规则有效版本和preDeduction源不猜。总信箱目标继续active，当前候选未标merged/deployed。
