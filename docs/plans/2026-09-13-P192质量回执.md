# P-192 质量回执

## 范围与状态

- 权威：本机 main `94e9dc25`，契约 v1.9.44；工作分支 `be/r010`。
- 代码 SHA：`ae171957`；BFF 测试路径修复独立 SHA：`a91523e3`。
- 七个新增延后入口 + 既有 review 共八个 501；订阅 test-send、on-call 留给 P199 真做，没有挂假成功/501。
- 七个 Next BFF 路由、鉴权后的 Worker 注册、稳定错误回包；未实现停止测试、升档、复刻等实际业务。
- 无 DB/Agent/媒体操作，无钉钉发送，无依赖清单/锁文件变更，无视觉改动，无 push。
- `data/http-server.ts` 仅注册/dispatch，未改 be2 数据查询逻辑。合并提交的 be2/fe 文件不属于本批作者 diff。

## TDD 与门禁

| 检查 | 实际结果 |
|---|---|
| Worker HTTP 红→绿 | 初始七条路径未注册返回404；最小实现后22/22绿；既有review测试补真实R014注册后绿 |
| Worker BFF覆盖 | 3/3绿 |
| Web 新增 BFF | 10/10绿：固定path/method、501、身份不自报、拒伪2xx/204、错误形/requestId、exact16MB/timeout |
| Domain full | 首轮1578过/1超时；同代码降低并发 `--maxWorkers=1` 后101文件/1579过；typecheck/lint绿 |
| DB full / 真实PG | 159文件/1781过；typecheck/lint绿；隔离合成库 `ka_be_r010_p194_test` |
| Worker full | 205文件/2374过，2条外部凭证opt-in跳过；含真实PG+HTTP；typecheck/lint绿 |
| Web full tests | 308/308过 |
| Web lint | 0 errors / 18 warnings（既有警告，不改视觉文件） |
| Web typecheck | **未通过**：本工作树缺主线新增依赖（ai/shiki/streamdown等），共85条缺模块/连带类型错误；离线补依赖 ENOTCACHED，未修改清单/锁文件、未绕过门禁 |
| diff检查 | `git diff --check`/cached均绿；未跟踪源文件0 |

日志在本工作树 `output/p192-{domain-full-r2,db-full,worker-full,web-full-r3,web-lint,web-typecheck,web-deps,coverage}.log`，未把运行产物混入源码提交。

## 自审（code-quality-checker）

- 安全：七条路由固定白名单；internal bearer/session与viewer防线先执行；body不解析不回显；无SQL/外部API/凭证访问。BFF只取服务器配置与session，不传浏览器账户范围。未发现本批新增高危问题。
- 性能：固定七项匹配，忽略body时不物化大JSON；没有数据库调用或循环工作。共享forwarder仍负责请求/响应大小边界。本批未跑生产负载测试。
- 契约：按冻结方法派发，错误方法405、额外query400、未登录401；501沿用稳定error envelope+requestId/no-store。任何意外2xx/204均不可显示成已操作成功。
- 覆盖：有明确正常/权限/错误方法/跨空间类型/上游错误形反例；V8定向测新增 `deferred-action-routes.ts` 的statements/branches/functions/lines均100%，只涵盖该文件，不宣称全系统覆盖率。
- 依赖：package/lock零diff；本批未取得新的全仓依赖审计结果，不称audit全绿。
- BFF门禁路径修复独立SHA `a91523e3`：两个测试把URL.pathname误当文件路径，中文目录变%编码；改用fileURLToPath。不涉及UI或业务契约。

## 后续

- arch仍需exact SHA独立验收、在完整前端依赖环境补typecheck；没有称已合流/部署/内网验证。
- P198：远端已接受而本地未写sent_at的故障窗口，以及重试/claim持久化字段与DM目标解析，已在inbox-arch提出；未获裁决前不实现可能重复发送的重试。
- P193已只读核查：现成员列表走旧全局provisioning.read，新契约要求workspace-local；后续实现时必须一并收窄，不能只补PATCH/PUT。
