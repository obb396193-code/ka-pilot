# P175 Worker安全诊断质量回执

## 交付与范围

- 代码 **2d836d80**（18文件）；最终验证基线 **c5d22aec**，含main **e46783a0**。本人be/r010候选，未push、未arch合流、未OS部署。
- F-OS-003：child→strict IPC→supervisor→CLI/HTTP内部日志用白名单阶段码，不修改公开HTTP envelope；仍等实际child close，不把kill失败当退出。
- `worker:diagnose`只读RR/RO统计，固定workspace/media范围、5s查询超时、1连接；不调度、不变更jobs、不读取输出凭证值。实际PG前后jobs全等。
- HTTP child额外传受控CA路径，禁止全量env传递和禁用TLS校验。真实OS TLS未验证。

## 实跑命令与结果

仅合成测试库 `ka_be_p158_20260909_test`（localhost:55432），未连接业务数据库/媒体。最终两个会话均exit0：

```sh
# apps/worker；TEST_DATABASE_URL 指向上述专用测试库
npx --no-install vitest run worker-once --hookTimeout=30000 --coverage --coverage.include=src/scheduling/worker-once-failure.ts --coverage.include=src/scheduling/worker-once-supervisor.ts --coverage.include=src/scheduling/worker-once-diagnose.ts
npm run typecheck
npm run lint
npm audit --omit=dev --offline
# packages/db；同上测试库
npx --no-install vitest run test/worker-once-diagnostics.test.ts test/worker-once-diagnostics.unit.test.ts --hookTimeout=30000 --coverage --coverage.include=src/worker-once-diagnostics-repository.ts
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

| 门禁 | 实际结果 |
|---|---|
| Worker单轮全部回归 | 13文件82/82，含真实CLI/HTTP/PG/锁/重试/身份恢复 |
| DB新诊断回归 | 2文件10/10，3真实PG + 7unit |
| Worker覆盖限定三个模块 | 行96.92%，分支90.69% |
| DB诊断模块覆盖 | 行100%，分支97.91% |
| 两包typecheck/lint | 均exit0 |
| 两包production离线audit | 0 vulnerabilities，基于本机缓存，非在线最新数据库 |

Task1与CA测试有先红后绿；Task2诊断是实现后补回归，不声称全批严格TDD。

## 安全/性能/边界复核

- 未增加依赖/lock修改。typed错误不能被原error.message/cause/stack或结构相似对象替代；实测私有DB名/DSN不出stderr。
- 真实spawn诊断命令只证明DB可读及空间状态；JSON明确奇航网络和上游数据not_checked。无workspace返回exists=false而非假ready。
- 统计SQL全参数化，ETL type/media/workspace限制，RR只读；非法数值/分区计数/metadata拒绝；事务失败rollback/release，坏连接销毁。
- 保留原同空间进程锁、硬期限、断IPC终止、失效租约可恢复、跨空间/媒体/媒体写任务不消费。恢复状态没有因诊断新增修改路径。
- 查询只有固定数量聚合，输出固定大小；底层扫描量取决于实际workspace jobs规模，不能承诺无限历史队列常数成本。

## 未完成/限制

- 磁盘实测约6.9–7.7GiB，低于arch全包门禁8GiB，未跑Domain/DB/Worker全包；不能用P174/旧门禁数字冒充本批全绿。请arch独立全包验收。
- F-OS-001③逐批容错/tuple-day missing、F-BI-001/002等仍待后续；v1.9.8已明确语义，不再等待同一裁决。
- BUC/Pod/M0新源仍按信箱条件等待，未接真实凭证。图片按老板取消。全信箱目标仍active。
