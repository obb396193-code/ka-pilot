# P180 授权谓词自查 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 执行arch新P-178（非本人旧测试编号P178），逐查询审计r010读权限，优先验证业务出口并收敛重复谓词；不把仓储内部维护查询误报成浏览器越权。

**Architecture:** 基线同步main b0351577（95e46310）；复用r014/workspace-authority的accountScopeClause。保持现有Session认证、tuple shape、空grant拒绝扩大和expected missing。任务级工作项与旧个人无账户项冲突独立报arch，不擅自删除旧安全边界或团队放宽。

**Tech Stack:** PostgreSQL真实合成跨空间/跨媒体反例、Vitest、现有Semantic/WindowAssessment仓储与业务Service调用链。

---

1. 只读盘点所有指定表SQL及public/internal caller，逐条记“已核/待核/语义冲突”，不以rg命中当完成。
2. `semantic-query-support.ts`与`semantic-query-health.ts`的两个重复tuple谓词接共享accountScopeClause；空scope、当前workspace过滤不变，不改公开DTO。
3. 新`packages/db/test/semantic-authority-regression.test.ts`真实PG：同ID跨媒体/空间、空grant、撤销后下一请求scope变化；summary/trend/dimension/health/WindowAssessment同时核。负对照在测试query wrapper摘掉共同谓词，证明泄露确会出现，不能只测空库。
4. 定向PG、三包type/lint/audit、限定覆盖与质量回执；按文件/调用点列审计进度。本批不谎称全域查完；随后继续work-item/changeset/alert边界再F-P179 endpoint。

执行子技能不可用，按持续授权本人执行。writing-plans负责步骤留痕，code-quality-checker负责提交前检测。不派子agent、不push、不生图、不做媒体写。
