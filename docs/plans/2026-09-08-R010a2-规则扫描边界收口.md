# R010a2 规则扫描边界收口 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 子技能不可用；按老板连续执行授权，本地TDD，不开其他平台/子会话。

**Goal:** 消除规则扫描的原始异常泄漏、评估器可改写写入tuple、非法sink结果继续通知问题，为后续真实Provider接入提供可验边界。
**Architecture:** 保持既有RuleScanSummary/三规则/工作项及通知port；新增内部strict输出校验；复制候选给评估器，失败只返回固定消息。不是新权限系统，批准账户scope仍必须由生产Provider/Sink保证。
**Tech Stack:** TypeScript、Zod、Vitest。

### Task 1 TDD

- 修改 `apps/worker/test/rule-scan-handler.test.ts` 原始错误断言为安全固定消息；新增 `test/rule-scan-boundary.test.ts`。
- RED复现：Provider报错泄漏、evaluator改workspace/media/accountId后下游写错、非法severity/outcome/trace/disposition/id继续进入通知。
- 同时测试Provider持有的原数组不被修改、部分失败仍处理其他项、全部失败安全抛错。

### Task 2 实现

- `src/rules/rule-scan-handler.ts`：固定安全错误、受控workspace mismatch，克隆Provider结果/给evaluator独立副本；输出schema严格限长/枚举，非法输出无下游副作用。
- 新 `src/rules/scan-boundary.ts`：内部schema，不修改Contract。不伪造执行scope、不开放媒体/队列。
- 失败/异步能力输出保持当前摘要语义，不将异常视为正常命中。

### Task 3 质量门禁和交审

- Worker定向回归、typecheck/lint、coverage、安全扫描/缓存audit；按机器容量运行全量，不虚报PG。
- 实读发现：runtime没有RuleScan注册，也没有生产RuleCandidateProvider；schema未见独立规则检查/静音抑制计数表，只有work_items.occurrence_count。
- 写P132给arch：需要真实Provider+sink及覆盖流水持久化设计，不把纯handler通过当每天跑扫描；不借用任意JSON列存检查结果。013其他类型冲突仍保留。
