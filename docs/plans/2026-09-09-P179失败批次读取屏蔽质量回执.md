# P179 失败批次读取屏蔽质量回执

## 交付与状态

- 代码 **c11dde2d**，10文件、219增10删；实现P176 Task2读取侧，不代表F-OS-001③全部完成。
- 新共享谓词 `packages/db/src/etl-batch-readability.ts`：失败tuple-day由ledger判定，不删历史；Raw同资源成功采样晚于failedAt才解屏蔽；Canonical还要求采样不晚于computed_at。
- 接Raw loadMergeInputs、Semantic table/count/lineage、health coverage、EXPECTED_METRIC_CTE的LEFT JOIN ON。summary/trend/WindowAssessment/Dimension保留expected missing日，不把成功账户小计冒充全量。
- 私有failure evidence/record input增加ad过滤范围hh/adIds，公开BATCH_FAILED warning不变。相同fingerprint不同过滤事实冲突；其他资源/空间/媒体/日期/小时不足/广告子集不足均不能解除失败。
- **尚未合流、部署或内网实证；不push，不改前端，不开媒体写。** 生图停止，保留旧候选。

## TDD与真实证据

先新增4个真实PG红例：前三例旧Canonical仍cost20/rowCount2而非missing，第四例私有ad filters尚不支持。实现过程中修了SQL JSONB运算符分组错误；公开测试第一次误断言不存在的coverage account-day字段，按既有Contract改为requestedObjects/returnedObjects，同时单独核内部lineage的account-day计数，没有改Contract凑测试。

| 最终门禁 | 结果 |
|---|---|
| Domain failure contracts | 19/19 |
| DB failure ledger PG + reader PG + ledger unit | 39/39（27真PG、12unit） |
| DB SemanticQuery / WindowAssessment / Raw旧回归 | 31/31，真PG |
| Worker公开来源/window/RR快照/failure mapper | 69/69，其中窗口PG9、快照PG2 |
| Domain/DB/Worker typecheck + lint | 全部exit0 |
| 三包production audit --offline | 0，缓存结果，不声称联网最新审计 |
| v8限定覆盖 | Domain100%行/90%分支；DB两模块100%行/97.36%分支；新SQL谓词100%行/分支 |
| git diff/show --check、前端范围检查 | 通过；apps/web与ui-layout-demo零diff |

独占库`ka_ci_be_r010_p178_test`，本机55432；本批新测试随机workspace定点清理，未动arch/be2库。旧semantic测试会保留合成workspace，本人库仍专用。最终观测磁盘7.7GiB，仍低于arch规定8GiB全包门，因此**仅定向门禁，不宣称全包通过**，未清缓存。

### 可复跑命令

先设置TEST_DATABASE_URL为本人独占合成库，分别进入对应包：

```sh
# packages/domain
npm test -- --run test/etl-batch-failure.test.ts --coverage --coverage.include=src/etl-batch-failure.ts --coverage.reporter=text
# packages/db
npm test -- --run test/etl-batch-readability.test.ts test/etl-batch-failure-repository.test.ts test/etl-batch-failure-repository.unit.test.ts --coverage --coverage.include=src/etl-batch-readability.ts --coverage.include=src/etl-batch-failure-repository.ts --coverage.reporter=text
npm test -- --run test/semantic-query-repository.test.ts test/window-assessment-repository.test.ts test/raw-metrics-repository.test.ts
# apps/worker
npm test -- --run test/platform-window-query-pg.integration.test.ts test/platform-data-source.test.ts test/platform-window-query.test.ts test/platform-read-snapshot-pg.integration.test.ts test/etl-batch-failure.test.ts
# 每包分别
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

## 质量自查与待收口

- 安全：SQL仅插入代码控制且验证的alias；对象条件都比较workspace/media/account/day，不用用户身份作为SQL文字。未新增凭证、错误body、真实业务样本。测试证明跨空间/同号跨媒体不受影响；坏ledger不返回ready。
- 复杂度：共享SQL37行；无逐对象HTTP请求。相关子查询会扫描本workspace失败历史，费用随canonical候选行、失败数和Raw历史增长，**未做大规模性能基准**；上线启用容错前需要EXPLAIN/历史量验证。现有日期/account scope边界保持。
- 成功解屏蔽依赖Raw恢复证据；未来Raw保留期/清理必须与失败ledger归档或持久恢复标记协调，否则宁可再次missing，不得删证据后假装完整。本批未增加清理任务。
- 未完成：canonical加载/写入并发竞态防线、full/incr逐批继续与all-failed语义、readiness、BATCH_FAILED公开warning贯通。**Full/Incr runtime仍遇耗尽失败整个job；没有现在就启用半成功。**
- be2所有的账户/任务列表直接SQL、详情/日报等衍生读取需要arch协调接同一屏蔽谓词。个人普通数据summary/table已证；team ingestion不在本批，不能把本批当团队snapshot原子发布证明。
- 后续继续P176 Task3，之后BI001/002、F-OS-004（020仓储依赖已解除）、021/hourly、Gap、dimension source；BUC/Pod仍条件待办。全信箱目标保持active，不以本批替代。
