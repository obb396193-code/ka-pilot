# F-P139-1 日历排序质量报告

代码 **7697a67**，main@64d9ed9已合。候选待arch exact复验，未合流/部署/push。无前端、契约、DDL、媒体写改动。

## 根因与TDD

原SQL `SELECT id::text AS id ... ORDER BY ... id DESC` 使用输出文本别名排序。ID位数跨界时，字典序与Domain要求的数字降序不同，合法数据被严格契约拒为502。这解释了先前同库重跑偶尔通过：不跨位数的ID区间可能恰好同序；不是去掉10000限制或放宽校验能解决的问题。

新增真实PG→Session→HTTP反例：两条同日、跨10^14位数的合成安全整数ID，旧SQL稳定502 `UPSTREAM_INVALID_RESPONSE`；修后200且较大ID在前。显式ID仅该workspace两行，不改全局sequence，finally限定清理。

修复只把ORDER BY限定为原表 `business_calendar.event_date DESC,business_calendar.id DESC`。仍文本传输/安全整数验证；SQL LIMIT10001及10000上限不变。纯逻辑永久锁定原列排序。

## 本轮门禁

在各包执行 `npm test -- --run <下列文件> --maxWorkers=1`，PG仅本后端55432合成测试库：

- Worker `admin-calendar-http-pg.integration.test.ts`：8/8真实PG（含跨位数、exact10000成功/10001截断、权限/轮换/退出/非法字段）。
- Worker `admin-calendar-http.test.ts`：22/22。
- DB `admin-calendar-repository.unit.test.ts`：14/14，`--coverage --coverage.include=src/admin-calendar-repository.ts` 行/分支/函数100%。
- Domain `admin-calendar.test.ts`：13/13。
- 总计57/57。DB typecheck/lint通过；diff --check通过。无依赖变更，前一相同依赖快照P152离线audit0，不声称新的联网审计。
- 原始红绿证据：`output/qa/p152/calendar-{red,green,db,domain}.log`。目录沿用P152但本次代码独立P153。

## 未通过与阻断

Worker `npm run typecheck`退出2：account-list-http.test.ts:37、account-list-service.test.ts:32缺poolStatus/poolStatusSource/productName/productRef等6字段；task-list-http.test.ts:40、task-list-service.test.ts:31缺stage/stageSource/stageChangedAt/sopRunId等6字段。来自新合入be2 RepositoryRow required字段，而旧测试fixture未跟进，非日历文件错误。未替be2改移交服务或放宽其契约；请arch分派收口。因&&，Worker整包lint未执行，不冒称全绿。

磁盘最低321MiB，低于8GiB门槛；完整Domain/DB/Worker回归不执行，未清缓存或改其他工作树。此前P150/151门禁只是当时快照，本批不沿用为新主线证明。
