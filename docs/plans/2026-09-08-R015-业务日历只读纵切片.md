# Admin calendar read Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 让管理员通过真实API/BFF读取当前workspace业务日历，不依赖014/016。

**Architecture:** 冻结admin/calendar fixture→独立strict Domain→RR/RO参数化DB→Service scope校验→主HTTP Session+bearer→非视觉BFF。只开放GET，POST/DELETE留后续；缺表时间元数据返回dataAsOf:null；个人/团队均要求approved role=admin，只读本空间，不读取grants。

**Tech Stack:** Zod、pg、Node HTTP、Next BFF、Vitest/node:test。

---

1. Domain `admin-calendar.ts` 和test：字段/日历日期/安全整数ID/重复/顺序/严格错误、权威fixture；先RED。
2. DB `admin-calendar-repository.ts` 和unit/PG test：scope SQL、10001哨兵、非法/过大字段不静默缺省、真实RR/RO与跨空间反例。
3. Worker `admin/calendar-service.ts`、`r010/admin-calendar-route.ts`、主http/data-api接线：只GET、admin前置、Session撤销/伪造scope无效、错误与exact16MiB。
4. Web新 `lib/data/admin-calendar-bff.ts` + server-only包装 + `/api/internal/admin/calendar/route.ts`；直接导入独立Domain schema，不手抄DTO；定向node:test。
5. 写P150质量回执和状态，路径限定提交、不push。按writing-plans/代码质检执行，未安装executing-plans则自行TDD；不上传Coze。磁盘<8GiB不跑全量，准确注明定向证据；整个R015/总信箱不因本GET完成。
