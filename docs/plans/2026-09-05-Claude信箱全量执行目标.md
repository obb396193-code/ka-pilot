# Claude Backend Mailbox Completion Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将Claude/arch信箱中本后端负责的有效未完成项逐条实现、验证、提交和交接；不冒充独立验收或部署。

**Architecture:** 从当前be/r009继续；2026-09-06已通过11faf95同步arch main@f841da4，不再跟integration-control。每子批独立代码SHA和留痕SHA，未冻结项先给arch提案，继续其余不依赖项。完成以逐项对账为准，不把blocked/unavailable记为功能完成。

**Tech Stack:** Domain TypeScript/Zod、PostgreSQL migrations/repositories、Worker HTTP/CLI、Next API BFF（非视觉）、既有Agent与钉钉内核。

执行模式：老板2026-09-05明确设置持续目标并授权把Claude派活全部做完；executing-plans技能当前不可用，由本会话依步骤执行，不另派写共享文件的agent。代码质量按code-quality-checker；不调用外部Coze上传公司工程。

## 对账与依赖

| 派工 | 当前真实状态 | 要交付的剩余内容 / 依赖 |
|---|---|---|
| R001–R008 | 信箱已完成 | 不重复实现；历史审核范围沿用arch记录 |
| R009#1/12/9 | 第一批已被arch审过；85ea1bf已折011 | Session BFF/hh完成；折合包的PG回放尚待，不能引用旧双011库证据 |
| R009#3/6/7 | P039/P041已审；未整批合流 | 保持任务排斥/双主体/Backfill DAG，批末全回归 |
| R009#4/5 | main8c240f7已通过P042/P043；9715125补齐新dead冻结 | 不重写已交SHA；增量待审，未整批合流 |
| R009#2 | Domain/SQL/报告/双Adapter/v2/BFF代码与非PG门禁完成 | 最新3b76ed3补最终Service截断掩码；全链PG/arch最终验收尚待 |
| R009#8/10/13 | 路由+BFF+绑定team reader已实现，未整批验收 | ordinary拒dataView、admin独立entitlement、双空间PG代码已补但55432拒连；team anomalies缺规则语义已问arch，其余四类KA模板有真实本机SQLite执行证据 |
| R013 | 待处理；顺序已获arch明确 | 012先迁移；seed拆bootstrap（不依赖012）/coefficients（依赖op），discover后显式grants，生效日期不自造 |
| R010a1 | 待R009 | migration012 + 语义查询、系统只读、账户详情+BFF；v1.4.1窗口和现金口径/系数op随此批 |
| R010a2 | 待a1 | 工作项动作/coverage、变更集、任务详情/写、账户其余；只能实现批准的产品路由，不自动实发媒体 |
| R011 | 待a、且先提案后冻结 | 团队KA Data staging/version/publish，013；旧source-neutral草稿不能原样复用 |
| R012 | 待R011，素材/结算先提案 | migration014、警报/派发/账户闭环/知识库/卡片/订阅/8维/口径设置+BFF；未获冻结的素材结算DTO不自造 |
| R010b | 按信箱顺序在R012之后 | 日报、Agent SSE、rerun/search、Capability Registry |
| R013b | main@cda3303新派，随R013 | 安装/打包顺序、FaaS三进程骨架、worker:once、production拒KA_DATA_DEV_*、30天session清理；不接真实凭证 |
| R014 | main@cda3303新派，R012后 | migration015、小传/小时gap/transfer/watchlist/views/export/integrations/capabilities/decision/dailybrief与BFF；按v1.5逐项实施 |

## 固定每个子批的步骤

1. 读最新main信箱/Contract相应段，检查当前branch/HEAD/dirty与arch新回执；只改自己的工作树。
2. 写相应子批逐文件计划与预期反例；先运行失败测试，工具/环境失败不算业务红灯。
3. 最小实现；未知来源返回明确missing/error/unavailable，不伪造数据、身份或数据新鲜度。
4. 定向+真实PG；依风险跑Domain/DB/Worker/Gateway/Web全门禁，DB/Worker全量PG串行。失败先修，不靠跳过伪报。
5. 质量/安全/覆盖率检查，`git diff --check`；限定路径`[be]`提交，不push；`git show --stat`复查。
6. 更新状态/台账/inbox-arch，交SHA/测试/未合流未部署/下一项。继续下一个无阻断子批。

## 当前立即执行：P0-04

### 2026-09-06 02:00 接续点

- 本节下方保留早期执行记录；**不重做已完成P0-04**。最新代码3b76ed3/85ea1bf；合main为11faf95，双方信箱/台账冲突仅追加内容，均保留。
- 合并后实际门禁：Domain518、DB纯逻辑37、Worker非PG724+2外部skip、Web111、Gateway非PG34；五包typecheck/lint通过。PG仍连接拒绝（最近01:53只读probe），这不是五包完整PG门禁。最近Worker/DB offline audit均0；合并无依赖变更。
- R009剩余：团队异常未支持（先等arch语义，不自造规则）、PG恢复后的旧迁移历史核对/专用库回放/KA开关双空间HTTP、P045最终回执。已经main→be不等于be已合入main。
- 下一独立建设：按arch已冻的012迁移-only→R013 bootstrap/coefficients→R010a1功能次序准备逐文件计划；不提前实现v3/写路由，也不把这些依赖项算完成。

1. 新 `packages/domain/src/metric-value.ts` + `test/metric-value.test.ts`，严格三态与聚合传播；复用RatioValue，不把denominator_zero混成缺数据。
2. 审计 `semantic-query-metrics.ts`、`semantic-query-dimension.ts`、`report-facts-source.ts`、`data-quality-repository.ts` 的COALESCE/sum忽略null；用expected account-days证明缺日，不只检查现存行count。
3. `data-query-rows.ts`的指标改三态、版本升v2；`canonical-query-rows.ts`两侧统一、SQL aggregate不能部分和装全量；Domain/Worker fixture一起升级；BFF只更新契约适配，不改视觉。
4. missing/error不静默0；真实0仍available；任一成员missing/error聚合missing；非法字符串/NaN拒绝；CPA 0分母继续finite/infinite/undefined。
5. 本批未完全接通v2之前不称P0-04完成。

2026-09-05进展：ee62db2完成步骤1；3e7f932完成步骤2中的语义SQL/任务日报/报告缺数传播（DB211、Worker641+2skip，详见R009状态）。data-quality raw/canonical空对账尚待修，然后继续步骤3两Adapter/v2；不是只有基础helper，也尚未全链完成。

接续更新：35d2482已完成data-quality空对账/unknown回灌（DB219、Worker643+2skip）；现在直接进入步骤3严格v2，而非重复前两个已完成内部修复。工作树仍be/r009，arch main8c240f7，旧v1输出和fixtures未升级，剩余范围不变。

21:40最新：bdc5273已提交步骤3后端v2，Domain514/Worker非PG648+2skip；PG由16:32可用变为21:32拒连，需恢复后补Worker全量。Web旧测试无法证明三份新fixture兼容（直接校验均false）；main@cda3303 A-001明确#8一并修BFF，继续非视觉契约接线。R013依赖012按最新migrate全部→seed处理，不造系数生效日期；R010a1新增alert_rules两列、R010a2缺数抑制与retry状态、R012 ubp永久unsupported/bid_tool枚举先提案均已收到，做到时完整读对应Contract。

## 停止/完成边界

- 目标保持active，只有信箱有效范围逐条真正完成才标complete；单一外部阻断不阻止其他可做任务。
- Contract缺口不自行拍板，写信箱；真实内网/凭证/媒体动作需要相应授权与OS证据，不用mock替代production-verified。
- 不改apps/web/ui-layout-demo视觉、PRD或冻结契约正文；前端API/BFF按派工精确范围接线，不随手修改页面。
- `merged/deployed/production-verified`只能按arch与OS实际证据更新；所有未审SHA完整保留。
