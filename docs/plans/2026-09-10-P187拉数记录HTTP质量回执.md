# P187 拉数记录 HTTP 质量回执

日期：2026-09-10；本人分支 `be/r010`，worktree `be-r010`。

## 代码与当前状态

- **c284224e**：按 arch `e32892a5` / v1.9.13，旧 run 日期缺失保留 `businessDate:null + LEGACY_NO_DATE`；非法日期仍拒绝，不拼当前日期、不丢行。legacy attempt 的固定消息与冻结fixture一致；新增固定 `_note` 类型。
- **1c94f081**：新增 EtlRunListService、R010 GET route；真实 `data-api.ts` 注入真实PG仓储，HTTP壳识别路由并复用Session认证；不是只有可挂载函数。
- **GET /api/v1/system/etl-runs 已在本人候选实现且本机真实启动验证**；尚未arch合流/内网部署，未实现POST rerun，未改前端、不push、无媒体写。
- arch同日把“本人所有公开端点fixture对拍”编号为P187，本回执只是该大项的 **ETL GET部分**（P187a）。其他端点对拍未宣称完成。

## 实现证据

| 要求 | 实际验证 |
|---|---|
| 可信授权 | internal bearer+真实Session cookie，客户端x-ka-workspace/role无效；非admin在仓储前拒绝。team仅读自己workspace |
| 分页 | 默认1/50，上限200；只准page/pageSize；重复键、未知键、零、负数、指数/小数/空字符串/前导零、安全整数溢出拒绝；超末页空返回200 |
| 输出守卫 | Service再次核workspace，strict envelope + requested page/pageSize；HTTP再核requestId/workspaceKind/page与固定观测说明，不信假Service返回 |
| 新旧日期 | run自己的日期来自仓储；旧缺日期null+warning；meta.businessDate按上海03请求业务日，不用于伪造run日期；dataAsOf仅真实finished_at最大值 |
| 大小 | 完整HTTP envelope按实际UTF8字节计，exact配置上限拒绝、低1字节通过；额外测试合法public warning响应超过默认16MiB→502 SOURCE_TRUNCATED、不发data |
| 错误与方法 | 400/401/403/405/500/502/503/504都有测试，405 Allow GET；错误正文固定，不带SQL/token/private body；no-store/nosniff/requestId |
| 真实启动 | 子进程直接 `node --import tsx src/data-api.ts`，严格合成ENV白名单，没有KA配置/凭证，健康检查200、真实KDF登录、真实PG查询 |
| Session闭环 | login→personal读取→switch team+token轮换→team仅本空间→旧token401→logout→401；非admin/撤销membership无法靠伪造头绕过 |
| fixture对拍 | 真HTTP返回顶层/data/meta/首行/rows键集与`system/etl-runs-page.json`逐一相同；LEGACY_NO_ATTEMPT warning code/message逐值相同。不是仅mock里手抄一个DTO |

## 门禁命令与结果

所有PG用 `TEST_DATABASE_URL` 指向本人独占本机 `ka_ci_be_r010_p178_test`（local55432+ka_*_test守卫），无真实业务数据；随机workspace只清本测试seed。测试只启动Data API，不启动Worker/Beat/任务消费。

1. 新HTTP TDD首次缺Service import红；实现后35/35，补默认16MiB后36/36。
2. `npm --prefix apps/worker test -- run test/etl-runs-http.test.ts test/etl-run-list-service.test.ts test/data-api-http.test.ts test/session-http.test.ts` → **111/111**（36+7+57+11）。包含原真实Web BFF回环数据路由回归，未修改该BFF。
3. `TEST_DATABASE_URL=... npm --prefix apps/worker test -- run test/etl-runs-http-pg.integration.test.ts test/data-api-startup.test.ts` → **10/10**（真实PG+入口7、启动条件3）。其中新增全部基于真实启动进程，不是只new factory。
4. 新GET/Service与治理目录一起：`test/etl-runs-http.test.ts test/etl-run-list-service.test.ts test/admin-calendar-http.test.ts test/admin-members-http.test.ts` → **83/83**；新Service/route coverage **statements/lines/branches=100%，functions=85.71%**（Service默认clock函数在子进程PG测试实际执行，但未纳入父进程coverage）。
5. `npm --prefix packages/domain test -- run test/etl-run-list.test.ts test/etl-batch-failure.test.ts` → **61/61**（42+19）。
6. `TEST_DATABASE_URL=... npm --prefix packages/db test -- run test/etl-run-list-repository.test.ts test/etl-run-list-repository.unit.test.ts` → **41/41**（PG20+unit21），包含新缺日期旧run。
7. Domain/DB/Worker 各自 `run typecheck`、`run lint` → 全通过；各包 `audit --omit=dev --offline` → **0 vulnerabilities**（缓存审计）；`git diff --check`通过。

## 质量与未完成

- 已按code-quality-checker检查：新增生产模块24/46行；无N+1、无动态SQL/外部抓取/环境凭证读取/console输出；真实启动依赖只多一个仓储服务。持久化/大字段守卫复用P186，类型及隔离反例没有降级。
- 有意只认GET：POST/PUT/PATCH/DELETE/OPTIONS在列表路径均405；`/:id/rerun`继续未注册，下一独立批按原job/owner/事务/幂等/timeline做，不靠管理员身份替换凭证。
- P187全端点对拍仍待逐项覆盖：Session、query/reconcile、tasks/accounts/work-items/list/detail、dry-run/mute、模型目录、calendar/members等现有端点需与各自真实fixture核对，不拿ETL一条代表全部。ETL暂无独立冻结错误fixture，本批测试稳定码/安全shape；不擅自冻结其他端点message。
- 分页索引已获arch授权归021；本批不抢先修改迁移。P178矩阵仍等be2 Q027；Full/Incr部分容错、密码管理、021、BI等总信箱目标仍未完。
- 磁盘6.2→7.3GiB，仍低于arch8GiB整包前置条件，未跑五包全量、不声称完整门禁或部署通过。
