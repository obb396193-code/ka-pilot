# I-002 工作项列表 BFF 质量回执

代码候选：`6377ff9`；基于已同步 main@f4205ce。仅工作项列表服务端接线和测试，不改视觉、Contract、权限模型或写路由。未 push / 未部署 / 未宣称 arch 接受。

## 实现与安全边界

- 新 GET /api/internal/work-items → GET /api/v1/work-items；复用服务端配置、ka_session、requestId、10s 超时、no-store、禁止 redirect 和 exact 16MiB 守卫。
- query 白名单、重复键/非法状态/规范分页校验；仅注入服务 token 和 session cookie，浏览器 Authorization/x-ka-* 不透传。无免鉴权 fallback。
- 用实际 Domain envelope 建 Web strict schema；永久双包运行时 parity 防手抄漂移。成功/错误 HTTP 状态、header/body requestId 与分页一致性均验证。JSON null 是非法响应，不冒充截断。
- 未同步的有记录和无记录响应均保留 partial；空超末页保留 total；没有合成数据新鲜度。

## 已执行门禁与失败记录

| 命令（各 package cwd） | 实际结果 |
|---|---|
| Web node --test work-item-list-bff/task-list-bff/read-model-bff/bff-session-routing 四文件 | 51/51，新增14 |
| Worker npm test -- --run test/work-item-list-http.test.ts test/work-item-list-bff-parity.test.ts | 23/23（HTTP22+双包parity1） |
| Worker npm run typecheck && npm run lint | exit 0 |
| Web eslint 本批5文件 | exit 0 |
| Web tsc --noEmit --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --strict lib/data/work-item-list-{bff,contracts,bff.test}.ts | exit 0 |
| Web npm run typecheck | exit 2；已有 shiki/ai/streamdown/xyflow/BlockNote 等组件依赖与类型错误，无本批 work-item-list 文件诊断。原始日志 /private/tmp/ka-i002-web-typecheck.log |
| git diff --check / 提交路径检查 | 通过 |

TDD 初始缺模块 RED。HTTP 新用例首次 502：测试错误地给有持久工作项的结果 dataAsOf:null，既有 Service 正确拒绝；纠正样本为真实活动时间，另加 total0/null 的未同步场景，未放宽生产守卫。新测试 HeadersInit 联合类型错误已修并重跑类型检查。

HTTP 用例确实启动 createDataApiServer 并从 Node 子进程调用真实 Web BFF/fetch；**Repository/session ports 为合成测试注入，不是 PG，也不是 Next 浏览器运行**。Next route 本批只验证文件接线；请 arch 合流后复跑实际工作台请求。

## 未关闭项

- 当前列表后端仍旧 coverage.complete；Contract 新 checked/pending/undeterminable ledger、ruleSetVersion 等属于 R010a2 真实扫描流水升级。不能由 BFF 捏造，后续需前后端一起切 strict schema。
- 真实 PG 55432 最近探针 ECONNREFUSED；磁盘约1.6GiB低于 arch 8GiB 全量门槛。本批未跑 PG/全量/实际部署，保留阻断，不借 ContentRadar 库、不清缓存。
- 新增代码不含依赖更新；未将上轮缓存 audit 结果冒充本次新审计。质控技能驱动了双包契约、真实 loopback、失败分类和路径/类型检查；全量限制如上。

I-002 候选提交后按长期队列继续 pivot2 公共查询接线，不等本条审批，总目标仍 active。
