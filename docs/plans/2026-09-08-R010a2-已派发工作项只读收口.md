# 已派发工作项只读收口 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** P083 冻结的 dispatched 在列表请求/响应、详情响应及默认活动态查询中可见，授权边界不变。

**Architecture:** Domain 共用状态常量，DB 的 count/page 共用同一活动态集合。只修读链；不顺带开放派发动作、旧告警升级写路径或团队写。

**Tech Stack:** TypeScript / Zod / Vitest / PostgreSQL。

---

技能：writing-plans 用于分步计划，code-quality-checker 用于门禁；executing-plans 当前不可用，按老板连续任务授权在本 worktree 本地 TDD 执行。

## 013 预检发现（单项阻断，继续其他工作）

- schema.sql:228 的 changeset_items.id 是 BIGSERIAL；:1100 的 reverse_item_id/original_item_id 和 :1104 的 execution_run_items.item_id 却是 UUID。旧 read-detail DTO 也按整数 item.id。不能通过字符串强转、生成随机 UUID 或丢 FK 假装可关联。请 arch 统一类型后再按其 schema 切片落 013；本批不改 Contract/迁移。
- 同时旧 active unique 包含 open/processing/dispatched，不含 escalated。013 仍须同步 CHECK/partial unique，并预检已有重复。旧写路径升级仍原地改，superseded_by 新建替代也在 013 后续，不能算本次完成。
- task-list-sql.ts 的两处活动态过滤缺 dispatched，文件暂归 be2，交 arch/be2 同步；本批不越界修改。

## Task 1：Domain 一致状态（TDD）

Files: packages/domain/src/work-items.ts、work-item-list-contract.ts、read-detail-contract.ts；对应现有 test 文件。

1. 添加 dispatched 请求、列表响应、详情响应正例及未知状态拒绝，先运行确认失败。
2. 导出 WORK_ITEM_STATUSES / ACTIVE_WORK_ITEM_STATUSES；两个 Zod 状态复用常量。WorkItemStatus 类型同步，但既有 transition 不放宽。
3. 运行三个定向文件与 typecheck/lint，保持终态/拒绝行为回归。

## Task 2：列表活动态与权限（TDD）

Files: packages/db/src/work-item-list-sql.ts、test/work-item-list-repository.unit.test.ts、现有 test/work-item-list-repository.test.ts（复用其跨媒体/跨空间合成种子）。

1. count/page 默认谓词使用只来自 Domain 常量的 SQL literal 集合；所有参数化过滤、workspace/tuple/本人无账户规则保留。
2. PG 反例：授权 dispatched 可见，跨 media/workspace 同号不可见；空 grant 仅本人无账户；team 排除无账户；显式 dispatched 过滤与超末页总数不变。
3. 本 PG 文件取消共享 ka 默认回退，只收显式 local55432/ka_*_test；沿用文件现有隔离库种子策略，不另建库。PG 不可用则写实际失败，不拿 unit 充 PG。

## Task 3：Worker 真实 HTTP 与交审

Files: apps/worker/test/work-item-list-http.test.ts、read-detail-service.test.ts。

1. 默认列表和 status=dispatched 200；详情 dispatched 保留；旧 status 502、scope401/403、team/个人规则及16MB回归。
2. Domain/DB/Worker 定向、三包 typecheck/lint。磁盘≥8GiB且隔离PG可用后才全量；未达到明确标未跑。
3. git merge main；路径限定代码提交；质量报告、R010-状态、P123信箱留痕独立提交；不push，继续队列。
