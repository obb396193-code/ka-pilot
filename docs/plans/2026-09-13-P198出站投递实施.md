# P198 出站投递 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 消费真实 outbound_messages，按 arch v1.9.42/46 发送钉钉消息，正确持久化重试与未知结果；不是新增一个仅供演示的队列。

**Architecture:** 单轮、限定 workspace、复用 workerOnceLock；领取与结果更新短事务并带 fencing token，HTTP 不占数据库事务。Domain 决定去重和尝试结果，DB 持久化 due/claim/unknown 状态，Worker 调用经过服务端接收人解析的 Transport。

**Tech Stack:** TypeScript、Zod、PostgreSQL、Node crypto/fetch、Vitest；不增加依赖。

---

## 当前依据与边界

- 基线 be/r010@206281fe，main@1e304507。前一轮仅状态答复，未推进实现；本轮恢复到实读及 TDD。
- brainstorming 的需求/方案批准依据是用户要求按 Claude 派单执行及 api v1.9.42/46 已冻结方案，不重新要求老板审批同一方案。单轮优于常驻循环；结果未知允许重试但不能声称 exactly-once（远端恰好一次）。
- writing-plans 执行子技能本机未提供，按本计划逐步 TDD；不转 Coze 云端，不修改 be2 数据链/页面。
- 现有入队者：failure-notifier 的 jobId、quality/check-handler 的 ds；target 为 user:<UUID> / workspace:<UUID>:admins。派发/考核价通知为 channel=inbox，不能擅自转钉钉。
- 待 arch 明确：群与单聊 target 到真实接收人的映射；DM API/凭证缺失的行为；各 kind 的业务主键/业务日及渲染模板；旧记录如何补 dedupe。没有明确映射前不注册真实发送入口，不 stringify 业务 payload 发群。

## Task 1：已冻结的投递策略与单轮编排

文件：新增 packages/domain/src/outbound-delivery.ts、test/outbound-delivery.test.ts；导出 src/index.ts；新增 apps/worker/src/notifications/outbound-once.ts 与 test/outbound-once.test.ts。

1. 先写失败用例：五段元组无拼接碰撞；所有维度隔离；非法日期/缺字段拒绝；sent-only 24h 去重。
2. 运行 `npm --prefix packages/domain test -- run test/outbound-delivery.test.ts`，确认缺模块红。
3. 实现 strict 私有类型。dedupe 用五段元组 JSON 的 SHA256，不以消息 id 替代业务主键；时间必须由调用者提供已确定业务日，不猜。
4. 增加 reducer 测试：成功才 sent；unknown 第一次 queued、连续第二次 failed；已知失败清 unknown 连续计数；总尝试不超过5；稳定错误枚举不接收原始错误文本。
5. 编排测试：未配置不触 DB/lock；并发锁拒绝；逐条领取+确认去重后发；发送前持久化；锁丢失中止、fencing 由 DB 执行；异常不吞为 sent；每轮有界且不消费 ETL。
6. 两包定向 test/typecheck/lint；独立代码提交，回执明确“内核，不是接通了发送”。

## Task 2：持久化与真实 PG

文件：packages/db/migrations/下一个空号_outbound_delivery.cjs；src/outbound-delivery-repository.ts；test/outbound-delivery-repository.test.ts；既有 outbound-message-repository.ts。

1. 待接收人/旧行处理明确后补迁移：dedupe_key、run_after、claimed_at、lease_token/lease_until、consecutive_unknown。旧历史不捏造业务键，缺值 fail closed。
2. 先真实 PG 写反例：两实例只领取一次、跨 workspace、同 key 24h、stale token 无法 complete、crash recovery 算未知、attempt5封顶、已 sent 不重发。
3. 领取/结果短事务；同 workspace 复用锁，数据库时钟决定 due/租约。响应成功而 DB 更新失败留下 sending，下轮按未知恢复，不重置 attempts。
4. up/down/up 与数据保护测试；不得删除既有审计。迁移编号落地前再次查 main。

## Task 3：真实 Transport 与入口

文件：apps/dingtalk-gateway/src/出站模块与现有 URL 白名单复用；apps/worker/src/notifications/配置和运行时；scheduling/worker-once-diagnose.ts；package.json 脚本、.env.example、权威部署手册。

1. 群 webhook 签名/成功 envelope/超时/连接中断/HTTP拒绝/非法JSON/限流/SSRF/重定向，用 mock fetch 测，不发送真实群。
2. 根据 arch 冻结的 target 与模板接入群和 DM。凭证完全服务端；错误和诊断仅状态码，不含URL/正文/Token。
3. CLI 单次入口有界 batch，不依赖 Qihang 凭证、不跑 ETL；配置缺失整轮 skip，worker:diagnose 增加 not_configured。
4. 合成 PG→真实入口进程→mock transport 联合冒烟；证明 sent/failed 可查询和重试恢复。

## Task 4：交审

- code-quality-checker 检查状态机/时钟/幂等/SSRF/密钥泄露/覆盖；先定向红绿再三包全量 test/typecheck/lint、gateway 回归、真实 PG。
- 交付前 merge main，无冲突才继续；apps/packages 无未跟踪源码；路径限定提交，不 push。
- 回 inbox-arch：各 SHA、原始门禁、形状变化、旧记录兼容、未接通外部依赖。不称已合流/部署/真实群验证。

## 执行记录

2026-09-13 Task1已交审：`0aa677fc`，Domain37/Worker32定向，三包全量1650/1832/2475+2跳过，typecheck/lint全绿。详见`2026-09-13-P198内核质量回执.md`。当时Task2/3尚未实现，接收人/模板/旧行去重与skip状态已于`787a5bb5`回arch。

### v1.9.48已收到（main@c111f6e3，合并1bac7a72）

此前待定项已解除，不再重复等待：每空间独立群webhook；user映射只查本workspace；缺配置/映射分别NO_CHANNEL_FOR_WORKSPACE/NO_DM_BINDING；未知kind为UNSUPPORTED_KIND；重复行deduplicated+dedupe_of，不伪造sent_at。诊断group/dm分开。**覆盖上面Task3“配置缺失整轮skip”：缺哪类只失败哪条，其他继续。**

顺序按新信箱调整为P198持久化/Transport/注册→P193接线；P193仓储39bf2683已提交，Service尚未动。信箱/台账冲突依arch批准只删除冲突标记、双方原文全保留。

Task2落地细分：先加030真实迁移+业务键准备（有效旧行补算、不用message id），后接仓储claim/finish/dedupe和真实PG并发。030仅增列/约束/索引，旧历史键保持NULL直到消费；down遇已投递管理状态拒绝丢失审计，维护窗口迁移不称在线低锁。组/单聊外部请求留Task3，测试不用真实凭证。

实读jobs没有business_date物理列，实际业务日位于jobs.payload.businessDate；etl_runs位于scope。按同workspace+jobId解析真实字段，缺失按v1.9.48回退入队时上海03点业务日；present-invalid不偷偷回退。业务键计算的映射须在质量回执明确，不能发明物理列。

### 恢复验证记录

上一轮并行DB回归90文件失败（6个测试失败、720项因hook失败未执行），首条迁移30秒超时后发生锁/缺表连锁；Worker日志未有全量终态。两轮都不算绿。恢复时旧会话句柄已不存在、进程表确认本轮测试停止，磁盘从约582MiB恢复至7.7GiB（未由本Agent清理）。保留失败库与日志，改用全新合成库`ka_be_r010_p198_recheck_test`串行回归，不调整超时，不关闭门禁。

新增稳定哈希稀疏数组反例先红，改Array.from遍历后59项定向+Domain typecheck/lint通过；030迁移测试补上真正旧sent行的up/down/up保留，不再只靠测试标题。当前全量待最终记录。

实读补充：`approvedScheduledSyncPayload`明确把businessDate同时写到full的asOfDate/incr的ds；run的full scope实际是asOfDate，incr/quality/day实际是ds，不是统一的scope.businessDate。后续Repository按run_kind做已知字段映射；跨日coordinator只有区间不能捏造单业务日。未知/缺失按已冻入队业务日回退，present-invalid拒绝。

主管main已到75eb0d35且已合前述P193/P194/P198内核，交付前继续同步；新增A46要求Domain源文件能由Web ES2017编译，必须追加Web typecheck证明，不能仅凭Domain自身ES2022门禁。

串行新库回归已完成：DB163文件1874项（含030迁移8项）及typecheck/lint全绿；定向Domain59项+typecheck/lint绿。旧失败证据不覆盖。此时仅保存持久化基础独立代码，尚需同步main与合并后Worker/Domain/DB/Web门禁；不将保存提交视为整批交付。

### Task2仓储实施边界

- 每次claim/finish使用短事务、固定workspace与随机lease token，数据库时钟校验租约；网络发送不占事务。旧queued按真实job/run日期补键一次；payload不合法永久失败且不外发原文。claim扫描上限100，不能无限循环。
- expired sending先记一次unknown并按现有状态机延后；不能假定未发送、不能清零attempts。finish必须比较workspace/id/token/status/lease与原attempt，过期持有者不能写成功。
- sent去重只查同workspace/kind/target且24h真实sent_at；对旧sent缺键只读计算，不伪造历史字段。候选有界，溢出fail closed；mark再次证明首发记录并写dedupe_of，sent_at仍null。
- 先PG反例后实现：并发单飞、跨空间/站内消息不消费、旧键回放、stale token、expired unknown两次终止、5次封顶、sent-only去重与重复审计。
- 新增门禁纪律：DB完整migration回放和Worker全量使用**不同的新合成库**。Worker路由测试留系数审计数据会让DB down合理拒绝；不删除残留来假装同库复跑安全、不放宽保护性迁移。

Task2复查补充：近16MiB旧sent证据原先只算payload，真实PG反例先红（错误返回sent证据），现在SQL在返回前同时计target/kind及固定元数据安全余量；14项PG通过、仓储覆盖95.93%行/86.11%分支。claim维护达到100行返回maintenanceLimit，由once显式batch_limit而非谎称drained。Worker联合用例逐行断言另一workspace和inbox仍queued/attempts0。

### Task3官方调用面核对（只读调研，尚未发送）

- 钉钉官方文档：[主动单聊](https://open.dingtalk.com/document/orgapp/chatbots-send-one-on-one-chat-messages-in-batches)、[内部应用token](https://open.dingtalk.com/document/orgapp/obtain-the-access_token-of-an-internal-app)、[机器人安全设置](https://open.dingtalk.com/document/orgapp/customize-robot-security-settings)。网页本轮未提供可读正文，因此没有把搜索摘要当字段证据。
- 已只读检查阿里官方npm包`@alicloud/dingtalk@2.2.48`，来源`https://registry.npmjs.org/@alicloud/dingtalk/-/dingtalk-2.2.48.tgz`，只流式读取源码、不安装、不执行。`package/src/robot_1_0/client.ts:642-668,3982-4025`确认POST `/v1.0/robot/oToMessages/batchSend`；字段msgKey/msgParam/robotCode/userIds及x-acs-dingtalk-access-token；响应包含filteredStaffIdList/flowControlledStaffIdList/invalidStaffIdList/processQueryKey。
- `package/src/oauth2_1_0/client.ts:105-124,1300-1326`确认POST `/v1.0/oauth2/accessToken`，appKey/appSecret，响应accessToken/expireIn。固定HTTPS目标、禁止重定向，token仅内存缓存，不落日志/出站payload。
- 发送确认不代表用户已读；HTTP200缺形状/包含失败用户列表不能记成功。单接收人请求按目标staffId检查拒绝/限流列表；模糊结果遵循已冻结unknown策略。后续Transport测试必须覆盖这三个失败列表，不只测HTTP状态。

Task3落点细化：实际出站HTTP模块放`apps/worker/src/notifications/dingtalk-outbound-http.ts`，直接被同进程单轮运行时使用；不import入站网关的启动模块，不新增跨app运行依赖。沿用入站HTTPS/钉钉域/无userinfo/无自定义端口规则，出站额外限定`/robot/send`和access_token；官方OAuth/OTO地址固定在代码。入站网关仍独立并跑回归，不改变其Stream机制。

当前32项fake-fetch定向绿（覆盖100%行/85%分支），包括首次red缺模块、取消后仍fetch反例由红修绿；初轮lint命中控制字符regex规则后改显式字符码检查，无禁用规则。所有secret均为合成值，未发送真实请求。群签名与DM token+OTO调用面已实现但尚未注册；全量Worker/gateway正在跑，只有终态后补交审。

接线剩余的逐文件范围：
1. DB私有出站上下文读取：以claim的workspace/id/token再验租约和active workspace；同空间取workspace name、job media/businessDate、最新run线索，DM反查active user + identity_mappings，重复映射拒绝，不读取其他凭证字段。
2. Worker出站配置/模板：群配置按workspace严格解析，缺该群不回落；DM只使用服务端AppKey/AppSecret/robotCode；仅三类固定模板，原始error永不外发，缺字段显示未知；日期使用已冻结job/run/入队顺序。只有受信Web base URL能生成链接。
3. Worker单次CLI：独立于ETL，不要求QIHANG_BASE_URL；复用workerOnceLock，受信workspace限制与batch/总时长上限；用假Transport+合成PG验证实际入口，不触外网。
4. worker:diagnose追加group/dm配置存在状态；日报Domain与Repository新增deduplicated展示，按arch要求写回fe镜像/文案接线说明，不改视觉。

Task3私有HTTP模块已独立保存`2262302b`：Worker2526+2外部跳过/gateway36全量与两包typecheck/lint通过，定向32项、覆盖100%行/85%分支。未注册CLI/解析器/模板；继续以上四项，不把发送类可测当产品已接通。
