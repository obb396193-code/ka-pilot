# KA BI 转化纠偏实施计划

**Goal:** 按已核验 ka-src-0010 原文将账户 conv 映射到 realConversion，不冒充媒体回传数。

**Architecture:** 复用现有 Canonical mapper 和 Query Registry，仅修来源语义；公开行版本与 Contract 不变。没有实证的回传字段保持 missing，CPA 后端计算，cvr/gap 不猜。P061 考核版本/effectiveDate 缺口仍等 arch。

**Tech Stack:** TypeScript、Vitest、真实内存 SQLite、现有 PG 回归。

1. `apps/worker/test/ka-bi-conversion.test.ts`：六 Query source parity，BI 零/缺失/非法类型与显式回传分离；先运行确认旧实现失败。
2. `apps/worker/test/query-registry-sqlite.test.ts`：实跑 summary/trend/reconcile SQL，不手抄聚合结果，核对同号跨媒体隔离与 BI CPA。
3. `apps/worker/src/data/canonical-query-rows.ts` 修 source-aware 别名；`query-registry.ts` 将 conv 聚合别名改 real_conversion；既有误口径 fixture 同步。不得发明媒体回传来源或查询别的 ID 域广告组表来补。
4. 定向测试、Domain/DB 真实 PG/Worker 全量、typecheck/lint、production offline audit、diff/security 自检；路径限定独立代码 SHA，回执写 inbox-arch 与 R010-状态，不写总台账。

执行：`npm --prefix apps/worker test -- --run test/ka-bi-conversion.test.ts test/query-registry-sqlite.test.ts test/canonical-query-rows.test.ts test/ka-data-client.test.ts`。既有获批计划续批，直接按 TDD 执行，不另开子代理。

## 交付候选 591ab67

17新增映射例+6新增SQLite例。原样SQLite JSON→KaDataClient又实证reconcile的整数ds违约，SQL改CAST AS TEXT；未通过在测试里重写结果掩盖问题。冲突BI别名拒绝，等价数值别名不重复求和；source原文不复制到Git。公开rowSchemaVersion仍v2（字段形状未变），不是已完成v3公开窗口。

### 质量报告

- 功能：定向89/89；包含Registry的覆盖113/113。初次15fail/13pass含1个新anomaly fixture漏必需标志，已补；随后实跑客户端锁定1个reconcile日期失败；别名冲突反例先2fail/15pass后17pass。不声称所有红例都是生产缺陷。
- 全量：Domain617，DB真实PG362，Worker891+2外部opt-in skipped；三包typecheck/lint均exit0。日志/tmp/ka-bi-domain-final.log、ka-bi-db-retry.log、ka-bi-worker-final.log。Gateway/Web本批0diff未重跑，不混用旧数字。
- 覆盖：两核心文件行95%、分支88.78%；mapper98.27%/93.22%，Registry93.04%/83.8%。/tmp/ka-bi-coverage.log。
- 安全/性能：固定SQL和原有参数化/Registry约束不变，原样客户端错误不带body，scope未放宽；新增每行3个别名有限校验，不新增I/O/N+1；截断/exact16MB回归通过；production offline audit0，不冒称实时漏洞数据库扫描。
- 失败留存：DB首轮migrations beforeAll默认10s超时，在合成库schema重建间中断导致36套失败、131pass/231skip；仅该库用hookTimeout30s重跑通过。定向10k测试首次5s超时，改测试命令30s重跑，产品限额和性能断言未改。失败日志保留ka-bi-db-final.log、ka-bi-focus-final.log。
- 边界：未合流、未部署、未访问真实源，无媒体写、前端视觉、Contract或依赖改动。P058/P061仍待arch。下一独立子批为R013b产品登录会话清理（只拟实现，不在本轮删除实际会话）。
