# P158 Worker 交界测试样本质量报告

日期：2026-09-09。代码 `cb73aea`；已含 main@7dd47e6。候选待 arch 审查，未 push、未部署。

## 修改范围

仅四个 Worker 测试文件，各自的 Repository 合成返回补齐新必填字段：

- `test/account-list-http.test.ts`、`test/account-list-service.test.ts`：六个可空经营元数据为 null。
- `test/task-list-http.test.ts`、`test/task-list-service.test.ts`：四个可空阶段字段、空 overrides、一个未充值/未基建合成账户的明确事实。

无生产、Contract、视觉、依赖、be2 服务/仓储改动；没有使用 cast 或把字段改 optional 消除报错。null/测试数值不是业务系统默认值。

## 红绿证据与命令

修复前 `apps/worker: npm run typecheck` 返回四个 TS2740，正是上述样本缺字段；修复后通过。

| 检查 | 结果 |
|---|---|
| 四文件 `npx vitest run test/account-list-http.test.ts test/account-list-service.test.ts test/task-list-http.test.ts test/task-list-service.test.ts --maxWorkers=1` | 56/56 |
| Worker `npx vitest run --maxWorkers=1 --reporter=dot` | 148 文件通过、2 跳过；1643 项通过、2 跳过，76.67 秒 |
| Domain 同上全量 | 82 文件、1213 项全部通过 |
| Worker / Domain / DB `npm run typecheck` + `npm run lint` | 三包全过 |
| Worker `npm audit --omit=dev --offline --json` | 缓存审计 0 vulnerabilities，不代表联网最新公告 |
| `git diff --check` 与 path-limited commit stat | 通过；4 文件、30 行新增 |

Worker 全量显式 `TEST_DATABASE_URL` 指向本机 55432 的 `ka_be_r010_20260907_test`；设置 `IDEALAB_ASR_SMOKE=false`、`KA_RUN_AGENT_SDK_SMOKE=0`。PG/localhost HTTP 实际执行，外部 ASR/SDK 探针未调用；不是内网真数据证明。磁盘开始 11GiB，满足本机至少 8GiB 才跑全量约定。

## DB 全量复验记录

首次复用 Worker 测试库：102 文件通过、1 文件失败；1140 项通过、3 项失败。三个失败均为 `contract-v1-3-migration.test.ts` 的首次 down 遇已有 typed JSON，迁移按设计拒绝有损回退。没有删除残留或放宽迁移保护。

为区分库残留与代码问题，新建独立 `ka_be_p158_20260909_test`，不覆盖旧库；显式指向该库执行相同串行命令，**103 文件、1143 项全部通过，46.45 秒**。旧库失败不能抹掉或冒充首次全绿。两个测试库均保留，未做删除。当前迁移套件仍依赖干净库，这项测试隔离债交后续批次处理，不改生产迁移的有损降级保护。

## 质控结论与遗留

- 本批只有静态合成 fixture，无新 SQL/HTTP/循环/依赖，未增加运行时安全、性能或复杂度路径；未发现本批新增高风险问题。
- 测试覆盖：56 定向和 Worker 全量回归通过。本批无生产模块新增，不伪报新的模块覆盖百分比。
- 新字段服务投影尚未完成，不因补齐 fixture 宣称 S6 完成；精确位置见 `2026-09-09-R010a2详情与S6交界实读缺口.md`，交 arch/be2。
- R010a2 新旧详情 envelope 与决策缺证据表达需 arch 裁决；不得填假的近期人工操作数、额度或历史成功率。
- Web 前轮整包类型错误尚未在本批重验/修复；没有 build、浏览器或部署验收。总目标仍 active，生图已按老板取消。
