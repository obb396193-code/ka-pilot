# P161 命令 BFF 代理域名修复 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 关闭 arch F-P157-1：反向代理后同源命令不再因内部 localhost Origin 被拒。

**Architecture:** 采用 arch 冻结方案①，存在 Sec-Fetch-Site 时仅 same-origin 通过；缺失时严格对比 Origin 与 request.url.origin。永不使用 forwarded host。会话、JSON、三值校验、大小限制和媒体写关闭不变。

**Tech Stack:** TypeScript、Web Request/Response、node:test。

---

执行技能未安装；按用户持续开工授权，本会话自行逐步 TDD，不另派子 Agent。

## Task 1：先锁定边界

- 修改 `apps/web/lib/data/r010-command-bff.test.ts`：公网映射域名/127.0.0.1 Origin 与内部 localhost URL 不同但 same-origin，三条命令应抵达模拟 upstream；不得拿合法 Origin 覆盖 cross-site/same-site/none/非法值；缺 metadata 时 Origin 缺失/null/路径/异域拒绝；forwarded-host 不改变判据。
- 执行 `node --test apps/web/lib/data/r010-command-bff.test.ts`，预期新增代理域名用例为红。

## Task 2：最小修复

- 修改 `apps/web/lib/data/r010-command-bff.ts`：

```ts
if (fetchSite !== null ? fetchSite !== "same-origin" : request.headers.get("origin") !== url.origin)
  return fail("FORBIDDEN")
```

- 重跑上述测试与 `r010-command-contracts.test.ts`；检查 cookie、requestId、exact16MiB、source-off 不退化。
- ESLint 仅此二文件；质量扫描 diff，必要覆盖率检查，不改 UI/共享契约。
- 路径限定代码提交；`git show --stat HEAD`；独立留痕提交写 R010 状态与 inbox-arch P161。

## 验证边界

本机磁盘 7.7GiB，未满足≥8GiB 全量门槛，不宣称本批全包/构建通过。单元传输模拟不等于真实公网部署；交给 arch 复跑 next start 3411 与沙箱代理路径。既有图候选保留，用户已取消全部生图任务。
