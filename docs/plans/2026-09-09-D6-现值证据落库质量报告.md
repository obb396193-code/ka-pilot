# D6 现值证据与原子落库（P155）

代码 **a317f7c**。当前已含main@7dd47e6；前批P134–153已由arch合流，新P154/P155仍候选。无前端/Contract/迁移/真实媒体写改动，未push/部署。

## 本批实现

- 新 `ChangeSetDryRunService.preview()` 复用旧run授权/绑定/超时/TTL核验；旧run状态接口仅内部保留。public preview要求Provider带逐项observed和明确dataAsOf（可null），不能将旧status-only proof适配为页面成功。
- Provider请求新增`requireObservedValues:true`。响应必须保留workspace/media/account/credentialOwner/draftHash绑定，items精确覆盖且不可重复，逐项受控status/failReason/observed；不接收任意上游消息。缺字段、假success、future dataAsOf均在持久化前拒绝。
- 起草值/目标值来自私有prepared快照；现值只能来自受控Provider。changed/unknown/blocked保持ready预检结果但阻止confirm；全部ok仍需当前账户execute grant，preview-only授权不显示可确认。
- 在 `recordDryRun` 同一行锁/事务内重新校验observations与实际草稿的ID/目标/字段/from/to、结果status及checkedAt；保存到execution_runs.result_payload.observations，和状态/hash变更一起提交。status-only内部调用不制造observations。
- 证据和完整结果JSON分别检查exact16MiB；沿用10000项上限。unknown或changed清空此前dry_run_hash/confirm_hash；changeset仍draft、无jobs。响应不包含凭证/原始异常。

## TDD 与验证

新Worker preview10例先红（方法尚不存在）；DB7例先红（证据被忽略/非法证据被接受）。实现期间发现DB包没有zod直接依赖，已将snapshot schema放回Domain，由DB复用；不新增依赖。

最终命令在对应包执行：

```text
Worker: npm test -- --run test/changeset-dry-run-service.test.ts test/changeset-dry-run-http.test.ts --maxWorkers=1 --coverage --coverage.include=src/changesets/observed-preflight.ts --coverage.include=src/changesets/dry-run-service.ts
77 passed (54 service + 23 old HTTP)
DB: npm test -- --run test/changeset-observations.unit.test.ts test/changeset-dry-run.unit.test.ts --maxWorkers=1 --coverage --coverage.include=src/changeset-observations.ts
52 passed (14 + 38)
Worker + 独立本机PG: npm test -- --run test/changeset-dry-run-service-pg.integration.test.ts --maxWorkers=1
9 passed (synthetic Provider / real PG)
Domain: npm test -- --run test/changeset-preflight-presentation.test.ts test/changeset-values.test.ts --maxWorkers=1
66 passed
```

合计204定向通过。真实PG用本后端`ka_be_r010_20260907_test`，未清共享库；逐一验证同run精确保存观测值、changed/unknown撤旧proof、注入foreign目标整体拒绝，以及旧scope/TTL并发变更/无源无副作用回归。不是媒体实测。

覆盖：Worker两模块行100%/分支92.74%（service92.66、mapper93.33；不可达防御分支等未覆盖）；DB新validator行/分支100%。Domain与DB typecheck/lint过，Worker lint过。

**Worker typecheck仍4处旧account/task fixture缺be2新增必填字段**，路径同P153；本次新增文件无剩余类型错误，但不称Worker整体typecheck通过。第一次HTTP回归长期无后续输出，已用原handle9577中断(exit130)，在允许本机监听环境重跑77绿；未将中断当通过。原始证据在本会话tool sessions76812/96465/2876/11169，未虚构额外磁盘日志。

## 未完成

- **公开HTTP/BFF仍未切preview**，目前保持source-off503。下一子批接三值成功envelope、requestId/metadata/exact16MB及BFF parity，不扩真实Provider/confirm/execute。
- 无网络依赖变化，本轮未新增联网audit。磁盘曾短暂11GiB，收口6.7GiB<8，完整五包/PG未跑，不用旧全量绿替代。
- R-FE-IMG-003老板已取消（P152有原话），不再执行头像提醒，请arch同步fe。F-Q011-1已在0ce730f/P152交付，本次主线也已包含。
