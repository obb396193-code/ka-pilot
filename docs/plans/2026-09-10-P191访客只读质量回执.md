# P191 访客统一只读门质量回执

代码 `dfa692af`；当前分支 be/r010，基于合流 arch@1d93a975（本地 merge08395e9d）。状态：后端中央门 implemented + HTTP verified；未push/未部署、待arch。P190两条覆盖红闸仍存在，整分支不是全绿。

## 实现

`apps/worker/src/data/http-server.ts:486`，完成 internal bearer + Session授权后、所有业务Handler与body解析前，role=viewer 的 POST/PUT/PATCH/DELETE 统一返回403 READ_ONLY_ROLE，四字段稳定错误体，requestId/no-store保持；请求体drain不解析。

三类Query的POST（/query、/data/query、/admin/data/reconcile）是只读传参，不当写操作；仍受原Query及诊断授权约束。Session路由在另一分支保留，viewer能logout（否则访客无法退出）、切换仍核active membership，不因本门放松。be2的R014独立guard不变，本壳门也在R014调用前生效。

## 证据

- 自动从P190扫描库存取20路径；排除4个session路径、仅排除三个查询路径的POST，产生**61个路径/写方法组合**，真实HTTP每条都严格403 READ_ONLY_ROLE，业务Service spy计数0。
- 修复前 **62失败/3通过**，证明不是空测试；修复后本文件65/65（61组合 + 库存检查 + 超限非法body先拒 + 保留读取 + logout/认证/非viewer）。
- 七文件定向回归 **187/187**：viewer65、data-api57、ETL36、loginIP10、session11、解析器5、r014 viewer3。保留现有requestId/exact16MiB等旧门。
- Worker typecheck/lint、git diff check均绿，cached production audit0；仅11行HTTP实现+新测试，DB/Domain/迁移/前端0改动，不涉及新PG逻辑。
- 当前磁盘5.5GiB，按既定门规没跑五包全量；不是全量全绿证明。

## code-quality-checker：仍未完成的边界

1. **真实BFF探针发现前端错误码遗漏**：读 `apps/web/lib/data/r010-command-contracts.ts:20-32`，自己的commandErrorCodeSchema无READ_ONLY_ROLE。实际导入当前 `handleR010CommandRequest`、用合成fetch返回后端403 READ_ONLY_ROLE（正确requestId）后，BFF输出 **502 UPSTREAM_INVALID_RESPONSE**。无需外网、没有真实写。首个探针遇web CJS/ESM named-export不兼容，使用module.default互操作后获得以上实际结果；不是只凭grep猜。请fe补命令schema/status/固定提示，不能把“后端拒绝了”说成“用户已看到正确提示”。
2. 后端没有被启用的 rerun/reset/灰度/batch 等新路径尚不能枚举；仍404且不执行，后续挂入统一壳会继承该门，库存测试自动增加。**不是已经实现这些端点**。
3. 静态库存是路径下限，本文件逐路径验证方法和业务调用前拒绝；不替代真实BUC/guest身份落库/部署验收，Session使用合成批准上下文。无真实媒体/队列调用。
4. POST Query只读例外必须持续只读，不能在现有Query分支塞入写行为；该安全不变量需要后续审查保留。

下一步依序收尾P178，F-P179-Q两问仍待arch，继续其它可做项而非整目标blocked。生图不恢复。
