# P192 工作项列表共享授权质量回执

- 代码：`529a345a`，`be/r010`；依据 arch P178 / v1.9.11 与 main 已合的 Q027 helper。
- 状态：本机 implemented + targeted_verified，等待 arch exact-SHA 验收；未合流/未部署/未 push。不是整个 P178 完成。
- 生图继续停止；没有媒体写、前端改动、合同修改、be2 auth/session 文件改动、arch 台账改动。

## 审查结果

| 项 | 结果 |
|---|---|
| count/page 同源授权 | 都引用 workItemScopeClause，外层 workspace + 半空 tuple 拦截保留 |
| 个人账户型 | 只能命中获授 media/account，不因 assignee/creator 本人绕过 |
| 个人任务型 | 同 workspace/task 获授关系 OR 本人；空 grant 仍只保留本人项 |
| 团队任务型 | 当前 workspace 全量；纯私人项仍隐藏 |
| Service 输出防线 | 内部 taskScopeAccount 严格校验；非本人任务须获授 tuple；证据不会出现在 DTO |
| 一致性 | RR/RO 同 snapshot 的 count/page/任务证据；并发删关系，本次完整读、下次不可见 |
| 负对照 | 只在测试代理摘掉共享谓词：授权计数 3 → 5，并暴露跨媒体/跨空间关系的错误任务 |
| SQL/秘密 | 请求均绑定参数；SQL 插值仅现有常量/helper；无新增秘密配置、响应字段 |
| 依赖/类型 | DB、Worker typecheck/lint 通过；两包 npm audit --omit=dev --offline 均 0（缓存审计，不是实时漏洞库） |

内部证据是同一 SQL 中 LATERAL 查询得到的一个真实 task_accounts 获授 tuple，C 排序 LIMIT 1，不做 N+1 应用请求。Service 只能证明证据 tuple 在 approved scope，任务关系真实性由可信 Repository + SQL snapshot 证明，不能宣称防御任意伪造内部 Repository。任务关系时间规则保持 Q027 原样，未擅加 businessDate 有效期条件。

## 可复验命令和结果

合成独占库 `ka_be_r010_p192_test`，localhost:55432；先实测 public 表 0，套件自己执行迁移。未删除/覆盖共享业务库；仅合成身份账户。测试库保留供复验。

1. `packages/db`：设置该库 TEST_DATABASE_URL，`npm test -- --run test/work-item-list-repository.test.ts test/work-item-list-repository.unit.test.ts`：**21/21（PG 12 + unit 9）**。
2. `apps/worker`：同测试库，`npm test -- --run test/business-read-session-pg.integration.test.ts test/work-item-list-service.test.ts test/work-item-list-http.test.ts test/data-api-http.test.ts test/read-detail-service.test.ts test/r010/viewer-readonly-http.test.ts test/work-item-list-bff-parity.test.ts`：**7 files / 185 tests passed**。
   - 真实 Session/PG/HTTP 4，Service 22，list HTTP 22，data HTTP 57，detail 14，viewer 65，BFF parity 1。
   - 已有联合用例增加获授/隐藏任务型、team 任务型、撤权后只余本人私人项；旧 token / 伪造 header / logout 及其他路由保持通过。
3. 两包 `npm run typecheck && npm run lint && npm audit --omit=dev --offline` 全部 exit 0；最后新增 unit 后再跑 DB type/lint 通过。
4. 覆盖率：同定向追加 `--coverage --coverage.include=src/work-item-list-repository.ts`：行 **97.43%**、分支 **88.13%**、函数 **100%**；Worker Service 行 **97.33%**、分支 **93.85%**、函数 **100%**。报告 `/tmp/ka-p192-db-coverage`、`/tmp/ka-p192-worker-coverage`。
5. `git diff --check` 通过；8 个代码/测试文件路径限定提交，前端/台账 0 diff。

红绿证据：改实现前 PG 新三例红（个人期望3实际2，团队期望8实际3）；Service 新两例红。改后全部绿。追加非法证据 unit 初跑因 mock 漏 readiness 返回而 5 红，已补正确 mock 重跑 9/9；不是绕过生产 readiness。

命令纠正记录：根目录不是 npm workspaces，第一次 `--workspace` 命令未执行测试，后续全部在各包工作目录运行；一次回归误列 `data-api.test.ts` 不存在，最终已改为真实 `data-api-http.test.ts` 并确认 57 项实际运行。

## 未完成与风险

- **工作项详情仍旧语义**：任务型非本人可能列表可见但详情 403。下一独立批次将共享谓词接入 detail Repository/Service；本 SHA 不应单独宣传整条工作项闭环已可用。
- P178 其他仓储全域审计尚未全部完成；P187 全公开端点对拍也未全部完成。
- P190 两个路由覆盖测试仍红（三个真实 BFF/后端缺口已回执），P191 命令 BFF 缺 READ_ONLY_ROLE、P189 BFF 缺 IP 转发仍由 fe/arch 接；没有加入豁免制造绿灯。
- F-P179-Q 的 rerun error/jobId 与 timeline 两问尚待 arch，未偷偷自定 DTO。P176 Full/Incr 容错、F-OS-004、021/索引等继续排队。
- 磁盘约 5.7GiB，小于本机 8GiB 门槛，**未跑全五包**；本报告 206 项是当前批次不同定向测试总数，不是全仓测试数。生产级数据规模/性能未验证，未运行真实 ETL。

按 code-quality-checker 检查了权限、参数化 SQL、响应泄露、测试反证和依赖；未新发现本批范围内 P0/P1，以上跨批缺口保留，未写“整仓全绿”。
