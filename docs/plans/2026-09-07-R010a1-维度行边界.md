# R010a1 维度行边界 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 按 arch main@9c0b375 的真实 dimension-v3 fixtures 实现严格行边界；本批不声称完整公开维度查询完成。

**Architecture:** Domain 复用 MetricSet 与窗口考核校验；账户维度保留 media/accountId 与冻结 key=media:accountId，其他维度不借用账户身份。Source envelope 仍有未解决 lineage 缺口，因此不提前注册一个返回损坏 envelope 的公开 Query ID。

**Tech Stack:** TypeScript、Zod、Vitest；独立 be/r010 worktree。

## Task 1：先固定真实样例与反例

- Create `packages/domain/test/dimension-window-rows.test.ts`。
- 直接读六份 `packages/contract/fixtures/data-query/dimension-v3*.json` 的 dimension+rows，禁止手抄或改权威样例。
- 验证跨媒体同号保留两行；账户缺 media/accountId、伪造 key、重复 key、非法数值/布尔值、缺指标、无现金却有达标结论均拒绝。
- `npm --prefix packages/domain test -- --run dimension-window-rows.test.ts`，先见 missing module/exports 红灯。

## Task 2：最小 Domain 校验

- Create `packages/domain/src/dimension-window-rows.ts`，Modify `packages/domain/src/index.ts`。
- 复用现有 v3 summary row 的 metrics/assessment 交叉校验，不另定义 CPA/现金判断公式。
- 单独校验维度与 rows；账户使用明确 media/accountId；空 rows 可表达，重复键拒绝。不上 SQL、HTTP、BFF，不改 Contract。
- 独立模块核心行/分支覆盖要求 >80%；Domain 全量与 typecheck/lint，保留旧样例失败。

## Task 3：逐单元交付

- 路径限定代码 SHA；回执追加 inbox-arch 最新可用编号、更新自己的 R010-状态。
- 报告 main 提交说明与实际文件差异：ready/unknown-lineage、health、etl-runs 未变；dimension lineage 仍缺 datasetVersion/queryTemplateVersion/metricVersion/objectIdentity。
- PG 55432 拒连为 pg_blocked；不沿用历史证据，不宣称合流、部署、真实源或媒体写。

`superpowers:executing-plans` 未在当前技能目录提供，按主 Agent TDD 顺序执行；不启动未授权子 Agent。本批完成后继续总信箱目标，剩余语义由 arch 冻结，不改成“小内核即全功能”。

## 14:52 现场变化（覆盖以上阻断快照）

- arch@492b5c4 已回复 P065–P101 并补齐 v1.7.5 与剩余 fixtures；已合入 be/r010，旧 P073 两样例阻断解除。P102/P103 已被 arch 使用，本批回执顺延 P104。
- 新维度代码 c30f6f1：45 项新增行测试、22 项 summary 回归；核心模块100%行/分支，含共享检查合计97.31%行/93.47%分支。Domain765、Web140全过；不是完整维度 SQL/Registry/HTTP 实现。
- 真实 PG 已恢复，专用 ka_be_r013_20260906 首轮 DB684过/8失败。实证失败是新测试残留 typed 值阻止其他套件迁移 down，以及 account_mutes FK 阻止旧套件全表清理；不得关闭生产迁移保护/FK。
- 新建隔离合成库 ka_be_r010_20260907_test（符合 benchmark 安全命名），旧失败库保留。不使用默认共享 ka，不清理用户数据。

## Task 4：真实 PG 测试隔离修复（独立代码 SHA）

- Modify `packages/db/test/account-mute-repository.test.ts`、`changeset-repository.test.ts`、`changeset-follow-up.test.ts`。
- 每次 seed 记录本套件创建的 workspace/identity UUID（部分 seed 失败也能回收），afterAll 按精确 UUID 删除自己的依赖行，finally 关闭连接。不全表 TRUNCATE/DELETE，不改 runtime 或迁移降级保护。
- 红灯证据 `/tmp/ka-p104-db-pg.log`；绿灯须新库真实全量 DB，接着 Worker、Gateway 串行使用同库（避免迁移与其他套件同时运行）；benchmark 显式用新库名，不降低目标守卫。
- 通过后限定测试文件 commit，附数字与误判纠正：PG 连接已恢复，剩余是实际工程问题，不再 pg_blocked。

## 已交代码与实测纠偏

- `e1702e3`：3份测试按本套件UUID回收。第一次补丁误以为 execution_runs 有 workspace_id，真实PG指出后改为经 changesets JOIN 定位；失败日志保留 `/tmp/ka-p104-db-pg-fixed.log`。最终DB全量69文件/692项通过（`/tmp/ka-p104-db-pg-final.log`）。原始8失败不隐去。
- `b46ae5d`：Worker旧v2快照测试未传第三参数window reader，而v3 summary不走第二参数的普通snapshot；改为真实PlatformWindowQuery+WindowAssessmentRepository，共享RR连接；新增真实20元考核历史。并发写90后，旧请求仍10/green/旧time，新请求90/red/新time，不降低为unavailable预期。真实PG定向5项过。
- `1f4a974`：按arch F-P103-1追加仅测试原始异常观测（Error构造器Proxy，finally还原）；捕获原始 `Window reader unavailable` 与平台query堆栈，证明在查询snapshot前就失败。测试不把任何任意上游异常带进生产日志/响应。定向2项过，Worker typecheck/lint过。
- F-P103-2：当前DB与Worker vitest配置本来都有 `fileParallelism:false`。本轮问题不是必须手加maxWorkers，而是套件数据残留；不同命令仍须用独立库或串行。没有放宽lock_timeout/迁移保护，也不声称支持多进程同时回放同一数据库DDL。
- Worker首轮1140过/1旧快照失败；修后全量遇宿主ENOSPC+PG拒连，28文件失败/914过/3用例失败，不能算通过。宿主恢复3.9GiB后重跑，结果以最终回执为准；不自行清用户缓存/重启Docker。
- 最新前端合流后，本工作树node_modules缺shiki/ai/ReactFlow/BlockNote等，Web typecheck失败；Web test140过、lint0error/12warn。只读核对package已声明依赖；遵守arch本地不重装要求，不改视觉/不伪报五包类型全绿。后端三包与Gateway type/lint通过。
