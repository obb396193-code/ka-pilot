# P149 — 016 冻结DDL草稿 / 前置依赖实证

## 状态（不得扩大）

- 代码 **990c5202b96c0eeca624c68bd0be7ab738129b5a**；main c3451db 已包含，be/r010。
- **draft_sql_verified + prerequisite_blocked + claude_review_pending**，不是 registered/implemented/merged/deployed；未push。
- 草稿放 `packages/db/migration-drafts/016_contract_v1_6.cjs`，故意不放 runner 的 migrations/。没有改当前迁移链、Contract、前端或be2文件。

## 实际发现及修正路线

1. 初以为015已到位即可执行016。真实PG `runMigrations` 返回 **relation materials does not exist**；5静态通过、49PG跳过，不能称PG通过（focus.log）。事务回滚，未安装016。
2. 实读schema.sql:597–667，materials/material_analyses/product_material_experiments/material_briefs/settlement_templates/settlements/settlement_lines是014待建的基础表，不在现001–012/015中。不能把它们伪装成016已存在前置。
3. 014还需accounts的agent_type/is_ubp/claimed_by/claimed_at/closed_at/close_reason六列；防冲突文档§6要求先由arch裁归属。已在P149请求原样冻结DDL归be，不改be2保护的六个仓储/DTO文件。
4. 016退回未注册草稿；用专用数据库**事务级随机schema**，原样读取7张冻结前置表DDL，验证草稿up/down/up。该证据只证明SQL片段，不证明014/016完整迁移链可部署。
5. 013编号仍待已登记的item ID类型冲突；沙箱部署按arch规定从012等013/014齐后顺序升，不跳号。

## 当前代码与验证

| 检查 | 证据 |
|---|---|
| RED | bundle-red.log：缺016文件，5失败；focus.log：真实迁移缺materials，49跳过 |
| 冻结DDL对账 | 46语句完全同序同内容（去注释/空白/超时设置），6新表+40新列；不手抄另一套schema |
| 真实PG草稿验证 | 最终49通过：40新增列、6新表的有损down拒绝；旧行默认值保留up/down/up；三键FK/跨媒体空间；谱系方法及唯一键 |
| 静态测试 | 5通过；唯一未注册草稿、完整up和反向down、逐项保护、无越界DDL |
| 合成样本修正 | 首次隔离测试52通过/2失败，因为account_tests遗漏冻结started_at NOT NULL；补样本日期，未改DDL以迁就测试 |
| typecheck/lint | DB两项最终均通过；CJS草稿显式声明exports全局，不忽略新目录的lint |
| 遗留检查 | 只读SQL返回test_schemas_remaining=0、registered_016=0 |
| 全量门禁 | **本批未运行**；磁盘5.6GiB，小于防冲突文档8GiB阈值；不套P148的1028/1560绿灯 |

原始日志 `output/qa/p149/`：bundle-red、focus、draft-focus（52/2）、draft-focus-final（54/54）、db-typecheck-final、db-lint-final。Domain/Worker/Web无生产代码改动，未重跑，不称全链路验证。

## 安全与性能自审

- up仅冻结DDL，无业务行UPDATE/DELETE。默认source_status=unknown、data_cutoff_at/preview_status=null，不把旧数据冒充可拆片/可结算。
- down先锁全部13张受影响表再检查任何新增业务值，之后才DROP；无CASCADE。任意一处非默认值/新表行均整笔拒绝，有49个PG反例覆盖。
- **停写维护窗口**；5s锁等待/5min语句限时，不宣称在线低锁。40列守卫多次扫描旧表，规模时长尚未压测，上线需预检与窗口。
- 冻结DDL中一些新表只有workspace字段而非复合FK，不能声称自动提供租户权限；后续Repository必须按Session workspace/tuple鉴权，不能直接外露表。
- 未新增依赖、网络、凭证、媒体执行、开关启用；未清理用户缓存或其他worktree。

## 下一步

- arch裁014六个账户列归属及未齐的ad_entities六raw证据列；be可先按已冻结范围补014，bid_tool源仍不得解除unsupported。
- 014真实全链up/down/up过后，将同一016草稿正式注册并跑完整顺序/旧数据/down门禁；当前草稿不作为可部署交付。
- 随后才接R015素材/结算/治理API与BFF，完整范围不删。整信箱目标active；其他无依赖任务继续。
