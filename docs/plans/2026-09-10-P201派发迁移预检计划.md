# P201 Dispatch Migration Preflight Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在落014之前实证当前冻结DDL/部署顺序的边界，避免更改已运行迁移、跨空间外键或假报timeline源完整。

**Architecture:** 只跑本人本机合成PG探针：事务内私有schema执行原样dispatches DDL并尝试同/跨空间关联；始终ROLLBACK。复制迁移目录到专属临时目录、加不准执行DDL的014 sentinel，调用同款checkOrder=true runner验证已升015后的拒绝；不改真实migrations目录或迁移配置。

**Tech Stack:** TypeScript、PostgreSQL、node-pg-migrate、临时目录、现有冻结schema。

---

brainstorming设计检查发现需要arch裁决而非自行换编号/约束；本计划只是已授权任务的非生产诊断，不实施新产品决策。writing-plans记录步骤；用户要求本会话直接执行，不开子Agent。

1. 新建`packages/db/scripts/probe-dispatch-migration.ts`，仅显式local55432/ka_*_test；输出布尔与版本事实，无公司ID/凭证。
2. 真实PG事务执行冻结CREATE TABLE；同空间作为对照，跨空间作为负例；验证rollback后临时表与合成行均不在。
3. 临时迁移目录013空缺保留、014 sentinel；用生产等价runner配置验证当前已升015之后无法补014，失败后pgmigrations原封不动。不关闭checkOrder、不更改旧run记录。
4. 只读扫描be2 timeline，若表存在检查不代表实际UNION已接，将此问题交arch派be2。本人不改其文件。
5. 报告给arch三项：编号/既有部署升级路径；dispatches↔work_items联合FK；timeline接源条件。探针结果不是migration implemented，014启用等待冻结选择；期间其他无依赖项仍可做。
