# P181：F-P180 成员 PG 套件超时回执

## 范围与结果

来源为 live main 信箱 F-P180（arch 2026-09-09）。仅调整
`apps/worker/test/admin-members-http-pg.integration.test.ts` 的 suite options，
令九个真实 PG 用例继承 `{ timeout: 30_000 }`，避免构建争用时依赖默认 5 秒。
代码 SHA：`a0e4c01b`。没有改生产 HTTP/SQL 超时，没有删断言、跳过测试、扩大行数限制。

## 本机实跑

在 `apps/worker` 执行；PG 使用本后端独占的 localhost:55432 合成库
`ka_ci_be_r010_p178_test`，没有访问真实业务数据，也没有清其他 Agent 的库。

| 命令 | 结果 |
| --- | --- |
| `TEST_DATABASE_URL=<独占本机合成库> npm test -- --run test/admin-members-http-pg.integration.test.ts` | 1 文件 9/9；用例 1155ms，总计 3.05s |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm audit --omit=dev --offline` | 缓存审计 0 vulnerabilities；不是在线 advisory 刷新 |
| `git diff --check` | 通过 |

1000 条成功、1001 条 SOURCE_TRUNCATED、旧/撤销 session 在 repository 前拒绝、
伪造 role/workspace header、跨媒体同号、team 不读取历史 grants 等原断言均保留且实跑。
仅测试配置修改，不新增生产分支，覆盖率无新增生产模块分母。
磁盘剩余约 7.55GiB，低于 arch 全包 8GiB 门禁要求；本轮不宣称全包门禁通过。

## 状态与剩余项

- 本候选 implemented + 本机 real-PG verified，待 arch exact SHA 审查/合流；未 push、未部署。
- live main 最新信箱已确认旧 P179（`95e46310` 范围）通过主门禁并合入。
  P180 授权首轮 `d124cfce/e209e321` 仍待 arch 下一圈审查。
- P-178 全域授权自查仍未完成；工作项 task-level/纯私人双 null 与后台任务授权边界待 arch 裁决。
  不以本笔测试修复替代授权审计，也不擅自放宽 team 私人项。
- 后续仍按信箱：授权自查 → F-P179 ETL runs → P176 Full/Incr 容错与 coverage →
  F-OS-004 密码 → 021 小时源等。全部信箱目标仍 active。
- 生图保持取消，旧图片保留。无前端视觉改动、无真实媒体写。

按 arch 最新要求，只更新 R010 状态及审查信箱，不再写主管工作台账。
