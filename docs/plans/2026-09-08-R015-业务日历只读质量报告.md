# P150 业务日历只读纵切片质量报告

代码 **dc53b55**；本人 `be/r010`，交付前 `git merge main` up-to-date（main c3451db）。**已实现并定向验证，未合流、未部署、未生产验证**。无 push、无媒体写、无视觉变更。

## 本批实际范围

- 既有001 `business_calendar` → RR/RO Repository → Service → 主 `http-server.ts` / `data-api.ts` → `GET /api/v1/admin/calendar` → `/api/internal/admin/calendar` BFF。
- 直接复用冻结 `fixtures/admin/calendar.json` 字段，BFF直接导入Domain strict schema。GET只读取当前Session批准的workspace；personal/team均要求当前membership role=admin。普通角色在Repository前403，浏览器不能自报workspace/role；无账户grant也不妨碍管理员读本空间配置。
- SQL绑定workspace；10001哨兵，恰好10000完整、10001拒绝而不吐部分列表。源字段大小在SQL侧限幅，非法值不冒充null；严格日期、排序、重复ID和安全整数。超出JS安全范围的BIGSERIAL不舍入返回。
- 表没有来源时间字段，**dataAsOf=null**，不拿请求时间当数据新鲜度。businessDate仅为上海03:00业务日；selectedSource=platform。
- 仅GET；POST/DELETE等405。requestId相关性、安全错误、no-store、exact16MiB；共享注册没有动be2路由。没有日历新增/删除、阈值规则应用或管理页面，不能称整个日历/R015完成。

## 本次门禁（不是全量）

| 检查 | 真实结果 |
|---|---|
| Domain 新模块 | 13/13；typecheck/lint通过 |
| DB 新仓储unit | 14/14；typecheck/lint通过 |
| Worker 新HTTP + Session/PG | 22 + **7真实PG**通过 |
| Worker旧路由回归 | 9文件179/179；typecheck/lint通过 |
| Web新BFF | node 5/5；核心BFF及test定向tsc、4新文件eslint通过 |
| 核心覆盖 | Domain100%行/87.5%分支；DB100%行/分支；Worker Service+Route100%行/92.5%分支 |
| 依赖 | 无package/lock改动；Worker offline cached audit0，不是在线新公告复核 |
| 静态 | diff --check、限定路径提交后show --stat通过；无Contract/保护文件/真实数据改动 |

合计新定向61项，另旧回归179项。**磁盘实测4.5GiB，低于双会话规则8GiB门槛，因此本次不跑Domain/DB/Worker全量**；不能套P148或更早全绿数字。Next build、完整Web类型/浏览器门禁也未跑。

主要命令（各包目录执行，日志 `output/qa/p150/` 和工具记录）：

```sh
# packages/domain
npm test -- --run test/admin-calendar.test.ts --coverage --coverage.include=src/admin-calendar.ts --coverage.reporter=text
# packages/db
npm test -- --run test/admin-calendar-repository.unit.test.ts --coverage --coverage.include=src/admin-calendar-repository.ts --coverage.reporter=text
# apps/worker；TEST_DATABASE_URL显式指向本机55432/ka_be_r010_20260907_test
npm test -- --run test/admin-calendar-http.test.ts test/admin-calendar-http-pg.integration.test.ts --maxWorkers=1 --coverage --coverage.include=src/admin/calendar-service.ts --coverage.include=src/r010/admin-calendar-route.ts --coverage.reporter=text
npm test -- --run test/data-api-http.test.ts test/task-list-http.test.ts test/account-list-http.test.ts test/work-item-list-http.test.ts test/read-detail-service.test.ts test/session-http.test.ts test/agent-model-http.test.ts test/changeset-dry-run-http.test.ts test/account-mute-http.integration.test.ts --maxWorkers=1
# 三包分别执行
npm run typecheck
npm run lint
# apps/web
node --test lib/data/admin-calendar-bff.test.ts
npx tsc --noEmit --target es2022 --module esnext --moduleResolution bundler --allowImportingTsExtensions --skipLibCheck --strict lib/data/admin-calendar-bff.ts lib/data/admin-calendar-bff.test.ts
npx eslint lib/data/admin-calendar-bff.ts lib/data/admin-calendar-bff.test.ts lib/data/admin-calendar-server.ts app/api/internal/admin/calendar/route.ts
```

真实PG包括：personal/team各自数据、同identity切换、旧token/退出401、成员撤销403且无repo执行、三个非admin角色不能伪造header提权、实际超大字段502、10000/10001哨兵。测试只建合成identity/workspace/calendar，afterAll只删本人创建的记录，不启动Worker队列。

## 红灯和未消除的风险

- TDD最初Domain缺模块RED；首次unit lint因mock未使用参数失败，已修并重跑通过。
- 新增10000边界的**首次联合运行有1项502≠200失败**；当次没有打印error body，尚不能确定原因。只加断言诊断信息后，独立PG7、联合29、覆盖联合29连续三次通过。**不宣称已定位或修复该瞬态失败**；请arch exact复验保留关注。报告不能用最终绿灯抹掉第一次结果。
- 10000配置项有单次查询/序列化成本，但查询有15秒statement timeout、5秒lock timeout、行/字段/响应大小约束；无N+1、用户SQL、媒体/Agent调用。当前schema无workspace日期索引，本批未擅自改arch DDL；配置规模增长时可单独评估索引。
- 核心未覆盖分支仍存在，未报分支100%。未知来源时间始终null；nullable旧坏数据拒绝整次响应，不会默默过滤。

## 后续

按代码质检技能完成权限、SQL、敏感数据、边界/覆盖和依赖审计。待arch合流与磁盘条件允许后的全量复验；全信箱目标继续，013/014/016及Agent wire等已问契约不自行改。日历POST/DELETE保留待做，GET纵切片不替代完整治理功能。
