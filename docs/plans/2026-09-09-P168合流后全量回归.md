# P168 合流后全量回归 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在磁盘恢复到项目门槛后，验证 P166/P167 与 arch 新合入任务详情的组合，记录真实失败并按文件所有权修复或派回。

**Architecture:** 合 main 后在本人工作树执行测试；DB 与 Worker 使用不同的既有本机合成隔离库，串行运行各包 PG 套件。保持公开契约、媒体写开关和其他实现方源码不变。

**Tech Stack:** TypeScript、Vitest、PostgreSQL、ESLint。

---

执行技能当前不可用；按老板持续授权由本人逐步执行，不另派子 agent。代码质检技能用于审计差异、测试、覆盖及依赖，不把绿灯当部署。

## Task 1：同步与前检

- 已确认本人分支 `be/r010` 无 tracked dirty，旧 output 候选保留；main `10c26cf`。
- `git merge main` 仅信箱冲突，保留 P166/P167 与 Q018 两边原文；合并 `cbeb920`。
- 磁盘 9.0 GiB ≥ 8 GiB，允许全量；测试专库：DB `ka_be_p158_20260909_test`，Worker `ka_be_r010_20260907_test`。不使用共享 `ka`。

## Task 2：执行门禁

- Domain：`npx --no-install vitest run --maxWorkers=1`。
- DB：显式本机 55432 TEST_DATABASE_URL，`npx --no-install vitest run --maxWorkers=1 --hookTimeout=30000`；迁移套件会重建本专库 schema，仅使用合成数据。
- Worker：另一专库同命令；不启动真实 worker/beat，不调用真实媒体。
- 三包 `npm run typecheck`、`npm run lint`；离线 audit 明确仅缓存证据。
- 大输出取工具终态，不新建多份大日志。磁盘跌破门槛时不启动下一全量包。

## Task 3：失败处理与交审

- 真红先定位到生产回归/旧 fixture/残留/环境，不改期望掩盖问题。
- 本人文件修复要有红绿反例，代码独立路径限定提交；be2 文件只向 arch 提供精确证据。
- 写质量报告、R010 状态与 P168 回执，保留全信箱未完成列表；不声称 merged/deployed。

## 执行中新发现：D5 范围诊断

新合入的 be2 TaskDetailRepository 仅按 workspace 查询；追加本人审计脚本
`apps/worker/scripts/audit-task-detail-scope.ts`，只在本机 ka_be_*_test 合成随机范围建夹具，
真实 AuthSessionRepository 发 session → 原 createDataApiServer → 原 D5 handler/repository。
不替换被测的授权、路由或仓储；无关路由以抛错替身防止误触。脚本不加入默认测试套件，
发现越权退出2，结果只打印合成计数/状态；finally 清本例对象。be2 生产文件只报 arch。
