# P172 重复调度幂等修复 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 关闭F-OS-003中实际发现的scheduled tick→lease前冲突，使失败后重触发能够到达既有队列，而不是只改报错文字。

**Architecture:** 同一确定性job的身份仅比较不可变workspace/jobType/payload/priority/credentialOwner/maxAttempts；status/last_error为运行结果，不再作为幂等冲突判据。SELECT不更新状态、不清错误、不重置attempts/run_after/leaseToken；blocked_auth恢复是F-OS-002独立逻辑。

**Tech Stack:** pg、TypeScript、Vitest、真实PG。

---

writing-plans已读；执行子技能不可用，依用户持续授权直接TDD。基线be/r010@9fcd055（P171代码已交）。不改Contract/UI/媒体写。

## Task 1：先复现

- New `packages/db/test/scheduled-job-replay.test.ts`。
- 在随机workspace中 `enqueueScheduled → leaseNext → markRunning → markFailure → enqueueScheduled`，最后一步当前应抛conflict。
- 加running/leased/done/failed/blocked_auth幂等反例及payload/owner/workspace/maxAttempts篡改仍拒绝；无任何全表清理。
- 运行显式本机专用ka_be测试库，记录红灯。

## Task 2：最小代码

- Modify `packages/db/src/job-repository.ts`：去除scheduled SELECT的status/last_error比较与对应占位参数；保留其他不变。
- 重跑PG：重试run_after到期后原job可再次lease，attempts递增；未到期不lease；终态/blocked不自动复活；并发tick不产生第二个有效job。
- 运行DB typecheck/lint、相关Worker调度/once回归；code-quality-checker检查不变量、泄密、测试覆盖。
- 独立代码SHA、质量回执及状态记录，不push。

## 剩余

- F-OS-001③批次missing与ready不造假仍在收口；实读发现此确定性P0，所以先修复该小而明确的路径，避免将supervisor当未经证实根因。
- F-OS-003父子进程安全诊断还需做；F-OS-002身份绑定/补齐恢复不能直接靠此幂等修复冒称完成。
