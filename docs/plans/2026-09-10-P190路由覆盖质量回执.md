# P190 路由覆盖检查：三处真实缺口

代码 `fd16592c`。状态：检测代码 implemented，**覆盖门未通过**，不可作为全绿交付。未 push、未修改前端/业务实现。仅在 rerun 的 F-P179-Q 两问待裁期间推进独立检测。

## 机械证据

TypeScript AST（不执行源文件）扫描本人 r010 + data/http-server + 该壳明确导入的 *_HTTP_PATH 常量，实际 **20** 个路径；递归扫描 web/lib/data 非测试 TS，实际 **52** 个路径。反向加入 r014 的路由路径，避免把其他实现方的端点误判为缺失。未解析表达式 **0**。

不能只用原始正则：管理员成员 BFF 的条件 suffix、read-model 的 Kind 字面量联合、command 的相关 if 分支均真实存在。解析器测试覆盖注释不入库、正则参数、条件suffix两支、联合资源名、相关work-item/ignore vs changeset/dry-run（不产生错误笛卡尔组合）、未知资源/无界正则必须失败。

`npm --prefix apps/worker test -- run test/r010/route-path-inventory.test.ts test/r010/bff-coverage.test.ts`：**6过/2红**；5个解析单测全过，实际扫描数量/sentinel 1过，正反覆盖各1红。不是 infrastructure blocked，也没有伪报通过。

| 方向 | 路径 | 核实与后续 |
|---|---|---|
| 后端→BFF 缺 | /api/v1/system/etl-runs | GET 后端已落地，web/lib/data 没有透传；请fe补，不能豁免治理页需求 |
| 后端→BFF 缺 | /api/v1/admin/data/reconcile | 后端管理员诊断出口存在、BFF未接；请arch决定正式内用豁免或派fe接通，不由本测试假定不用 |
| BFF→后端 缺 | /api/v1/admin/members/:p/reset-password | r014/handlers已有BFF，本人后端 F-OS-004 尚未实现；依冻结队列另批实现 |

`BACKEND_ONLY=[]`：没有把缺口塞进去换绿。捕获缺口按 P190 要求单列本报告，不同提交顺手改功能。请 arch 安排对应修补之后再合绿门；若先接该检验，主门禁预期红上述2断言。

## 质量与边界

- Worker typecheck / lint / git diff check 通过；无 package/lock 改动，cached production audit0。
- 安全：只读取仓库源文件，解析不执行，未发HTTP/使用凭证/连接数据库；无生产行为改变。
- 覆盖：路径层双向下限，**不证明 method/DTO/auth/可用状态/实际部署**；比如 POST /admin/members 未实现，与 GET 共路径，本门不能发现方法差异，已在 F-OS-004 队列，绝不声称这条写路由完成。P191另做真实HTTP枚举写方法。
- 可维护性：解析器仅127行，使用已有 typescript，无第三方新增；结构样式超出已支持语法会以 unresolved 红，需维护解析器。不是对任意 TypeScript 的完备静态证明。r014 文件发现也是路径存在下限，运行时注册与响应仍须其单独测试。
- 资源：磁盘低于8GiB，不跑全量；本批无PG变更，不冒称新PG验证。

下一步 P191 viewer统一只读门；rerun等arch两点，P178 helper已经具备继续接入的条件。生图取消不恢复。
