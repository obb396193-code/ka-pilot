# R010a1 pivot2 公开查询接线

I002已交审。继续同一透视功能，不另加页面/策略对象。权威 main@f4205ce api.md:897 与 pivot2 fixtures。

1. Domain 将 account.pivot2/v1 纳入 canonical Query ID/row/source schema；严格 dimA/dimB、窗口与价格来源，成功 meta.cellCoverage 校验数量，旧查询不允许乱带 pivot 字段。
2. Registry 接 dimA/dimB/window_from/window_to/media；第一段只开放已实证 account/task/biz 与 platform。taskIds/filters 尚无完整有效日过滤证明，非空时明确拒绝，不静默忽略；team/其他维度无源明确 unsupported。
3. Platform pivot 专用只读 port 接唯一 ApprovedWorkspaceAuthContext（由 Service 保留完整判别式并缩小批准 tuple），不伪造 role、不把 team 降成个人。复用已有 reader/WindowQuery，事实 observation 生成 partial/unknown lineage，缺 computedAt 不充 ready metadata。
4. DataQueryService→guard→strict success meta→既有 HTTP；账户轴再次按批准 media/accountId 守卫，聚合授权在可信同快照 reader 校验。坏契约顶层502，截断不产生看似完整汇总。
5. 独立 TDD：canonical fixtures/缺字段/非法数值、Registry输入、恶意 port/account轴、缺源/空scope/部分覆盖/未知时钟、同HTTP只读/session回归。公开BFF schema必须同步，不把前端schema与Domain分叉。
6. data-api.ts共享结构由arch开缝；交付提供明确factory接线位置，不擅改。若仍无startup注入，只能称可调用HTTP组合已测，不能称已部署可用。PG/全量仍遵守磁盘与隔离库门槛。

分步code SHA+质量回执，保留未完成filters/额外维度/部署接线，不把基础双维等同全部策略中心。
