# R010a1 KA窗口reader候选质量报告

日期：2026-09-06。权威main@b8d2b0b/v1.7.4。仅后端候选，不是公开v3切换、合流、部署或真实KA验证。

## 范围与TDD

- b445e62：严格history/ka_daily，KA逐日现金价格按Σprice×realConversion计算，不造生效日期/历史版本。
- dc9e18f：Registry单SQL本窗与前窗成员网格，保留缺失日；10k请求上限、10001 SQL哨兵；49个Registry/真实SQLite测试通过。
- 7fc7fbe：共用受限HTTPS传输+team绑定+严格成员解码；新reader20红→20绿，后补到27例。供内部v3组合，不替换旧公开query方法。
- b988e3c汇总子批：新summary8红→8绿；同一reader快照组装加权考核、CPA、按可判定账户算比较差，today比较全unknown。另增真实SQLite SQL→reader→v3算式合成集成例。

## 检测结果

| 检测 | 命令/结果 | 说明 |
|---|---|---|
| Domain全量 | `npm --prefix packages/domain test -- --run`：630过/3失败 | 三个arch fixture缺priceSource；P068/P070已报，不改Contract或绕过校验 |
| DB纯逻辑 | `npm --prefix packages/db test -- --run unit.test.ts window-assessment-counts.test.ts --maxWorkers=1`：158/158 | 不是PG实测 |
| Worker非PG全量 | `npm --prefix apps/worker test -- --run --exclude '**/*-pg.integration.test.ts' --exclude '**/benchmark-data-pipeline-pg.test.ts' --testTimeout=30000 --hookTimeout=30000 --maxWorkers=1`：943过/2跳过 | opt-in为真实ASR/SDK外部联调；未运行 |
| 定向覆盖 | ka-window-summary/reader/plan-sqlite与ka-data-client四文件：72/72 | 新summary100%行/分支，member100%行/95.65%分支，既有client81.19%行/79.9%分支；总83.86%行/83.26%分支 |
| 类型/规范 | Domain/DB/Worker typecheck+lint通过；最终Worker再次过 | 无Contract或视觉文件变化 |
| 依赖审计 | `npm --prefix apps/worker audit --omit=dev --offline`：0 | 本地缓存审计，不宣称在线最新公告检查；依赖未变 |
| 敏感信息/差异 | `git diff --check`通过；新增文件凭证模式扫描无命中 | 只使用标明synthetic的样例，无真实账户/凭证/源调用 |
| 真实PG | 3秒连接超时的SELECT1：ECONNREFUSED55432 | 不重启其他项目，不删缓存；本轮PG门禁仍待恢复 |

日志：`/tmp/ka-window-source-domain.log`、`ka-window-reader-{red,coverage,worker-full}.log`、`ka-window-summary-{red,coverage,worker-full,db-unit}.log`。临时日志不替代本报告与可复验测试源码。

## 安全与真实性边界

1. team window必须受信workspace绑定且user为合法UUID；不解析team grants、不用个人credential兜底。来源行不接受workspace字段，workspace由可信上下文注入。
2. 固定HTTPS origin/api/query，无任意URL/SQL入口，不跟随跳转。rowCount错配、2000/10000疑似上游硬cap、10001、truncated/limit_clamped、exact16MB均SOURCE_TRUNCATED；没有假完整总数。
3. 成员要求合法日期/媒体/账户、严格有限数值与完整预期网格；重复/漏行/跨范围/伪造缺行数值均UPSTREAM_INVALID_RESPONSE；source body/token不进入外显错误。
4. cash/conv/price缺失保留未知；KA conv只作BI realConversion，缺媒体回传不造cvr/gap。预算历史尚未落014，预算使用率undefined+BUDGET_SOURCE_NOT_READY。
5. 只证明已观测账户在选定窗口的网格完整，**无法证明reader含全团队库存**；lineage.partial=true、coverage.complete=false，来源timestamp未知即null。
6. 当前和前窗一次SQL，但未做真实远端SQL执行/全量容量性能测试；查询结果10k/16MB限制不等于源端扫描成本上限。

## 未完成项与下一步

- arch更新三个priceSource fixture后重跑Domain全量；真实PG恢复后补原候选全量门禁。
- 下一步公开SourceQueryResult/v3、Platform Adapter与KA Adapter统一接上述内部窗口组合，再HTTP/BFF同步切换；目前public仍v2，不能称数据页已具备新功能。
- 继续R010a1其他query/系统端点、R010a2/R010b及R011–R016；R013 f.yml仍等OS。整个信箱目标保持active，不把本子批冒充全部完成。
