# Agent Run 会话绑定 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 当前未提供 executing-plans；按已批准队列由本会话本地逐项 TDD，不另建云项目。

**Goal:** 消除同一用户不同 Agent 会话之间串写结果；为后续消息幂等/SSE提供真实 session_id 关联。

**Architecture:** 复用012已有 agent_runs.session_id；创建用户消息与run的原事务同时保存session。completeRun在既有FOR UPDATE所有权检查中额外核对session，失配/旧null绑定不得写助手消息或改终态。没有新公开接口/表/媒体能力。

**Tech Stack:** TypeScript、pg、Vitest，专用local55432合成测试库。

---

### Task 1: 真实PG红灯

Files: 新建 `packages/db/test/agent-run-session-binding.test.ts`。

1. 同用户创建A/B，A中startRun，SQL确认session_id=A；对B completeRun(A.run)必须拒绝、B消息仍空、run仍running。
2. 旧null session run不允许偷偷归到任一会话；同会话正常完成；同run并发complete只留一条助手消息；跨用户/workspace继续拒绝。
3. Run: `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_20260907_test npm test -- --run test/agent-run-session-binding.test.ts --maxWorkers=1 --hookTimeout=30000`（packages/db）。预期先因旧代码实际串会话/缺关联失败。

### Task 2: 最小修复

File: `packages/db/src/agent-repository.ts`。

1. INSERT agent_runs 加session_id=$5，同用户消息事务提交；没有事后绑定窗口。
2. lockOwnedRun增加内部可选expectedSessionId，SQL约束`($4::uuid IS NULL OR session_id=$4)`；completeRun必传，failRun仍只改自己run的失败态，不插会话消息。
3. 不猜历史null关联，不迁移回填，不把旧success/run DTO当作新公开SSE契约。重跑 Task1 与 agent-repository.test.ts。

### Task 3: 门禁、审查回执

1. 串行DB全量与Worker全量（同隔离PG）；DB/Worker typecheck/lint；定向覆盖检查。
2. 复读diff，确认消息写入在所有权/session检查之后、事务失败回滚；无Contract/be2/视觉改动；merge main后路径限定代码SHA。
3. 质量报告、R010状态、inbox-arch追加回执另提交，明确未合流部署。

### 已发现另一个公开契约冲突（回执arch，暂不替其决策）

api.md P008冻结 `{type:session|run|delta|tool|evidence|done|error,run_id,seq,ts,data}`，fixture agent/sse-frames.json却是event+data，含context/token/diagnosis，部分帧无run_id/seq/ts，且7帧done.seq=6；run-events.json又是run_started/tool_call/tool_result等。需要arch确认“显示日志”与“续传帧”是否不同DTO，并给统一SSE样例。该冲突不阻止本批现有Repository绑定修复。
