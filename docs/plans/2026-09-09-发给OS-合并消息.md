# 发给 OS（2026-09-09，合并三件事：部署回收 / BI 管线对照 / 内网页面变形排查）

【1. 部署报告收到，三条发现全部属实，已进 runbook】
- ETL 挂在「奇航响应非 JSON」：我核了 client.ts:368，确实所有 QihangError 直接抛不重试，只重试 5xx/429。你的诊断对。已派后端修三点：非 JSON/信封不过当瞬时错误重试；重试耗尽 last_error 带 resource+参数+body 前 200 字节；单批失败只丢那一批不拖死整 job。
- 绑定启航身份：仓库确实没有任何命令能写 users.qihang_user_id，你那条 UPDATE 是唯一正确做法。后端加 seed:qihang-identity 命令，并让 blocked_auth 的 job 在身份补齐后自动重排。
- 重触发直接 "Worker once failed"：后端让失败原因打到 stderr，并给你一条自查命令。
- NODE_EXTRA_CA_CERTS 必须合并 CA：已写进 runbook §OS-1，三个 node 进程都带。请把 combined-ca.crt 的生成命令放进部署脚本。
- 系数 effective_date=2026-07-01：按「自数据起点生效」先用，不改。
- 修好我合入 main 就通知你拉；在那之前先别挂 worker 10 分钟循环。

【2. 你现在就做的三件事】
1) 重新拉代码：你部署的 30ba1a73 是旧 SHA（12 个迁移）。origin/main 现在是 e29a5c3（14 个迁移，含 015/018，CI 全绿）。git pull → npm run migrate 跑到头 → 自检 select count(*) from pgmigrations 等于 migrations 文件数（14）→ next build → 重启三进程。少这两批迁移，任务详情/日报/归属清洗等新端点全 500。
2) 团队空间现在就验：它直查 ka-data 不走 worker ETL，应该已能出数。看「数据分析」大盘 + 交叉表，截图。
3) 页面「变形」主因已定位在我们前端（壳最小宽 1290px，Windows 125% 缩放下视口只有 1093px），前端在修，你不用查布局。但要排除部署侧三项：在内网浏览器 F12 控制台粘 docs/plans/2026-09-09-内测Mac-Win适配测试计划.md §0 那段，把返回的 JSON 发我——重点看「被套在框架里」（沙箱预览是不是把页面套进了它自己的框）、「失败或空的静态资源」（_next/static 是否全 200）、「构建ID」是否等于沙箱 .next/BUILD_ID。另外「不顺畅」请帮我看：请在内网浏览器开 DevTools Network，给我四样——① 登录页有没有红色 blocked 的 images.unsplash.com（登录页两张背景图是外网的，内网拉不到，前端正在改本地图）；② /_next/static/media/*.woff2 字体请求是否 200、大概多少个；③ 任意一页 document 请求的 TTFB；④ Windows 机器的显示缩放比例（125% 时视口只有 1093px）。再各截一张 Windows 和 Mac 的账户池页。

【3. 你给的 BI 取数管线定义：已存档，和我们系统对上了】
- 我们团队空间的 BI 数 = ka-data dwd_account_daily.conv（按 media + 授权账户 + task_id 过滤后 SUM），也就是你说的 bi_conv_daily.conv；个人空间 = 启航 account_realtime 的「真实转化」列，9-5 你对平过两者点击/转化相等。我们不再算 bi_defs 那层字段选择，只保证 media + 账户 + task_id 过滤（对应你的「防跨任务污染」）。
- 数据起点 2026-08-31、T-1 媒体 08:30 / BI 11:10 这两条已进契约：8-31 之前显缺数不显 0；上午 08:30–11:10 昨天的 conv 还没到时，成本/Gap 显「待到」不显 0。
- 请确认三点：a) agent_daily → dwd_account_daily、bi_conv_daily → fact_conv_daily / dwd_account_daily.conv 这个映射是不是这样；b) BI 未到时 dwd_account_daily.conv 是 NULL 还是行不存在；c) agent_hourly 有没有通过 /api/query 暴露（我们小时盯盘目前定的是走启航 account_realtime 的 hh，如果 ka-data 小时表能查，团队空间可以多一个源）。
- 这份定义和我们 9-8 给腾讯同事的手册有三处不同（闲鱼 fst_callapp_uv/u1_uv、促活三分支、CVR 无端→wd1h），以你这份为准，手册我改。

【4. 顺序】
拉新代码 + 验团队空间 + 四样排查截图（现在）→ 等后端三修合入（今天）→ 重跑 worker:once → 账户池出数 → 挂 10 分钟循环 → 恢复演练。

【5. 内测同事开户】
现在没有自助注册（内网工具不开放注册），账号由管理员开。治理后台「新增成员 + 一次性初始密码」正在做；落地前你按 runbook §OS-3 手工开：用户名 ASCII → scrypt 追加到 INTERNAL_TEST_AUTH_CREDENTIALS_JSON → 重启 data-api → seed:bootstrap 追加身份/空间/成员 → 治理后台授权账户。先把老板点名的内测同事开出来。另外正式化要接 BUC 单点登录，请你回一句内网 BUC 的接入方式（应用接入入口 / 回调 / 拿到的身份字段），我们契约里留了 provider=buc 的位置。

