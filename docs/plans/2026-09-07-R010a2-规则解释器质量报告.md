# R010a2 复合规则解释器质量报告

## 交付状态

- 独立代码 `d5e3efe`；交审前 main@4222d4e 已同步，无新合并增量。
- 126行受限AST纯解释器：all/any/not，同节点多组AND，not=NOT(OR)，深度8/叶子128；连续业务日逐日成立；assessment_price每窗每日引用；相同metric/window/day缓存去重。
- 任一missing/error/undefined全树pass=null，any/not不能短路掩盖。Infinity只用于内部比较，输出仍RV；动态考核阈值必须有限或未知。
- 数据窗来自注入reader：省略window_hours保留null（日常当前窗口），不自填24；非24倍数窗口拒绝daily证据。额外4096观测预算防连续天展开耗尽内存；不是产品最大天数约定。
- **尚未接公开explain/规则存取/RuleScan适配/SLA/静音**，不把纯函数称作功能端到端完成；旧三种扫描规则行为未变。A1未完成项目仍保留。

## 实测

| 位置/命令 | 本次结果 |
|---|---|
| Domain `npm test -- --run test/condition-tree.test.ts`（实现前） | 缺模块RED |
| Domain `npm test -- --run test/condition-tree.test.ts test/alert-rules.test.ts test/rule-readiness.test.ts` | 初轮53；补负例后64/64（新模块35） |
| 上述命令加 `--coverage --coverage.include=src/condition-tree.ts --coverage.reporter=text` | 行/语句/函数100%，分支97.93%；只代表新模块覆盖 |
| Worker `npm test -- --run test/rule-scan-handler.test.ts` | 14/14 |
| Domain/Worker 各 `npm run typecheck`、`npm run lint` | 四项exit0 |
| Domain `npm audit --omit=dev --offline` | 缓存audit0 |
| `git diff --check` / 静态安全检查 | 过；无eval/Function/SQL/网络/凭证/环境变量/输出日志；仅Domain新文件、本人export块、计划 |

全量未跑（空间约4.6GiB，低于8GiB），PG未跑（纯函数不需PG，后续真实reader仍必须补）。不能把本次纯逻辑数字顶替P120的PG未执行。

## 质量审查要点

- 直接读取arch的三条conditionTree fixture，不另抄一套规则；额外验证嵌套NOT/OR缺数、每业务日动态阈值、infinite/undefined、全部六运算符、循环/稀疏数组/额外字段/表达式字符串/成本禁项/类型异常。
- parseTree先完整校验预算再调用reader；不支持Shell/SQL/任意脚本。reader只拿metric/window/day请求，不拿表达式。
- 缺数只得到内部不可判定，不能单凭pass=false/null关闭既有工作项；已存在readiness/SLA/通知/去重门仍要由上层组合。
- 可信reader需要按批准三键与时间窗口、实际来源粒度取数；本纯函数不替代授权或freshness验证。未开放媒体写、未push、未部署、待Claude审。
