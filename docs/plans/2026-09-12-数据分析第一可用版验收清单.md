# 数据分析「第一可用版」验收清单（2026-09-12 arch 定，三方共用）

老板的口径：**别人拿提示词 + 仓库地址，让自己的 OS/devix 部署出来，打开数据分析页就能看到自己个人和团队的真数据，能按账户昵称字段透视，页面上没有假数据。** 下面每条都要能在联调库（`ka-arch-tools/integ-smoke.sh` 四种窗口）与内网部署上各验一次。

| # | 验收项 | 归属 | 状态（09-12） |
|---|---|---|---|
| 1 | 个人空间抓数：`worker:once` 出数、`account_metrics_daily` 有行 | OS 执行 / be2 兜底 | 代码已修（F-OS-005），待 OS 重跑 |
| 2 | 团队空间取数：ka-data 窗口查询 200 | be2 | 代码已修（F-OS-006），待内网验 |
| 3 | 大盘六张卡出数，缺数窗口给部分合计 + 缺数点名 | be2 | 重做中（122574e8 待门禁） |
| 4 | 按昵称字段分组：优化师 / 目标 / 版位 出真名 | be2 ✅ + fe 默认维度 | 后端已实证；fe 改默认维度待交 |
| 5 | 自定义透视：行维 × 列维 × 指标 × 图型，含 `segment:<段名>` | be2 ⑦⑩ + fe F8-22 | 后端待合；前端已在 |
| 6 | 真实模式下数据分析页不出现任何 fixture | fe | F8-25 ① 待合（漏文件已打回） |
| 7 | 盯盘：小时数据真实来源（采样 job） | be2 Q-042 | 未开工 |
| 8 | 未开放按钮回 501 并显「一期未开放」，不再显「当前为示例」 | Codex Q-045 ① | 未开工 |
| 9 | 治理后台成员/授权可改（改角色、停用、增撤授权） | Codex | 未开工 |
| 10 | 考核价历史真数据（`/settings/change-log`） | Codex | 未开工 |
| 11 | 部署提示词照做一遍能走通（含 5b 昵称规则与重解析） | arch + OS | 提示词已复核（a9fc9992） |

**并行边界（防冲突，三方各自只动自己这栏）**

| 方 | 可动 | 不可动 |
|---|---|---|
| be2 | `apps/worker/src/data/**`、`packages/db/src/semantic-*`、`packages/domain/src/{summary-window,window-assessment,metric-value,dashboard-*,data-query-*,named-dimension,dimension-window-rows}`、对应用例与 fixtures | `apps/worker/src/r010/**`、`apps/web/**`（镜像改动先报 arch） |
| Codex | `apps/worker/src/r010/**`、`apps/worker/src/{reports,work-items,admin,materials}/**`、对应 BFF 路由与用例 | `apps/worker/src/data/**`、上面 be2 那栏的 domain 文件 |
| fe | `apps/web/**`（除 `lib/data/fixtures/real-backend/` 与 arch 热修过的镜像需报备） | `packages/**`、`apps/worker/**` |

冲突了先停手报 arch，别自己合。
