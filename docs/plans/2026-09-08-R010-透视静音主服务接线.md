# R010 透视与静音主服务接线 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 子技能不可用，按长期目标授权本地逐项执行。

**Goal:** 按arch@0d358d8的文件owner澄清，移除P133/P134仅factory可用的缺口，透视和静音进入真实启动composition。

**Architecture:** main入口注入createPlatformPivotQuery(pool)；主HTTP if链单独匹配本人静音route factory，共用原Bearer+Session解析。静音仍使用锁内复核授权的原子Repository，不执行媒体动作，不占be2的全局数组。

**Tech Stack:** TypeScript、PostgreSQL、Vitest、Node HTTP。

### Task 1：先写红测试

- `apps/worker/test/account-mute-http-pg.integration.test.ts` 从自建HTTP harness改为真正createDataApiServer+AuthSessionRepository+SessionAuthService；真实session发放后请求两静音路由，补无cookie/无bearer与注销拦截。保留3项真实事务/同号跨media/撤权反例。
- 运行该文件，预期主壳尚未注册静音导致404。

### Task 2：接线

- `src/data/http-server.ts` 加可选accountMuteService；匹配本人route并在已批准Session之后调用，不占r014。
- `src/data-api.ts` 注入AccountMuteService/Repository，并给PlatformDataSource添加createPlatformPivotQuery(pool)第5参。
- 未配置服务不能伪造成功；media写/Job仍无注册。纯ignore尚未实现时保留503。

### Task 3：真实启动与回归

- 新真实进程PG测试：运行src/data-api.ts，使用合成隔离DB的服务端session，HTTP查询pivot2确实读SQL+筛task，账户静音确实落本人三键，越权/注销拒绝。关闭KA/env不影响两路。
- 定向HTTP/PG/原D6/Session/查询测试+Worker type/lint/覆盖/离线audit。空间<8GiB不跑全量且如实列出；独立代码SHA与P136交审，不push。
