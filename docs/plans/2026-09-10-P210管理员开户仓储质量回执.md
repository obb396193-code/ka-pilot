# P210 管理员开户仓储质量回执（2026-09-10）

## 状态

候选代码 **48d84bcf**，F-OS-004 Task2。实现数据库内核，**未接 Service/HTTP、未合流、未部署、未push**；旧成员HTTP/局部grants仍保留。生产无入口调用新仓储，不接真实账号/凭证/媒体操作。上一轮P209 Task1代码f38a88fa已交；本轮采用 writing-plans/code-quality-checker。

## 实际实现

- 新 `packages/db/src/admin-member-provisioning-repository.ts`：先严格解析ApprovedContext，再反查当前live workspace/user/member/identity；查同identity任一active非demo team admin。当前personal role可以是optimizer但必须真有team管理资格；personal admin本身不授全局管理权。viewer/guest拒绝。写事务锁住授权依赖，team grants不参与授权。
- create：同client原子写 identity → personal workspace → workspace-local user → membership → password；新用户不加入管理员personal空间、不建team成员、不复制奇航身份/Secret/grants。初始密码16位随机，或接受请求已冻结12..1024位；BUC不写密码。
- 重复 provider_subject 按v1.9.21原文字面跨provider也409：本入口事务级advisory lock串行同名请求，然后查重；DB原有(provider,subject) UNIQUE保留，不改schema。其他内部导入路径是否采用同名全局策略未修改，不声称新增了全库唯一约束。
- reset：FOR UPDATE锁目标identity；只internal_test且active。写新password和撤销全部未revoked session同事务。缺目标NOT_FOUND、BUC/停用目标CONFLICT；任何失败回滚，无明文日志/error detail。
- read：RR/RO同快照全局personal成员列表，非active仍显示isActive=false；一个identity多personal行不任取第一行，由strict重复校验拒绝；1001行sentinel。grantsCount只算目标personal未撤销tuple，team legacy grants不计。只查密码是否本人已修改，绝不读取哈希/明文；本轮没有改变旧局部grants子接口。
- 复用 `identity-password-repository.ts` 的setPassword，仅加第四个可选executor（默认pool保持兼容），新开户/reset传事务client；未复制scrypt参数/算法。生产新模块140行，不增依赖。

## TDD与真实证据

1. 首次新测试导入新仓储不存在而红（无tests，不算行为红）。实现后首13项绿。
2. 增补“两个provider同名并发”的真实PG反例，**两次均fulfilled**违背409，真实红；加入事务级锁/同名检查后只一成功、一CONFLICT。
3. 密码写入失败：故障注入共享password方法，实际PG已写identity/workspace均回滚。reset在password SQL成功后注入session UPDATE失败，实际旧password仍有效、原session未撤销。不是只assert调用顺序。
4. 新19项PG套件含实际创建/并发/撤权/换密码/list1001，也含before-IO参数拒绝与故障注入，不把每一条都说成纯上游真调用。真实表来自既有migration；测试只写随机前缀合成identity/workspace，结束精确清理该suite的数据。

## 门禁（exact候选前最后一跑）

隔离库显式 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test`，未用共享业务库、没启动消费者。

```text
DB npm test -- --run test/admin-member-provisioning-repository.test.ts
  test/admin-member-provisioning-repository.unit.test.ts test/admin-members-repository.unit.test.ts
  test/r014/must-change-password.test.ts --coverage
  --coverage.include=src/admin-member-provisioning-repository.ts --coverage.include=src/identity-password-repository.ts
55/55（19+16+16+4）
DB npm run typecheck && npm run lint && npm audit --omit=dev --offline
通过；缓存audit0
Worker TEST_DATABASE_URL=同隔离库 npm test -- --run test/internal-test-login-provider.test.ts
  test/r014/password-routes.test.ts test/admin-members-http.test.ts
29/29（4+7+18），Worker typecheck/lint通过
```

总84项不同测试。Worker7项是PG+模拟HTTP handler，18项是旧成员HTTP回归；**尚无新管理员端点的真实登录→重置→401 HTTP闭环**。

覆盖首跑因只测新用法，共享密码模块未跑change导致总行75.7%不达门；补真实自助改密后重跑，**未调低门槛**。最终新仓储行100%/分支94.68%，共享password行97.39%/分支65.78%，合计行98.63%/分支86.36%。后者未覆盖主要旧校验/错误分支，不虚报每个模块100%。

静态：SQL参数化，动态SQL仅常量FOR SHARE开关；无日志/凭证字段回传（仅成功的一次性initialPassword）；输入与输出strict；timeout/unique/未知DB错误固定映射，connection正确释放。diff --check通过。磁盘约2GiB低于arch8GiB门，未五包全量/build、未清理，缓存audit不代表实时联网扫描。

## 下一步

Task3切入新全局Service并装配POST create/reset、GET新列表；移除旧route对current role=admin的过早推断，让真实全局授权生效，同时保留viewer统一403与Session门。补201/409/requestId/字节上限/真实登录改密重置闭环。随后024→025→readiness→sop-run→P176 Task3；当前信箱目标仍未完成。
