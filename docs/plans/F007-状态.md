# F007 状态（前端 fe · 分支 `fe/f006` 续 · 基线 = main `cd46c00` 已 merge 进分支 `724843e`）

> 目标（老板 2026-09-05 口头 + arch F-007 提示词）：按 `docs/relay/F-007-全站页面清单.md` 全站一次铺开，连续做，不等审；老板逐页精修。
> 本地起法：工作树 `/private/tmp/ka-fe-f006/apps/web`，mock env 同 F-007 提示词，**端口 3401**（3400 被退役 Codex root 会话的 dev server 占着）。
> 老板拍板 D1–D13 冻结在 `docs/plans/F006-状态.md` 顶部；与 F-007 清单冲突处以老板为准，冲突点列在下面「冲突点」。

## 冲突点（F-007 清单 vs 老板已拍板；已按老板执行，请老板扫一眼）

| # | F-007 写的 | 老板拍的 | 我怎么做 |
|---|---|---|---|
| C1 | 登录 = shadcn login-01～05 多候选给老板挑 | D9：账号密码为主 + 分屏 + 大厂式品牌图（图由 Codex 生 Q5，图到前用光谱） | 按 D9，候选页仍在 `/login/candidates`、`/login/directions` |
| C2 | 账户池详情 = 右侧 Drawer；全站详情统一抽屉 | D7：行内展开 + 保留「打开完整账户页」 | 账户池按 D7；工作项/任务详情本来就是页 |
| C3 | 账户池顶部 = **九态库存流水线**（契约 v1.5.1 `poolStatus`：可用/已分配/待开户/待充值/待搭建/投放中/暂停/已关/异常）+ `lifecycleStage` 另一字段 | D8：老板口述八档（基建/等待/冷启动/起量/稳定/掉量/待关/已关）混了库存态和生命周期 | **按契约拆成两层**：顶部分层 = 九态 poolStatus（fixture `accounts/pipeline.json`）；生命周期（冷启动/起量/稳定/衰退）= 表里一列 + 筛选。老板的三种表达（分层卡/流程条/看板）保留，作用在九态上。**请老板确认叫法** |
| C4 | 端口 3400 | — | 3401（3400 被占） |
| C5 | `lib/data/` 归后端 | 我 F-006 期间改过 `lib/data/mock-data.ts`（补 21 户）、加了 `lib/data/account-lifecycle.ts` + fixture | 已在 inbox-arch 报备；F-007 起不再动 `lib/data/`，新 mock 全走 `lib/fixtures/`（前端自己的 fixture 读取层，直接 import `@contract/fixtures/*.json`） |

## 底座（F-007 期新增，全站复用）

| # | 项 | 状态 |
|---|---|---|
| B1 | `@contract/*` 路径别名 → `packages/contract/*`；`lib/fixtures/contract.ts`：MetricValue/RatioValue 三态取值（缺数 −）、三色只从 DTO 拿 | ✅ |
| B2 | `components/business/state/page-state.tsx`：八态壳 `StateFrame` + `?state=` 切换（mock 期页头 Select）+ 「示例」角标 `ExampleBlock` | ✅ |
| B3 | `components/business/tabs/page-tabs.tsx`：`?tab=` 视图收敛 | ✅ |
| B4 | 页头常显五件「空间 · 来源 · 数据日期 · 更新时间 · 口径 ⓘ」+ 三色（`system/health.json`；黄/红时整页顶部条）| ✅ |

## 页面（顺序 = 老板精修顺序）

| # | 页 | 壳 | 八态 | fixture 接 | 交互 | commit | 老板精修 |
|---|---|---|---|---|---|---|---|
| 0 | 登录 `/login` | ✅ (F-006) | 401 | session-http | 登录/切空间/退出（mock） | `449ccec` | 等图（Q5） |
| 1 | 数据分析 `/data?tab=` 七 tab（大盘 / 数据总表 / 维度透视 / 盯盘 / Gap / 策略分析 / 自助报表） | ✅ | ✅ `?state=` | table-v3 · summary-window-v3 ×3（页内样例切换）· trend-v3 · dimension-v3 + unsupported · hourly · watchlist · gap · pivot2 · views · config-v1 · render · exports/queued | 窗口预设 · 媒体/账户筛选 · 自定义列 · 存列→个人视图 · 导出→队列 toast · 8 维切换 + 勾选分析 · 盯盘名单增删 · Gap 分组 · 策略预设/自定义两维 + 勾格分析 · 报表四步向导 + 定时推送对话框 | 见下 SHA | 待老板 |
| 2 | 账户池 `/accounts?tab=`（账户池 / 基建管理 / 开户测试）+ 详情 `/accounts/[media]/[id]` | ✅ | ✅ `?state=` | accounts/pipeline · list-v151 · detail · timeline · overlay · structure · open-flow · tests · transfer · replicate · replication-compare · changesets/group-preview · infra/requests · trend-v3（账户级趋势占位） | 九态流水线三表达（分层卡 / 流程条 / 看板，点态即筛）· 搜索 / 渠道 / 产品名 / 负责人 / 生命周期 / 星标 筛选 · 分组（生命周期 / 产品名 / 负责人 / 任务）· 行内展开小传（同一种 KPI 卡）· 批量操作 → 变更集组预览（dry-run → confirm）· 新建账户向导（open-flow）· 转移负责人 · 改库存态 / 改产品名 · 优质户复制（方案 + 母子并排）· 加入盯盘 · 基建管理（示例态：队列 + 矩阵表单 + 状态机）· 开户测试（新建 / 填结论）· 详情：小传 fund 七字段 + 断量 + 趋势打点 + 计划层树（垃圾计划勾选 → 关停变更集）+ 操作史 T+1 | 见下 SHA | 待老板 |
| 3 | 工作台 `/`（我的 ⇄ 负责人；`?tab=today|collab`） | ✅ | ✅ `?state=` + 队列七样例 + 早报 ready/pending | summary-window-v3 · trend-v3 · work-item-list ×7 · work-items/detail + actions · changesets/detail · workflows/runs-running · alerts/stream + roster + escalations · reports/daily-brief ×2 · collab/dispatches + approvals · workbench/lead | 六卡（待处理 = 队列计数）· 队列 tabs 全部/P0/P1/机会 + 四件套卡（证据对话框：真值表 + 诊断 + 分级决策；变更集草稿 dry-run/确认；忽略原因 chip；静音 N 天）· 覆盖三数（只有 pending=0&&undeterminable=0 才写「其余 N 户在阈值内」）· 待确认变更集 · 运行中工作流步骤点 · 警报监控（值班/P0 未确认/升级中 + 确认）· T+1 回收 · 我的待办 · AI 早报（pending_data 不造假）· 协作 tab：派发回执（不同意合法）/ 提审批准驳回 + 自动通过规则 / 升级链 · 负责人视图六卡 + 风险机会 + 团队阻塞 + 需要拍板 + 经营简报（差距树 / 观察结果 = 示例） | 见下 SHA | 待老板 |
| 4 | 投放任务 `/tasks` + 详情 `/tasks/[taskId]` 八页签（`?tab=overview|data|accounts|materials|sop|issues|timeline|reports`） | ✅ | ✅ `?state=` + 样例切换 v1.5.1 / TASK-LIST-001 ready·partial·stale·empty | tasks/list-v151 · task-list ×4 · task-detail/overview-v151 + overview-no-cap · tasks/metrics + funnel + accounts + timeline · settings/change-log · rules/list · work-item-list/coverage-complete + work-items/detail | 列表：母版 DataGrid（任务｜业务 chip｜阶段七态 chip｜就绪度六段小条 hover 缺项｜目标/已完成｜达成 · pacing 双进度条｜考核价｜成本状态三色｜RTA｜投放位置｜账户数｜周期｜负责人｜里程碑｜编号 + ⋮ 查看/问 AI/关注/编辑(禁)/归档(禁)）· tabs 全部/进行中/准备中/已结束 + 所有/我负责的/关注 · 右栏 任务分布(按阶段)/健康分布(三色+不可判断)/即将到达的里程碑 · 新建任务禁用（R-010）。详情：头 = 名 + 阶段 chip(含 source) + 成本状态 chip + 一行元信息（业务/周期/预算/负责人/RTA/版位/costStatusReason）；总览 = 六 KPI（目标量/已完成+达成/现金 CPA vs 考核/成本空间/日预算卡+用量/待处理 P0P1）+ SOP 六步条（done/running/pending + 未绑 run 时「起 SOP」）+ pacing 卡（达成/时间双条 + 已过/总天 + 预计完成缺口 + 需日均/7 日均 + 外推 CPA + 可承受）+ 就绪度环 + 六段 + 缺项列表「勾就绪」+ 阻塞（P0/P1 → 去处理）+ 下一步（只来自真实来源）+ 考核价卡「改价」/日预算卡「改卡」对话框（金额/生效日/证据/备注，回溯改提示重算）+ 变更记录 DataGrid（从/到/重算 N 日/证据）；数据 = 六 KPI（周同比来自 compare）+ 趋势双轴 + 漏斗在线/离线（离线缺 = −）；账户 = 挂载账户 DataGrid（库存态/生命周期/有效期/达标/日卡/容量）；商品与素材 = 501 示例；SOP 与自动化 = 步骤列表 + 打开运行详情 + 绑定规则(rules/list，未触发原因人话)；异常与工作项 = 复用工作台四件套卡；时间线 = 五源倒序；报告与结算 = 501 示例入口；置阶段对话框(stage_source=manual) | 见下 SHA | 待老板 |
| 5 | 自动化 `/automation`（`?tab=official|mine|team|rules|runs|capabilities`）+ 画布 `/automation/workflows/[id]`（`new` = 空白）+ 运行详情 `/automation/runs/[id]` | ✅ | ✅ `?state=` 三页都有；团队共享空态 | system/health（五卡）· workflows/definitions + graph-v1 + validate + simulate + runs + run-detail · rules/list + explain · capabilities/list · agent/runs + run-events | 顶部健康五卡（连接器/执行器/队列/Agent/健康分）· 模板卡（版本/风险 chip/耗时/成功率/使用次数 + 打开画布 / 从模板创建）· 我的（copiedFrom）/ 团队（空态解释）· 规则 = 母版 DataGrid（名称｜类型｜触发条件人话｜下次评估｜启用 Switch｜**为什么未触发** chip → explain 对话框叶子真值表 + fallbackCopy｜近 7 天 触发/成功｜自治度 Slider 三档 只提醒/出草稿/自动执行｜负责人 + ⋮）· 运行中心 = DataGrid（状态六态 chip/步骤 Progress/ETA 缺 −/发起人）+ 状态 tabs + Agent/OS 运行监控 DataGrid（tokens/花费/产物 → 工作项；事件对话框受限日志）· 原子能力 = 能力卡（类别/状态/执行器/媒体/权限）+ form_schema 渲染表单（enum→Select，number，array→逗号）→ write 类只出变更集草稿 toast，disabled 灰 · Agent 帮编 = 示例块 · **画布** = React Flow（@xyflow/react 12.8.4）：左栏节点库十类可拖拽/点选（写媒体红点/外部黄点），自定义节点（类型图标 + 执行身份 + 副作用/幂等 chip + 缺参 chip），分层自动布局，边条件表达式作标签，右栏属性面板（名称/类型/执行身份/副作用/幂等/重试三项/超时/权限范围/参数 JSON + 删除），已发布只读 → 「基于 vN 新建草稿」解锁编辑，本地红规则 = write 前无 human_confirm / write·external 非 key_required，校验对话框（结构/权限/连线三组 + 缺参 + canPublish）、模拟运行对话框（ok/skipped/would_wait）、发布按钮只在四组全过且缺参空且本地红点 0 时可点 · 运行详情 = 头（版本不可变/状态/发起人/执行身份/任务链接）+ 确认执行（权限未过或账户锁冲突禁用并给原因条）/拒绝/重新预览 + 阶段条 + 节点日志表（input/output 摘录）+ 变更集预览（hash/到期/风险）+ 权限校验 + 账户锁（冲突 → 红 chip 含 locked_by）+ 审计 + 重试/回读策略 + UNKNOWN 说明 + 链路三 id | 见下 SHA | 待老板 |
| 6 | 报告 `/reports`（`?tab=business|daily|review|settlement|templates|schedules|ai`） | ✅ | ✅ `?state=`；结算 blocked/ready 样例可切；导出 queued→done | reports/library + daily-v1 + render + config-v1 · exports/queued + done · settlements/template + preview-blocked + preview-ready + frozen · integrations/subscriptions（非 alert 部分） | 顶五卡（本月生成/待核对/定时/已分享/归档）· 经营报告 = 报告库 DataGrid（名称/类型/所有者/期间/模板版本/数据状态/投递状态 + ⋮ 打开/导出/推群/问 AI）+ report-config/v1 渲染卡（分组 × 列 + 高亮规则红行 + lineage 页脚 + 导出 xlsx queued→done 签名链接失效时间 + Agent 帮做表）· 日报 = daily-report/v1 12 模块左导航 + Executive Summary 六卡 + 异常列表；模块三态（有数 / 无数据不用 0 代 / UNSUPPORTED）+ 健康度 p1_pending；角色切换（fixture 只 optimizer，切换标注）；导出 PDF / 一键发钉钉按 actions 开关 · 任务复盘 = 示例块（7.2 P2）· 结算单 = 四步向导：① 模板版本（v3.2/v3.1，新版本=新行）+ 期间 + 样例；② 字段映射表（类型/汇总/来源 fact｜公式人话/必填/可校正）+ 校验规则容差；③ 校验预览（行表 ƒ 标公式、校验 chip + 差异、合计行）+ 问题清单（block/warn；allowCorrection 字段给「校正」对话框留痕，否则提示回源）；blocked 红条不许冻结；④ 冻结生成（frozen 快照/指纹/确认人 + 结算行「差异转工作项」）+ 分发（冻结后才可导出 xlsx / 推群）· 模板 = 结算模板 DataGrid + report-config 列表 · 定时任务 = subscriptions DataGrid（类型/目标/计划人话/免打扰只压 P1P2/启用 Switch/立即发送）· AI 提效 = 示例块，用词「操作后观察结果」，数值全 − | 见下 SHA | 待老板 |
| 7 | 集成与通知 `/integrations`（`?tab=connections|assistant|subscriptions|cards|oncall|messages`） | ✅ | ✅ `?state=` | integrations/connections + identity-mappings + subscriptions + cards + card-callbacks + messages · alerts/policies + roster + escalations | 接入管理 = 连接卡（钉钉：连接/健康 chip、clientId 脱敏、机器人、群、探活/测试消息；断连红边）+ 新增接入（飞书/企微置灰）+ 身份映射 DataGrid（外部 ID / 实名验证 chip + 发起验证 / 解绑）· 群助手 = 示例块（查数 / 建任务 / 调工作流三指令说明）· 推送订阅 = subscriptions DataGrid（含 alert；配置人话 / 免打扰 / 启用 Switch / 立即发送）· 卡片中心 = L0–L3 模板卡（动作 chip + hash 校验标）+ 卡片实例 DataGrid（状态/变更集/hash/到期/发往 + 重发）+ 回调记录 DataGrid（动作/操作者未映射标/hash 校验通过·不匹配/结果红字/幂等键）· 值守告警 = 分级策略表（破静默/确认时限/升级链人话 + 免打扰）+ 值班表（主/备班 + 换班）+ 升级链（L1/L2 状态 chip + 暂停/恢复 Switch）· 消息记录 = DataGrid（方向/类型/对象/状态五态/尝试/失败原因/完成/关联 + 出站 failed 重试 · 入站 dead admin 重置）+ 全部/出站/入站/失败 dead 筛选 | 见下 SHA | 待老板 |
| 8 | 知识库 `/knowledge` + `/knowledge/[id]`（同一页，选中 ↔ URL） | ✅ | ✅ `?state=`；团队空间只读 | kb/tree + document + search | **复制 ContentRadar**（/Users/aik/cr-r128 只读）：`knowledge-editor.tsx`（BlockNote 0.51.4 + zh 字典 + R-032 `@` 触发；双链内联块 wikilink{itemId,title} + 业务对象 mention{refType,refId,label}；打字停 500ms 整篇保存 → 本地 store，接口后换 PATCH）· `knowledge-tree.tsx`（react-arborist 3.13.2 + fractional-indexing 4；拖拽 = 移动/排序 position 只改一行；右键 = 新建笔记/SOP/案例、重命名、删除（非空不能删）；行内改名；kind 标签 + 反链数 ⇠N）· `knowledge-page.tsx`（左树右文、搜索 = FTS 本地过滤 + kb/search 命中分数、窄屏 master-detail、kb-navigate 事件跳双链、`/knowledge/[id]` replace 同步）· 正文下：关联业务对象 chip（business_refs → 任务/账户详情）、出链、反链；修订号/改动人/时间；contentJson（ProseMirror doc）→ BlockNote 块转换 `docToBlocks`；编辑器/树 `dynamic ssr:false`（BlockNote 依赖 DOM，SSR 会挂死） | 见下 SHA | 待老板 |
| 9 | 商品素材 `/materials`（`?tab=products|materials|analysis|replication|experiments|aigc`） | ✅ | ✅ `?state=`；整页示例态（角标 + 解锁条件） | materials/list（cards）+ products + detail + analysis + similar + lineage + brief + backtest + experiments | 顶六卡 · 商品池 = DataGrid（状态 chip/类目/价格带/素材数/消耗/曝光/真实转化/CTR/真实 CPA）+ 商品 × 素材效果矩阵（experiments cells，样本充足/不足）· 素材池 = 卡片库（缩略占位 + 类型/复刻角标 + 时长；来源 / 视频源可达 chip / 拆片状态 chip；真实 CPA / CTR / CVR（DTO 无 → −）/ 消耗 / 曝光 / 真实转化；标签；看拆片 / 拆片（不可达禁用，409）/ 复刻）+ 商品 / 类型筛选 · 拆片分析 = 素材下拉 + 面板：预览（时长/分辨率/指纹）、关键帧墙（ready 占位 / 抽帧失败虚框 + 视觉摘要四数）、段落时间轴（角色分段按时长比例）、内容拆解（钩子类型 / 卖点 / 人群 / 节奏 / CTA，每条 evidenceIds → 口播 / 镜头 chip）、口播（whole_video 不显句级时间戳）、相似素材（六维分量，证据不足不打分）、谱系 + 用在哪（任务 / 账户链接）；未拆片 / 不可达给诚实说明 · 详情抽屉（Sheet）复用同面板 · 复刻任务 = 谱系（源 → 变体 method chip，指标缺 −）+ 回测就绪三态（等交付 / 等样本 / 可回测 + 样本不足 id）+ 设计 brief（目标 / 全局约束 / 变体表 改动维度·假设·指令·保持 / 状态 / 指纹 + 发送 + 登记交付对话框）· 测品复盘 = 实验策略阈值 + 每商品矩阵（账户/天、曝光、点击、真实转化、消耗、CTR、推断转化率 + 95% 区间、真实 CPA、样本状态 + 排除原因 hover）+ 结论人话（样本不足 / 候选不足 … 不出结论）· AIGC 下单 = iframe 占位（内网） | 见下 SHA | 待老板 |
| 10 | 设置 `/settings`（`?tab=credentials|notifications|workload|metrics|views`；入口 = 侧栏用户菜单「设置 / 个人与授权 / 通知偏好」） | ✅ | ✅ `?state=`；口径 team 只读 | settings/credentials + channel-coefficients + decision-policy · me/views + watchlist · integrations/subscriptions · settings/change-log | 三凭证 = 三卡（奇航 / Multica / IdeaLab：绑定状态 chip + 脱敏引用 + 绑定时间 + 未绑影响说明；绑定对话框 password 输入不回显；解绑）· 通知偏好 = 免打扰时段（只压 P1/P2）+ 订阅 DataGrid（类型/送到/内容人话/免打扰/开关）· 我的负载 = 示例块（4.9 P1，五格全 −）· 口径 = 返点系数 DataGrid（公式人话 × / ÷、生效、改动人、历史版数、证据；改系数对话框新版本 + 回溯重算提示；团队空间只读 chip + 禁用）+ 统一变更记录（三表 UNION，任务链接）· 个人视图 = saved_views DataGrid（页面/列数窗口排序/共享/更新 + 打开/分享/删）+ 关注账户 chips | 见下 SHA | 待老板 |
| 11 | 治理后台 `/admin`（admin 才显；`?tab=members|etl|calendar|flags|assets|diagnostics`；非 admin 显锁页） | ✅ | ✅ `?state=`；非 admin 403 页 | admin/members + grants + calendar + flags · system/etl-runs · integrations/connections · settings/channel-coefficients + decision-policy · assets/list · data-query/reconcile-pending | 成员与授权 = DataGrid（角色 chip/在职·已停用/身份源/授权账户数 → 授权对话框 read·execute 撤销·新增/加入/最近活跃 + ⋮ 改角色 / 停用(撤 session 行不删) / 恢复）· 连接与 ETL = 连接健康卡（钉钉 / 奇航补拉中 / ka_data D-1）+ etl-runs DataGrid（任务类型/业务日/状态含 failedStage/开始结束/raw·canonical 行数/警告 BLOCKED_AUTH + 重跑 + 按日补拉）· 口径与日历 = 运营日历表（类型/影响基线/阈值档 + 加事件对话框）+ 返点系数只读汇总 + 分级决策策略四阈值 · 灰度开关 = 五开关（write_enabled 等，每个带后果说明，Switch + 状态 chip）· 资产 = DataGrid（类型/状态机 chip/所有者/版本/范围/成功率/使用/依赖 + ⋮ shared→verified / verified→official / 弃用须 superseded_by）· 诊断 = 对账两源并排表（ka_data 权威 vs 奇航对照；comparison 引擎未就绪 → 前端不算差值只并排） | 见下 SHA | 待老板 |
| 12 | 全局 Agent 抽屉 + ⌘K（所有页；光晕球 B） | ✅ | 空态 / 回放中 / 完成；接口未接 → 「fixture 回放」角标 | agent/session（上下文四 chip）+ sse-frames（七帧）+ suggestion（accept 结果）· system/search（⌘K 对象 + 最近） | **AI Elements 官方源码**（components/ai-elements：conversation / prompt-input / suggestion 原有 + 新拉 message / tool / reasoning / sources / confirmation / code-block / shimmer；import 路径按 CLI 规则改为 @/components/*，源码不改）· 抽屉：上下文 chips（页面 / 任务 / 账户 / 时间 / 异常 / 空间，可移除，+ 账户下拉）· 发送 = 按契约七帧回放：session → context → token（逐字流入 MessageResponse）→ tool（Tool 折叠：query.account_summary 输入 / 输出）→ diagnosis（结论 + 证据 ref 表 + 四建议动作：生成分析表 → 报告页 / 创建待办 / 形成调价变更集 → 工作台待确认 / 保存视图）→ suggestion（**Patch 卡**：diff 逐路径勾选，全部接受 / 只接受勾选 / 拒绝 → accept {mode, paths} 新版本）→ done（tokens 用量 + 运行记录链接）· 高频问题 Suggestions · 模型选择器（网关分配，不伪造清单）· ⌘K：搜索时「对象」组（system/search items，类型 chip + id）、空查询「最近访问」、页面九项、动作（切空间 / 颜色模式）、无命中 → 问 AI 带 query 唤起抽屉 | 见下 SHA | 待老板 |

## TODO-fixture（缺的按 api.md 自造最小 mock，arch 补）

- `data-query/dimension-v3-{task,biz,account,agent_type,deduction_range}.json` —— 只给了资源位一维；前端自造最小 mock（只有维度值，指标全 −），标「示例」
- `data-query/gap-{task,biz}.json` —— 只给了按账户；按任务/业务显 TODO 空态
- `data-query/pivot2-{bid_tool-task,biz-resource_position}.json` —— 只给了版位×任务；出价工具维 UNSUPPORTED，业务×版位显 TODO 空态
- `accounts/detail-<id>.json`、`accounts/timeline-<id>.json`、`accounts/structure-<id>.json` —— 只有 account-1；其他账户详情页显诚实空态
- `accounts/trend-<id>.json` —— 账户级 account.trend 样例缺，详情页趋势暂用 trend-v3 并注明
- `tasks/list-v151.json` 只有 2 条（进行中 1 / 准备中 1 / 已结束 0），「已结束」tab 与「关注」tab 空；`saved_views` 关注列表无 fixture，关注按钮只 toast
- `rules/bindings-<taskId>.json` —— 任务绑定的规则/工作流无 fixture；详情「SOP 与自动化」暂显 rules/list 全部启用规则 + 官方模板一行
- `rules/explain.json` 只有规则 3 · account-5 一份；其他规则的「为什么未触发」对话框显诚实空态
- `agent/run-events.json` 只有 …1801；失败的 …1802 无事件样例
- `workflows/graph-<id>.json` 只有 …1701（新任务开户到基建）；其他模板打开画布都显示这一份并注明
- `reports/daily-v1.json` 只有 optimizer 角色、12 模块中只有 executive_summary 有数（其余 rows 空）；角色切换只标注不换数据
- `reports/library.json` 只 2 条；任务复盘 / AI 提效无 fixture（P2）
- `integrations/connections.json` 只有钉钉一条；断连 / 降级态靠 `?state=` 演示
- 群助手（联调项）无 fixture；示例态
- `kb/document.json` 只有「新任务开户到基建 SOP」一篇正文；其他树节点无正文（页内注明，可本地编辑）；无 revisions / by-object fixture
- `materials/list.json` 无 cvr 字段（清单要 CVR）→ 卡片显 −；缩略图 / 关键帧只有 asset:// 引用无真图 → 占位块；无 deliveries fixture
- `admin/grants.json` 只有第一位成员；「我的负载」（4.9）无 fixture；reconcile comparison 引擎未就绪（fixture 即如此）
- `agent/sse-frames.json` 只有一段回放（无 reasoning 帧；contract 七帧无 reasoning）；`system/search.json` 只 2 个对象

## 阻塞

（暂无）

## 提交 SHA（fe/f006，未 push）

| 页 | SHA |
|---|---|
| 0 登录（F-006 存档点） | 449ccec |
| 底座 + 1 数据分析 | a4fcbc9 |
| 2 账户池 | b2b70da |
| 3 工作台 | b747fb0 |
| 4 投放任务 | b5c4ad4 |
| 5 自动化 | 4ed0924 |
| 6 报告 | 50f6dff |
| 7 集成与通知 | fcddbea |
| 8 知识库 | e81c17e |
| 9 商品素材 | 32653be |
| 10 设置 + 11 治理后台 | 5cf2227 |
| 契约缺口 G1–G9（inbox-arch） | 9cca12d |
| 12 Agent 抽屉 + ⌘K | 13e624c |

## 事故记录

- 2026-09-06 机器重启清空 `/private/tmp`，fe 工作树丢失；commit 全在主仓，重建 = `git worktree prune && git worktree add /private/tmp/ka-fe-f006 fe/f006 && npm ci`。页 12 当时未提交的改动按会话记录重做后提交（13e624c）。教训：每页做完立即 commit；是否把工作树搬到持久路径请老板定。
