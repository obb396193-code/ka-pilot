# F007 状态（前端 fe · 分支 `fe/f006` 续 · 基线 = main `cd46c00` 已 merge 进分支 `724843e`）

> 目标（老板 2026-09-05 口头 + arch F-007 提示词）：按 `docs/relay/F-007-全站页面清单.md` 全站一次铺开，连续做，不等审；老板逐页精修。
> 本地起法：工作树 `/private/tmp/ka-fe-f006/apps/web`，mock env 同 F-007 提示词，**端口 3401**（3400 被退役 Codex root 会话的 dev server 占着）。
> 老板拍板 D1–D13 冻结在 `docs/plans/F006-状态.md` 顶部；与 F-007 清单冲突处以老板为准，冲突点列在下面「冲突点」。

## 冲突点（F-007 清单 vs 老板已拍板；已按老板执行，请老板扫一眼）

| # | F-007 写的 | 老板拍的 | 我怎么做 |
|---|---|---|---|
| C1 | 登录 = shadcn login-01～05 多候选给老板挑 | D9：账号密码为主 + 分屏 + 大厂式品牌图（图由 Codex 生 Q5，图到前用光谱） | 按 D9，候选页仍在 `/login/candidates`、`/login/directions` |
| C2 | 账户池详情 = 右侧 Drawer；全站详情统一抽屉 | D7：行内展开 + 保留「打开完整账户页」 | 账户池按 D7；工作项/任务详情本来就是页 |
| C3 | 账户池顶部 = **九态库存流水线**（契约 v1.5.1 `poolStatus`：可用/已分配/待开户/待充值/待搭建/投放中/暂停/已关/异常）+ `lifecycleStage` 另一字段 | D8：老板口述八档（基建/等待/冷启动/起量/稳定/掉量/待关/已关）混了库存态和生命周期 | **按契约拆成两层**：顶部分层 = 九态 poolStatus（fixture `accounts/pipeline.json`）；生命周期（冷启动/起量/稳定/衰退）= 表里一列 + 筛选。老板的三种表达（分层卡/流程条/看板）保留，作用在九态上。**请老板确认叫法** |
| C4 | 端口 3400 | — | 3401（3400 被占） |
| C5 | `lib/data/` 归后端 | 我 F-006 期间改过 `lib/data/mock-data.ts`（补 21 户）、加了 `lib/data/account-lifecycle.ts` + fixture | 已在 inbox-arch 报备；F-007 起不再动 `lib/data/`，新 mock 全走 `lib/fixtures/`（前端自己的 fixture 读取层，直接 import `@contract/fixtures/*.json`） |

## 底座（F-007 期新增，全站复用）

| # | 项 | 状态 |
|---|---|---|
| B1 | `@contract/*` 路径别名 → `packages/contract/*`；`lib/fixtures/contract.ts`：MetricValue/RatioValue 三态取值（缺数 −）、三色只从 DTO 拿 | ✅ |
| B2 | `components/business/state/page-state.tsx`：八态壳 `StateFrame` + `?state=` 切换（mock 期页头 Select）+ 「示例」角标 `ExampleBlock` | ✅ |
| B3 | `components/business/tabs/page-tabs.tsx`：`?tab=` 视图收敛 | ✅ |
| B4 | 页头常显五件「空间 · 来源 · 数据日期 · 更新时间 · 口径 ⓘ」+ 三色（`system/health.json`；黄/红时整页顶部条）| ✅ |

## 页面（顺序 = 老板精修顺序）

| # | 页 | 壳 | 八态 | fixture 接 | 交互 | commit | 老板精修 |
|---|---|---|---|---|---|---|---|
| 0 | 登录 `/login` | ✅ (F-006) | 401 | session-http | 登录/切空间/退出（mock） | `449ccec` | 等图（Q5） |
| 1 | 数据分析 `/data?tab=` 七 tab（大盘 / 数据总表 / 维度透视 / 盯盘 / Gap / 策略分析 / 自助报表） | ✅ | ✅ `?state=` | table-v3 · summary-window-v3 ×3（页内样例切换）· trend-v3 · dimension-v3 + unsupported · hourly · watchlist · gap · pivot2 · views · config-v1 · render · exports/queued | 窗口预设 · 媒体/账户筛选 · 自定义列 · 存列→个人视图 · 导出→队列 toast · 8 维切换 + 勾选分析 · 盯盘名单增删 · Gap 分组 · 策略预设/自定义两维 + 勾格分析 · 报表四步向导 + 定时推送对话框 | 见下 SHA | 待老板 |
| 2 | 账户池 `/accounts?tab=` + 详情 | ⏳ | | | | | |
| 3 | 工作台 `/`（我的 ⇄ 负责人；今日｜协作） | ⏳ | | | | | |
| 4 | 投放任务 `/tasks` + 详情八页签 | ⏳ | | | | | |
| 5 | 自动化 `/automation` + 画布 + 运行详情 | ⏳ | | | | | |
| 6 | 报告 `/reports` | ⏳ | | | | | |
| 7 | 集成与通知 `/integrations` | ⏳ | | | | | |
| 8 | 知识库 `/knowledge` | ⏳ | | | | | |
| 9 | 商品素材 `/materials` | ⏳ | | | | | |
| 10 | 设置 `/settings` | ⏳ | | | | | |
| 11 | 治理后台 `/admin` | ⏳ | | | | | |
| 12 | 全局 Agent 抽屉 + ⌘K | ⏳ | | | | | |

## TODO-fixture（缺的按 api.md 自造最小 mock，arch 补）

- `data-query/dimension-v3-{task,biz,account,agent_type,deduction_range}.json` —— 只给了资源位一维；前端自造最小 mock（只有维度值，指标全 −），标「示例」
- `data-query/gap-{task,biz}.json` —— 只给了按账户；按任务/业务显 TODO 空态
- `data-query/pivot2-{bid_tool-task,biz-resource_position}.json` —— 只给了版位×任务；出价工具维 UNSUPPORTED，业务×版位显 TODO 空态

## 阻塞

（暂无）
