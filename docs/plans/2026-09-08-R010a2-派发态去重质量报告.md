# P145 派发态去重质量报告

代码 **ba0c400**；交审前`git merge main`已up-to-date（main c3451db）。本人分支候选，未合流/部署/push，不开放媒体写。

## 真实问题与修复

`findActiveAlert`只查open/processing/escalated，遗漏冻结的dispatched。真实PG先建工作项再置dispatched，两个并发同等级信号出现：一次created新open、一次merged到新open，原派发项留在原处。RED日志`output/qa/p145/pg-red.log`，1失败/11通过。

生产修复只改导入+SQL+参数：使用共用`ACTIVE_WORK_ITEM_STATUSES`绑定`status=ANY($5::text[])`，沿用三键+rule_id、advisory事务锁和FOR UPDATE。修后两个信号均merged原ID，仍dispatched且仅一行；跨媒体同号互不吞信号，终态不阻止新的活动项。

测试文件原默认连接共享ka已移除，必须显式local55432 ka_*_test；只清理本文件记录的合成workspace，不扫业务库/其他测试残留。

## 门禁与证据

- DB定向：真实PG12/12 + unit33/33，`db-focus.log`；核心99.05%行/95.08%分支；未覆盖分支为旧null severity拒绝，不称100%。
- DB全量：92 files / **995/995**（unit+真实PG混合套件，不是995项PG），`db-full.log`。
- DB typecheck/lint通过，`db-type.log`/`db-lint.log`。
- Domain未改，沿用相同源P144全量1131及type/lint证据，明确不是本批新跑。
- 没有新增依赖，production离线audit沿用同锁文件P144的0，非本批在线刷新。
- Worker本批完整重跑：142 files pass / **1541 pass+2外部opt-in skip**，worker-full.log，退出0；typecheck/lint均通过。Web无变更，P144 node196为最近证据；未做Next build。

全部只用本人合成库串行PG；无Contract/保护路径/视觉修改。质量检查验证SQL参数绑定与共用枚举，防活动态字面量再次漂移。

## 尚未完成

本次不实现dispatch/escalate HTTP、通知、权限迁移、occurrence_count或superseded_by关旧建新。旧原地severity upgrade仍是已登记债，不作为P083最终验收；013列/表冲突仍等arch。全局数据库partial unique同步也必须随冻结迁移，不能仅凭本repository并发测试宣称任意写入口都不会重复。

整个R010a2及长期信箱目标未完成，继续公开详情/规则与其他未阻塞项，不等待该批审完。
