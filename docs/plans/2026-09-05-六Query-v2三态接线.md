# Canonical Query Rows v2 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** R009 P0-04六Query公开rows真正统一三态，不保留v1 fixtures冒充v2。

**Architecture:** 复用CanonicalMetricValue；普通metric set九项和daily五项变严格对象，RatioValue不改。rowCount/accountCount/anomalyRows为查询元信息，保持原严格计数。两Adapter先解析来源数字，再封装；来源截断需使业务值error而不是仍available。来源失败无行，不捏造账户。Registry KA聚合检查成员字段与预期账户日，不让SUM忽略NULL。

**Tech Stack:** Zod / TypeScript / PostgreSQL与SQLite只读模板 / Vitest /现有canonical JSON fixtures。

在既有独立工作树继续，不启用外部Coze、不上传工程；executing-plans不可用，由当前会话按老板持续目标执行。@writing-plans和@code-quality-checker用于计划与门禁。

1. `packages/domain/test/data-query-rows.test.ts`先补v2对象接受、bare number/不合法availability/旧版本拒绝；跑红灯。
2. 修改`packages/domain/src/data-query-rows.ts`、`apps/worker/src/data/canonical-query-rows.ts`，九业务指标与daily五项封装`metricValue`；分母0 RatioValue不动。调整Domain/Worker canonical fixtures，JSON fixtures三个成功样本升级，stable-error保持。
3. KA `query-registry.ts`聚合SQL检查字段完整性和账户日预期数；truncation在Adapter后置钳制metric为error、ratio undefined；对应真实SQLite/双Adapter parity/非法值/缺数/截断反例。SQL不能收外部列名或自由表达式。
4. 对service恶意adapter补严格rows边界、v1拒绝，原requestId/16MB/身份范围不降低。普通dataView请求移除留到下一绑源子批，不混新入口。
5. backend全量与真实PG、type/lint/coverage；只读检测web编译与fixture兼容。未获arch明确机械适配权限前，不修改apps/web/lib/data；若失败明确记录给arch/fe，不降级v2回v1求绿。
6. 独立代码+留痕SHA，继续空间绑源。尚待端到端frontend适配的不称整批完成，不push/部署/开媒体写。

## 执行记录（2026-09-05）

- 后端代码bdc5273；TDD及门禁详见R009-状态P044-v2。Domain514、Worker最终非PG648+2skip，核心84/行91.85%；DB219早前PG通过、晚间WorkerPG拒连，明确pg_blocked。
- 实际KA SQL采用approved tuple×日期的CTE，SUM不忽略缺失成员；额外内部account_day_count证明多日coverage。CTE/数字异常反例使用Node22内置SQLite，不引新生产依赖。内网网关执行此模板仍待OS证据。
- 新A-001已要求#8同时修改BFF；前端自有v1测试78绿不代表互通，直接解析三份v2成功fixtures均false。下一空间绑源批修非视觉契约/适配；当前前端0diff，不称全链v2已可用。
