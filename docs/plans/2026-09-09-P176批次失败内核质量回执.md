# P176 Task1 批次失败事实内核质量回执

- 代码 **42e98717**（9文件）；分支be/r010，基于400b63e1。**只是内核，未接Runtime/Full/Incr、未改变run成功条件或公开数据，不是F-OS-001③已完成。** 后续按同日P176计划Task2/3接缺数屏蔽再开容错。
- 按v1.9.8 warning形状，Domain严格校验真实日期、显式媒体、唯一账户≤1000与SHA256；私有run batchScope只保存可信范围，batchFailures含DB failedAt。公开warning将剥去私有media/time，不携带qid/错误原文。
- Worker仅把typed RetryExhaustedError归为可记录批次失败；鉴权/业务/截断/限额等不降级。调用边界必须仅包source.query，不能包appendRaw/upsert/lease；当前没有caller启用。
- DB行锁同时锁run/job；同workspace/job/attempt/current lease、父job媒体及账户集合、冻结日期范围均检查。写入前再次检查lease时钟；不可变旧attempt不能被回写。单事务、有界10000事实与PG实际JSONB文本16MiB，重复指纹幂等、异事实同指纹拒绝。失败一律安全固定文本。

## 测试证据

三组先因缺实现文件红，再补实现；真实PG中queued反例初稿触发既有lease CHECK，修测试为同时清租约后重跑。后续TS exactOptional测试错误也已修，不隐藏中间失败。

| 范围 | 最终结果 |
|---|---|
| Domain warning/scope | 18/18，覆盖100%行/100%分支 |
| DB ledger + 原ETL生命周期 | 4文件54/54（23真实PG、31unit）；新模块100%行/96.96%分支 |
| Worker mapper + handler/attempt/protocol回归 | 4文件68/68；新模块100%行/100%分支 |
| 三包typecheck/lint | 均exit0 |
| 三包production offline audit | 缓存0 vulnerabilities，非在线最新advisory |

真实PG限定localhost:55432专用合成库，各测试只清理自己随机workspace，旧int64测试用事务temp table。两种并发warning/重复warning总共只留下两条；真实UPDATE后注入COMMIT失败可回滚；near-write过期租约拒绝；10000可重放但10001拒绝；16MiB等于边界/追加后超边界不写。

```sh
# packages/domain
npx --no-install vitest run test/etl-batch-failure.test.ts --coverage --coverage.include=src/etl-batch-failure.ts
# packages/db；TEST_DATABASE_URL指向专用本机库
npx --no-install vitest run test/etl-batch-failure-repository.test.ts test/etl-batch-failure-repository.unit.test.ts test/etl-run-repository.unit.test.ts test/etl-run-bigint-pg.integration.test.ts --hookTimeout=30000 --coverage --coverage.include=src/etl-batch-failure-repository.ts
# apps/worker
npx --no-install vitest run test/etl-batch-failure.test.ts test/etl-handlers.test.ts test/etl-attempt-scope.test.ts test/qihang-protocol-retry.test.ts --coverage --coverage.include=src/etl/batch-failure.ts
# 三目录分别
npm run typecheck
npm run lint
npm audit --omit=dev --offline
```

## 质量与未完成

- 无新依赖/迁移/契约文件/视觉改动，不push、不接真实源或媒体写。SQL参数化，返回只有recorded布尔；断事务release/坏连接destroy。
- JSON ledger每次有界重写，适用于现有批次数；不能宣称无限历史/任意规模常数复杂度。尚未引入永久分区失效表。
- scope.batchScope未由Full/Incr生成，原旧run缺此字段拒绝；这避免仅合内核就悄悄改变失败处理。**旧Raw/Canonical失效屏蔽、公开warnings与coverage、后续成功解除缺失、Runtime接线均未完成。** 不能向OS称“坏一批也可跑完”。
- 本机磁盘约7.7GiB<8GiB，未跑三包全量；交arch独立门禁。尚未arch合流/OS部署。
- main新增arch P-175库名守卫小修已收到（与本人P175诊断编号同名，后续回执用P177区分）；将独立放宽指定测试库名到ka_*_test，不共用arch实际数据库。
