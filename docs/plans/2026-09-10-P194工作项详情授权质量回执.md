# P194 工作项详情共享授权质量回执

- **代码：3db9e42b**，be/r010，9文件；依据 arch P178 / v1.9.11 / Q027共享helper。
- 本机 implemented + targeted verified；待arch exact-SHA审查，**未合流/内网部署/push**。P192列表可见但任务详情403的已知接缝在本批关闭；不是P178全域审计结束。

## 实现与安全审查

| 要求 | 代码/证据 |
|---|---|
| 与列表同矩阵 | findForRead 使用 workItemScopeClause；账户 tuple、任务关联grant OR本人、team排纯私人 |
| 无权不读正文 | RR/RO首查询只取id/workspace/allowed/内部任务tuple；拒绝前从未查询evidence_snapshot/diagnosis/title |
| 身份/内容一致 | preflight与body同snapshot，两个查询都用共享谓词，双重核验workspace/id；损坏授权类型/身份/证据/正文ID回滚 |
| 后台隔离 | 新HTTP port强制findForRead(ws,id,auth)，旧find(ws,id)仅保留原后台逻辑；不改变transition/告警回收 |
| 内部证据不外泄 | Repository回{record,taskScopeAccount}；Service只投影record且校验proof tuple，不增加任何公开字段 |
| 同号与撤权 | 同accountId跨媒体、跨workspace、只在另一workspace有关联的任务均不越权；Session撤权后下一次详情403 |
| 空/NULL | 无grant只保留本人任务/私人项；SQL allowed=NULL明确COALESCE false，不误报500 |
| 团队 | 任务型/账户型可读；私人永不出；ChangeSet team限制保持原样 |
| 并发 | preflight后另一连接删任务关系，本次依原snapshot完整读取，下次403；不拼两个快照 |
| 负对照 | 测试代理摘掉共享谓词，可真正读到TENCENT错误账户和team私人正文，证明断言不是空测 |

任务链接时间语义照Q027，未擅加valid_from/valid_to过滤。Service能验证授权tuple和记录身份，关联真实性由可信Repository查询保证，不宣称防御任意伪造内部Port。数据库超时沿用withSemanticReadSnapshot（statement15s/lock5s/idle15s）。没有新HTTP入口、媒体执行或外部调用。

## 测试命令与原始摘要

环境：独占合成库 `ka_be_r010_p192_test`；另建 `ka_be_r010_p194_test` 并确认public表数量**0**，新详情套件自主runMigrations后通过。两库均localhost:55432，仅合成数据；未清其他库/队列。P194套件按自身随机workspace清理合成行。

### DB：7个不同文件，共106项

在packages/db，以TEST_DATABASE_URL指向上述独占库：

1. `npm test -- --run test/work-item-read-authority.test.ts test/work-item-list-repository.test.ts test/work-item-list-repository.unit.test.ts test/changeset-read-authority.test.ts` → **51/51**（21+12+9+9）。
2. 新空库：`npm test -- --run test/work-item-read-authority.test.ts test/work-item-repository.test.ts test/work-item-occurrences.test.ts test/work-item-transition.unit.test.ts --coverage --coverage.include=src/work-item-repository.ts --coverage.reportsDirectory=/tmp/ka-p194-db-coverage` → **76/76**（21+12+10+33）。两次重叠新suite21只计一次，独立总数106。
3. 覆盖率：work-item-repository 行 **99.21%**、分支 **96.51%**、函数 **100%**。

### Worker：9文件231项

在apps/worker，相同合成TEST_DATABASE_URL：

`npm test -- --run test/read-detail-service.test.ts test/data-api-http.test.ts test/business-read-session-pg.integration.test.ts test/bootstrap-session-pg.integration.test.ts test/work-item-list-http.test.ts test/task-list-http.test.ts test/account-list-http.test.ts test/work-item-list-service.test.ts test/r010/viewer-readonly-http.test.ts`

→ **231/231**（24/57/4/2/22/17/18/22/65）。真实Session/PG/HTTP6项含登录、个人获授/隐藏任务详情、团队切换、私人拒绝、旧token、撤权、伪造header、退出。原响应大小上限“恰好命中即拒绝”逻辑继续通过（用小配置限额，未在本批宣称实际传输了16MiB正文）。

`npm test -- --run test/read-detail-service.test.ts --coverage --coverage.include=src/data/read-detail-service.ts --coverage.reportsDirectory=/tmp/ka-p194-worker-coverage` →24/24，行 **97.22%**、分支 **91.04%**、函数 **100%**。

### Domain与质量门

- packages/domain：`npm test -- --run test/read-detail-contract.test.ts test/work-item-list-contract.test.ts test/work-item-list-response-fixtures.test.ts test/work-items.test.ts` → **36/36**。
- DB/Worker各自 `npm run typecheck && npm run lint && npm audit --omit=dev --offline` → exit0、缓存漏洞0；不等于实时漏洞库审计。Domain本批未改代码，仅定向契约回归。
- `git diff --check`通过，前端/UI/arch台账/合同0diff；合计不同定向测试**373**，不是全仓测试数。

## 红绿与边界

- 初始新详情suite14项全部因findForRead不存在而红。
- 第一版真实Session发现4项预期403实际500；补真实PG两例复现：无assignee/creator导致SQL三值逻辑NULL。改预检COALESCE为false后DB21、Session/HTTP全通过。没有放宽未授权访问。
- 旧“私人”Service fixture实际遗留taskId，现明确taskId:null；新增独立team任务可见测试，未靠改断言隐藏旧行为。
- 本机余量约6.2GiB，未达8GiB全五包门槛，未执行全仓门禁、无内网部署/真实凭证验证。
- P193在arch明确补裁第三项后17/17通过（d7d659b9/96729c6b），三项仍是到期PENDING不是功能实现。
- **P178其他仓储全域检查仍未完**；rerun fixture UUID/BIGINT待F-P179-Q3；014 dispatches、021/hourly索引、F-OS-004、sop-run、P176仍排队，目标未缩减。

按 code-quality-checker 审查了SQL参数绑定、正文读取时机、同snapshot证据、错误脱敏、旧后台兼容与测试反证；本批未遗留已知P0/P1，跨批缺口按上节保留。
