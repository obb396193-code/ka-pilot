# P133 静音 HTTP 适配质量报告

## 交付与边界

- 代码 `c2527dc`，7个自有文件；已同步main@d6ecab2（merge8b23603），信箱P132/Q008双方追加保留。没有修改共享http-server/data-api/runtime结构，也不注册到be2的r014Routes。
- `createAccountMuteRoutes(AccountMuteService)` 提供`POST /api/v1/accounts/:media/:id/mute`与`POST /api/v1/work-items/:id/ignore`（仅ignore+mute实际执行）。返回P096成功数据，所有错误固定、安全、带requestId。
- 壳层须先internal bearer+Session解析；工厂只消费服务器批准context并再次拒team/空scope/非法context。同账户跨媒体用tuple校验，浏览器x-ka-*不参与鉴权。Repo仍在事务锁内复核活动授权。
- 1MiB请求字节限制；默认16MiB响应等于上限即失败；不可信Service成功先strict校验。超限请求不destroy socket导致客户端收不到413；有限响应错误带Connection:close。
- 没有媒体调用、Job入队、新登录或监听入口。没有将测试里传入的合成auth说成正式Session链路。

## 实测记录

命令与日志位于本worktree `output/qa/p133/`。

| 项目 | 结果 |
|---|---|
| TDD red | 模块尚不存在，测试collection失败（red.log） |
| Worker路由/helper/既有Service | 58/58：32路由、5传输边界、21Service；合main再跑58/58 |
| 真实loopback HTTP | 2/2：200与正确头、超限413且无命令执行/无socket reset |
| HTTP→真实Service→真实PG | 3/3：正确tuple+上海03点截止、原子ignore/mute+重复409、撤权后旧context403 |
| DB静音真实PG回归 | 13/13，含第二步强制失败后工作项回滚、并发和跨workspace/media |
| Domain全量 | 73文件1035/1035；旧search.meta问题已修 |
| 合main后的Web BFF全量 | 176/176 |
| Worker全typecheck/lint | 最终均exit0 |
| 新可执行文件覆盖 | account-mute-routes.ts行100%/分支90.9%；http.ts四项100% |
| coverage汇总（含纯类型routes.ts） | 行86.33%/分支93.58%，纯类型文件无运行语句按0计；未删文件美化总数 |
| Worker production离线audit | 0，缓存结果，不冒充在线安全公告 |
| diff --check/安全扫描 | 无新增凭证、任意SQL、媒体写或原始异常响应；Q008提的Headers对象展开写法在本树lib/data未发现 |

真实PG仅显式本人`127.0.0.1:55432/ka_be_r010_20260907_test`，测试强守卫，单worker，30秒hook/test；只按本套创建UUID清理。无真实凭证/业务数据。

## 尚未完成

- **未注册到主服务，未加BFF，未上线**。请arch把factory接入其共享通用路由缝，不能占用be2数组；服务本身已做live权限检查。
- 纯ignore（无mute_days）契约入参合法，但本适配缺其响应契约/命令实现，所以明确503 SOURCE_UNAVAILABLE；不能误报400或借机造一个mute。
- HTTP发送与数据库提交无法原子化；客户端断开或返回体校验失败不代表先前事务未发生。尤其ignore重放终态409，不声称网络请求“恰好一次”。未加超时Promise.race来制造晚提交。
- RuleScan生产Provider、静音抑制计数、SLA仍见P132；当前手动静音落库并不代表自动扫描已消费。
- 磁盘约4.5～6.8GiB，未跑DB/Worker全量。本批定向PG通过，不复用历史全量证据；仅candidate + local targeted verified，非merged/deployed。

合并记录：同步d6ecab2首次提交被自动检查误认为“仅文档提交夹带其他文件”而拒绝。只读核验MERGE_HEAD==main、暂存Web/R014状态对main零diff、P133已独立提交后，重新申请同一正常merge获批。未绕过检查，未reset/覆盖其他worktree。
