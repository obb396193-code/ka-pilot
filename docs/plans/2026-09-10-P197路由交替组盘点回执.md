# P197 路由有限交替组盘点

arch在main `04e145e4`把materials/review两条暂挂P196；本地P196已用于rerun，解析修复独立编号P197。

## 实现

`a80c378c`：syntax-only AST inventory把`(materials|review)`和`(?:tasks|accounts)`的纯字面分支展开，不再抹成`:p`。动态账户/任务ID仍参数化；最多100组合，超限和嵌套无法解析报unresolved，不执行正则/前端模块。

同步main后移除arch临时加入的materials/review两条PENDING——真实路由已能识别，保留会被“已解决但未删除”守卫报红。没有修改be2生产路由，也没伪称501占位功能已实现。

## 验证与边界

- 新增2个测试先红后绿；解析器7/7，PENDING守卫9/9；真实覆盖2/3。合计**18过1红**，剩余唯一红是新增rerun无BFF，与解析无关。
- 命令：Worker `npm test -- --run test/r010/route-path-inventory.test.ts test/r010/coverage-pending.test.ts test/r010/bff-coverage.test.ts`。
- Worker typecheck/lint通过；P196阶段缓存audit0；无新依赖/网络/前端/真实写改动。用code-quality-checker检查有限展开与失败显式化。
- 路径覆盖不证明方法/DTO/501功能完成；rerun PENDING未经批准，未新增。磁盘<8GiB未五包，未push/部署。
