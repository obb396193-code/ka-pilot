# Quality Reconciliation Missing Values Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** R009 P0-04剩余质量对账不把空源/缺字段补0，也不把无法判断记通过。

**Architecture:** 保持raw/canonical账户三键并集与最新resource选取，SQL先按field_sources选择来源文本，再校验数值；任一成员缺失则该侧总和NULL。内部ReconciliationResult保留nullable总数/差值/容忍值与passed=null，映射既有data_quality_checks.passed可空列，无迁移。仅真实两侧有限总数计算差值，非法已存在值抛受控错误。

**Tech Stack:** PostgreSQL16 / TypeScript / Vitest；独立be/r009工作树，不改前端/Contract，不跑真实上游。

执行：延续老板active goal在当前会话TDD；executing-plans技能不可用，不另开写文件agent。

## Task 1：质量SQL与持久化

- 修改`packages/db/src/data-quality-repository.ts`，提取固定SQL至`data-quality-reconciliation.ts`以控制复杂度；测试`packages/db/test/data-quality-repository.test.ts`。
- 先测试空两侧得到`{rawTotal:null,canonicalTotal:null,delta:null,tolerance:null,passed:null}`，真实零双方得到0+passed=true；现实现会首例失败。
- 再测试单侧缺户/字段、同号跨媒体、NaN/坏字符串、field_sources指定realtime但仅offline存在，不允许串口径兜底。
- `TEST_DATABASE_URL=…/ka_r009_test npm test -- --run test/data-quality-repository.test.ts`，红灯后修改SQL与类型，再绿灯。
- 未观测到的整个workspace账户集合不是本对账的预期集；这里只对raw/canonical三键并集证明对平，完整账户覆盖由独立health/ETL scope检查负责。

## Task 2：Worker回灌门

- `apps/worker/test/data-quality-check.test.ts`参数化passed=false/null，unknown也要recordCheck并让backfill失败，不改媒体写。
- `apps/worker/src/quality/check-handler.ts`只允许`passed===true`通过；普通job仍完成检查过程并发出已有固定告警，不伪称数据质量通过。
- 定向Worker质量/回灌真实PG后，串行DB与Worker全量；Domain/Web回归，typecheck/lint、安全与核心coverage。

## Task 3：交付

- 代码路径限定独立SHA，状态/台账/inbox-arch独立SHA；报告nullable内部值不等于公开v2已完成，下一步六Query v2与绑源继续。
