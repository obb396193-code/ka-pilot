# B10 真实奇航只读适配状态

> 分支：`be/b8a`
>
> 基线：`f756140`
>
> 当前状态：代码与脱敏证据已提交为 `41c6646`，等待 Claude/arch 按 P-017 审查；真实请求由 OS 执行，产品 Worker/FaaS 全链尚未联通。

## 已完成

1. 真实奇航 OS 只读回传逐项对照现有 Client、ETL、Raw 和 Canonical。
2. 请求日期从内部 ISO 严格转换为奇航 `YYYYMMDD`。
3. Full ETL 增加最多 3 日的离线空分区回退并调整 Canonical 日期范围。
4. 历史日离线 `account_real_conversion` 优先于实时补齐。
5. 保留离线动态 Schema，不固定旧文档字段集。
6. 新增日期、回退、边界和字段优先级反例测试。

## 没有做

- 没有从 Codex 本机访问内网；
- 没有部署或修改 FaaS；
- 没有保存真实 userId 或业务数据；
- 没有测试任何媒体写操作；
- 没有推断 `hh`、跨日区间、其它媒体或现金缺失口径；
- 没有修改公开 Contract、Migration、API 或前端。

## 验证

详见 `docs/evidence/B10-真实奇航只读适配报告.md`。本批改动相关 tests、typecheck、lint、audit、复杂度和敏感扫描通过；本机 PostgreSQL 回归因 Docker 引擎未就绪未完成，合并/部署前必须补跑。

## 下一步

1. 内网 OS 第二轮只读验证 `hh`、跨日离线和空/部分分区语义。
2. 在可用 daily 测试库用本项目 SHA 跑 Qihang→Raw→Canonical→质量 trace。
3. Claude/arch 审 P-017，更新 `packages/contract/metrics.md` 中过时的“T+1固定产出”和字段来源描述。
