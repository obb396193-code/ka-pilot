# 自查04｜025账户小时表迁移质量回执

## 范围与状态

代码 **5b5aad4f**。依据 arch api v1.9.4/v1.9.21/v1.9.23；本批只实现冻结表、月分区维护、运行记录分页索引与测试。没有新 job、消费者启动、factory 注入、公开响应改形或媒体写。未合流/部署/OS真数据验证。

使用 writing-plans 拆分红绿测试、code-quality-checker 做 SQL/回滚/隔离/覆盖/依赖审计。未使用子 Agent。

## 代码核对

- `025_account_metrics_hourly.cjs` DDL 与 schema.sql 去注释后的完整 CREATE TABLE 对拍：五键 PK、三键 account FK、hh 0..23、指标 nullable、真实源/采样两个 NOT NULL 时间、complete NOT NULL、source_run_id nullable。
- up 保留三个原父表，维护函数增第四表，初始过去三月至下月；后续现有维护器自动扩未来分区。`etl_runs_workspace_started_id_idx` 按 workspace/started_at DESC/id DESC。
- down 先 ACCESS EXCLUSIVE 锁小时表，检查非空则整笔拒绝，防检查与删除间并发插入；无数据时恢复三表维护函数并删本人表/索引。没有 CASCADE/业务行重写/原始数据清理。
- SQL 动态标识符只来自迁移文件内部固定四表，分区名用 format %I、日期用 %L；不接受浏览器/上游输入，不接真实凭证。
- 原 ad_metrics_hourly、daily、Raw 父表 OID 在 down/up 前后相同。新表缺失指标保留 null，与真实 0 区分。

## 测试原始摘要

运行目录均为本人 worktree：

1. DB `npm test -- --run test/account-hourly-migration.unit.test.ts`：缺迁移文件 **5 failed**，实现后 **5 passed**。
2. DB 专属合成库 `ka_be_r010_p194_test`：
   `npm test -- --run test/account-hourly-migration.test.ts test/account-hourly-migration.unit.test.ts test/account-hourly-read-repository.test.ts --coverage --coverage.include=migrations/025_account_metrics_hourly.cjs --coverage.include=src/partition-maintenance.ts`
   **3 files / 59 passed**，3.91s。
   - 新 migration CJS 行/函数/分支 100%；包含未修改维护器后总体行94.94%、分支61.53%（其旧错误路径未在这组覆盖，不宣称全模块100）。
   - 实际PG孤儿/跨媒体/跨空间同号、0/23/-1/24、五键重复、缺源时间、null/0、非空down拒绝且pgmigrations不变、up/down/up、四表未来分区均过。
3. 新建专属空库 `ka_be_r010_025_replay_0910_test`（CREATE DATABASE 成功才启动测试）：
   `npm test -- --run test/migrations.test.ts test/account-hourly-migration.test.ts`
   **2 files / 6 passed**，3.24s，完整迁移回放本体2.36s。此旧回放测试会重建 public，因此没有使用共享 `/ka` 或原 p194 测试库。
4. DB `npm run typecheck` / `npm run lint` / `npm audit --omit=dev --offline`：退出0，缓存审计0 vulnerabilities。未联网刷新审计，不冒称最新通告已查。
5. Worker `npm test -- --run test/account-hourly-sample.test.ts test/qihang-account-hourly.test.ts test/hourly-public-query.test.ts`：第一次普通沙箱运行 **99 passed / 2 failed**；两条均为 localhost listen EPERM，不改测试逃避；授权重跑 **3 files / 101 passed**，3.61s。含真实本机HTTP+假源的Session/Registry/BFF合同，不是生产OS通路。

## 发现并修复

第一次 DB 联测 **58 pass / 1 fail**：旧 reader「缺表」测试把随机 schema 表 rename 后，search_path 的 public 又找到了025新表。已将该探针用独立 pool 限制 search_path 为随机 schema，不影响业务实现；真PG全组59绿。不是把真实缺表改成返回空数据。

## 发布限制 / 待审

- **开发025在先不等于发布025在先。** 当前024尚不存在；runner `checkOrder:true` 保持。请 arch 将024与025一并发布、按编号执行，否则以后插入024将触发乱序。没有空占位/关排序/改迁移号。
- 维护函数创建所有月子表、索引和down表锁都属于首次/停写维护窗口；小样本秒数不代表线上低锁。必须在服务/worker启动前执行，生产无实测。
- schema.sql 注释仍写021（arch已改编号025）；024联合FK/UNIQUE在api/信箱已批，但schema.sql仍旧单FK。公共Contract由arch修，不越权改。
- 专属新测试库保留供复验；没有删除用户文件/清缓存。磁盘可用约4.6GiB，按arch规则未五包全量/build。
- 小时采样函数/client/reader此前已有；本批仅补正式DDL。还缺持久化采样job与公开装配；首次readiness日期范围问题仍是自查02待裁，P176可选失败记录仍未注入Runtime。
