# P194②变更记录 WIP / 合并冲突现场

## 初始暂停现场（历史快照，已由下方收口记录更新）

- 代码尚未提交，不是可合流交付。HEAD仍eaf1f6a6，正在merge main@2a291f8a。
- 唯一unmerged文件为docs/relay/inbox-codex.md，两边仅在末尾追加不同信件；已按“冲突停手报我”写inbox-arch，未擅自择一边覆盖，未reset/abort。
- 当前工作树 `/Users/aik/Desktop/投放agent/.worktrees/be-r010`，新源文件未stage，A44未完成。已有其他output/review未跟踪文件保留。

## 已实现WIP

- 028预算历史表：最新物理列daily_cap，联合workspace/task FK，非负有限金额，真实版本时间，有记录时禁止有损down。
- 029系数审计：老行created_at保留NULL，新行DEFAULT now()；新增evidence_url，存在真实metadata时禁止有损down。
- Domain v1.9.44 strict id/op/revoke-null/nullable actor、共享URL参数解析；新版真PG→HTTP fixture `settings/change-log-v1944.json`，旧arch参照未替换。
- Repository三源UNION、RR/RO、参数化tuple/live grant范围；旧值按effective_date/created_at/id取前一版，显示按真实at倒序，50+1分页，微秒cursor。
- Worker service + GET /api/v1/settings/change-log已接HTTP composition和data-api实例注入（仅授权hunk）。无新写路由、无be2指标/ETL改动。
- **BFF尚未实现**，不得把这批叫完整P194。

## 实证与失败记录

| 验证 | 结果 |
|---|---|
| 新迁移TDD | 缺迁移8红后8绿；新旧迁移组合13/13绿 |
| 迁移旧回归 | 012测试被新审计保护提前拦住；合成旧系数显式created_at=NULL后仍验证原乘除语义保护，不放松down |
| Domain | 定向38；全量103文件1608绿、typecheck/lint第一轮绿。改为读取实际新fixture后最终全量1608绿，最终TC/lint进程8435尚需收exit |
| DB | 查询定向30/30（真实PG10+unit20）；全量161文件1813绿、typecheck/lint绿；output/p194-history-db-full.log |
| Worker | route/service31/31及typecheck/lint绿；真HTTP/PG4/4绿；第一轮错误bearer预期401而现壳正确403，调整测试并补缺bearer401；未改认证壳逻辑 |
| fixture | output/p194-history-http-r2.log的P194_HISTORY_FIXTURE原始body，合成数据、DB生成id，test明确写入的历史时间，非生产资料 |
| 局部覆盖 | repository语句/行98.63%、函数100%、分支72.13%，unit-only；尚需补跨源分页/错误分支覆盖，不称全面覆盖 |
| 安全静态 | packages/apps diff --check绿；没有新依赖/真实凭证/媒体写；merge marker只在信箱 |

Worker最终全量进程session **35635**已收退出码1：206文件通过、1失败、2外部opt-in跳过；2415用例通过、1失败、2跳过。唯一失败为`r010/bff-coverage.test.ts`：`/api/v1/settings/change-log`尚无BFF，符合上列未完成边界，不豁免。该命令因测试失败未进入后续typecheck/lint，不能声称本轮全量门禁通过。日志output/p194-history-worker-full.log。Domain最终进程8435已收exit0，1608用例与typecheck/lint均绿。

恢复核实：arch已以`9a564eb8`将上一批`eaf1f6a6`合入main，提交明确记录同一信箱冲突，主线信箱保留双方原文。可对照采用arch的已提交解决结果；本地merge仍未完成，当前P194②WIP没有因此合入。恢复检查时main已推进至`2626799d`。

## 暂停时的待办（历史快照）

1. 信箱append-only合并获准后保留两侧原文并完成merge；此后再继续代码。
2. 新迁移预算同日唯一仍沿旧schema，需复核v1.9.44“同日多版本”是否覆盖预算：当前不能同日追加两笔预算。不能把旧schema的约束当最终产品正确性的证据；结合arch回应处理。
3. 老系数缺真实at，当前strict契约未允许at:null，默认三源请求会明确503；价格/预算筛选不受影响。已请求arch冻结未知时间展示，不能把迁移时刻作为历史修改时刻。
4. 全量Worker结果、最终Domain类型/规范检查待收；补更多三源混合cursor/metadata坏值反例及覆盖审查。
5. BFF/route/parity、两笔迁移独立SHA和代码/信箱交付尚未做。Web全门禁按v1.9.46由arch门禁树统一跑，不再本机反复跑缺依赖的Web typecheck。
6. v1.9.46 P193全局identity治理、P198未知重试和新账户筛选已实读，排队续做，不与此批混改。

## 本批已收口交审（2026-09-13）

- 两笔迁移`6fdbce49`、只读HTTP/BFF`79e331ca`；信箱冲突沿arch主线解法解除，并同步main@1e304507。所有本批apps/packages源文件均已跟踪；没有push。
- 同日预算版本限制已按v1.9.44修复，三源混合cursor PG反例通过；BFF已接真实路由，生产data-api→PG→BFF的session/空间切换实证通过。
- 合并后最终Domain1613、DB1832、Worker2443+2外部跳过，三包typecheck/lint绿；BFF10与限定eslint绿。Repository局部分支覆盖98.57%。完整命令与失败过程见同目录`2026-09-13-P194变更记录质量回执.md`。
- **仍未完成**：旧系数at未知如何显示待arch冻结；旧fixture替换策略待arch确认；前端页面需fe把旧fixture/timeline回退切到新GET，不由我修改视觉。因此不是P194全产品完成，也不是已合流/部署。
