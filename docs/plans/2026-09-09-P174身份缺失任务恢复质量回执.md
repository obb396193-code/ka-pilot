# P174：F-OS-002安全恢复

## 交付与范围

- 代码 **06186485**，同步arch main **44937540**后分支HEAD **f83e16fc**。P173命令 **faa2045** 已在本分支。状态 implemented / 本机定向verified；未arch合流、未OS部署，不push。
- 原WorkspaceSyncTickService在approved candidate入队前调用明确恢复方法；当前及历史日期的同workspace/user/media任务都覆盖。不新增浏览器API，不开放媒体写。
- Domain共享approved payload构造，重排保持原businessDate/owner/attempts，初始无scope任务首次按当前显式grant冻结；执行期已有snapshot保持原identity/role/tuples，增加新授权不扩旧任务。初始历史blocked snapshot本来没有identityId，本批只能验证原workspace-local actor与当前唯一active链；没有伪造旧identity证据。
- 初始身份缺失、执行期缺可用身份两条固定原因白名单。team/失活/撤权/identity或role漂移/其他原因/耗尽/非blocked/残留有效lease拒绝。旧incr复用现有loadWorkspaceSyncReadiness，首次full未成功仍不可恢复。
- 锁active personal身份链、账户grant和候选job；1001哨兵、有界16MiB读取及构建批次；两条bulk SQL写状态与audit同事务。5秒锁超时/15秒单语句超时，不宣称整个事务有15秒硬截止。原last_error保留至正常lease清理，审计长期留原原因/attempt/snapshot，不含qid。

## 最终门禁（f83e16fc）

命令均在相应package目录运行，PG显式使用本人 `ka_be_p158_20260909_test` 合成隔离库；没有连接本机演示库或OS。

| 门禁 | 结果 |
|---|---|
| Domain `vitest run test/workspace-sync-scheduler.test.ts test/scheduled-recovery-payload.test.ts` | 8/8 |
| DB `vitest run test/qihang-job-recovery.test.ts test/scheduled-job-replay.test.ts test/job-repository.test.ts test/qihang-identity-seed.test.ts --hookTimeout=30000` | 48/48，全部真实PG |
| Worker `vitest run test/worker-once-identity-recovery-pg.integration.test.ts test/worker-once-retry-pg.integration.test.ts test/workspace-sync-pg.integration.test.ts test/workspace-sync-service.test.ts test/worker-once-supervisor.test.ts --hookTimeout=30000` | 28/28，含7项PG联合用例与实际进程回归 |
| Domain/DB/Worker `npm run typecheck && npm run lint` | 全过 |
| DB/Worker `npm audit --omit=dev --offline` | 0，缓存证据，非在线更新 |
| recovery模块coverage | 100%行、90.9%分支 |
| 全包 | 未跑：空闲6.8–7.8GiB低于arch的8GiB门；不借用以前全量数字 |

DB最终测试16:27:54开始、7.22秒结束，Worker16:28:06开始，无PG测试窗口重叠。旧job-repository测试会清独立库jobs，因此不得与Worker PG在同库并行；该历史测试未在本批改动。

## 红绿及自审

- 原版真实once：昨日blocked→绑定→今日once后，旧job仍blocked/attempt0，真实红。新实现变done/attempt1，原ID/owner/date保留，audit一条，private qid不入存储快照。
- 同日执行期blocked/attempt1，再获授第二户，原once成功done/attempt2，实际上游仅原账户；不以重新enqueue覆盖旧snapshot。
- 自审补首full前置：原WIP恢复incr为1（预期0）真红；接共享readiness后通过，已有full完成才放行。
- 初版导入helper放错包与DB直接import未声明zod导致测试/typecheck失败，已纠正：schema只在Domain，无新依赖。
- 21个恢复PG覆盖跨空间/媒体同号、撤权、无grant、四种inactive、team、role/identity漂移、非法日期/额外私有payload、不同owner、耗尽/其他原因、并发只一条audit、1001job/grant边界。注入audit写失败时实际job UPDATE已执行，随后真实ROLLBACK保留blocked，无半恢复。
- 质量检查未发现本批未修Critical/High；N+1写已换两条bulk。事务锁期间防撤权，commit后到执行前若再撤权仍由P167运行时凭证复核阻断，不宣称取消在途HTTP。
- 同日已经恢复的job之后若再改变授权并重新tick，原enqueue的immutable snapshot冲突规则仍存在；本批只处理恢复当次，不改普通幂等/授权漂移策略。

## 尚未完成的总队列

- F-OS-001③批次失败容忍+missing/readiness闭环；原文body片段改安全摘要尚请arch采纳。
- F-OS-003 supervisor/child安全失败阶段码与OS自查命令；P172仅解决tick前冲突，不替代诊断。
- F-BI-001/002，F-OS-004等待be2密码仓储，021/hourly→Gap→dimension source与P160/P164其他依赖。v1.9.6/1.9.7源/BUC条件已知会，未拿AppCode不自行实施。
- 不生图；无前端、Contract、be2 owner文件差异（同步main变更不冒称本批实现）。
