# P162 跨包 TS 子进程兼容 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 关闭信箱 Node20 裸 node 加载 TypeScript 问题，不依赖 Node22 原生类型剥离。

**Architecture:** 复用 Worker 已安装 tsx，通过 `--import tsx` 加载；动态导入 Web 非 type:module 包时规范取 `m.default ?? m`。仅改测试，不改业务、包依赖或前端 package type。

**Tech Stack:** Node、tsx、Vitest、Web BFF loopback。

---

执行技能未安装，本会话按持续授权自行 TDD。

## Task 1：可重复红灯与修复

- `NODE_OPTIONS=--no-experimental-strip-types npx --no-install vitest run test/r010-command-bff-parity.test.ts --maxWorkers=1`：2红，ERR_UNKNOWN_FILE_EXTENSION；加tsx后仍2红，CJS命名导出undefined；规范导入后2绿。
- 八个测试文件：`apps/worker/test/{data-api-http,hourly-public-query,gap-public-query,pivot-public-query,r010-command-bff-parity,r010-production-composition-pg.integration,work-item-list-http,work-item-list-bff-parity}.test.ts`。
- 变更仅为execFile参数补 `"--import", "tsx"`，脚本加载改 `await import(process.argv[1]).then(m => m.default ?? m)`；其他断言不变。

## Task 2：持久回归与交审

- 新增 `apps/worker/test/web-ts-subprocess-loader.test.ts`：锁定上述11个子进程入口全部显式注册tsx，防CI Node22偶然通过掩盖回退。
- 重跑八文件（其中一文件真实PG），使用已有专用测试库，HTTP/PG局部权限运行；禁用外部smoke，无媒体操作。
- Worker typecheck/lint、git diff --check；独立代码SHA与文档SHA。
- 当前只有Node22.22.2，禁用原生剥离是针对性兼容实证，不冒充真实Node20/沙箱验证；交arch CI复验。
- 当前磁盘<8GiB，不触发全包门禁或新建PG库。
