# F-Q011-1 系数测试隔离质量报告

代码：0ce730f；仅 packages/db/test/coefficient-seed-repository.test.ts。已合 main@6814c05（7af1a46）。状态：定向已验证、待 arch 复验；未合流/部署/push。

## 实证与修复

- 原文件已有随机 workspace + 定向删除，在本机已有数据的独立库原样7/7。不能直接认定旧红由历史系数残留导致。
- 新反例真实PG复现 Promise.all 首错提前结束而兄弟请求仍运行：预期4行实为0。改 allSettled 再传播原错误；最终使用手动释放门替代时间延迟，保证清理前所有请求完成。不吞错、不重试、不串行化并发验证。
- 跨空间例新增第三空间冲突系数历史；两个目标空间各4行、无关历史保持不变。加本机55432/ka_*_test连接守卫；不清整库。
- 生产 repository、事务隔离、授权、系数和重试参数0 diff。

## 门禁

在 packages/db 执行，TEST_DATABASE_URL 使用本后端专用合成库 ka_be_r010_20260907_test：

```text
npm test -- --run test/coefficient-seed-repository.test.ts test/coefficient-seed-repository.unit.test.ts test/seed-coefficients.unit.test.ts --maxWorkers=1 --sequence.shuffle --sequence.seed=152
同上 --sequence.seed=901
npm run typecheck
npm run lint
npm audit --omit=dev --offline --json
```

两轮分别38/38（8真实PG+30unit）；typecheck/lint退出0。离线缓存audit0，不冒充联网最新依赖结论。日志 output/qa/p152/{red,shuffle152,shuffle901}.log、audit.json。红灯原始版本用延迟释放，最终绿版本已换确定性手动门。

安全审查：新增SQL均绑定workspace值，测试生成标识无真实业务数据；清理仅本例创建workspace，失败仍等在途操作。无生产变更，无新增依赖，无新增生产覆盖率承诺。

## 未完成

- 磁盘当轮2.3–3.9GiB，低于双方规定8GiB；不跑全量、不擅自清缓存。arch原全量失败必须补exact复跑，不能据两轮定向宣称根因全部关闭。
- 新到F-P139-1日历P1属于独立批次，不混本提交。
- 老板取消生图R-FE-IMG-003，已停工具脚本；无头像提交，候选不删。
