# P200 任务就绪度授权质量回执

## 交付与已证问题

代码 **ee459216**：`packages/db/src/task-list-sql.ts`只加共享helper import与readiness子查询一条谓词；新增Worker真实PG/HTTP测试。不修改be2的TaskListService、不改Contract或前端。implemented + targeted_verified，待arch终审，不push/部署。

P178继续审计发现：过滤任务可见性、指标和linkedAccountCount已有tuple，但readiness里的`account_task`子查询只限制task/workspace/date。混合授权任务因此读入所有户的balance/unit，构造`unfundedAccounts/unbuiltAccounts`，现有Service直接透传成公开missing提示。

**本机真实HTTP反例**：只获授KUAISHOU/same，任务另有TENCENT/same和KUAISHOU/synthetic-private；原响应列出`synthetic-private 余额不足/无单元`，就绪比例1/3而非授权范围内1。改授TENCENT/same时还错误借用快手余额和unit得1/3，应该0。SQL层、HTTP层、同号换media三条先红后绿。没有使用公司账户或生产数据。

现在readiness枚举在查询余额/unit之前调用`accountScopeClause($3,$4,account_task.media,account_task.account_id)`；team分支仍仅本workspace全量，个人空scope无任务；保留原有效期、RR/RO、coverage=false，不把仅获授范围的就绪度伪装成全任务覆盖。

## 门禁

| 命令范围 | 结果 |
|---|---|
| Worker `test/task-readiness-authority-pg.integration.test.ts` | 新5/5，真实PG+Session+HTTP，含换team后旧token401、伪造头无效、撤权空列表、logout401 |
| Worker `test/business-read-session-pg.integration.test.ts test/task-list-http.test.ts test/task-list-service.test.ts test/r010/bff-coverage.test.ts test/r010/coverage-pending.test.ts` | 43/43 |
| Worker `test/r010/route-path-inventory.test.ts` | 7/7 |
| DB `test/task-list-repository.test.ts test/account-list-repository.test.ts test/account-list-repository.unit.test.ts test/r014/task-list-v151-repository.test.ts test/r014/task-list-dispatched.test.ts` | 31/31 |
| DB、Worker `npm run typecheck` / `npm run lint` / `npm audit --offline --omit=dev` | 通过，缓存audit各0 |

共 **86项不同定向**；DB覆盖率复跑15项不加计。`--coverage.include=src/task-list-sql.ts`行/分支100%，这是SQL字符串模块的JS覆盖，**授权行为依据新增真实PG/HTTP三红→绿，不拿字符串覆盖率替代**。

所有PG命令明确 `TEST_DATABASE_URL=postgres://ka:ka@127.0.0.1:55432/ka_be_r010_p194_test`（仅专用合成库）。新测试只清自己随机workspace/identity，关闭HTTP/连接，无消费者/外网源/媒体写。磁盘最新5.3GiB，未五包全量/build/清理。

采用writing-plans与code-quality-checker：仅参数化共享谓词，无新依赖/宽权限/错误body；现有三键join不改；scope过滤先缩小集合，正常查询次数不增加，不在事务内发网络请求。

## P178审计本轮收口清单（行号为本批快照）

新增真正公开泄漏 **1处**（本批readiness）；P199首读规范 **2处**（不是已证响应泄漏）。以下结构性限权与后台查询不应误报为“漏一个WHERE就越权”，也不为追求文本一致而添加重复扫描：

| 文件/入口 | 分类、证据与处理 |
|---|---|
| `task-list-sql.ts:15/138/208/244` | filtered_tasks需有效授权关系，metrics/items按tuple；readiness是唯一漏项，本批已修。任务级manual overrides只在已可见任务内；totalLinkedAccountCount仅内部coverage，不公开私户ID |
| `account-list-sql.ts:8/111/126/150/159` | filtered_accounts先核ws/media/allowed_scope；动作/工作项/指标/余额全部join同一已授权tuple。旧账户PG8+unit3回归，未发现新增漏 |
| `semantic-query-support.ts`、`semantic-query-metrics.ts`、`semantic-query-repository.ts`、`semantic-query-dimension.ts`、`window-assessment-repository.ts` | 共享buildMetricFilter/accountScopeClause；expected/metric/task/price三键及有效日，窗口输出二次tuple检查。P180/P184已修收敛；本轮semantic负对照3+window5过 |
| `semantic-query-health.ts:119/161`、`platform-health-repository.ts:22/81` | 账户分母/Raw/canonical按scope；platform P182已接共享helper。本轮platform PG7通过 |
| `platform-pivot-sql.ts:7`、`platform-pivot-repository.ts:66/80` | 数据集合本身直接来自已parse的受信tuple列表，不是枚举workspace后少过滤。所有联接同ws/media/id/day，库后验证expected集合，不使用浏览器列表。P195已补失败批次。PG7+unit45过；team明确source unavailable，不落个人fallback |
| `rule-definition-repository.ts:51/65/88`、`rule-evidence-repository.ts:24/33/42` | 内部取证reader，先验证auth与目标tuple，再按ws/rule取定义；task适用性按同tuple/有效期；证据进一步窄到单账户并同RR读pivot。规则定义是workspace资产，不把scope描述当账户指标。PG17+unit38过；Worker src无公开RuleDefinition/RuleEvidence调用 |
| `work-item-list-sql.ts:12`、`work-item-repository.ts:331` | P192/P194已按共享三类workItemScopeClause，详情先授权后正文；本轮详情PG21过。旧find/transition只保留内部路径，HTTP强制findForRead |
| `changeset-repository.ts:260`、`changeset-dry-run.ts:28`、`apps/worker/src/changesets/dry-run-service.ts` | P183详情/P198两次预检均强制批准auth；内部run按已核父对象锁+hash。详情PG9过；P198并发换media等PG证据详其回执 |
| `account-mute-repository.ts:139/185`、`work-item-command-repository.ts:25/35` | P199首读共享tuple；既有find/set显式目标先validate再live grant锁；不扩写权，209项证据见P199 |
| `changeset-follow-up.ts`、`changeset-execution-queue.ts`、`changeset-reconciliation.ts` | 后台父对象/执行run/ws/原owner绑定，不套页面三类矩阵。P198实读与原PG/单测回归证据；无真实执行上线声明 |
| `metrics-repository.ts:127/198`、`ad-hourly-metrics-repository.ts:14` | 前者内部ETL按ws+输入tuple查配置/历史；后者只有写无读出口，冲突tuple拒绝。不是Session页面，021小时链另批 |
| `data-quality-repository.ts`、`data-quality-reconciliation.ts`、`quality/check-handler.ts:36` | 后台工作区质量维护：先job.ws===payload.ws再读写，canonical/raw三键。不是普通成员统计源；P198真实PG13已回归 |

另有两项**保持边界，不冒称已经产品可用**：`semantic-query-health.ts:186/207` ETL/quality汇总仅ws/date，当前Worker没有queryHealth公开调用，后续若公开须单独冻结授权；`r014/external-change-*`等归be2，依赖其审计与arch终审，不改其代码。

上述透视/规则/详情/健康的11文件 **152/152** 在本批SQL修前已对同步后的主线实际复跑；与86是不同范围但不合成一个“全项目”数字。P178本轮可交审，shared predicate已用于所有这几轮实际修复的漏项；结构性作用域/后台例外如arch要求进一步形式统一再按派单收敛。014/021等功能依赖继续，不因等待终审停工。

## 合流提示

同步main为 **a5c943fa**（父main d5a01bfd）；只解决PENDING同尾冲突：保留arch新增rerun授权暂挂，不增加自批豁免。当前BFF库存3/3过，pending9/9过。新的main 5b65800f已出现，下一批同步后如已接BFF须删除相应resolved暂挂，不能把这次绿永久沿用。
