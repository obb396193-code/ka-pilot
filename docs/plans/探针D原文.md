# 探针 D 原文（发给 OS，存档）

> 2026-08-13 生成。探针 B 已由 OS 实测回答（见 `docs/08-平台能力实证.md`），探针 C 的 FaaS/存储/登录部分已被 B 的回答覆盖。
> **D 只问一件核心事 + 三件小事。第 1 组决定整个数据架构，请优先。**

```
上一轮回答很有用，尤其是你明确区分"查到的"和"查不到的"——那几条"查不到"对我的价值不比查到的低，我按未证实处理，没拿去做架构假设。你提醒的"不要假设 FBI 应用身份通路存在再去设计架构"我已经写进项目红线了。

已经清楚、不用再答的：FaaS 能托常驻 Node 服务和 Next.js SSR、支持 chunked 流式、gateway.paths 多路由、三环境内网域名 + fs.alibaba-inc.com 绑外网域名、BUC 登录走 scaffold-id 10 + NextAuth.js（含 @ali/xy-jose 那个坑）、RDS/Redis 走 Normandy IaC、publish + cd-pipeline 发布链路、create aone-platform 要已有 Code group 权限。

现在只剩下面几组。第一组是最关键的，请优先，其余答不了也没关系。

一、conv_real 到底从哪来（★这一个答案决定我的整个数据架构）
背景：我们自己做的那个钉钉网关，在生产里展示过"真实转化"和"真实 CPA"，字段名叫 conv_real，和账面口径的 conv_report 是两个不同的字段。我现在必须搞清这个 conv_real 的来源，因为你上轮已经证实 FBI/DataWorks 的现有路径都是个人凭证驱动的。

1. 把 ks-data-queryer 这个 skill 的 SKILL.md 全文贴出来。
2. 把它 scripts/ 和 connectors/ 目录下每个文件的内容贴出来（凭证值打码，但请保留变量名、域名、接口路径、字段名）。
3. 明确回答：ks-data-queryer 返回的字段里，有没有一个是"真实转化/入库转化/深度转化"这类严格口径的转化数？字段名叫什么？
4. 这个字段的数据来源是哪里——快手媒体侧本身就返回的（比如深度转化回传），还是这个 skill 另外去调了 dataworks/FBI/内部数仓拼出来的？
5. 如果是另外调的，调的是什么、用的什么凭证（个人 token 还是别的）？
6. 除了 ks-data-queryer，广告类 skill 还有哪些？各自 SKILL.md 的 description 一句话列出来，我想知道有没有别的取数入口。

二、这个 skill 的数据能力边界
7. ks-data-queryer 支持哪些查询维度（账户/计划/单元/创意）、时间粒度（日/小时）、历史能回溯几天？
8. 单次查询的账户数量上限、条数上限、有没有频控？
9. 返回的完整字段清单（消耗、各类转化、CTR、CVR、CPM、预算、余额、账户状态、考核价……）字段名请照抄原样。
10. 数据延迟多久（比如小时级数据滞后多久可用）？

三、账户归属关系
11. "哪个优化师负责哪些快手广告账户"这个归属关系，在哪个系统里能查到？启航？有没有 skill 或接口能查？还是只能人工维护映射表？

四、daily 环境常驻稳定性（我要在进程里跑定时任务）
12. FaaS daily 环境的实例会不会自动回收、休眠、或长时间无请求后被释放？如果会，间隔大概多久？
（我要判断能不能在常驻 Node 进程里用 node-cron 跑夜间定时任务，还是必须接 ScheduerX。你上轮说 ScheduerX 脚手架是 ginkgo-java8，是 Java，跟我的 Node 服务不匹配，所以我更希望进程内解决。）

请优先第一组的 1~5。如果 SKILL.md 或脚本内容因为敏感不能贴全，就只告诉我第 3、4、5 题的结论也行——我需要知道的核心是：真实转化这个数，究竟能不能在不依赖任何人个人凭证的前提下拿到。
```

## 回收（待贴）
