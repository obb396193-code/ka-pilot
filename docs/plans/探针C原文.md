# 探针 C 原文（发给 OS，存档）

> 2026-08-13 生成，**取代探针 B**——B 里一半问题已由我们自建钉钉网关的架构文档实证回答（见 `docs/07-已有网关资产.md`）。
> C 只问剩余未知，且把已知信息前置，避免 OS 重复解释。

```
先同步我已经掌握的信息，避免你重复解释：

我们自己已经做过一个部署在 a1 faas 上的服务（钉钉群机器人网关）：custom.debian10 自定义运行时、bootstrap 里跑常驻 Koa 服务、用 bin/multica CLI 配 mcn_ 令牌建 issue 并 @agent、每 8 秒轮询评论拿结果、再用 vendor 里的无头 Chromium 把 HTML 模板渲染成 PNG 发钉钉。这套跑通过、部署过、踩过 18 个坑。所以 FaaS 能跑常驻服务、能跑 Chromium、config vars 怎么配、mcn_/mul_/mat_ 三种令牌的区别，我都清楚了，不用再讲。

现在我要做的是产品级的下一步：一个多用户的投放提效 Web 应用（有状态、有工作台、有任务分发、四种角色视图），后端仍走 FaaS。以下都是我还没答案的问题，请**实际跑命令或查文档**再回答，贴命令和原始输出（凭证打码保留字段名）。查不到就直接说查不到。

一、绕开 agent 直连数据（最重要）
现有网关的取数是"建 issue → @agent → agent 调 skill → 轮询评论"，一问一答、每次几十秒到几分钟。产品要批量拉几十个账户的多日数据、定时跑诊断，这个链路撑不住，而且我不希望让 agent 做指标计算（已经踩过 agent 把真实 CPA 和回传 CPA 算成同一个值的坑）。所以：
1. ks-data-queryer 这个 skill 底层实际调什么？把它的 SKILL.md 全文、scripts/ 和 connectors/ 里每个文件内容贴出来（凭证值打码但保留变量名、域名、接口路径）。
2. 它调的是 kuaishou-cli 二进制、HTTP 接口，还是内部数据服务？给服务名、接口地址形态。
3. 我的 FaaS 服务能不能用自己的身份直连同一个底层服务／接口，不经过 agent？需要申请什么、找谁。
4. 如果必须经过 agent：有没有比"建 issue + 轮询评论"更适合程序化调用的方式？比如同步 API、批量接口、或者让 agent 把结果写到某个我能直接读的地方。
5. kuaishou-cli 的只读子命令清单、报表类命令的完整字段名列表、支持的维度拆分、时间粒度、历史回溯上限、单次条数上限与频控。
6. conv_real（真实转化）和 conv_report（回传转化）这两个字段的准确口径定义是什么？数据延迟多久？能回溯几天？能不能按小时／按计划／按单元拆分？还有哪些字段（考核价、预算、余额、状态）？

二、数据存哪（当前最大空白）
现有网关是纯内存态，产品必须持久化（工作项、状态流转、指标历史快照、效果回收）。
7. FaaS 应用能申请什么数据库？RDS/MySQL、PolarDB、Tair/Redis、表格存储 OTS？申请流程、找谁、多久、要不要成本中心。
8. 有没有不用等审批就能先用的轻量持久化方案（平台自带 KV、或挂载持久盘）？FaaS 实例重启后本地文件还在吗？
9. 有没有对象存储可用（存导出报表、渲染好的图）？
10. 数据库连接凭证走 a1 faas config vars 就够了吗，还是有专门的密钥管理服务？

三、多用户与组织关系
现有网关是钉钉机器人，没有登录概念。Web 应用需要。
11. FaaS 上的 Web 应用怎么接公司统一登录（BUC／域账号 SSO）？有现成中间件或 SDK 吗，给文档地址或示例。
12. 登录后能拿到用户什么信息——工号、姓名、部门、**汇报关系（谁是谁的上级）**？有没有组织架构查询接口？我要用它做数据可见范围和任务分发。
13. 快手广告账户的归属关系（哪个优化师管哪些账户）在哪个系统里能查到？启航？还是只能人工维护一张映射表？
14. 有没有现成的权限／角色管理服务，还是要自己在应用里实现。

四、生产环境
15. a1 faas deploy 只发 daily。要发到能给部门同事长期稳定使用的环境，完整路径是什么？需要什么权限、什么审批、有没有安全评审或发布窗口？
16. 建 aone-platform 应用（新建代码仓库 + 应用登记）需要什么前置——所属团队、成本中心、负责人审批？会不会需要我主管或技术部门同意？
17. 先在 daily 环境给自己和几个同事试用，流程上合规吗？有人数或时长限制吗？
18. daily 环境的稳定性如何——会不会自动回收、休眠、限流？

五、别人做到哪一步
19. 这套体系里现在有没有已经做成看板／报表／日报形态的产品？谁做的、覆盖哪些渠道、业务侧能不能直接用？
20. 有没有已经在跑的自动化诊断或自动调控（定时预警、自动改价）？规则是什么、谁在用、效果如何？
21. 除了我们的钉钉网关，还有哪些业务侧（非技术部门）自建的应用在跑？做的什么、怎么拿到数据权限的？

请按编号回答。最需要的四个答案是：3、6、7、12。
```

## 回收（待贴）
