# P186 拉数记录分页 Implementation Plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.
> 本会话未提供该子技能，按老板持续开工授权在独立 worktree 直接执行，不另派实现 Agent。

**Goal:** 按 arch `eb4fd37b` / v1.9.12 完成 F-P179 的严格分页契约，再接真实只读仓储与 HTTP；重跑单列后续提交。

**Architecture:** 复用 ApprovedWorkspaceAuthContext、RR/RO snapshot、etl_runs 与 attempt execution 快照。未知旧次数不得用 jobs.attempts 回填；各阶段行数只来自本 run。分页总数/最新完成时间/页行同一 snapshot；SQL 只投影公开信息，不拉 error_summary/凭证/整个 scope。管理员入口，不扩大普通用户授权。

**Tech Stack:** TypeScript、Zod、PostgreSQL、Vitest。

### Task 1：冻结响应与请求

Files: `packages/domain/src/etl-run-list.ts`、`packages/domain/test/etl-run-list.test.ts`、`packages/contract/fixtures/system/etl-runs.json`。

1. 测试先升级 fixture 的 page/pageSize/total；新增新 arch fixture 原形对拍、nullable attempt + LEGACY_NO_ATTEMPT、单阶段 null、边界页及未知参数拒绝。
2. 执行 `npm --prefix packages/domain test -- run test/etl-run-list.test.ts`，确认旧模型红。
3. 实现 `etlRunListRequestSchema`（默认 1/50，上限200、safe offset）、strict pagination response 与 legacy warning。只 absent/null 表示未知，非法类型继续拒绝。
4. 重跑测试及 Domain typecheck/lint/coverage、安全审计，路径限定独立提交。

### Task 2：只读仓储

Files: 新增 `packages/db/src/etl-run-list-repository.ts`、对应 `test/etl-run-list-repository.test.ts`；按必要范围导出。

1. 真 PG 假数据覆盖 workspace隔离、非admin拒绝、稳定翻页/超末页、未知旧attempt、阶段行数、batch warning无私密字段、同snapshot并发插入。
2. 先红，后实现参数化 SQL + strict output mapper；按 run_kind 映射，不跨run拼接。RR/RO、有限LIMIT，排序 started_at DESC,id DESC。
3. 真PG +定向回归 + DB/Domain/Worker type/lint，独立提交与留痕。

### Task 3：Service/HTTP 与重跑

在前两步通过后接管理员只读 GET：server session auth、requestId、exact16MB、固定安全错误与 fixture parity；POST rerun 另一个独立实现提交，保留 owner、runId幂等、timeline，先检查既有队列/事务接口，不启动真实执行。

### 当前门禁/依赖

- 主线按 arch；P178 工作项矩阵等 be2 Q027 helper 合 main，不能复制一个替代版本。
- 当前磁盘7.4GiB，未满足arch8GiB整包门，先跑定向；不称全包通过。
- merge `c67e7050` 中前端/be2文件是已审main带入，非本人新增修改；自己的实现不改前端、媒体写保持关闭、不push。
