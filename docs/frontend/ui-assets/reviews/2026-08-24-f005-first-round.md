# F-005 第一轮：Neutral 工作台与页面框架

> 状态：老板已拍板 Neutral；允许先用现有组件完成功能与页面框架。
> 实施位置：`codex/fe-vertical-slice` 隔离 worktree。
> 保护边界：Desktop `fe/f001` 仅做只读审计，未修改、未 reset、未覆盖。

## 1. live 基线审计

### 可保留

- Next.js 15、React 19、Tailwind 4 与 shadcn New York 基座。
- 官方 Sidebar/Inset/Card/Table/Drawer/Sheet/Tabs/Input/Badge/Button 等 runtime primitives。
- 1440 内容容器、侧栏形态、九个业务目录壳和部分中文业务文案。
- 17 库、7,056 条资产、离线展厅、规范实验室、侧栏/顶栏/KPI 对比 Demo；本轮未重复制作。

### 仍是 demo 或未完成

- Desktop live 首页仍为 `Total Revenue / Total Visitors / Eddie Lake` 原始 demo。
- `NavDocuments` 仍有 `Documents/Open/Share/Delete/More`；用户菜单有“计费”。
- Storybook、ECharts、主题 bridge、产品级 Playwright、正式 API Adapter 尚未落 Desktop live。
- live `NavMain` 没有把 `url` 绑定到链接，导航视觉有文案但实际不可跳转。

### 口径与契约 P0

- `/analytics` 前端计算 Gap/达标；`/campaigns` 前端计算达标率/pacing。
- `/accounts` 零消耗账户显示 `¥0.00 + 达标`。
- 冻结路由是 `/tasks /data /materials`，live 未提交树改成 `/campaigns /analytics /products`；待老板/契约裁决，本视觉批次未改 Desktop 路由。
- Desktop live 没有 `packages/domain`，只有 `packages/contract`；不能把旧状态文件里的 domain 完成声明当 live 事实。

## 2. 本轮视觉样板

首轮候选曾使用同一 React 页面比较 Neutral/专业蓝/青绿经营；老板已选择 Neutral。正式界面已移除蓝/青绿查询开关，只保留 Neutral semantic tokens。

| 当前正式方向 | 本地入口 | 1440 截图 |
|---|---|---|
| Neutral | `/?data_view=ka_data` | `output/playwright/f005-workbench/1440/neutral-final.png` |

补充证据：1366 stale/禁写态、390 青绿移动端均已截图；390 初验发现 ECharts 撑宽到 656，修复后 `scrollWidth=clientWidth=390`。

## 3. 已实现的 F-005 工作台结构

- 数据血缘：来源、截止时间、版本、覆盖、截断、部分/过期和 warning。
- 六 KPI：消耗、真实 CPA、达标率、成本空间、BI 量级、待处理；均为 API 展示字段。
- ECharts“消耗与真实 CPA”；真实 CPA 缺失点为 `null`，图表断点，不补 0。
- 今日队列四件套、其余 15 户阈值内、忽略原因 chip、静音 3 天。
- 变更预览两步确认；最终只标“未调用媒体写接口”。
- T+1 回收、我的待办、AI 早报、P0/P1 监控。
- ⌘K：对象搜索与 AI 对话入口分态；Escape 关闭。正式对话不仿写，等待复用 AI Elements 官方源码。
- 七态通过 query 参数真实渲染：`loading/empty/error/no-access/partial/stale/success`。

## 4. 组件与来源

- shadcn runtime：复用仓内已有 primitives；本轮没有重新下载或改写上游组件。历史审计中 exact upstream ref/hash 未验证，继续标 inherited provenance gap。
- ECharts `6.1.0`，Apache-2.0；新增运行依赖，按需注册 Line/Grid/Legend/Tooltip/Aria/Canvas。
- 头像：复用 `apps/ui-layout-demo/public/avatars/shadcn-morty-official.jpg`，SHA-256 `c080d627acdedca5ba0b7d8e30ddf4ddcc15aa56c896f67037c273233cde18c3`。
- Tremor KPI B/C 未静默选择；当前用 shadcn A 骨架隔离颜色变量。
- coss/ReUI/Dice/AI Elements 未新增运行源码；第一轮没有重复调研或重新造 Demo。

## 5. 测试

- Node test：16/16。
- TypeScript：通过。
- ESLint：通过。
- Next production build：通过，`/` 为动态 SSR，189 kB route、367 kB First Load JS。
- Playwright CLI：1440/1366/390；七态；⌘K/AI tab/Escape；预览两步确认；stale 禁写；console error/warning 0。
- 390：最终无横向溢出。

## 6. 质量问题

### P0

- Desktop live 的路由冲突、前端算口径和零 CPA 误导仍未修；本轮按保护现场要求未触碰。
- `npm audit --omit=dev` 报 3 个 high，位于 Next 15.5.23 的 PostCSS/sharp 依赖；自动修复要求破坏性升级 Next 16.3.2。部署前必须升级验证或形成明确风险处置，不能 `--force` 静默处理。

### P1

- 老板已允许先用当前 shadcn KPI 结构完成功能；Tremor B/C 不再是本批阻塞。
- dark 与已发布 preset 尚未形成正式全站主题；第一轮只做页面级 token。
- AI Elements 正式 conversation/message/tool/sources 尚未 vendored，当前只有入口壳。
- 没有独立 Storybook；七态通过实际页面 query 验收，后续应迁入 Storybook/等价隔离页。
- 首页 ECharts 增加首屏体积，后续需按路由 lazy load 和性能预算复核。

## 7. 已拍板与后续边界

1. Neutral 为当前正式方向。
2. 先复用现成 shadcn 基础组件完成可用功能，KPI 后续可替换。
3. 侧栏保持正式基线；本批不另做顶栏对比。
