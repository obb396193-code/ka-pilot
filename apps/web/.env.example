# 默认使用仓内脱敏 mock；内网联调时改为 internal_api。
KA_DATA_PROVIDER=mock

# 完整 Query Registry 入口。服务端接收 { queryId, dataView, state?, params? }。
# 返回信封必须包含 state、lineage、data；严格字段见 lib/data/contracts.ts。
KA_DATA_API_URL=

# 仅服务端读取，不得改成 NEXT_PUBLIC_*。
KA_DATA_API_TOKEN=
